# api documentation for  [sequelize (v3.30.3)](https://github.com/sequelize/sequelize#readme)  [![travis-ci.org build-status](https://api.travis-ci.org/npmdoc/node-npmdoc-sequelize.svg)](https://travis-ci.org/npmdoc/node-npmdoc-sequelize)
#### Multi dialect ORM for Node.JS/io.js

[![NPM](https://nodei.co/npm/sequelize.png?downloads=true)](https://www.npmjs.com/package/sequelize)

[![apidoc](https://npmdoc.github.io/node-npmdoc-sequelize/build/screen-capture.buildNpmdoc.browser._2Fhome_2Ftravis_2Fbuild_2Fnpmdoc_2Fnode-npmdoc-sequelize_2Ftmp_2Fbuild_2Fapidoc.html.png)](https://npmdoc.github.io/node-npmdoc-sequelize/build..beta..travis-ci.org/apidoc.html)

![package-listing](https://npmdoc.github.io/node-npmdoc-sequelize/build/screen-capture.npmPackageListing.svg)



# package.json

```json

{
    "author": {
        "name": "Sascha Depold",
        "email": "sascha@depold.com"
    },
    "bugs": {
        "url": "https://github.com/sequelize/sequelize/issues"
    },
    "contributors": [
        {
            "name": "Sascha Depold",
            "email": "sascha@depold.com"
        },
        {
            "name": "Jan Aagaard Meier",
            "email": "janzeh@gmail.com,jmei@itu.dk"
        },
        {
            "name": "Daniel Durante",
            "email": "me@danieldurante.com"
        },
        {
            "name": "Mick Hansen",
            "email": "mick.kasper.hansen@gmail.com"
        }
    ],
    "dependencies": {
        "bluebird": "^3.3.4",
        "depd": "^1.1.0",
        "dottie": "^1.0.0",
        "generic-pool": "2.4.2",
        "inflection": "^1.6.0",
        "lodash": "4.12.0",
        "moment": "^2.13.0",
        "moment-timezone": "^0.5.4",
        "retry-as-promised": "^2.0.0",
        "semver": "^5.0.1",
        "shimmer": "1.1.0",
        "terraformer-wkt-parser": "^1.1.0",
        "toposort-class": "^1.0.1",
        "uuid": "^3.0.0",
        "validator": "^5.2.0",
        "wkx": "0.2.0"
    },
    "description": "Multi dialect ORM for Node.JS/io.js",
    "devDependencies": {
        "chai": "^3.5.0",
        "chai-as-promised": "^5.1.0",
        "chai-datetime": "^1.4.0",
        "chai-spies": "^0.7.0",
        "commander": "^2.6.0",
        "continuation-local-storage": "^3.1.4",
        "cross-env": "^2.0.1",
        "dox": "~0.8.0",
        "git": "^0.1.5",
        "hints": "^0.2.0",
        "istanbul": "^0.4.3",
        "jshint": "^2.9.2",
        "lcov-result-merger": "^1.2.0",
        "mocha": "^2.4.2",
        "mysql": "~2.10.1",
        "pg": "^4.4.6",
        "pg-hstore": "^2.3.1",
        "pg-native": "^1.8.0",
        "pg-types": "^1.11.0",
        "rimraf": "^2.5.4",
        "sinon": "^1.17.4",
        "sinon-chai": "^2.8.0",
        "sqlite3": "^3.1.4",
        "tedious": "^1.13.2",
        "uuid-validate": "^0.0.2",
        "watchr": "~2.4.11"
    },
    "directories": {},
    "dist": {
        "shasum": "f9dc9b7c073bc0a0360dd5bae64a516f736f21ed",
        "tarball": "https://registry.npmjs.org/sequelize/-/sequelize-3.30.3.tgz"
    },
    "engines": {
        "node": ">=0.6.21"
    },
    "gitHead": "5e3ebed9fb3e3d319f4527b7e7c9c999e8478a74",
    "homepage": "https://github.com/sequelize/sequelize#readme",
    "keywords": [
        "mysql",
        "sqlite",
        "postgresql",
        "postgres",
        "mssql",
        "orm",
        "nodejs",
        "object relational mapper"
    ],
    "license": "MIT",
    "main": "index",
    "maintainers": [
        {
            "name": "sdepold",
            "email": "sascha@depold.com"
        },
        {
            "name": "durango",
            "email": "me@danieldurante.com"
        },
        {
            "name": "mickhansen",
            "email": "mick.kasper.hansen@gmail.com"
        },
        {
            "name": "janaameier",
            "email": "janzeh@gmail.com"
        }
    ],
    "name": "sequelize",
    "optionalDependencies": {},
    "readme": "ERROR: No README data found!",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/sequelize/sequelize.git"
    },
    "scripts": {
        "build-docker": "docker-compose build",
        "cover": "rimraf coverage && npm run teaser && npm run cover-unit && npm run cover-integration && npm run merge-coverage",
        "cover-integration": "cross-env COVERAGE=true node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -t 60000 --ui tdd \"test/integration/**/*.test.js\" && node -e \"require('fs').renameSync('coverage/lcov.info', 'coverage/integration.info')\"",
        "cover-unit": "cross-env COVERAGE=true node_modules/.bin/istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -t 30000 --ui tdd \"test/unit/**/*.test.js\" && node -e \"require('fs').renameSync('coverage/lcov.info', 'coverage/unit.info')\"",
        "docs": "node docs/docs-generator.js",
        "jshint": "jshint lib test",
        "merge-coverage": "lcov-result-merger \"coverage/*.info\" \"coverage/lcov.info\"",
        "sscce": "docker-compose run sequelize /bin/sh -c \"node sscce.js\"",
        "sscce-mysql": "cross-env DIALECT=mysql npm run sscce",
        "sscce-postgres": "cross-env DIALECT=postgres npm run sscce",
        "sscce-sqlite": "cross-env DIALECT=sqlite npm run sscce",
        "teaser": "echo ''; node -pe \"Array(20 + process.env.DIALECT.length + 3).join('#')\"; echo '# Running tests for '$DIALECT' #'; node -pe \"Array(20 + process.env.DIALECT.length + 3).join('#')\";echo '';",
        "test": "npm run jshint && npm run teaser && npm run test-unit && npm run test-integration",
        "test-all": "npm run test-mysql && npm run test-sqlite && npm run test-postgres && npm run test-postgres-native && npm run test-mariadb && npm run test-mssql",
        "test-docker": "docker-compose run sequelize /bin/sh -c \"npm run test-all\"",
        "test-docker-integration": "docker-compose run sequelize /bin/sh -c \"npm run test-integration-all\"",
        "test-docker-unit": "docker-compose run sequelize /bin/sh -c \"npm run test-unit-all\"",
        "test-integration": "mocha --globals setImmediate,clearImmediate --ui tdd --check-leaks --colors -t 30000 --reporter spec \"test/integration/**/*.test.js\"",
        "test-integration-all": "npm run test-integration-mysql && npm run test-integration-postgres && npm run test-integration-postgres-native && npm run test-integration-mssql && npm run test-integration-sqlite && npm run test-integration-mariadb",
        "test-integration-mariadb": "cross-env DIALECT=mariadb npm run test-integration",
        "test-integration-mssql": "cross-env DIALECT=mssql npm run test-integration",
        "test-integration-mysql": "cross-env DIALECT=mysql npm run test-integration",
        "test-integration-postgres": "cross-env DIALECT=postgres npm run test-integration",
        "test-integration-postgres-native": "cross-env DIALECT=postgres-native npm run test-integration",
        "test-integration-sqlite": "cross-env DIALECT=sqlite npm run test-integration",
        "test-mariadb": "cross-env DIALECT=mariadb npm test",
        "test-mssql": "cross-env DIALECT=mssql npm test",
        "test-mysql": "cross-env DIALECT=mysql npm test",
        "test-pgsql": "npm run test-postgres",
        "test-postgres": "cross-env DIALECT=postgres npm test",
        "test-postgres-native": "cross-env DIALECT=postgres-native npm test",
        "test-postgresn": "npm run test-postgres-native",
        "test-sqlite": "cross-env DIALECT=sqlite npm test",
        "test-unit": "mocha --globals setImmediate,clearImmediate --ui tdd --check-leaks --colors -t 30000 --reporter spec \"test/unit/**/*.js\"",
        "test-unit-all": "npm run test-unit-mysql && npm run test-unit-postgres && npm run test-unit-postgres-native && npm run test-unit-mssql && npm run test-unit-sqlite && npm run test-unit-mariadb",
        "test-unit-mariadb": "cross-env DIALECT=mariadb npm run test-unit",
        "test-unit-mssql": "cross-env DIALECT=mssql npm run test-unit",
        "test-unit-mysql": "cross-env DIALECT=mysql npm run test-unit",
        "test-unit-postgres": "cross-env DIALECT=postgres npm run test-unit",
        "test-unit-postgres-native": "cross-env DIALECT=postgres-native npm run test-unit",
        "test-unit-sqlite": "cross-env DIALECT=sqlite npm run test-unit"
    },
    "version": "3.30.3"
}
```



# <a name="apidoc.tableOfContents"></a>[table of contents](#apidoc.tableOfContents)

#### [module sequelize](#apidoc.module.sequelize)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ABSTRACT (options)](#apidoc.element.sequelize.ABSTRACT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ARRAY (type)](#apidoc.element.sequelize.ARRAY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>AccessDeniedError (parent)](#apidoc.element.sequelize.AccessDeniedError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Association ()](#apidoc.element.sequelize.Association)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Association.BelongsTo (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Association.BelongsToMany (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Association.HasMany (source, target, options)](#apidoc.element.sequelize.Association.HasMany)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Association.HasOne (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>BIGINT (length)](#apidoc.element.sequelize.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>BLOB (length)](#apidoc.element.sequelize.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>BOOLEAN ()](#apidoc.element.sequelize.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>CHAR (length, binary)](#apidoc.element.sequelize.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ConnectionError (parent)](#apidoc.element.sequelize.ConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ConnectionRefusedError (parent)](#apidoc.element.sequelize.ConnectionRefusedError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ConnectionTimedOutError (parent)](#apidoc.element.sequelize.ConnectionTimedOutError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DATE (length)](#apidoc.element.sequelize.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DATEONLY ()](#apidoc.element.sequelize.DATEONLY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DECIMAL (precision, scale)](#apidoc.element.sequelize.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DOUBLE (length, decimals)](#apidoc.element.sequelize.DOUBLE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DatabaseError (parent)](#apidoc.element.sequelize.DatabaseError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.INITIALLY_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.INITIALLY_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.NOT {{signature}}](#apidoc.element.sequelize.Deferrable.NOT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.SET_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.SET_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.SET_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ENUM (value)](#apidoc.element.sequelize.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>EmptyResultError (message)](#apidoc.element.sequelize.EmptyResultError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Error ()](#apidoc.element.sequelize.Error)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Error.super_ ()](#apidoc.element.sequelize.Error.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ExclusionConstraintError (options)](#apidoc.element.sequelize.ExclusionConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ForeignKeyConstraintError (options)](#apidoc.element.sequelize.ForeignKeyConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>GEOGRAPHY (type, srid)](#apidoc.element.sequelize.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>GEOMETRY (type, srid)](#apidoc.element.sequelize.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>HSTORE ()](#apidoc.element.sequelize.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>HostNotFoundError (parent)](#apidoc.element.sequelize.HostNotFoundError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>HostNotReachableError (parent)](#apidoc.element.sequelize.HostNotReachableError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>INTEGER (length)](#apidoc.element.sequelize.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Instance (values, options)](#apidoc.element.sequelize.Instance)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>InstanceError (message)](#apidoc.element.sequelize.InstanceError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>InvalidConnectionError (parent)](#apidoc.element.sequelize.InvalidConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>JSON ()](#apidoc.element.sequelize.JSON)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>JSONB ()](#apidoc.element.sequelize.JSONB)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Model (name, attributes, options)](#apidoc.element.sequelize.Model)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>NONE (ReturnType, fields)](#apidoc.element.sequelize.NONE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>NOW ()](#apidoc.element.sequelize.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>NUMBER (options)](#apidoc.element.sequelize.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>NUMERIC (precision, scale)](#apidoc.element.sequelize.NUMERIC)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise (executor)](#apidoc.element.sequelize.Promise)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.AggregateError (message)](#apidoc.element.sequelize.Promise.AggregateError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.CancellationError (message)](#apidoc.element.sequelize.Promise.CancellationError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.OperationalError (message)](#apidoc.element.sequelize.Promise.OperationalError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.PromiseInspection (promise)](#apidoc.element.sequelize.Promise.PromiseInspection)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.TimeoutError (message)](#apidoc.element.sequelize.Promise.TimeoutError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise._SomePromiseArray (values)](#apidoc.element.sequelize.Promise._SomePromiseArray)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.coroutine (generatorFunction, options)](#apidoc.element.sequelize.Promise.coroutine)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.each ()](#apidoc.element.sequelize.Promise.each)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.filter ()](#apidoc.element.sequelize.Promise.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.join ()](#apidoc.element.sequelize.Promise.join)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.map ()](#apidoc.element.sequelize.Promise.map)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.mapSeries ()](#apidoc.element.sequelize.Promise.mapSeries)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.catch ()](#apidoc.element.sequelize.Promise.prototype.catch)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.done ()](#apidoc.element.sequelize.Promise.prototype.done)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.each ()](#apidoc.element.sequelize.Promise.prototype.each)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.error ()](#apidoc.element.sequelize.Promise.prototype.error)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.filter ()](#apidoc.element.sequelize.Promise.prototype.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.finally ()](#apidoc.element.sequelize.Promise.prototype.finally)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.map ()](#apidoc.element.sequelize.Promise.prototype.map)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.mapSeries ()](#apidoc.element.sequelize.Promise.prototype.mapSeries)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.nodeify ()](#apidoc.element.sequelize.Promise.prototype.nodeify)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.reduce ()](#apidoc.element.sequelize.Promise.prototype.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.spread ()](#apidoc.element.sequelize.Promise.prototype.spread)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.tap ()](#apidoc.element.sequelize.Promise.prototype.tap)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.then ()](#apidoc.element.sequelize.Promise.prototype.then)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise.reduce ()](#apidoc.element.sequelize.Promise.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>RANGE (subtype)](#apidoc.element.sequelize.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>REAL (length, decimals)](#apidoc.element.sequelize.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>STRING (length, binary)](#apidoc.element.sequelize.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Sequelize (database, username, password, options)](#apidoc.element.sequelize.Sequelize)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>TEXT (length)](#apidoc.element.sequelize.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>TIME ()](#apidoc.element.sequelize.TIME)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>TimeoutError (parent)](#apidoc.element.sequelize.TimeoutError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Transaction (sequelize, options)](#apidoc.element.sequelize.Transaction)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UUID ()](#apidoc.element.sequelize.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UUIDV1 ()](#apidoc.element.sequelize.UUIDV1)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UUIDV4 ()](#apidoc.element.sequelize.UUIDV4)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UniqueConstraintError (options)](#apidoc.element.sequelize.UniqueConstraintError)
1.  function <span class="apidocSignatureSpan">sequelize.</span>Utils._
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Utils.fn (fn, args)](#apidoc.element.sequelize.Utils.fn)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>VIRTUAL (ReturnType, fields)](#apidoc.element.sequelize.VIRTUAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ValidationError (message, errors)](#apidoc.element.sequelize.ValidationError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ValidationErrorItem (message, type, path, value)](#apidoc.element.sequelize.ValidationErrorItem)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>addHook (hookType, name, fn)](#apidoc.element.sequelize.addHook)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>and ()](#apidoc.element.sequelize.and)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>asIs (val)](#apidoc.element.sequelize.asIs)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>cast (val, type)](#apidoc.element.sequelize.cast)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>col (col)](#apidoc.element.sequelize.col)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>condition (attr, comparator, logic)](#apidoc.element.sequelize.condition)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>default (database, username, password, options)](#apidoc.element.sequelize.default)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>fn (fn)](#apidoc.element.sequelize.fn)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>hasHook (hookType)](#apidoc.element.sequelize.hasHook)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>hasHooks (hookType)](#apidoc.element.sequelize.hasHooks)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>hook ()](#apidoc.element.sequelize.hook)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>instance_validator (modelInstance, options)](#apidoc.element.sequelize.instance_validator)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>json (conditionsOrPath, value)](#apidoc.element.sequelize.json)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>literal (val)](#apidoc.element.sequelize.literal)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>model_manager (sequelize)](#apidoc.element.sequelize.model_manager)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.BIGINT ()](#apidoc.element.sequelize.mssql.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.BLOB ()](#apidoc.element.sequelize.mssql.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.BOOLEAN ()](#apidoc.element.sequelize.mssql.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.DATE ()](#apidoc.element.sequelize.mssql.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.ENUM ()](#apidoc.element.sequelize.mssql.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.FLOAT ()](#apidoc.element.sequelize.mssql.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.INTEGER ()](#apidoc.element.sequelize.mssql.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.NOW ()](#apidoc.element.sequelize.mssql.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.REAL ()](#apidoc.element.sequelize.mssql.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.STRING ()](#apidoc.element.sequelize.mssql.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.TEXT ()](#apidoc.element.sequelize.mssql.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mssql.UUID ()](#apidoc.element.sequelize.mssql.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mysql.DATE ()](#apidoc.element.sequelize.mysql.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mysql.ENUM ()](#apidoc.element.sequelize.mysql.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mysql.GEOMETRY ()](#apidoc.element.sequelize.mysql.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>mysql.UUID ()](#apidoc.element.sequelize.mysql.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>or ()](#apidoc.element.sequelize.or)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.BIGINT ()](#apidoc.element.sequelize.postgres.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.BLOB ()](#apidoc.element.sequelize.postgres.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.BOOLEAN ()](#apidoc.element.sequelize.postgres.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.CHAR ()](#apidoc.element.sequelize.postgres.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.DATE ()](#apidoc.element.sequelize.postgres.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.DECIMAL ()](#apidoc.element.sequelize.postgres.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.FLOAT ()](#apidoc.element.sequelize.postgres.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.GEOGRAPHY ()](#apidoc.element.sequelize.postgres.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.GEOMETRY ()](#apidoc.element.sequelize.postgres.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.HSTORE ()](#apidoc.element.sequelize.postgres.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.INTEGER ()](#apidoc.element.sequelize.postgres.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.RANGE ()](#apidoc.element.sequelize.postgres.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.REAL ()](#apidoc.element.sequelize.postgres.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.STRING ()](#apidoc.element.sequelize.postgres.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>postgres.TEXT ()](#apidoc.element.sequelize.postgres.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>query_interface (sequelize)](#apidoc.element.sequelize.query_interface)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>removeHook (hookType, name)](#apidoc.element.sequelize.removeHook)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>replaceHookAliases (hooks)](#apidoc.element.sequelize.replaceHookAliases)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>runHooks (hooks)](#apidoc.element.sequelize.runHooks)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.BIGINT (length)](#apidoc.element.sequelize.sqlite.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.CHAR ()](#apidoc.element.sequelize.sqlite.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.DATE ()](#apidoc.element.sequelize.sqlite.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.ENUM ()](#apidoc.element.sequelize.sqlite.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.FLOAT (length, decimals)](#apidoc.element.sequelize.sqlite.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.INTEGER (length)](#apidoc.element.sequelize.sqlite.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.NUMBER ()](#apidoc.element.sequelize.sqlite.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.REAL (length, decimals)](#apidoc.element.sequelize.sqlite.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.STRING ()](#apidoc.element.sequelize.sqlite.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>sqlite.TEXT ()](#apidoc.element.sequelize.sqlite.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>where (attr, comparator, logic)](#apidoc.element.sequelize.where)
1.  object <span class="apidocSignatureSpan">sequelize.</span>ABSTRACT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>ARRAY.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Association.BelongsTo.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Association.BelongsToMany.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Association.HasMany.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Association.HasOne.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Association.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>BIGINT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>BLOB.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>BOOLEAN.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>CHAR.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>DATE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>DATEONLY.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>DECIMAL.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>DOUBLE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>DataTypes
1.  object <span class="apidocSignatureSpan">sequelize.</span>Deferrable
1.  object <span class="apidocSignatureSpan">sequelize.</span>Deferrable.INITIALLY_DEFERRED.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Deferrable.INITIALLY_DEFERRED.super_.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Deferrable.INITIALLY_IMMEDIATE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Deferrable.NOT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Deferrable.SET_DEFERRED.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Deferrable.SET_IMMEDIATE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>ENUM.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>FLOAT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>GEOGRAPHY.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>GEOMETRY.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>HSTORE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>INTEGER.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Instance.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>JSON.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Model.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>NUMBER.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Promise.AggregateError.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Promise.CancellationError.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Promise.OperationalError.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Promise.PromiseInspection.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Promise.TimeoutError.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Promise._SomePromiseArray.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>QueryTypes
1.  object <span class="apidocSignatureSpan">sequelize.</span>RANGE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>REAL.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>STRING.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>TEXT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>TIME.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Transaction.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>UUID.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>UUIDV1.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>UUIDV4.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Utils
1.  object <span class="apidocSignatureSpan">sequelize.</span>Utils._.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Utils.fn.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Utils.inflection
1.  object <span class="apidocSignatureSpan">sequelize.</span>ValidationError.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>Validator
1.  object <span class="apidocSignatureSpan">sequelize.</span>errors
1.  object <span class="apidocSignatureSpan">sequelize.</span>hooks
1.  object <span class="apidocSignatureSpan">sequelize.</span>instance_validator.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mariadb
1.  object <span class="apidocSignatureSpan">sequelize.</span>model_manager.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.BLOB.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.BOOLEAN.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.DATE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.ENUM.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.NOW.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.STRING.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.TEXT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mssql.UUID.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mysql
1.  object <span class="apidocSignatureSpan">sequelize.</span>mysql.DATE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mysql.ENUM.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mysql.GEOMETRY.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>mysql.UUID.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>options
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.BLOB.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.BOOLEAN.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.CHAR.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.DATE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.GEOGRAPHY.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.GEOMETRY.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.HSTORE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.RANGE.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.STRING.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>postgres.TEXT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>query_interface.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sql_string
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.BIGINT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.CHAR.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.ENUM.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.FLOAT.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.INTEGER.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.NUMBER.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.REAL.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.STRING.prototype
1.  object <span class="apidocSignatureSpan">sequelize.</span>sqlite.TEXT.prototype
1.  string <span class="apidocSignatureSpan">sequelize.</span>version

#### [module sequelize.ABSTRACT](#apidoc.module.sequelize.ABSTRACT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ABSTRACT (options)](#apidoc.element.sequelize.ABSTRACT.ABSTRACT)
1.  [function <span class="apidocSignatureSpan">sequelize.ABSTRACT.</span>inherits (Constructor)](#apidoc.element.sequelize.ABSTRACT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.ABSTRACT.</span>warn (link, text)](#apidoc.element.sequelize.ABSTRACT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.ABSTRACT.</span>types

#### [module sequelize.ABSTRACT.prototype](#apidoc.module.sequelize.ABSTRACT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.ABSTRACT.prototype.</span>stringify (value, options)](#apidoc.element.sequelize.ABSTRACT.prototype.stringify)
1.  [function <span class="apidocSignatureSpan">sequelize.ABSTRACT.prototype.</span>toSql ()](#apidoc.element.sequelize.ABSTRACT.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.ABSTRACT.prototype.</span>toString (options)](#apidoc.element.sequelize.ABSTRACT.prototype.toString)
1.  string <span class="apidocSignatureSpan">sequelize.ABSTRACT.prototype.</span>dialectTypes

#### [module sequelize.ARRAY](#apidoc.module.sequelize.ARRAY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ARRAY (type)](#apidoc.element.sequelize.ARRAY.ARRAY)
1.  [function <span class="apidocSignatureSpan">sequelize.ARRAY.</span>is (obj, type)](#apidoc.element.sequelize.ARRAY.is)
1.  [function <span class="apidocSignatureSpan">sequelize.ARRAY.</span>super_ (options)](#apidoc.element.sequelize.ARRAY.super_)
1.  object <span class="apidocSignatureSpan">sequelize.ARRAY.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.ARRAY.</span>key

#### [module sequelize.ARRAY.prototype](#apidoc.module.sequelize.ARRAY.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.ARRAY.prototype.</span>escape
1.  [function <span class="apidocSignatureSpan">sequelize.ARRAY.prototype.</span>toSql ()](#apidoc.element.sequelize.ARRAY.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.ARRAY.prototype.</span>validate (value)](#apidoc.element.sequelize.ARRAY.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.ARRAY.prototype.</span>key

#### [module sequelize.AccessDeniedError](#apidoc.module.sequelize.AccessDeniedError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>AccessDeniedError (parent)](#apidoc.element.sequelize.AccessDeniedError.AccessDeniedError)
1.  [function <span class="apidocSignatureSpan">sequelize.AccessDeniedError.</span>super_ (parent)](#apidoc.element.sequelize.AccessDeniedError.super_)

#### [module sequelize.Association](#apidoc.module.sequelize.Association)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Association ()](#apidoc.element.sequelize.Association.Association)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsTo (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsToMany (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>HasMany (source, target, options)](#apidoc.element.sequelize.Association.HasMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>HasOne (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>default ()](#apidoc.element.sequelize.Association.default)

#### [module sequelize.Association.BelongsTo](#apidoc.module.sequelize.Association.BelongsTo)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsTo (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo.BelongsTo)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.</span>default (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo.default)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.</span>super_ ()](#apidoc.element.sequelize.Association.BelongsTo.super_)

#### [module sequelize.Association.BelongsTo.prototype](#apidoc.module.sequelize.Association.BelongsTo.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>get (instances, options)](#apidoc.element.sequelize.Association.BelongsTo.prototype.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.BelongsTo.prototype.injectAttributes)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>injectCreator (instancePrototype)](#apidoc.element.sequelize.Association.BelongsTo.prototype.injectCreator)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>injectSetter (instancePrototype)](#apidoc.element.sequelize.Association.BelongsTo.prototype.injectSetter)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>mixin (obj)](#apidoc.element.sequelize.Association.BelongsTo.prototype.mixin)

#### [module sequelize.Association.BelongsToMany](#apidoc.module.sequelize.Association.BelongsToMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsToMany (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany.BelongsToMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.</span>default (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany.default)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.</span>super_ ()](#apidoc.element.sequelize.Association.BelongsToMany.super_)

#### [module sequelize.Association.BelongsToMany.prototype](#apidoc.module.sequelize.Association.BelongsToMany.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>get (instance, options)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectAttributes)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectCreator (obj)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectCreator)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectGetter (obj)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectGetter)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectSetter (obj)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectSetter)

#### [module sequelize.Association.HasMany](#apidoc.module.sequelize.Association.HasMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>HasMany (source, target, options)](#apidoc.element.sequelize.Association.HasMany.HasMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.</span>default (source, target, options)](#apidoc.element.sequelize.Association.HasMany.default)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.</span>super_ ()](#apidoc.element.sequelize.Association.HasMany.super_)

#### [module sequelize.Association.HasMany.prototype](#apidoc.module.sequelize.Association.HasMany.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>add (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.add)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>count (instance, options)](#apidoc.element.sequelize.Association.HasMany.prototype.count)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>create (sourceInstance, values, options)](#apidoc.element.sequelize.Association.HasMany.prototype.create)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>get (instances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>has (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.has)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.HasMany.prototype.injectAttributes)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>mixin (obj)](#apidoc.element.sequelize.Association.HasMany.prototype.mixin)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>remove (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.remove)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>set (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.set)

#### [module sequelize.Association.HasOne](#apidoc.module.sequelize.Association.HasOne)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.</span>HasOne (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne.HasOne)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasOne.</span>default (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne.default)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasOne.</span>super_ ()](#apidoc.element.sequelize.Association.HasOne.super_)

#### [module sequelize.Association.HasOne.prototype](#apidoc.module.sequelize.Association.HasOne.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>get (instances, options)](#apidoc.element.sequelize.Association.HasOne.prototype.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.HasOne.prototype.injectAttributes)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>injectCreator (instancePrototype)](#apidoc.element.sequelize.Association.HasOne.prototype.injectCreator)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>injectSetter (instancePrototype)](#apidoc.element.sequelize.Association.HasOne.prototype.injectSetter)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>mixin (obj)](#apidoc.element.sequelize.Association.HasOne.prototype.mixin)

#### [module sequelize.Association.prototype](#apidoc.module.sequelize.Association.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.prototype.</span>inspect ()](#apidoc.element.sequelize.Association.prototype.inspect)
1.  [function <span class="apidocSignatureSpan">sequelize.Association.prototype.</span>toInstanceArray (objs)](#apidoc.element.sequelize.Association.prototype.toInstanceArray)

#### [module sequelize.BIGINT](#apidoc.module.sequelize.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>BIGINT (length)](#apidoc.element.sequelize.BIGINT.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.BIGINT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.BIGINT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.BIGINT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.BIGINT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.BIGINT.</span>key

#### [module sequelize.BIGINT.prototype](#apidoc.module.sequelize.BIGINT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.BIGINT.prototype.</span>validate (value)](#apidoc.element.sequelize.BIGINT.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.BIGINT.prototype.</span>key

#### [module sequelize.BLOB](#apidoc.module.sequelize.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>BLOB (length)](#apidoc.element.sequelize.BLOB.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.BLOB.</span>inherits (Constructor)](#apidoc.element.sequelize.BLOB.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.BLOB.</span>super_ (options)](#apidoc.element.sequelize.BLOB.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.BLOB.</span>warn (link, text)](#apidoc.element.sequelize.BLOB.warn)
1.  object <span class="apidocSignatureSpan">sequelize.BLOB.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.BLOB.</span>key

#### [module sequelize.BLOB.prototype](#apidoc.module.sequelize.BLOB.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.BLOB.prototype.</span>escape
1.  [function <span class="apidocSignatureSpan">sequelize.BLOB.prototype.</span>toSql ()](#apidoc.element.sequelize.BLOB.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.BLOB.prototype.</span>validate (value)](#apidoc.element.sequelize.BLOB.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.BLOB.prototype.</span>key

#### [module sequelize.BOOLEAN](#apidoc.module.sequelize.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>BOOLEAN ()](#apidoc.element.sequelize.BOOLEAN.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>inherits (Constructor)](#apidoc.element.sequelize.BOOLEAN.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>super_ (options)](#apidoc.element.sequelize.BOOLEAN.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>warn (link, text)](#apidoc.element.sequelize.BOOLEAN.warn)
1.  object <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>key

#### [module sequelize.BOOLEAN.prototype](#apidoc.module.sequelize.BOOLEAN.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.BOOLEAN.prototype.</span>toSql ()](#apidoc.element.sequelize.BOOLEAN.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.BOOLEAN.prototype.</span>validate (value)](#apidoc.element.sequelize.BOOLEAN.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.BOOLEAN.prototype.</span>key

#### [module sequelize.CHAR](#apidoc.module.sequelize.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>CHAR (length, binary)](#apidoc.element.sequelize.CHAR.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.CHAR.</span>inherits (Constructor)](#apidoc.element.sequelize.CHAR.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.CHAR.</span>super_ (options)](#apidoc.element.sequelize.CHAR.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.CHAR.</span>warn (link, text)](#apidoc.element.sequelize.CHAR.warn)
1.  object <span class="apidocSignatureSpan">sequelize.CHAR.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.CHAR.</span>key

#### [module sequelize.CHAR.prototype](#apidoc.module.sequelize.CHAR.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.CHAR.prototype.</span>toSql ()](#apidoc.element.sequelize.CHAR.prototype.toSql)
1.  string <span class="apidocSignatureSpan">sequelize.CHAR.prototype.</span>key

#### [module sequelize.ConnectionError](#apidoc.module.sequelize.ConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ConnectionError (parent)](#apidoc.element.sequelize.ConnectionError.ConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.ConnectionError.</span>super_ ()](#apidoc.element.sequelize.ConnectionError.super_)

#### [module sequelize.ConnectionRefusedError](#apidoc.module.sequelize.ConnectionRefusedError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ConnectionRefusedError (parent)](#apidoc.element.sequelize.ConnectionRefusedError.ConnectionRefusedError)
1.  [function <span class="apidocSignatureSpan">sequelize.ConnectionRefusedError.</span>super_ (parent)](#apidoc.element.sequelize.ConnectionRefusedError.super_)

#### [module sequelize.ConnectionTimedOutError](#apidoc.module.sequelize.ConnectionTimedOutError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ConnectionTimedOutError (parent)](#apidoc.element.sequelize.ConnectionTimedOutError.ConnectionTimedOutError)
1.  [function <span class="apidocSignatureSpan">sequelize.ConnectionTimedOutError.</span>super_ (parent)](#apidoc.element.sequelize.ConnectionTimedOutError.super_)

#### [module sequelize.DATE](#apidoc.module.sequelize.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DATE (length)](#apidoc.element.sequelize.DATE.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.DATE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.DATE.</span>super_ (options)](#apidoc.element.sequelize.DATE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.DATE.</span>warn (link, text)](#apidoc.element.sequelize.DATE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.DATE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.DATE.</span>key

#### [module sequelize.DATE.prototype](#apidoc.module.sequelize.DATE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.DATE.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.DATE.prototype.</span>validate (value)](#apidoc.element.sequelize.DATE.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.DATE.prototype.</span>key

#### [module sequelize.DATEONLY](#apidoc.module.sequelize.DATEONLY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DATEONLY ()](#apidoc.element.sequelize.DATEONLY.DATEONLY)
1.  [function <span class="apidocSignatureSpan">sequelize.DATEONLY.</span>super_ (options)](#apidoc.element.sequelize.DATEONLY.super_)
1.  object <span class="apidocSignatureSpan">sequelize.DATEONLY.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.DATEONLY.</span>key

#### [module sequelize.DATEONLY.prototype](#apidoc.module.sequelize.DATEONLY.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.DATEONLY.prototype.</span>toSql ()](#apidoc.element.sequelize.DATEONLY.prototype.toSql)
1.  string <span class="apidocSignatureSpan">sequelize.DATEONLY.prototype.</span>key

#### [module sequelize.DECIMAL](#apidoc.module.sequelize.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DECIMAL (precision, scale)](#apidoc.element.sequelize.DECIMAL.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>inherits (Constructor)](#apidoc.element.sequelize.DECIMAL.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>super_ (options)](#apidoc.element.sequelize.DECIMAL.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>warn (link, text)](#apidoc.element.sequelize.DECIMAL.warn)
1.  object <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>key

#### [module sequelize.DECIMAL.prototype](#apidoc.module.sequelize.DECIMAL.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.DECIMAL.prototype.</span>toSql ()](#apidoc.element.sequelize.DECIMAL.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.DECIMAL.prototype.</span>validate (value)](#apidoc.element.sequelize.DECIMAL.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.DECIMAL.prototype.</span>key

#### [module sequelize.DOUBLE](#apidoc.module.sequelize.DOUBLE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DOUBLE (length, decimals)](#apidoc.element.sequelize.DOUBLE.DOUBLE)
1.  [function <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>inherits (Constructor)](#apidoc.element.sequelize.DOUBLE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>super_ (options)](#apidoc.element.sequelize.DOUBLE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>warn (link, text)](#apidoc.element.sequelize.DOUBLE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>key

#### [module sequelize.DOUBLE.prototype](#apidoc.module.sequelize.DOUBLE.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.DOUBLE.prototype.</span>escape
1.  string <span class="apidocSignatureSpan">sequelize.DOUBLE.prototype.</span>key

#### [module sequelize.DataTypes](#apidoc.module.sequelize.DataTypes)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>ABSTRACT (options)](#apidoc.element.sequelize.DataTypes.ABSTRACT)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>ARRAY (type)](#apidoc.element.sequelize.DataTypes.ARRAY)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>BIGINT (length)](#apidoc.element.sequelize.DataTypes.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>BLOB (length)](#apidoc.element.sequelize.DataTypes.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>BOOLEAN ()](#apidoc.element.sequelize.DataTypes.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>CHAR (length, binary)](#apidoc.element.sequelize.DataTypes.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DATE (length)](#apidoc.element.sequelize.DataTypes.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DATEONLY ()](#apidoc.element.sequelize.DataTypes.DATEONLY)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DECIMAL (precision, scale)](#apidoc.element.sequelize.DataTypes.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DOUBLE (length, decimals)](#apidoc.element.sequelize.DataTypes.DOUBLE)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>ENUM (value)](#apidoc.element.sequelize.DataTypes.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.DataTypes.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>GEOGRAPHY (type, srid)](#apidoc.element.sequelize.DataTypes.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>GEOMETRY (type, srid)](#apidoc.element.sequelize.DataTypes.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>HSTORE ()](#apidoc.element.sequelize.DataTypes.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>INTEGER (length)](#apidoc.element.sequelize.DataTypes.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>JSON ()](#apidoc.element.sequelize.DataTypes.JSON)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>JSONB ()](#apidoc.element.sequelize.DataTypes.JSONB)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NONE (ReturnType, fields)](#apidoc.element.sequelize.DataTypes.NONE)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NOW ()](#apidoc.element.sequelize.DataTypes.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NUMBER (options)](#apidoc.element.sequelize.DataTypes.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NUMERIC (precision, scale)](#apidoc.element.sequelize.DataTypes.NUMERIC)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>RANGE (subtype)](#apidoc.element.sequelize.DataTypes.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>REAL (length, decimals)](#apidoc.element.sequelize.DataTypes.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>STRING (length, binary)](#apidoc.element.sequelize.DataTypes.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>TEXT (length)](#apidoc.element.sequelize.DataTypes.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>TIME ()](#apidoc.element.sequelize.DataTypes.TIME)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>UUID ()](#apidoc.element.sequelize.DataTypes.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>UUIDV1 ()](#apidoc.element.sequelize.DataTypes.UUIDV1)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>UUIDV4 ()](#apidoc.element.sequelize.DataTypes.UUIDV4)
1.  [function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>VIRTUAL (ReturnType, fields)](#apidoc.element.sequelize.DataTypes.VIRTUAL)
1.  object <span class="apidocSignatureSpan">sequelize.DataTypes.</span>mariadb
1.  object <span class="apidocSignatureSpan">sequelize.DataTypes.</span>mssql
1.  object <span class="apidocSignatureSpan">sequelize.DataTypes.</span>mysql
1.  object <span class="apidocSignatureSpan">sequelize.DataTypes.</span>postgres
1.  object <span class="apidocSignatureSpan">sequelize.DataTypes.</span>sqlite

#### [module sequelize.DatabaseError](#apidoc.module.sequelize.DatabaseError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>DatabaseError (parent)](#apidoc.element.sequelize.DatabaseError.DatabaseError)
1.  [function <span class="apidocSignatureSpan">sequelize.DatabaseError.</span>super_ ()](#apidoc.element.sequelize.DatabaseError.super_)

#### [module sequelize.Deferrable](#apidoc.module.sequelize.Deferrable)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>NOT {{signature}}](#apidoc.element.sequelize.Deferrable.NOT)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.SET_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE)

#### [module sequelize.Deferrable.INITIALLY_DEFERRED](#apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.INITIALLY_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.</span>super_ ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.</span>toString ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.toString)

#### [module sequelize.Deferrable.INITIALLY_DEFERRED.prototype](#apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.prototype.</span>toSql ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.prototype.toSql)

#### [module sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype](#apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype.</span>toString ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype.toString)

#### [module sequelize.Deferrable.INITIALLY_IMMEDIATE](#apidoc.module.sequelize.Deferrable.INITIALLY_IMMEDIATE)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.INITIALLY_IMMEDIATE)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_IMMEDIATE.</span>super_ ()](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_IMMEDIATE.</span>toString ()](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.toString)

#### [module sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype](#apidoc.module.sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype.</span>toSql ()](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype.toSql)

#### [module sequelize.Deferrable.NOT](#apidoc.module.sequelize.Deferrable.NOT)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>NOT {{signature}}](#apidoc.element.sequelize.Deferrable.NOT.NOT)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.NOT.</span>super_ ()](#apidoc.element.sequelize.Deferrable.NOT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.NOT.</span>toString ()](#apidoc.element.sequelize.Deferrable.NOT.toString)

#### [module sequelize.Deferrable.NOT.prototype](#apidoc.module.sequelize.Deferrable.NOT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.NOT.prototype.</span>toSql ()](#apidoc.element.sequelize.Deferrable.NOT.prototype.toSql)

#### [module sequelize.Deferrable.SET_DEFERRED](#apidoc.module.sequelize.Deferrable.SET_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.SET_DEFERRED)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_DEFERRED.</span>super_ ()](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_DEFERRED.</span>toString ()](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.toString)

#### [module sequelize.Deferrable.SET_DEFERRED.prototype](#apidoc.module.sequelize.Deferrable.SET_DEFERRED.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_DEFERRED.prototype.</span>toSql (queryGenerator)](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.prototype.toSql)

#### [module sequelize.Deferrable.SET_IMMEDIATE](#apidoc.module.sequelize.Deferrable.SET_IMMEDIATE)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.SET_IMMEDIATE)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_IMMEDIATE.</span>super_ ()](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_IMMEDIATE.</span>toString ()](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.toString)

#### [module sequelize.Deferrable.SET_IMMEDIATE.prototype](#apidoc.module.sequelize.Deferrable.SET_IMMEDIATE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_IMMEDIATE.prototype.</span>toSql (queryGenerator)](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.prototype.toSql)

#### [module sequelize.ENUM](#apidoc.module.sequelize.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ENUM (value)](#apidoc.element.sequelize.ENUM.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.ENUM.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.ENUM.</span>super_ (options)](#apidoc.element.sequelize.ENUM.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.ENUM.warn)
1.  object <span class="apidocSignatureSpan">sequelize.ENUM.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.ENUM.</span>key

#### [module sequelize.ENUM.prototype](#apidoc.module.sequelize.ENUM.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.ENUM.prototype.</span>validate (value)](#apidoc.element.sequelize.ENUM.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.ENUM.prototype.</span>key

#### [module sequelize.EmptyResultError](#apidoc.module.sequelize.EmptyResultError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>EmptyResultError (message)](#apidoc.element.sequelize.EmptyResultError.EmptyResultError)
1.  [function <span class="apidocSignatureSpan">sequelize.EmptyResultError.</span>super_ ()](#apidoc.element.sequelize.EmptyResultError.super_)

#### [module sequelize.Error](#apidoc.module.sequelize.Error)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Error ()](#apidoc.element.sequelize.Error.Error)
1.  [function <span class="apidocSignatureSpan">sequelize.Error.</span>super_ ()](#apidoc.element.sequelize.Error.super_)

#### [module sequelize.Error.super_](#apidoc.module.sequelize.Error.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.Error.</span>super_ ()](#apidoc.element.sequelize.Error.super_.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.Error.super_.</span>captureStackTrace ()](#apidoc.element.sequelize.Error.super_.captureStackTrace)
1.  number <span class="apidocSignatureSpan">sequelize.Error.super_.</span>stackTraceLimit

#### [module sequelize.ExclusionConstraintError](#apidoc.module.sequelize.ExclusionConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ExclusionConstraintError (options)](#apidoc.element.sequelize.ExclusionConstraintError.ExclusionConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.ExclusionConstraintError.</span>super_ (parent)](#apidoc.element.sequelize.ExclusionConstraintError.super_)

#### [module sequelize.FLOAT](#apidoc.module.sequelize.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.FLOAT.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.FLOAT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.FLOAT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.FLOAT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.FLOAT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.FLOAT.</span>key

#### [module sequelize.FLOAT.prototype](#apidoc.module.sequelize.FLOAT.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.FLOAT.prototype.</span>escape
1.  [function <span class="apidocSignatureSpan">sequelize.FLOAT.prototype.</span>validate (value)](#apidoc.element.sequelize.FLOAT.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.FLOAT.prototype.</span>key

#### [module sequelize.ForeignKeyConstraintError](#apidoc.module.sequelize.ForeignKeyConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ForeignKeyConstraintError (options)](#apidoc.element.sequelize.ForeignKeyConstraintError.ForeignKeyConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.ForeignKeyConstraintError.</span>super_ (parent)](#apidoc.element.sequelize.ForeignKeyConstraintError.super_)

#### [module sequelize.GEOGRAPHY](#apidoc.module.sequelize.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>GEOGRAPHY (type, srid)](#apidoc.element.sequelize.GEOGRAPHY.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>inherits (Constructor)](#apidoc.element.sequelize.GEOGRAPHY.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>super_ (options)](#apidoc.element.sequelize.GEOGRAPHY.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>warn (link, text)](#apidoc.element.sequelize.GEOGRAPHY.warn)
1.  object <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>key

#### [module sequelize.GEOGRAPHY.prototype](#apidoc.module.sequelize.GEOGRAPHY.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.prototype.</span>escape
1.  string <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.prototype.</span>key

#### [module sequelize.GEOMETRY](#apidoc.module.sequelize.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>GEOMETRY (type, srid)](#apidoc.element.sequelize.GEOMETRY.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>inherits (Constructor)](#apidoc.element.sequelize.GEOMETRY.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>super_ (options)](#apidoc.element.sequelize.GEOMETRY.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>warn (link, text)](#apidoc.element.sequelize.GEOMETRY.warn)
1.  object <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>key

#### [module sequelize.GEOMETRY.prototype](#apidoc.module.sequelize.GEOMETRY.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.GEOMETRY.prototype.</span>escape
1.  string <span class="apidocSignatureSpan">sequelize.GEOMETRY.prototype.</span>key

#### [module sequelize.HSTORE](#apidoc.module.sequelize.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>HSTORE ()](#apidoc.element.sequelize.HSTORE.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.HSTORE.</span>inherits (Constructor)](#apidoc.element.sequelize.HSTORE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.HSTORE.</span>super_ (options)](#apidoc.element.sequelize.HSTORE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.HSTORE.</span>warn (link, text)](#apidoc.element.sequelize.HSTORE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.HSTORE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.HSTORE.</span>key

#### [module sequelize.HSTORE.prototype](#apidoc.module.sequelize.HSTORE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.HSTORE.prototype.</span>validate (value)](#apidoc.element.sequelize.HSTORE.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.HSTORE.prototype.</span>key

#### [module sequelize.HostNotFoundError](#apidoc.module.sequelize.HostNotFoundError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>HostNotFoundError (parent)](#apidoc.element.sequelize.HostNotFoundError.HostNotFoundError)
1.  [function <span class="apidocSignatureSpan">sequelize.HostNotFoundError.</span>super_ (parent)](#apidoc.element.sequelize.HostNotFoundError.super_)

#### [module sequelize.HostNotReachableError](#apidoc.module.sequelize.HostNotReachableError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>HostNotReachableError (parent)](#apidoc.element.sequelize.HostNotReachableError.HostNotReachableError)
1.  [function <span class="apidocSignatureSpan">sequelize.HostNotReachableError.</span>super_ (parent)](#apidoc.element.sequelize.HostNotReachableError.super_)

#### [module sequelize.INTEGER](#apidoc.module.sequelize.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>INTEGER (length)](#apidoc.element.sequelize.INTEGER.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.INTEGER.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.INTEGER.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.INTEGER.warn)
1.  object <span class="apidocSignatureSpan">sequelize.INTEGER.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.INTEGER.</span>key

#### [module sequelize.INTEGER.prototype](#apidoc.module.sequelize.INTEGER.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.INTEGER.prototype.</span>validate (value)](#apidoc.element.sequelize.INTEGER.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.INTEGER.prototype.</span>key

#### [module sequelize.Instance](#apidoc.module.sequelize.Instance)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Instance (values, options)](#apidoc.element.sequelize.Instance.Instance)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.</span>default (values, options)](#apidoc.element.sequelize.Instance.default)

#### [module sequelize.Instance.prototype](#apidoc.module.sequelize.Instance.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>_setInclude (key, value, options)](#apidoc.element.sequelize.Instance.prototype._setInclude)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>changed (key, value)](#apidoc.element.sequelize.Instance.prototype.changed)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>decrement (fields, options)](#apidoc.element.sequelize.Instance.prototype.decrement)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>destroy (options)](#apidoc.element.sequelize.Instance.prototype.destroy)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>equals (other)](#apidoc.element.sequelize.Instance.prototype.equals)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>equalsOneOf (others)](#apidoc.element.sequelize.Instance.prototype.equalsOneOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>get (key, options)](#apidoc.element.sequelize.Instance.prototype.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>getDataValue (key)](#apidoc.element.sequelize.Instance.prototype.getDataValue)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>hookValidate (options)](#apidoc.element.sequelize.Instance.prototype.hookValidate)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>increment (fields, options)](#apidoc.element.sequelize.Instance.prototype.increment)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>previous (key)](#apidoc.element.sequelize.Instance.prototype.previous)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>reload (options)](#apidoc.element.sequelize.Instance.prototype.reload)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>restore (options)](#apidoc.element.sequelize.Instance.prototype.restore)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>save (options)](#apidoc.element.sequelize.Instance.prototype.save)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>set (key, value, options)](#apidoc.element.sequelize.Instance.prototype.set)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>setAttributes (updates)](#apidoc.element.sequelize.Instance.prototype.setAttributes)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>setDataValue (key, value)](#apidoc.element.sequelize.Instance.prototype.setDataValue)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>setValidators (attribute, validators)](#apidoc.element.sequelize.Instance.prototype.setValidators)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>toJSON ()](#apidoc.element.sequelize.Instance.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>toString ()](#apidoc.element.sequelize.Instance.prototype.toString)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>update (values, options)](#apidoc.element.sequelize.Instance.prototype.update)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>updateAttributes (values, options)](#apidoc.element.sequelize.Instance.prototype.updateAttributes)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>validate (options)](#apidoc.element.sequelize.Instance.prototype.validate)
1.  [function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>where ()](#apidoc.element.sequelize.Instance.prototype.where)

#### [module sequelize.InstanceError](#apidoc.module.sequelize.InstanceError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>InstanceError (message)](#apidoc.element.sequelize.InstanceError.InstanceError)
1.  [function <span class="apidocSignatureSpan">sequelize.InstanceError.</span>super_ ()](#apidoc.element.sequelize.InstanceError.super_)

#### [module sequelize.InvalidConnectionError](#apidoc.module.sequelize.InvalidConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>InvalidConnectionError (parent)](#apidoc.element.sequelize.InvalidConnectionError.InvalidConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.InvalidConnectionError.</span>super_ (parent)](#apidoc.element.sequelize.InvalidConnectionError.super_)

#### [module sequelize.JSON](#apidoc.module.sequelize.JSON)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>JSON ()](#apidoc.element.sequelize.JSON.JSON)
1.  [function <span class="apidocSignatureSpan">sequelize.JSON.</span>super_ (options)](#apidoc.element.sequelize.JSON.super_)
1.  object <span class="apidocSignatureSpan">sequelize.JSON.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.JSON.</span>key

#### [module sequelize.JSON.prototype](#apidoc.module.sequelize.JSON.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.JSON.prototype.</span>validate (value)](#apidoc.element.sequelize.JSON.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.JSON.prototype.</span>key

#### [module sequelize.JSONB](#apidoc.module.sequelize.JSONB)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>JSONB ()](#apidoc.element.sequelize.JSONB.JSONB)
1.  [function <span class="apidocSignatureSpan">sequelize.JSONB.</span>super_ ()](#apidoc.element.sequelize.JSONB.super_)
1.  object <span class="apidocSignatureSpan">sequelize.JSONB.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.JSONB.</span>key

#### [module sequelize.Model](#apidoc.module.sequelize.Model)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Model (name, attributes, options)](#apidoc.element.sequelize.Model.Model)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>addHook (hookType, name, fn)](#apidoc.element.sequelize.Model.addHook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>default (name, attributes, options)](#apidoc.element.sequelize.Model.default)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>hasHook (hookType)](#apidoc.element.sequelize.Model.hasHook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>hasHooks (hookType)](#apidoc.element.sequelize.Model.hasHooks)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>hook ()](#apidoc.element.sequelize.Model.hook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>removeHook (hookType, name)](#apidoc.element.sequelize.Model.removeHook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>replaceHookAliases (hooks)](#apidoc.element.sequelize.Model.replaceHookAliases)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.</span>runHooks (hooks)](#apidoc.element.sequelize.Model.runHooks)

#### [module sequelize.Model.prototype](#apidoc.module.sequelize.Model.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>addHook (hookType, name, fn)](#apidoc.element.sequelize.Model.prototype.addHook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>addScope (name, scope, options)](#apidoc.element.sequelize.Model.prototype.addScope)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkCreate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkDelete)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkDestroy)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkRestore)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkSync (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkSync)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkUpdate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterConnect (name, callback)](#apidoc.element.sequelize.Model.prototype.afterConnect)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterCreate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterDefine (name, callback)](#apidoc.element.sequelize.Model.prototype.afterDefine)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.afterDelete)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.afterDestroy)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterFind (name, callback)](#apidoc.element.sequelize.Model.prototype.afterFind)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterInit (name, callback)](#apidoc.element.sequelize.Model.prototype.afterInit)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.afterRestore)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterSync (name, callback)](#apidoc.element.sequelize.Model.prototype.afterSync)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterUpdate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterValidate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterValidate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>aggregate (attribute, aggregateFunction, options)](#apidoc.element.sequelize.Model.prototype.aggregate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>all (options)](#apidoc.element.sequelize.Model.prototype.all)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkCreate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkDelete)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkDestroy)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkRestore)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkSync (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkSync)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkUpdate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeConnect (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeConnect)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeConnection (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeConnection)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeCount (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeCount)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeCreate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeDefine (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeDefine)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeDelete)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeDestroy)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeFind (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeFind)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeFindAfterExpandIncludeAll (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeFindAfterExpandIncludeAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeFindAfterOptions (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeFindAfterOptions)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeInit (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeInit)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeRestore)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeSync (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeSync)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeUpdate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeValidate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeValidate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>belongsTo (target, options)](#apidoc.element.sequelize.Model.prototype.belongsTo)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>belongsToMany (targetModel, options)](#apidoc.element.sequelize.Model.prototype.belongsToMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>build (values, options)](#apidoc.element.sequelize.Model.prototype.build)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>bulkBuild (valueSets, options)](#apidoc.element.sequelize.Model.prototype.bulkBuild)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>bulkCreate (records, options)](#apidoc.element.sequelize.Model.prototype.bulkCreate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>count (options)](#apidoc.element.sequelize.Model.prototype.count)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>create (values, options)](#apidoc.element.sequelize.Model.prototype.create)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>describe (schema, options)](#apidoc.element.sequelize.Model.prototype.describe)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>destroy (options)](#apidoc.element.sequelize.Model.prototype.destroy)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>drop (options)](#apidoc.element.sequelize.Model.prototype.drop)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>dropSchema (schema)](#apidoc.element.sequelize.Model.prototype.dropSchema)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>find (options)](#apidoc.element.sequelize.Model.prototype.find)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findAll (options)](#apidoc.element.sequelize.Model.prototype.findAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findAndCount (options)](#apidoc.element.sequelize.Model.prototype.findAndCount)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findAndCountAll (options)](#apidoc.element.sequelize.Model.prototype.findAndCountAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findById (param, options)](#apidoc.element.sequelize.Model.prototype.findById)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findByPrimary (param, options)](#apidoc.element.sequelize.Model.prototype.findByPrimary)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findCreateFind (options)](#apidoc.element.sequelize.Model.prototype.findCreateFind)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOne (options)](#apidoc.element.sequelize.Model.prototype.findOne)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOrBuild (options)](#apidoc.element.sequelize.Model.prototype.findOrBuild)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOrCreate (options)](#apidoc.element.sequelize.Model.prototype.findOrCreate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOrInitialize (options)](#apidoc.element.sequelize.Model.prototype.findOrInitialize)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>getAssociation (target, alias)](#apidoc.element.sequelize.Model.prototype.getAssociation)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>getTableName (options)](#apidoc.element.sequelize.Model.prototype.getTableName)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasHook (hookType)](#apidoc.element.sequelize.Model.prototype.hasHook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasHooks (hookType)](#apidoc.element.sequelize.Model.prototype.hasHooks)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasMany (target, options)](#apidoc.element.sequelize.Model.prototype.hasMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasOne (target, options)](#apidoc.element.sequelize.Model.prototype.hasOne)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hook ()](#apidoc.element.sequelize.Model.prototype.hook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>init (modelManager)](#apidoc.element.sequelize.Model.prototype.init)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>insertOrUpdate (values, options)](#apidoc.element.sequelize.Model.prototype.insertOrUpdate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>inspect ()](#apidoc.element.sequelize.Model.prototype.inspect)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>max (field, options)](#apidoc.element.sequelize.Model.prototype.max)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>min (field, options)](#apidoc.element.sequelize.Model.prototype.min)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>refreshAttributes ()](#apidoc.element.sequelize.Model.prototype.refreshAttributes)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>removeAttribute (attribute)](#apidoc.element.sequelize.Model.prototype.removeAttribute)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>removeHook (hookType, name)](#apidoc.element.sequelize.Model.prototype.removeHook)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>replaceHookAliases (hooks)](#apidoc.element.sequelize.Model.prototype.replaceHookAliases)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>restore (options)](#apidoc.element.sequelize.Model.prototype.restore)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>runHooks (hooks)](#apidoc.element.sequelize.Model.prototype.runHooks)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>schema (schema, options)](#apidoc.element.sequelize.Model.prototype.schema)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>scope (option)](#apidoc.element.sequelize.Model.prototype.scope)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>sum (field, options)](#apidoc.element.sequelize.Model.prototype.sum)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>sync (options)](#apidoc.element.sequelize.Model.prototype.sync)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>toString ()](#apidoc.element.sequelize.Model.prototype.toString)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>truncate (options)](#apidoc.element.sequelize.Model.prototype.truncate)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>unscoped ()](#apidoc.element.sequelize.Model.prototype.unscoped)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>update (values, options)](#apidoc.element.sequelize.Model.prototype.update)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>upsert (values, options)](#apidoc.element.sequelize.Model.prototype.upsert)
1.  [function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>validationFailed (name, callback)](#apidoc.element.sequelize.Model.prototype.validationFailed)

#### [module sequelize.NOW](#apidoc.module.sequelize.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>NOW ()](#apidoc.element.sequelize.NOW.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.NOW.</span>inherits (Constructor)](#apidoc.element.sequelize.NOW.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.NOW.</span>super_ (options)](#apidoc.element.sequelize.NOW.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.NOW.</span>warn (link, text)](#apidoc.element.sequelize.NOW.warn)
1.  object <span class="apidocSignatureSpan">sequelize.NOW.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.NOW.</span>key

#### [module sequelize.NUMBER](#apidoc.module.sequelize.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>NUMBER (options)](#apidoc.element.sequelize.NUMBER.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.NUMBER.</span>inherits (Constructor)](#apidoc.element.sequelize.NUMBER.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.NUMBER.</span>super_ (options)](#apidoc.element.sequelize.NUMBER.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.NUMBER.</span>warn (link, text)](#apidoc.element.sequelize.NUMBER.warn)
1.  object <span class="apidocSignatureSpan">sequelize.NUMBER.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.NUMBER.</span>key

#### [module sequelize.NUMBER.prototype](#apidoc.module.sequelize.NUMBER.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.NUMBER.prototype.</span>toSql ()](#apidoc.element.sequelize.NUMBER.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.NUMBER.prototype.</span>validate (value)](#apidoc.element.sequelize.NUMBER.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.NUMBER.prototype.</span>key

#### [module sequelize.Promise](#apidoc.module.sequelize.Promise)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Promise (executor)](#apidoc.element.sequelize.Promise.Promise)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>AggregateError (message)](#apidoc.element.sequelize.Promise.AggregateError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>CancellationError (message)](#apidoc.element.sequelize.Promise.CancellationError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>OperationalError (message)](#apidoc.element.sequelize.Promise.OperationalError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>PromiseInspection (promise)](#apidoc.element.sequelize.Promise.PromiseInspection)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>RangeError ()](#apidoc.element.sequelize.Promise.RangeError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>RejectionError (message)](#apidoc.element.sequelize.Promise.RejectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>Sequelize (database, username, password, options)](#apidoc.element.sequelize.Promise.Sequelize)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>TimeoutError (message)](#apidoc.element.sequelize.Promise.TimeoutError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>TypeError ()](#apidoc.element.sequelize.Promise.TypeError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>_SomePromiseArray (values)](#apidoc.element.sequelize.Promise._SomePromiseArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>_peekContext ()](#apidoc.element.sequelize.Promise._peekContext)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>all (promises)](#apidoc.element.sequelize.Promise.all)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>any (promises)](#apidoc.element.sequelize.Promise.any)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>attempt (fn)](#apidoc.element.sequelize.Promise.attempt)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>bind (thisArg, value)](#apidoc.element.sequelize.Promise.bind)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>cast (obj)](#apidoc.element.sequelize.Promise.cast)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>config (opts)](#apidoc.element.sequelize.Promise.config)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.sequelize.Promise.coroutine)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>defer ()](#apidoc.element.sequelize.Promise.defer)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>delay (ms, value)](#apidoc.element.sequelize.Promise.delay)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>each ()](#apidoc.element.sequelize.Promise.each)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>filter ()](#apidoc.element.sequelize.Promise.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>fromCallback (fn)](#apidoc.element.sequelize.Promise.fromCallback)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>fromNode (fn)](#apidoc.element.sequelize.Promise.fromNode)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>fulfilled (obj)](#apidoc.element.sequelize.Promise.fulfilled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>getNewLibraryCopy ()](#apidoc.element.sequelize.Promise.getNewLibraryCopy)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>hasLongStackTraces ()](#apidoc.element.sequelize.Promise.hasLongStackTraces)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>is (val)](#apidoc.element.sequelize.Promise.is)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>join ()](#apidoc.element.sequelize.Promise.join)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>longStackTraces ()](#apidoc.element.sequelize.Promise.longStackTraces)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>map ()](#apidoc.element.sequelize.Promise.map)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>mapSeries ()](#apidoc.element.sequelize.Promise.mapSeries)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>method (fn)](#apidoc.element.sequelize.Promise.method)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>noConflict ()](#apidoc.element.sequelize.Promise.noConflict)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>onPossiblyUnhandledRejection (fn)](#apidoc.element.sequelize.Promise.onPossiblyUnhandledRejection)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>onUnhandledRejectionHandled (fn)](#apidoc.element.sequelize.Promise.onUnhandledRejectionHandled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>pending ()](#apidoc.element.sequelize.Promise.pending)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>promisify (fn, options)](#apidoc.element.sequelize.Promise.promisify)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>promisifyAll (target, options)](#apidoc.element.sequelize.Promise.promisifyAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>props (promises)](#apidoc.element.sequelize.Promise.props)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>race (promises)](#apidoc.element.sequelize.Promise.race)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>reduce ()](#apidoc.element.sequelize.Promise.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>reject (reason)](#apidoc.element.sequelize.Promise.reject)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>rejected (reason)](#apidoc.element.sequelize.Promise.rejected)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>resolve (obj)](#apidoc.element.sequelize.Promise.resolve)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>setScheduler (fn)](#apidoc.element.sequelize.Promise.setScheduler)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>settle (promises)](#apidoc.element.sequelize.Promise.settle)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>some (promises, howMany)](#apidoc.element.sequelize.Promise.some)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>spawn (generatorFunction)](#apidoc.element.sequelize.Promise.spawn)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>try (fn)](#apidoc.element.sequelize.Promise.try)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>using ()](#apidoc.element.sequelize.Promise.using)
1.  string <span class="apidocSignatureSpan">sequelize.Promise.</span>version

#### [module sequelize.Promise.AggregateError](#apidoc.module.sequelize.Promise.AggregateError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>AggregateError (message)](#apidoc.element.sequelize.Promise.AggregateError.AggregateError)

#### [module sequelize.Promise.AggregateError.prototype](#apidoc.module.sequelize.Promise.AggregateError.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>isOperational
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.AggregateError.prototype.constructor)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>every ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.every)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>filter ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>forEach ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>indexOf ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.indexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>join ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.join)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>lastIndexOf ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.lastIndexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>map ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.map)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>pop ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.pop)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>push ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.push)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>reduce ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>reduceRight ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.reduceRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>reverse ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.reverse)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>shift ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.shift)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>slice ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.slice)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>some ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.some)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>sort ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.sort)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>toString ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.toString)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>unshift ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.unshift)
1.  number <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>length

#### [module sequelize.Promise.CancellationError](#apidoc.module.sequelize.Promise.CancellationError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>CancellationError (message)](#apidoc.element.sequelize.Promise.CancellationError.CancellationError)

#### [module sequelize.Promise.CancellationError.prototype](#apidoc.module.sequelize.Promise.CancellationError.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.CancellationError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.CancellationError.prototype.constructor)

#### [module sequelize.Promise.OperationalError](#apidoc.module.sequelize.Promise.OperationalError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>OperationalError (message)](#apidoc.element.sequelize.Promise.OperationalError.OperationalError)

#### [module sequelize.Promise.OperationalError.prototype](#apidoc.module.sequelize.Promise.OperationalError.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.OperationalError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.OperationalError.prototype.constructor)

#### [module sequelize.Promise.PromiseInspection](#apidoc.module.sequelize.Promise.PromiseInspection)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>PromiseInspection (promise)](#apidoc.element.sequelize.Promise.PromiseInspection.PromiseInspection)

#### [module sequelize.Promise.PromiseInspection.prototype](#apidoc.module.sequelize.Promise.PromiseInspection.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>_settledValue ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype._settledValue)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>error ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.error)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isCancelled ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isFulfilled ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isFulfilled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isPending ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isPending)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isRejected ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isRejected)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isResolved ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isResolved)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>reason ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.reason)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>value ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.value)

#### [module sequelize.Promise.TimeoutError](#apidoc.module.sequelize.Promise.TimeoutError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>TimeoutError (message)](#apidoc.element.sequelize.Promise.TimeoutError.TimeoutError)

#### [module sequelize.Promise.TimeoutError.prototype](#apidoc.module.sequelize.Promise.TimeoutError.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.TimeoutError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.TimeoutError.prototype.constructor)

#### [module sequelize.Promise._SomePromiseArray](#apidoc.module.sequelize.Promise._SomePromiseArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>_SomePromiseArray (values)](#apidoc.element.sequelize.Promise._SomePromiseArray._SomePromiseArray)

#### [module sequelize.Promise._SomePromiseArray.prototype](#apidoc.module.sequelize.Promise._SomePromiseArray.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_addFulfilled (value)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._addFulfilled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_addRejected (reason)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._addRejected)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_canPossiblyFulfill ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._canPossiblyFulfill)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_checkOutcome ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._checkOutcome)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_fulfilled ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._fulfilled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_getRangeError (count)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._getRangeError)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_init ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._init)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_promiseCancelled ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_promiseFulfilled (value)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseFulfilled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_promiseRejected (reason)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseRejected)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_rejected ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._rejected)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_resolveEmptyArray ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._resolveEmptyArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>constructor (values)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.constructor)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>howMany ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.howMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>init ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.init)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>setHowMany (count)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.setHowMany)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>setUnwrap ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.setUnwrap)

#### [module sequelize.Promise.coroutine](#apidoc.module.sequelize.Promise.coroutine)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.sequelize.Promise.coroutine.coroutine)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.coroutine.</span>addYieldHandler (fn)](#apidoc.element.sequelize.Promise.coroutine.addYieldHandler)

#### [module sequelize.Promise.each](#apidoc.module.sequelize.Promise.each)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.each.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>each ()](#apidoc.element.sequelize.Promise.each.each)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.each.</span>__original (promises, fn)](#apidoc.element.sequelize.Promise.each.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.each.</span>__unwrap ()](#apidoc.element.sequelize.Promise.each.__unwrap)

#### [module sequelize.Promise.filter](#apidoc.module.sequelize.Promise.filter)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.filter.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>filter ()](#apidoc.element.sequelize.Promise.filter.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.filter.</span>__original (promises, fn, options)](#apidoc.element.sequelize.Promise.filter.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.filter.</span>__unwrap ()](#apidoc.element.sequelize.Promise.filter.__unwrap)

#### [module sequelize.Promise.join](#apidoc.module.sequelize.Promise.join)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.join.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>join ()](#apidoc.element.sequelize.Promise.join.join)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.join.</span>__original ()](#apidoc.element.sequelize.Promise.join.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.join.</span>__unwrap ()](#apidoc.element.sequelize.Promise.join.__unwrap)

#### [module sequelize.Promise.map](#apidoc.module.sequelize.Promise.map)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.map.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>map ()](#apidoc.element.sequelize.Promise.map.map)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.map.</span>__original (promises, fn, options, _filter)](#apidoc.element.sequelize.Promise.map.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.map.</span>__unwrap ()](#apidoc.element.sequelize.Promise.map.__unwrap)

#### [module sequelize.Promise.mapSeries](#apidoc.module.sequelize.Promise.mapSeries)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.mapSeries.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>mapSeries ()](#apidoc.element.sequelize.Promise.mapSeries.mapSeries)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.mapSeries.</span>__original (promises, fn)](#apidoc.element.sequelize.Promise.mapSeries.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.mapSeries.</span>__unwrap ()](#apidoc.element.sequelize.Promise.mapSeries.__unwrap)

#### [module sequelize.Promise.prototype](#apidoc.module.sequelize.Promise.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>__isCancelled ()](#apidoc.element.sequelize.Promise.prototype.__isCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_addCallbacks ( fulfill, reject, promise, receiver, domain )](#apidoc.element.sequelize.Promise.prototype._addCallbacks)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_attachCancellationCallback (onCancel)](#apidoc.element.sequelize.Promise.prototype._attachCancellationCallback)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_attachExtraTrace ()](#apidoc.element.sequelize.Promise.prototype._attachExtraTrace)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_boundValue ()](#apidoc.element.sequelize.Promise.prototype._boundValue)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_branchHasCancelled ()](#apidoc.element.sequelize.Promise.prototype._branchHasCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancel ()](#apidoc.element.sequelize.Promise.prototype._cancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancelBranched ()](#apidoc.element.sequelize.Promise.prototype._cancelBranched)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancelBy (canceller)](#apidoc.element.sequelize.Promise.prototype._cancelBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancelPromises ()](#apidoc.element.sequelize.Promise.prototype._cancelPromises)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_captureStackTrace ()](#apidoc.element.sequelize.Promise.prototype._captureStackTrace)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_clearCallbackDataAtIndex (index)](#apidoc.element.sequelize.Promise.prototype._clearCallbackDataAtIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_clearCancellationData ()](#apidoc.element.sequelize.Promise.prototype._clearCancellationData)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_doInvokeOnCancel (onCancelCallback, internalOnly)](#apidoc.element.sequelize.Promise.prototype._doInvokeOnCancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_enoughBranchesHaveCancelled ()](#apidoc.element.sequelize.Promise.prototype._enoughBranchesHaveCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_ensurePossibleRejectionHandled ()](#apidoc.element.sequelize.Promise.prototype._ensurePossibleRejectionHandled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_execute (executor, resolve, reject)](#apidoc.element.sequelize.Promise.prototype._execute)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fireEvent ()](#apidoc.element.sequelize.Promise.prototype._fireEvent)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_followee ()](#apidoc.element.sequelize.Promise.prototype._followee)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fulfill (value)](#apidoc.element.sequelize.Promise.prototype._fulfill)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fulfillPromises (len, value)](#apidoc.element.sequelize.Promise.prototype._fulfillPromises)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fulfillmentHandlerAt (index)](#apidoc.element.sequelize.Promise.prototype._fulfillmentHandlerAt)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_getDisposer ()](#apidoc.element.sequelize.Promise.prototype._getDisposer)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_invokeInternalOnCancel ()](#apidoc.element.sequelize.Promise.prototype._invokeInternalOnCancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_invokeOnCancel ()](#apidoc.element.sequelize.Promise.prototype._invokeOnCancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isBound ()](#apidoc.element.sequelize.Promise.prototype._isBound)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isCancellable ()](#apidoc.element.sequelize.Promise.prototype._isCancellable)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isCancelled ()](#apidoc.element.sequelize.Promise.prototype._isCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isDisposable ()](#apidoc.element.sequelize.Promise.prototype._isDisposable)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isFateSealed ()](#apidoc.element.sequelize.Promise.prototype._isFateSealed)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isFinal ()](#apidoc.element.sequelize.Promise.prototype._isFinal)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isFollowing ()](#apidoc.element.sequelize.Promise.prototype._isFollowing)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isRejectionUnhandled ()](#apidoc.element.sequelize.Promise.prototype._isRejectionUnhandled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isUnhandledRejectionNotified ()](#apidoc.element.sequelize.Promise.prototype._isUnhandledRejectionNotified)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_length ()](#apidoc.element.sequelize.Promise.prototype._length)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_migrateCallback0 (follower)](#apidoc.element.sequelize.Promise.prototype._migrateCallback0)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_migrateCallbackAt (follower, index)](#apidoc.element.sequelize.Promise.prototype._migrateCallbackAt)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_notifyUnhandledRejection ()](#apidoc.element.sequelize.Promise.prototype._notifyUnhandledRejection)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_notifyUnhandledRejectionIsHandled ()](#apidoc.element.sequelize.Promise.prototype._notifyUnhandledRejectionIsHandled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_onCancel ()](#apidoc.element.sequelize.Promise.prototype._onCancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_passThrough (handler, type, success, fail)](#apidoc.element.sequelize.Promise.prototype._passThrough)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_peekContext ()](#apidoc.element.sequelize.Promise.prototype._peekContext)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_popContext ()](#apidoc.element.sequelize.Promise.prototype._popContext)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_promiseAt (index)](#apidoc.element.sequelize.Promise.prototype._promiseAt)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_promiseCreated ()](#apidoc.element.sequelize.Promise.prototype._promiseCreated)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_propagateFrom (parent, flags)](#apidoc.element.sequelize.Promise.prototype._propagateFrom)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_proxy (proxyable, arg)](#apidoc.element.sequelize.Promise.prototype._proxy)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_pushContext ()](#apidoc.element.sequelize.Promise.prototype._pushContext)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_reason ()](#apidoc.element.sequelize.Promise.prototype._reason)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_receiverAt (index)](#apidoc.element.sequelize.Promise.prototype._receiverAt)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_reject (reason)](#apidoc.element.sequelize.Promise.prototype._reject)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_rejectCallback (reason, synchronous, ignoreNonErrorWarnings)](#apidoc.element.sequelize.Promise.prototype._rejectCallback)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_rejectPromises (len, reason)](#apidoc.element.sequelize.Promise.prototype._rejectPromises)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_rejectionHandlerAt (index)](#apidoc.element.sequelize.Promise.prototype._rejectionHandlerAt)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resolveCallback (value, shouldBind)](#apidoc.element.sequelize.Promise.prototype._resolveCallback)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resolveFromExecutor (executor)](#apidoc.element.sequelize.Promise.prototype._resolveFromExecutor)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resolveFromSyncValue (value)](#apidoc.element.sequelize.Promise.prototype._resolveFromSyncValue)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resultCancelled ()](#apidoc.element.sequelize.Promise.prototype._resultCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_returnedNonUndefined ()](#apidoc.element.sequelize.Promise.prototype._returnedNonUndefined)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setAsyncGuaranteed ()](#apidoc.element.sequelize.Promise.prototype._setAsyncGuaranteed)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setBoundTo (obj)](#apidoc.element.sequelize.Promise.prototype._setBoundTo)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setCancelled ()](#apidoc.element.sequelize.Promise.prototype._setCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setDisposable (disposer)](#apidoc.element.sequelize.Promise.prototype._setDisposable)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setFollowee (promise)](#apidoc.element.sequelize.Promise.prototype._setFollowee)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setFollowing ()](#apidoc.element.sequelize.Promise.prototype._setFollowing)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setFulfilled ()](#apidoc.element.sequelize.Promise.prototype._setFulfilled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setIsFinal ()](#apidoc.element.sequelize.Promise.prototype._setIsFinal)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setLength (len)](#apidoc.element.sequelize.Promise.prototype._setLength)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setOnCancel (handler)](#apidoc.element.sequelize.Promise.prototype._setOnCancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setRejected ()](#apidoc.element.sequelize.Promise.prototype._setRejected)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setRejectionIsUnhandled ()](#apidoc.element.sequelize.Promise.prototype._setRejectionIsUnhandled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setReturnedNonUndefined ()](#apidoc.element.sequelize.Promise.prototype._setReturnedNonUndefined)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setUnhandledRejectionIsNotified ()](#apidoc.element.sequelize.Promise.prototype._setUnhandledRejectionIsNotified)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setWillBeCancelled ()](#apidoc.element.sequelize.Promise.prototype._setWillBeCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromise (promise, handler, receiver, value)](#apidoc.element.sequelize.Promise.prototype._settlePromise)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromise0 (handler, value, bitField)](#apidoc.element.sequelize.Promise.prototype._settlePromise0)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromiseCtx (ctx)](#apidoc.element.sequelize.Promise.prototype._settlePromiseCtx)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromiseFromHandler ( handler, receiver, value, promise )](#apidoc.element.sequelize.Promise.prototype._settlePromiseFromHandler)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromiseLateCancellationObserver (ctx)](#apidoc.element.sequelize.Promise.prototype._settlePromiseLateCancellationObserver)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromises ()](#apidoc.element.sequelize.Promise.prototype._settlePromises)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settledValue ()](#apidoc.element.sequelize.Promise.prototype._settledValue)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_target ()](#apidoc.element.sequelize.Promise.prototype._target)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_then ( didFulfill, didReject, _, receiver, internalData )](#apidoc.element.sequelize.Promise.prototype._then)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetCancelled ()](#apidoc.element.sequelize.Promise.prototype._unsetCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetDisposable ()](#apidoc.element.sequelize.Promise.prototype._unsetDisposable)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetOnCancel ()](#apidoc.element.sequelize.Promise.prototype._unsetOnCancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetRejectionIsUnhandled ()](#apidoc.element.sequelize.Promise.prototype._unsetRejectionIsUnhandled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetUnhandledRejectionIsNotified ()](#apidoc.element.sequelize.Promise.prototype._unsetUnhandledRejectionIsNotified)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_value ()](#apidoc.element.sequelize.Promise.prototype._value)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_warn (message, shouldUseOwnTrace, promise)](#apidoc.element.sequelize.Promise.prototype._warn)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>all ()](#apidoc.element.sequelize.Promise.prototype.all)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>any ()](#apidoc.element.sequelize.Promise.prototype.any)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>asCallback (nodeback, options)](#apidoc.element.sequelize.Promise.prototype.asCallback)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>bind (thisArg)](#apidoc.element.sequelize.Promise.prototype.bind)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>break ()](#apidoc.element.sequelize.Promise.prototype.break)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>call (methodName)](#apidoc.element.sequelize.Promise.prototype.call)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>cancel ()](#apidoc.element.sequelize.Promise.prototype.cancel)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catch ()](#apidoc.element.sequelize.Promise.prototype.catch)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catchReturn (value)](#apidoc.element.sequelize.Promise.prototype.catchReturn)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catchThrow (reason)](#apidoc.element.sequelize.Promise.prototype.catchThrow)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>caught (fn)](#apidoc.element.sequelize.Promise.prototype.caught)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>delay (ms)](#apidoc.element.sequelize.Promise.prototype.delay)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>disposer (fn)](#apidoc.element.sequelize.Promise.prototype.disposer)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>done ()](#apidoc.element.sequelize.Promise.prototype.done)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>each ()](#apidoc.element.sequelize.Promise.prototype.each)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>error ()](#apidoc.element.sequelize.Promise.prototype.error)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>filter ()](#apidoc.element.sequelize.Promise.prototype.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>finally ()](#apidoc.element.sequelize.Promise.prototype.finally)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>get (propertyName)](#apidoc.element.sequelize.Promise.prototype.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isCancellable ()](#apidoc.element.sequelize.Promise.prototype.isCancellable)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isCancelled ()](#apidoc.element.sequelize.Promise.prototype.isCancelled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isFulfilled ()](#apidoc.element.sequelize.Promise.prototype.isFulfilled)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isPending ()](#apidoc.element.sequelize.Promise.prototype.isPending)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isRejected ()](#apidoc.element.sequelize.Promise.prototype.isRejected)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isResolved ()](#apidoc.element.sequelize.Promise.prototype.isResolved)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>lastly (handler)](#apidoc.element.sequelize.Promise.prototype.lastly)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>map ()](#apidoc.element.sequelize.Promise.prototype.map)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>mapSeries ()](#apidoc.element.sequelize.Promise.prototype.mapSeries)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>nodeify ()](#apidoc.element.sequelize.Promise.prototype.nodeify)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>props ()](#apidoc.element.sequelize.Promise.prototype.props)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>race ()](#apidoc.element.sequelize.Promise.prototype.race)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reason ()](#apidoc.element.sequelize.Promise.prototype.reason)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reduce ()](#apidoc.element.sequelize.Promise.prototype.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reflect ()](#apidoc.element.sequelize.Promise.prototype.reflect)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>return (value)](#apidoc.element.sequelize.Promise.prototype.return)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>settle ()](#apidoc.element.sequelize.Promise.prototype.settle)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>some (howMany)](#apidoc.element.sequelize.Promise.prototype.some)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>spread ()](#apidoc.element.sequelize.Promise.prototype.spread)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>suppressUnhandledRejections ()](#apidoc.element.sequelize.Promise.prototype.suppressUnhandledRejections)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>tap ()](#apidoc.element.sequelize.Promise.prototype.tap)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>tapCatch (handlerOrPredicate)](#apidoc.element.sequelize.Promise.prototype.tapCatch)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>then ()](#apidoc.element.sequelize.Promise.prototype.then)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>thenReturn (value)](#apidoc.element.sequelize.Promise.prototype.thenReturn)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>thenThrow (reason)](#apidoc.element.sequelize.Promise.prototype.thenThrow)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>throw (reason)](#apidoc.element.sequelize.Promise.prototype.throw)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>timeout (ms, message)](#apidoc.element.sequelize.Promise.prototype.timeout)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>toJSON ()](#apidoc.element.sequelize.Promise.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>toString ()](#apidoc.element.sequelize.Promise.prototype.toString)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>value ()](#apidoc.element.sequelize.Promise.prototype.value)

#### [module sequelize.Promise.prototype.catch](#apidoc.module.sequelize.Promise.prototype.catch)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.catch.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catch ()](#apidoc.element.sequelize.Promise.prototype.catch.catch)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.catch.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.catch.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.catch.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.catch.__unwrap)

#### [module sequelize.Promise.prototype.done](#apidoc.module.sequelize.Promise.prototype.done)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.done.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>done ()](#apidoc.element.sequelize.Promise.prototype.done.done)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.done.</span>__original (didFulfill, didReject)](#apidoc.element.sequelize.Promise.prototype.done.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.done.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.done.__unwrap)

#### [module sequelize.Promise.prototype.each](#apidoc.module.sequelize.Promise.prototype.each)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.each.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>each ()](#apidoc.element.sequelize.Promise.prototype.each.each)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.each.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.each.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.each.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.each.__unwrap)

#### [module sequelize.Promise.prototype.error](#apidoc.module.sequelize.Promise.prototype.error)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.error.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>error ()](#apidoc.element.sequelize.Promise.prototype.error.error)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.error.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.error.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.error.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.error.__unwrap)

#### [module sequelize.Promise.prototype.filter](#apidoc.module.sequelize.Promise.prototype.filter)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.filter.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>filter ()](#apidoc.element.sequelize.Promise.prototype.filter.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.filter.</span>__original (fn, options)](#apidoc.element.sequelize.Promise.prototype.filter.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.filter.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.filter.__unwrap)

#### [module sequelize.Promise.prototype.finally](#apidoc.module.sequelize.Promise.prototype.finally)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.finally.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>finally ()](#apidoc.element.sequelize.Promise.prototype.finally.finally)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.finally.</span>__original (handler)](#apidoc.element.sequelize.Promise.prototype.finally.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.finally.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.finally.__unwrap)

#### [module sequelize.Promise.prototype.map](#apidoc.module.sequelize.Promise.prototype.map)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.map.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>map ()](#apidoc.element.sequelize.Promise.prototype.map.map)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.map.</span>__original (fn, options)](#apidoc.element.sequelize.Promise.prototype.map.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.map.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.map.__unwrap)

#### [module sequelize.Promise.prototype.mapSeries](#apidoc.module.sequelize.Promise.prototype.mapSeries)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.mapSeries.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>mapSeries ()](#apidoc.element.sequelize.Promise.prototype.mapSeries.mapSeries)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.mapSeries.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.mapSeries.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.mapSeries.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.mapSeries.__unwrap)

#### [module sequelize.Promise.prototype.nodeify](#apidoc.module.sequelize.Promise.prototype.nodeify)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.nodeify.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>nodeify ()](#apidoc.element.sequelize.Promise.prototype.nodeify.nodeify)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.nodeify.</span>__original (nodeback, options)](#apidoc.element.sequelize.Promise.prototype.nodeify.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.nodeify.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.nodeify.__unwrap)

#### [module sequelize.Promise.prototype.reduce](#apidoc.module.sequelize.Promise.prototype.reduce)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.reduce.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reduce ()](#apidoc.element.sequelize.Promise.prototype.reduce.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.reduce.</span>__original (fn, initialValue)](#apidoc.element.sequelize.Promise.prototype.reduce.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.reduce.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.reduce.__unwrap)

#### [module sequelize.Promise.prototype.spread](#apidoc.module.sequelize.Promise.prototype.spread)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.spread.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>spread ()](#apidoc.element.sequelize.Promise.prototype.spread.spread)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.spread.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.spread.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.spread.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.spread.__unwrap)

#### [module sequelize.Promise.prototype.tap](#apidoc.module.sequelize.Promise.prototype.tap)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.tap.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>tap ()](#apidoc.element.sequelize.Promise.prototype.tap.tap)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.tap.</span>__original (handler)](#apidoc.element.sequelize.Promise.prototype.tap.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.tap.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.tap.__unwrap)

#### [module sequelize.Promise.prototype.then](#apidoc.module.sequelize.Promise.prototype.then)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.prototype.then.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>then ()](#apidoc.element.sequelize.Promise.prototype.then.then)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.then.</span>__original (didFulfill, didReject)](#apidoc.element.sequelize.Promise.prototype.then.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.prototype.then.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.then.__unwrap)

#### [module sequelize.Promise.reduce](#apidoc.module.sequelize.Promise.reduce)
1.  boolean <span class="apidocSignatureSpan">sequelize.Promise.reduce.</span>__wrapped
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.</span>reduce ()](#apidoc.element.sequelize.Promise.reduce.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.reduce.</span>__original (promises, fn, initialValue, _each)](#apidoc.element.sequelize.Promise.reduce.__original)
1.  [function <span class="apidocSignatureSpan">sequelize.Promise.reduce.</span>__unwrap ()](#apidoc.element.sequelize.Promise.reduce.__unwrap)

#### [module sequelize.RANGE](#apidoc.module.sequelize.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>RANGE (subtype)](#apidoc.element.sequelize.RANGE.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.RANGE.</span>inherits (Constructor)](#apidoc.element.sequelize.RANGE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.RANGE.</span>super_ (options)](#apidoc.element.sequelize.RANGE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.RANGE.</span>warn (link, text)](#apidoc.element.sequelize.RANGE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.RANGE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.RANGE.</span>key

#### [module sequelize.RANGE.prototype](#apidoc.module.sequelize.RANGE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.RANGE.prototype.</span>toSql ()](#apidoc.element.sequelize.RANGE.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.RANGE.prototype.</span>validate (value)](#apidoc.element.sequelize.RANGE.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.RANGE.prototype.</span>key

#### [module sequelize.REAL](#apidoc.module.sequelize.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>REAL (length, decimals)](#apidoc.element.sequelize.REAL.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.REAL.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.REAL.</span>super_ (options)](#apidoc.element.sequelize.REAL.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.REAL.</span>warn (link, text)](#apidoc.element.sequelize.REAL.warn)
1.  object <span class="apidocSignatureSpan">sequelize.REAL.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.REAL.</span>key

#### [module sequelize.REAL.prototype](#apidoc.module.sequelize.REAL.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.REAL.prototype.</span>escape
1.  string <span class="apidocSignatureSpan">sequelize.REAL.prototype.</span>key

#### [module sequelize.STRING](#apidoc.module.sequelize.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>STRING (length, binary)](#apidoc.element.sequelize.STRING.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.STRING.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.STRING.</span>super_ (options)](#apidoc.element.sequelize.STRING.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.STRING.</span>warn (link, text)](#apidoc.element.sequelize.STRING.warn)
1.  object <span class="apidocSignatureSpan">sequelize.STRING.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.STRING.</span>key

#### [module sequelize.STRING.prototype](#apidoc.module.sequelize.STRING.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.STRING.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.STRING.prototype.</span>validate (value)](#apidoc.element.sequelize.STRING.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.STRING.prototype.</span>key

#### [module sequelize.TEXT](#apidoc.module.sequelize.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>TEXT (length)](#apidoc.element.sequelize.TEXT.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.TEXT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.TEXT.</span>super_ (options)](#apidoc.element.sequelize.TEXT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.TEXT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.TEXT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.TEXT.</span>key

#### [module sequelize.TEXT.prototype](#apidoc.module.sequelize.TEXT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.TEXT.prototype.toSql)
1.  [function <span class="apidocSignatureSpan">sequelize.TEXT.prototype.</span>validate (value)](#apidoc.element.sequelize.TEXT.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.TEXT.prototype.</span>key

#### [module sequelize.TIME](#apidoc.module.sequelize.TIME)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>TIME ()](#apidoc.element.sequelize.TIME.TIME)
1.  [function <span class="apidocSignatureSpan">sequelize.TIME.</span>inherits (Constructor)](#apidoc.element.sequelize.TIME.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.TIME.</span>super_ (options)](#apidoc.element.sequelize.TIME.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.TIME.</span>warn (link, text)](#apidoc.element.sequelize.TIME.warn)
1.  object <span class="apidocSignatureSpan">sequelize.TIME.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.TIME.</span>key

#### [module sequelize.TIME.prototype](#apidoc.module.sequelize.TIME.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.TIME.prototype.</span>toSql ()](#apidoc.element.sequelize.TIME.prototype.toSql)
1.  string <span class="apidocSignatureSpan">sequelize.TIME.prototype.</span>key

#### [module sequelize.TimeoutError](#apidoc.module.sequelize.TimeoutError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>TimeoutError (parent)](#apidoc.element.sequelize.TimeoutError.TimeoutError)
1.  [function <span class="apidocSignatureSpan">sequelize.TimeoutError.</span>super_ ()](#apidoc.element.sequelize.TimeoutError.super_)

#### [module sequelize.Transaction](#apidoc.module.sequelize.Transaction)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>Transaction (sequelize, options)](#apidoc.element.sequelize.Transaction.Transaction)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.</span>default (sequelize, options)](#apidoc.element.sequelize.Transaction.default)
1.  object <span class="apidocSignatureSpan">sequelize.Transaction.</span>ISOLATION_LEVELS
1.  object <span class="apidocSignatureSpan">sequelize.Transaction.</span>LOCK
1.  object <span class="apidocSignatureSpan">sequelize.Transaction.</span>TYPES

#### [module sequelize.Transaction.prototype](#apidoc.module.sequelize.Transaction.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>begin ()](#apidoc.element.sequelize.Transaction.prototype.begin)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>cleanup ()](#apidoc.element.sequelize.Transaction.prototype.cleanup)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>commit ()](#apidoc.element.sequelize.Transaction.prototype.commit)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>prepareEnvironment ()](#apidoc.element.sequelize.Transaction.prototype.prepareEnvironment)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>rollback ()](#apidoc.element.sequelize.Transaction.prototype.rollback)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>setAutocommit ()](#apidoc.element.sequelize.Transaction.prototype.setAutocommit)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>setDeferrable ()](#apidoc.element.sequelize.Transaction.prototype.setDeferrable)
1.  [function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>setIsolationLevel ()](#apidoc.element.sequelize.Transaction.prototype.setIsolationLevel)
1.  object <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>LOCK

#### [module sequelize.UUID](#apidoc.module.sequelize.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UUID ()](#apidoc.element.sequelize.UUID.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.UUID.</span>inherits (Constructor)](#apidoc.element.sequelize.UUID.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.UUID.</span>super_ (options)](#apidoc.element.sequelize.UUID.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.UUID.</span>warn (link, text)](#apidoc.element.sequelize.UUID.warn)
1.  object <span class="apidocSignatureSpan">sequelize.UUID.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.UUID.</span>key

#### [module sequelize.UUID.prototype](#apidoc.module.sequelize.UUID.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.UUID.prototype.</span>validate (value, options)](#apidoc.element.sequelize.UUID.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.UUID.prototype.</span>key

#### [module sequelize.UUIDV1](#apidoc.module.sequelize.UUIDV1)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UUIDV1 ()](#apidoc.element.sequelize.UUIDV1.UUIDV1)
1.  [function <span class="apidocSignatureSpan">sequelize.UUIDV1.</span>super_ (options)](#apidoc.element.sequelize.UUIDV1.super_)
1.  object <span class="apidocSignatureSpan">sequelize.UUIDV1.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.UUIDV1.</span>key

#### [module sequelize.UUIDV1.prototype](#apidoc.module.sequelize.UUIDV1.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.UUIDV1.prototype.</span>validate (value, options)](#apidoc.element.sequelize.UUIDV1.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.UUIDV1.prototype.</span>key

#### [module sequelize.UUIDV4](#apidoc.module.sequelize.UUIDV4)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UUIDV4 ()](#apidoc.element.sequelize.UUIDV4.UUIDV4)
1.  [function <span class="apidocSignatureSpan">sequelize.UUIDV4.</span>super_ (options)](#apidoc.element.sequelize.UUIDV4.super_)
1.  object <span class="apidocSignatureSpan">sequelize.UUIDV4.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.UUIDV4.</span>key

#### [module sequelize.UUIDV4.prototype](#apidoc.module.sequelize.UUIDV4.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.UUIDV4.prototype.</span>validate (value, options)](#apidoc.element.sequelize.UUIDV4.prototype.validate)
1.  string <span class="apidocSignatureSpan">sequelize.UUIDV4.prototype.</span>key

#### [module sequelize.UniqueConstraintError](#apidoc.module.sequelize.UniqueConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>UniqueConstraintError (options)](#apidoc.element.sequelize.UniqueConstraintError.UniqueConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.UniqueConstraintError.</span>super_ (message, errors)](#apidoc.element.sequelize.UniqueConstraintError.super_)

#### [module sequelize.Utils](#apidoc.module.sequelize.Utils)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>Promise (executor)](#apidoc.element.sequelize.Utils.Promise)
1.  function <span class="apidocSignatureSpan">sequelize.Utils.</span>_
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>addTicks (s, tickChar)](#apidoc.element.sequelize.Utils.addTicks)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>argsArePrimaryKeys (args, primaryKeys)](#apidoc.element.sequelize.Utils.argsArePrimaryKeys)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>camelize (str)](#apidoc.element.sequelize.Utils.camelize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>camelizeIf (string, condition)](#apidoc.element.sequelize.Utils.camelizeIf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>canTreatArrayAsAnd (arr)](#apidoc.element.sequelize.Utils.canTreatArrayAsAnd)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>cast (val, type)](#apidoc.element.sequelize.Utils.cast)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>cloneDeep (obj)](#apidoc.element.sequelize.Utils.cloneDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>col (col)](#apidoc.element.sequelize.Utils.col)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>combineTableNames (tableName1, tableName2)](#apidoc.element.sequelize.Utils.combineTableNames)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>defaultValueSchemable (value)](#apidoc.element.sequelize.Utils.defaultValueSchemable)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>fn (fn, args)](#apidoc.element.sequelize.Utils.fn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>format (arr, dialect)](#apidoc.element.sequelize.Utils.format)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>formatNamedParameters (sql, parameters, dialect)](#apidoc.element.sequelize.Utils.formatNamedParameters)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>formatReferences (obj)](#apidoc.element.sequelize.Utils.formatReferences)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>inherit (SubClass, SuperClass)](#apidoc.element.sequelize.Utils.inherit)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>isColString (value)](#apidoc.element.sequelize.Utils.isColString)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>isPrimitive (val)](#apidoc.element.sequelize.Utils.isPrimitive)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>json (conditionsOrPath, value)](#apidoc.element.sequelize.Utils.json)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>literal (val)](#apidoc.element.sequelize.Utils.literal)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>lowercaseFirst (s)](#apidoc.element.sequelize.Utils.lowercaseFirst)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapFinderOptions (options, Model)](#apidoc.element.sequelize.Utils.mapFinderOptions)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapOptionFieldNames (options, Model)](#apidoc.element.sequelize.Utils.mapOptionFieldNames)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapValueFieldNames (dataValues, fields, Model)](#apidoc.element.sequelize.Utils.mapValueFieldNames)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapWhereFieldNames (attributes, Model)](#apidoc.element.sequelize.Utils.mapWhereFieldNames)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>merge ()](#apidoc.element.sequelize.Utils.merge)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>mergeDefaults (a, b)](#apidoc.element.sequelize.Utils.mergeDefaults)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>now (dialect)](#apidoc.element.sequelize.Utils.now)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>pluralize (s)](#apidoc.element.sequelize.Utils.pluralize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>removeCommentsFromFunctionString (s)](#apidoc.element.sequelize.Utils.removeCommentsFromFunctionString)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>removeNullValuesFromHash (hash, omitNull, options)](#apidoc.element.sequelize.Utils.removeNullValuesFromHash)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>removeTicks (s, tickChar)](#apidoc.element.sequelize.Utils.removeTicks)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>singularize (s)](#apidoc.element.sequelize.Utils.singularize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>sliceArgs (args, begin)](#apidoc.element.sequelize.Utils.sliceArgs)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>spliceStr (str, index, count, add)](#apidoc.element.sequelize.Utils.spliceStr)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>stack ()](#apidoc.element.sequelize.Utils.stack)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>tick (func)](#apidoc.element.sequelize.Utils.tick)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>toDefaultValue (value)](#apidoc.element.sequelize.Utils.toDefaultValue)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>underscoredIf (string, condition)](#apidoc.element.sequelize.Utils.underscoredIf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>uppercaseFirst (s)](#apidoc.element.sequelize.Utils.uppercaseFirst)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>validateParameter (value, expectation, options)](#apidoc.element.sequelize.Utils.validateParameter)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>where (attribute, comparator, logic)](#apidoc.element.sequelize.Utils.where)
1.  object <span class="apidocSignatureSpan">sequelize.Utils.</span>inflection
1.  string <span class="apidocSignatureSpan">sequelize.Utils.</span>TICK_CHAR

#### [module sequelize.Utils._](#apidoc.module.sequelize.Utils._)
1.  function <span class="apidocSignatureSpan">sequelize.Utils.</span>_
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>add (value, other)](#apidoc.element.sequelize.Utils._.add)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>after (n, func)](#apidoc.element.sequelize.Utils._.after)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>ary (func, n, guard)](#apidoc.element.sequelize.Utils._.ary)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assign ()](#apidoc.element.sequelize.Utils._.assign)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assignIn ()](#apidoc.element.sequelize.Utils._.assignIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assignInWith ()](#apidoc.element.sequelize.Utils._.assignInWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assignWith ()](#apidoc.element.sequelize.Utils._.assignWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>at ()](#apidoc.element.sequelize.Utils._.at)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>attempt ()](#apidoc.element.sequelize.Utils._.attempt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>before (n, func)](#apidoc.element.sequelize.Utils._.before)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>bind ()](#apidoc.element.sequelize.Utils._.bind)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>bindAll ()](#apidoc.element.sequelize.Utils._.bindAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>bindKey ()](#apidoc.element.sequelize.Utils._.bindKey)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>camelCase (string)](#apidoc.element.sequelize.Utils._.camelCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>capitalize (string)](#apidoc.element.sequelize.Utils._.capitalize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>castArray ()](#apidoc.element.sequelize.Utils._.castArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>ceil (number, precision)](#apidoc.element.sequelize.Utils._.ceil)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>chain (value)](#apidoc.element.sequelize.Utils._.chain)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>chunk (array, size, guard)](#apidoc.element.sequelize.Utils._.chunk)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>clamp (number, lower, upper)](#apidoc.element.sequelize.Utils._.clamp)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>clone (value)](#apidoc.element.sequelize.Utils._.clone)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cloneDeep (value)](#apidoc.element.sequelize.Utils._.cloneDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cloneDeepWith (value, customizer)](#apidoc.element.sequelize.Utils._.cloneDeepWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cloneWith (value, customizer)](#apidoc.element.sequelize.Utils._.cloneWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>compact (array)](#apidoc.element.sequelize.Utils._.compact)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>concat ()](#apidoc.element.sequelize.Utils._.concat)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cond (pairs)](#apidoc.element.sequelize.Utils._.cond)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>conforms (source)](#apidoc.element.sequelize.Utils._.conforms)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>constant (value)](#apidoc.element.sequelize.Utils._.constant)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>countBy (collection, iteratee)](#apidoc.element.sequelize.Utils._.countBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>create (prototype, properties)](#apidoc.element.sequelize.Utils._.create)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>curry (func, arity, guard)](#apidoc.element.sequelize.Utils._.curry)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>curryRight (func, arity, guard)](#apidoc.element.sequelize.Utils._.curryRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>debounce (func, wait, options)](#apidoc.element.sequelize.Utils._.debounce)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>deburr (string)](#apidoc.element.sequelize.Utils._.deburr)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>defaults ()](#apidoc.element.sequelize.Utils._.defaults)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>defaultsDeep ()](#apidoc.element.sequelize.Utils._.defaultsDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>defer ()](#apidoc.element.sequelize.Utils._.defer)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>delay ()](#apidoc.element.sequelize.Utils._.delay)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>difference ()](#apidoc.element.sequelize.Utils._.difference)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>differenceBy ()](#apidoc.element.sequelize.Utils._.differenceBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>differenceWith ()](#apidoc.element.sequelize.Utils._.differenceWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>divide (value, other)](#apidoc.element.sequelize.Utils._.divide)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>drop (array, n, guard)](#apidoc.element.sequelize.Utils._.drop)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>dropRight (array, n, guard)](#apidoc.element.sequelize.Utils._.dropRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>dropRightWhile (array, predicate)](#apidoc.element.sequelize.Utils._.dropRightWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>dropWhile (array, predicate)](#apidoc.element.sequelize.Utils._.dropWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>each (collection, iteratee)](#apidoc.element.sequelize.Utils._.each)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>eachRight (collection, iteratee)](#apidoc.element.sequelize.Utils._.eachRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>endsWith (string, target, position)](#apidoc.element.sequelize.Utils._.endsWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>entries (object)](#apidoc.element.sequelize.Utils._.entries)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>entriesIn (object)](#apidoc.element.sequelize.Utils._.entriesIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>eq (value, other)](#apidoc.element.sequelize.Utils._.eq)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>escape (string)](#apidoc.element.sequelize.Utils._.escape)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>escapeRegExp (string)](#apidoc.element.sequelize.Utils._.escapeRegExp)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>every (collection, predicate, guard)](#apidoc.element.sequelize.Utils._.every)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>extend ()](#apidoc.element.sequelize.Utils._.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>extendWith ()](#apidoc.element.sequelize.Utils._.extendWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>fill (array, value, start, end)](#apidoc.element.sequelize.Utils._.fill)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>filter (collection, predicate)](#apidoc.element.sequelize.Utils._.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>find (collection, predicate)](#apidoc.element.sequelize.Utils._.find)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findIndex (array, predicate)](#apidoc.element.sequelize.Utils._.findIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findKey (object, predicate)](#apidoc.element.sequelize.Utils._.findKey)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findLast (collection, predicate)](#apidoc.element.sequelize.Utils._.findLast)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findLastIndex (array, predicate)](#apidoc.element.sequelize.Utils._.findLastIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findLastKey (object, predicate)](#apidoc.element.sequelize.Utils._.findLastKey)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>first (array)](#apidoc.element.sequelize.Utils._.first)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatMap (collection, iteratee)](#apidoc.element.sequelize.Utils._.flatMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatMapDeep (collection, iteratee)](#apidoc.element.sequelize.Utils._.flatMapDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatMapDepth (collection, iteratee, depth)](#apidoc.element.sequelize.Utils._.flatMapDepth)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatten (array)](#apidoc.element.sequelize.Utils._.flatten)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flattenDeep (array)](#apidoc.element.sequelize.Utils._.flattenDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flattenDepth (array, depth)](#apidoc.element.sequelize.Utils._.flattenDepth)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flip (func)](#apidoc.element.sequelize.Utils._.flip)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>floor (number, precision)](#apidoc.element.sequelize.Utils._.floor)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flow ()](#apidoc.element.sequelize.Utils._.flow)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flowRight ()](#apidoc.element.sequelize.Utils._.flowRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forEach (collection, iteratee)](#apidoc.element.sequelize.Utils._.forEach)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forEachRight (collection, iteratee)](#apidoc.element.sequelize.Utils._.forEachRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forIn (object, iteratee)](#apidoc.element.sequelize.Utils._.forIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forInRight (object, iteratee)](#apidoc.element.sequelize.Utils._.forInRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forOwn (object, iteratee)](#apidoc.element.sequelize.Utils._.forOwn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forOwnRight (object, iteratee)](#apidoc.element.sequelize.Utils._.forOwnRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>fromPairs (pairs)](#apidoc.element.sequelize.Utils._.fromPairs)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>functions (object)](#apidoc.element.sequelize.Utils._.functions)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>functionsIn (object)](#apidoc.element.sequelize.Utils._.functionsIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>get (object, path, defaultValue)](#apidoc.element.sequelize.Utils._.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>groupBy (collection, iteratee)](#apidoc.element.sequelize.Utils._.groupBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>gt (value, other)](#apidoc.element.sequelize.Utils._.gt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>gte (value, other)](#apidoc.element.sequelize.Utils._.gte)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>has (object, path)](#apidoc.element.sequelize.Utils._.has)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>hasIn (object, path)](#apidoc.element.sequelize.Utils._.hasIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>head (array)](#apidoc.element.sequelize.Utils._.head)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>identity (value)](#apidoc.element.sequelize.Utils._.identity)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>inRange (number, start, end)](#apidoc.element.sequelize.Utils._.inRange)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>includes (collection, value, fromIndex, guard)](#apidoc.element.sequelize.Utils._.includes)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>indexOf (array, value, fromIndex)](#apidoc.element.sequelize.Utils._.indexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>initial (array)](#apidoc.element.sequelize.Utils._.initial)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>intersection ()](#apidoc.element.sequelize.Utils._.intersection)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>intersectionBy ()](#apidoc.element.sequelize.Utils._.intersectionBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>intersectionWith ()](#apidoc.element.sequelize.Utils._.intersectionWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invert (object, iteratee)](#apidoc.element.sequelize.Utils._.invert)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invertBy (object, iteratee)](#apidoc.element.sequelize.Utils._.invertBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invoke ()](#apidoc.element.sequelize.Utils._.invoke)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invokeMap ()](#apidoc.element.sequelize.Utils._.invokeMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArguments (value)](#apidoc.element.sequelize.Utils._.isArguments)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArray ()](#apidoc.element.sequelize.Utils._.isArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArrayBuffer (value)](#apidoc.element.sequelize.Utils._.isArrayBuffer)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArrayLike (value)](#apidoc.element.sequelize.Utils._.isArrayLike)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArrayLikeObject (value)](#apidoc.element.sequelize.Utils._.isArrayLikeObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isBoolean (value)](#apidoc.element.sequelize.Utils._.isBoolean)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isBuffer (value)](#apidoc.element.sequelize.Utils._.isBuffer)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isDate (value)](#apidoc.element.sequelize.Utils._.isDate)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isElement (value)](#apidoc.element.sequelize.Utils._.isElement)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isEmpty (value)](#apidoc.element.sequelize.Utils._.isEmpty)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isEqual (value, other)](#apidoc.element.sequelize.Utils._.isEqual)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isEqualWith (value, other, customizer)](#apidoc.element.sequelize.Utils._.isEqualWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isError (value)](#apidoc.element.sequelize.Utils._.isError)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isFinite (value)](#apidoc.element.sequelize.Utils._.isFinite)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isFunction (value)](#apidoc.element.sequelize.Utils._.isFunction)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isInteger (value)](#apidoc.element.sequelize.Utils._.isInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isLength (value)](#apidoc.element.sequelize.Utils._.isLength)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isMap (value)](#apidoc.element.sequelize.Utils._.isMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isMatch (object, source)](#apidoc.element.sequelize.Utils._.isMatch)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isMatchWith (object, source, customizer)](#apidoc.element.sequelize.Utils._.isMatchWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNaN (value)](#apidoc.element.sequelize.Utils._.isNaN)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNative (value)](#apidoc.element.sequelize.Utils._.isNative)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNil (value)](#apidoc.element.sequelize.Utils._.isNil)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNull (value)](#apidoc.element.sequelize.Utils._.isNull)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNumber (value)](#apidoc.element.sequelize.Utils._.isNumber)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isObject (value)](#apidoc.element.sequelize.Utils._.isObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isObjectLike (value)](#apidoc.element.sequelize.Utils._.isObjectLike)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isPlainObject (value)](#apidoc.element.sequelize.Utils._.isPlainObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isRegExp (value)](#apidoc.element.sequelize.Utils._.isRegExp)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isSafeInteger (value)](#apidoc.element.sequelize.Utils._.isSafeInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isSet (value)](#apidoc.element.sequelize.Utils._.isSet)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isString (value)](#apidoc.element.sequelize.Utils._.isString)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isSymbol (value)](#apidoc.element.sequelize.Utils._.isSymbol)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isTypedArray (value)](#apidoc.element.sequelize.Utils._.isTypedArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isUndefined (value)](#apidoc.element.sequelize.Utils._.isUndefined)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isWeakMap (value)](#apidoc.element.sequelize.Utils._.isWeakMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isWeakSet (value)](#apidoc.element.sequelize.Utils._.isWeakSet)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>iteratee (func)](#apidoc.element.sequelize.Utils._.iteratee)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>join (array, separator)](#apidoc.element.sequelize.Utils._.join)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>kebabCase (string)](#apidoc.element.sequelize.Utils._.kebabCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>keyBy (collection, iteratee)](#apidoc.element.sequelize.Utils._.keyBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>keys (object)](#apidoc.element.sequelize.Utils._.keys)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>keysIn (object)](#apidoc.element.sequelize.Utils._.keysIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>last (array)](#apidoc.element.sequelize.Utils._.last)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lastIndexOf (array, value, fromIndex)](#apidoc.element.sequelize.Utils._.lastIndexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lowerCase (string)](#apidoc.element.sequelize.Utils._.lowerCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lowerFirst (string)](#apidoc.element.sequelize.Utils._.lowerFirst)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lt (value, other)](#apidoc.element.sequelize.Utils._.lt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lte (value, other)](#apidoc.element.sequelize.Utils._.lte)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>map (collection, iteratee)](#apidoc.element.sequelize.Utils._.map)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mapKeys (object, iteratee)](#apidoc.element.sequelize.Utils._.mapKeys)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mapValues (object, iteratee)](#apidoc.element.sequelize.Utils._.mapValues)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>matches (source)](#apidoc.element.sequelize.Utils._.matches)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>matchesProperty (path, srcValue)](#apidoc.element.sequelize.Utils._.matchesProperty)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>max (array)](#apidoc.element.sequelize.Utils._.max)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>maxBy (array, iteratee)](#apidoc.element.sequelize.Utils._.maxBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mean (array)](#apidoc.element.sequelize.Utils._.mean)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>meanBy (array, iteratee)](#apidoc.element.sequelize.Utils._.meanBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>memoize (func, resolver)](#apidoc.element.sequelize.Utils._.memoize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>merge ()](#apidoc.element.sequelize.Utils._.merge)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mergeWith ()](#apidoc.element.sequelize.Utils._.mergeWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>method ()](#apidoc.element.sequelize.Utils._.method)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>methodOf ()](#apidoc.element.sequelize.Utils._.methodOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>min (array)](#apidoc.element.sequelize.Utils._.min)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>minBy (array, iteratee)](#apidoc.element.sequelize.Utils._.minBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mixin (object, source, options)](#apidoc.element.sequelize.Utils._.mixin)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>multiply (value, other)](#apidoc.element.sequelize.Utils._.multiply)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>negate (predicate)](#apidoc.element.sequelize.Utils._.negate)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>noConflict ()](#apidoc.element.sequelize.Utils._.noConflict)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>noop ()](#apidoc.element.sequelize.Utils._.noop)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>now ()](#apidoc.element.sequelize.Utils._.now)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>nth (array, n)](#apidoc.element.sequelize.Utils._.nth)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>nthArg (n)](#apidoc.element.sequelize.Utils._.nthArg)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>omit ()](#apidoc.element.sequelize.Utils._.omit)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>omitBy (object, predicate)](#apidoc.element.sequelize.Utils._.omitBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>once (func)](#apidoc.element.sequelize.Utils._.once)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>orderBy (collection, iteratees, orders, guard)](#apidoc.element.sequelize.Utils._.orderBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>over ()](#apidoc.element.sequelize.Utils._.over)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>overArgs ()](#apidoc.element.sequelize.Utils._.overArgs)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>overEvery ()](#apidoc.element.sequelize.Utils._.overEvery)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>overSome ()](#apidoc.element.sequelize.Utils._.overSome)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pad (string, length, chars)](#apidoc.element.sequelize.Utils._.pad)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>padEnd (string, length, chars)](#apidoc.element.sequelize.Utils._.padEnd)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>padStart (string, length, chars)](#apidoc.element.sequelize.Utils._.padStart)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>parseInt (string, radix, guard)](#apidoc.element.sequelize.Utils._.parseInt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>partial ()](#apidoc.element.sequelize.Utils._.partial)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>partialRight ()](#apidoc.element.sequelize.Utils._.partialRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>partition (collection, iteratee)](#apidoc.element.sequelize.Utils._.partition)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pick ()](#apidoc.element.sequelize.Utils._.pick)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pickBy (object, predicate)](#apidoc.element.sequelize.Utils._.pickBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>property (path)](#apidoc.element.sequelize.Utils._.property)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>propertyOf (object)](#apidoc.element.sequelize.Utils._.propertyOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pull ()](#apidoc.element.sequelize.Utils._.pull)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAll (array, values)](#apidoc.element.sequelize.Utils._.pullAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAllBy (array, values, iteratee)](#apidoc.element.sequelize.Utils._.pullAllBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAllWith (array, values, comparator)](#apidoc.element.sequelize.Utils._.pullAllWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAt ()](#apidoc.element.sequelize.Utils._.pullAt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>random (lower, upper, floating)](#apidoc.element.sequelize.Utils._.random)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>range (start, end, step)](#apidoc.element.sequelize.Utils._.range)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>rangeRight (start, end, step)](#apidoc.element.sequelize.Utils._.rangeRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>rearg ()](#apidoc.element.sequelize.Utils._.rearg)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reduce (collection, iteratee, accumulator)](#apidoc.element.sequelize.Utils._.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reduceRight (collection, iteratee, accumulator)](#apidoc.element.sequelize.Utils._.reduceRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reject (collection, predicate)](#apidoc.element.sequelize.Utils._.reject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>remove (array, predicate)](#apidoc.element.sequelize.Utils._.remove)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>repeat (string, n, guard)](#apidoc.element.sequelize.Utils._.repeat)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>replace ()](#apidoc.element.sequelize.Utils._.replace)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>rest (func, start)](#apidoc.element.sequelize.Utils._.rest)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>result (object, path, defaultValue)](#apidoc.element.sequelize.Utils._.result)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reverse (array)](#apidoc.element.sequelize.Utils._.reverse)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>round (number, precision)](#apidoc.element.sequelize.Utils._.round)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>runInContext (context)](#apidoc.element.sequelize.Utils._.runInContext)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sample (collection)](#apidoc.element.sequelize.Utils._.sample)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sampleSize (collection, n, guard)](#apidoc.element.sequelize.Utils._.sampleSize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>set (object, path, value)](#apidoc.element.sequelize.Utils._.set)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>setWith (object, path, value, customizer)](#apidoc.element.sequelize.Utils._.setWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>shuffle (collection)](#apidoc.element.sequelize.Utils._.shuffle)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>size (collection)](#apidoc.element.sequelize.Utils._.size)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>slice (array, start, end)](#apidoc.element.sequelize.Utils._.slice)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>snakeCase (string)](#apidoc.element.sequelize.Utils._.snakeCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>some (collection, predicate, guard)](#apidoc.element.sequelize.Utils._.some)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortBy ()](#apidoc.element.sequelize.Utils._.sortBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedIndex (array, value)](#apidoc.element.sequelize.Utils._.sortedIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedIndexBy (array, value, iteratee)](#apidoc.element.sequelize.Utils._.sortedIndexBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedIndexOf (array, value)](#apidoc.element.sequelize.Utils._.sortedIndexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedLastIndex (array, value)](#apidoc.element.sequelize.Utils._.sortedLastIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedLastIndexBy (array, value, iteratee)](#apidoc.element.sequelize.Utils._.sortedLastIndexBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedLastIndexOf (array, value)](#apidoc.element.sequelize.Utils._.sortedLastIndexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedUniq (array)](#apidoc.element.sequelize.Utils._.sortedUniq)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedUniqBy (array, iteratee)](#apidoc.element.sequelize.Utils._.sortedUniqBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>split (string, separator, limit)](#apidoc.element.sequelize.Utils._.split)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>spread (func, start)](#apidoc.element.sequelize.Utils._.spread)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>startCase (string)](#apidoc.element.sequelize.Utils._.startCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>startsWith (string, target, position)](#apidoc.element.sequelize.Utils._.startsWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>subtract (value, other)](#apidoc.element.sequelize.Utils._.subtract)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sum (array)](#apidoc.element.sequelize.Utils._.sum)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sumBy (array, iteratee)](#apidoc.element.sequelize.Utils._.sumBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>tail (array)](#apidoc.element.sequelize.Utils._.tail)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>take (array, n, guard)](#apidoc.element.sequelize.Utils._.take)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>takeRight (array, n, guard)](#apidoc.element.sequelize.Utils._.takeRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>takeRightWhile (array, predicate)](#apidoc.element.sequelize.Utils._.takeRightWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>takeWhile (array, predicate)](#apidoc.element.sequelize.Utils._.takeWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>tap (value, interceptor)](#apidoc.element.sequelize.Utils._.tap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>template (string, options, guard)](#apidoc.element.sequelize.Utils._.template)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>throttle (func, wait, options)](#apidoc.element.sequelize.Utils._.throttle)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>thru (value, interceptor)](#apidoc.element.sequelize.Utils._.thru)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>times (n, iteratee)](#apidoc.element.sequelize.Utils._.times)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toArray (value)](#apidoc.element.sequelize.Utils._.toArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toFinite (value)](#apidoc.element.sequelize.Utils._.toFinite)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toInteger (value)](#apidoc.element.sequelize.Utils._.toInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toLength (value)](#apidoc.element.sequelize.Utils._.toLength)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toLower (value)](#apidoc.element.sequelize.Utils._.toLower)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toNumber (value)](#apidoc.element.sequelize.Utils._.toNumber)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPairs (object)](#apidoc.element.sequelize.Utils._.toPairs)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPairsIn (object)](#apidoc.element.sequelize.Utils._.toPairsIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPath (value)](#apidoc.element.sequelize.Utils._.toPath)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPlainObject (value)](#apidoc.element.sequelize.Utils._.toPlainObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toSafeInteger (value)](#apidoc.element.sequelize.Utils._.toSafeInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toString (value)](#apidoc.element.sequelize.Utils._.toString)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toUpper (value)](#apidoc.element.sequelize.Utils._.toUpper)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>transform (object, iteratee, accumulator)](#apidoc.element.sequelize.Utils._.transform)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>trim (string, chars, guard)](#apidoc.element.sequelize.Utils._.trim)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>trimEnd (string, chars, guard)](#apidoc.element.sequelize.Utils._.trimEnd)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>trimStart (string, chars, guard)](#apidoc.element.sequelize.Utils._.trimStart)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>truncate (string, options)](#apidoc.element.sequelize.Utils._.truncate)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unary (func)](#apidoc.element.sequelize.Utils._.unary)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unescape (string)](#apidoc.element.sequelize.Utils._.unescape)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>union ()](#apidoc.element.sequelize.Utils._.union)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unionBy ()](#apidoc.element.sequelize.Utils._.unionBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unionWith ()](#apidoc.element.sequelize.Utils._.unionWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniq (array)](#apidoc.element.sequelize.Utils._.uniq)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniqBy (array, iteratee)](#apidoc.element.sequelize.Utils._.uniqBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniqWith (array, comparator)](#apidoc.element.sequelize.Utils._.uniqWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniqueId (prefix)](#apidoc.element.sequelize.Utils._.uniqueId)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unset (object, path)](#apidoc.element.sequelize.Utils._.unset)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unzip (array)](#apidoc.element.sequelize.Utils._.unzip)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unzipWith (array, iteratee)](#apidoc.element.sequelize.Utils._.unzipWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>update (object, path, updater)](#apidoc.element.sequelize.Utils._.update)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>updateWith (object, path, updater, customizer)](#apidoc.element.sequelize.Utils._.updateWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>upperCase (string)](#apidoc.element.sequelize.Utils._.upperCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>upperFirst (string)](#apidoc.element.sequelize.Utils._.upperFirst)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>values (object)](#apidoc.element.sequelize.Utils._.values)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>valuesIn (object)](#apidoc.element.sequelize.Utils._.valuesIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>without ()](#apidoc.element.sequelize.Utils._.without)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>words (string, pattern, guard)](#apidoc.element.sequelize.Utils._.words)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>wrap (value, wrapper)](#apidoc.element.sequelize.Utils._.wrap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>xor ()](#apidoc.element.sequelize.Utils._.xor)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>xorBy ()](#apidoc.element.sequelize.Utils._.xorBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>xorWith ()](#apidoc.element.sequelize.Utils._.xorWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zip ()](#apidoc.element.sequelize.Utils._.zip)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zipObject (props, values)](#apidoc.element.sequelize.Utils._.zipObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zipObjectDeep (props, values)](#apidoc.element.sequelize.Utils._.zipObjectDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zipWith ()](#apidoc.element.sequelize.Utils._.zipWith)
1.  object <span class="apidocSignatureSpan">sequelize.Utils._.</span>templateSettings
1.  string <span class="apidocSignatureSpan">sequelize.Utils._.</span>VERSION

#### [module sequelize.Utils._.prototype](#apidoc.module.sequelize.Utils._.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>add ()](#apidoc.element.sequelize.Utils._.prototype.add)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>after ()](#apidoc.element.sequelize.Utils._.prototype.after)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>ary ()](#apidoc.element.sequelize.Utils._.prototype.ary)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assign ()](#apidoc.element.sequelize.Utils._.prototype.assign)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assignIn ()](#apidoc.element.sequelize.Utils._.prototype.assignIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assignInWith ()](#apidoc.element.sequelize.Utils._.prototype.assignInWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assignWith ()](#apidoc.element.sequelize.Utils._.prototype.assignWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>at ()](#apidoc.element.sequelize.Utils._.prototype.at)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>attempt ()](#apidoc.element.sequelize.Utils._.prototype.attempt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>before ()](#apidoc.element.sequelize.Utils._.prototype.before)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>bind ()](#apidoc.element.sequelize.Utils._.prototype.bind)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>bindAll ()](#apidoc.element.sequelize.Utils._.prototype.bindAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>bindKey ()](#apidoc.element.sequelize.Utils._.prototype.bindKey)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>camelCase ()](#apidoc.element.sequelize.Utils._.prototype.camelCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>capitalize ()](#apidoc.element.sequelize.Utils._.prototype.capitalize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>castArray ()](#apidoc.element.sequelize.Utils._.prototype.castArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>ceil ()](#apidoc.element.sequelize.Utils._.prototype.ceil)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>chain ()](#apidoc.element.sequelize.Utils._.prototype.chain)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>chunk ()](#apidoc.element.sequelize.Utils._.prototype.chunk)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>clamp ()](#apidoc.element.sequelize.Utils._.prototype.clamp)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>clone ()](#apidoc.element.sequelize.Utils._.prototype.clone)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cloneDeep ()](#apidoc.element.sequelize.Utils._.prototype.cloneDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cloneDeepWith ()](#apidoc.element.sequelize.Utils._.prototype.cloneDeepWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cloneWith ()](#apidoc.element.sequelize.Utils._.prototype.cloneWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>commit ()](#apidoc.element.sequelize.Utils._.prototype.commit)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>compact ()](#apidoc.element.sequelize.Utils._.prototype.compact)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>concat ()](#apidoc.element.sequelize.Utils._.prototype.concat)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cond ()](#apidoc.element.sequelize.Utils._.prototype.cond)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>conforms ()](#apidoc.element.sequelize.Utils._.prototype.conforms)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>constant ()](#apidoc.element.sequelize.Utils._.prototype.constant)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>countBy ()](#apidoc.element.sequelize.Utils._.prototype.countBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>create ()](#apidoc.element.sequelize.Utils._.prototype.create)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>curry ()](#apidoc.element.sequelize.Utils._.prototype.curry)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>curryRight ()](#apidoc.element.sequelize.Utils._.prototype.curryRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>debounce ()](#apidoc.element.sequelize.Utils._.prototype.debounce)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>deburr ()](#apidoc.element.sequelize.Utils._.prototype.deburr)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>defaults ()](#apidoc.element.sequelize.Utils._.prototype.defaults)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>defaultsDeep ()](#apidoc.element.sequelize.Utils._.prototype.defaultsDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>defer ()](#apidoc.element.sequelize.Utils._.prototype.defer)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>delay ()](#apidoc.element.sequelize.Utils._.prototype.delay)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>difference ()](#apidoc.element.sequelize.Utils._.prototype.difference)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>differenceBy ()](#apidoc.element.sequelize.Utils._.prototype.differenceBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>differenceWith ()](#apidoc.element.sequelize.Utils._.prototype.differenceWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>divide ()](#apidoc.element.sequelize.Utils._.prototype.divide)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>drop ()](#apidoc.element.sequelize.Utils._.prototype.drop)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>dropRight ()](#apidoc.element.sequelize.Utils._.prototype.dropRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>dropRightWhile ()](#apidoc.element.sequelize.Utils._.prototype.dropRightWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>dropWhile ()](#apidoc.element.sequelize.Utils._.prototype.dropWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>each ()](#apidoc.element.sequelize.Utils._.prototype.each)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>eachRight ()](#apidoc.element.sequelize.Utils._.prototype.eachRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>endsWith ()](#apidoc.element.sequelize.Utils._.prototype.endsWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>entries ()](#apidoc.element.sequelize.Utils._.prototype.entries)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>entriesIn ()](#apidoc.element.sequelize.Utils._.prototype.entriesIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>eq ()](#apidoc.element.sequelize.Utils._.prototype.eq)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>escape ()](#apidoc.element.sequelize.Utils._.prototype.escape)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>escapeRegExp ()](#apidoc.element.sequelize.Utils._.prototype.escapeRegExp)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>every ()](#apidoc.element.sequelize.Utils._.prototype.every)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>extend ()](#apidoc.element.sequelize.Utils._.prototype.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>extendWith ()](#apidoc.element.sequelize.Utils._.prototype.extendWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>fill ()](#apidoc.element.sequelize.Utils._.prototype.fill)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>filter ()](#apidoc.element.sequelize.Utils._.prototype.filter)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>find ()](#apidoc.element.sequelize.Utils._.prototype.find)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findIndex ()](#apidoc.element.sequelize.Utils._.prototype.findIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findKey ()](#apidoc.element.sequelize.Utils._.prototype.findKey)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findLast ()](#apidoc.element.sequelize.Utils._.prototype.findLast)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findLastIndex ()](#apidoc.element.sequelize.Utils._.prototype.findLastIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findLastKey ()](#apidoc.element.sequelize.Utils._.prototype.findLastKey)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>first ()](#apidoc.element.sequelize.Utils._.prototype.first)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatMap ()](#apidoc.element.sequelize.Utils._.prototype.flatMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatMapDeep ()](#apidoc.element.sequelize.Utils._.prototype.flatMapDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatMapDepth ()](#apidoc.element.sequelize.Utils._.prototype.flatMapDepth)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatten ()](#apidoc.element.sequelize.Utils._.prototype.flatten)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flattenDeep ()](#apidoc.element.sequelize.Utils._.prototype.flattenDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flattenDepth ()](#apidoc.element.sequelize.Utils._.prototype.flattenDepth)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flip ()](#apidoc.element.sequelize.Utils._.prototype.flip)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>floor ()](#apidoc.element.sequelize.Utils._.prototype.floor)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flow ()](#apidoc.element.sequelize.Utils._.prototype.flow)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flowRight ()](#apidoc.element.sequelize.Utils._.prototype.flowRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forEach ()](#apidoc.element.sequelize.Utils._.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forEachRight ()](#apidoc.element.sequelize.Utils._.prototype.forEachRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forIn ()](#apidoc.element.sequelize.Utils._.prototype.forIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forInRight ()](#apidoc.element.sequelize.Utils._.prototype.forInRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forOwn ()](#apidoc.element.sequelize.Utils._.prototype.forOwn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forOwnRight ()](#apidoc.element.sequelize.Utils._.prototype.forOwnRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>fromPairs ()](#apidoc.element.sequelize.Utils._.prototype.fromPairs)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>functions ()](#apidoc.element.sequelize.Utils._.prototype.functions)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>functionsIn ()](#apidoc.element.sequelize.Utils._.prototype.functionsIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>get ()](#apidoc.element.sequelize.Utils._.prototype.get)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>groupBy ()](#apidoc.element.sequelize.Utils._.prototype.groupBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>gt ()](#apidoc.element.sequelize.Utils._.prototype.gt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>gte ()](#apidoc.element.sequelize.Utils._.prototype.gte)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>has ()](#apidoc.element.sequelize.Utils._.prototype.has)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>hasIn ()](#apidoc.element.sequelize.Utils._.prototype.hasIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>head ()](#apidoc.element.sequelize.Utils._.prototype.head)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>identity ()](#apidoc.element.sequelize.Utils._.prototype.identity)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>inRange ()](#apidoc.element.sequelize.Utils._.prototype.inRange)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>includes ()](#apidoc.element.sequelize.Utils._.prototype.includes)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>indexOf ()](#apidoc.element.sequelize.Utils._.prototype.indexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>initial ()](#apidoc.element.sequelize.Utils._.prototype.initial)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>intersection ()](#apidoc.element.sequelize.Utils._.prototype.intersection)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>intersectionBy ()](#apidoc.element.sequelize.Utils._.prototype.intersectionBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>intersectionWith ()](#apidoc.element.sequelize.Utils._.prototype.intersectionWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invert ()](#apidoc.element.sequelize.Utils._.prototype.invert)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invertBy ()](#apidoc.element.sequelize.Utils._.prototype.invertBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invoke ()](#apidoc.element.sequelize.Utils._.prototype.invoke)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invokeMap ()](#apidoc.element.sequelize.Utils._.prototype.invokeMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArguments ()](#apidoc.element.sequelize.Utils._.prototype.isArguments)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArray ()](#apidoc.element.sequelize.Utils._.prototype.isArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArrayBuffer ()](#apidoc.element.sequelize.Utils._.prototype.isArrayBuffer)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArrayLike ()](#apidoc.element.sequelize.Utils._.prototype.isArrayLike)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArrayLikeObject ()](#apidoc.element.sequelize.Utils._.prototype.isArrayLikeObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isBoolean ()](#apidoc.element.sequelize.Utils._.prototype.isBoolean)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isBuffer ()](#apidoc.element.sequelize.Utils._.prototype.isBuffer)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isDate ()](#apidoc.element.sequelize.Utils._.prototype.isDate)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isElement ()](#apidoc.element.sequelize.Utils._.prototype.isElement)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isEmpty ()](#apidoc.element.sequelize.Utils._.prototype.isEmpty)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isEqual ()](#apidoc.element.sequelize.Utils._.prototype.isEqual)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isEqualWith ()](#apidoc.element.sequelize.Utils._.prototype.isEqualWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isError ()](#apidoc.element.sequelize.Utils._.prototype.isError)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isFinite ()](#apidoc.element.sequelize.Utils._.prototype.isFinite)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isFunction ()](#apidoc.element.sequelize.Utils._.prototype.isFunction)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isInteger ()](#apidoc.element.sequelize.Utils._.prototype.isInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isLength ()](#apidoc.element.sequelize.Utils._.prototype.isLength)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isMap ()](#apidoc.element.sequelize.Utils._.prototype.isMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isMatch ()](#apidoc.element.sequelize.Utils._.prototype.isMatch)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isMatchWith ()](#apidoc.element.sequelize.Utils._.prototype.isMatchWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNaN ()](#apidoc.element.sequelize.Utils._.prototype.isNaN)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNative ()](#apidoc.element.sequelize.Utils._.prototype.isNative)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNil ()](#apidoc.element.sequelize.Utils._.prototype.isNil)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNull ()](#apidoc.element.sequelize.Utils._.prototype.isNull)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNumber ()](#apidoc.element.sequelize.Utils._.prototype.isNumber)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isObject ()](#apidoc.element.sequelize.Utils._.prototype.isObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isObjectLike ()](#apidoc.element.sequelize.Utils._.prototype.isObjectLike)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isPlainObject ()](#apidoc.element.sequelize.Utils._.prototype.isPlainObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isRegExp ()](#apidoc.element.sequelize.Utils._.prototype.isRegExp)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isSafeInteger ()](#apidoc.element.sequelize.Utils._.prototype.isSafeInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isSet ()](#apidoc.element.sequelize.Utils._.prototype.isSet)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isString ()](#apidoc.element.sequelize.Utils._.prototype.isString)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isSymbol ()](#apidoc.element.sequelize.Utils._.prototype.isSymbol)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isTypedArray ()](#apidoc.element.sequelize.Utils._.prototype.isTypedArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isUndefined ()](#apidoc.element.sequelize.Utils._.prototype.isUndefined)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isWeakMap ()](#apidoc.element.sequelize.Utils._.prototype.isWeakMap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isWeakSet ()](#apidoc.element.sequelize.Utils._.prototype.isWeakSet)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>iteratee ()](#apidoc.element.sequelize.Utils._.prototype.iteratee)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>join ()](#apidoc.element.sequelize.Utils._.prototype.join)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>kebabCase ()](#apidoc.element.sequelize.Utils._.prototype.kebabCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>keyBy ()](#apidoc.element.sequelize.Utils._.prototype.keyBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>keys ()](#apidoc.element.sequelize.Utils._.prototype.keys)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>keysIn ()](#apidoc.element.sequelize.Utils._.prototype.keysIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>last ()](#apidoc.element.sequelize.Utils._.prototype.last)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lastIndexOf ()](#apidoc.element.sequelize.Utils._.prototype.lastIndexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lowerCase ()](#apidoc.element.sequelize.Utils._.prototype.lowerCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lowerFirst ()](#apidoc.element.sequelize.Utils._.prototype.lowerFirst)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lt ()](#apidoc.element.sequelize.Utils._.prototype.lt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lte ()](#apidoc.element.sequelize.Utils._.prototype.lte)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>map ()](#apidoc.element.sequelize.Utils._.prototype.map)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mapKeys ()](#apidoc.element.sequelize.Utils._.prototype.mapKeys)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mapValues ()](#apidoc.element.sequelize.Utils._.prototype.mapValues)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>matches ()](#apidoc.element.sequelize.Utils._.prototype.matches)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>matchesProperty ()](#apidoc.element.sequelize.Utils._.prototype.matchesProperty)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>max ()](#apidoc.element.sequelize.Utils._.prototype.max)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>maxBy ()](#apidoc.element.sequelize.Utils._.prototype.maxBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mean ()](#apidoc.element.sequelize.Utils._.prototype.mean)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>meanBy ()](#apidoc.element.sequelize.Utils._.prototype.meanBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>memoize ()](#apidoc.element.sequelize.Utils._.prototype.memoize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>merge ()](#apidoc.element.sequelize.Utils._.prototype.merge)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mergeWith ()](#apidoc.element.sequelize.Utils._.prototype.mergeWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>method ()](#apidoc.element.sequelize.Utils._.prototype.method)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>methodOf ()](#apidoc.element.sequelize.Utils._.prototype.methodOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>min ()](#apidoc.element.sequelize.Utils._.prototype.min)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>minBy ()](#apidoc.element.sequelize.Utils._.prototype.minBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mixin ()](#apidoc.element.sequelize.Utils._.prototype.mixin)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>multiply ()](#apidoc.element.sequelize.Utils._.prototype.multiply)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>negate ()](#apidoc.element.sequelize.Utils._.prototype.negate)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>next ()](#apidoc.element.sequelize.Utils._.prototype.next)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>noConflict ()](#apidoc.element.sequelize.Utils._.prototype.noConflict)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>noop ()](#apidoc.element.sequelize.Utils._.prototype.noop)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>now ()](#apidoc.element.sequelize.Utils._.prototype.now)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>nth ()](#apidoc.element.sequelize.Utils._.prototype.nth)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>nthArg ()](#apidoc.element.sequelize.Utils._.prototype.nthArg)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>omit ()](#apidoc.element.sequelize.Utils._.prototype.omit)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>omitBy ()](#apidoc.element.sequelize.Utils._.prototype.omitBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>once ()](#apidoc.element.sequelize.Utils._.prototype.once)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>orderBy ()](#apidoc.element.sequelize.Utils._.prototype.orderBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>over ()](#apidoc.element.sequelize.Utils._.prototype.over)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>overArgs ()](#apidoc.element.sequelize.Utils._.prototype.overArgs)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>overEvery ()](#apidoc.element.sequelize.Utils._.prototype.overEvery)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>overSome ()](#apidoc.element.sequelize.Utils._.prototype.overSome)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pad ()](#apidoc.element.sequelize.Utils._.prototype.pad)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>padEnd ()](#apidoc.element.sequelize.Utils._.prototype.padEnd)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>padStart ()](#apidoc.element.sequelize.Utils._.prototype.padStart)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>parseInt ()](#apidoc.element.sequelize.Utils._.prototype.parseInt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>partial ()](#apidoc.element.sequelize.Utils._.prototype.partial)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>partialRight ()](#apidoc.element.sequelize.Utils._.prototype.partialRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>partition ()](#apidoc.element.sequelize.Utils._.prototype.partition)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pick ()](#apidoc.element.sequelize.Utils._.prototype.pick)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pickBy ()](#apidoc.element.sequelize.Utils._.prototype.pickBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>plant (value)](#apidoc.element.sequelize.Utils._.prototype.plant)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pop ()](#apidoc.element.sequelize.Utils._.prototype.pop)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>property ()](#apidoc.element.sequelize.Utils._.prototype.property)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>propertyOf ()](#apidoc.element.sequelize.Utils._.prototype.propertyOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pull ()](#apidoc.element.sequelize.Utils._.prototype.pull)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAll ()](#apidoc.element.sequelize.Utils._.prototype.pullAll)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAllBy ()](#apidoc.element.sequelize.Utils._.prototype.pullAllBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAllWith ()](#apidoc.element.sequelize.Utils._.prototype.pullAllWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAt ()](#apidoc.element.sequelize.Utils._.prototype.pullAt)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>push ()](#apidoc.element.sequelize.Utils._.prototype.push)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>random ()](#apidoc.element.sequelize.Utils._.prototype.random)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>range ()](#apidoc.element.sequelize.Utils._.prototype.range)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>rangeRight ()](#apidoc.element.sequelize.Utils._.prototype.rangeRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>rearg ()](#apidoc.element.sequelize.Utils._.prototype.rearg)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reduce ()](#apidoc.element.sequelize.Utils._.prototype.reduce)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reduceRight ()](#apidoc.element.sequelize.Utils._.prototype.reduceRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reject ()](#apidoc.element.sequelize.Utils._.prototype.reject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>remove ()](#apidoc.element.sequelize.Utils._.prototype.remove)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>repeat ()](#apidoc.element.sequelize.Utils._.prototype.repeat)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>replace ()](#apidoc.element.sequelize.Utils._.prototype.replace)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>rest ()](#apidoc.element.sequelize.Utils._.prototype.rest)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>result ()](#apidoc.element.sequelize.Utils._.prototype.result)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reverse ()](#apidoc.element.sequelize.Utils._.prototype.reverse)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>round ()](#apidoc.element.sequelize.Utils._.prototype.round)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>runInContext ()](#apidoc.element.sequelize.Utils._.prototype.runInContext)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sample ()](#apidoc.element.sequelize.Utils._.prototype.sample)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sampleSize ()](#apidoc.element.sequelize.Utils._.prototype.sampleSize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>set ()](#apidoc.element.sequelize.Utils._.prototype.set)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>setWith ()](#apidoc.element.sequelize.Utils._.prototype.setWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>shift ()](#apidoc.element.sequelize.Utils._.prototype.shift)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>shuffle ()](#apidoc.element.sequelize.Utils._.prototype.shuffle)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>size ()](#apidoc.element.sequelize.Utils._.prototype.size)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>slice ()](#apidoc.element.sequelize.Utils._.prototype.slice)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>snakeCase ()](#apidoc.element.sequelize.Utils._.prototype.snakeCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>some ()](#apidoc.element.sequelize.Utils._.prototype.some)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sort ()](#apidoc.element.sequelize.Utils._.prototype.sort)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortBy ()](#apidoc.element.sequelize.Utils._.prototype.sortBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedIndex ()](#apidoc.element.sequelize.Utils._.prototype.sortedIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedIndexBy ()](#apidoc.element.sequelize.Utils._.prototype.sortedIndexBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedIndexOf ()](#apidoc.element.sequelize.Utils._.prototype.sortedIndexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedLastIndex ()](#apidoc.element.sequelize.Utils._.prototype.sortedLastIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedLastIndexBy ()](#apidoc.element.sequelize.Utils._.prototype.sortedLastIndexBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedLastIndexOf ()](#apidoc.element.sequelize.Utils._.prototype.sortedLastIndexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedUniq ()](#apidoc.element.sequelize.Utils._.prototype.sortedUniq)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedUniqBy ()](#apidoc.element.sequelize.Utils._.prototype.sortedUniqBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>splice ()](#apidoc.element.sequelize.Utils._.prototype.splice)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>split ()](#apidoc.element.sequelize.Utils._.prototype.split)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>spread ()](#apidoc.element.sequelize.Utils._.prototype.spread)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>startCase ()](#apidoc.element.sequelize.Utils._.prototype.startCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>startsWith ()](#apidoc.element.sequelize.Utils._.prototype.startsWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>subtract ()](#apidoc.element.sequelize.Utils._.prototype.subtract)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sum ()](#apidoc.element.sequelize.Utils._.prototype.sum)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sumBy ()](#apidoc.element.sequelize.Utils._.prototype.sumBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>tail ()](#apidoc.element.sequelize.Utils._.prototype.tail)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>take ()](#apidoc.element.sequelize.Utils._.prototype.take)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>takeRight ()](#apidoc.element.sequelize.Utils._.prototype.takeRight)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>takeRightWhile ()](#apidoc.element.sequelize.Utils._.prototype.takeRightWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>takeWhile ()](#apidoc.element.sequelize.Utils._.prototype.takeWhile)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>tap ()](#apidoc.element.sequelize.Utils._.prototype.tap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>template ()](#apidoc.element.sequelize.Utils._.prototype.template)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>throttle ()](#apidoc.element.sequelize.Utils._.prototype.throttle)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>thru ()](#apidoc.element.sequelize.Utils._.prototype.thru)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>times ()](#apidoc.element.sequelize.Utils._.prototype.times)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toArray ()](#apidoc.element.sequelize.Utils._.prototype.toArray)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toFinite ()](#apidoc.element.sequelize.Utils._.prototype.toFinite)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toInteger ()](#apidoc.element.sequelize.Utils._.prototype.toInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toJSON ()](#apidoc.element.sequelize.Utils._.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toLength ()](#apidoc.element.sequelize.Utils._.prototype.toLength)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toLower ()](#apidoc.element.sequelize.Utils._.prototype.toLower)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toNumber ()](#apidoc.element.sequelize.Utils._.prototype.toNumber)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPairs ()](#apidoc.element.sequelize.Utils._.prototype.toPairs)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPairsIn ()](#apidoc.element.sequelize.Utils._.prototype.toPairsIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPath ()](#apidoc.element.sequelize.Utils._.prototype.toPath)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPlainObject ()](#apidoc.element.sequelize.Utils._.prototype.toPlainObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toSafeInteger ()](#apidoc.element.sequelize.Utils._.prototype.toSafeInteger)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toString ()](#apidoc.element.sequelize.Utils._.prototype.toString)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toUpper ()](#apidoc.element.sequelize.Utils._.prototype.toUpper)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>transform ()](#apidoc.element.sequelize.Utils._.prototype.transform)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>trim ()](#apidoc.element.sequelize.Utils._.prototype.trim)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>trimEnd ()](#apidoc.element.sequelize.Utils._.prototype.trimEnd)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>trimStart ()](#apidoc.element.sequelize.Utils._.prototype.trimStart)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>truncate ()](#apidoc.element.sequelize.Utils._.prototype.truncate)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unary ()](#apidoc.element.sequelize.Utils._.prototype.unary)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unescape ()](#apidoc.element.sequelize.Utils._.prototype.unescape)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>union ()](#apidoc.element.sequelize.Utils._.prototype.union)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unionBy ()](#apidoc.element.sequelize.Utils._.prototype.unionBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unionWith ()](#apidoc.element.sequelize.Utils._.prototype.unionWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniq ()](#apidoc.element.sequelize.Utils._.prototype.uniq)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniqBy ()](#apidoc.element.sequelize.Utils._.prototype.uniqBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniqWith ()](#apidoc.element.sequelize.Utils._.prototype.uniqWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniqueId ()](#apidoc.element.sequelize.Utils._.prototype.uniqueId)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unset ()](#apidoc.element.sequelize.Utils._.prototype.unset)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unshift ()](#apidoc.element.sequelize.Utils._.prototype.unshift)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unzip ()](#apidoc.element.sequelize.Utils._.prototype.unzip)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unzipWith ()](#apidoc.element.sequelize.Utils._.prototype.unzipWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>update ()](#apidoc.element.sequelize.Utils._.prototype.update)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>updateWith ()](#apidoc.element.sequelize.Utils._.prototype.updateWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>upperCase ()](#apidoc.element.sequelize.Utils._.prototype.upperCase)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>upperFirst ()](#apidoc.element.sequelize.Utils._.prototype.upperFirst)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>value ()](#apidoc.element.sequelize.Utils._.prototype.value)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>valueOf ()](#apidoc.element.sequelize.Utils._.prototype.valueOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>values ()](#apidoc.element.sequelize.Utils._.prototype.values)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>valuesIn ()](#apidoc.element.sequelize.Utils._.prototype.valuesIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>without ()](#apidoc.element.sequelize.Utils._.prototype.without)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>words ()](#apidoc.element.sequelize.Utils._.prototype.words)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>wrap ()](#apidoc.element.sequelize.Utils._.prototype.wrap)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>xor ()](#apidoc.element.sequelize.Utils._.prototype.xor)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>xorBy ()](#apidoc.element.sequelize.Utils._.prototype.xorBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>xorWith ()](#apidoc.element.sequelize.Utils._.prototype.xorWith)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zip ()](#apidoc.element.sequelize.Utils._.prototype.zip)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zipObject ()](#apidoc.element.sequelize.Utils._.prototype.zipObject)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zipObjectDeep ()](#apidoc.element.sequelize.Utils._.prototype.zipObjectDeep)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zipWith ()](#apidoc.element.sequelize.Utils._.prototype.zipWith)

#### [module sequelize.Utils.fn](#apidoc.module.sequelize.Utils.fn)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.</span>fn (fn, args)](#apidoc.element.sequelize.Utils.fn.fn)

#### [module sequelize.Utils.fn.prototype](#apidoc.module.sequelize.Utils.fn.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.Utils.fn.prototype.</span>_isSequelizeMethod
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.fn.prototype.</span>clone ()](#apidoc.element.sequelize.Utils.fn.prototype.clone)

#### [module sequelize.Utils.inflection](#apidoc.module.sequelize.Utils.inflection)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>_apply_rules ( str, rules, skip, override )](#apidoc.element.sequelize.Utils.inflection._apply_rules)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>camelize ( str, low_first_letter )](#apidoc.element.sequelize.Utils.inflection.camelize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>capitalize ( str )](#apidoc.element.sequelize.Utils.inflection.capitalize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>classify ( str )](#apidoc.element.sequelize.Utils.inflection.classify)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>dasherize ( str )](#apidoc.element.sequelize.Utils.inflection.dasherize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>demodulize ( str )](#apidoc.element.sequelize.Utils.inflection.demodulize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>foreign_key ( str, drop_id_ubar )](#apidoc.element.sequelize.Utils.inflection.foreign_key)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>humanize ( str, low_first_letter )](#apidoc.element.sequelize.Utils.inflection.humanize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>indexOf ( arr, item, from_index, compare_func )](#apidoc.element.sequelize.Utils.inflection.indexOf)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>inflect ( str, count, singular, plural )](#apidoc.element.sequelize.Utils.inflection.inflect)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>ordinalize ( str )](#apidoc.element.sequelize.Utils.inflection.ordinalize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>pluralize ( str, plural )](#apidoc.element.sequelize.Utils.inflection.pluralize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>singularize ( str, singular )](#apidoc.element.sequelize.Utils.inflection.singularize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>tableize ( str )](#apidoc.element.sequelize.Utils.inflection.tableize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>titleize ( str )](#apidoc.element.sequelize.Utils.inflection.titleize)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>transform ( str, arr )](#apidoc.element.sequelize.Utils.inflection.transform)
1.  [function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>underscore ( str, all_upper_case )](#apidoc.element.sequelize.Utils.inflection.underscore)
1.  string <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>version

#### [module sequelize.VIRTUAL](#apidoc.module.sequelize.VIRTUAL)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>VIRTUAL (ReturnType, fields)](#apidoc.element.sequelize.VIRTUAL.VIRTUAL)
1.  [function <span class="apidocSignatureSpan">sequelize.VIRTUAL.</span>super_ (options)](#apidoc.element.sequelize.VIRTUAL.super_)
1.  object <span class="apidocSignatureSpan">sequelize.VIRTUAL.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.VIRTUAL.</span>key

#### [module sequelize.ValidationError](#apidoc.module.sequelize.ValidationError)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>ValidationError (message, errors)](#apidoc.element.sequelize.ValidationError.ValidationError)
1.  [function <span class="apidocSignatureSpan">sequelize.ValidationError.</span>super_ ()](#apidoc.element.sequelize.ValidationError.super_)

#### [module sequelize.ValidationError.prototype](#apidoc.module.sequelize.ValidationError.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.ValidationError.prototype.</span>get (path)](#apidoc.element.sequelize.ValidationError.prototype.get)

#### [module sequelize.Validator](#apidoc.module.sequelize.Validator)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>blacklist (str, chars)](#apidoc.element.sequelize.Validator.blacklist)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>contains (str, elem)](#apidoc.element.sequelize.Validator.contains)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>equals (str, comparison)](#apidoc.element.sequelize.Validator.equals)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>escape (str)](#apidoc.element.sequelize.Validator.escape)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>extend (name, fn)](#apidoc.element.sequelize.Validator.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>is (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.is)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAfter (str)](#apidoc.element.sequelize.Validator.isAfter)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAlpha (str)](#apidoc.element.sequelize.Validator.isAlpha)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAlphanumeric (str)](#apidoc.element.sequelize.Validator.isAlphanumeric)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAscii (str)](#apidoc.element.sequelize.Validator.isAscii)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isBase64 (str)](#apidoc.element.sequelize.Validator.isBase64)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isBefore (str)](#apidoc.element.sequelize.Validator.isBefore)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isBoolean (str)](#apidoc.element.sequelize.Validator.isBoolean)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isByteLength (str, options)](#apidoc.element.sequelize.Validator.isByteLength)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isCreditCard (str)](#apidoc.element.sequelize.Validator.isCreditCard)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isCurrency (str, options)](#apidoc.element.sequelize.Validator.isCurrency)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDataURI (str)](#apidoc.element.sequelize.Validator.isDataURI)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDate (str)](#apidoc.element.sequelize.Validator.isDate)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDecimal (str)](#apidoc.element.sequelize.Validator.isDecimal)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDivisibleBy (str, num)](#apidoc.element.sequelize.Validator.isDivisibleBy)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isEmail (str, options)](#apidoc.element.sequelize.Validator.isEmail)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isFQDN (str, options)](#apidoc.element.sequelize.Validator.isFQDN)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isFloat (str, options)](#apidoc.element.sequelize.Validator.isFloat)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isFullWidth (str)](#apidoc.element.sequelize.Validator.isFullWidth)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isHalfWidth (str)](#apidoc.element.sequelize.Validator.isHalfWidth)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isHexColor (str)](#apidoc.element.sequelize.Validator.isHexColor)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isHexadecimal (str)](#apidoc.element.sequelize.Validator.isHexadecimal)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIP (str)](#apidoc.element.sequelize.Validator.isIP)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIPv4 (str)](#apidoc.element.sequelize.Validator.isIPv4)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIPv6 (str)](#apidoc.element.sequelize.Validator.isIPv6)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isISBN (str)](#apidoc.element.sequelize.Validator.isISBN)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isISIN (str)](#apidoc.element.sequelize.Validator.isISIN)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isISO8601 (str)](#apidoc.element.sequelize.Validator.isISO8601)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIn (str, options)](#apidoc.element.sequelize.Validator.isIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isInt (str, options)](#apidoc.element.sequelize.Validator.isInt)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isJSON (str)](#apidoc.element.sequelize.Validator.isJSON)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isLength (str, options)](#apidoc.element.sequelize.Validator.isLength)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isLowercase (str)](#apidoc.element.sequelize.Validator.isLowercase)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMACAddress (str)](#apidoc.element.sequelize.Validator.isMACAddress)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMD5 (str)](#apidoc.element.sequelize.Validator.isMD5)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMobilePhone (str, locale)](#apidoc.element.sequelize.Validator.isMobilePhone)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMongoId (str)](#apidoc.element.sequelize.Validator.isMongoId)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMultibyte (str)](#apidoc.element.sequelize.Validator.isMultibyte)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isNull (str)](#apidoc.element.sequelize.Validator.isNull)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isNumeric (str)](#apidoc.element.sequelize.Validator.isNumeric)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isSurrogatePair (str)](#apidoc.element.sequelize.Validator.isSurrogatePair)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isURL (url, options)](#apidoc.element.sequelize.Validator.isURL)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isUUID (str)](#apidoc.element.sequelize.Validator.isUUID)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isUppercase (str)](#apidoc.element.sequelize.Validator.isUppercase)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isUrl (str)](#apidoc.element.sequelize.Validator.isUrl)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isVariableWidth (str)](#apidoc.element.sequelize.Validator.isVariableWidth)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>isWhitelisted (str, chars)](#apidoc.element.sequelize.Validator.isWhitelisted)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>len (str, min, max)](#apidoc.element.sequelize.Validator.len)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>ltrim (str, chars)](#apidoc.element.sequelize.Validator.ltrim)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>matches (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.matches)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>max (str, val)](#apidoc.element.sequelize.Validator.max)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>min (str, val)](#apidoc.element.sequelize.Validator.min)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>normalizeEmail (email, options)](#apidoc.element.sequelize.Validator.normalizeEmail)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>not (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.not)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>notContains (str, elem)](#apidoc.element.sequelize.Validator.notContains)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>notEmpty (str)](#apidoc.element.sequelize.Validator.notEmpty)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>notIn (str, values)](#apidoc.element.sequelize.Validator.notIn)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>notNull ()](#apidoc.element.sequelize.Validator.notNull)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>notRegex (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.notRegex)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>regex (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.regex)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>rtrim (str, chars)](#apidoc.element.sequelize.Validator.rtrim)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>stripLow (str, keep_new_lines)](#apidoc.element.sequelize.Validator.stripLow)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>toBoolean (str, strict)](#apidoc.element.sequelize.Validator.toBoolean)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>toDate (date)](#apidoc.element.sequelize.Validator.toDate)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>toFloat (str)](#apidoc.element.sequelize.Validator.toFloat)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>toInt (str, radix)](#apidoc.element.sequelize.Validator.toInt)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>toString (input)](#apidoc.element.sequelize.Validator.toString)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>trim (str, chars)](#apidoc.element.sequelize.Validator.trim)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>unescape (str)](#apidoc.element.sequelize.Validator.unescape)
1.  [function <span class="apidocSignatureSpan">sequelize.Validator.</span>whitelist (str, chars)](#apidoc.element.sequelize.Validator.whitelist)
1.  string <span class="apidocSignatureSpan">sequelize.Validator.</span>version

#### [module sequelize.errors](#apidoc.module.sequelize.errors)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>AccessDeniedError (parent)](#apidoc.element.sequelize.errors.AccessDeniedError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>BaseError ()](#apidoc.element.sequelize.errors.BaseError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>ConnectionError (parent)](#apidoc.element.sequelize.errors.ConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>ConnectionRefusedError (parent)](#apidoc.element.sequelize.errors.ConnectionRefusedError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>ConnectionTimedOutError (parent)](#apidoc.element.sequelize.errors.ConnectionTimedOutError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>DatabaseError (parent)](#apidoc.element.sequelize.errors.DatabaseError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>EmptyResultError (message)](#apidoc.element.sequelize.errors.EmptyResultError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>ExclusionConstraintError (options)](#apidoc.element.sequelize.errors.ExclusionConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>ForeignKeyConstraintError (options)](#apidoc.element.sequelize.errors.ForeignKeyConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>HostNotFoundError (parent)](#apidoc.element.sequelize.errors.HostNotFoundError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>HostNotReachableError (parent)](#apidoc.element.sequelize.errors.HostNotReachableError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>InstanceError (message)](#apidoc.element.sequelize.errors.InstanceError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>InvalidConnectionError (parent)](#apidoc.element.sequelize.errors.InvalidConnectionError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>TimeoutError (parent)](#apidoc.element.sequelize.errors.TimeoutError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>UniqueConstraintError (options)](#apidoc.element.sequelize.errors.UniqueConstraintError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>ValidationError (message, errors)](#apidoc.element.sequelize.errors.ValidationError)
1.  [function <span class="apidocSignatureSpan">sequelize.errors.</span>ValidationErrorItem (message, type, path, value)](#apidoc.element.sequelize.errors.ValidationErrorItem)

#### [module sequelize.hooks](#apidoc.module.sequelize.hooks)
1.  [function <span class="apidocSignatureSpan">sequelize.hooks.</span>applyTo (Model)](#apidoc.element.sequelize.hooks.applyTo)
1.  object <span class="apidocSignatureSpan">sequelize.</span>hooks
1.  object <span class="apidocSignatureSpan">sequelize.hooks.</span>hookAliases

#### [module sequelize.instance_validator](#apidoc.module.sequelize.instance_validator)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>instance_validator (modelInstance, options)](#apidoc.element.sequelize.instance_validator.instance_validator)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.</span>InstanceValidator (modelInstance, options)](#apidoc.element.sequelize.instance_validator.InstanceValidator)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.</span>default (modelInstance, options)](#apidoc.element.sequelize.instance_validator.default)
1.  string <span class="apidocSignatureSpan">sequelize.instance_validator.</span>RAW_KEY_NAME

#### [module sequelize.instance_validator.prototype](#apidoc.module.sequelize.instance_validator.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_builtinAttrValidate (value, field)](#apidoc.element.sequelize.instance_validator.prototype._builtinAttrValidate)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_builtinValidators ()](#apidoc.element.sequelize.instance_validator.prototype._builtinValidators)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_customValidators ()](#apidoc.element.sequelize.instance_validator.prototype._customValidators)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_extractValidatorArgs (test, validatorType, field)](#apidoc.element.sequelize.instance_validator.prototype._extractValidatorArgs)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_handleReflectedResult (field, promiseInspections)](#apidoc.element.sequelize.instance_validator.prototype._handleReflectedResult)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_invokeBuiltinValidator ()](#apidoc.element.sequelize.instance_validator.prototype._invokeBuiltinValidator)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_invokeCustomValidator ()](#apidoc.element.sequelize.instance_validator.prototype._invokeCustomValidator)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_pushError (isBuiltin, errorKey, rawError)](#apidoc.element.sequelize.instance_validator.prototype._pushError)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_validateSchema (rawAttribute, field, value)](#apidoc.element.sequelize.instance_validator.prototype._validateSchema)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>hookValidate ()](#apidoc.element.sequelize.instance_validator.prototype.hookValidate)
1.  [function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>validate ()](#apidoc.element.sequelize.instance_validator.prototype.validate)

#### [module sequelize.model_manager](#apidoc.module.sequelize.model_manager)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>model_manager (sequelize)](#apidoc.element.sequelize.model_manager.model_manager)
1.  [function <span class="apidocSignatureSpan">sequelize.model_manager.</span>ModelManager (sequelize)](#apidoc.element.sequelize.model_manager.ModelManager)
1.  [function <span class="apidocSignatureSpan">sequelize.model_manager.</span>default (sequelize)](#apidoc.element.sequelize.model_manager.default)

#### [module sequelize.model_manager.prototype](#apidoc.module.sequelize.model_manager.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>addModel (model)](#apidoc.element.sequelize.model_manager.prototype.addModel)
1.  [function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>forEachModel (iterator, options)](#apidoc.element.sequelize.model_manager.prototype.forEachModel)
1.  [function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>getModel (against, options)](#apidoc.element.sequelize.model_manager.prototype.getModel)
1.  [function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>removeModel (model)](#apidoc.element.sequelize.model_manager.prototype.removeModel)

#### [module sequelize.mssql](#apidoc.module.sequelize.mssql)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>BIGINT ()](#apidoc.element.sequelize.mssql.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>BLOB ()](#apidoc.element.sequelize.mssql.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>BOOLEAN ()](#apidoc.element.sequelize.mssql.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>DATE ()](#apidoc.element.sequelize.mssql.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>ENUM ()](#apidoc.element.sequelize.mssql.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>FLOAT ()](#apidoc.element.sequelize.mssql.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>INTEGER ()](#apidoc.element.sequelize.mssql.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>NOW ()](#apidoc.element.sequelize.mssql.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>REAL ()](#apidoc.element.sequelize.mssql.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>STRING ()](#apidoc.element.sequelize.mssql.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>TEXT ()](#apidoc.element.sequelize.mssql.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>UUID ()](#apidoc.element.sequelize.mssql.UUID)

#### [module sequelize.mssql.BIGINT](#apidoc.module.sequelize.mssql.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>BIGINT ()](#apidoc.element.sequelize.mssql.BIGINT.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>extend (oldType)](#apidoc.element.sequelize.mssql.BIGINT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.BIGINT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.mssql.BIGINT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.mssql.BIGINT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>key

#### [module sequelize.mssql.BLOB](#apidoc.module.sequelize.mssql.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>BLOB ()](#apidoc.element.sequelize.mssql.BLOB.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>extend (oldType)](#apidoc.element.sequelize.mssql.BLOB.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.BLOB.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>super_ (options)](#apidoc.element.sequelize.mssql.BLOB.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>warn (link, text)](#apidoc.element.sequelize.mssql.BLOB.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>key

#### [module sequelize.mssql.BLOB.prototype](#apidoc.module.sequelize.mssql.BLOB.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.BLOB.prototype.toSql)

#### [module sequelize.mssql.BOOLEAN](#apidoc.module.sequelize.mssql.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>BOOLEAN ()](#apidoc.element.sequelize.mssql.BOOLEAN.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>extend (oldType)](#apidoc.element.sequelize.mssql.BOOLEAN.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.BOOLEAN.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>super_ (options)](#apidoc.element.sequelize.mssql.BOOLEAN.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>warn (link, text)](#apidoc.element.sequelize.mssql.BOOLEAN.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>key

#### [module sequelize.mssql.BOOLEAN.prototype](#apidoc.module.sequelize.mssql.BOOLEAN.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.BOOLEAN.prototype.toSql)

#### [module sequelize.mssql.DATE](#apidoc.module.sequelize.mssql.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>DATE ()](#apidoc.element.sequelize.mssql.DATE.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>extend (oldType)](#apidoc.element.sequelize.mssql.DATE.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.DATE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>super_ (options)](#apidoc.element.sequelize.mssql.DATE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>warn (link, text)](#apidoc.element.sequelize.mssql.DATE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>key

#### [module sequelize.mssql.DATE.prototype](#apidoc.module.sequelize.mssql.DATE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.DATE.prototype.toSql)

#### [module sequelize.mssql.ENUM](#apidoc.module.sequelize.mssql.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>ENUM ()](#apidoc.element.sequelize.mssql.ENUM.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>extend (oldType)](#apidoc.element.sequelize.mssql.ENUM.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.ENUM.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>super_ (options)](#apidoc.element.sequelize.mssql.ENUM.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.mssql.ENUM.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>key

#### [module sequelize.mssql.ENUM.prototype](#apidoc.module.sequelize.mssql.ENUM.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.ENUM.prototype.toSql)

#### [module sequelize.mssql.FLOAT](#apidoc.module.sequelize.mssql.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>FLOAT ()](#apidoc.element.sequelize.mssql.FLOAT.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>extend (oldType)](#apidoc.element.sequelize.mssql.FLOAT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.FLOAT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.mssql.FLOAT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.mssql.FLOAT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>key

#### [module sequelize.mssql.INTEGER](#apidoc.module.sequelize.mssql.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>INTEGER ()](#apidoc.element.sequelize.mssql.INTEGER.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>extend (oldType)](#apidoc.element.sequelize.mssql.INTEGER.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.INTEGER.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.mssql.INTEGER.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.mssql.INTEGER.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>key

#### [module sequelize.mssql.NOW](#apidoc.module.sequelize.mssql.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>NOW ()](#apidoc.element.sequelize.mssql.NOW.NOW)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>extend (oldType)](#apidoc.element.sequelize.mssql.NOW.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.NOW.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>super_ (options)](#apidoc.element.sequelize.mssql.NOW.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>warn (link, text)](#apidoc.element.sequelize.mssql.NOW.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>key

#### [module sequelize.mssql.NOW.prototype](#apidoc.module.sequelize.mssql.NOW.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.NOW.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.NOW.prototype.toSql)

#### [module sequelize.mssql.REAL](#apidoc.module.sequelize.mssql.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>REAL ()](#apidoc.element.sequelize.mssql.REAL.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>extend (oldType)](#apidoc.element.sequelize.mssql.REAL.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.REAL.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>super_ (options)](#apidoc.element.sequelize.mssql.REAL.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>warn (link, text)](#apidoc.element.sequelize.mssql.REAL.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>key

#### [module sequelize.mssql.STRING](#apidoc.module.sequelize.mssql.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>STRING ()](#apidoc.element.sequelize.mssql.STRING.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>extend (oldType)](#apidoc.element.sequelize.mssql.STRING.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.STRING.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>super_ (options)](#apidoc.element.sequelize.mssql.STRING.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>warn (link, text)](#apidoc.element.sequelize.mssql.STRING.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>key

#### [module sequelize.mssql.STRING.prototype](#apidoc.module.sequelize.mssql.STRING.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.mssql.STRING.prototype.</span>escape
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.STRING.prototype.toSql)

#### [module sequelize.mssql.TEXT](#apidoc.module.sequelize.mssql.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>TEXT ()](#apidoc.element.sequelize.mssql.TEXT.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>extend (oldType)](#apidoc.element.sequelize.mssql.TEXT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.TEXT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>super_ (options)](#apidoc.element.sequelize.mssql.TEXT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.mssql.TEXT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>key

#### [module sequelize.mssql.TEXT.prototype](#apidoc.module.sequelize.mssql.TEXT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.TEXT.prototype.toSql)

#### [module sequelize.mssql.UUID](#apidoc.module.sequelize.mssql.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.</span>UUID ()](#apidoc.element.sequelize.mssql.UUID.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>extend (oldType)](#apidoc.element.sequelize.mssql.UUID.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.UUID.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>super_ (options)](#apidoc.element.sequelize.mssql.UUID.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>warn (link, text)](#apidoc.element.sequelize.mssql.UUID.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>key

#### [module sequelize.mssql.UUID.prototype](#apidoc.module.sequelize.mssql.UUID.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mssql.UUID.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.UUID.prototype.toSql)

#### [module sequelize.mysql](#apidoc.module.sequelize.mysql)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>DATE ()](#apidoc.element.sequelize.mysql.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>ENUM ()](#apidoc.element.sequelize.mysql.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>GEOMETRY ()](#apidoc.element.sequelize.mysql.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>UUID ()](#apidoc.element.sequelize.mysql.UUID)

#### [module sequelize.mysql.DATE](#apidoc.module.sequelize.mysql.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>DATE ()](#apidoc.element.sequelize.mysql.DATE.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>extend (oldType)](#apidoc.element.sequelize.mysql.DATE.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.DATE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>parse (value, options)](#apidoc.element.sequelize.mysql.DATE.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>super_ (options)](#apidoc.element.sequelize.mysql.DATE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>warn (link, text)](#apidoc.element.sequelize.mysql.DATE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>key

#### [module sequelize.mysql.DATE.prototype](#apidoc.module.sequelize.mysql.DATE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.mysql.DATE.prototype.toSql)

#### [module sequelize.mysql.ENUM](#apidoc.module.sequelize.mysql.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>ENUM ()](#apidoc.element.sequelize.mysql.ENUM.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>extend (oldType)](#apidoc.element.sequelize.mysql.ENUM.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.ENUM.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>super_ (options)](#apidoc.element.sequelize.mysql.ENUM.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.mysql.ENUM.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>key

#### [module sequelize.mysql.ENUM.prototype](#apidoc.module.sequelize.mysql.ENUM.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.prototype.</span>toSql (options)](#apidoc.element.sequelize.mysql.ENUM.prototype.toSql)

#### [module sequelize.mysql.GEOMETRY](#apidoc.module.sequelize.mysql.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>GEOMETRY ()](#apidoc.element.sequelize.mysql.GEOMETRY.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>extend (oldType)](#apidoc.element.sequelize.mysql.GEOMETRY.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.GEOMETRY.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>parse (value)](#apidoc.element.sequelize.mysql.GEOMETRY.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>super_ (options)](#apidoc.element.sequelize.mysql.GEOMETRY.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>warn (link, text)](#apidoc.element.sequelize.mysql.GEOMETRY.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>key

#### [module sequelize.mysql.GEOMETRY.prototype](#apidoc.module.sequelize.mysql.GEOMETRY.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.prototype.</span>parse (value)](#apidoc.element.sequelize.mysql.GEOMETRY.prototype.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.prototype.</span>toSql ()](#apidoc.element.sequelize.mysql.GEOMETRY.prototype.toSql)

#### [module sequelize.mysql.UUID](#apidoc.module.sequelize.mysql.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.</span>UUID ()](#apidoc.element.sequelize.mysql.UUID.UUID)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>extend (oldType)](#apidoc.element.sequelize.mysql.UUID.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.UUID.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>super_ (options)](#apidoc.element.sequelize.mysql.UUID.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>warn (link, text)](#apidoc.element.sequelize.mysql.UUID.warn)
1.  object <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>key

#### [module sequelize.mysql.UUID.prototype](#apidoc.module.sequelize.mysql.UUID.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.mysql.UUID.prototype.</span>toSql ()](#apidoc.element.sequelize.mysql.UUID.prototype.toSql)

#### [module sequelize.postgres](#apidoc.module.sequelize.postgres)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>BIGINT ()](#apidoc.element.sequelize.postgres.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>BLOB ()](#apidoc.element.sequelize.postgres.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>BOOLEAN ()](#apidoc.element.sequelize.postgres.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>CHAR ()](#apidoc.element.sequelize.postgres.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>DATE ()](#apidoc.element.sequelize.postgres.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>DECIMAL ()](#apidoc.element.sequelize.postgres.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>FLOAT ()](#apidoc.element.sequelize.postgres.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOGRAPHY ()](#apidoc.element.sequelize.postgres.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOMETRY ()](#apidoc.element.sequelize.postgres.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>HSTORE ()](#apidoc.element.sequelize.postgres.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>INTEGER ()](#apidoc.element.sequelize.postgres.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>RANGE ()](#apidoc.element.sequelize.postgres.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>REAL ()](#apidoc.element.sequelize.postgres.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>STRING ()](#apidoc.element.sequelize.postgres.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>TEXT ()](#apidoc.element.sequelize.postgres.TEXT)

#### [module sequelize.postgres.BIGINT](#apidoc.module.sequelize.postgres.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>BIGINT ()](#apidoc.element.sequelize.postgres.BIGINT.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>extend (oldType)](#apidoc.element.sequelize.postgres.BIGINT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.BIGINT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.postgres.BIGINT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.postgres.BIGINT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>key

#### [module sequelize.postgres.BLOB](#apidoc.module.sequelize.postgres.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>BLOB ()](#apidoc.element.sequelize.postgres.BLOB.BLOB)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>extend (oldType)](#apidoc.element.sequelize.postgres.BLOB.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.BLOB.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>super_ (options)](#apidoc.element.sequelize.postgres.BLOB.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>warn (link, text)](#apidoc.element.sequelize.postgres.BLOB.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>key

#### [module sequelize.postgres.BLOB.prototype](#apidoc.module.sequelize.postgres.BLOB.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.BLOB.prototype.toSql)

#### [module sequelize.postgres.BOOLEAN](#apidoc.module.sequelize.postgres.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>BOOLEAN ()](#apidoc.element.sequelize.postgres.BOOLEAN.BOOLEAN)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>extend (oldType)](#apidoc.element.sequelize.postgres.BOOLEAN.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.BOOLEAN.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>super_ (options)](#apidoc.element.sequelize.postgres.BOOLEAN.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>warn (link, text)](#apidoc.element.sequelize.postgres.BOOLEAN.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>key

#### [module sequelize.postgres.BOOLEAN.prototype](#apidoc.module.sequelize.postgres.BOOLEAN.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.BOOLEAN.prototype.toSql)

#### [module sequelize.postgres.CHAR](#apidoc.module.sequelize.postgres.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>CHAR ()](#apidoc.element.sequelize.postgres.CHAR.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>extend (oldType)](#apidoc.element.sequelize.postgres.CHAR.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.CHAR.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>super_ (options)](#apidoc.element.sequelize.postgres.CHAR.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>warn (link, text)](#apidoc.element.sequelize.postgres.CHAR.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>key

#### [module sequelize.postgres.CHAR.prototype](#apidoc.module.sequelize.postgres.CHAR.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.CHAR.prototype.toSql)

#### [module sequelize.postgres.DATE](#apidoc.module.sequelize.postgres.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>DATE ()](#apidoc.element.sequelize.postgres.DATE.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>extend (oldType)](#apidoc.element.sequelize.postgres.DATE.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.DATE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>super_ (options)](#apidoc.element.sequelize.postgres.DATE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>warn (link, text)](#apidoc.element.sequelize.postgres.DATE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>key

#### [module sequelize.postgres.DATE.prototype](#apidoc.module.sequelize.postgres.DATE.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.DATE.prototype.toSql)

#### [module sequelize.postgres.DECIMAL](#apidoc.module.sequelize.postgres.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>DECIMAL ()](#apidoc.element.sequelize.postgres.DECIMAL.DECIMAL)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>extend (oldType)](#apidoc.element.sequelize.postgres.DECIMAL.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.DECIMAL.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>parse (value)](#apidoc.element.sequelize.postgres.DECIMAL.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>super_ (options)](#apidoc.element.sequelize.postgres.DECIMAL.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>warn (link, text)](#apidoc.element.sequelize.postgres.DECIMAL.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>key

#### [module sequelize.postgres.FLOAT](#apidoc.module.sequelize.postgres.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>FLOAT ()](#apidoc.element.sequelize.postgres.FLOAT.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>extend (oldType)](#apidoc.element.sequelize.postgres.FLOAT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.FLOAT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.postgres.FLOAT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.postgres.FLOAT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>key

#### [module sequelize.postgres.GEOGRAPHY](#apidoc.module.sequelize.postgres.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOGRAPHY ()](#apidoc.element.sequelize.postgres.GEOGRAPHY.GEOGRAPHY)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>extend (oldType)](#apidoc.element.sequelize.postgres.GEOGRAPHY.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.GEOGRAPHY.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOGRAPHY.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>super_ (options)](#apidoc.element.sequelize.postgres.GEOGRAPHY.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>warn (link, text)](#apidoc.element.sequelize.postgres.GEOGRAPHY.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>key

#### [module sequelize.postgres.GEOGRAPHY.prototype](#apidoc.module.sequelize.postgres.GEOGRAPHY.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.prototype.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOGRAPHY.prototype.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.GEOGRAPHY.prototype.toSql)

#### [module sequelize.postgres.GEOMETRY](#apidoc.module.sequelize.postgres.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOMETRY ()](#apidoc.element.sequelize.postgres.GEOMETRY.GEOMETRY)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>extend (oldType)](#apidoc.element.sequelize.postgres.GEOMETRY.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.GEOMETRY.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOMETRY.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>super_ (options)](#apidoc.element.sequelize.postgres.GEOMETRY.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>warn (link, text)](#apidoc.element.sequelize.postgres.GEOMETRY.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>key

#### [module sequelize.postgres.GEOMETRY.prototype](#apidoc.module.sequelize.postgres.GEOMETRY.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.prototype.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOMETRY.prototype.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.GEOMETRY.prototype.toSql)

#### [module sequelize.postgres.HSTORE](#apidoc.module.sequelize.postgres.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>HSTORE ()](#apidoc.element.sequelize.postgres.HSTORE.HSTORE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>extend (oldType)](#apidoc.element.sequelize.postgres.HSTORE.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.HSTORE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>parse (value)](#apidoc.element.sequelize.postgres.HSTORE.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>super_ (options)](#apidoc.element.sequelize.postgres.HSTORE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>warn (link, text)](#apidoc.element.sequelize.postgres.HSTORE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>key

#### [module sequelize.postgres.HSTORE.prototype](#apidoc.module.sequelize.postgres.HSTORE.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.prototype.</span>escape

#### [module sequelize.postgres.INTEGER](#apidoc.module.sequelize.postgres.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>INTEGER ()](#apidoc.element.sequelize.postgres.INTEGER.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>extend (oldType)](#apidoc.element.sequelize.postgres.INTEGER.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.INTEGER.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>parse (value)](#apidoc.element.sequelize.postgres.INTEGER.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.postgres.INTEGER.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.postgres.INTEGER.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>key

#### [module sequelize.postgres.RANGE](#apidoc.module.sequelize.postgres.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>RANGE ()](#apidoc.element.sequelize.postgres.RANGE.RANGE)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>extend (oldType)](#apidoc.element.sequelize.postgres.RANGE.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.RANGE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>parse (value, oid, getTypeParser)](#apidoc.element.sequelize.postgres.RANGE.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>super_ (options)](#apidoc.element.sequelize.postgres.RANGE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>warn (link, text)](#apidoc.element.sequelize.postgres.RANGE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>oid_map
1.  object <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>key

#### [module sequelize.postgres.RANGE.prototype](#apidoc.module.sequelize.postgres.RANGE.prototype)
1.  boolean <span class="apidocSignatureSpan">sequelize.postgres.RANGE.prototype.</span>escape

#### [module sequelize.postgres.REAL](#apidoc.module.sequelize.postgres.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>REAL ()](#apidoc.element.sequelize.postgres.REAL.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>extend (oldType)](#apidoc.element.sequelize.postgres.REAL.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.REAL.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>super_ (options)](#apidoc.element.sequelize.postgres.REAL.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>warn (link, text)](#apidoc.element.sequelize.postgres.REAL.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>key

#### [module sequelize.postgres.STRING](#apidoc.module.sequelize.postgres.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>STRING ()](#apidoc.element.sequelize.postgres.STRING.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>extend (oldType)](#apidoc.element.sequelize.postgres.STRING.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.STRING.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>super_ (options)](#apidoc.element.sequelize.postgres.STRING.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>warn (link, text)](#apidoc.element.sequelize.postgres.STRING.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>key

#### [module sequelize.postgres.STRING.prototype](#apidoc.module.sequelize.postgres.STRING.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.STRING.prototype.toSql)

#### [module sequelize.postgres.TEXT](#apidoc.module.sequelize.postgres.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.</span>TEXT ()](#apidoc.element.sequelize.postgres.TEXT.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>extend (oldType)](#apidoc.element.sequelize.postgres.TEXT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.TEXT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>super_ (options)](#apidoc.element.sequelize.postgres.TEXT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.postgres.TEXT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>key

#### [module sequelize.postgres.TEXT.prototype](#apidoc.module.sequelize.postgres.TEXT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.TEXT.prototype.toSql)

#### [module sequelize.query_interface](#apidoc.module.sequelize.query_interface)
1.  [function <span class="apidocSignatureSpan">sequelize.</span>query_interface (sequelize)](#apidoc.element.sequelize.query_interface.query_interface)

#### [module sequelize.query_interface.prototype](#apidoc.module.sequelize.query_interface.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>addColumn (table, key, attribute, options)](#apidoc.element.sequelize.query_interface.prototype.addColumn)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>addIndex (tableName, attributes, options, rawTablename)](#apidoc.element.sequelize.query_interface.prototype.addIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>bulkDelete (tableName, identifier, options, model)](#apidoc.element.sequelize.query_interface.prototype.bulkDelete)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>bulkInsert (tableName, records, options, attributes)](#apidoc.element.sequelize.query_interface.prototype.bulkInsert)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>bulkUpdate (tableName, values, identifier, options, attributes)](#apidoc.element.sequelize.query_interface.prototype.bulkUpdate)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>changeColumn (tableName, attributeName, dataTypeOrOptions, options)](#apidoc.element.sequelize.query_interface.prototype.changeColumn)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>commitTransaction (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.commitTransaction)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createFunction (functionName, params, returnType, language, body, options)](#apidoc.element.sequelize.query_interface.prototype.createFunction)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createSchema (schema, options)](#apidoc.element.sequelize.query_interface.prototype.createSchema)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createTable (tableName, attributes, options, model)](#apidoc.element.sequelize.query_interface.prototype.createTable)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createTrigger (tableName, triggerName, timingType, fireOnArray, functionName, functionParams, optionsArray, options)](#apidoc.element.sequelize.query_interface.prototype.createTrigger)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>databaseVersion (options)](#apidoc.element.sequelize.query_interface.prototype.databaseVersion)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>deferConstraints (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.deferConstraints)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>delete (instance, tableName, identifier, options)](#apidoc.element.sequelize.query_interface.prototype.delete)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>describeTable (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.describeTable)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropAllEnums (options)](#apidoc.element.sequelize.query_interface.prototype.dropAllEnums)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropAllSchemas (options)](#apidoc.element.sequelize.query_interface.prototype.dropAllSchemas)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropAllTables (options)](#apidoc.element.sequelize.query_interface.prototype.dropAllTables)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropFunction (functionName, params, options)](#apidoc.element.sequelize.query_interface.prototype.dropFunction)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropSchema (schema, options)](#apidoc.element.sequelize.query_interface.prototype.dropSchema)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropTable (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.dropTable)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropTrigger (tableName, triggerName, options)](#apidoc.element.sequelize.query_interface.prototype.dropTrigger)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>escape (value)](#apidoc.element.sequelize.query_interface.prototype.escape)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>getForeignKeysForTables (tableNames, options)](#apidoc.element.sequelize.query_interface.prototype.getForeignKeysForTables)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>increment (instance, tableName, values, identifier, options)](#apidoc.element.sequelize.query_interface.prototype.increment)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>insert (instance, tableName, values, options)](#apidoc.element.sequelize.query_interface.prototype.insert)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>nameIndexes (indexes, rawTablename)](#apidoc.element.sequelize.query_interface.prototype.nameIndexes)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>pgListEnums (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.pgListEnums)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>quoteIdentifier (identifier, force)](#apidoc.element.sequelize.query_interface.prototype.quoteIdentifier)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>quoteIdentifiers (identifiers, force)](#apidoc.element.sequelize.query_interface.prototype.quoteIdentifiers)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>quoteTable (identifier)](#apidoc.element.sequelize.query_interface.prototype.quoteTable)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>rawSelect (tableName, options, attributeSelector, Model)](#apidoc.element.sequelize.query_interface.prototype.rawSelect)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>removeColumn (tableName, attributeName, options)](#apidoc.element.sequelize.query_interface.prototype.removeColumn)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>removeIndex (tableName, indexNameOrAttributes, options)](#apidoc.element.sequelize.query_interface.prototype.removeIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameColumn (tableName, attrNameBefore, attrNameAfter, options)](#apidoc.element.sequelize.query_interface.prototype.renameColumn)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameFunction (oldFunctionName, params, newFunctionName, options)](#apidoc.element.sequelize.query_interface.prototype.renameFunction)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameTable (before, after, options)](#apidoc.element.sequelize.query_interface.prototype.renameTable)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameTrigger (tableName, oldTriggerName, newTriggerName, options)](#apidoc.element.sequelize.query_interface.prototype.renameTrigger)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>rollbackTransaction (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.rollbackTransaction)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>select (model, tableName, options)](#apidoc.element.sequelize.query_interface.prototype.select)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>setAutocommit (transaction, value, options)](#apidoc.element.sequelize.query_interface.prototype.setAutocommit)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>setIsolationLevel (transaction, value, options)](#apidoc.element.sequelize.query_interface.prototype.setIsolationLevel)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>showAllSchemas (options)](#apidoc.element.sequelize.query_interface.prototype.showAllSchemas)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>showAllTables (options)](#apidoc.element.sequelize.query_interface.prototype.showAllTables)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>showIndex (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.showIndex)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>startTransaction (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.startTransaction)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>update (instance, tableName, values, identifier, options)](#apidoc.element.sequelize.query_interface.prototype.update)
1.  [function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>upsert (tableName, valuesByField, updateValues, where, model, options)](#apidoc.element.sequelize.query_interface.prototype.upsert)

#### [module sequelize.sql_string](#apidoc.module.sequelize.sql_string)
1.  [function <span class="apidocSignatureSpan">sequelize.sql_string.</span>escape (val, timeZone, dialect, format)](#apidoc.element.sequelize.sql_string.escape)
1.  [function <span class="apidocSignatureSpan">sequelize.sql_string.</span>escapeId (val, forbidQualified)](#apidoc.element.sequelize.sql_string.escapeId)
1.  [function <span class="apidocSignatureSpan">sequelize.sql_string.</span>format (sql, values, timeZone, dialect)](#apidoc.element.sequelize.sql_string.format)
1.  [function <span class="apidocSignatureSpan">sequelize.sql_string.</span>formatNamedParameters (sql, values, timeZone, dialect)](#apidoc.element.sequelize.sql_string.formatNamedParameters)

#### [module sequelize.sqlite](#apidoc.module.sequelize.sqlite)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>BIGINT (length)](#apidoc.element.sequelize.sqlite.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>CHAR ()](#apidoc.element.sequelize.sqlite.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>DATE ()](#apidoc.element.sequelize.sqlite.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>ENUM ()](#apidoc.element.sequelize.sqlite.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.sqlite.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>INTEGER (length)](#apidoc.element.sequelize.sqlite.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>NUMBER ()](#apidoc.element.sequelize.sqlite.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>REAL (length, decimals)](#apidoc.element.sequelize.sqlite.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>STRING ()](#apidoc.element.sequelize.sqlite.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>TEXT ()](#apidoc.element.sequelize.sqlite.TEXT)

#### [module sequelize.sqlite.BIGINT](#apidoc.module.sequelize.sqlite.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>BIGINT (length)](#apidoc.element.sequelize.sqlite.BIGINT.BIGINT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.BIGINT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.BIGINT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.sqlite.BIGINT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.BIGINT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>key

#### [module sequelize.sqlite.BIGINT.prototype](#apidoc.module.sequelize.sqlite.BIGINT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.BIGINT.prototype.toSql)
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.prototype.</span>key

#### [module sequelize.sqlite.CHAR](#apidoc.module.sequelize.sqlite.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>CHAR ()](#apidoc.element.sequelize.sqlite.CHAR.CHAR)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.CHAR.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.CHAR.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>super_ (options)](#apidoc.element.sequelize.sqlite.CHAR.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.CHAR.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>key

#### [module sequelize.sqlite.CHAR.prototype](#apidoc.module.sequelize.sqlite.CHAR.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.CHAR.prototype.toSql)

#### [module sequelize.sqlite.DATE](#apidoc.module.sequelize.sqlite.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>DATE ()](#apidoc.element.sequelize.sqlite.DATE.DATE)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.DATE.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.DATE.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>parse (date, options)](#apidoc.element.sequelize.sqlite.DATE.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>super_ (options)](#apidoc.element.sequelize.sqlite.DATE.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.DATE.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>key

#### [module sequelize.sqlite.ENUM](#apidoc.module.sequelize.sqlite.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>ENUM ()](#apidoc.element.sequelize.sqlite.ENUM.ENUM)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.ENUM.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.ENUM.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>super_ (options)](#apidoc.element.sequelize.sqlite.ENUM.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.ENUM.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>key

#### [module sequelize.sqlite.ENUM.prototype](#apidoc.module.sequelize.sqlite.ENUM.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.ENUM.prototype.toSql)

#### [module sequelize.sqlite.FLOAT](#apidoc.module.sequelize.sqlite.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.sqlite.FLOAT.FLOAT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.FLOAT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.FLOAT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>parse (value)](#apidoc.element.sequelize.sqlite.FLOAT.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.sqlite.FLOAT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.FLOAT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>key

#### [module sequelize.sqlite.FLOAT.prototype](#apidoc.module.sequelize.sqlite.FLOAT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.FLOAT.prototype.toSql)
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.prototype.</span>key

#### [module sequelize.sqlite.INTEGER](#apidoc.module.sequelize.sqlite.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>INTEGER (length)](#apidoc.element.sequelize.sqlite.INTEGER.INTEGER)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.INTEGER.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.INTEGER.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.sqlite.INTEGER.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.INTEGER.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>key

#### [module sequelize.sqlite.INTEGER.prototype](#apidoc.module.sequelize.sqlite.INTEGER.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.INTEGER.prototype.toSql)
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.prototype.</span>key

#### [module sequelize.sqlite.NUMBER](#apidoc.module.sequelize.sqlite.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>NUMBER ()](#apidoc.element.sequelize.sqlite.NUMBER.NUMBER)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.NUMBER.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.NUMBER.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>super_ (options)](#apidoc.element.sequelize.sqlite.NUMBER.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.NUMBER.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>key

#### [module sequelize.sqlite.NUMBER.prototype](#apidoc.module.sequelize.sqlite.NUMBER.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.NUMBER.prototype.toSql)

#### [module sequelize.sqlite.REAL](#apidoc.module.sequelize.sqlite.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>REAL (length, decimals)](#apidoc.element.sequelize.sqlite.REAL.REAL)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.REAL.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.REAL.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>parse (value)](#apidoc.element.sequelize.sqlite.REAL.parse)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>super_ (options)](#apidoc.element.sequelize.sqlite.REAL.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.REAL.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>key

#### [module sequelize.sqlite.REAL.prototype](#apidoc.module.sequelize.sqlite.REAL.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.REAL.prototype.toSql)
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.REAL.prototype.</span>key

#### [module sequelize.sqlite.STRING](#apidoc.module.sequelize.sqlite.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>STRING ()](#apidoc.element.sequelize.sqlite.STRING.STRING)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.STRING.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.STRING.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>super_ (options)](#apidoc.element.sequelize.sqlite.STRING.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.STRING.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>key

#### [module sequelize.sqlite.STRING.prototype](#apidoc.module.sequelize.sqlite.STRING.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.STRING.prototype.toSql)

#### [module sequelize.sqlite.TEXT](#apidoc.module.sequelize.sqlite.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.</span>TEXT ()](#apidoc.element.sequelize.sqlite.TEXT.TEXT)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.TEXT.extend)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.TEXT.inherits)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>super_ (options)](#apidoc.element.sequelize.sqlite.TEXT.super_)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.TEXT.warn)
1.  object <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>types
1.  string <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>key

#### [module sequelize.sqlite.TEXT.prototype](#apidoc.module.sequelize.sqlite.TEXT.prototype)
1.  [function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.TEXT.prototype.toSql)



# <a name="apidoc.module.sequelize"></a>[module sequelize](#apidoc.module.sequelize)

#### <a name="apidoc.element.sequelize.ABSTRACT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ABSTRACT (options)](#apidoc.element.sequelize.ABSTRACT)
- description and source-code
```javascript
ABSTRACT = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ARRAY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ARRAY (type)](#apidoc.element.sequelize.ARRAY)
- description and source-code
```javascript
ARRAY = function (type) {
  var options = _.isPlainObject(type) ? type : {
    type: type
  };
  if (!(this instanceof ARRAY)) return new ARRAY(options);
  this.type = typeof options.type === 'function' ? new options.type() : options.type;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.AccessDeniedError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>AccessDeniedError (parent)](#apidoc.element.sequelize.AccessDeniedError)
- description and source-code
```javascript
AccessDeniedError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeAccessDeniedError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Association ()](#apidoc.element.sequelize.Association)
- description and source-code
```javascript
Association = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Association.BelongsTo (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo)
- description and source-code
```javascript
Association.BelongsTo = function (source, target, options) {
  Association.call(this);

  this.associationType = 'BelongsTo';
  this.source = source;
  this.target = target;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.as, this.source.options.underscored),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.identifier = this.foreignKey;

  if (this.source.rawAttributes[this.identifier]) {
    this.identifierField = this.source.rawAttributes[this.identifier].field || this.identifier;
  }

  this.targetKey = this.options.targetKey || this.target.primaryKeyAttribute;
  this.targetKeyField = this.target.rawAttributes[this.targetKey].field || this.targetKey;
  this.targetKeyIsPrimary = this.targetKey === this.target.primaryKeyAttribute;

  this.targetIdentifier = this.targetKey;
  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false.
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of an instance to associate with
 this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to 'this.save'
     * @param {Boolean} [options.save=true] Skip saving this after setting the foreign key if false.
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Association.BelongsToMany (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany)
- description and source-code
```javascript
Association.BelongsToMany = function (source, target, options) {
  Association.call(this);

  options = options || {};

  if (options.through === undefined || options.through === true || options.through === null) {
    throw new Error('belongsToMany must be given a through option, either a string or a model');
  }

  if (!options.through.model) {
    options.through = {
      model: options.through
    };
  }

  this.associationType = 'BelongsToMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options;
  this.sequelize = source.modelManager.sequelize;
  this.through = _.assign({}, options.through);
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.doubleLinked = false;
  this.as = this.options.as;

  if (!this.as && this.isSelfAssociation) {
    throw new Error('\'as\' must be defined for many-to-many self-associations');
  }

  if (this.as) {
    this.isAliased = true;

    if (Utils._.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  this.combinedTableName = Utils.combineTableNames(
    this.source.tableName,
    this.isSelfAssociation ? (this.as || this.target.tableName) : this.target.tableName
  );

<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association - Unless we find a pairing association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  /*
   * Default/generated foreign/other keys
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else {
    if (!this.options.foreignKey) {
      this.foreignKeyDefault = true;
    }

    this.foreignKeyAttribute = {};
    this.foreignKey = this.options.foreignKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (_.isObject(this.options.otherKey)) {
    this.otherKeyAttribute = this.options.otherKey;
    this.otherKey = this.otherKeyAttribute.name || this.otherKeyAttribute.fieldName;
  } else {
    if (!this.options.otherKey) {
      this.otherKeyDefault = true;
    }

    this.otherKeyAttribute = {};
    this.otherKey = this.options.otherKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(
          this.isSelfAssociation ?
            Utils.singularize(this.as) :
            this.target.options.name.singular,
          this.target.options.underscored
        ),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.target.options.underscored
    );
  }

  /*
   * Find paired association (if exists)
   */
  _.each(this.target.associations, function(association) {
    if (association.associationType !== 'BelongsToMany') return;
    if (association.target !== this.source) return;

    if (this.options.through.model === association.options.through.model) {
      this.paired = association;
      association.paired = this;
    }
  }.bind(this));

  if (typeof this.through.model === 'string') {
    if (!this.sequelize.isDefined(this.through.model)) {
      this.through.model = this.sequelize.define(this.through.model, {}, _.extend(this.options, {
        tableName: this.through.model,
        indexes: [], //we don't want indexes here (as referenced in #2416)
        paranoid: false,  // A paranoid join table does not make sense
        validate: {} // Don't propagate model-level validations
      }));
    } else {
      this.through.model = this.sequelize.model(this.through.model);
    }
  }

  this.options = _.assign(this.options, _.pick(this.through.model.options, [
    'timestamps', 'createdAt', 'updatedAt ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Association.HasMany (source, target, options)](#apidoc.element.sequelize.Association.HasMany)
- description and source-code
```javascript
Association.HasMany = function (source, target, options) {
  Association.call(this);

  this.associationType = 'HasMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options || {};
  this.sequelize = source.modelManager.sequelize;
  this.through = options.through;
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.options.through) {
    throw new Error('N:M associations are not supported with hasMany. Use belongsToMany instead');
  }


<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  if (this.as) {
    this.isAliased = true;

    if (_.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  /*
   * Foreign key setup
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
    this.foreignKeyField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  this.sourceKey = this.options.sourceKey || this.source.primaryKeyAttribute;
  if (this.target.rawAttributes[this.sourceKey]) {
    this.sourceKeyField = this.source.rawAttributes[this.sourceKey].field || this.sourceKey;
  } else {
    this.sourceKeyField = this.sourceKey;
  }

  if (this.source.fieldRawAttributesMap[this.sourceKey]) {
    this.sourceKeyAttribute = this.source.fieldRawAttributesMap[this.sourceKey].fieldName;
  } else {
    this.sourceKeyAttribute = this.source.primaryKeyAttribute;
  }
  this.sourceIdentifier = this.sourceKey;
  this.associationAccessor = this.as;

  // Get singular and plural names, trying to uppercase the first letter, unless the model forbids it
  var plural = Utils.uppercaseFirst(this.options.name.plural)
    , singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
    /**
     * Get everything currently associated with this, using an optional where clause.
     *
     * @param {Object} [options]
     * @param {Object} [options.where] An optional where clause to limit the associated models
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Array<Instance>>}
     * @method getAssociations
     */
    get: 'get' + plural,
    /**
     * Set the associated models by passing an array of persisted instances or their primary keys. Everything that is not in the
 passed array will be un-associated
     *
     * @param {Array<Instance|String|Number>} [newAssociations] An array of persisted instances or primary key of instances to associate
 with this. Pass 'null' or 'undefined' to remove all associations.
     * @param {Object} [options] Options passed to 'target.findAll' and 'update'.
     * @param {Object} [options.validate] Run validation for the join model
     * @return {Promise}
     * @method setAssociations
     */
    set: 'set' + plural,
    /**
     * Associate several persisted instances with this.
     *
     * @param {Array<In ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasOne"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Association.HasOne (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne)
- description and source-code
```javascript
Association.HasOne = function (srcModel, targetModel, options) {
  Association.call(this);

  this.associationType = 'HasOne';
  this.source = srcModel;
  this.target = targetModel;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(Utils.singularize(this.source.name), this.target.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.sourceIdentifier = this.source.primaryKeyAttribute;
  this.sourceKey = this.source.primaryKeyAttribute;
  this.sourceKeyIsPrimary = this.sourceKey === this.source.primaryKeyAttribute;

  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of a persisted instance to associate
 with this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to getAssociation and 'target.save'
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>BIGINT (length)](#apidoc.element.sequelize.BIGINT)
- description and source-code
```javascript
BIGINT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BIGINT)) return new BIGINT(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.</span>BLOB (length)](#apidoc.element.sequelize.BLOB)
- description and source-code
```javascript
BLOB = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BLOB)) return new BLOB(options);
  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.</span>BOOLEAN ()](#apidoc.element.sequelize.BOOLEAN)
- description and source-code
```javascript
BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.</span>CHAR (length, binary)](#apidoc.element.sequelize.CHAR)
- description and source-code
```javascript
CHAR = function (length, binary) {
  var options = typeof length === 'object' && length || {
    length: length,
    binary: binary
  };

  if (!(this instanceof CHAR)) return new CHAR(options);
  STRING.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ConnectionError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ConnectionError (parent)](#apidoc.element.sequelize.ConnectionError)
- description and source-code
```javascript
ConnectionError = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ConnectionRefusedError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ConnectionRefusedError (parent)](#apidoc.element.sequelize.ConnectionRefusedError)
- description and source-code
```javascript
ConnectionRefusedError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeConnectionRefusedError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ConnectionTimedOutError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ConnectionTimedOutError (parent)](#apidoc.element.sequelize.ConnectionTimedOutError)
- description and source-code
```javascript
ConnectionTimedOutError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeConnectionTimedOutError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DATE (length)](#apidoc.element.sequelize.DATE)
- description and source-code
```javascript
DATE = function (length) {
  var options = typeof length === 'object' && length || {
      length: length
    };

  if (!(this instanceof DATE)) return new DATE(options);

  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DATEONLY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DATEONLY ()](#apidoc.element.sequelize.DATEONLY)
- description and source-code
```javascript
DATEONLY = function () {
  if (!(this instanceof DATEONLY)) return new DATEONLY();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DECIMAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DECIMAL (precision, scale)](#apidoc.element.sequelize.DECIMAL)
- description and source-code
```javascript
DECIMAL = function (precision, scale) {
  var options = typeof precision === 'object' && precision || {
    precision: precision,
    scale: scale
  };
  if (!(this instanceof DECIMAL)) return new DECIMAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DOUBLE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DOUBLE (length, decimals)](#apidoc.element.sequelize.DOUBLE)
- description and source-code
```javascript
DOUBLE = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof DOUBLE)) return new DOUBLE(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DatabaseError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DatabaseError (parent)](#apidoc.element.sequelize.DatabaseError)
- description and source-code
```javascript
DatabaseError = function (parent) {
  error.BaseError.apply(this, [parent.message]);
  this.name = 'SequelizeDatabaseError';

<span class="apidocCodeCommentSpan">/**
 * The database specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
/**
 * The SQL that triggered the error
 * @member sql
 */
  this.sql = parent.sql;
/**
 * The message from the DB.
 * @member message
 * @name message
 */
/**
 * The fields of the unique constraint
 * @member fields
 * @name fields
 */
/**
 * The value(s) which triggered the error
 * @member value
 * @name value
 */
/**
 * The name of the index that triggered the error
 * @member index
 * @name index
 */
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.INITIALLY_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED)
- description and source-code
```javascript
DEFERRABLE INITIALLY DEFERRED
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.INITIALLY_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE)
- description and source-code
```javascript
DEFERRABLE INITIALLY IMMEDIATE
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.NOT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.NOT {{signature}}](#apidoc.element.sequelize.Deferrable.NOT)
- description and source-code
```javascript
NOT DEFERRABLE
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_DEFERRED"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.SET_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.SET_DEFERRED)
- description and source-code
```javascript
n/a
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_IMMEDIATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Deferrable.SET_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE)
- description and source-code
```javascript
n/a
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ENUM (value)](#apidoc.element.sequelize.ENUM)
- description and source-code
```javascript
ENUM = function (value) {
  var options = typeof value === 'object' && !Array.isArray(value) && value || {
    values: Array.prototype.slice.call(arguments).reduce(function(result, element) {
      return result.concat(Array.isArray(element) ? element : [element]);
    }, [])
  };
  if (!(this instanceof ENUM)) return new ENUM(options);
  this.values = options.values;
  this.options = options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.EmptyResultError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>EmptyResultError (message)](#apidoc.element.sequelize.EmptyResultError)
- description and source-code
```javascript
EmptyResultError = function (message) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeEmptyResultError';
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Error"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Error ()](#apidoc.element.sequelize.Error)
- description and source-code
```javascript
Error = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Error.super_"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Error.super_ ()](#apidoc.element.sequelize.Error.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ExclusionConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ExclusionConstraintError (options)](#apidoc.element.sequelize.ExclusionConstraintError)
- description and source-code
```javascript
ExclusionConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };

  error.DatabaseError.call(this, options.parent);
  this.name = 'SequelizeExclusionConstraintError';

  this.message = options.message || options.parent.message;
  this.constraint = options.constraint;
  this.fields = options.fields;
  this.table = options.table;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.FLOAT)
- description and source-code
```javascript
FLOAT = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof FLOAT)) return new FLOAT(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ForeignKeyConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ForeignKeyConstraintError (options)](#apidoc.element.sequelize.ForeignKeyConstraintError)
- description and source-code
```javascript
ForeignKeyConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };

  error.DatabaseError.call(this, options.parent);
  this.name = 'SequelizeForeignKeyConstraintError';

  this.message = options.message || options.parent.message || 'Database Error';
  this.fields = options.fields;
  this.table = options.table;
  this.value = options.value;
  this.index = options.index;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.GEOGRAPHY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>GEOGRAPHY (type, srid)](#apidoc.element.sequelize.GEOGRAPHY)
- description and source-code
```javascript
GEOGRAPHY = function (type, srid) {
  var options = _.isPlainObject(type) ? type : {
    type: type,
    srid: srid
  };

  if (!(this instanceof GEOGRAPHY)) return new GEOGRAPHY(options);

  this.options = options;
  this.type = options.type;
  this.srid = options.srid;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>GEOMETRY (type, srid)](#apidoc.element.sequelize.GEOMETRY)
- description and source-code
```javascript
GEOMETRY = function (type, srid) {
  var options = _.isPlainObject(type) ? type : {
    type: type,
    srid: srid
  };

  if (!(this instanceof GEOMETRY)) return new GEOMETRY(options);

  this.options = options;
  this.type = options.type;
  this.srid = options.srid;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.HSTORE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>HSTORE ()](#apidoc.element.sequelize.HSTORE)
- description and source-code
```javascript
HSTORE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.HostNotFoundError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>HostNotFoundError (parent)](#apidoc.element.sequelize.HostNotFoundError)
- description and source-code
```javascript
HostNotFoundError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeHostNotFoundError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.HostNotReachableError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>HostNotReachableError (parent)](#apidoc.element.sequelize.HostNotReachableError)
- description and source-code
```javascript
HostNotReachableError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeHostNotReachableError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>INTEGER (length)](#apidoc.element.sequelize.INTEGER)
- description and source-code
```javascript
INTEGER = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof INTEGER)) return new INTEGER(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Instance (values, options)](#apidoc.element.sequelize.Instance)
- description and source-code
```javascript
Instance = function (values, options) {
  this.dataValues = {};
  this._previousDataValues = {};
  this._changed = {};
  this.$modelOptions = this.Model.options;
  this.$options = options || {};
  this.hasPrimaryKeys = this.Model.options.hasPrimaryKeys;
  this.__eagerlyLoadedAssociations = [];
<span class="apidocCodeCommentSpan">  /**
   * Returns true if this instance has not yet been persisted to the database
   * @property isNewRecord
   * @return {Boolean}
   */
</span>  this.isNewRecord = options.isNewRecord;

  /**
   * Returns the Model the instance was created from.
   * @see {Model}
   * @property Model
   * @return {Model}
   */

  initValues.call(this, values, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.InstanceError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>InstanceError (message)](#apidoc.element.sequelize.InstanceError)
- description and source-code
```javascript
InstanceError = function (message) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeInstanceError';
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.InvalidConnectionError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>InvalidConnectionError (parent)](#apidoc.element.sequelize.InvalidConnectionError)
- description and source-code
```javascript
InvalidConnectionError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeInvalidConnectionError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.JSON"></a>[function <span class="apidocSignatureSpan">sequelize.</span>JSON ()](#apidoc.element.sequelize.JSON)
- description and source-code
```javascript
JSON = function () {
  if (!(this instanceof JSONTYPE)) return new JSONTYPE();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.JSONB"></a>[function <span class="apidocSignatureSpan">sequelize.</span>JSONB ()](#apidoc.element.sequelize.JSONB)
- description and source-code
```javascript
JSONB = function () {
  if (!(this instanceof JSONB)) return new JSONB();
  JSONTYPE.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Model (name, attributes, options)](#apidoc.element.sequelize.Model)
- description and source-code
```javascript
Model = function (name, attributes, options) {

  this.options = Utils._.extend({
    timestamps: true,
    instanceMethods: {},
    classMethods: {},
    validate: {},
    freezeTableName: false,
    underscored: false,
    underscoredAll: false,
    paranoid: false,
    rejectOnEmpty: false,
    whereCollection: null,
    schema: null,
    schemaDelimiter: '',
    defaultScope: {},
    scopes: [],
    hooks: {},
    indexes: []
  }, options || {});

  this.associations = {};
  this.modelManager = null;
  this.name = name;
  this.options.hooks = _.mapValues(this.replaceHookAliases(this.options.hooks), function (hooks) {
    if (!Array.isArray(hooks)) hooks = [hooks];
    return hooks;
  });

  this.sequelize = options.sequelize;
  this.underscored = this.underscored || this.underscoredAll;

  if (!this.options.tableName) {
    this.tableName = this.options.freezeTableName ? name : Utils.underscoredIf(Utils.pluralize(name), this.options.underscoredAll
);
  } else {
    this.tableName = this.options.tableName;
  }

  this.$schema = this.options.schema;
  this.$schemaDelimiter = this.options.schemaDelimiter;

  // error check options
  _.each(options.validate, function(validator, validatorType) {
    if (_.includes(Utils._.keys(attributes), validatorType)) {
      throw new Error('A model validator function must not have the same name as a field. Model: ' + name + ', field/validation
name: ' + validatorType);
    }

    if (!_.isFunction(validator)) {
      throw new Error('Members of the validate option must be functions. Model: ' + name + ', error with validate member ' + validatorType
);
    }
  });

 this.attributes = this.rawAttributes = _.mapValues(attributes, function(attribute, name) {
   if (!Utils._.isPlainObject(attribute)) {
     attribute = { type: attribute };
   }


    attribute = this.sequelize.normalizeAttribute(attribute);

    if (attribute.references) {
      attribute = Utils.formatReferences(attribute);

      if (attribute.references.model instanceof Model) {
        attribute.references.model = attribute.references.model.tableName;
      }
    }

    if (attribute.type === undefined) {
      throw new Error('Unrecognized data type for field ' + name);
    }

    return attribute;
  }.bind(this));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NONE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>NONE (ReturnType, fields)](#apidoc.element.sequelize.NONE)
- description and source-code
```javascript
NONE = function (ReturnType, fields) {
  if (!(this instanceof VIRTUAL)) return new VIRTUAL(ReturnType, fields);
  if (typeof ReturnType === 'function') ReturnType = new ReturnType();

  this.returnType = ReturnType;
  this.fields = fields;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NOW"></a>[function <span class="apidocSignatureSpan">sequelize.</span>NOW ()](#apidoc.element.sequelize.NOW)
- description and source-code
```javascript
NOW = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NUMBER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>NUMBER (options)](#apidoc.element.sequelize.NUMBER)
- description and source-code
```javascript
NUMBER = function (options) {
  this.options = options;
  this._length = options.length;
  this._zerofill = options.zerofill;
  this._decimals = options.decimals;
  this._precision = options.precision;
  this._scale = options.scale;
  this._unsigned = options.unsigned;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NUMERIC"></a>[function <span class="apidocSignatureSpan">sequelize.</span>NUMERIC (precision, scale)](#apidoc.element.sequelize.NUMERIC)
- description and source-code
```javascript
NUMERIC = function (precision, scale) {
  var options = typeof precision === 'object' && precision || {
    precision: precision,
    scale: scale
  };
  if (!(this instanceof DECIMAL)) return new DECIMAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise (executor)](#apidoc.element.sequelize.Promise)
- description and source-code
```javascript
function Promise(executor) {
    if (executor !== INTERNAL) {
        check(this, executor);
    }
    this._bitField = 0;
    this._fulfillmentHandler0 = undefined;
    this._rejectionHandler0 = undefined;
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._resolveFromExecutor(executor);
    this._promiseCreated();
    this._fireEvent("promiseCreated", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.AggregateError (message)](#apidoc.element.sequelize.Promise.AggregateError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.CancellationError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.CancellationError (message)](#apidoc.element.sequelize.Promise.CancellationError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.OperationalError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.OperationalError (message)](#apidoc.element.sequelize.Promise.OperationalError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.PromiseInspection (promise)](#apidoc.element.sequelize.Promise.PromiseInspection)
- description and source-code
```javascript
function PromiseInspection(promise) {
    if (promise !== undefined) {
        promise = promise._target();
        this._bitField = promise._bitField;
        this._settledValueField = promise._isFateSealed()
            ? promise._settledValue() : undefined;
    }
    else {
        this._bitField = 0;
        this._settledValueField = undefined;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.TimeoutError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.TimeoutError (message)](#apidoc.element.sequelize.Promise.TimeoutError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise._SomePromiseArray (values)](#apidoc.element.sequelize.Promise._SomePromiseArray)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.coroutine"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.coroutine (generatorFunction, options)](#apidoc.element.sequelize.Promise.coroutine)
- description and source-code
```javascript
Promise.coroutine = function (generatorFunction, options) {
    if (typeof generatorFunction !== "function") {
        throw new TypeError("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var yieldHandler = Object(options).yieldHandler;
    var PromiseSpawn$ = PromiseSpawn;
    var stack = new Error().stack;
    return function () {
        var generator = generatorFunction.apply(this, arguments);
        var spawn = new PromiseSpawn$(undefined, undefined, yieldHandler,
                                      stack);
        var ret = spawn.promise();
        spawn._generator = generator;
        spawn._promiseFulfilled(undefined);
        return ret;
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.each"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.each ()](#apidoc.element.sequelize.Promise.each)
- description and source-code
```javascript
Promise.each = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.filter"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.filter ()](#apidoc.element.sequelize.Promise.filter)
- description and source-code
```javascript
Promise.filter = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.join"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.join ()](#apidoc.element.sequelize.Promise.join)
- description and source-code
```javascript
Promise.join = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.map"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.map ()](#apidoc.element.sequelize.Promise.map)
- description and source-code
```javascript
Promise.map = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.mapSeries"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.mapSeries ()](#apidoc.element.sequelize.Promise.mapSeries)
- description and source-code
```javascript
Promise.mapSeries = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.catch"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.catch ()](#apidoc.element.sequelize.Promise.prototype.catch)
- description and source-code
```javascript
Promise.prototype.catch = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.done"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.done ()](#apidoc.element.sequelize.Promise.prototype.done)
- description and source-code
```javascript
Promise.prototype.done = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.each"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.each ()](#apidoc.element.sequelize.Promise.prototype.each)
- description and source-code
```javascript
Promise.prototype.each = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.error"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.error ()](#apidoc.element.sequelize.Promise.prototype.error)
- description and source-code
```javascript
Promise.prototype.error = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.filter"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.filter ()](#apidoc.element.sequelize.Promise.prototype.filter)
- description and source-code
```javascript
Promise.prototype.filter = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.finally"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.finally ()](#apidoc.element.sequelize.Promise.prototype.finally)
- description and source-code
```javascript
Promise.prototype.finally = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.map"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.map ()](#apidoc.element.sequelize.Promise.prototype.map)
- description and source-code
```javascript
Promise.prototype.map = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.mapSeries"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.mapSeries ()](#apidoc.element.sequelize.Promise.prototype.mapSeries)
- description and source-code
```javascript
Promise.prototype.mapSeries = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.nodeify"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.nodeify ()](#apidoc.element.sequelize.Promise.prototype.nodeify)
- description and source-code
```javascript
Promise.prototype.nodeify = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.reduce ()](#apidoc.element.sequelize.Promise.prototype.reduce)
- description and source-code
```javascript
Promise.prototype.reduce = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.spread"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.spread ()](#apidoc.element.sequelize.Promise.prototype.spread)
- description and source-code
```javascript
Promise.prototype.spread = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.tap"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.tap ()](#apidoc.element.sequelize.Promise.prototype.tap)
- description and source-code
```javascript
Promise.prototype.tap = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.then"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.prototype.then ()](#apidoc.element.sequelize.Promise.prototype.then)
- description and source-code
```javascript
Promise.prototype.then = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise.reduce ()](#apidoc.element.sequelize.Promise.reduce)
- description and source-code
```javascript
Promise.reduce = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.RANGE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>RANGE (subtype)](#apidoc.element.sequelize.RANGE)
- description and source-code
```javascript
RANGE = function (subtype) {
  var options = _.isPlainObject(subtype) ? subtype : { subtype: subtype };

  if (!options.subtype) options.subtype = new INTEGER();

  if (_.isFunction(options.subtype)) {
    options.subtype = new options.subtype();
  }

  if (!(this instanceof RANGE)) return new RANGE(options);
  ABSTRACT.apply(this, arguments);

  this._subtype = options.subtype.key;
  this.options = options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>REAL (length, decimals)](#apidoc.element.sequelize.REAL)
- description and source-code
```javascript
REAL = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof REAL)) return new REAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.</span>STRING (length, binary)](#apidoc.element.sequelize.STRING)
- description and source-code
```javascript
STRING = function (length, binary) {
  var options = typeof length === 'object' && length || {
    length: length,
    binary: binary
  };

  if (!(this instanceof STRING)) return new STRING(options);

  this.options = options;
  this._binary = options.binary;
  this._length = options.length || 255;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Sequelize"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Sequelize (database, username, password, options)](#apidoc.element.sequelize.Sequelize)
- description and source-code
```javascript
Sequelize = function (database, username, password, options) {
  var config;

  if (arguments.length === 1 && typeof database === 'object') {
    // new Sequelize({ ... options })
    options = database;
    config = _.pick(options, 'host', 'port', 'database', 'username', 'password');
  } else if ((arguments.length === 1 && typeof database === 'string') || (arguments.length === 2 && typeof username === 'object')) {
    // new Sequelize(URI, { ... options })

    config = {};
    options = username || {};

    var urlParts = url.parse(arguments[0]);

    // SQLite don't have DB in connection url
    if (urlParts.pathname) {
      config.database = urlParts.pathname.replace(/^\//, '');
    }

    options.dialect = urlParts.protocol.replace(/:$/, '');
    options.host = urlParts.hostname;

    if (urlParts.port) {
      options.port = urlParts.port;
    }

    if (urlParts.auth) {
      config.username = urlParts.auth.split(':')[0];
      config.password = urlParts.auth.split(':')[1];
    }
  } else {
    // new Sequelize(database, username, password, { ... options })
    options = options || {};
    config = {database: database, username: username, password: password};
  }

  Sequelize.runHooks('beforeInit', config, options);

  this.options = Utils._.extend({
    dialect: 'mysql',
    dialectModulePath: null,
    host: 'localhost',
    protocol: 'tcp',
    define: {},
    query: {},
    sync: {},
    timezone:'+00:00',
    logging: console.log,
    omitNull: false,
    native: false,
    replication: false,
    ssl: undefined,
    pool: {},
    quoteIdentifiers: true,
    hooks: {},
    retry: {max: 5, match: ['SQLITE_BUSY: database is locked']},
    transactionType: Transaction.TYPES.DEFERRED,
    isolationLevel: Transaction.ISOLATION_LEVELS.REPEATABLE_READ,
    databaseVersion: 0,
    typeValidation: false,
    benchmark: false
  }, options || {});

  if (this.options.dialect === 'postgresql') {
    this.options.dialect = 'postgres';
  }

  if (this.options.dialect === 'sqlite' && this.options.timezone !== '+00:00') {
    throw new Error('Setting a custom timezone is not supported by SQLite, dates are always returned as UTC. Please remove the custom
 timezone parameter.');
  }

  if (this.options.logging === true) {
    console.log('DEPRECATION WARNING: The logging-option should be either a function or false. Default: console.log');
    this.options.logging = console.log;
  }

  this.options.hooks = this.replaceHookAliases(this.options.hooks);

  if ((['', null, false].indexOf(config.password) > -1) || (typeof config.password === 'undefined')) {
    config.password = null;
  }

  this.config = {
    database: config.database,
    username: config.username,
    password: config.password,
    host: config.host || this.options.host,
    port: config.port || this.options.port,
    pool: this.options.pool,
    protocol: this.options.protocol,
    native: this.options.native,
    ssl: this.options.ssl,
    replication: this.options.replication,
    dialectModulePath: this.options.dialectModulePath,
    keepDefaultTimezone: this.options.keepDefaultTimezone,
    dialectOptions: this.options.dialectOptions
  };

  var Dialect;
  // Requiring the dialect in a switch-case to keep the
  // require calls static. (Browserify fix)
  switch (this.getDialect()){
    case 'mariadb':
      Dialect = require('./dialects/mariadb');
      break;
    case 'mssql':
      Dialect = require('./dialects/mssql');
      break;
    case 'mysql':
      Dialect = require('./dialects/mysql');
      break;
    case 'postgres':
      Dialect = require('./dialects/postgres');
      break;
    case 'sqlite':
      Dialect = require('./dialects/sqlite');
      break;
    default:
      throw new Error('The dialect ' + this.getDialect() + ' is not supported. Supported dialects: mariadb, mssql, mysql, postgres
, and sqlite.');
  }
  this.dialect = new Dialect(this);

  this.dialect.QueryGenerator.typeValidation = options.typeValidation;

<span class="apidocCodeCommentSpan">  /**
   * Models are stored here under the name given to 'sequelize.define'
   * @property models
   */
</span>  this.models = {};
  this.mod ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>TEXT (length)](#apidoc.element.sequelize.TEXT)
- description and source-code
```javascript
TEXT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof TEXT)) return new TEXT(options);
  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TIME"></a>[function <span class="apidocSignatureSpan">sequelize.</span>TIME ()](#apidoc.element.sequelize.TIME)
- description and source-code
```javascript
TIME = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TimeoutError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>TimeoutError (parent)](#apidoc.element.sequelize.TimeoutError)
- description and source-code
```javascript
TimeoutError = function (parent) {
  error.DatabaseError.call(this, parent);
  this.name = 'SequelizeTimeoutError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Transaction (sequelize, options)](#apidoc.element.sequelize.Transaction)
- description and source-code
```javascript
function Transaction(sequelize, options) {
  this.sequelize = sequelize;
  this.savepoints = [];
  var generateTransactionId = this.sequelize.dialect.QueryGenerator.generateTransactionId;

  this.options = Utils._.extend({
    autocommit: true,
    type: sequelize.options.transactionType,
    isolationLevel: sequelize.options.isolationLevel,
    readOnly: false
  }, options || {});

  this.parent = this.options.transaction;
  this.id = this.parent ? this.parent.id : generateTransactionId();

  if (this.parent) {
    this.id = this.parent.id;
    this.parent.savepoints.push(this);
    this.name = this.id + '-savepoint-' + this.parent.savepoints.length;
  } else {
    this.id = this.name = generateTransactionId();
  }

  delete this.options.transaction;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UUID ()](#apidoc.element.sequelize.UUID)
- description and source-code
```javascript
UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UUIDV1"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UUIDV1 ()](#apidoc.element.sequelize.UUIDV1)
- description and source-code
```javascript
UUIDV1 = function () {
  if (!(this instanceof UUIDV1)) return new UUIDV1();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UUIDV4"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UUIDV4 ()](#apidoc.element.sequelize.UUIDV4)
- description and source-code
```javascript
UUIDV4 = function () {
  if (!(this instanceof UUIDV4)) return new UUIDV4();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UniqueConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UniqueConstraintError (options)](#apidoc.element.sequelize.UniqueConstraintError)
- description and source-code
```javascript
UniqueConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };
  options.message = options.message || options.parent.message || 'Validation Error';
  options.errors = options.errors || {};

  error.ValidationError.call(this, options.message, options.errors);
  this.name = 'SequelizeUniqueConstraintError';
  this.message = options.message;
  this.errors = options.errors;
  this.fields = options.fields;
  this.parent = options.parent;
  this.original = options.parent;
  this.sql = options.parent.sql;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.fn"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Utils.fn (fn, args)](#apidoc.element.sequelize.Utils.fn)
- description and source-code
```javascript
Utils.fn = function (fn, args) {
  this.fn = fn;
  this.args = args;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.VIRTUAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>VIRTUAL (ReturnType, fields)](#apidoc.element.sequelize.VIRTUAL)
- description and source-code
```javascript
VIRTUAL = function (ReturnType, fields) {
  if (!(this instanceof VIRTUAL)) return new VIRTUAL(ReturnType, fields);
  if (typeof ReturnType === 'function') ReturnType = new ReturnType();

  this.returnType = ReturnType;
  this.fields = fields;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ValidationError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ValidationError (message, errors)](#apidoc.element.sequelize.ValidationError)
- description and source-code
```javascript
ValidationError = function (message, errors) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeValidationError';
  this.message = 'Validation Error';
<span class="apidocCodeCommentSpan">/**
 * An array of ValidationErrorItems
 * @member errors
 */
</span>  this.errors = errors || [];

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
}
```
- example usage
```shell
...
  return Promise.all([
    self._builtinValidators(),
    self._customValidators()
  ].map(function(promise) {
    return promise.reflect();
  })).then(function() {
    if (self.errors.length) {
      return new sequelizeError.ValidationError(null, self.errors);
    } else {
      return null;
    }
  });
};

/**
...
```

#### <a name="apidoc.element.sequelize.ValidationErrorItem"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ValidationErrorItem (message, type, path, value)](#apidoc.element.sequelize.ValidationErrorItem)
- description and source-code
```javascript
ValidationErrorItem = function (message, type, path, value) {
  this.message = message || '';
  this.type = type || null;
  this.path = path || null;
  this.value = value || null;
}
```
- example usage
```shell
...
 * @param {*} value anything.
 * @private
 */
InstanceValidator.prototype._validateSchema = function(rawAttribute, field, value) {
var error;

if (rawAttribute.allowNull === false && ((value === null) || (value === undefined))) {
  error = new sequelizeError.ValidationErrorItem(field + ' cannot be null', 'notNull Violation', field, value);
  this.errors.push(error);
}

if (rawAttribute.type === DataTypes.STRING || rawAttribute.type instanceof DataTypes.STRING || rawAttribute.type === DataTypes.TEXT
 || rawAttribute.type instanceof DataTypes.TEXT) {
  if (Array.isArray(value) || (_.isObject(value) && !value._isSequelizeMethod) && !Buffer.isBuffer(value)) {
    error = new sequelizeError.ValidationErrorItem(field + ' cannot be an array or an object', 'string violation', field, value);
    this.errors.push(error);
...
```

#### <a name="apidoc.element.sequelize.addHook"></a>[function <span class="apidocSignatureSpan">sequelize.</span>addHook (hookType, name, fn)](#apidoc.element.sequelize.addHook)
- description and source-code
```javascript
addHook = function (hookType, name, fn) {
  if (typeof name === 'function') {
    fn = name;
    name = null;
  }

  hookType = hookAliases[hookType] || hookType;

  this.options.hooks[hookType] = this.options.hooks[hookType] || [];
  this.options.hooks[hookType].push(!!name ? {name: name, fn: fn} : fn);
  return this;
}
```
- example usage
```shell
...
  applyTo: function(Model) {
    Utils._.mixin(Model, Hooks);
    Utils._.mixin(Model.prototype, Hooks);

    var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
    allHooks.forEach(function(hook) {
      Model.prototype[hook] = function(name, callback) {
        return this.addHook(hook, name, callback);
      };
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.and"></a>[function <span class="apidocSignatureSpan">sequelize.</span>and ()](#apidoc.element.sequelize.and)
- description and source-code
```javascript
and = function () {
  return { $and: Utils.sliceArgs(arguments) };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.asIs"></a>[function <span class="apidocSignatureSpan">sequelize.</span>asIs (val)](#apidoc.element.sequelize.asIs)
- description and source-code
```javascript
asIs = function (val) {
  return new Utils.literal(val);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.cast"></a>[function <span class="apidocSignatureSpan">sequelize.</span>cast (val, type)](#apidoc.element.sequelize.cast)
- description and source-code
```javascript
cast = function (val, type) {
  return new Utils.cast(val, type);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.col"></a>[function <span class="apidocSignatureSpan">sequelize.</span>col (col)](#apidoc.element.sequelize.col)
- description and source-code
```javascript
col = function (col) {
  return new Utils.col(col);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.condition"></a>[function <span class="apidocSignatureSpan">sequelize.</span>condition (attr, comparator, logic)](#apidoc.element.sequelize.condition)
- description and source-code
```javascript
condition = function (attr, comparator, logic) {
  return new Utils.where(attr, comparator, logic);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.default"></a>[function <span class="apidocSignatureSpan">sequelize.</span>default (database, username, password, options)](#apidoc.element.sequelize.default)
- description and source-code
```javascript
default = function (database, username, password, options) {
  var config;

  if (arguments.length === 1 && typeof database === 'object') {
    // new Sequelize({ ... options })
    options = database;
    config = _.pick(options, 'host', 'port', 'database', 'username', 'password');
  } else if ((arguments.length === 1 && typeof database === 'string') || (arguments.length === 2 && typeof username === 'object')) {
    // new Sequelize(URI, { ... options })

    config = {};
    options = username || {};

    var urlParts = url.parse(arguments[0]);

    // SQLite don't have DB in connection url
    if (urlParts.pathname) {
      config.database = urlParts.pathname.replace(/^\//, '');
    }

    options.dialect = urlParts.protocol.replace(/:$/, '');
    options.host = urlParts.hostname;

    if (urlParts.port) {
      options.port = urlParts.port;
    }

    if (urlParts.auth) {
      config.username = urlParts.auth.split(':')[0];
      config.password = urlParts.auth.split(':')[1];
    }
  } else {
    // new Sequelize(database, username, password, { ... options })
    options = options || {};
    config = {database: database, username: username, password: password};
  }

  Sequelize.runHooks('beforeInit', config, options);

  this.options = Utils._.extend({
    dialect: 'mysql',
    dialectModulePath: null,
    host: 'localhost',
    protocol: 'tcp',
    define: {},
    query: {},
    sync: {},
    timezone:'+00:00',
    logging: console.log,
    omitNull: false,
    native: false,
    replication: false,
    ssl: undefined,
    pool: {},
    quoteIdentifiers: true,
    hooks: {},
    retry: {max: 5, match: ['SQLITE_BUSY: database is locked']},
    transactionType: Transaction.TYPES.DEFERRED,
    isolationLevel: Transaction.ISOLATION_LEVELS.REPEATABLE_READ,
    databaseVersion: 0,
    typeValidation: false,
    benchmark: false
  }, options || {});

  if (this.options.dialect === 'postgresql') {
    this.options.dialect = 'postgres';
  }

  if (this.options.dialect === 'sqlite' && this.options.timezone !== '+00:00') {
    throw new Error('Setting a custom timezone is not supported by SQLite, dates are always returned as UTC. Please remove the custom
 timezone parameter.');
  }

  if (this.options.logging === true) {
    console.log('DEPRECATION WARNING: The logging-option should be either a function or false. Default: console.log');
    this.options.logging = console.log;
  }

  this.options.hooks = this.replaceHookAliases(this.options.hooks);

  if ((['', null, false].indexOf(config.password) > -1) || (typeof config.password === 'undefined')) {
    config.password = null;
  }

  this.config = {
    database: config.database,
    username: config.username,
    password: config.password,
    host: config.host || this.options.host,
    port: config.port || this.options.port,
    pool: this.options.pool,
    protocol: this.options.protocol,
    native: this.options.native,
    ssl: this.options.ssl,
    replication: this.options.replication,
    dialectModulePath: this.options.dialectModulePath,
    keepDefaultTimezone: this.options.keepDefaultTimezone,
    dialectOptions: this.options.dialectOptions
  };

  var Dialect;
  // Requiring the dialect in a switch-case to keep the
  // require calls static. (Browserify fix)
  switch (this.getDialect()){
    case 'mariadb':
      Dialect = require('./dialects/mariadb');
      break;
    case 'mssql':
      Dialect = require('./dialects/mssql');
      break;
    case 'mysql':
      Dialect = require('./dialects/mysql');
      break;
    case 'postgres':
      Dialect = require('./dialects/postgres');
      break;
    case 'sqlite':
      Dialect = require('./dialects/sqlite');
      break;
    default:
      throw new Error('The dialect ' + this.getDialect() + ' is not supported. Supported dialects: mariadb, mssql, mysql, postgres
, and sqlite.');
  }
  this.dialect = new Dialect(this);

  this.dialect.QueryGenerator.typeValidation = options.typeValidation;

<span class="apidocCodeCommentSpan">  /**
   * Models are stored here under the name given to 'sequelize.define'
   * @property models
   */
</span>  this.models = {};
  this.mod ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.fn"></a>[function <span class="apidocSignatureSpan">sequelize.</span>fn (fn)](#apidoc.element.sequelize.fn)
- description and source-code
```javascript
fn = function (fn) {
  return new Utils.fn(fn, Utils.sliceArgs(arguments, 1));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.hasHook"></a>[function <span class="apidocSignatureSpan">sequelize.</span>hasHook (hookType)](#apidoc.element.sequelize.hasHook)
- description and source-code
```javascript
hasHook = function (hookType) {
  return this.options.hooks[hookType] && !!this.options.hooks[hookType].length;
}
```
- example usage
```shell
...
   *
   * @param {String} hookType
   * @param {String} name
   */
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
...
```

#### <a name="apidoc.element.sequelize.hasHooks"></a>[function <span class="apidocSignatureSpan">sequelize.</span>hasHooks (hookType)](#apidoc.element.sequelize.hasHooks)
- description and source-code
```javascript
hasHooks = function (hookType) {
  return this.options.hooks[hookType] && !!this.options.hooks[hookType].length;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.hook"></a>[function <span class="apidocSignatureSpan">sequelize.</span>hook ()](#apidoc.element.sequelize.hook)
- description and source-code
```javascript
hook = function () {
  return Hooks.addHook.apply(this, arguments);
}
```
- example usage
```shell
...
*       function () {},
*       function() {} // Or an array of several
*     ]
*   }
* })
*
* // Method 2
* Model.hook('afterDestroy', function () {})
*
* // Method 3
* Model.afterBulkUpdate(function () {})
* '''
*
* @see {Sequelize#define}
* @mixin Hooks
...
```

#### <a name="apidoc.element.sequelize.instance_validator"></a>[function <span class="apidocSignatureSpan">sequelize.</span>instance_validator (modelInstance, options)](#apidoc.element.sequelize.instance_validator)
- description and source-code
```javascript
function InstanceValidator(modelInstance, options) {
  options = _.clone(options) || {};

  if (options.fields && !options.skip) {
    options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
  }

  // assign defined and default options
  this.options = Utils._.defaults(options, {
    skip: []
  });

  this.modelInstance = modelInstance;

<span class="apidocCodeCommentSpan">  /**
   * Exposes a reference to validator.js. This allows you to add custom validations using 'validator.extend'
   * @name validator
   */
</span>  this.validator = validator;

  /**
   *  All errors will be stored here from the validations.
   *
   * @type {Array} Will contain keys that correspond to attributes which will
   *   be Arrays of Errors.
   */
  this.errors = [];

  /** @type {boolean} Indicates if validations are in progress */
  this.inProgress = false;

  extendModelValidations(modelInstance);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.json"></a>[function <span class="apidocSignatureSpan">sequelize.</span>json (conditionsOrPath, value)](#apidoc.element.sequelize.json)
- description and source-code
```javascript
json = function (conditionsOrPath, value) {
  return new Utils.json(conditionsOrPath, value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.literal"></a>[function <span class="apidocSignatureSpan">sequelize.</span>literal (val)](#apidoc.element.sequelize.literal)
- description and source-code
```javascript
literal = function (val) {
  return new Utils.literal(val);
}
```
- example usage
```shell
...
return sequelize.sync({
force: true,
logging: console.log
})
.then(() => {
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
    }
  });
})
.then(console.log)
.finally(() => {
  sequelize.close();
});
...
```

#### <a name="apidoc.element.sequelize.model_manager"></a>[function <span class="apidocSignatureSpan">sequelize.</span>model_manager (sequelize)](#apidoc.element.sequelize.model_manager)
- description and source-code
```javascript
model_manager = function (sequelize) {
  this.models = [];
  this.sequelize = sequelize;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.BIGINT ()](#apidoc.element.sequelize.mssql.BIGINT)
- description and source-code
```javascript
mssql.BIGINT = function () {
  if (!(this instanceof BIGINT)) return new BIGINT();
  BaseTypes.BIGINT.apply(this, arguments);

  // MSSQL does not support any options for bigint
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support BIGINT with options. Plain 'BIGINT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.BLOB ()](#apidoc.element.sequelize.mssql.BLOB)
- description and source-code
```javascript
mssql.BLOB = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.BOOLEAN ()](#apidoc.element.sequelize.mssql.BOOLEAN)
- description and source-code
```javascript
mssql.BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.DATE ()](#apidoc.element.sequelize.mssql.DATE)
- description and source-code
```javascript
mssql.DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.ENUM ()](#apidoc.element.sequelize.mssql.ENUM)
- description and source-code
```javascript
mssql.ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.FLOAT ()](#apidoc.element.sequelize.mssql.FLOAT)
- description and source-code
```javascript
mssql.FLOAT = function () {
  if (!(this instanceof FLOAT)) return new FLOAT();
  BaseTypes.FLOAT.apply(this, arguments);

  // MSSQL does only support lengths as option.
  // Values between 1-24 result in 7 digits precision (4 bytes storage size)
  // Values between 25-53 result in 15 digits precision (8 bytes storage size)
  // If decimals are provided remove these and print a warning
  if (this._decimals) {
    warn('MSSQL does not support Float with decimals. Plain 'FLOAT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
  }
  if (this._unsigned) {
    warn('MSSQL does not support Float unsigned. 'UNSIGNED' was removed.');
    this._unsigned = undefined;
  }
  if (this._zerofill) {
    warn('MSSQL does not support Float zerofill. 'ZEROFILL' was removed.');
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.INTEGER ()](#apidoc.element.sequelize.mssql.INTEGER)
- description and source-code
```javascript
mssql.INTEGER = function () {
  if (!(this instanceof INTEGER)) return new INTEGER();
  BaseTypes.INTEGER.apply(this, arguments);

  // MSSQL does not support any options for integer
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support INTEGER with options. Plain 'INTEGER' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.NOW"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.NOW ()](#apidoc.element.sequelize.mssql.NOW)
- description and source-code
```javascript
mssql.NOW = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.REAL ()](#apidoc.element.sequelize.mssql.REAL)
- description and source-code
```javascript
mssql.REAL = function () {
  if (!(this instanceof REAL)) return new REAL();
  BaseTypes.REAL.apply(this, arguments);

  // MSSQL does not support any options for real
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support REAL with options. Plain 'REAL' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.STRING ()](#apidoc.element.sequelize.mssql.STRING)
- description and source-code
```javascript
mssql.STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.TEXT ()](#apidoc.element.sequelize.mssql.TEXT)
- description and source-code
```javascript
mssql.TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mssql.UUID ()](#apidoc.element.sequelize.mssql.UUID)
- description and source-code
```javascript
mssql.UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mysql.DATE ()](#apidoc.element.sequelize.mysql.DATE)
- description and source-code
```javascript
mysql.DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mysql.ENUM ()](#apidoc.element.sequelize.mysql.ENUM)
- description and source-code
```javascript
mysql.ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mysql.GEOMETRY ()](#apidoc.element.sequelize.mysql.GEOMETRY)
- description and source-code
```javascript
mysql.GEOMETRY = function () {
  if (!(this instanceof GEOMETRY)) return new GEOMETRY();
  BaseTypes.GEOMETRY.apply(this, arguments);

  if (_.isEmpty(this.type)) {
    this.sqlType = this.key;
  } else if (_.includes(SUPPORTED_GEOMETRY_TYPES, this.type)) {
    this.sqlType = this.type;
  } else {
    throw new Error('Supported geometry types are: ' + SUPPORTED_GEOMETRY_TYPES.join(', '));
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.</span>mysql.UUID ()](#apidoc.element.sequelize.mysql.UUID)
- description and source-code
```javascript
mysql.UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.or"></a>[function <span class="apidocSignatureSpan">sequelize.</span>or ()](#apidoc.element.sequelize.or)
- description and source-code
```javascript
or = function () {
  return { $or: Utils.sliceArgs(arguments) };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.BIGINT ()](#apidoc.element.sequelize.postgres.BIGINT)
- description and source-code
```javascript
postgres.BIGINT = function () {
  if (!(this instanceof BIGINT)) return new BIGINT();
  BaseTypes.BIGINT.apply(this, arguments);

  // POSTGRES does not support any parameters for bigint
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support BIGINT with options. Plain 'BIGINT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.BLOB ()](#apidoc.element.sequelize.postgres.BLOB)
- description and source-code
```javascript
postgres.BLOB = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.BOOLEAN ()](#apidoc.element.sequelize.postgres.BOOLEAN)
- description and source-code
```javascript
postgres.BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.CHAR ()](#apidoc.element.sequelize.postgres.CHAR)
- description and source-code
```javascript
postgres.CHAR = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.DATE ()](#apidoc.element.sequelize.postgres.DATE)
- description and source-code
```javascript
postgres.DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DECIMAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.DECIMAL ()](#apidoc.element.sequelize.postgres.DECIMAL)
- description and source-code
```javascript
postgres.DECIMAL = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.FLOAT ()](#apidoc.element.sequelize.postgres.FLOAT)
- description and source-code
```javascript
postgres.FLOAT = function () {
  if (!(this instanceof FLOAT)) return new FLOAT();
  BaseTypes.FLOAT.apply(this, arguments);

  // POSTGRES does only support lengths as parameter.
  // Values between 1-24 result in REAL
  // Values between 25-53 result in DOUBLE PRECISION
  // If decimals are provided remove these and print a warning
  if (this._decimals) {
    warn('PostgreSQL does not support FLOAT with decimals. Plain 'FLOAT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._decimals = undefined;
  }
  if (this._unsigned) {
    warn('PostgreSQL does not support FLOAT unsigned. 'UNSIGNED' was removed.');
    this._unsigned = undefined;
  }
  if (this._zerofill) {
    warn('PostgreSQL does not support FLOAT zerofill. 'ZEROFILL' was removed.');
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.GEOGRAPHY ()](#apidoc.element.sequelize.postgres.GEOGRAPHY)
- description and source-code
```javascript
postgres.GEOGRAPHY = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.GEOMETRY ()](#apidoc.element.sequelize.postgres.GEOMETRY)
- description and source-code
```javascript
postgres.GEOMETRY = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.HSTORE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.HSTORE ()](#apidoc.element.sequelize.postgres.HSTORE)
- description and source-code
```javascript
postgres.HSTORE = function () {
  if (!(this instanceof HSTORE)) return new HSTORE();
  BaseTypes.HSTORE.apply(this, arguments);

  if (!hstore) {
    // All datatype files are loaded at import - make sure we don't load the hstore parser before a hstore is instantiated
    hstore = require('./hstore');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.INTEGER ()](#apidoc.element.sequelize.postgres.INTEGER)
- description and source-code
```javascript
postgres.INTEGER = function () {
  if (!(this instanceof INTEGER)) return new INTEGER();
  BaseTypes.INTEGER.apply(this, arguments);

  // POSTGRES does not support any parameters for integer
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support INTEGER with options. Plain 'INTEGER' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.RANGE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.RANGE ()](#apidoc.element.sequelize.postgres.RANGE)
- description and source-code
```javascript
postgres.RANGE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.REAL ()](#apidoc.element.sequelize.postgres.REAL)
- description and source-code
```javascript
postgres.REAL = function () {
  if (!(this instanceof REAL)) return new REAL();
  BaseTypes.REAL.apply(this, arguments);

  // POSTGRES does not support any parameters for real
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support REAL with options. Plain 'REAL' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.STRING ()](#apidoc.element.sequelize.postgres.STRING)
- description and source-code
```javascript
postgres.STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>postgres.TEXT ()](#apidoc.element.sequelize.postgres.TEXT)
- description and source-code
```javascript
postgres.TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface"></a>[function <span class="apidocSignatureSpan">sequelize.</span>query_interface (sequelize)](#apidoc.element.sequelize.query_interface)
- description and source-code
```javascript
query_interface = function (sequelize) {
  this.sequelize = sequelize;
  this.QueryGenerator = this.sequelize.dialect.QueryGenerator;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.removeHook"></a>[function <span class="apidocSignatureSpan">sequelize.</span>removeHook (hookType, name)](#apidoc.element.sequelize.removeHook)
- description and source-code
```javascript
removeHook = function (hookType, name) {
  hookType = hookAliases[hookType] || hookType;

  if (!this.hasHook(hookType)) {
    return this;
  }

  this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
    // don't remove unnamed hooks
    if (typeof hook === 'function') {
      return true;
    }

    return typeof hook === 'object' && hook.name !== name;
  });

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.replaceHookAliases"></a>[function <span class="apidocSignatureSpan">sequelize.</span>replaceHookAliases (hooks)](#apidoc.element.sequelize.replaceHookAliases)
- description and source-code
```javascript
replaceHookAliases = function (hooks) {
  var realHookName;

  Utils._.each(hooks, function(hooksArray, name) {
    // Does an alias for this hook name exist?
    if (realHookName = hookAliases[name]) {
      // Add the hooks to the actual hook
      hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

      // Delete the alias
      delete hooks[name];
    }
  });

  return hooks;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.runHooks"></a>[function <span class="apidocSignatureSpan">sequelize.</span>runHooks (hooks)](#apidoc.element.sequelize.runHooks)
- description and source-code
```javascript
runHooks = function (hooks) {
  var self = this
    , fn
    , fnArgs = Utils.sliceArgs(arguments, 1)
    , hookType;

  if (typeof fnArgs[fnArgs.length - 1] === 'function') {
    fn = fnArgs.pop();
  }

  if (typeof hooks === 'string') {
    hookType = hooks;
    hooks = this.options.hooks[hookType] || [];
    if (!Array.isArray(hooks)) hooks = hooks === undefined ? [] : [hooks];
    if (this.sequelize) hooks = hooks.concat(this.sequelize.options.hooks[hookType] || []);
  }

  if (!Array.isArray(hooks)) {
    hooks = hooks === undefined ? [] : [hooks];
  }

  // run hooks as sync functions if flagged as sync
  if (hookTypes[hookType] && hookTypes[hookType].sync) {
    hooks.forEach(function(hook) {
      if (typeof hook === 'object') hook = hook.fn;
      return hook.apply(self, fnArgs);
    });
    return;
  }

  // run hooks async
  var promise = Promise.each(hooks, function (hook) {
    if (typeof hook === 'object') {
      hook = hook.fn;
    }

    if (hookType && hook.length > hookTypes[hookType].params) {
      hook = Promise.promisify(hook, self);
    }

    return hook.apply(self, fnArgs);
  }).return();

  if (fn) {
    return promise.nodeify(fn);
  }

  return promise;
}
```
- example usage
```shell
...
 *   - On validation success: After Validation Model Hooks
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
...
```

#### <a name="apidoc.element.sequelize.sqlite.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.BIGINT (length)](#apidoc.element.sequelize.sqlite.BIGINT)
- description and source-code
```javascript
sqlite.BIGINT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BIGINT)) return new BIGINT(options);
  BaseTypes.BIGINT.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.CHAR ()](#apidoc.element.sequelize.sqlite.CHAR)
- description and source-code
```javascript
sqlite.CHAR = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.DATE ()](#apidoc.element.sequelize.sqlite.DATE)
- description and source-code
```javascript
sqlite.DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.ENUM ()](#apidoc.element.sequelize.sqlite.ENUM)
- description and source-code
```javascript
sqlite.ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.FLOAT (length, decimals)](#apidoc.element.sequelize.sqlite.FLOAT)
- description and source-code
```javascript
sqlite.FLOAT = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof FLOAT)) return new FLOAT(options);
  BaseTypes.FLOAT.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.INTEGER (length)](#apidoc.element.sequelize.sqlite.INTEGER)
- description and source-code
```javascript
sqlite.INTEGER = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof INTEGER)) return new INTEGER(options);
  BaseTypes.INTEGER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.NUMBER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.NUMBER ()](#apidoc.element.sequelize.sqlite.NUMBER)
- description and source-code
```javascript
sqlite.NUMBER = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.REAL (length, decimals)](#apidoc.element.sequelize.sqlite.REAL)
- description and source-code
```javascript
sqlite.REAL = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof REAL)) return new REAL(options);
  BaseTypes.REAL.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.STRING ()](#apidoc.element.sequelize.sqlite.STRING)
- description and source-code
```javascript
sqlite.STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>sqlite.TEXT ()](#apidoc.element.sequelize.sqlite.TEXT)
- description and source-code
```javascript
sqlite.TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.where"></a>[function <span class="apidocSignatureSpan">sequelize.</span>where (attr, comparator, logic)](#apidoc.element.sequelize.where)
- description and source-code
```javascript
where = function (attr, comparator, logic) {
  return new Utils.where(attr, comparator, logic);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ABSTRACT"></a>[module sequelize.ABSTRACT](#apidoc.module.sequelize.ABSTRACT)

#### <a name="apidoc.element.sequelize.ABSTRACT.ABSTRACT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ABSTRACT (options)](#apidoc.element.sequelize.ABSTRACT.ABSTRACT)
- description and source-code
```javascript
ABSTRACT = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ABSTRACT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.ABSTRACT.</span>inherits (Constructor)](#apidoc.element.sequelize.ABSTRACT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.ABSTRACT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.ABSTRACT.</span>warn (link, text)](#apidoc.element.sequelize.ABSTRACT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ABSTRACT.prototype"></a>[module sequelize.ABSTRACT.prototype](#apidoc.module.sequelize.ABSTRACT.prototype)

#### <a name="apidoc.element.sequelize.ABSTRACT.prototype.stringify"></a>[function <span class="apidocSignatureSpan">sequelize.ABSTRACT.prototype.</span>stringify (value, options)](#apidoc.element.sequelize.ABSTRACT.prototype.stringify)
- description and source-code
```javascript
stringify = function (value, options) {
  if (this.$stringify) {
    return this.$stringify(value, options);
  }
  return value;
}
```
- example usage
```shell
...
  // In mssql, prepend N to all quoted vals which are originally a string (for
  // unicode compatibility)
  prependN = dialect === 'mssql';
  break;
}

if (val instanceof Date) {
  val = dataTypes[dialect].DATE.prototype.stringify(val, { timezone: timeZone });
}

if (Buffer.isBuffer(val)) {
  if (dataTypes[dialect].BLOB) {
    return dataTypes[dialect].BLOB.prototype.stringify(val);
  }
...
```

#### <a name="apidoc.element.sequelize.ABSTRACT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.ABSTRACT.prototype.</span>toSql ()](#apidoc.element.sequelize.ABSTRACT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return this.key;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ABSTRACT.prototype.toString"></a>[function <span class="apidocSignatureSpan">sequelize.ABSTRACT.prototype.</span>toString (options)](#apidoc.element.sequelize.ABSTRACT.prototype.toString)
- description and source-code
```javascript
toString = function (options) {
  return this.toSql(options);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ARRAY"></a>[module sequelize.ARRAY](#apidoc.module.sequelize.ARRAY)

#### <a name="apidoc.element.sequelize.ARRAY.ARRAY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ARRAY (type)](#apidoc.element.sequelize.ARRAY.ARRAY)
- description and source-code
```javascript
ARRAY = function (type) {
  var options = _.isPlainObject(type) ? type : {
    type: type
  };
  if (!(this instanceof ARRAY)) return new ARRAY(options);
  this.type = typeof options.type === 'function' ? new options.type() : options.type;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ARRAY.is"></a>[function <span class="apidocSignatureSpan">sequelize.ARRAY.</span>is (obj, type)](#apidoc.element.sequelize.ARRAY.is)
- description and source-code
```javascript
is = function (obj, type) {
  return obj instanceof ARRAY && obj.type instanceof type;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ARRAY.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ARRAY.</span>super_ (options)](#apidoc.element.sequelize.ARRAY.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ARRAY.prototype"></a>[module sequelize.ARRAY.prototype](#apidoc.module.sequelize.ARRAY.prototype)

#### <a name="apidoc.element.sequelize.ARRAY.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.ARRAY.prototype.</span>toSql ()](#apidoc.element.sequelize.ARRAY.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return this.type.toSql() + '[]';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ARRAY.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.ARRAY.prototype.</span>validate (value)](#apidoc.element.sequelize.ARRAY.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!_.isArray(value)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid array', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.AccessDeniedError"></a>[module sequelize.AccessDeniedError](#apidoc.module.sequelize.AccessDeniedError)

#### <a name="apidoc.element.sequelize.AccessDeniedError.AccessDeniedError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>AccessDeniedError (parent)](#apidoc.element.sequelize.AccessDeniedError.AccessDeniedError)
- description and source-code
```javascript
AccessDeniedError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeAccessDeniedError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.AccessDeniedError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.AccessDeniedError.</span>super_ (parent)](#apidoc.element.sequelize.AccessDeniedError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association"></a>[module sequelize.Association](#apidoc.module.sequelize.Association)

#### <a name="apidoc.element.sequelize.Association.Association"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Association ()](#apidoc.element.sequelize.Association.Association)
- description and source-code
```javascript
Association = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsTo (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo)
- description and source-code
```javascript
BelongsTo = function (source, target, options) {
  Association.call(this);

  this.associationType = 'BelongsTo';
  this.source = source;
  this.target = target;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.as, this.source.options.underscored),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.identifier = this.foreignKey;

  if (this.source.rawAttributes[this.identifier]) {
    this.identifierField = this.source.rawAttributes[this.identifier].field || this.identifier;
  }

  this.targetKey = this.options.targetKey || this.target.primaryKeyAttribute;
  this.targetKeyField = this.target.rawAttributes[this.targetKey].field || this.targetKey;
  this.targetKeyIsPrimary = this.targetKey === this.target.primaryKeyAttribute;

  this.targetIdentifier = this.targetKey;
  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false.
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of an instance to associate with
 this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to 'this.save'
     * @param {Boolean} [options.save=true] Skip saving this after setting the foreign key if false.
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsToMany (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany)
- description and source-code
```javascript
BelongsToMany = function (source, target, options) {
  Association.call(this);

  options = options || {};

  if (options.through === undefined || options.through === true || options.through === null) {
    throw new Error('belongsToMany must be given a through option, either a string or a model');
  }

  if (!options.through.model) {
    options.through = {
      model: options.through
    };
  }

  this.associationType = 'BelongsToMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options;
  this.sequelize = source.modelManager.sequelize;
  this.through = _.assign({}, options.through);
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.doubleLinked = false;
  this.as = this.options.as;

  if (!this.as && this.isSelfAssociation) {
    throw new Error('\'as\' must be defined for many-to-many self-associations');
  }

  if (this.as) {
    this.isAliased = true;

    if (Utils._.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  this.combinedTableName = Utils.combineTableNames(
    this.source.tableName,
    this.isSelfAssociation ? (this.as || this.target.tableName) : this.target.tableName
  );

<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association - Unless we find a pairing association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  /*
   * Default/generated foreign/other keys
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else {
    if (!this.options.foreignKey) {
      this.foreignKeyDefault = true;
    }

    this.foreignKeyAttribute = {};
    this.foreignKey = this.options.foreignKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (_.isObject(this.options.otherKey)) {
    this.otherKeyAttribute = this.options.otherKey;
    this.otherKey = this.otherKeyAttribute.name || this.otherKeyAttribute.fieldName;
  } else {
    if (!this.options.otherKey) {
      this.otherKeyDefault = true;
    }

    this.otherKeyAttribute = {};
    this.otherKey = this.options.otherKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(
          this.isSelfAssociation ?
            Utils.singularize(this.as) :
            this.target.options.name.singular,
          this.target.options.underscored
        ),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.target.options.underscored
    );
  }

  /*
   * Find paired association (if exists)
   */
  _.each(this.target.associations, function(association) {
    if (association.associationType !== 'BelongsToMany') return;
    if (association.target !== this.source) return;

    if (this.options.through.model === association.options.through.model) {
      this.paired = association;
      association.paired = this;
    }
  }.bind(this));

  if (typeof this.through.model === 'string') {
    if (!this.sequelize.isDefined(this.through.model)) {
      this.through.model = this.sequelize.define(this.through.model, {}, _.extend(this.options, {
        tableName: this.through.model,
        indexes: [], //we don't want indexes here (as referenced in #2416)
        paranoid: false,  // A paranoid join table does not make sense
        validate: {} // Don't propagate model-level validations
      }));
    } else {
      this.through.model = this.sequelize.model(this.through.model);
    }
  }

  this.options = _.assign(this.options, _.pick(this.through.model.options, [
    'timestamps', 'createdAt', 'updatedAt ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>HasMany (source, target, options)](#apidoc.element.sequelize.Association.HasMany)
- description and source-code
```javascript
HasMany = function (source, target, options) {
  Association.call(this);

  this.associationType = 'HasMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options || {};
  this.sequelize = source.modelManager.sequelize;
  this.through = options.through;
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.options.through) {
    throw new Error('N:M associations are not supported with hasMany. Use belongsToMany instead');
  }


<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  if (this.as) {
    this.isAliased = true;

    if (_.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  /*
   * Foreign key setup
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
    this.foreignKeyField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  this.sourceKey = this.options.sourceKey || this.source.primaryKeyAttribute;
  if (this.target.rawAttributes[this.sourceKey]) {
    this.sourceKeyField = this.source.rawAttributes[this.sourceKey].field || this.sourceKey;
  } else {
    this.sourceKeyField = this.sourceKey;
  }

  if (this.source.fieldRawAttributesMap[this.sourceKey]) {
    this.sourceKeyAttribute = this.source.fieldRawAttributesMap[this.sourceKey].fieldName;
  } else {
    this.sourceKeyAttribute = this.source.primaryKeyAttribute;
  }
  this.sourceIdentifier = this.sourceKey;
  this.associationAccessor = this.as;

  // Get singular and plural names, trying to uppercase the first letter, unless the model forbids it
  var plural = Utils.uppercaseFirst(this.options.name.plural)
    , singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
    /**
     * Get everything currently associated with this, using an optional where clause.
     *
     * @param {Object} [options]
     * @param {Object} [options.where] An optional where clause to limit the associated models
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Array<Instance>>}
     * @method getAssociations
     */
    get: 'get' + plural,
    /**
     * Set the associated models by passing an array of persisted instances or their primary keys. Everything that is not in the
 passed array will be un-associated
     *
     * @param {Array<Instance|String|Number>} [newAssociations] An array of persisted instances or primary key of instances to associate
 with this. Pass 'null' or 'undefined' to remove all associations.
     * @param {Object} [options] Options passed to 'target.findAll' and 'update'.
     * @param {Object} [options.validate] Run validation for the join model
     * @return {Promise}
     * @method setAssociations
     */
    set: 'set' + plural,
    /**
     * Associate several persisted instances with this.
     *
     * @param {Array<In ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasOne"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>HasOne (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne)
- description and source-code
```javascript
HasOne = function (srcModel, targetModel, options) {
  Association.call(this);

  this.associationType = 'HasOne';
  this.source = srcModel;
  this.target = targetModel;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(Utils.singularize(this.source.name), this.target.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.sourceIdentifier = this.source.primaryKeyAttribute;
  this.sourceKey = this.source.primaryKeyAttribute;
  this.sourceKeyIsPrimary = this.sourceKey === this.source.primaryKeyAttribute;

  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of a persisted instance to associate
 with this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to getAssociation and 'target.save'
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.default"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>default ()](#apidoc.element.sequelize.Association.default)
- description and source-code
```javascript
default = function () {}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association.BelongsTo"></a>[module sequelize.Association.BelongsTo](#apidoc.module.sequelize.Association.BelongsTo)

#### <a name="apidoc.element.sequelize.Association.BelongsTo.BelongsTo"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsTo (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo.BelongsTo)
- description and source-code
```javascript
BelongsTo = function (source, target, options) {
  Association.call(this);

  this.associationType = 'BelongsTo';
  this.source = source;
  this.target = target;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.as, this.source.options.underscored),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.identifier = this.foreignKey;

  if (this.source.rawAttributes[this.identifier]) {
    this.identifierField = this.source.rawAttributes[this.identifier].field || this.identifier;
  }

  this.targetKey = this.options.targetKey || this.target.primaryKeyAttribute;
  this.targetKeyField = this.target.rawAttributes[this.targetKey].field || this.targetKey;
  this.targetKeyIsPrimary = this.targetKey === this.target.primaryKeyAttribute;

  this.targetIdentifier = this.targetKey;
  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false.
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of an instance to associate with
 this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to 'this.save'
     * @param {Boolean} [options.save=true] Skip saving this after setting the foreign key if false.
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo.default"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.</span>default (source, target, options)](#apidoc.element.sequelize.Association.BelongsTo.default)
- description and source-code
```javascript
default = function (source, target, options) {
  Association.call(this);

  this.associationType = 'BelongsTo';
  this.source = source;
  this.target = target;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.as, this.source.options.underscored),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.identifier = this.foreignKey;

  if (this.source.rawAttributes[this.identifier]) {
    this.identifierField = this.source.rawAttributes[this.identifier].field || this.identifier;
  }

  this.targetKey = this.options.targetKey || this.target.primaryKeyAttribute;
  this.targetKeyField = this.target.rawAttributes[this.targetKey].field || this.targetKey;
  this.targetKeyIsPrimary = this.targetKey === this.target.primaryKeyAttribute;

  this.targetIdentifier = this.targetKey;
  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false.
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of an instance to associate with
 this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to 'this.save'
     * @param {Boolean} [options.save=true] Skip saving this after setting the foreign key if false.
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.</span>super_ ()](#apidoc.element.sequelize.Association.BelongsTo.super_)
- description and source-code
```javascript
super_ = function () {}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association.BelongsTo.prototype"></a>[module sequelize.Association.BelongsTo.prototype](#apidoc.module.sequelize.Association.BelongsTo.prototype)

#### <a name="apidoc.element.sequelize.Association.BelongsTo.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>get (instances, options)](#apidoc.element.sequelize.Association.BelongsTo.prototype.get)
- description and source-code
```javascript
get = function (instances, options) {
  var association = this
    , Target = association.target
    , instance
    , where = {};

  options = Utils.cloneDeep(options);

  if (options.hasOwnProperty('scope')) {
    if (!options.scope) {
      Target = Target.unscoped();
    } else {
      Target = Target.scope(options.scope);
    }
  }

  if (options.hasOwnProperty('schema')) {
    Target = Target.schema(options.schema, options.schemaDelimiter);
  }

  if (!Array.isArray(instances)) {
    instance = instances;
    instances = undefined;
  }

  if (instances) {
    where[association.targetKey] = {
      $in: instances.map(function (instance) {
        return instance.get(association.foreignKey);
      })
    };
  } else {
    if (association.targetKeyIsPrimary && !options.where) {
      return Target.findById(instance.get(association.foreignKey), options);
    } else {
      where[association.targetKey] = instance.get(association.foreignKey);
      options.limit = null;
    }
  }

  options.where = options.where ?
                  {$and: [where, options.where]} :
                  where;

  if (instances) {
    return Target.findAll(options).then(function (results) {
      var result = {};
      instances.forEach(function (instance) {
        result[instance.get(association.foreignKey, {raw: true})] = null;
      });

      results.forEach(function (instance) {
        result[instance.get(association.targetKey, {raw: true})] = instance;
      });

      return result;
    });
  }
  return Target.findOne(options);
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo.prototype.injectAttributes"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.BelongsTo.prototype.injectAttributes)
- description and source-code
```javascript
injectAttributes = function () {
  var newAttributes = {};

  newAttributes[this.foreignKey] = _.defaults({}, this.foreignKeyAttribute, {
    type: this.options.keyType || this.target.rawAttributes[this.targetKey].type,
    allowNull : true
  });

  if (this.options.constraints !== false) {
    var source = this.source.rawAttributes[this.foreignKey] || newAttributes[this.foreignKey];
    this.options.onDelete = this.options.onDelete || (source.allowNull ? 'SET NULL' : 'NO ACTION');
    this.options.onUpdate = this.options.onUpdate || 'CASCADE';
  }

  Helpers.addForeignKeyConstraints(newAttributes[this.foreignKey], this.target, this.source, this.options, this.targetKeyField);
  Utils.mergeDefaults(this.source.rawAttributes, newAttributes);

  this.identifierField = this.source.rawAttributes[this.foreignKey].field || this.foreignKey;

  this.source.refreshAttributes();

  Helpers.checkNamingCollision(this);

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo.prototype.injectCreator"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>injectCreator (instancePrototype)](#apidoc.element.sequelize.Association.BelongsTo.prototype.injectCreator)
- description and source-code
```javascript
injectCreator = function (instancePrototype) {
  var association = this;

  instancePrototype[this.accessors.create] = function(values, fieldsOrOptions) {
    var instance = this
      , options = {};

    if ((fieldsOrOptions || {}).transaction instanceof Transaction) {
      options.transaction = fieldsOrOptions.transaction;
    }
    options.logging = (fieldsOrOptions || {}).logging;

    return association.target.create(values, fieldsOrOptions).then(function(newAssociatedObject) {
      return instance[association.accessors.set](newAssociatedObject, options);
    });
  };

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo.prototype.injectSetter"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>injectSetter (instancePrototype)](#apidoc.element.sequelize.Association.BelongsTo.prototype.injectSetter)
- description and source-code
```javascript
injectSetter = function (instancePrototype) {
  var association = this;

  instancePrototype[this.accessors.set] = function(associatedInstance, options) {
    options = options || {};

    var value = associatedInstance;
    if (associatedInstance instanceof association.target.Instance) {
      value = associatedInstance[association.targetKey];
    }

    this.set(association.foreignKey, value);

    if (options.save === false) return;

    options = _.extend({
      fields: [association.foreignKey],
      allowNull: [association.foreignKey],
      association: true
    }, options);


    // passes the changed field to save, so only that field get updated.
    return this.save(options);
  };

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsTo.prototype.mixin"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsTo.prototype.</span>mixin (obj)](#apidoc.element.sequelize.Association.BelongsTo.prototype.mixin)
- description and source-code
```javascript
mixin = function (obj) {
  var association = this;

  obj[this.accessors.get] = function(options) {
    return association.get(this, options);
  };

  association.injectSetter(obj);
  association.injectCreator(obj);
}
```
- example usage
```shell
...
   */

module.exports = {
  hooks: hookTypes,
  hookAliases: hookAliases,

  applyTo: function(Model) {
Utils._.mixin(Model, Hooks);
Utils._.mixin(Model.prototype, Hooks);

var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
allHooks.forEach(function(hook) {
  Model.prototype[hook] = function(name, callback) {
    return this.addHook(hook, name, callback);
  };
...
```



# <a name="apidoc.module.sequelize.Association.BelongsToMany"></a>[module sequelize.Association.BelongsToMany](#apidoc.module.sequelize.Association.BelongsToMany)

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.BelongsToMany"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>BelongsToMany (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany.BelongsToMany)
- description and source-code
```javascript
BelongsToMany = function (source, target, options) {
  Association.call(this);

  options = options || {};

  if (options.through === undefined || options.through === true || options.through === null) {
    throw new Error('belongsToMany must be given a through option, either a string or a model');
  }

  if (!options.through.model) {
    options.through = {
      model: options.through
    };
  }

  this.associationType = 'BelongsToMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options;
  this.sequelize = source.modelManager.sequelize;
  this.through = _.assign({}, options.through);
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.doubleLinked = false;
  this.as = this.options.as;

  if (!this.as && this.isSelfAssociation) {
    throw new Error('\'as\' must be defined for many-to-many self-associations');
  }

  if (this.as) {
    this.isAliased = true;

    if (Utils._.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  this.combinedTableName = Utils.combineTableNames(
    this.source.tableName,
    this.isSelfAssociation ? (this.as || this.target.tableName) : this.target.tableName
  );

<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association - Unless we find a pairing association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  /*
   * Default/generated foreign/other keys
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else {
    if (!this.options.foreignKey) {
      this.foreignKeyDefault = true;
    }

    this.foreignKeyAttribute = {};
    this.foreignKey = this.options.foreignKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (_.isObject(this.options.otherKey)) {
    this.otherKeyAttribute = this.options.otherKey;
    this.otherKey = this.otherKeyAttribute.name || this.otherKeyAttribute.fieldName;
  } else {
    if (!this.options.otherKey) {
      this.otherKeyDefault = true;
    }

    this.otherKeyAttribute = {};
    this.otherKey = this.options.otherKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(
          this.isSelfAssociation ?
            Utils.singularize(this.as) :
            this.target.options.name.singular,
          this.target.options.underscored
        ),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.target.options.underscored
    );
  }

  /*
   * Find paired association (if exists)
   */
  _.each(this.target.associations, function(association) {
    if (association.associationType !== 'BelongsToMany') return;
    if (association.target !== this.source) return;

    if (this.options.through.model === association.options.through.model) {
      this.paired = association;
      association.paired = this;
    }
  }.bind(this));

  if (typeof this.through.model === 'string') {
    if (!this.sequelize.isDefined(this.through.model)) {
      this.through.model = this.sequelize.define(this.through.model, {}, _.extend(this.options, {
        tableName: this.through.model,
        indexes: [], //we don't want indexes here (as referenced in #2416)
        paranoid: false,  // A paranoid join table does not make sense
        validate: {} // Don't propagate model-level validations
      }));
    } else {
      this.through.model = this.sequelize.model(this.through.model);
    }
  }

  this.options = _.assign(this.options, _.pick(this.through.model.options, [
    'timestamps', 'createdAt', 'updatedAt ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.default"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.</span>default (source, target, options)](#apidoc.element.sequelize.Association.BelongsToMany.default)
- description and source-code
```javascript
default = function (source, target, options) {
  Association.call(this);

  options = options || {};

  if (options.through === undefined || options.through === true || options.through === null) {
    throw new Error('belongsToMany must be given a through option, either a string or a model');
  }

  if (!options.through.model) {
    options.through = {
      model: options.through
    };
  }

  this.associationType = 'BelongsToMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options;
  this.sequelize = source.modelManager.sequelize;
  this.through = _.assign({}, options.through);
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.doubleLinked = false;
  this.as = this.options.as;

  if (!this.as && this.isSelfAssociation) {
    throw new Error('\'as\' must be defined for many-to-many self-associations');
  }

  if (this.as) {
    this.isAliased = true;

    if (Utils._.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  this.combinedTableName = Utils.combineTableNames(
    this.source.tableName,
    this.isSelfAssociation ? (this.as || this.target.tableName) : this.target.tableName
  );

<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association - Unless we find a pairing association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  /*
   * Default/generated foreign/other keys
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else {
    if (!this.options.foreignKey) {
      this.foreignKeyDefault = true;
    }

    this.foreignKeyAttribute = {};
    this.foreignKey = this.options.foreignKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (_.isObject(this.options.otherKey)) {
    this.otherKeyAttribute = this.options.otherKey;
    this.otherKey = this.otherKeyAttribute.name || this.otherKeyAttribute.fieldName;
  } else {
    if (!this.options.otherKey) {
      this.otherKeyDefault = true;
    }

    this.otherKeyAttribute = {};
    this.otherKey = this.options.otherKey || Utils.camelizeIf(
      [
        Utils.underscoredIf(
          this.isSelfAssociation ?
            Utils.singularize(this.as) :
            this.target.options.name.singular,
          this.target.options.underscored
        ),
        this.target.primaryKeyAttribute
      ].join('_'),
      !this.target.options.underscored
    );
  }

  /*
   * Find paired association (if exists)
   */
  _.each(this.target.associations, function(association) {
    if (association.associationType !== 'BelongsToMany') return;
    if (association.target !== this.source) return;

    if (this.options.through.model === association.options.through.model) {
      this.paired = association;
      association.paired = this;
    }
  }.bind(this));

  if (typeof this.through.model === 'string') {
    if (!this.sequelize.isDefined(this.through.model)) {
      this.through.model = this.sequelize.define(this.through.model, {}, _.extend(this.options, {
        tableName: this.through.model,
        indexes: [], //we don't want indexes here (as referenced in #2416)
        paranoid: false,  // A paranoid join table does not make sense
        validate: {} // Don't propagate model-level validations
      }));
    } else {
      this.through.model = this.sequelize.model(this.through.model);
    }
  }

  this.options = _.assign(this.options, _.pick(this.through.model.options, [
    'timestamps', 'createdAt', 'updatedAt ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.</span>super_ ()](#apidoc.element.sequelize.Association.BelongsToMany.super_)
- description and source-code
```javascript
super_ = function () {}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association.BelongsToMany.prototype"></a>[module sequelize.Association.BelongsToMany.prototype](#apidoc.module.sequelize.Association.BelongsToMany.prototype)

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>get (instance, options)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.get)
- description and source-code
```javascript
get = function (instance, options) {
  options = Utils.cloneDeep(options) || {};

  var association = this
    , through = association.through
    , scopeWhere
    , throughWhere;

  if (association.scope) {
    scopeWhere = _.clone(association.scope);
  }

  options.where = {
    $and: [
      scopeWhere,
      options.where
    ]
  };

  if (Object(through.model) === through.model) {
    throughWhere = {};
    throughWhere[association.foreignKey] = instance.get(association.source.primaryKeyAttribute);

    if (through.scope) {
      _.assign(throughWhere, through.scope);
    }

    //If a user pass a where on the options through options, make an "and" with the current throughWhere
    if (options.through && options.through.where) {
      throughWhere = {
        $and: [throughWhere, options.through.where]
      };
    }

    options.include = options.include || [];
    options.include.push({
      association: association.oneFromTarget,
      attributes: options.joinTableAttributes,
      required: true,
      where: throughWhere
    });
  }

  var model = association.target;
  if (options.hasOwnProperty('scope')) {
    if (!options.scope) {
      model = model.unscoped();
    } else {
      model = model.scope(options.scope);
    }
  }

  if (options.hasOwnProperty('schema')) {
    model = model.schema(options.schema, options.schemaDelimiter);
  }

  return model.findAll(options);
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.prototype.injectAttributes"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectAttributes)
- description and source-code
```javascript
injectAttributes = function () {
  var self = this;

  this.identifier = this.foreignKey;
  this.foreignIdentifier = this.otherKey;

  // remove any PKs previously defined by sequelize
  // but ignore any keys that are part of this association (#5865)
  _.each(this.through.model.rawAttributes, function(attribute, attributeName) {
    if (attribute.primaryKey === true && attribute._autoGenerated === true) {
      if (attributeName === self.foreignKey || attributeName === self.otherKey) {
        // this key is still needed as it's part of the association
        // so just set primaryKey to false
        attribute.primaryKey = false;
      }
      else {
        delete self.through.model.rawAttributes[attributeName];
      }
      self.primaryKeyDeleted = true;
    }
  });

  var sourceKey = this.source.rawAttributes[this.source.primaryKeyAttribute]
    , sourceKeyType = sourceKey.type
    , sourceKeyField = sourceKey.field || this.source.primaryKeyAttribute
    , targetKey = this.target.rawAttributes[this.target.primaryKeyAttribute]
    , targetKeyType = targetKey.type
    , targetKeyField = targetKey.field || this.target.primaryKeyAttribute
    , sourceAttribute = _.defaults({}, this.foreignKeyAttribute, { type: sourceKeyType })
    , targetAttribute = _.defaults({}, this.otherKeyAttribute, { type: targetKeyType });

  if (this.primaryKeyDeleted === true) {
    targetAttribute.primaryKey = sourceAttribute.primaryKey = true;
  } else if (this.through.unique !== false) {
    var uniqueKey = [this.through.model.tableName, this.foreignKey, this.otherKey, 'unique'].join('_');
    targetAttribute.unique = sourceAttribute.unique = uniqueKey;
  }

  if (!this.through.model.rawAttributes[this.foreignKey]) {
    this.through.model.rawAttributes[this.foreignKey] = {
      _autoGenerated: true
    };
  }

  if (!this.through.model.rawAttributes[this.otherKey]) {
    this.through.model.rawAttributes[this.otherKey] = {
      _autoGenerated: true
    };
  }

  if (this.options.constraints !== false) {
    sourceAttribute.references = {
      model: this.source.getTableName(),
      key:   sourceKeyField
    };
    // For the source attribute the passed option is the priority
    sourceAttribute.onDelete = this.options.onDelete || this.through.model.rawAttributes[this.foreignKey].onDelete;
    sourceAttribute.onUpdate = this.options.onUpdate || this.through.model.rawAttributes[this.foreignKey].onUpdate;

    if (!sourceAttribute.onDelete) sourceAttribute.onDelete = 'CASCADE';
    if (!sourceAttribute.onUpdate) sourceAttribute.onUpdate = 'CASCADE';

    targetAttribute.references = {
      model: this.target.getTableName(),
      key:   targetKeyField
    };
    // But the for target attribute the previously defined option is the priority (since it could've been set by another belongsToMany
 call)
    targetAttribute.onDelete = this.through.model.rawAttributes[this.otherKey].onDelete || this.options.onDelete;
    targetAttribute.onUpdate = this.through.model.rawAttributes[this.otherKey].onUpdate || this.options.onUpdate;

    if (!targetAttribute.onDelete) targetAttribute.onDelete = 'CASCADE';
    if (!targetAttribute.onUpdate) targetAttribute.onUpdate = 'CASCADE';
  }

  this.through.model.rawAttributes[this.foreignKey] = _.extend(this.through.model.rawAttributes[this.foreignKey], sourceAttribute
);
  this.through.model.rawAttributes[this.otherKey] = _.extend(this.through.model.rawAttributes[this.otherKey], targetAttribute);

  this.identifierField = this.through.model.rawAttributes[this.foreignKey].field || this.foreignKey;
  this.foreignIdentifierField = this.through.model.rawAttributes[this.otherKey].field || this.otherKey;

  if (this.paired && !this.paired.foreignIdentifierField) {
    this.paired.foreignIdentifierField = this.through.model.rawAttributes[this.paired.otherKey].field || this.paired.otherKey;
  }

  this.through.model.refreshAttributes();

  this.toSource = new BelongsTo(this.through.model, this.source, {
    foreignKey: this.foreignKey
  });
  this.manyFromSource = new HasMany(this.source, this.through.model, {
    foreignKey: this.fo ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.prototype.injectCreator"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectCreator (obj)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectCreator)
- description and source-code
```javascript
injectCreator = function (obj) {
  var association = this;

  obj[this.accessors.create] = function(values, options) {
    var instance = this;
    options = options || {};
    values = values || {};

    if (Array.isArray(options)) {
      options = {
        fields: options
      };
    }

    if (association.scope) {
      _.assign(values, association.scope);
      if (options.fields) {
        options.fields = options.fields.concat(Object.keys(association.scope));
      }
    }

    // Create the related model instance
    return association.target.create(values, options).then(function(newAssociatedObject) {
      return instance[association.accessors.add](newAssociatedObject, _.omit(options, ['fields'])).return(newAssociatedObject);
    });
  };

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.prototype.injectGetter"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectGetter (obj)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectGetter)
- description and source-code
```javascript
injectGetter = function (obj) {
  var association = this;

  obj[this.accessors.get] = function(options) {
    return association.get(this, options);
  };

  obj[this.accessors.count] = function(options) {
    var model = association.target
      , sequelize = model.sequelize;

    options = Utils.cloneDeep(options);
    options.attributes = [
      [sequelize.fn('COUNT', sequelize.col([association.target.name, model.primaryKeyAttribute].join('.'))), 'count']
    ];
    options.joinTableAttributes = [];
    options.raw = true;
    options.plain = true;

    return obj[association.accessors.get].call(this, options).then(function (result) {
      return parseInt(result.count, 10);
    });
  };

  obj[this.accessors.hasSingle] = obj[this.accessors.hasAll] = function(instances, options) {
    var where = {};

    if (!Array.isArray(instances)) {
      instances = [instances];
    }

    options = _.assign({
      raw: true
    }, options, {
      scope: false
    });

    where.$or = instances.map(function (instance) {
      if (instance instanceof association.target.Instance) {
        return instance.where();
      } else {
        var $where = {};
        $where[association.target.primaryKeyAttribute] = instance;
        return $where;
      }
    });

    options.where = {
      $and: [
        where,
        options.where
      ]
    };

    return this[association.accessors.get](options).then(function(associatedObjects) {
      return associatedObjects.length === instances.length;
    });
  };

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.BelongsToMany.prototype.injectSetter"></a>[function <span class="apidocSignatureSpan">sequelize.Association.BelongsToMany.prototype.</span>injectSetter (obj)](#apidoc.element.sequelize.Association.BelongsToMany.prototype.injectSetter)
- description and source-code
```javascript
injectSetter = function (obj) {
  var association = this;

  obj[this.accessors.set] = function(newAssociatedObjects, options) {
    options = options || {};
    var instance = this
      , sourceKey = association.source.primaryKeyAttribute
      , targetKey = association.target.primaryKeyAttribute
      , identifier = association.identifier
      , foreignIdentifier = association.foreignIdentifier
      , where = {};

    if (newAssociatedObjects === null) {
      newAssociatedObjects = [];
    } else {
      newAssociatedObjects = association.toInstanceArray(newAssociatedObjects);
    }

    where[identifier] = this.get(sourceKey);
    _.assign(where, association.through.scope);

    return association.through.model.findAll(_.defaults({
      where: where,
      raw: true,
    }, options)).then(function (currentRows) {
      var obsoleteAssociations = []
        , defaultAttributes = options
        , promises = []
        , unassociatedObjects;

      // Don't try to insert the transaction as an attribute in the through table
      defaultAttributes = _.omit(defaultAttributes, ['transaction', 'hooks', 'individualHooks', 'ignoreDuplicates', 'validate', '
fields', 'logging']);

      unassociatedObjects = newAssociatedObjects.filter(function(obj) {
        return !_.find(currentRows, function(currentRow) {
          return currentRow[foreignIdentifier] === obj.get(targetKey);
        });
      });

      currentRows.forEach(function(currentRow) {
        var newObj = _.find(newAssociatedObjects, function(obj) {
          return currentRow[foreignIdentifier] === obj.get(targetKey);
        });

        if (!newObj) {
          obsoleteAssociations.push(currentRow);
        } else {
          var throughAttributes = newObj[association.through.model.name];
          // Quick-fix for subtle bug when using existing objects that might have the through model attached (not as an attribute
 object)
          if (throughAttributes instanceof association.through.model.Instance) {
            throughAttributes = {};
          }

          var where = {}
            , attributes = _.defaults({}, throughAttributes, defaultAttributes);

          where[identifier] = instance.get(sourceKey);
          where[foreignIdentifier] = newObj.get(targetKey);

          if (Object.keys(attributes).length) {
            promises.push(association.through.model.update(attributes, _.extend(options, {
              where: where
            })));
          }
        }
      });

      if (obsoleteAssociations.length > 0) {
        var where = {};
        where[identifier] = instance.get(sourceKey);
        where[foreignIdentifier] = obsoleteAssociations.map(function(obsoleteAssociation) {
          return obsoleteAssociation[foreignIdentifier];
        });

        promises.push(association.through.model.destroy(_.defaults({
          where: where
        }, options)));
      }

      if (unassociatedObjects.length > 0) {
        var bulk = unassociatedObjects.map(function(unassociatedObject) {
          var attributes = {};

          attributes[identifier] = instance.get(sourceKey);
          attributes[foreignIdentifier] = unassociatedObject.get(targetKey);

          attributes = _.defaults(attributes, unassociatedObject[association.through.model.name], defaultAttributes);

          _.assign(attributes, association.through.scope);

          return attributes;
        }.bind(this));

        promises.push(association.through.model.bulkCreate(bulk, _.assign({ validate: true }, options)));
      }

      return Utils.Promise.all(promises);
    });
  };

  obj[this.accessors.addMultiple] = obj[this.accessors.add] = function(newInstances, additionalAttributes) {
    // If newInstances is null or undefined, no-op
    if (!newInstances) return Utils.Promise.resolve();

    additionalAttributes = _.clone(additionalAttributes) || {};

    var instance = this
      , defaultAttributes = _.omit(additionalAttributes, ['transaction', 'hooks', 'individualHooks', 'ignoreDuplicates', 'validate
', 'fields', 'logging'])
      , sourceKey = association.source.primaryKeyAttr ...
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association.HasMany"></a>[module sequelize.Association.HasMany](#apidoc.module.sequelize.Association.HasMany)

#### <a name="apidoc.element.sequelize.Association.HasMany.HasMany"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>HasMany (source, target, options)](#apidoc.element.sequelize.Association.HasMany.HasMany)
- description and source-code
```javascript
HasMany = function (source, target, options) {
  Association.call(this);

  this.associationType = 'HasMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options || {};
  this.sequelize = source.modelManager.sequelize;
  this.through = options.through;
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.options.through) {
    throw new Error('N:M associations are not supported with hasMany. Use belongsToMany instead');
  }


<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  if (this.as) {
    this.isAliased = true;

    if (_.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  /*
   * Foreign key setup
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
    this.foreignKeyField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  this.sourceKey = this.options.sourceKey || this.source.primaryKeyAttribute;
  if (this.target.rawAttributes[this.sourceKey]) {
    this.sourceKeyField = this.source.rawAttributes[this.sourceKey].field || this.sourceKey;
  } else {
    this.sourceKeyField = this.sourceKey;
  }

  if (this.source.fieldRawAttributesMap[this.sourceKey]) {
    this.sourceKeyAttribute = this.source.fieldRawAttributesMap[this.sourceKey].fieldName;
  } else {
    this.sourceKeyAttribute = this.source.primaryKeyAttribute;
  }
  this.sourceIdentifier = this.sourceKey;
  this.associationAccessor = this.as;

  // Get singular and plural names, trying to uppercase the first letter, unless the model forbids it
  var plural = Utils.uppercaseFirst(this.options.name.plural)
    , singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
    /**
     * Get everything currently associated with this, using an optional where clause.
     *
     * @param {Object} [options]
     * @param {Object} [options.where] An optional where clause to limit the associated models
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Array<Instance>>}
     * @method getAssociations
     */
    get: 'get' + plural,
    /**
     * Set the associated models by passing an array of persisted instances or their primary keys. Everything that is not in the
 passed array will be un-associated
     *
     * @param {Array<Instance|String|Number>} [newAssociations] An array of persisted instances or primary key of instances to associate
 with this. Pass 'null' or 'undefined' to remove all associations.
     * @param {Object} [options] Options passed to 'target.findAll' and 'update'.
     * @param {Object} [options.validate] Run validation for the join model
     * @return {Promise}
     * @method setAssociations
     */
    set: 'set' + plural,
    /**
     * Associate several persisted instances with this.
     *
     * @param {Array<In ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany.default"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.</span>default (source, target, options)](#apidoc.element.sequelize.Association.HasMany.default)
- description and source-code
```javascript
default = function (source, target, options) {
  Association.call(this);

  this.associationType = 'HasMany';
  this.source = source;
  this.target = target;
  this.targetAssociation = null;
  this.options = options || {};
  this.sequelize = source.modelManager.sequelize;
  this.through = options.through;
  this.scope = options.scope;
  this.isMultiAssociation = true;
  this.isSelfAssociation = this.source === this.target;
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.options.through) {
    throw new Error('N:M associations are not supported with hasMany. Use belongsToMany instead');
  }


<span class="apidocCodeCommentSpan">  /*
   * If self association, this is the target association
   */
</span>  if (this.isSelfAssociation) {
    this.targetAssociation = this;
  }

  if (this.as) {
    this.isAliased = true;

    if (_.isPlainObject(this.as)) {
      this.options.name = this.as;
      this.as = this.as.plural;
    } else {
      this.options.name = {
        plural: this.as,
        singular: Utils.singularize(this.as)
      };
    }
  } else {
    this.as = this.target.options.name.plural;
    this.options.name = this.target.options.name;
  }

  /*
   * Foreign key setup
   */
  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(this.source.options.name.singular, this.source.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
    this.foreignKeyField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  this.sourceKey = this.options.sourceKey || this.source.primaryKeyAttribute;
  if (this.target.rawAttributes[this.sourceKey]) {
    this.sourceKeyField = this.source.rawAttributes[this.sourceKey].field || this.sourceKey;
  } else {
    this.sourceKeyField = this.sourceKey;
  }

  if (this.source.fieldRawAttributesMap[this.sourceKey]) {
    this.sourceKeyAttribute = this.source.fieldRawAttributesMap[this.sourceKey].fieldName;
  } else {
    this.sourceKeyAttribute = this.source.primaryKeyAttribute;
  }
  this.sourceIdentifier = this.sourceKey;
  this.associationAccessor = this.as;

  // Get singular and plural names, trying to uppercase the first letter, unless the model forbids it
  var plural = Utils.uppercaseFirst(this.options.name.plural)
    , singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
    /**
     * Get everything currently associated with this, using an optional where clause.
     *
     * @param {Object} [options]
     * @param {Object} [options.where] An optional where clause to limit the associated models
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Array<Instance>>}
     * @method getAssociations
     */
    get: 'get' + plural,
    /**
     * Set the associated models by passing an array of persisted instances or their primary keys. Everything that is not in the
 passed array will be un-associated
     *
     * @param {Array<Instance|String|Number>} [newAssociations] An array of persisted instances or primary key of instances to associate
 with this. Pass 'null' or 'undefined' to remove all associations.
     * @param {Object} [options] Options passed to 'target.findAll' and 'update'.
     * @param {Object} [options.validate] Run validation for the join model
     * @return {Promise}
     * @method setAssociations
     */
    set: 'set' + plural,
    /**
     * Associate several persisted instances with this.
     *
     * @param {Array<In ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.</span>super_ ()](#apidoc.element.sequelize.Association.HasMany.super_)
- description and source-code
```javascript
super_ = function () {}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association.HasMany.prototype"></a>[module sequelize.Association.HasMany.prototype](#apidoc.module.sequelize.Association.HasMany.prototype)

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.add"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>add (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.add)
- description and source-code
```javascript
add = function (sourceInstance, targetInstances, options) {
  if (!targetInstances) return Utils.Promise.resolve();

  var association = this
    , update = {}
    , where = {};

  options = options || {};

  targetInstances = association.toInstanceArray(targetInstances);

  update[association.foreignKey] = sourceInstance.get(association.sourceKey);
  _.assign(update, association.scope);

  where[association.target.primaryKeyAttribute] = targetInstances.map(function (unassociatedObject) {
    return unassociatedObject.get(association.target.primaryKeyAttribute);
  });

  return association.target.unscoped().update(
    update,
    _.defaults({
      where: where
    }, options)
  ).return(sourceInstance);
}
```
- example usage
```shell
...
    }
  }

  deps = deps.filter(function(dep) {
    return tableName !== dep;
  });

  sorter.add(tableName, deps);
});

sorted = sorter.sort();
if (options.reverse) {
  sorted = sorted.reverse();
}
sorted.forEach(function(name) {
...
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.count"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>count (instance, options)](#apidoc.element.sequelize.Association.HasMany.prototype.count)
- description and source-code
```javascript
count = function (instance, options) {
  var association = this
    , model = association.target
    , sequelize = model.sequelize;

  options = Utils.cloneDeep(options);
  options.attributes = [
    [sequelize.fn('COUNT', sequelize.col(model.primaryKeyField)), 'count']
  ];
  options.raw = true;
  options.plain = true;

  return this.get(instance, options).then(function (result) {
    return parseInt(result.count, 10);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.create"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>create (sourceInstance, values, options)](#apidoc.element.sequelize.Association.HasMany.prototype.create)
- description and source-code
```javascript
create = function (sourceInstance, values, options) {
  var association = this;

  options = options || {};

  if (Array.isArray(options)) {
    options = {
      fields: options
    };
  }

  if (values === undefined) {
    values = {};
  }

  if (association.scope) {
    Object.keys(association.scope).forEach(function (attribute) {
      values[attribute] = association.scope[attribute];
      if (options.fields) options.fields.push(attribute);
    });
  }

  values[association.foreignKey] = sourceInstance.get(association.sourceKey);
  if (options.fields) options.fields.push(association.foreignKey);
  return association.target.create(values, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>get (instances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.get)
- description and source-code
```javascript
get = function (instances, options) {
  var association = this
    , where = {}
    , Model = association.target
    , instance
    , values;

  if (!Array.isArray(instances)) {
    instance = instances;
    instances = undefined;
  }

  options = Utils.cloneDeep(options) || {};

  if (association.scope) {
    _.assign(where, association.scope);
  }

  if (instances) {
    values = instances.map(function (instance) {
      return instance.get(association.sourceKey, {raw: true});
    });

    if (options.limit && instances.length > 1) {
      options.groupedLimit = {
        limit: options.limit,
        on: association.foreignKeyField,
        values: values
      };

      delete options.limit;
    } else {
      where[association.foreignKey] = {
        $in: values
      };
      delete options.groupedLimit;
    }
  } else {
    where[association.foreignKey] = instance.get(association.sourceKey, {raw: true});
  }


  options.where = options.where ?
                  {$and: [where, options.where]} :
                  where;

  if (options.hasOwnProperty('scope')) {
    if (!options.scope) {
      Model = Model.unscoped();
    } else {
      Model = Model.scope(options.scope);
    }
  }

  if (options.hasOwnProperty('schema')) {
    Model = Model.schema(options.schema, options.schemaDelimiter);
  }


  return Model.findAll(options).then(function (results) {
    if (instance) return results;

    var result = {};
    instances.forEach(function (instance) {
      result[instance.get(association.sourceKey, {raw: true})] = [];
    });

    results.forEach(function (instance) {
      result[instance.get(association.foreignKey, {raw: true})].push(instance);
    });

    return result;
  });
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.has"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>has (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.has)
- description and source-code
```javascript
has = function (sourceInstance, targetInstances, options) {
  var association = this
    , where = {};

  if (!Array.isArray(targetInstances)) {
    targetInstances = [targetInstances];
  }

  options = _.assign({}, options, {
    scope: false,
    raw: true
  });

  where.$or = targetInstances.map(function (instance) {
    if (instance instanceof association.target.Instance) {
      return instance.where();
    } else {
      var _where = {};
      _where[association.target.primaryKeyAttribute] = instance;
      return _where;
    }
  });

  options.where = {
    $and: [
      where,
      options.where
    ]
  };

  return this.get(
    sourceInstance,
    options
  ).then(function(associatedObjects) {
    return associatedObjects.length === targetInstances.length;
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.injectAttributes"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.HasMany.prototype.injectAttributes)
- description and source-code
```javascript
injectAttributes = function () {
  var newAttributes = {};
  var constraintOptions = _.clone(this.options); // Create a new options object for use with addForeignKeyConstraints, to avoid
polluting this.options in case it is later used for a n:m
  newAttributes[this.foreignKey] = _.defaults({}, this.foreignKeyAttribute, {
    type: this.options.keyType || this.source.rawAttributes[this.sourceKeyAttribute].type,
    allowNull : true
  });

  if (this.options.constraints !== false) {
    var target = this.target.rawAttributes[this.foreignKey] || newAttributes[this.foreignKey];
    constraintOptions.onDelete = constraintOptions.onDelete || (target.allowNull ? 'SET NULL' : 'CASCADE');
    constraintOptions.onUpdate = constraintOptions.onUpdate || 'CASCADE';
  }
  Helpers.addForeignKeyConstraints(newAttributes[this.foreignKey], this.source, this.target, constraintOptions, this.sourceKeyField
);
  Utils.mergeDefaults(this.target.rawAttributes, newAttributes);

  this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  this.foreignKeyField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;

  this.target.refreshAttributes();
  this.source.refreshAttributes();

  Helpers.checkNamingCollision(this);

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.mixin"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>mixin (obj)](#apidoc.element.sequelize.Association.HasMany.prototype.mixin)
- description and source-code
```javascript
mixin = function (obj) {
  var association = this;

  obj[this.accessors.get] = function(options) {
    return association.get(this, options);
  };

  if (this.accessors.count) {
    obj[this.accessors.count] = function(options) {
      return association.count(this, options);
    };
  }

  obj[this.accessors.hasSingle] = obj[this.accessors.hasAll] = function(instances, options) {
    return association.has(this, instances, options);
  };

  obj[this.accessors.set] = function(instances, options) {
    return association.set(this, instances, options);
  };

  obj[this.accessors.add] = obj[this.accessors.addMultiple] = function(instances, options) {
    return association.add(this, instances, options);
  };

  obj[this.accessors.remove] = obj[this.accessors.removeMultiple] = function(instances, options) {
    return association.remove(this, instances, options);
  };

  obj[this.accessors.create] = function(values, options) {
    return association.create(this, values, options);
  };
}
```
- example usage
```shell
...
   */

module.exports = {
  hooks: hookTypes,
  hookAliases: hookAliases,

  applyTo: function(Model) {
Utils._.mixin(Model, Hooks);
Utils._.mixin(Model.prototype, Hooks);

var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
allHooks.forEach(function(hook) {
  Model.prototype[hook] = function(name, callback) {
    return this.addHook(hook, name, callback);
  };
...
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.remove"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>remove (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.remove)
- description and source-code
```javascript
remove = function (sourceInstance, targetInstances, options) {
  var association = this
    , update = {}
    , where = {};

  options = options || {};
  targetInstances = association.toInstanceArray(targetInstances);

  update[association.foreignKey] = null;

  where[association.foreignKey] = sourceInstance.get(association.sourceKey);
  where[association.target.primaryKeyAttribute] = targetInstances.map(function (targetInstance) {
    return targetInstance.get(association.target.primaryKeyAttribute);
  });

  return association.target.unscoped().update(
    update,
    _.defaults({
      where: where
    }, options)
  ).return(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasMany.prototype.set"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasMany.prototype.</span>set (sourceInstance, targetInstances, options)](#apidoc.element.sequelize.Association.HasMany.prototype.set)
- description and source-code
```javascript
set = function (sourceInstance, targetInstances, options) {
  var association = this;

  if (targetInstances === null) {
    targetInstances = [];
  } else {
    targetInstances = association.toInstanceArray(targetInstances);
  }

  return association.get(sourceInstance, _.defaults({
    scope: false,
    raw: true
  }, options)).then(function(oldAssociations) {
    var promises = []
      , obsoleteAssociations = oldAssociations.filter(function(old) {
          return !_.find(targetInstances, function(obj) {
            return obj[association.target.primaryKeyAttribute] === old[association.target.primaryKeyAttribute];
          });
        })
      , unassociatedObjects = targetInstances.filter(function(obj) {
          return !_.find(oldAssociations, function(old) {
            return obj[association.target.primaryKeyAttribute] === old[association.target.primaryKeyAttribute];
          });
        })
      , updateWhere
      , update;

    if (obsoleteAssociations.length > 0) {
      update = {};
      update[association.foreignKey] = null;

      updateWhere = {};

      updateWhere[association.target.primaryKeyAttribute] = obsoleteAssociations.map(function(associatedObject) {
        return associatedObject[association.target.primaryKeyAttribute];
      });

      promises.push(association.target.unscoped().update(
        update,
        _.defaults({
          where: updateWhere
        }, options)
      ));
    }

    if (unassociatedObjects.length > 0) {
      updateWhere = {};

      update = {};
      update[association.foreignKey] = sourceInstance.get(association.sourceKey);

      _.assign(update, association.scope);
      updateWhere[association.target.primaryKeyAttribute] = unassociatedObjects.map(function(unassociatedObject) {
        return unassociatedObject[association.target.primaryKeyAttribute];
      });

      promises.push(association.target.unscoped().update(
        update,
        _.defaults({
          where: updateWhere
        }, options)
      ));
    }

    return Utils.Promise.all(promises).return(sourceInstance);
  });
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association.HasOne"></a>[module sequelize.Association.HasOne](#apidoc.module.sequelize.Association.HasOne)

#### <a name="apidoc.element.sequelize.Association.HasOne.HasOne"></a>[function <span class="apidocSignatureSpan">sequelize.Association.</span>HasOne (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne.HasOne)
- description and source-code
```javascript
HasOne = function (srcModel, targetModel, options) {
  Association.call(this);

  this.associationType = 'HasOne';
  this.source = srcModel;
  this.target = targetModel;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(Utils.singularize(this.source.name), this.target.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.sourceIdentifier = this.source.primaryKeyAttribute;
  this.sourceKey = this.source.primaryKeyAttribute;
  this.sourceKeyIsPrimary = this.sourceKey === this.source.primaryKeyAttribute;

  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of a persisted instance to associate
 with this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to getAssociation and 'target.save'
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasOne.default"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasOne.</span>default (srcModel, targetModel, options)](#apidoc.element.sequelize.Association.HasOne.default)
- description and source-code
```javascript
default = function (srcModel, targetModel, options) {
  Association.call(this);

  this.associationType = 'HasOne';
  this.source = srcModel;
  this.target = targetModel;
  this.options = options;
  this.scope = options.scope;
  this.isSingleAssociation = true;
  this.isSelfAssociation = (this.source === this.target);
  this.as = this.options.as;
  this.foreignKeyAttribute = {};

  if (this.as) {
    this.isAliased = true;
    this.options.name = {
      singular: this.as
    };
  } else {
    this.as = this.target.options.name.singular;
    this.options.name = this.target.options.name;
  }

  if (_.isObject(this.options.foreignKey)) {
    this.foreignKeyAttribute = this.options.foreignKey;
    this.foreignKey = this.foreignKeyAttribute.name || this.foreignKeyAttribute.fieldName;
  } else if (this.options.foreignKey) {
    this.foreignKey = this.options.foreignKey;
  }

  if (!this.foreignKey) {
    this.foreignKey = Utils.camelizeIf(
      [
        Utils.underscoredIf(Utils.singularize(this.source.name), this.target.options.underscored),
        this.source.primaryKeyAttribute
      ].join('_'),
      !this.source.options.underscored
    );
  }

  this.sourceIdentifier = this.source.primaryKeyAttribute;
  this.sourceKey = this.source.primaryKeyAttribute;
  this.sourceKeyIsPrimary = this.sourceKey === this.source.primaryKeyAttribute;

  this.associationAccessor = this.as;
  this.options.useHooks = options.useHooks;

  if (this.target.rawAttributes[this.foreignKey]) {
    this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;
  }

  // Get singular name, trying to uppercase the first letter, unless the model forbids it
  var singular = Utils.uppercaseFirst(this.options.name.singular);

  this.accessors = {
<span class="apidocCodeCommentSpan">    /**
     * Get the associated instance.
     *
     * @param {Object} [options]
     * @param {String|Boolean} [options.scope] Apply a scope on the related model, or remove its default scope by passing false
     * @param {String} [options.schema] Apply a schema on the related model
     * @return {Promise<Instance>}
     * @method getAssociation
     */
</span>    get: 'get' + singular,
    /**
     * Set the associated model.
     *
     * @param {Instance|String|Number} [newAssociation] An persisted instance or the primary key of a persisted instance to associate
 with this. Pass 'null' or 'undefined' to remove the association.
     * @param {Object} [options] Options passed to getAssociation and 'target.save'
     * @return {Promise}
     * @method setAssociation
     */
    set: 'set' + singular,
    /**
     * Create a new instance of the associated model and associate it with this.
     *
     * @param {Object} [values]
     * @param {Object} [options] Options passed to 'target.create' and setAssociation.
     * @return {Promise}
     * @method createAssociation
     */
    create: 'create' + singular
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasOne.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasOne.</span>super_ ()](#apidoc.element.sequelize.Association.HasOne.super_)
- description and source-code
```javascript
super_ = function () {}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Association.HasOne.prototype"></a>[module sequelize.Association.HasOne.prototype](#apidoc.module.sequelize.Association.HasOne.prototype)

#### <a name="apidoc.element.sequelize.Association.HasOne.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>get (instances, options)](#apidoc.element.sequelize.Association.HasOne.prototype.get)
- description and source-code
```javascript
get = function (instances, options) {
  var association = this
    , Target = association.target
    , instance
    , where = {};

  options = Utils.cloneDeep(options);

  if (options.hasOwnProperty('scope')) {
    if (!options.scope) {
      Target = Target.unscoped();
    } else {
      Target = Target.scope(options.scope);
    }
  }

  if (options.hasOwnProperty('schema')) {
    Target = Target.schema(options.schema, options.schemaDelimiter);
  }

  if (!Array.isArray(instances)) {
    instance = instances;
    instances = undefined;
  }

  if (instances) {
    where[association.foreignKey] = {
      $in: instances.map(function (instance) {
        return instance.get(association.sourceKey);
      })
    };
  } else {
    where[association.foreignKey] = instance.get(association.sourceKey);
  }

  if (association.scope) {
    _.assign(where, association.scope);
  }

  options.where = options.where ?
                  {$and: [where, options.where]} :
                  where;

  if (instances) {
    return Target.findAll(options).then(function (results) {
      var result = {};
      instances.forEach(function (instance) {
        result[instance.get(association.sourceKey, {raw: true})] = null;
      });

      results.forEach(function (instance) {
        result[instance.get(association.foreignKey, {raw: true})] = instance;
      });

      return result;
    });
  }
  return Target.findOne(options);
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Association.HasOne.prototype.injectAttributes"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>injectAttributes ()](#apidoc.element.sequelize.Association.HasOne.prototype.injectAttributes)
- description and source-code
```javascript
injectAttributes = function () {
  var newAttributes = {}
    , keyType = this.source.rawAttributes[this.source.primaryKeyAttribute].type;

  newAttributes[this.foreignKey] = _.defaults({}, this.foreignKeyAttribute, {
    type: this.options.keyType || keyType,
    allowNull : true
  });
  Utils.mergeDefaults(this.target.rawAttributes, newAttributes);

  this.identifierField = this.target.rawAttributes[this.foreignKey].field || this.foreignKey;

  if (this.options.constraints !== false) {
    var target = this.target.rawAttributes[this.foreignKey] || newAttributes[this.foreignKey];
    this.options.onDelete = this.options.onDelete || (target.allowNull ? 'SET NULL' : 'CASCADE');
    this.options.onUpdate = this.options.onUpdate || 'CASCADE';
  }

  Helpers.addForeignKeyConstraints(this.target.rawAttributes[this.foreignKey], this.source, this.target, this.options);

  // Sync attributes and setters/getters to Model prototype
  this.target.refreshAttributes();

  Helpers.checkNamingCollision(this);

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasOne.prototype.injectCreator"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>injectCreator (instancePrototype)](#apidoc.element.sequelize.Association.HasOne.prototype.injectCreator)
- description and source-code
```javascript
injectCreator = function (instancePrototype) {
  var association = this;

  instancePrototype[this.accessors.create] = function(values, options) {
    var instance = this;
    values = values || {};
    options = options || {};

    if (association.scope) {
      Object.keys(association.scope).forEach(function (attribute) {
        values[attribute] = association.scope[attribute];
        if (options.fields) options.fields.push(attribute);
      });
    }

    values[association.foreignKey] = instance.get(association.sourceIdentifier);
    if (options.fields) options.fields.push(association.foreignKey);
    return association.target.create(values, options);
  };

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasOne.prototype.injectSetter"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>injectSetter (instancePrototype)](#apidoc.element.sequelize.Association.HasOne.prototype.injectSetter)
- description and source-code
```javascript
injectSetter = function (instancePrototype) {
  var association = this;

  instancePrototype[this.accessors.set] = function(associatedInstance, options) {
    var instance = this,
      alreadyAssociated;

    options = _.assign({}, options, {
      scope: false
    });
    return instance[association.accessors.get](options).then(function(oldInstance) {
      // TODO Use equals method once #5605 is resolved
      alreadyAssociated = oldInstance && associatedInstance && _.every(association.target.primaryKeyAttributes, function(attribute
) {
        return oldInstance.get(attribute, {raw: true}) === associatedInstance.get(attribute, {raw: true});
      });

      if (oldInstance && !alreadyAssociated) {
        oldInstance[association.foreignKey] = null;
        return oldInstance.save(_.extend({}, options, {
          fields: [association.foreignKey],
          allowNull: [association.foreignKey],
          association: true
        }));
      }
    }).then(function() {
      if (associatedInstance && !alreadyAssociated) {
        if (!(associatedInstance instanceof association.target.Instance)) {
          var tmpInstance = {};
          tmpInstance[association.target.primaryKeyAttribute] = associatedInstance;
          associatedInstance = association.target.build(tmpInstance, {
            isNewRecord: false
          });
        }

        _.assign(associatedInstance, association.scope);
        associatedInstance.set(association.foreignKey, instance.get(association.sourceIdentifier));

        return associatedInstance.save(options);
      }
      return null;
    });
  };

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.HasOne.prototype.mixin"></a>[function <span class="apidocSignatureSpan">sequelize.Association.HasOne.prototype.</span>mixin (obj)](#apidoc.element.sequelize.Association.HasOne.prototype.mixin)
- description and source-code
```javascript
mixin = function (obj) {
  var association = this;

  obj[this.accessors.get] = function(options) {
    return association.get(this, options);
  };

  association.injectSetter(obj);
  association.injectCreator(obj);
}
```
- example usage
```shell
...
   */

module.exports = {
  hooks: hookTypes,
  hookAliases: hookAliases,

  applyTo: function(Model) {
Utils._.mixin(Model, Hooks);
Utils._.mixin(Model.prototype, Hooks);

var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
allHooks.forEach(function(hook) {
  Model.prototype[hook] = function(name, callback) {
    return this.addHook(hook, name, callback);
  };
...
```



# <a name="apidoc.module.sequelize.Association.prototype"></a>[module sequelize.Association.prototype](#apidoc.module.sequelize.Association.prototype)

#### <a name="apidoc.element.sequelize.Association.prototype.inspect"></a>[function <span class="apidocSignatureSpan">sequelize.Association.prototype.</span>inspect ()](#apidoc.element.sequelize.Association.prototype.inspect)
- description and source-code
```javascript
inspect = function () {
  return this.as;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Association.prototype.toInstanceArray"></a>[function <span class="apidocSignatureSpan">sequelize.Association.prototype.</span>toInstanceArray (objs)](#apidoc.element.sequelize.Association.prototype.toInstanceArray)
- description and source-code
```javascript
toInstanceArray = function (objs) {
  if (!Array.isArray(objs)) {
    objs = [objs];
  }
  return objs.map(function(obj) {
    if (!(obj instanceof this.target.Instance)) {
      var tmpInstance = {};
      tmpInstance[this.target.primaryKeyAttribute] = obj;
      return this.target.build(tmpInstance, {
        isNewRecord: false
      });
    }
    return obj;
  }, this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.BIGINT"></a>[module sequelize.BIGINT](#apidoc.module.sequelize.BIGINT)

#### <a name="apidoc.element.sequelize.BIGINT.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>BIGINT (length)](#apidoc.element.sequelize.BIGINT.BIGINT)
- description and source-code
```javascript
BIGINT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BIGINT)) return new BIGINT(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BIGINT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.BIGINT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.BIGINT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.BIGINT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BIGINT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.BIGINT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.BIGINT.prototype"></a>[module sequelize.BIGINT.prototype](#apidoc.module.sequelize.BIGINT.prototype)

#### <a name="apidoc.element.sequelize.BIGINT.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.BIGINT.prototype.</span>validate (value)](#apidoc.element.sequelize.BIGINT.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!Validator.isInt(String(value))) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid bigint', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.BLOB"></a>[module sequelize.BLOB](#apidoc.module.sequelize.BLOB)

#### <a name="apidoc.element.sequelize.BLOB.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.</span>BLOB (length)](#apidoc.element.sequelize.BLOB.BLOB)
- description and source-code
```javascript
BLOB = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BLOB)) return new BLOB(options);
  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BLOB.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.BLOB.</span>inherits (Constructor)](#apidoc.element.sequelize.BLOB.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.BLOB.super_"></a>[function <span class="apidocSignatureSpan">sequelize.BLOB.</span>super_ (options)](#apidoc.element.sequelize.BLOB.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BLOB.warn"></a>[function <span class="apidocSignatureSpan">sequelize.BLOB.</span>warn (link, text)](#apidoc.element.sequelize.BLOB.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.BLOB.prototype"></a>[module sequelize.BLOB.prototype](#apidoc.module.sequelize.BLOB.prototype)

#### <a name="apidoc.element.sequelize.BLOB.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.BLOB.prototype.</span>toSql ()](#apidoc.element.sequelize.BLOB.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  switch (this._length.toLowerCase()) {
  case 'tiny':
    return 'TINYBLOB';
  case 'medium':
    return 'MEDIUMBLOB';
  case 'long':
    return 'LONGBLOB';
  default:
    return this.key;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BLOB.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.BLOB.prototype.</span>validate (value)](#apidoc.element.sequelize.BLOB.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!_.isString(value) && !Buffer.isBuffer(value)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid blob', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.BOOLEAN"></a>[module sequelize.BOOLEAN](#apidoc.module.sequelize.BOOLEAN)

#### <a name="apidoc.element.sequelize.BOOLEAN.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.</span>BOOLEAN ()](#apidoc.element.sequelize.BOOLEAN.BOOLEAN)
- description and source-code
```javascript
BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BOOLEAN.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>inherits (Constructor)](#apidoc.element.sequelize.BOOLEAN.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.BOOLEAN.super_"></a>[function <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>super_ (options)](#apidoc.element.sequelize.BOOLEAN.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BOOLEAN.warn"></a>[function <span class="apidocSignatureSpan">sequelize.BOOLEAN.</span>warn (link, text)](#apidoc.element.sequelize.BOOLEAN.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.BOOLEAN.prototype"></a>[module sequelize.BOOLEAN.prototype](#apidoc.module.sequelize.BOOLEAN.prototype)

#### <a name="apidoc.element.sequelize.BOOLEAN.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.BOOLEAN.prototype.</span>toSql ()](#apidoc.element.sequelize.BOOLEAN.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'TINYINT(1)';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.BOOLEAN.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.BOOLEAN.prototype.</span>validate (value)](#apidoc.element.sequelize.BOOLEAN.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!Validator.isBoolean(String(value))) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid boolean', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.CHAR"></a>[module sequelize.CHAR](#apidoc.module.sequelize.CHAR)

#### <a name="apidoc.element.sequelize.CHAR.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.</span>CHAR (length, binary)](#apidoc.element.sequelize.CHAR.CHAR)
- description and source-code
```javascript
CHAR = function (length, binary) {
  var options = typeof length === 'object' && length || {
    length: length,
    binary: binary
  };

  if (!(this instanceof CHAR)) return new CHAR(options);
  STRING.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.CHAR.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.CHAR.</span>inherits (Constructor)](#apidoc.element.sequelize.CHAR.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.CHAR.super_"></a>[function <span class="apidocSignatureSpan">sequelize.CHAR.</span>super_ (options)](#apidoc.element.sequelize.CHAR.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.CHAR.warn"></a>[function <span class="apidocSignatureSpan">sequelize.CHAR.</span>warn (link, text)](#apidoc.element.sequelize.CHAR.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.CHAR.prototype"></a>[module sequelize.CHAR.prototype](#apidoc.module.sequelize.CHAR.prototype)

#### <a name="apidoc.element.sequelize.CHAR.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.CHAR.prototype.</span>toSql ()](#apidoc.element.sequelize.CHAR.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'CHAR(' + this._length + ')' + ((this._binary) ? ' BINARY' : '');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ConnectionError"></a>[module sequelize.ConnectionError](#apidoc.module.sequelize.ConnectionError)

#### <a name="apidoc.element.sequelize.ConnectionError.ConnectionError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ConnectionError (parent)](#apidoc.element.sequelize.ConnectionError.ConnectionError)
- description and source-code
```javascript
ConnectionError = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ConnectionError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ConnectionError.</span>super_ ()](#apidoc.element.sequelize.ConnectionError.super_)
- description and source-code
```javascript
super_ = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ConnectionRefusedError"></a>[module sequelize.ConnectionRefusedError](#apidoc.module.sequelize.ConnectionRefusedError)

#### <a name="apidoc.element.sequelize.ConnectionRefusedError.ConnectionRefusedError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ConnectionRefusedError (parent)](#apidoc.element.sequelize.ConnectionRefusedError.ConnectionRefusedError)
- description and source-code
```javascript
ConnectionRefusedError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeConnectionRefusedError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ConnectionRefusedError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ConnectionRefusedError.</span>super_ (parent)](#apidoc.element.sequelize.ConnectionRefusedError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ConnectionTimedOutError"></a>[module sequelize.ConnectionTimedOutError](#apidoc.module.sequelize.ConnectionTimedOutError)

#### <a name="apidoc.element.sequelize.ConnectionTimedOutError.ConnectionTimedOutError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ConnectionTimedOutError (parent)](#apidoc.element.sequelize.ConnectionTimedOutError.ConnectionTimedOutError)
- description and source-code
```javascript
ConnectionTimedOutError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeConnectionTimedOutError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ConnectionTimedOutError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ConnectionTimedOutError.</span>super_ (parent)](#apidoc.element.sequelize.ConnectionTimedOutError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.DATE"></a>[module sequelize.DATE](#apidoc.module.sequelize.DATE)

#### <a name="apidoc.element.sequelize.DATE.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DATE (length)](#apidoc.element.sequelize.DATE.DATE)
- description and source-code
```javascript
DATE = function (length) {
  var options = typeof length === 'object' && length || {
      length: length
    };

  if (!(this instanceof DATE)) return new DATE(options);

  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DATE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.DATE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.DATE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.DATE.</span>super_ (options)](#apidoc.element.sequelize.DATE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DATE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.DATE.</span>warn (link, text)](#apidoc.element.sequelize.DATE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.DATE.prototype"></a>[module sequelize.DATE.prototype](#apidoc.module.sequelize.DATE.prototype)

#### <a name="apidoc.element.sequelize.DATE.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.DATE.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'DATETIME';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DATE.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.DATE.prototype.</span>validate (value)](#apidoc.element.sequelize.DATE.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!Validator.isDate(String(value))) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid date', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.DATEONLY"></a>[module sequelize.DATEONLY](#apidoc.module.sequelize.DATEONLY)

#### <a name="apidoc.element.sequelize.DATEONLY.DATEONLY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DATEONLY ()](#apidoc.element.sequelize.DATEONLY.DATEONLY)
- description and source-code
```javascript
DATEONLY = function () {
  if (!(this instanceof DATEONLY)) return new DATEONLY();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DATEONLY.super_"></a>[function <span class="apidocSignatureSpan">sequelize.DATEONLY.</span>super_ (options)](#apidoc.element.sequelize.DATEONLY.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.DATEONLY.prototype"></a>[module sequelize.DATEONLY.prototype](#apidoc.module.sequelize.DATEONLY.prototype)

#### <a name="apidoc.element.sequelize.DATEONLY.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.DATEONLY.prototype.</span>toSql ()](#apidoc.element.sequelize.DATEONLY.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'DATE';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.DECIMAL"></a>[module sequelize.DECIMAL](#apidoc.module.sequelize.DECIMAL)

#### <a name="apidoc.element.sequelize.DECIMAL.DECIMAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DECIMAL (precision, scale)](#apidoc.element.sequelize.DECIMAL.DECIMAL)
- description and source-code
```javascript
DECIMAL = function (precision, scale) {
  var options = typeof precision === 'object' && precision || {
    precision: precision,
    scale: scale
  };
  if (!(this instanceof DECIMAL)) return new DECIMAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DECIMAL.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>inherits (Constructor)](#apidoc.element.sequelize.DECIMAL.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.DECIMAL.super_"></a>[function <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>super_ (options)](#apidoc.element.sequelize.DECIMAL.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DECIMAL.warn"></a>[function <span class="apidocSignatureSpan">sequelize.DECIMAL.</span>warn (link, text)](#apidoc.element.sequelize.DECIMAL.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.DECIMAL.prototype"></a>[module sequelize.DECIMAL.prototype](#apidoc.module.sequelize.DECIMAL.prototype)

#### <a name="apidoc.element.sequelize.DECIMAL.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.DECIMAL.prototype.</span>toSql ()](#apidoc.element.sequelize.DECIMAL.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._precision || this._scale) {
    return 'DECIMAL(' + [this._precision, this._scale].filter(_.identity).join(',') + ')';
  }

  return 'DECIMAL';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DECIMAL.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.DECIMAL.prototype.</span>validate (value)](#apidoc.element.sequelize.DECIMAL.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!Validator.isDecimal(String(value))) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid decimal', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.DOUBLE"></a>[module sequelize.DOUBLE](#apidoc.module.sequelize.DOUBLE)

#### <a name="apidoc.element.sequelize.DOUBLE.DOUBLE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DOUBLE (length, decimals)](#apidoc.element.sequelize.DOUBLE.DOUBLE)
- description and source-code
```javascript
DOUBLE = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof DOUBLE)) return new DOUBLE(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DOUBLE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>inherits (Constructor)](#apidoc.element.sequelize.DOUBLE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.DOUBLE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>super_ (options)](#apidoc.element.sequelize.DOUBLE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DOUBLE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.DOUBLE.</span>warn (link, text)](#apidoc.element.sequelize.DOUBLE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.DOUBLE.prototype"></a>[module sequelize.DOUBLE.prototype](#apidoc.module.sequelize.DOUBLE.prototype)



# <a name="apidoc.module.sequelize.DataTypes"></a>[module sequelize.DataTypes](#apidoc.module.sequelize.DataTypes)

#### <a name="apidoc.element.sequelize.DataTypes.ABSTRACT"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>ABSTRACT (options)](#apidoc.element.sequelize.DataTypes.ABSTRACT)
- description and source-code
```javascript
ABSTRACT = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.ARRAY"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>ARRAY (type)](#apidoc.element.sequelize.DataTypes.ARRAY)
- description and source-code
```javascript
ARRAY = function (type) {
  var options = _.isPlainObject(type) ? type : {
    type: type
  };
  if (!(this instanceof ARRAY)) return new ARRAY(options);
  this.type = typeof options.type === 'function' ? new options.type() : options.type;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>BIGINT (length)](#apidoc.element.sequelize.DataTypes.BIGINT)
- description and source-code
```javascript
BIGINT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BIGINT)) return new BIGINT(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>BLOB (length)](#apidoc.element.sequelize.DataTypes.BLOB)
- description and source-code
```javascript
BLOB = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BLOB)) return new BLOB(options);
  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>BOOLEAN ()](#apidoc.element.sequelize.DataTypes.BOOLEAN)
- description and source-code
```javascript
BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>CHAR (length, binary)](#apidoc.element.sequelize.DataTypes.CHAR)
- description and source-code
```javascript
CHAR = function (length, binary) {
  var options = typeof length === 'object' && length || {
    length: length,
    binary: binary
  };

  if (!(this instanceof CHAR)) return new CHAR(options);
  STRING.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DATE (length)](#apidoc.element.sequelize.DataTypes.DATE)
- description and source-code
```javascript
DATE = function (length) {
  var options = typeof length === 'object' && length || {
      length: length
    };

  if (!(this instanceof DATE)) return new DATE(options);

  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.DATEONLY"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DATEONLY ()](#apidoc.element.sequelize.DataTypes.DATEONLY)
- description and source-code
```javascript
DATEONLY = function () {
  if (!(this instanceof DATEONLY)) return new DATEONLY();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.DECIMAL"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DECIMAL (precision, scale)](#apidoc.element.sequelize.DataTypes.DECIMAL)
- description and source-code
```javascript
DECIMAL = function (precision, scale) {
  var options = typeof precision === 'object' && precision || {
    precision: precision,
    scale: scale
  };
  if (!(this instanceof DECIMAL)) return new DECIMAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.DOUBLE"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>DOUBLE (length, decimals)](#apidoc.element.sequelize.DataTypes.DOUBLE)
- description and source-code
```javascript
DOUBLE = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof DOUBLE)) return new DOUBLE(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>ENUM (value)](#apidoc.element.sequelize.DataTypes.ENUM)
- description and source-code
```javascript
ENUM = function (value) {
  var options = typeof value === 'object' && !Array.isArray(value) && value || {
    values: Array.prototype.slice.call(arguments).reduce(function(result, element) {
      return result.concat(Array.isArray(element) ? element : [element]);
    }, [])
  };
  if (!(this instanceof ENUM)) return new ENUM(options);
  this.values = options.values;
  this.options = options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.DataTypes.FLOAT)
- description and source-code
```javascript
FLOAT = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof FLOAT)) return new FLOAT(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.GEOGRAPHY"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>GEOGRAPHY (type, srid)](#apidoc.element.sequelize.DataTypes.GEOGRAPHY)
- description and source-code
```javascript
GEOGRAPHY = function (type, srid) {
  var options = _.isPlainObject(type) ? type : {
    type: type,
    srid: srid
  };

  if (!(this instanceof GEOGRAPHY)) return new GEOGRAPHY(options);

  this.options = options;
  this.type = options.type;
  this.srid = options.srid;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>GEOMETRY (type, srid)](#apidoc.element.sequelize.DataTypes.GEOMETRY)
- description and source-code
```javascript
GEOMETRY = function (type, srid) {
  var options = _.isPlainObject(type) ? type : {
    type: type,
    srid: srid
  };

  if (!(this instanceof GEOMETRY)) return new GEOMETRY(options);

  this.options = options;
  this.type = options.type;
  this.srid = options.srid;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.HSTORE"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>HSTORE ()](#apidoc.element.sequelize.DataTypes.HSTORE)
- description and source-code
```javascript
HSTORE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>INTEGER (length)](#apidoc.element.sequelize.DataTypes.INTEGER)
- description and source-code
```javascript
INTEGER = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof INTEGER)) return new INTEGER(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.JSON"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>JSON ()](#apidoc.element.sequelize.DataTypes.JSON)
- description and source-code
```javascript
JSON = function () {
  if (!(this instanceof JSONTYPE)) return new JSONTYPE();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.JSONB"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>JSONB ()](#apidoc.element.sequelize.DataTypes.JSONB)
- description and source-code
```javascript
JSONB = function () {
  if (!(this instanceof JSONB)) return new JSONB();
  JSONTYPE.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.NONE"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NONE (ReturnType, fields)](#apidoc.element.sequelize.DataTypes.NONE)
- description and source-code
```javascript
NONE = function (ReturnType, fields) {
  if (!(this instanceof VIRTUAL)) return new VIRTUAL(ReturnType, fields);
  if (typeof ReturnType === 'function') ReturnType = new ReturnType();

  this.returnType = ReturnType;
  this.fields = fields;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.NOW"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NOW ()](#apidoc.element.sequelize.DataTypes.NOW)
- description and source-code
```javascript
NOW = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.NUMBER"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NUMBER (options)](#apidoc.element.sequelize.DataTypes.NUMBER)
- description and source-code
```javascript
NUMBER = function (options) {
  this.options = options;
  this._length = options.length;
  this._zerofill = options.zerofill;
  this._decimals = options.decimals;
  this._precision = options.precision;
  this._scale = options.scale;
  this._unsigned = options.unsigned;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.NUMERIC"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>NUMERIC (precision, scale)](#apidoc.element.sequelize.DataTypes.NUMERIC)
- description and source-code
```javascript
NUMERIC = function (precision, scale) {
  var options = typeof precision === 'object' && precision || {
    precision: precision,
    scale: scale
  };
  if (!(this instanceof DECIMAL)) return new DECIMAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.RANGE"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>RANGE (subtype)](#apidoc.element.sequelize.DataTypes.RANGE)
- description and source-code
```javascript
RANGE = function (subtype) {
  var options = _.isPlainObject(subtype) ? subtype : { subtype: subtype };

  if (!options.subtype) options.subtype = new INTEGER();

  if (_.isFunction(options.subtype)) {
    options.subtype = new options.subtype();
  }

  if (!(this instanceof RANGE)) return new RANGE(options);
  ABSTRACT.apply(this, arguments);

  this._subtype = options.subtype.key;
  this.options = options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>REAL (length, decimals)](#apidoc.element.sequelize.DataTypes.REAL)
- description and source-code
```javascript
REAL = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof REAL)) return new REAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>STRING (length, binary)](#apidoc.element.sequelize.DataTypes.STRING)
- description and source-code
```javascript
STRING = function (length, binary) {
  var options = typeof length === 'object' && length || {
    length: length,
    binary: binary
  };

  if (!(this instanceof STRING)) return new STRING(options);

  this.options = options;
  this._binary = options.binary;
  this._length = options.length || 255;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>TEXT (length)](#apidoc.element.sequelize.DataTypes.TEXT)
- description and source-code
```javascript
TEXT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof TEXT)) return new TEXT(options);
  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.TIME"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>TIME ()](#apidoc.element.sequelize.DataTypes.TIME)
- description and source-code
```javascript
TIME = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>UUID ()](#apidoc.element.sequelize.DataTypes.UUID)
- description and source-code
```javascript
UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.UUIDV1"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>UUIDV1 ()](#apidoc.element.sequelize.DataTypes.UUIDV1)
- description and source-code
```javascript
UUIDV1 = function () {
  if (!(this instanceof UUIDV1)) return new UUIDV1();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.UUIDV4"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>UUIDV4 ()](#apidoc.element.sequelize.DataTypes.UUIDV4)
- description and source-code
```javascript
UUIDV4 = function () {
  if (!(this instanceof UUIDV4)) return new UUIDV4();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DataTypes.VIRTUAL"></a>[function <span class="apidocSignatureSpan">sequelize.DataTypes.</span>VIRTUAL (ReturnType, fields)](#apidoc.element.sequelize.DataTypes.VIRTUAL)
- description and source-code
```javascript
VIRTUAL = function (ReturnType, fields) {
  if (!(this instanceof VIRTUAL)) return new VIRTUAL(ReturnType, fields);
  if (typeof ReturnType === 'function') ReturnType = new ReturnType();

  this.returnType = ReturnType;
  this.fields = fields;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.DatabaseError"></a>[module sequelize.DatabaseError](#apidoc.module.sequelize.DatabaseError)

#### <a name="apidoc.element.sequelize.DatabaseError.DatabaseError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>DatabaseError (parent)](#apidoc.element.sequelize.DatabaseError.DatabaseError)
- description and source-code
```javascript
DatabaseError = function (parent) {
  error.BaseError.apply(this, [parent.message]);
  this.name = 'SequelizeDatabaseError';

<span class="apidocCodeCommentSpan">/**
 * The database specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
/**
 * The SQL that triggered the error
 * @member sql
 */
  this.sql = parent.sql;
/**
 * The message from the DB.
 * @member message
 * @name message
 */
/**
 * The fields of the unique constraint
 * @member fields
 * @name fields
 */
/**
 * The value(s) which triggered the error
 * @member value
 * @name value
 */
/**
 * The name of the index that triggered the error
 * @member index
 * @name index
 */
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.DatabaseError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.DatabaseError.</span>super_ ()](#apidoc.element.sequelize.DatabaseError.super_)
- description and source-code
```javascript
super_ = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable"></a>[module sequelize.Deferrable](#apidoc.module.sequelize.Deferrable)

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED)
- description and source-code
```javascript
DEFERRABLE INITIALLY DEFERRED
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE)
- description and source-code
```javascript
DEFERRABLE INITIALLY IMMEDIATE
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.NOT"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>NOT {{signature}}](#apidoc.element.sequelize.Deferrable.NOT)
- description and source-code
```javascript
NOT DEFERRABLE
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_DEFERRED"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.SET_DEFERRED)
- description and source-code
```javascript
n/a
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_IMMEDIATE"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE)
- description and source-code
```javascript
n/a
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED"></a>[module sequelize.Deferrable.INITIALLY_DEFERRED](#apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED)

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.INITIALLY_DEFERRED"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.INITIALLY_DEFERRED)
- description and source-code
```javascript
DEFERRABLE INITIALLY DEFERRED
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.</span>super_ ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.super_)
- description and source-code
```javascript
function ABSTRACT() {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.</span>toString ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.toString)
- description and source-code
```javascript
toString = function () {
  var instance = new DeferrableType();
  return instance.toString.apply(instance, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED.prototype"></a>[module sequelize.Deferrable.INITIALLY_DEFERRED.prototype](#apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED.prototype)

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.prototype.</span>toSql ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'DEFERRABLE INITIALLY DEFERRED';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype"></a>[module sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype](#apidoc.module.sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype)

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype.</span>toString ()](#apidoc.element.sequelize.Deferrable.INITIALLY_DEFERRED.super_.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return this.toSql.apply(this, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.INITIALLY_IMMEDIATE"></a>[module sequelize.Deferrable.INITIALLY_IMMEDIATE](#apidoc.module.sequelize.Deferrable.INITIALLY_IMMEDIATE)

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.INITIALLY_IMMEDIATE"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>INITIALLY_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.INITIALLY_IMMEDIATE)
- description and source-code
```javascript
DEFERRABLE INITIALLY IMMEDIATE
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_IMMEDIATE.</span>super_ ()](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.super_)
- description and source-code
```javascript
function ABSTRACT() {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_IMMEDIATE.</span>toString ()](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.toString)
- description and source-code
```javascript
toString = function () {
  var instance = new DeferrableType();
  return instance.toString.apply(instance, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype"></a>[module sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype](#apidoc.module.sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype)

#### <a name="apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype.</span>toSql ()](#apidoc.element.sequelize.Deferrable.INITIALLY_IMMEDIATE.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'DEFERRABLE INITIALLY IMMEDIATE';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.NOT"></a>[module sequelize.Deferrable.NOT](#apidoc.module.sequelize.Deferrable.NOT)

#### <a name="apidoc.element.sequelize.Deferrable.NOT.NOT"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>NOT {{signature}}](#apidoc.element.sequelize.Deferrable.NOT.NOT)
- description and source-code
```javascript
NOT DEFERRABLE
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.NOT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.NOT.</span>super_ ()](#apidoc.element.sequelize.Deferrable.NOT.super_)
- description and source-code
```javascript
function ABSTRACT() {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.NOT.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.NOT.</span>toString ()](#apidoc.element.sequelize.Deferrable.NOT.toString)
- description and source-code
```javascript
toString = function () {
  var instance = new DeferrableType();
  return instance.toString.apply(instance, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.NOT.prototype"></a>[module sequelize.Deferrable.NOT.prototype](#apidoc.module.sequelize.Deferrable.NOT.prototype)

#### <a name="apidoc.element.sequelize.Deferrable.NOT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.NOT.prototype.</span>toSql ()](#apidoc.element.sequelize.Deferrable.NOT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'NOT DEFERRABLE';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.SET_DEFERRED"></a>[module sequelize.Deferrable.SET_DEFERRED](#apidoc.module.sequelize.Deferrable.SET_DEFERRED)

#### <a name="apidoc.element.sequelize.Deferrable.SET_DEFERRED.SET_DEFERRED"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_DEFERRED {{signature}}](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.SET_DEFERRED)
- description and source-code
```javascript
n/a
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_DEFERRED.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_DEFERRED.</span>super_ ()](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.super_)
- description and source-code
```javascript
function ABSTRACT() {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_DEFERRED.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_DEFERRED.</span>toString ()](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.toString)
- description and source-code
```javascript
toString = function () {
  var instance = new DeferrableType();
  return instance.toString.apply(instance, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.SET_DEFERRED.prototype"></a>[module sequelize.Deferrable.SET_DEFERRED.prototype](#apidoc.module.sequelize.Deferrable.SET_DEFERRED.prototype)

#### <a name="apidoc.element.sequelize.Deferrable.SET_DEFERRED.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_DEFERRED.prototype.</span>toSql (queryGenerator)](#apidoc.element.sequelize.Deferrable.SET_DEFERRED.prototype.toSql)
- description and source-code
```javascript
toSql = function (queryGenerator) {
  return queryGenerator.setDeferredQuery(this.constraints);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.SET_IMMEDIATE"></a>[module sequelize.Deferrable.SET_IMMEDIATE](#apidoc.module.sequelize.Deferrable.SET_IMMEDIATE)

#### <a name="apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.SET_IMMEDIATE"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.</span>SET_IMMEDIATE {{signature}}](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.SET_IMMEDIATE)
- description and source-code
```javascript
n/a
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_IMMEDIATE.</span>super_ ()](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.super_)
- description and source-code
```javascript
function ABSTRACT() {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_IMMEDIATE.</span>toString ()](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.toString)
- description and source-code
```javascript
toString = function () {
  var instance = new DeferrableType();
  return instance.toString.apply(instance, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Deferrable.SET_IMMEDIATE.prototype"></a>[module sequelize.Deferrable.SET_IMMEDIATE.prototype](#apidoc.module.sequelize.Deferrable.SET_IMMEDIATE.prototype)

#### <a name="apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.Deferrable.SET_IMMEDIATE.prototype.</span>toSql (queryGenerator)](#apidoc.element.sequelize.Deferrable.SET_IMMEDIATE.prototype.toSql)
- description and source-code
```javascript
toSql = function (queryGenerator) {
  return queryGenerator.setImmediateQuery(this.constraints);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ENUM"></a>[module sequelize.ENUM](#apidoc.module.sequelize.ENUM)

#### <a name="apidoc.element.sequelize.ENUM.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ENUM (value)](#apidoc.element.sequelize.ENUM.ENUM)
- description and source-code
```javascript
ENUM = function (value) {
  var options = typeof value === 'object' && !Array.isArray(value) && value || {
    values: Array.prototype.slice.call(arguments).reduce(function(result, element) {
      return result.concat(Array.isArray(element) ? element : [element]);
    }, [])
  };
  if (!(this instanceof ENUM)) return new ENUM(options);
  this.values = options.values;
  this.options = options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ENUM.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.ENUM.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.ENUM.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ENUM.</span>super_ (options)](#apidoc.element.sequelize.ENUM.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ENUM.warn"></a>[function <span class="apidocSignatureSpan">sequelize.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.ENUM.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ENUM.prototype"></a>[module sequelize.ENUM.prototype](#apidoc.module.sequelize.ENUM.prototype)

#### <a name="apidoc.element.sequelize.ENUM.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.ENUM.prototype.</span>validate (value)](#apidoc.element.sequelize.ENUM.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!_.includes(this.values, value)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid choice in %j', value, this.values));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.EmptyResultError"></a>[module sequelize.EmptyResultError](#apidoc.module.sequelize.EmptyResultError)

#### <a name="apidoc.element.sequelize.EmptyResultError.EmptyResultError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>EmptyResultError (message)](#apidoc.element.sequelize.EmptyResultError.EmptyResultError)
- description and source-code
```javascript
EmptyResultError = function (message) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeEmptyResultError';
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.EmptyResultError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.EmptyResultError.</span>super_ ()](#apidoc.element.sequelize.EmptyResultError.super_)
- description and source-code
```javascript
super_ = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Error"></a>[module sequelize.Error](#apidoc.module.sequelize.Error)

#### <a name="apidoc.element.sequelize.Error.Error"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Error ()](#apidoc.element.sequelize.Error.Error)
- description and source-code
```javascript
Error = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Error.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Error.</span>super_ ()](#apidoc.element.sequelize.Error.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Error.super_"></a>[module sequelize.Error.super_](#apidoc.module.sequelize.Error.super_)

#### <a name="apidoc.element.sequelize.Error.super_.super_"></a>[function <span class="apidocSignatureSpan">sequelize.Error.</span>super_ ()](#apidoc.element.sequelize.Error.super_.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Error.super_.captureStackTrace"></a>[function <span class="apidocSignatureSpan">sequelize.Error.super_.</span>captureStackTrace ()](#apidoc.element.sequelize.Error.super_.captureStackTrace)
- description and source-code
```javascript
function captureStackTrace() { [native code] }
```
- example usage
```shell
...
*/
error.BaseError = function() {
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
...
```



# <a name="apidoc.module.sequelize.ExclusionConstraintError"></a>[module sequelize.ExclusionConstraintError](#apidoc.module.sequelize.ExclusionConstraintError)

#### <a name="apidoc.element.sequelize.ExclusionConstraintError.ExclusionConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ExclusionConstraintError (options)](#apidoc.element.sequelize.ExclusionConstraintError.ExclusionConstraintError)
- description and source-code
```javascript
ExclusionConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };

  error.DatabaseError.call(this, options.parent);
  this.name = 'SequelizeExclusionConstraintError';

  this.message = options.message || options.parent.message;
  this.constraint = options.constraint;
  this.fields = options.fields;
  this.table = options.table;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ExclusionConstraintError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ExclusionConstraintError.</span>super_ (parent)](#apidoc.element.sequelize.ExclusionConstraintError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent.message]);
  this.name = 'SequelizeDatabaseError';

<span class="apidocCodeCommentSpan">/**
 * The database specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
/**
 * The SQL that triggered the error
 * @member sql
 */
  this.sql = parent.sql;
/**
 * The message from the DB.
 * @member message
 * @name message
 */
/**
 * The fields of the unique constraint
 * @member fields
 * @name fields
 */
/**
 * The value(s) which triggered the error
 * @member value
 * @name value
 */
/**
 * The name of the index that triggered the error
 * @member index
 * @name index
 */
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.FLOAT"></a>[module sequelize.FLOAT](#apidoc.module.sequelize.FLOAT)

#### <a name="apidoc.element.sequelize.FLOAT.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.FLOAT.FLOAT)
- description and source-code
```javascript
FLOAT = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof FLOAT)) return new FLOAT(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.FLOAT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.FLOAT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.FLOAT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.FLOAT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.FLOAT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.FLOAT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.FLOAT.prototype"></a>[module sequelize.FLOAT.prototype](#apidoc.module.sequelize.FLOAT.prototype)

#### <a name="apidoc.element.sequelize.FLOAT.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.FLOAT.prototype.</span>validate (value)](#apidoc.element.sequelize.FLOAT.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!Validator.isFloat(String(value))) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid float', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.ForeignKeyConstraintError"></a>[module sequelize.ForeignKeyConstraintError](#apidoc.module.sequelize.ForeignKeyConstraintError)

#### <a name="apidoc.element.sequelize.ForeignKeyConstraintError.ForeignKeyConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ForeignKeyConstraintError (options)](#apidoc.element.sequelize.ForeignKeyConstraintError.ForeignKeyConstraintError)
- description and source-code
```javascript
ForeignKeyConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };

  error.DatabaseError.call(this, options.parent);
  this.name = 'SequelizeForeignKeyConstraintError';

  this.message = options.message || options.parent.message || 'Database Error';
  this.fields = options.fields;
  this.table = options.table;
  this.value = options.value;
  this.index = options.index;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.ForeignKeyConstraintError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ForeignKeyConstraintError.</span>super_ (parent)](#apidoc.element.sequelize.ForeignKeyConstraintError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent.message]);
  this.name = 'SequelizeDatabaseError';

<span class="apidocCodeCommentSpan">/**
 * The database specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
/**
 * The SQL that triggered the error
 * @member sql
 */
  this.sql = parent.sql;
/**
 * The message from the DB.
 * @member message
 * @name message
 */
/**
 * The fields of the unique constraint
 * @member fields
 * @name fields
 */
/**
 * The value(s) which triggered the error
 * @member value
 * @name value
 */
/**
 * The name of the index that triggered the error
 * @member index
 * @name index
 */
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.GEOGRAPHY"></a>[module sequelize.GEOGRAPHY](#apidoc.module.sequelize.GEOGRAPHY)

#### <a name="apidoc.element.sequelize.GEOGRAPHY.GEOGRAPHY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>GEOGRAPHY (type, srid)](#apidoc.element.sequelize.GEOGRAPHY.GEOGRAPHY)
- description and source-code
```javascript
GEOGRAPHY = function (type, srid) {
  var options = _.isPlainObject(type) ? type : {
    type: type,
    srid: srid
  };

  if (!(this instanceof GEOGRAPHY)) return new GEOGRAPHY(options);

  this.options = options;
  this.type = options.type;
  this.srid = options.srid;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.GEOGRAPHY.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>inherits (Constructor)](#apidoc.element.sequelize.GEOGRAPHY.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.GEOGRAPHY.super_"></a>[function <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>super_ (options)](#apidoc.element.sequelize.GEOGRAPHY.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.GEOGRAPHY.warn"></a>[function <span class="apidocSignatureSpan">sequelize.GEOGRAPHY.</span>warn (link, text)](#apidoc.element.sequelize.GEOGRAPHY.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.GEOGRAPHY.prototype"></a>[module sequelize.GEOGRAPHY.prototype](#apidoc.module.sequelize.GEOGRAPHY.prototype)



# <a name="apidoc.module.sequelize.GEOMETRY"></a>[module sequelize.GEOMETRY](#apidoc.module.sequelize.GEOMETRY)

#### <a name="apidoc.element.sequelize.GEOMETRY.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.</span>GEOMETRY (type, srid)](#apidoc.element.sequelize.GEOMETRY.GEOMETRY)
- description and source-code
```javascript
GEOMETRY = function (type, srid) {
  var options = _.isPlainObject(type) ? type : {
    type: type,
    srid: srid
  };

  if (!(this instanceof GEOMETRY)) return new GEOMETRY(options);

  this.options = options;
  this.type = options.type;
  this.srid = options.srid;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.GEOMETRY.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>inherits (Constructor)](#apidoc.element.sequelize.GEOMETRY.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.GEOMETRY.super_"></a>[function <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>super_ (options)](#apidoc.element.sequelize.GEOMETRY.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.GEOMETRY.warn"></a>[function <span class="apidocSignatureSpan">sequelize.GEOMETRY.</span>warn (link, text)](#apidoc.element.sequelize.GEOMETRY.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.GEOMETRY.prototype"></a>[module sequelize.GEOMETRY.prototype](#apidoc.module.sequelize.GEOMETRY.prototype)



# <a name="apidoc.module.sequelize.HSTORE"></a>[module sequelize.HSTORE](#apidoc.module.sequelize.HSTORE)

#### <a name="apidoc.element.sequelize.HSTORE.HSTORE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>HSTORE ()](#apidoc.element.sequelize.HSTORE.HSTORE)
- description and source-code
```javascript
HSTORE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.HSTORE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.HSTORE.</span>inherits (Constructor)](#apidoc.element.sequelize.HSTORE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.HSTORE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.HSTORE.</span>super_ (options)](#apidoc.element.sequelize.HSTORE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.HSTORE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.HSTORE.</span>warn (link, text)](#apidoc.element.sequelize.HSTORE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.HSTORE.prototype"></a>[module sequelize.HSTORE.prototype](#apidoc.module.sequelize.HSTORE.prototype)

#### <a name="apidoc.element.sequelize.HSTORE.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.HSTORE.prototype.</span>validate (value)](#apidoc.element.sequelize.HSTORE.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!_.isPlainObject(value)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid hstore', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.HostNotFoundError"></a>[module sequelize.HostNotFoundError](#apidoc.module.sequelize.HostNotFoundError)

#### <a name="apidoc.element.sequelize.HostNotFoundError.HostNotFoundError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>HostNotFoundError (parent)](#apidoc.element.sequelize.HostNotFoundError.HostNotFoundError)
- description and source-code
```javascript
HostNotFoundError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeHostNotFoundError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.HostNotFoundError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.HostNotFoundError.</span>super_ (parent)](#apidoc.element.sequelize.HostNotFoundError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.HostNotReachableError"></a>[module sequelize.HostNotReachableError](#apidoc.module.sequelize.HostNotReachableError)

#### <a name="apidoc.element.sequelize.HostNotReachableError.HostNotReachableError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>HostNotReachableError (parent)](#apidoc.element.sequelize.HostNotReachableError.HostNotReachableError)
- description and source-code
```javascript
HostNotReachableError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeHostNotReachableError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.HostNotReachableError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.HostNotReachableError.</span>super_ (parent)](#apidoc.element.sequelize.HostNotReachableError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.INTEGER"></a>[module sequelize.INTEGER](#apidoc.module.sequelize.INTEGER)

#### <a name="apidoc.element.sequelize.INTEGER.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>INTEGER (length)](#apidoc.element.sequelize.INTEGER.INTEGER)
- description and source-code
```javascript
INTEGER = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof INTEGER)) return new INTEGER(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.INTEGER.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.INTEGER.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.INTEGER.super_"></a>[function <span class="apidocSignatureSpan">sequelize.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.INTEGER.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.INTEGER.warn"></a>[function <span class="apidocSignatureSpan">sequelize.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.INTEGER.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.INTEGER.prototype"></a>[module sequelize.INTEGER.prototype](#apidoc.module.sequelize.INTEGER.prototype)

#### <a name="apidoc.element.sequelize.INTEGER.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.INTEGER.prototype.</span>validate (value)](#apidoc.element.sequelize.INTEGER.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!Validator.isInt(String(value))) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid integer', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.Instance"></a>[module sequelize.Instance](#apidoc.module.sequelize.Instance)

#### <a name="apidoc.element.sequelize.Instance.Instance"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Instance (values, options)](#apidoc.element.sequelize.Instance.Instance)
- description and source-code
```javascript
Instance = function (values, options) {
  this.dataValues = {};
  this._previousDataValues = {};
  this._changed = {};
  this.$modelOptions = this.Model.options;
  this.$options = options || {};
  this.hasPrimaryKeys = this.Model.options.hasPrimaryKeys;
  this.__eagerlyLoadedAssociations = [];
<span class="apidocCodeCommentSpan">  /**
   * Returns true if this instance has not yet been persisted to the database
   * @property isNewRecord
   * @return {Boolean}
   */
</span>  this.isNewRecord = options.isNewRecord;

  /**
   * Returns the Model the instance was created from.
   * @see {Model}
   * @property Model
   * @return {Model}
   */

  initValues.call(this, values, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.default"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.</span>default (values, options)](#apidoc.element.sequelize.Instance.default)
- description and source-code
```javascript
default = function (values, options) {
  this.dataValues = {};
  this._previousDataValues = {};
  this._changed = {};
  this.$modelOptions = this.Model.options;
  this.$options = options || {};
  this.hasPrimaryKeys = this.Model.options.hasPrimaryKeys;
  this.__eagerlyLoadedAssociations = [];
<span class="apidocCodeCommentSpan">  /**
   * Returns true if this instance has not yet been persisted to the database
   * @property isNewRecord
   * @return {Boolean}
   */
</span>  this.isNewRecord = options.isNewRecord;

  /**
   * Returns the Model the instance was created from.
   * @see {Model}
   * @property Model
   * @return {Model}
   */

  initValues.call(this, values, options);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Instance.prototype"></a>[module sequelize.Instance.prototype](#apidoc.module.sequelize.Instance.prototype)

#### <a name="apidoc.element.sequelize.Instance.prototype._setInclude"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>_setInclude (key, value, options)](#apidoc.element.sequelize.Instance.prototype._setInclude)
- description and source-code
```javascript
_setInclude = function (key, value, options) {
  if (!Array.isArray(value)) value = [value];
  if (value[0] instanceof Instance) {
    value = value.map(function(instance) {
      return instance.dataValues;
    });
  }

  var include = this.$options.includeMap[key]
    , association = include.association
    , self = this
    , accessor = key
    , childOptions
    , primaryKeyAttribute  = include.model.primaryKeyAttribute
    , isEmpty;

  if (!isEmpty) {
    childOptions = {
      isNewRecord: this.isNewRecord,
      include: include.include,
      includeNames: include.includeNames,
      includeMap: include.includeMap,
      includeValidated: true,
      raw: options.raw,
      attributes: include.originalAttributes
    };
  }
  if (include.originalAttributes === undefined || include.originalAttributes.length) {
    if (association.isSingleAssociation) {
      if (Array.isArray(value)) {
        value = value[0];
      }

      isEmpty = (value && value[primaryKeyAttribute] === null) || (value === null);
      self[accessor] = self.dataValues[accessor] = isEmpty ? null : include.model.build(value, childOptions);
    } else {
      isEmpty = value[0] && value[0][primaryKeyAttribute] === null;
      self[accessor] = self.dataValues[accessor] = isEmpty ? [] : include.model.bulkBuild(value, childOptions);
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.changed"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>changed (key, value)](#apidoc.element.sequelize.Instance.prototype.changed)
- description and source-code
```javascript
changed = function (key, value) {
  if (key) {
    if (value !== undefined) {
      this._changed[key] = value;
      return this;
    }
    return this._changed[key] || false;
  }

  var changed = Object.keys(this.dataValues).filter(function(key) {
    return this.changed(key);
  }.bind(this));

  return changed.length ? changed : false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.decrement"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>decrement (fields, options)](#apidoc.element.sequelize.Instance.prototype.decrement)
- description and source-code
```javascript
decrement = function (fields, options) {
  options = _.defaults({}, options, {
    by: 1
  });

  if (!Utils._.isString(fields) && !Utils._.isArray(fields)) { // Assume fields is key-value pairs
    Utils._.each(fields, function(value, field) {
      fields[field] = -value;
    });
  }

  options.by = 0 - options.by;

  return this.increment(fields, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.destroy"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>destroy (options)](#apidoc.element.sequelize.Instance.prototype.destroy)
- description and source-code
```javascript
destroy = function (options) {
  options = Utils._.extend({
    hooks: true,
    force: false
  }, options);

  return Promise.bind(this).then(function() {
    // Run before hook
    if (options.hooks) {
      return this.Model.runHooks('beforeDestroy', this, options);
    }
  }).then(function() {
    var where = this.where();

    if (this.Model._timestampAttributes.deletedAt && options.force === false) {
      var attribute = this.Model.rawAttributes[this.Model._timestampAttributes.deletedAt]
        , field = attribute.field || this.Model._timestampAttributes.deletedAt
        , values = {};

      values[field] = new Date();
      where[field] = attribute.hasOwnProperty('defaultValue') ? attribute.defaultValue : null;

      this.setDataValue(field, values[field]);

      return this.sequelize.getQueryInterface().update(this, this.$Model.getTableName(options), values, where, _.defaults({ hooks
: false }, options));
    } else {
      return this.sequelize.getQueryInterface().delete(this, this.$Model.getTableName(options), where, _.assign({ type: QueryTypes
.DELETE, limit: null }, options));
    }
  }).tap(function() {
    // Run after hook
    if (options.hooks) {
      return this.Model.runHooks('afterDestroy', this, options);
    }
  }).then(function(result) {
    return result;
  });
}
```
- example usage
```shell
...
      if (!instances) {
        return Promise.resolve();
      }

      if (!Array.isArray(instances)) instances = [instances];

      return Promise.each(instances, function (instance) {
        return instance.destroy(options);
      });
    });
  }).then(function () {
    options.instance = instance;
    return self.sequelize.query(sql, options);
  });
};
...
```

#### <a name="apidoc.element.sequelize.Instance.prototype.equals"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>equals (other)](#apidoc.element.sequelize.Instance.prototype.equals)
- description and source-code
```javascript
equals = function (other) {
  var result = true;

  if (!other || !other.dataValues) {
    return false;
  }

  Utils._.each(this.dataValues, function(value, key) {
    if (Utils._.isDate(value) && Utils._.isDate(other.dataValues[key])) {
      result = result && (value.getTime() === other.dataValues[key].getTime());
    } else {
      result = result && (value === other.dataValues[key]);
    }
  });

  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.equalsOneOf"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>equalsOneOf (others)](#apidoc.element.sequelize.Instance.prototype.equalsOneOf)
- description and source-code
```javascript
equalsOneOf = function (others) {
  var self = this;

  return _.some(others, function(other) {
    return self.equals(other);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>get (key, options)](#apidoc.element.sequelize.Instance.prototype.get)
- description and source-code
```javascript
get = function (key, options) { // testhint options:none
  if (options === undefined && typeof key === 'object') {
    options = key;
    key = undefined;
  }

  if (key) {
    if (this._customGetters[key]) {
      return this._customGetters[key].call(this, key);
    }
    if (options && options.plain && this.$options.include && this.$options.includeNames.indexOf(key) !== -1) {
      if (Array.isArray(this.dataValues[key])) {
        return this.dataValues[key].map(function (instance) {
          return instance.get({plain: options.plain});
        });
      } else if (this.dataValues[key] instanceof Instance) {
        return this.dataValues[key].get({plain: options.plain});
      } else {
        return this.dataValues[key];
      }
    }
		return this.dataValues[key];
  }

  if (this._hasCustomGetters || (options && options.plain && this.$options.include) || (options && options.clone)) {
    var values = {}
      , _key;

    if (this._hasCustomGetters) {
      for (_key in this._customGetters) {
        if (this._customGetters.hasOwnProperty(_key)) {
          values[_key] = this.get(_key, options);
        }
      }
    }

    for (_key in this.dataValues) {
      if (!values.hasOwnProperty(_key) && this.dataValues.hasOwnProperty(_key)) {
        values[_key] = this.get(_key, options);
      }
    }
    return values;
  }
  return this.dataValues;
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Instance.prototype.getDataValue"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>getDataValue (key)](#apidoc.element.sequelize.Instance.prototype.getDataValue)
- description and source-code
```javascript
getDataValue = function (key) {
  return this.dataValues[key];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.hookValidate"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>hookValidate (options)](#apidoc.element.sequelize.Instance.prototype.hookValidate)
- description and source-code
```javascript
hookValidate = function (options) {
  return new InstanceValidator(this, options).hookValidate();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.increment"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>increment (fields, options)](#apidoc.element.sequelize.Instance.prototype.increment)
- description and source-code
```javascript
increment = function (fields, options) {
  var identifier = this.where()
    , updatedAtAttr = this.Model._timestampAttributes.updatedAt
    , values = {}
    , where;

  options = _.defaults({}, options, {
    by: 1,
    attributes: {},
    where: {}
  });

  where = _.extend({}, options.where, identifier);

  if (Utils._.isString(fields)) {
    values[fields] = options.by;
  } else if (Utils._.isArray(fields)) {
    Utils._.each(fields, function(field) {
      values[field] = options.by;
    });
  } else { // Assume fields is key-value pairs
    values = fields;
  }

  if (!options.silent && updatedAtAttr && !values[updatedAtAttr]) {
    options.attributes[updatedAtAttr] = this.Model.$getDefaultTimestamp(updatedAtAttr) || Utils.now(this.sequelize.options.dialect
);
  }

  Object.keys(values).forEach(function(attr) {
    // Field name mapping
    if (this.Model.rawAttributes[attr] && this.Model.rawAttributes[attr].field && this.Model.rawAttributes[attr].field !== attr) {
      values[this.Model.rawAttributes[attr].field] = values[attr];
      delete values[attr];
    }
  }, this);

  return this.sequelize.getQueryInterface().increment(this, this.$Model.getTableName(options), values, where, options).return(this
);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.previous"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>previous (key)](#apidoc.element.sequelize.Instance.prototype.previous)
- description and source-code
```javascript
previous = function (key) {
  if (key) {
    return this._previousDataValues[key];
  }

  return _.pickBy(this._previousDataValues, function(value, key) {
    return this.changed(key);
  }.bind(this));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.reload"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>reload (options)](#apidoc.element.sequelize.Instance.prototype.reload)
- description and source-code
```javascript
reload = function (options) {
  options = _.defaults({}, options, {
    where: this.where(),
    include: this.$options.include || null
  });

  return this.Model.findOne(options).bind(this)
  .tap(function (reload) {
    if (!reload) {
      throw new sequelizeErrors.InstanceError(
        'Instance could not be reloaded because it does not exist anymore (find call returned null)'
      );
    }
  })
  .then(function(reload) {
    // update the internal options of the instance
    this.$options = reload.$options;
    // re-set instance values
    this.set(reload.dataValues, {
      raw: true,
      reset: true && !options.attributes
    });
  }).return(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.restore"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>restore (options)](#apidoc.element.sequelize.Instance.prototype.restore)
- description and source-code
```javascript
restore = function (options) {
  if (!this.Model._timestampAttributes.deletedAt) throw new Error('Model is not paranoid');

  options = Utils._.extend({
    hooks: true,
    force: false
  }, options);

  return Promise.bind(this).then(function() {
    // Run before hook
    if (options.hooks) {
      return this.Model.runHooks('beforeRestore', this, options);
    }
  }).then(function() {
    var deletedAtCol = this.Model._timestampAttributes.deletedAt
      , deletedAtAttribute = this.Model.rawAttributes[deletedAtCol]
      , deletedAtDefaultValue = deletedAtAttribute.hasOwnProperty('defaultValue') ? deletedAtAttribute.defaultValue : null;

    this.setDataValue(deletedAtCol, deletedAtDefaultValue);
    return this.save(_.extend({}, options, {hooks : false, omitNull : false}));
  }).tap(function() {
    // Run after hook
    if (options.hooks) {
      return this.Model.runHooks('afterRestore', this, options);
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.save"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>save (options)](#apidoc.element.sequelize.Instance.prototype.save)
- description and source-code
```javascript
save = function (options) {
  if (arguments.length > 1) {
    throw new Error('The second argument was removed in favor of the options object.');
  }

  options = Utils.cloneDeep(options);
  options = _.defaults(options, {
    hooks: true,
    validate: true
  });

  if (!options.fields) {
    if (this.isNewRecord) {
      options.fields = Object.keys(this.Model.attributes);
    } else {
      options.fields = _.intersection(this.changed(), Object.keys(this.Model.attributes));
    }

    options.defaultFields = options.fields;
  }

  if (options.returning === undefined) {
    if (options.association) {
      options.returning = false;
    } else if (this.isNewRecord) {
      options.returning = true;
    }
  }

  var self = this
    , primaryKeyName = this.Model.primaryKeyAttribute
    , primaryKeyAttribute = primaryKeyName && this.Model.rawAttributes[primaryKeyName]
    , updatedAtAttr = this.Model._timestampAttributes.updatedAt
    , createdAtAttr = this.Model._timestampAttributes.createdAt
    , hook = self.isNewRecord ? 'Create' : 'Update'
    , wasNewRecord = this.isNewRecord
    , now = Utils.now(this.sequelize.options.dialect);

  if (updatedAtAttr && options.fields.length >= 1 && options.fields.indexOf(updatedAtAttr) === -1) {
    options.fields.push(updatedAtAttr);
  }

  if (options.silent === true && !(this.isNewRecord && this.get(updatedAtAttr, {raw: true}))) {
    // UpdateAtAttr might have been added as a result of Object.keys(Model.attributes). In that case we have to remove it again
    Utils._.remove(options.fields, function(val) {
      return val === updatedAtAttr;
    });
    updatedAtAttr = false;
  }

  if (this.isNewRecord === true) {
    if (createdAtAttr && options.fields.indexOf(createdAtAttr) === -1) {
      options.fields.push(createdAtAttr);
    }

    if (primaryKeyAttribute && primaryKeyAttribute.defaultValue && options.fields.indexOf(primaryKeyName) < 0) {
      options.fields.unshift(primaryKeyName);
    }
  }

  if (this.isNewRecord === false) {
    if (primaryKeyName && this.get(primaryKeyName, {raw: true}) === undefined) {
      throw new Error('You attempted to save an instance with no primary key, this is not allowed since it would result in a global
 update');
    }
  }

  if (updatedAtAttr && !options.silent && options.fields.indexOf(updatedAtAttr) !== -1) {
    this.dataValues[updatedAtAttr] = this.Model.$getDefaultTimestamp(updatedAtAttr) || now;
  }

  if (this.isNewRecord && createdAtAttr && !this.dataValues[createdAtAttr]) {
    this.dataValues[createdAtAttr] = this.Model.$getDefaultTimestamp(createdAtAttr) || now;
  }

  return Promise.bind(this).then(function() {
    // Validate
    if (options.validate) {
      return Promise.bind(this).then(function () {
        // hookValidate rejects with errors, validate returns with errors
        if (options.hooks) return this.hookValidate(options);

        return this.validate(options).then(function (err) {
          if (err) throw err;
        });
      });
    }
  }).then(function() {
    return Promise.bind(this).then(function() {
      // Run before hook
      if (options.hooks) {
        var beforeHookValues = _.pick(this.dataValues, options.fields)
          , afterHookValues
          , hookChanged
          , ignoreChanged = _.difference(this.changed(), options.fields); // In case of update where it's only supposed to update
 the passed values and the hook values

        if (updatedAtAttr && options.fields.indexOf(updatedAtAttr) !== -1) {
          ignoreChanged = _.without(ignoreChanged, updatedAtAttr);
        }

        return this.Model.runHooks('before' + hook, this, options).bind(this).then(function() {
          if (options.defaultFields && !this.isNewRecord) {
            afterHookValues = _.pick(this.dataValues, _.difference(this.changed(), ignoreChanged));

            hookChanged = [];
            Object.keys(afterHookValues).forEach(function (key) {
              if (afterHookValues[key] !== beforeHookValues[key]) {
                hookChanged.push(key);
              }
            });

            options.fields = _.u ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.set"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>set (key, value, options)](#apidoc.element.sequelize.Instance.prototype.set)
- description and source-code
```javascript
set = function (key, value, options) { // testhint options:none
  var values
    , originalValue
    , keys
    , i
    , length;

  if (typeof key === 'object' && key !== null) {
    values = key;
    options = value || {};

    if (options.reset) {
      this.dataValues = {};
    }

    // If raw, and we're not dealing with includes or special attributes, just set it straight on the dataValues object
    if (options.raw && !(this.$options && this.$options.include) && !(options && options.attributes) && !this.Model._hasBooleanAttributes
 && !this.Model._hasDateAttributes) {
      if (Object.keys(this.dataValues).length) {
        this.dataValues = _.extend(this.dataValues, values);
      } else {
        this.dataValues = values;
      }
      // If raw, .changed() shouldn't be true
      this._previousDataValues = _.clone(this.dataValues);
    } else {
      // Loop and call set

      if (options.attributes) {
        keys = options.attributes;
        if (this.Model._hasVirtualAttributes) {
          keys = keys.concat(this.Model._virtualAttributes);
        }

        if (this.$options.includeNames) {
          keys = keys.concat(this.$options.includeNames);
        }

        for (i = 0, length = keys.length; i < length; i++) {
          if (values[keys[i]] !== undefined) {
            this.set(keys[i], values[keys[i]], options);
          }
        }
      } else {
        for (key in values) {
          this.set(key, values[key], options);
        }
      }

      if (options.raw) {
        // If raw, .changed() shouldn't be true
        this._previousDataValues = _.clone(this.dataValues);
      }
    }
  } else {
    if (!options)
      options = {};
    if (!options.raw) {
      originalValue = this.dataValues[key];
    }

    // If not raw, and there's a customer setter
    if (!options.raw && this._customSetters[key]) {
      this._customSetters[key].call(this, value, key);
      if (!Utils.isPrimitive(value) && value !== null || value !== originalValue) {
        this._previousDataValues[key] = originalValue;
        this.changed(key, true);
      }
    } else {
      // Check if we have included models, and if this key matches the include model names/aliases

      if (this.$options && this.$options.include && this.$options.includeNames.indexOf(key) !== -1) {
        // Pass it on to the include handler
        this._setInclude(key, value, options);
        return this;
      } else {
        // Bunch of stuff we won't do when its raw
        if (!options.raw) {
          // If attribute is not in model definition, return
          if (!this._isAttribute(key)) {
            if (key.indexOf('.') > -1 && this.Model._isJsonAttribute(key.split('.')[0])) {
              var previousDottieValue = Dottie.get(this.dataValues, key);
              if (!_.isEqual(previousDottieValue, value)) {
                Dottie.set(this.dataValues, key, value);
                this.changed(key.split('.')[0], true);
              }
            }
            return this;
          }

          // If attempting to set primary key and primary key is already defined, return
          if (this.Model._hasPrimaryKeys && originalValue && this.Model._isPrimaryKey(key)) {
            return this;
          }

          // If attempting to set read only attributes, return
          if (!this.isNewRecord && this.Model._hasReadOnlyAttributes && this.Model._isReadOnlyAttribute(key)) {
            return this;
          }

          // Convert date fields to real date objects
          if (this.Model._hasDateAttributes && this.Model._isDateAttribute(key) && !!value && !value._isSequelizeMethod) {
            if (!(value instanceof Date)) {
              value = new Date(value);
            }
            if (originalValue) {
              if (!(originalValue instanceof Date)) {
                originalValue = new Date(originalValue);
              }
              if (value.getTime() === originalValue.getTime()) {
                return this;
              }
            }
          }
        }

        // Convert boolean-ish values to booleans
        i ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.setAttributes"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>setAttributes (updates)](#apidoc.element.sequelize.Instance.prototype.setAttributes)
- description and source-code
```javascript
setAttributes = function (updates) {
  return this.set(updates);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.setDataValue"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>setDataValue (key, value)](#apidoc.element.sequelize.Instance.prototype.setDataValue)
- description and source-code
```javascript
setDataValue = function (key, value) {
  var originalValue = this._previousDataValues[key];
  if (!Utils.isPrimitive(value) || value !== originalValue) {
    this.changed(key, true);
  }

  this.dataValues[key] = value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.setValidators"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>setValidators (attribute, validators)](#apidoc.element.sequelize.Instance.prototype.setValidators)
- description and source-code
```javascript
setValidators = function (attribute, validators) {
  this.validators[attribute] = validators;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>toJSON ()](#apidoc.element.sequelize.Instance.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
  return this.get({
    plain: true
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>toString ()](#apidoc.element.sequelize.Instance.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return '[object SequelizeInstance:'+this.Model.name+']';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.update"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>update (values, options)](#apidoc.element.sequelize.Instance.prototype.update)
- description and source-code
```javascript
update = function (values, options) {
  var changedBefore = this.changed() || []
    , sideEffects
    , fields
    , setOptions;

  options = options || {};
  if (Array.isArray(options)) options = {fields: options};

  options = Utils.cloneDeep(options);
  setOptions = Utils.cloneDeep(options);
  setOptions.attributes = options.fields;
  this.set(values, setOptions);

  // Now we need to figure out which fields were actually affected by the setter.
  sideEffects = _.without.apply(this, [this.changed() || []].concat(changedBefore));
  fields = _.union(Object.keys(values), sideEffects);

  if (!options.fields) {
    options.fields = _.intersection(fields, this.changed());
    options.defaultFields = options.fields;
  }

  return this.save(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.updateAttributes"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>updateAttributes (values, options)](#apidoc.element.sequelize.Instance.prototype.updateAttributes)
- description and source-code
```javascript
updateAttributes = function (values, options) {
  var changedBefore = this.changed() || []
    , sideEffects
    , fields
    , setOptions;

  options = options || {};
  if (Array.isArray(options)) options = {fields: options};

  options = Utils.cloneDeep(options);
  setOptions = Utils.cloneDeep(options);
  setOptions.attributes = options.fields;
  this.set(values, setOptions);

  // Now we need to figure out which fields were actually affected by the setter.
  sideEffects = _.without.apply(this, [this.changed() || []].concat(changedBefore));
  fields = _.union(Object.keys(values), sideEffects);

  if (!options.fields) {
    options.fields = _.intersection(fields, this.changed());
    options.defaultFields = options.fields;
  }

  return this.save(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Instance.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>validate (options)](#apidoc.element.sequelize.Instance.prototype.validate)
- description and source-code
```javascript
validate = function (options) {
  return new InstanceValidator(this, options).validate();
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```

#### <a name="apidoc.element.sequelize.Instance.prototype.where"></a>[function <span class="apidocSignatureSpan">sequelize.Instance.prototype.</span>where ()](#apidoc.element.sequelize.Instance.prototype.where)
- description and source-code
```javascript
where = function () {
  var where;

  where = this.Model.primaryKeyAttributes.reduce(function (result, attribute) {
    result[attribute] = this.get(attribute, {raw: true});
    return result;
  }.bind(this), {});

  if (_.size(where) === 0) {
    return this.$modelOptions.whereCollection;
  }
  return Utils.mapWhereFieldNames(where, this.$Model);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.InstanceError"></a>[module sequelize.InstanceError](#apidoc.module.sequelize.InstanceError)

#### <a name="apidoc.element.sequelize.InstanceError.InstanceError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>InstanceError (message)](#apidoc.element.sequelize.InstanceError.InstanceError)
- description and source-code
```javascript
InstanceError = function (message) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeInstanceError';
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.InstanceError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.InstanceError.</span>super_ ()](#apidoc.element.sequelize.InstanceError.super_)
- description and source-code
```javascript
super_ = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.InvalidConnectionError"></a>[module sequelize.InvalidConnectionError](#apidoc.module.sequelize.InvalidConnectionError)

#### <a name="apidoc.element.sequelize.InvalidConnectionError.InvalidConnectionError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>InvalidConnectionError (parent)](#apidoc.element.sequelize.InvalidConnectionError.InvalidConnectionError)
- description and source-code
```javascript
InvalidConnectionError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeInvalidConnectionError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.InvalidConnectionError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.InvalidConnectionError.</span>super_ (parent)](#apidoc.element.sequelize.InvalidConnectionError.super_)
- description and source-code
```javascript
super_ = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.JSON"></a>[module sequelize.JSON](#apidoc.module.sequelize.JSON)

#### <a name="apidoc.element.sequelize.JSON.JSON"></a>[function <span class="apidocSignatureSpan">sequelize.</span>JSON ()](#apidoc.element.sequelize.JSON.JSON)
- description and source-code
```javascript
JSON = function () {
  if (!(this instanceof JSONTYPE)) return new JSONTYPE();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.JSON.super_"></a>[function <span class="apidocSignatureSpan">sequelize.JSON.</span>super_ (options)](#apidoc.element.sequelize.JSON.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.JSON.prototype"></a>[module sequelize.JSON.prototype](#apidoc.module.sequelize.JSON.prototype)

#### <a name="apidoc.element.sequelize.JSON.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.JSON.prototype.</span>validate (value)](#apidoc.element.sequelize.JSON.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.JSONB"></a>[module sequelize.JSONB](#apidoc.module.sequelize.JSONB)

#### <a name="apidoc.element.sequelize.JSONB.JSONB"></a>[function <span class="apidocSignatureSpan">sequelize.</span>JSONB ()](#apidoc.element.sequelize.JSONB.JSONB)
- description and source-code
```javascript
JSONB = function () {
  if (!(this instanceof JSONB)) return new JSONB();
  JSONTYPE.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.JSONB.super_"></a>[function <span class="apidocSignatureSpan">sequelize.JSONB.</span>super_ ()](#apidoc.element.sequelize.JSONB.super_)
- description and source-code
```javascript
super_ = function () {
  if (!(this instanceof JSONTYPE)) return new JSONTYPE();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Model"></a>[module sequelize.Model](#apidoc.module.sequelize.Model)

#### <a name="apidoc.element.sequelize.Model.Model"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Model (name, attributes, options)](#apidoc.element.sequelize.Model.Model)
- description and source-code
```javascript
Model = function (name, attributes, options) {

  this.options = Utils._.extend({
    timestamps: true,
    instanceMethods: {},
    classMethods: {},
    validate: {},
    freezeTableName: false,
    underscored: false,
    underscoredAll: false,
    paranoid: false,
    rejectOnEmpty: false,
    whereCollection: null,
    schema: null,
    schemaDelimiter: '',
    defaultScope: {},
    scopes: [],
    hooks: {},
    indexes: []
  }, options || {});

  this.associations = {};
  this.modelManager = null;
  this.name = name;
  this.options.hooks = _.mapValues(this.replaceHookAliases(this.options.hooks), function (hooks) {
    if (!Array.isArray(hooks)) hooks = [hooks];
    return hooks;
  });

  this.sequelize = options.sequelize;
  this.underscored = this.underscored || this.underscoredAll;

  if (!this.options.tableName) {
    this.tableName = this.options.freezeTableName ? name : Utils.underscoredIf(Utils.pluralize(name), this.options.underscoredAll
);
  } else {
    this.tableName = this.options.tableName;
  }

  this.$schema = this.options.schema;
  this.$schemaDelimiter = this.options.schemaDelimiter;

  // error check options
  _.each(options.validate, function(validator, validatorType) {
    if (_.includes(Utils._.keys(attributes), validatorType)) {
      throw new Error('A model validator function must not have the same name as a field. Model: ' + name + ', field/validation
name: ' + validatorType);
    }

    if (!_.isFunction(validator)) {
      throw new Error('Members of the validate option must be functions. Model: ' + name + ', error with validate member ' + validatorType
);
    }
  });

 this.attributes = this.rawAttributes = _.mapValues(attributes, function(attribute, name) {
   if (!Utils._.isPlainObject(attribute)) {
     attribute = { type: attribute };
   }


    attribute = this.sequelize.normalizeAttribute(attribute);

    if (attribute.references) {
      attribute = Utils.formatReferences(attribute);

      if (attribute.references.model instanceof Model) {
        attribute.references.model = attribute.references.model.tableName;
      }
    }

    if (attribute.type === undefined) {
      throw new Error('Unrecognized data type for field ' + name);
    }

    return attribute;
  }.bind(this));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.addHook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>addHook (hookType, name, fn)](#apidoc.element.sequelize.Model.addHook)
- description and source-code
```javascript
addHook = function (hookType, name, fn) {
  if (typeof name === 'function') {
    fn = name;
    name = null;
  }

  hookType = hookAliases[hookType] || hookType;

  this.options.hooks[hookType] = this.options.hooks[hookType] || [];
  this.options.hooks[hookType].push(!!name ? {name: name, fn: fn} : fn);
  return this;
}
```
- example usage
```shell
...
  applyTo: function(Model) {
    Utils._.mixin(Model, Hooks);
    Utils._.mixin(Model.prototype, Hooks);

    var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
    allHooks.forEach(function(hook) {
      Model.prototype[hook] = function(name, callback) {
        return this.addHook(hook, name, callback);
      };
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.Model.default"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>default (name, attributes, options)](#apidoc.element.sequelize.Model.default)
- description and source-code
```javascript
default = function (name, attributes, options) {

  this.options = Utils._.extend({
    timestamps: true,
    instanceMethods: {},
    classMethods: {},
    validate: {},
    freezeTableName: false,
    underscored: false,
    underscoredAll: false,
    paranoid: false,
    rejectOnEmpty: false,
    whereCollection: null,
    schema: null,
    schemaDelimiter: '',
    defaultScope: {},
    scopes: [],
    hooks: {},
    indexes: []
  }, options || {});

  this.associations = {};
  this.modelManager = null;
  this.name = name;
  this.options.hooks = _.mapValues(this.replaceHookAliases(this.options.hooks), function (hooks) {
    if (!Array.isArray(hooks)) hooks = [hooks];
    return hooks;
  });

  this.sequelize = options.sequelize;
  this.underscored = this.underscored || this.underscoredAll;

  if (!this.options.tableName) {
    this.tableName = this.options.freezeTableName ? name : Utils.underscoredIf(Utils.pluralize(name), this.options.underscoredAll
);
  } else {
    this.tableName = this.options.tableName;
  }

  this.$schema = this.options.schema;
  this.$schemaDelimiter = this.options.schemaDelimiter;

  // error check options
  _.each(options.validate, function(validator, validatorType) {
    if (_.includes(Utils._.keys(attributes), validatorType)) {
      throw new Error('A model validator function must not have the same name as a field. Model: ' + name + ', field/validation
name: ' + validatorType);
    }

    if (!_.isFunction(validator)) {
      throw new Error('Members of the validate option must be functions. Model: ' + name + ', error with validate member ' + validatorType
);
    }
  });

 this.attributes = this.rawAttributes = _.mapValues(attributes, function(attribute, name) {
   if (!Utils._.isPlainObject(attribute)) {
     attribute = { type: attribute };
   }


    attribute = this.sequelize.normalizeAttribute(attribute);

    if (attribute.references) {
      attribute = Utils.formatReferences(attribute);

      if (attribute.references.model instanceof Model) {
        attribute.references.model = attribute.references.model.tableName;
      }
    }

    if (attribute.type === undefined) {
      throw new Error('Unrecognized data type for field ' + name);
    }

    return attribute;
  }.bind(this));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.hasHook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>hasHook (hookType)](#apidoc.element.sequelize.Model.hasHook)
- description and source-code
```javascript
hasHook = function (hookType) {
  return this.options.hooks[hookType] && !!this.options.hooks[hookType].length;
}
```
- example usage
```shell
...
   *
   * @param {String} hookType
   * @param {String} name
   */
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
...
```

#### <a name="apidoc.element.sequelize.Model.hasHooks"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>hasHooks (hookType)](#apidoc.element.sequelize.Model.hasHooks)
- description and source-code
```javascript
hasHooks = function (hookType) {
  return this.options.hooks[hookType] && !!this.options.hooks[hookType].length;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.hook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>hook ()](#apidoc.element.sequelize.Model.hook)
- description and source-code
```javascript
hook = function () {
  return Hooks.addHook.apply(this, arguments);
}
```
- example usage
```shell
...
*       function () {},
*       function() {} // Or an array of several
*     ]
*   }
* })
*
* // Method 2
* Model.hook('afterDestroy', function () {})
*
* // Method 3
* Model.afterBulkUpdate(function () {})
* '''
*
* @see {Sequelize#define}
* @mixin Hooks
...
```

#### <a name="apidoc.element.sequelize.Model.removeHook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>removeHook (hookType, name)](#apidoc.element.sequelize.Model.removeHook)
- description and source-code
```javascript
removeHook = function (hookType, name) {
  hookType = hookAliases[hookType] || hookType;

  if (!this.hasHook(hookType)) {
    return this;
  }

  this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
    // don't remove unnamed hooks
    if (typeof hook === 'function') {
      return true;
    }

    return typeof hook === 'object' && hook.name !== name;
  });

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.replaceHookAliases"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>replaceHookAliases (hooks)](#apidoc.element.sequelize.Model.replaceHookAliases)
- description and source-code
```javascript
replaceHookAliases = function (hooks) {
  var realHookName;

  Utils._.each(hooks, function(hooksArray, name) {
    // Does an alias for this hook name exist?
    if (realHookName = hookAliases[name]) {
      // Add the hooks to the actual hook
      hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

      // Delete the alias
      delete hooks[name];
    }
  });

  return hooks;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.runHooks"></a>[function <span class="apidocSignatureSpan">sequelize.Model.</span>runHooks (hooks)](#apidoc.element.sequelize.Model.runHooks)
- description and source-code
```javascript
runHooks = function (hooks) {
  var self = this
    , fn
    , fnArgs = Utils.sliceArgs(arguments, 1)
    , hookType;

  if (typeof fnArgs[fnArgs.length - 1] === 'function') {
    fn = fnArgs.pop();
  }

  if (typeof hooks === 'string') {
    hookType = hooks;
    hooks = this.options.hooks[hookType] || [];
    if (!Array.isArray(hooks)) hooks = hooks === undefined ? [] : [hooks];
    if (this.sequelize) hooks = hooks.concat(this.sequelize.options.hooks[hookType] || []);
  }

  if (!Array.isArray(hooks)) {
    hooks = hooks === undefined ? [] : [hooks];
  }

  // run hooks as sync functions if flagged as sync
  if (hookTypes[hookType] && hookTypes[hookType].sync) {
    hooks.forEach(function(hook) {
      if (typeof hook === 'object') hook = hook.fn;
      return hook.apply(self, fnArgs);
    });
    return;
  }

  // run hooks async
  var promise = Promise.each(hooks, function (hook) {
    if (typeof hook === 'object') {
      hook = hook.fn;
    }

    if (hookType && hook.length > hookTypes[hookType].params) {
      hook = Promise.promisify(hook, self);
    }

    return hook.apply(self, fnArgs);
  }).return();

  if (fn) {
    return promise.nodeify(fn);
  }

  return promise;
}
```
- example usage
```shell
...
 *   - On validation success: After Validation Model Hooks
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
...
```



# <a name="apidoc.module.sequelize.Model.prototype"></a>[module sequelize.Model.prototype](#apidoc.module.sequelize.Model.prototype)

#### <a name="apidoc.element.sequelize.Model.prototype.addHook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>addHook (hookType, name, fn)](#apidoc.element.sequelize.Model.prototype.addHook)
- description and source-code
```javascript
addHook = function (hookType, name, fn) {
  if (typeof name === 'function') {
    fn = name;
    name = null;
  }

  hookType = hookAliases[hookType] || hookType;

  this.options.hooks[hookType] = this.options.hooks[hookType] || [];
  this.options.hooks[hookType].push(!!name ? {name: name, fn: fn} : fn);
  return this;
}
```
- example usage
```shell
...
  applyTo: function(Model) {
    Utils._.mixin(Model, Hooks);
    Utils._.mixin(Model.prototype, Hooks);

    var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
    allHooks.forEach(function(hook) {
      Model.prototype[hook] = function(name, callback) {
        return this.addHook(hook, name, callback);
      };
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.addScope"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>addScope (name, scope, options)](#apidoc.element.sequelize.Model.prototype.addScope)
- description and source-code
```javascript
addScope = function (name, scope, options) {
  options = _.assign({
    override: false
  }, options);

  if ((name === 'defaultScope' || name in this.options.scopes) && options.override === false) {
    throw new Error('The scope ' + name + ' already exists. Pass { override: true } as options to silence this error');
  }

  conformOptions(scope, this);

  if (name === 'defaultScope') {
    this.options.defaultScope = this.$scope = scope;
  } else {
    this.options.scopes[name] = scope;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterBulkCreate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkCreate)
- description and source-code
```javascript
afterBulkCreate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterBulkDelete"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkDelete)
- description and source-code
```javascript
afterBulkDelete = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterBulkDestroy"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkDestroy)
- description and source-code
```javascript
afterBulkDestroy = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterBulkRestore"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkRestore)
- description and source-code
```javascript
afterBulkRestore = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterBulkSync"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkSync (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkSync)
- description and source-code
```javascript
afterBulkSync = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterBulkUpdate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterBulkUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterBulkUpdate)
- description and source-code
```javascript
afterBulkUpdate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
...
*   }
* })
*
* // Method 2
* Model.hook('afterDestroy', function () {})
*
* // Method 3
* Model.afterBulkUpdate(function () {})
* '''
*
* @see {Sequelize#define}
* @mixin Hooks
* @name Hooks
*/
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterConnect"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterConnect (name, callback)](#apidoc.element.sequelize.Model.prototype.afterConnect)
- description and source-code
```javascript
afterConnect = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterCreate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterCreate)
- description and source-code
```javascript
afterCreate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterDefine"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterDefine (name, callback)](#apidoc.element.sequelize.Model.prototype.afterDefine)
- description and source-code
```javascript
afterDefine = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterDelete"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.afterDelete)
- description and source-code
```javascript
afterDelete = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterDestroy"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.afterDestroy)
- description and source-code
```javascript
afterDestroy = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterFind"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterFind (name, callback)](#apidoc.element.sequelize.Model.prototype.afterFind)
- description and source-code
```javascript
afterFind = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterInit"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterInit (name, callback)](#apidoc.element.sequelize.Model.prototype.afterInit)
- description and source-code
```javascript
afterInit = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterRestore"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.afterRestore)
- description and source-code
```javascript
afterRestore = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterSync"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterSync (name, callback)](#apidoc.element.sequelize.Model.prototype.afterSync)
- description and source-code
```javascript
afterSync = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterUpdate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterUpdate)
- description and source-code
```javascript
afterUpdate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.afterValidate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>afterValidate (name, callback)](#apidoc.element.sequelize.Model.prototype.afterValidate)
- description and source-code
```javascript
afterValidate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.aggregate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>aggregate (attribute, aggregateFunction, options)](#apidoc.element.sequelize.Model.prototype.aggregate)
- description and source-code
```javascript
aggregate = function (attribute, aggregateFunction, options) {
  options = Utils.cloneDeep(options);
  options = _.defaults(options, { attributes: [] });
  conformOptions(options, this);
  this.$injectScope(options);

  if (options.include) {
    expandIncludeAll.call(this, options);
    validateIncludedElements.call(this, options);
  }

  var attrOptions = this.rawAttributes[attribute]
    , field = attrOptions && attrOptions.field || attribute
    , aggregateColumn = this.sequelize.col(field);

  if (options.distinct) {
    aggregateColumn = this.sequelize.fn('DISTINCT', aggregateColumn);
  }
  options.attributes.push([this.sequelize.fn(aggregateFunction, aggregateColumn), aggregateFunction]);

  if (!options.dataType) {
    if (attrOptions) {
      options.dataType = attrOptions.type;
    } else {
      // Use FLOAT as fallback
      options.dataType = new DataTypes.FLOAT();
    }
  } else {
    options.dataType = this.sequelize.normalizeDataType(options.dataType);
  }

  Utils.mapOptionFieldNames(options, this);
  options = paranoidClause(this, options);

  return this.QueryInterface.rawSelect(this.getTableName(options), options, aggregateFunction, this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.all"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>all (options)](#apidoc.element.sequelize.Model.prototype.all)
- description and source-code
```javascript
all = function (options) {
  return this.findAll(options);
}
```
- example usage
```shell
...
InstanceValidator.prototype.validate = function() {
if (this.inProgress) {
  throw new Error('Validations already in progress.');
}
this.inProgress = true;

var self = this;
return Promise.all([
  self._builtinValidators(),
  self._customValidators()
].map(function(promise) {
  return promise.reflect();
})).then(function() {
  if (self.errors.length) {
    return new sequelizeError.ValidationError(null, self.errors);
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeBulkCreate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkCreate)
- description and source-code
```javascript
beforeBulkCreate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeBulkDelete"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkDelete)
- description and source-code
```javascript
beforeBulkDelete = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeBulkDestroy"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkDestroy)
- description and source-code
```javascript
beforeBulkDestroy = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeBulkRestore"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkRestore)
- description and source-code
```javascript
beforeBulkRestore = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeBulkSync"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkSync (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkSync)
- description and source-code
```javascript
beforeBulkSync = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeBulkUpdate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeBulkUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeBulkUpdate)
- description and source-code
```javascript
beforeBulkUpdate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeConnect"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeConnect (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeConnect)
- description and source-code
```javascript
beforeConnect = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeConnection"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeConnection (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeConnection)
- description and source-code
```javascript
beforeConnection = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeCount"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeCount (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeCount)
- description and source-code
```javascript
beforeCount = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeCreate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeCreate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeCreate)
- description and source-code
```javascript
beforeCreate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeDefine"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeDefine (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeDefine)
- description and source-code
```javascript
beforeDefine = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeDelete"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeDelete (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeDelete)
- description and source-code
```javascript
beforeDelete = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeDestroy"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeDestroy (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeDestroy)
- description and source-code
```javascript
beforeDestroy = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeFind"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeFind (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeFind)
- description and source-code
```javascript
beforeFind = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeFindAfterExpandIncludeAll"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeFindAfterExpandIncludeAll (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeFindAfterExpandIncludeAll)
- description and source-code
```javascript
beforeFindAfterExpandIncludeAll = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeFindAfterOptions"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeFindAfterOptions (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeFindAfterOptions)
- description and source-code
```javascript
beforeFindAfterOptions = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeInit"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeInit (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeInit)
- description and source-code
```javascript
beforeInit = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeRestore"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeRestore (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeRestore)
- description and source-code
```javascript
beforeRestore = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeSync"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeSync (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeSync)
- description and source-code
```javascript
beforeSync = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeUpdate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeUpdate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeUpdate)
- description and source-code
```javascript
beforeUpdate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.beforeValidate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>beforeValidate (name, callback)](#apidoc.element.sequelize.Model.prototype.beforeValidate)
- description and source-code
```javascript
beforeValidate = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.belongsTo"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>belongsTo (target, options)](#apidoc.element.sequelize.Model.prototype.belongsTo)
- description and source-code
```javascript
belongsTo = function (target, options) { // testhint options:none
  if (!(target instanceof this.sequelize.Model)) {
    throw new Error(this.name + '.' + Utils.lowercaseFirst(Type.toString()) + ' called with something that\'s not an instance of
 Sequelize.Model');
  }

  var source = this;

  // Since this is a mixin, we'll need a unique variable name for hooks (since Model will override our hooks option)
  options = options || {};
  options.hooks = options.hooks === undefined ? false : Boolean(options.hooks);
  options.useHooks = options.hooks;

  // the id is in the foreign table
  var association = new Type(source, target, _.extend(options, source.options));
  source.associations[association.associationAccessor] = association.injectAttributes();

  if (association.mixin) {
    association.mixin(source.Instance.prototype);
  } else {
    association.injectGetter(source.Instance.prototype);
    association.injectSetter(source.Instance.prototype);
    association.injectCreator(source.Instance.prototype);
  }

  return association;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.belongsToMany"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>belongsToMany (targetModel, options)](#apidoc.element.sequelize.Model.prototype.belongsToMany)
- description and source-code
```javascript
belongsToMany = function (targetModel, options) { // testhint options:none
  if (!(targetModel instanceof this.sequelize.Model)) {
    throw new Error(this.name + '.belongsToMany called with something that\'s not an instance of Sequelize.Model');
  }

  var sourceModel = this;

  // Since this is a mixin, we'll need a unique variable name for hooks (since Model will override our hooks option)
  options = options || {};
  options.hooks = options.hooks === undefined ? false : Boolean(options.hooks);
  options.useHooks = options.hooks;
  options.timestamps = options.timestamps === undefined ? this.sequelize.options.timestamps : options.timestamps;
  options = _.extend(options, _.omit(sourceModel.options, ['hooks', 'timestamps', 'scopes', 'defaultScope']));

  // the id is in the foreign table or in a connecting table
  var association = new BelongsToMany(sourceModel, targetModel, options);
  sourceModel.associations[association.associationAccessor] = association.injectAttributes();

  association.injectGetter(sourceModel.Instance.prototype);
  association.injectSetter(sourceModel.Instance.prototype);
  association.injectCreator(sourceModel.Instance.prototype);

  return association;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.build"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>build (values, options)](#apidoc.element.sequelize.Model.prototype.build)
- description and source-code
```javascript
build = function (values, options) { // testhint options:none
  if (Array.isArray(values)) {
    return this.bulkBuild(values, options);
  }
  options = _.extend({
    isNewRecord: true,
    $schema: this.$schema,
    $schemaDelimiter: this.$schemaDelimiter
  }, options || {});

  if (options.attributes) {
    options.attributes = options.attributes.map(function(attribute) {
      return Array.isArray(attribute) ? attribute[1] : attribute;
    });
  }

  if (!options.includeValidated) {
    conformOptions(options, this);
    if (options.include) {
      expandIncludeAll.call(this, options);
      validateIncludedElements.call(this, options);
    }
  }

  return new this.Instance(values, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.bulkBuild"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>bulkBuild (valueSets, options)](#apidoc.element.sequelize.Model.prototype.bulkBuild)
- description and source-code
```javascript
bulkBuild = function (valueSets, options) { // testhint options:none
  options = _.extend({
    isNewRecord: true
  }, options || {});

  if (!options.includeValidated) {
    conformOptions(options, this);
    if (options.include) {
      expandIncludeAll.call(this, options);
      validateIncludedElements.call(this, options);
    }
  }

  if (options.attributes) {
    options.attributes = options.attributes.map(function(attribute) {
      return Array.isArray(attribute) ? attribute[1] : attribute;
    });
  }

  return valueSets.map(function(values) {
    return this.build(values, options);
  }.bind(this));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.bulkCreate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>bulkCreate (records, options)](#apidoc.element.sequelize.Model.prototype.bulkCreate)
- description and source-code
```javascript
bulkCreate = function (records, options) {
  if (!records.length) {
    return Promise.resolve([]);
  }

  options = Utils._.extend({
    validate: false,
    hooks: true,
    individualHooks: false,
    ignoreDuplicates: false
  }, options || {});

  options.fields = options.fields || Object.keys(this.tableAttributes);

  // Expose model to global hooks
  options.model = this;

  var dialect = this.sequelize.options.dialect;
  if (options.ignoreDuplicates && ['postgres', 'mssql'].indexOf(dialect) !== -1) {
    return Promise.reject(new Error(dialect + ' does not support the \'ignoreDuplicates\' option.'));
  }
  if (options.updateOnDuplicate && ['mysql', 'mariadb'].indexOf(dialect) === -1) {
    return Promise.reject(new Error(dialect + ' does not support the \'updateOnDuplicate\' option.'));
  }

  if (options.updateOnDuplicate) {
    // By default, all attributes except 'createdAt' can be updated
    var updatableFields = Utils._.pull(Object.keys(this.tableAttributes), 'createdAt');
    if (Utils._.isArray(options.updateOnDuplicate) && !Utils._.isEmpty(options.updateOnDuplicate)) {
      updatableFields = Utils._.intersection(updatableFields, options.updateOnDuplicate);
    }
    options.updateOnDuplicate = updatableFields;
  }

  var self = this
    , createdAtAttr = this._timestampAttributes.createdAt
    , updatedAtAttr = this._timestampAttributes.updatedAt
    , now = Utils.now(self.modelManager.sequelize.options.dialect);

  var instances = records.map(function(values) {
    return self.build(values, {isNewRecord: true});
  });

  return Promise.try(function() {
    // Run before hook
    if (options.hooks) {
      return self.runHooks('beforeBulkCreate', instances, options);
    }
  }).then(function() {
    // Validate
    if (options.validate) {
      var errors = [];
      return Promise.map(instances, function(instance) {
        // hookValidate rejects with errors, validate returns with errors
        if (options.individualHooks) {
          return instance.hookValidate(options).catch(function (err) {
            if (err) {
              errors.push({record: instance, errors: err});
            }
          });
        } else {
          return instance.validate(options).then(function (err) {
            if (err) {
              errors.push({record: instance, errors: err});
            }
          });
        }
      }).then(function() {
        delete options.skip;
        if (errors.length) {
          return Promise.reject(errors);
        }
      });
    }
  }).then(function() {
    instances.forEach(function(instance) {
      var values = Utils.mapValueFieldNames(instance.dataValues, options.fields, self);

      // set createdAt/updatedAt attributes
      if (createdAtAttr && !values[createdAtAttr]) {
        values[createdAtAttr] = now;
      }
      if (updatedAtAttr && !values[updatedAtAttr]) {
        values[updatedAtAttr] = now;
      }

      instance.dataValues = values;
    });

    if (options.individualHooks) {
      // Create each instance individually
      return Promise.map(instances, function(instance) {
        var individualOptions = Utils._.clone(options);
        delete individualOptions.fields;
        delete individualOptions.individualHooks;
        delete individualOptions.ignoreDuplicates;
        individualOptions.validate = false;
        individualOptions.hooks = true;

        return instance.save(individualOptions);
      }).then(function(_instances) {
        instances = _instances;
      });
    } else {
      // Create all in one query
      // Recreate records from instances to represent any changes made in hooks or validation
      records = instances.map(function(instance) {
        return Utils._.omit(instance.dataValues, self._virtualAttributes);
      });

      // Map attributes for serial identification
      var attributes = {};
      for (var attr in self.tableAttributes) {
        attributes[attr] = self.rawAttributes[attr];
        if (self.rawAttributes[attr].field) {
          attributes[self.rawAttributes[attr].field] = self.rawAttributes[attr];
        }
      } ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.count"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>count (options)](#apidoc.element.sequelize.Model.prototype.count)
- description and source-code
```javascript
count = function (options) {
  return Promise.bind(this).then(function() {
    options = _.defaults(Utils.cloneDeep(options), { hooks: true });
    if (options.hooks) {
      return this.runHooks('beforeCount', options);
    }
  }).then(function() {
    var col = options.include ? this.name + '.' + this.primaryKeyField : '*';

    options.plain = !options.group;
    options.dataType = new DataTypes.INTEGER();
    options.includeIgnoreAttributes = false;

    // No limit, offset, order or attributes for the options max be given to count()
    // Set them to null to prevent scopes setting those values
    options.limit = null;
    options.offset = null;
    options.order = null;

    return this.aggregate(col, 'count', options);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.create"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>create (values, options)](#apidoc.element.sequelize.Model.prototype.create)
- description and source-code
```javascript
create = function (values, options) {
  options = Utils.cloneDeep(options || {});

  return this.build(values, {
    isNewRecord: true,
    attributes: options.fields,
    include: options.include,
    raw: options.raw,
    silent: options.silent
  }).save(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.describe"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>describe (schema, options)](#apidoc.element.sequelize.Model.prototype.describe)
- description and source-code
```javascript
describe = function (schema, options) {
  return this.QueryInterface.describeTable(this.tableName, _.assign({schema: schema || this.$schema || undefined}, options));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.destroy"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>destroy (options)](#apidoc.element.sequelize.Model.prototype.destroy)
- description and source-code
```javascript
destroy = function (options) {
  var self = this
    , instances;

  if (!options || !(options.where || options.truncate)) {
    throw new Error('Missing where or truncate attribute in the options parameter of model.destroy.');
  }

  if (!options.truncate && !_.isPlainObject(options.where) && !_.isArray(options.where) && options.where._isSequelizeMethod !==
true) {
    throw new Error('Expected plain object, array or sequelize method in the options.where parameter of model.destroy.');
  }

  options = Utils.cloneDeep(options);
  options = _.defaults(options, {
    hooks: true,
    individualHooks: false,
    force: false,
    cascade: false,
    restartIdentity: false
  });

  options.type = QueryTypes.BULKDELETE;
  this.$injectScope(options);

  Utils.mapOptionFieldNames(options, this);
  options.model = self;

  return Promise.try(function() {
    // Run before hook
    if (options.hooks) {
      return self.runHooks('beforeBulkDestroy', options);
    }
  }).then(function() {
    // Get daos and run beforeDestroy hook on each record individually
    if (options.individualHooks) {
      return self.findAll({where: options.where, transaction: options.transaction, logging: options.logging, benchmark: options.
benchmark}).map(function(instance) {
        return self.runHooks('beforeDestroy', instance, options).then(function() {
          return instance;
        });
      }).then(function(_instances) {
        instances = _instances;
      });
    }
  }).then(function() {

    // Run delete query (or update if paranoid)
    if (self._timestampAttributes.deletedAt && !options.force) {
      // Set query type appropriately when running soft delete
      options.type = QueryTypes.BULKUPDATE;

      var attrValueHash = {}
        , deletedAtAttribute = self.rawAttributes[self._timestampAttributes.deletedAt]
        , field = self.rawAttributes[self._timestampAttributes.deletedAt].field
        , where = {};

      where[field] = deletedAtAttribute.hasOwnProperty('defaultValue') ? deletedAtAttribute.defaultValue : null;

      attrValueHash[field] = Utils.now(self.modelManager.sequelize.options.dialect);
      return self.QueryInterface.bulkUpdate(self.getTableName(options), attrValueHash, _.defaults(where, options.where), options
, self.rawAttributes);
    } else {
      return self.QueryInterface.bulkDelete(self.getTableName(options), options.where, options, self);
    }
  }).tap(function() {
    // Run afterDestroy hook on each record individually
    if (options.individualHooks) {
      return Promise.map(instances, function(instance) {
        return self.runHooks('afterDestroy', instance, options);
      });
    }
  }).tap(function() {
    // Run after hook
    if (options.hooks) {
      return self.runHooks('afterBulkDestroy', options);
    }
  }).then(function(affectedRows) {
    return affectedRows;
  });
}
```
- example usage
```shell
...
      if (!instances) {
        return Promise.resolve();
      }

      if (!Array.isArray(instances)) instances = [instances];

      return Promise.each(instances, function (instance) {
        return instance.destroy(options);
      });
    });
  }).then(function () {
    options.instance = instance;
    return self.sequelize.query(sql, options);
  });
};
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.drop"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>drop (options)](#apidoc.element.sequelize.Model.prototype.drop)
- description and source-code
```javascript
drop = function (options) {
  return this.QueryInterface.dropTable(this.getTableName(options), options);
}
```
- example usage
```shell
...
};

QueryInterface.prototype.dropAllSchemas = function(options) {
  options = options || {};

  var self = this;
  if (!this.QueryGenerator._dialect.supports.schemas) {
    return this.sequelize.drop(options);
  } else {
    return this.showAllSchemas(options).map(function(schemaName) {
      return self.dropSchema(schemaName, options);
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.dropSchema"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>dropSchema (schema)](#apidoc.element.sequelize.Model.prototype.dropSchema)
- description and source-code
```javascript
dropSchema = function (schema) {
  return this.QueryInterface.dropSchema(schema);
}
```
- example usage
```shell
...
options = options || {};
var sql = this.QueryGenerator.createSchema(schema);
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.dropSchema = function(schema, options) {
options = options || {};
var sql = this.QueryGenerator.dropSchema(schema);
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.dropAllSchemas = function(options) {
options = options || {};

var self = this;
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.find"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>find (options)](#apidoc.element.sequelize.Model.prototype.find)
- description and source-code
```javascript
find = function (options) {
  if (options !== undefined && !_.isPlainObject(options)) {
    throw new Error('The argument passed to findOne must be an options object, use findById if you wish to pass a single primary
 key value');
  }
  options = Utils.cloneDeep(options);

  if (options.limit === undefined) {
    var pkVal = options.where && options.where[this.primaryKeyAttribute];

    // Don't add limit if querying directly on the pk
    if (!options.where || !(Utils.isPrimitive(pkVal) || Buffer.isBuffer(pkVal))) {
      options.limit = 1;
    }
  }

  // Bypass a possible overloaded findAll.
  return Model.prototype.findAll.call(this, _.defaults(options, {
    plain: true,
    rejectOnEmpty: false
  }));
}
```
- example usage
```shell
...

QueryInterface.prototype.bulkUpdate = function(tableName, values, identifier, options, attributes) {
options = Utils.cloneDeep(options);
if (typeof identifier === 'object') identifier = Utils.cloneDeep(identifier);

var sql = this.QueryGenerator.updateQuery(tableName, values, identifier, options, attributes)
  , table = Utils._.isObject(tableName) ? tableName : { tableName: tableName }
  , model = Utils._.find(this.sequelize.modelManager.models, { tableName: table.tableName });

options.model = model;
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.delete = function(instance, tableName, identifier, options) {
var self = this
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.findAll"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findAll (options)](#apidoc.element.sequelize.Model.prototype.findAll)
- description and source-code
```javascript
findAll = function (options) {
  if (options !== undefined && !_.isPlainObject(options)) {
    throw new Error('The argument passed to findAll must be an options object, use findById if you wish to pass a single primary
 key value');
  }
  // TODO: Remove this in the next major version (4.0)
  if (arguments.length > 1) {
    throw new Error('Please note that find* was refactored and uses only one options object from now on.');
  }
  var tableNames = {}
    , originalOptions;

  tableNames[this.getTableName(options)] = true;
  options = Utils.cloneDeep(options);

  _.defaults(options, { hooks: true, rejectOnEmpty: this.options.rejectOnEmpty });

    //set rejectOnEmpty option from model config
    options.rejectOnEmpty = options.rejectOnEmpty || this.options.rejectOnEmpty;

  return Promise.bind(this).then(function() {
    conformOptions(options, this);
    this.$injectScope(options);

    if (options.hooks) {
      return this.runHooks('beforeFind', options);
    }
  }).then(function() {
    expandIncludeAll.call(this, options);

    if (options.hooks) {
      return this.runHooks('beforeFindAfterExpandIncludeAll', options);
    }
  }).then(function() {
    if (options.include) {
      options.hasJoin = true;

      validateIncludedElements.call(this, options, tableNames);

      // If we're not raw, we have to make sure we include the primary key for deduplication
      if (options.attributes && !options.raw && this.primaryKeyAttribute &&  options.attributes.indexOf(this.primaryKeyAttribute
) === -1) {
        options.originalAttributes = options.attributes;
        options.attributes = [this.primaryKeyAttribute].concat(options.attributes);
      }
    }

    if (!options.attributes) {
      options.attributes = Object.keys(this.tableAttributes);
    }

    // whereCollection is used for non-primary key updates
    this.options.whereCollection = options.where || null;

    Utils.mapFinderOptions(options, this);

    options = paranoidClause(this, options);

    if (options.hooks) {
      return this.runHooks('beforeFindAfterOptions', options);
    }
  }).then(function() {
    originalOptions = Utils.cloneDeep(options);
    options.tableNames = Object.keys(tableNames);
    return this.QueryInterface.select(this, this.getTableName(options), options);
  }).tap(function(results) {
    if (options.hooks) {
      return this.runHooks('afterFind', results, options);
    }
  }).then(function (results) {

    //rejectOnEmpty mode
    if (_.isEmpty(results) && options.rejectOnEmpty) {
      if (typeof options.rejectOnEmpty === 'function') {
        throw new options.rejectOnEmpty();
      } else if (typeof options.rejectOnEmpty === 'object') {
        throw options.rejectOnEmpty;
      } else {
        throw new sequelizeErrors.EmptyResultError();
      }
    }

    return Model.$findSeparate(results, originalOptions);
  });
}
```
- example usage
```shell
...


return sequelize.sync({
force: true,
logging: console.log
})
.then(() => {
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
    }
  });
})
.then(console.log)
.finally(() => {
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.findAndCount"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findAndCount (options)](#apidoc.element.sequelize.Model.prototype.findAndCount)
- description and source-code
```javascript
findAndCount = function (options) {
  if (options !== undefined && !_.isPlainObject(options)) {
    throw new Error('The argument passed to findAndCount must be an options object, use findById if you wish to pass a single primary
 key value');
  }

  var self = this;
  var countOptions = Utils.cloneDeep(options);
  if (countOptions.attributes) {
    countOptions.attributes = undefined;
  }
  return self.count(countOptions).then(function(count) {
    if (count === 0) {
      return {
        count: count || 0,
        rows: []
      };
    }
    return self.findAll(options).then(function(results) {
      return {
        count: count || 0,
        rows: results
      };
    });
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findAndCountAll"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findAndCountAll (options)](#apidoc.element.sequelize.Model.prototype.findAndCountAll)
- description and source-code
```javascript
findAndCountAll = function (options) {
  if (options !== undefined && !_.isPlainObject(options)) {
    throw new Error('The argument passed to findAndCount must be an options object, use findById if you wish to pass a single primary
 key value');
  }

  var self = this;
  var countOptions = Utils.cloneDeep(options);
  if (countOptions.attributes) {
    countOptions.attributes = undefined;
  }
  return self.count(countOptions).then(function(count) {
    if (count === 0) {
      return {
        count: count || 0,
        rows: []
      };
    }
    return self.findAll(options).then(function(results) {
      return {
        count: count || 0,
        rows: results
      };
    });
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findById"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findById (param, options)](#apidoc.element.sequelize.Model.prototype.findById)
- description and source-code
```javascript
findById = function (param, options) {
  // return Promise resolved with null if no arguments are passed
  if ([null, undefined].indexOf(param) !== -1) {
    return Promise.resolve(null);
  }

  options = Utils.cloneDeep(options) || {};

  if (typeof param === 'number' || typeof param === 'string' || Buffer.isBuffer(param)) {
    options.where = {};
    options.where[this.primaryKeyAttribute] = param;
  } else {
    throw new Error('Argument passed to findById is invalid: '+param);
  }

  // Bypass a possible overloaded findOne
  return Model.prototype.findOne.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findByPrimary"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findByPrimary (param, options)](#apidoc.element.sequelize.Model.prototype.findByPrimary)
- description and source-code
```javascript
findByPrimary = function (param, options) {
  // return Promise resolved with null if no arguments are passed
  if ([null, undefined].indexOf(param) !== -1) {
    return Promise.resolve(null);
  }

  options = Utils.cloneDeep(options) || {};

  if (typeof param === 'number' || typeof param === 'string' || Buffer.isBuffer(param)) {
    options.where = {};
    options.where[this.primaryKeyAttribute] = param;
  } else {
    throw new Error('Argument passed to findById is invalid: '+param);
  }

  // Bypass a possible overloaded findOne
  return Model.prototype.findOne.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findCreateFind"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findCreateFind (options)](#apidoc.element.sequelize.Model.prototype.findCreateFind)
- description and source-code
```javascript
findCreateFind = function (options) {
  if (!options || !options.where) {
    throw new Error(
      'Missing where attribute in the options parameter passed to findOrCreate.'
    );
  }

  var values = Utils._.clone(options.defaults) || {};
  if (Utils._.isPlainObject(options.where)) {
    values = _.defaults(values, options.where);
  }


  return this.findOne(options).bind(this).then(function (result) {
    if (result) return [result, false];

    return this.create(values, options).bind(this).then(function (result) {
      return [result, true];
    }).catch(this.sequelize.UniqueConstraintError, function (err) {
      return this.findOne(options).then(function (result) {
        return [result, false];
      });
    });
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findOne"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOne (options)](#apidoc.element.sequelize.Model.prototype.findOne)
- description and source-code
```javascript
findOne = function (options) {
  if (options !== undefined && !_.isPlainObject(options)) {
    throw new Error('The argument passed to findOne must be an options object, use findById if you wish to pass a single primary
 key value');
  }
  options = Utils.cloneDeep(options);

  if (options.limit === undefined) {
    var pkVal = options.where && options.where[this.primaryKeyAttribute];

    // Don't add limit if querying directly on the pk
    if (!options.where || !(Utils.isPrimitive(pkVal) || Buffer.isBuffer(pkVal))) {
      options.limit = 1;
    }
  }

  // Bypass a possible overloaded findAll.
  return Model.prototype.findAll.call(this, _.defaults(options, {
    plain: true,
    rejectOnEmpty: false
  }));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findOrBuild"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOrBuild (options)](#apidoc.element.sequelize.Model.prototype.findOrBuild)
- description and source-code
```javascript
findOrBuild = function (options) {
  if (!options || !options.where || arguments.length > 1) {
    throw new Error(
      'Missing where attribute in the options parameter passed to findOrInitialize. ' +
      'Please note that the API has changed, and is now options only (an object with where, defaults keys, transaction etc.)'
    );
  }

  var self = this
    , values;

  return self.find(options).then(function(instance) {
    if (instance === null) {
      values = Utils._.clone(options.defaults) || {};
      if (Utils._.isPlainObject(options.where)) {
        values = Utils._.defaults(values, options.where);
      }

      instance = self.build(values);

      return Promise.resolve([instance, true]);
    }

    return Promise.resolve([instance, false]);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findOrCreate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOrCreate (options)](#apidoc.element.sequelize.Model.prototype.findOrCreate)
- description and source-code
```javascript
findOrCreate = function (options) {
  if (!options || !options.where || arguments.length > 1) {
    throw new Error(
      'Missing where attribute in the options parameter passed to findOrCreate. '+
      'Please note that the API has changed, and is now options only (an object with where, defaults keys, transaction etc.)'
    );
  }

  options = _.assign({}, options);

  if (options.transaction === undefined && this.sequelize.constructor.cls) {
    var t = this.sequelize.constructor.cls.get('transaction');
    if (t) {
      options.transaction = t;
    }
  }

  var self = this
    , internalTransaction = !options.transaction
    , values
    , whereFields = Object.keys(options.where)
    , defaultFields
    , transaction;

  if (options.defaults) defaultFields = Object.keys(options.defaults);

  // Create a transaction or a savepoint, depending on whether a transaction was passed in
  return self.sequelize.transaction(options).bind({}).then(function (t) {
    transaction = t;
    options.transaction = t;

    return self.findOne(_.defaults({
      transaction: transaction
    }, options));
  }).then(function(instance) {
    if (instance !== null) {
      return [instance, false];
    }

    values = Utils._.clone(options.defaults) || {};
    if (Utils._.isPlainObject(options.where)) {
      values = _.defaults(values, options.where);
    }

    options.exception = true;

    return self.create(values, options).bind(this).then(function(instance) {
      if (instance.get(self.primaryKeyAttribute, { raw: true }) === null) {
        // If the query returned an empty result for the primary key, we know that this was actually a unique constraint violation
        throw new self.sequelize.UniqueConstraintError();
      }

      return [instance, true];
    }).catch(self.sequelize.UniqueConstraintError, function (err) {
      if (defaultFields) {
        if (!_.intersection(err.fields, whereFields).length && _.intersection(err.fields, defaultFields).length) {
          throw err;
        }
      }

      // Someone must have created a matching instance inside the same transaction since we last did a find. Let's find it!
      return self.findOne(_.defaults({
        transaction: internalTransaction ? null : transaction
      }, options)).then(function(instance) {
        // Sanity check, ideally we caught this at the defaultFeilds/err.fields check
        // But if we didn't and instance is null, we will throw
        if (instance === null) throw err;
        return [instance, false];
      });
    });
  }).finally(function () {
    if (internalTransaction && transaction) {
      // If we created a transaction internally (and not just a savepoint), we should clean it up
      return transaction.commit();
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.findOrInitialize"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>findOrInitialize (options)](#apidoc.element.sequelize.Model.prototype.findOrInitialize)
- description and source-code
```javascript
findOrInitialize = function (options) {
  if (!options || !options.where || arguments.length > 1) {
    throw new Error(
      'Missing where attribute in the options parameter passed to findOrInitialize. ' +
      'Please note that the API has changed, and is now options only (an object with where, defaults keys, transaction etc.)'
    );
  }

  var self = this
    , values;

  return self.find(options).then(function(instance) {
    if (instance === null) {
      values = Utils._.clone(options.defaults) || {};
      if (Utils._.isPlainObject(options.where)) {
        values = Utils._.defaults(values, options.where);
      }

      instance = self.build(values);

      return Promise.resolve([instance, true]);
    }

    return Promise.resolve([instance, false]);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.getAssociation"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>getAssociation (target, alias)](#apidoc.element.sequelize.Model.prototype.getAssociation)
- description and source-code
```javascript
getAssociation = function (target, alias) {
  for (var associationName in this.associations) {
    if (this.associations.hasOwnProperty(associationName)) {
      var association = this.associations[associationName];

      if (association.target.name === target.name && (alias === undefined ? !association.isAliased : association.as === alias)) {
        return association;
      }
    }
  }

  return null;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.getTableName"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>getTableName (options)](#apidoc.element.sequelize.Model.prototype.getTableName)
- description and source-code
```javascript
getTableName = function (options) { // testhint options:none
  return this.QueryGenerator.addSchema(this);
}
```
- example usage
```shell
...

  options = _.defaults(options || {}, {
reverse: true
  });

  this.models.forEach(function(model) {
var deps = []
  , tableName = model.getTableName();

if (_.isObject(tableName)) {
  tableName = tableName.schema + '.' + tableName.tableName;
}

models[tableName] = model;
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.hasHook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasHook (hookType)](#apidoc.element.sequelize.Model.prototype.hasHook)
- description and source-code
```javascript
hasHook = function (hookType) {
  return this.options.hooks[hookType] && !!this.options.hooks[hookType].length;
}
```
- example usage
```shell
...
   *
   * @param {String} hookType
   * @param {String} name
   */
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.hasHooks"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasHooks (hookType)](#apidoc.element.sequelize.Model.prototype.hasHooks)
- description and source-code
```javascript
hasHooks = function (hookType) {
  return this.options.hooks[hookType] && !!this.options.hooks[hookType].length;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.hasMany"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasMany (target, options)](#apidoc.element.sequelize.Model.prototype.hasMany)
- description and source-code
```javascript
hasMany = function (target, options) { // testhint options:none
  if (!(target instanceof this.sequelize.Model)) {
    throw new Error(this.name + '.hasMany called with something that\'s not an instance of Sequelize.Model');
  }

  var source = this;

  // Since this is a mixin, we'll need a unique variable name for hooks (since Model will override our hooks option)
  options = options || {};
  options.hooks = options.hooks === undefined ? false : Boolean(options.hooks);
  options.useHooks = options.hooks;

  options = _.extend(options, _.omit(source.options, ['hooks']));

  // the id is in the foreign table or in a connecting table
  var association = new HasMany(source, target, options);
  source.associations[association.associationAccessor] = association;

  association.injectAttributes();
  association.mixin(source.Instance.prototype);

  return association;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.hasOne"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hasOne (target, options)](#apidoc.element.sequelize.Model.prototype.hasOne)
- description and source-code
```javascript
hasOne = function (target, options) { // testhint options:none
  if (!(target instanceof this.sequelize.Model)) {
    throw new Error(this.name + '.' + Utils.lowercaseFirst(Type.toString()) + ' called with something that\'s not an instance of
 Sequelize.Model');
  }

  var source = this;

  // Since this is a mixin, we'll need a unique variable name for hooks (since Model will override our hooks option)
  options = options || {};
  options.hooks = options.hooks === undefined ? false : Boolean(options.hooks);
  options.useHooks = options.hooks;

  // the id is in the foreign table
  var association = new Type(source, target, _.extend(options, source.options));
  source.associations[association.associationAccessor] = association.injectAttributes();

  if (association.mixin) {
    association.mixin(source.Instance.prototype);
  } else {
    association.injectGetter(source.Instance.prototype);
    association.injectSetter(source.Instance.prototype);
    association.injectCreator(source.Instance.prototype);
  }

  return association;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.hook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>hook ()](#apidoc.element.sequelize.Model.prototype.hook)
- description and source-code
```javascript
hook = function () {
  return Hooks.addHook.apply(this, arguments);
}
```
- example usage
```shell
...
*       function () {},
*       function() {} // Or an array of several
*     ]
*   }
* })
*
* // Method 2
* Model.hook('afterDestroy', function () {})
*
* // Method 3
* Model.afterBulkUpdate(function () {})
* '''
*
* @see {Sequelize#define}
* @mixin Hooks
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.init"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>init (modelManager)](#apidoc.element.sequelize.Model.prototype.init)
- description and source-code
```javascript
init = function (modelManager) {
  var self = this;

  this.modelManager = modelManager;
  this.primaryKeys = {};

  // Setup names of timestamp attributes
  this._timestampAttributes = {};
  if (this.options.timestamps) {
    if (this.options.createdAt !== false) {
      this._timestampAttributes.createdAt = this.options.createdAt || Utils.underscoredIf('createdAt', this.options.underscored);
    }
    if (this.options.updatedAt !== false) {
      this._timestampAttributes.updatedAt = this.options.updatedAt || Utils.underscoredIf('updatedAt', this.options.underscored);
    }
    if (this.options.paranoid && this.options.deletedAt !== false) {
      this._timestampAttributes.deletedAt = this.options.deletedAt || Utils.underscoredIf('deletedAt', this.options.underscored);
    }
  }

  // Add head and tail default attributes (id, timestamps)
  addOptionalClassMethods.call(this);

  // Instance prototype
  this.Instance = function() {
    Instance.apply(this, arguments);
  };

  Util.inherits(this.Instance, Instance);

  this._readOnlyAttributes = Utils._.values(this._timestampAttributes);
  this._hasReadOnlyAttributes = this._readOnlyAttributes && this._readOnlyAttributes.length;
  this._isReadOnlyAttribute = Utils._.memoize(function(key) {
    return self._hasReadOnlyAttributes && self._readOnlyAttributes.indexOf(key) !== -1;
  });

  if (this.options.instanceMethods) {
    Utils._.each(this.options.instanceMethods, function(fct, name) {
      self.Instance.prototype[name] = fct;
    });
  }

  addDefaultAttributes.call(this);
  this.refreshAttributes();

  findAutoIncrementField.call(this);

  this.$scope = this.options.defaultScope;

  if (_.isPlainObject(this.$scope)) {
    conformOptions(this.$scope, this);
  }

  _.each(this.options.scopes, function (scope) {
    if (_.isPlainObject(scope)) {
      conformOptions(scope, this);
    }
  }.bind(this));

  this.options.indexes = this.options.indexes.map(this.$conformIndex);

  this.Instance.prototype.$Model =
  this.Instance.prototype.Model = this;

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.insertOrUpdate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>insertOrUpdate (values, options)](#apidoc.element.sequelize.Model.prototype.insertOrUpdate)
- description and source-code
```javascript
insertOrUpdate = function (values, options) {
  options = Utils.cloneDeep(options) || {};

  if (!options.fields) {
    options.fields = Object.keys(this.attributes);
  }

  var createdAtAttr = this._timestampAttributes.createdAt
    , updatedAtAttr = this._timestampAttributes.updatedAt
    , hadPrimary = this.primaryKeyField in values || this.primaryKeyAttribute in values
    , instance = this.build(values);

  return instance.hookValidate(options).bind(this).then(function () {
    // Map field names
    var updatedDataValues = _.pick(instance.dataValues, Object.keys(instance._changed))
      , insertValues = Utils.mapValueFieldNames(instance.dataValues, options.fields, this)
      , updateValues = Utils.mapValueFieldNames(updatedDataValues, options.fields, this)
      , now = Utils.now(this.sequelize.options.dialect);

    // Attach createdAt
    if (createdAtAttr && !updateValues[createdAtAttr]) {
      insertValues[createdAtAttr] = this.$getDefaultTimestamp(createdAtAttr) || now;
    }
    if (updatedAtAttr && !insertValues[updatedAtAttr]) {
      insertValues[updatedAtAttr] = updateValues[updatedAtAttr] = this.$getDefaultTimestamp(updatedAtAttr) || now;
    }

    // Build adds a null value for the primary key, if none was given by the user.
    // We need to remove that because of some Postgres technicalities.
    if (!hadPrimary && this.primaryKeyAttribute && !this.rawAttributes[this.primaryKeyAttribute].defaultValue) {
      delete insertValues[this.primaryKeyField];
      delete updateValues[this.primaryKeyField];
    }

    return this.QueryInterface.upsert(this.getTableName(options), insertValues, updateValues, instance.where(), this, options);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.inspect"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>inspect ()](#apidoc.element.sequelize.Model.prototype.inspect)
- description and source-code
```javascript
inspect = function () {
  return this.name;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.max"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>max (field, options)](#apidoc.element.sequelize.Model.prototype.max)
- description and source-code
```javascript
max = function (field, options) {
  return this.aggregate(field, 'max', options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.min"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>min (field, options)](#apidoc.element.sequelize.Model.prototype.min)
- description and source-code
```javascript
min = function (field, options) {
  return this.aggregate(field, 'min', options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.refreshAttributes"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>refreshAttributes ()](#apidoc.element.sequelize.Model.prototype.refreshAttributes)
- description and source-code
```javascript
refreshAttributes = function () {
  var self = this
    , attributeManipulation = {};

  this.Instance.prototype._customGetters = {};
  this.Instance.prototype._customSetters = {};

  Utils._.each(['get', 'set'], function(type) {
    var opt = type + 'terMethods'
      , funcs = Utils._.clone(Utils._.isObject(self.options[opt]) ? self.options[opt] : {})
      , _custom = type === 'get' ? self.Instance.prototype._customGetters : self.Instance.prototype._customSetters;

    Utils._.each(funcs, function(method, attribute) {
      _custom[attribute] = method;

      if (type === 'get') {
        funcs[attribute] = function() {
          return this.get(attribute);
        };
      }
      if (type === 'set') {
        funcs[attribute] = function(value) {
          return this.set(attribute, value);
        };
      }
    });

    Utils._.each(self.rawAttributes, function(options, attribute) {
      if (options.hasOwnProperty(type)) {
        _custom[attribute] = options[type];
      }

      if (type === 'get') {
        funcs[attribute] = function() {
          return this.get(attribute);
        };
      }
      if (type === 'set') {
        funcs[attribute] = function(value) {
          return this.set(attribute, value);
        };
      }
    });

    Utils._.each(funcs, function(fct, name) {
      if (!attributeManipulation[name]) {
        attributeManipulation[name] = {
          configurable: true
        };
      }
      attributeManipulation[name][type] = fct;
    });
  });

  this._booleanAttributes = [];
  this._dateAttributes = [];
  this._hstoreAttributes = [];
  this._rangeAttributes = [];
  this._jsonAttributes = [];
  this._geometryAttributes = [];
  this._virtualAttributes = [];
  this._defaultValues = {};
  this.Instance.prototype.validators = {};

  this.fieldRawAttributesMap = {};

  this.primaryKeys = {};
  self.options.uniqueKeys = {};

  _.each(this.rawAttributes, function(definition, name) {
    definition.type = self.sequelize.normalizeDataType(definition.type);

    definition.Model = self;
    definition.fieldName = name;
    definition._modelAttribute = true;

    if (definition.field === undefined) {
      definition.field = name;
    }

    if (definition.primaryKey === true) {
      self.primaryKeys[name] = definition;
    }

    self.fieldRawAttributesMap[definition.field] = definition;

    if (definition.type instanceof DataTypes.BOOLEAN) {
      self._booleanAttributes.push(name);
    } else if (definition.type instanceof DataTypes.DATE) {
      self._dateAttributes.push(name);
    } else if (definition.type instanceof DataTypes.HSTORE || DataTypes.ARRAY.is(definition.type, DataTypes.HSTORE)) {
      self._hstoreAttributes.push(name);
    } else if (definition.type instanceof DataTypes.RANGE || DataTypes.ARRAY.is(definition.type, DataTypes.RANGE)) {
      self._rangeAttributes.push(name);
    } else if (definition.type instanceof DataTypes.JSON) {
      self._jsonAttributes.push(name);
    } else if (definition.type instanceof DataTypes.VIRTUAL) {
      self._virtualAttributes.push(name);
    } else if (definition.type instanceof DataTypes.GEOMETRY) {
      self._geometryAttributes.push(name);
    }

    if (definition.hasOwnProperty('defaultValue')) {
      self._defaultValues[name] = Utils._.partial(Utils.toDefaultValue, definition.defaultValue);
    }

    if (definition.hasOwnProperty('unique') && definition.unique !== false) {
      var idxName;
      if (typeof definition.unique === 'object' && definition.unique.hasOwnProperty('name')) {
        idxName = definition.unique.name;
      } else if (typeof definition.unique === 'string') {
        idxName = definition.unique;
      } else {
        idxName = self.tableName + '_' + name + '_unique';
      }

      var idx = self.options.uniqueKeys[idxName] || { fields: [] };
      idx = idx || {fields: [], msg: null};
      idx.fields.push(definition.field);
      idx.msg = idx.msg || definition.unique.msg || null;
      idx.name = idxName || false;
      idx.column = name;

      self.options.uniqueKeys[idxName] = idx;
    }

    if (definition.hasOwnProper ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.removeAttribute"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>removeAttribute (attribute)](#apidoc.element.sequelize.Model.prototype.removeAttribute)
- description and source-code
```javascript
removeAttribute = function (attribute) {
  delete this.rawAttributes[attribute];
  this.refreshAttributes();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.removeHook"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>removeHook (hookType, name)](#apidoc.element.sequelize.Model.prototype.removeHook)
- description and source-code
```javascript
removeHook = function (hookType, name) {
  hookType = hookAliases[hookType] || hookType;

  if (!this.hasHook(hookType)) {
    return this;
  }

  this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
    // don't remove unnamed hooks
    if (typeof hook === 'function') {
      return true;
    }

    return typeof hook === 'object' && hook.name !== name;
  });

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.replaceHookAliases"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>replaceHookAliases (hooks)](#apidoc.element.sequelize.Model.prototype.replaceHookAliases)
- description and source-code
```javascript
replaceHookAliases = function (hooks) {
  var realHookName;

  Utils._.each(hooks, function(hooksArray, name) {
    // Does an alias for this hook name exist?
    if (realHookName = hookAliases[name]) {
      // Add the hooks to the actual hook
      hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

      // Delete the alias
      delete hooks[name];
    }
  });

  return hooks;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.restore"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>restore (options)](#apidoc.element.sequelize.Model.prototype.restore)
- description and source-code
```javascript
restore = function (options) {
  if (!this._timestampAttributes.deletedAt) throw new Error('Model is not paranoid');

  options = Utils._.extend({
    hooks: true,
    individualHooks: false
  }, options || {});

  options.type = QueryTypes.RAW;

  // Expose model to global hooks
  options.model = this;

  var self = this
    , instances;

  Utils.mapOptionFieldNames(options, this);

  return Promise.try(function() {
    // Run before hook
    if (options.hooks) {
      return self.runHooks('beforeBulkRestore', options);
    }
  }).then(function() {
    // Get daos and run beforeRestore hook on each record individually
    if (options.individualHooks) {
      return self.findAll({where: options.where, transaction: options.transaction, logging: options.logging, benchmark: options.
benchmark, paranoid: false}).map(function(instance) {
        return self.runHooks('beforeRestore', instance, options).then(function() {
          return instance;
        });
      }).then(function(_instances) {
        instances = _instances;
      });
    }
  }).then(function() {
    // Run undelete query
    var attrValueHash = {}
      , deletedAtCol = self._timestampAttributes.deletedAt
      , deletedAtAttribute = self.rawAttributes[deletedAtCol]
      , deletedAtDefaultValue = deletedAtAttribute.hasOwnProperty('defaultValue') ? deletedAtAttribute.defaultValue : null;

    attrValueHash[deletedAtAttribute.field || deletedAtCol] = deletedAtDefaultValue;
    options.omitNull = false;
    return self.QueryInterface.bulkUpdate(self.getTableName(options), attrValueHash, options.where, options, self._timestampAttributes
.deletedAt);
  }).tap(function() {
    // Run afterDestroy hook on each record individually
    if (options.individualHooks) {
      return Promise.map(instances, function(instance) {
        return self.runHooks('afterRestore', instance, options);
      });
    }
  }).tap(function() {
    // Run after hook
    if (options.hooks) {
      return self.runHooks('afterBulkRestore', options);
    }
  }).then(function(affectedRows) {
    return affectedRows;
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.runHooks"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>runHooks (hooks)](#apidoc.element.sequelize.Model.prototype.runHooks)
- description and source-code
```javascript
runHooks = function (hooks) {
  var self = this
    , fn
    , fnArgs = Utils.sliceArgs(arguments, 1)
    , hookType;

  if (typeof fnArgs[fnArgs.length - 1] === 'function') {
    fn = fnArgs.pop();
  }

  if (typeof hooks === 'string') {
    hookType = hooks;
    hooks = this.options.hooks[hookType] || [];
    if (!Array.isArray(hooks)) hooks = hooks === undefined ? [] : [hooks];
    if (this.sequelize) hooks = hooks.concat(this.sequelize.options.hooks[hookType] || []);
  }

  if (!Array.isArray(hooks)) {
    hooks = hooks === undefined ? [] : [hooks];
  }

  // run hooks as sync functions if flagged as sync
  if (hookTypes[hookType] && hookTypes[hookType].sync) {
    hooks.forEach(function(hook) {
      if (typeof hook === 'object') hook = hook.fn;
      return hook.apply(self, fnArgs);
    });
    return;
  }

  // run hooks async
  var promise = Promise.each(hooks, function (hook) {
    if (typeof hook === 'object') {
      hook = hook.fn;
    }

    if (hookType && hook.length > hookTypes[hookType].params) {
      hook = Promise.promisify(hook, self);
    }

    return hook.apply(self, fnArgs);
  }).return();

  if (fn) {
    return promise.nodeify(fn);
  }

  return promise;
}
```
- example usage
```shell
...
 *   - On validation success: After Validation Model Hooks
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.schema"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>schema (schema, options)](#apidoc.element.sequelize.Model.prototype.schema)
- description and source-code
```javascript
schema = function (schema, options) { // testhint options:none
  var self = this;
  var clone = Object.create(self);

  clone.$schema = schema;

  if (!!options) {
    if (typeof options === 'string') {
      clone.$schemaDelimiter = options;
    } else {
      if (!!options.schemaDelimiter) {
        clone.$schemaDelimiter = options.schemaDelimiter;
      }
    }
  }

  clone.Instance = function() {
    self.Instance.apply(this, arguments);
  };
  clone.Instance.prototype = Object.create(self.Instance.prototype);
  clone.Instance.prototype.$Model = clone;

  return clone;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.scope"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>scope (option)](#apidoc.element.sequelize.Model.prototype.scope)
- description and source-code
```javascript
scope = function (option) {
  var self = Object.create(this)
    , options
    , scope
    , scopeName;

  self.$scope = {};
  self.scoped = true;

  if (!option) {
    return self;
  }

  options = _.flatten(arguments);
  options.forEach(function(option) {
    scope = null;
    scopeName = null;

    if (_.isPlainObject(option)) {
      if (!!option.method) {
        if (Array.isArray(option.method) && !!self.options.scopes[option.method[0]]) {
          scopeName = option.method[0];
          scope = self.options.scopes[scopeName].apply(self, option.method.splice(1));
        }
        else if (!!self.options.scopes[option.method]) {
          scopeName = option.method;
          scope = self.options.scopes[scopeName].apply(self);
        }
      } else {
        scope = option;
      }
    } else {
      if (option === 'defaultScope' && _.isPlainObject(self.options.defaultScope)) {
        scope = self.options.defaultScope;
      } else {
        scopeName = option;
        scope = self.options.scopes[scopeName];

        if (_.isFunction(scope)) {
          scope = scope();
          conformOptions(scope, self);
        }
      }
    }

    if (!!scope) {
      _.assignWith(self.$scope, scope, function scopeCustomizer(objectValue, sourceValue, key) {
        if (key === 'where') {
          return Array.isArray(sourceValue) ? sourceValue : _.assign(objectValue || {}, sourceValue);
        } else if ( (['attributes','include'].indexOf(key) >= 0) && Array.isArray(objectValue) && Array.isArray(sourceValue)) {
          return objectValue.concat(sourceValue);
        }

        return objectValue ? objectValue : sourceValue;
      });
    } else {
      throw new Error('Invalid scope ' + scopeName + ' called.');
    }
  });

  return self;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.sum"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>sum (field, options)](#apidoc.element.sequelize.Model.prototype.sum)
- description and source-code
```javascript
sum = function (field, options) {
  return this.aggregate(field, 'sum', options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.sync"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>sync (options)](#apidoc.element.sequelize.Model.prototype.sync)
- description and source-code
```javascript
sync = function (options) {
  options = _.extend({}, this.options, options);
  options.hooks = options.hooks === undefined ? true : !!options.hooks;

  var self = this
    , attributes = this.tableAttributes;

  return Promise.try(function () {
    if (options.hooks) {
      return self.runHooks('beforeSync', options);
    }
  }).then(function () {
    if (options.force) {
      return self.drop(options);
    }
  }).then(function () {
    return self.QueryInterface.createTable(self.getTableName(options), attributes, options, self);
  }).then(function () {
    return self.QueryInterface.showIndex(self.getTableName(options), options);
  }).then(function (indexes) {
    // Assign an auto-generated name to indexes which are not named by the user
    self.options.indexes = self.QueryInterface.nameIndexes(self.options.indexes, self.tableName);

    indexes = _.filter(self.options.indexes, function (item1) {
      return !_.some(indexes, function (item2) {
        return item1.name === item2.name;
      });
    });

    return Promise.map(indexes, function (index) {
      return self.QueryInterface.addIndex(
                self.getTableName(options),
                _.assign({logging: options.logging,
                          benchmark: options.benchmark,
                          transaction: options.transaction}, index),
                self.tableName);
    });
  }).then(function () {
    if (options.hooks) {
      return self.runHooks('afterSync', options);
    }
  }).return(this);
}
```
- example usage
```shell
...

const User = sequelize.define('user', {
name: Sequelize.STRING
});



return sequelize.sync({
force: true,
logging: console.log
})
.then(() => {
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
...
```

#### <a name="apidoc.element.sequelize.Model.prototype.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>toString ()](#apidoc.element.sequelize.Model.prototype.toString)
- description and source-code
```javascript
toString = function () {
  return '[object SequelizeModel:'+this.name+']';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.truncate"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>truncate (options)](#apidoc.element.sequelize.Model.prototype.truncate)
- description and source-code
```javascript
truncate = function (options) {
  options = Utils.cloneDeep(options) || {};
  options.truncate = true;
  return this.destroy(options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.unscoped"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>unscoped ()](#apidoc.element.sequelize.Model.prototype.unscoped)
- description and source-code
```javascript
unscoped = function () {
  return this.scope();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.update"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>update (values, options)](#apidoc.element.sequelize.Model.prototype.update)
- description and source-code
```javascript
update = function (values, options) {
  var self = this;

  if (!options || !options.where) {
    throw new Error('Missing where attribute in the options parameter passed to update.');
  }

  if (!_.isPlainObject(options.where) && !_.isArray(options.where) && options.where._isSequelizeMethod !== true) {
    throw new Error('Expected plain object, array or sequelize method in the options.where parameter of model.update.');
  }

  options = Utils.cloneDeep(options);
  options = _.defaults(options, {
    validate: true,
    hooks: true,
    individualHooks: false,
    returning: false,
    force: false,
    sideEffects: true
  });

  options.type = QueryTypes.BULKUPDATE;

  // Expose model to global hooks
  options.model = this;

  this.$injectScope(options);

  // Clone values so it doesn't get modified for caller scope
  values = _.clone(values);

  // Remove values that are not in the options.fields
  if (options.fields && options.fields instanceof Array) {
    Object.keys(values).forEach(function(key) {
      if (options.fields.indexOf(key) < 0) {
        delete values[key];
      }
    });
  } else {
    var updatedAtAttr = this._timestampAttributes.updatedAt;
    options.fields = _.intersection(Object.keys(values), Object.keys(this.tableAttributes));
    if (updatedAtAttr && options.fields.indexOf(updatedAtAttr) === -1) {
      options.fields.push(updatedAtAttr);
    }
  }

  if (this._timestampAttributes.updatedAt && !options.silent) {
    values[this._timestampAttributes.updatedAt] = this.$getDefaultTimestamp(this._timestampAttributes.updatedAt) || Utils.now(this
.sequelize.options.dialect);
  }

  var instances
    , valuesUse;

  return Promise.try(function() {
    // Validate
    if (options.validate) {
      var build = self.build(values);
      build.set(self._timestampAttributes.updatedAt, values[self._timestampAttributes.updatedAt], { raw: true });

      if (options.sideEffects) {
        values = Utils._.assign(values, Utils._.pick(build.get(), build.changed()));
        options.fields = Utils._.union(options.fields, Object.keys(values));
      }

      // We want to skip validations for all other fields
      options.skip = Utils._.difference(Object.keys(self.attributes), Object.keys(values));
      return build.hookValidate(options).then(function(attributes) {
        options.skip = undefined;
        if (attributes && attributes.dataValues) {
          values = Utils._.pick(attributes.dataValues, Object.keys(values));
        }
      });
    }
    return null;
  }).then(function() {
    // Run before hook
    if (options.hooks) {
      options.attributes = values;
      return self.runHooks('beforeBulkUpdate', options).then(function() {
        values = options.attributes;
        delete options.attributes;
      });
    }
    return null;
  }).then(function() {
    valuesUse = values;

    // Get instances and run beforeUpdate hook on each record individually
    if (options.individualHooks) {
      return self.findAll({where: options.where, transaction: options.transaction, logging: options.logging, benchmark: options.
benchmark}).then(function(_instances) {
        instances = _instances;
        if (!instances.length) {
          return [];
        }

        // Run beforeUpdate hooks on each record and check whether beforeUpdate hook changes values uniformly
        // i.e. whether they change values for each record in the same way
        var changedValues
          , different = false;

        return Promise.map(instances, function(instance) {
          // Record updates in instances dataValues
          Utils._.extend(instance.dataValues, values);
          // Set the changed fields on the instance
          Utils._.forIn(valuesUse, function(newValue, attr) {
            if (newValue !== instance._previousDataValues[attr]) {
              instance.setDataValue(attr, newValue);
            }
          });

          // Run beforeUpdate hook
          return self.runHooks('beforeUpdate', instance, options).then(function() {
            if (!different) {
              var thisChangedValues = {}; ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.upsert"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>upsert (values, options)](#apidoc.element.sequelize.Model.prototype.upsert)
- description and source-code
```javascript
upsert = function (values, options) {
  options = Utils.cloneDeep(options) || {};

  if (!options.fields) {
    options.fields = Object.keys(this.attributes);
  }

  var createdAtAttr = this._timestampAttributes.createdAt
    , updatedAtAttr = this._timestampAttributes.updatedAt
    , hadPrimary = this.primaryKeyField in values || this.primaryKeyAttribute in values
    , instance = this.build(values);

  return instance.hookValidate(options).bind(this).then(function () {
    // Map field names
    var updatedDataValues = _.pick(instance.dataValues, Object.keys(instance._changed))
      , insertValues = Utils.mapValueFieldNames(instance.dataValues, options.fields, this)
      , updateValues = Utils.mapValueFieldNames(updatedDataValues, options.fields, this)
      , now = Utils.now(this.sequelize.options.dialect);

    // Attach createdAt
    if (createdAtAttr && !updateValues[createdAtAttr]) {
      insertValues[createdAtAttr] = this.$getDefaultTimestamp(createdAtAttr) || now;
    }
    if (updatedAtAttr && !insertValues[updatedAtAttr]) {
      insertValues[updatedAtAttr] = updateValues[updatedAtAttr] = this.$getDefaultTimestamp(updatedAtAttr) || now;
    }

    // Build adds a null value for the primary key, if none was given by the user.
    // We need to remove that because of some Postgres technicalities.
    if (!hadPrimary && this.primaryKeyAttribute && !this.rawAttributes[this.primaryKeyAttribute].defaultValue) {
      delete insertValues[this.primaryKeyField];
      delete updateValues[this.primaryKeyField];
    }

    return this.QueryInterface.upsert(this.getTableName(options), insertValues, updateValues, instance.where(), this, options);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Model.prototype.validationFailed"></a>[function <span class="apidocSignatureSpan">sequelize.Model.prototype.</span>validationFailed (name, callback)](#apidoc.element.sequelize.Model.prototype.validationFailed)
- description and source-code
```javascript
validationFailed = function (name, callback) {
  return this.addHook(hook, name, callback);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.NOW"></a>[module sequelize.NOW](#apidoc.module.sequelize.NOW)

#### <a name="apidoc.element.sequelize.NOW.NOW"></a>[function <span class="apidocSignatureSpan">sequelize.</span>NOW ()](#apidoc.element.sequelize.NOW.NOW)
- description and source-code
```javascript
NOW = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NOW.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.NOW.</span>inherits (Constructor)](#apidoc.element.sequelize.NOW.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.NOW.super_"></a>[function <span class="apidocSignatureSpan">sequelize.NOW.</span>super_ (options)](#apidoc.element.sequelize.NOW.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NOW.warn"></a>[function <span class="apidocSignatureSpan">sequelize.NOW.</span>warn (link, text)](#apidoc.element.sequelize.NOW.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.NUMBER"></a>[module sequelize.NUMBER](#apidoc.module.sequelize.NUMBER)

#### <a name="apidoc.element.sequelize.NUMBER.NUMBER"></a>[function <span class="apidocSignatureSpan">sequelize.</span>NUMBER (options)](#apidoc.element.sequelize.NUMBER.NUMBER)
- description and source-code
```javascript
NUMBER = function (options) {
  this.options = options;
  this._length = options.length;
  this._zerofill = options.zerofill;
  this._decimals = options.decimals;
  this._precision = options.precision;
  this._scale = options.scale;
  this._unsigned = options.unsigned;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NUMBER.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.NUMBER.</span>inherits (Constructor)](#apidoc.element.sequelize.NUMBER.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.NUMBER.super_"></a>[function <span class="apidocSignatureSpan">sequelize.NUMBER.</span>super_ (options)](#apidoc.element.sequelize.NUMBER.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NUMBER.warn"></a>[function <span class="apidocSignatureSpan">sequelize.NUMBER.</span>warn (link, text)](#apidoc.element.sequelize.NUMBER.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.NUMBER.prototype"></a>[module sequelize.NUMBER.prototype](#apidoc.module.sequelize.NUMBER.prototype)

#### <a name="apidoc.element.sequelize.NUMBER.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.NUMBER.prototype.</span>toSql ()](#apidoc.element.sequelize.NUMBER.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  var result = this.key;
  if (this._length) {
    result += '(' + this._length;
    if (typeof this._decimals === 'number') {
      result += ',' + this._decimals;
    }
    result += ')';
  }
  if (this._unsigned) {
    result += ' UNSIGNED';
  }
  if (this._zerofill) {
    result += ' ZEROFILL';
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.NUMBER.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.NUMBER.prototype.</span>validate (value)](#apidoc.element.sequelize.NUMBER.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!Validator.isFloat(String(value))) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid number', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.Promise"></a>[module sequelize.Promise](#apidoc.module.sequelize.Promise)

#### <a name="apidoc.element.sequelize.Promise.Promise"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Promise (executor)](#apidoc.element.sequelize.Promise.Promise)
- description and source-code
```javascript
function Promise(executor) {
    if (executor !== INTERNAL) {
        check(this, executor);
    }
    this._bitField = 0;
    this._fulfillmentHandler0 = undefined;
    this._rejectionHandler0 = undefined;
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._resolveFromExecutor(executor);
    this._promiseCreated();
    this._fireEvent("promiseCreated", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>AggregateError (message)](#apidoc.element.sequelize.Promise.AggregateError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.CancellationError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>CancellationError (message)](#apidoc.element.sequelize.Promise.CancellationError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.OperationalError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>OperationalError (message)](#apidoc.element.sequelize.Promise.OperationalError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>PromiseInspection (promise)](#apidoc.element.sequelize.Promise.PromiseInspection)
- description and source-code
```javascript
function PromiseInspection(promise) {
    if (promise !== undefined) {
        promise = promise._target();
        this._bitField = promise._bitField;
        this._settledValueField = promise._isFateSealed()
            ? promise._settledValue() : undefined;
    }
    else {
        this._bitField = 0;
        this._settledValueField = undefined;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.RangeError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>RangeError ()](#apidoc.element.sequelize.Promise.RangeError)
- description and source-code
```javascript
function RangeError() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.RejectionError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>RejectionError (message)](#apidoc.element.sequelize.Promise.RejectionError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.Sequelize"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>Sequelize (database, username, password, options)](#apidoc.element.sequelize.Promise.Sequelize)
- description and source-code
```javascript
Sequelize = function (database, username, password, options) {
  var config;

  if (arguments.length === 1 && typeof database === 'object') {
    // new Sequelize({ ... options })
    options = database;
    config = _.pick(options, 'host', 'port', 'database', 'username', 'password');
  } else if ((arguments.length === 1 && typeof database === 'string') || (arguments.length === 2 && typeof username === 'object')) {
    // new Sequelize(URI, { ... options })

    config = {};
    options = username || {};

    var urlParts = url.parse(arguments[0]);

    // SQLite don't have DB in connection url
    if (urlParts.pathname) {
      config.database = urlParts.pathname.replace(/^\//, '');
    }

    options.dialect = urlParts.protocol.replace(/:$/, '');
    options.host = urlParts.hostname;

    if (urlParts.port) {
      options.port = urlParts.port;
    }

    if (urlParts.auth) {
      config.username = urlParts.auth.split(':')[0];
      config.password = urlParts.auth.split(':')[1];
    }
  } else {
    // new Sequelize(database, username, password, { ... options })
    options = options || {};
    config = {database: database, username: username, password: password};
  }

  Sequelize.runHooks('beforeInit', config, options);

  this.options = Utils._.extend({
    dialect: 'mysql',
    dialectModulePath: null,
    host: 'localhost',
    protocol: 'tcp',
    define: {},
    query: {},
    sync: {},
    timezone:'+00:00',
    logging: console.log,
    omitNull: false,
    native: false,
    replication: false,
    ssl: undefined,
    pool: {},
    quoteIdentifiers: true,
    hooks: {},
    retry: {max: 5, match: ['SQLITE_BUSY: database is locked']},
    transactionType: Transaction.TYPES.DEFERRED,
    isolationLevel: Transaction.ISOLATION_LEVELS.REPEATABLE_READ,
    databaseVersion: 0,
    typeValidation: false,
    benchmark: false
  }, options || {});

  if (this.options.dialect === 'postgresql') {
    this.options.dialect = 'postgres';
  }

  if (this.options.dialect === 'sqlite' && this.options.timezone !== '+00:00') {
    throw new Error('Setting a custom timezone is not supported by SQLite, dates are always returned as UTC. Please remove the custom
 timezone parameter.');
  }

  if (this.options.logging === true) {
    console.log('DEPRECATION WARNING: The logging-option should be either a function or false. Default: console.log');
    this.options.logging = console.log;
  }

  this.options.hooks = this.replaceHookAliases(this.options.hooks);

  if ((['', null, false].indexOf(config.password) > -1) || (typeof config.password === 'undefined')) {
    config.password = null;
  }

  this.config = {
    database: config.database,
    username: config.username,
    password: config.password,
    host: config.host || this.options.host,
    port: config.port || this.options.port,
    pool: this.options.pool,
    protocol: this.options.protocol,
    native: this.options.native,
    ssl: this.options.ssl,
    replication: this.options.replication,
    dialectModulePath: this.options.dialectModulePath,
    keepDefaultTimezone: this.options.keepDefaultTimezone,
    dialectOptions: this.options.dialectOptions
  };

  var Dialect;
  // Requiring the dialect in a switch-case to keep the
  // require calls static. (Browserify fix)
  switch (this.getDialect()){
    case 'mariadb':
      Dialect = require('./dialects/mariadb');
      break;
    case 'mssql':
      Dialect = require('./dialects/mssql');
      break;
    case 'mysql':
      Dialect = require('./dialects/mysql');
      break;
    case 'postgres':
      Dialect = require('./dialects/postgres');
      break;
    case 'sqlite':
      Dialect = require('./dialects/sqlite');
      break;
    default:
      throw new Error('The dialect ' + this.getDialect() + ' is not supported. Supported dialects: mariadb, mssql, mysql, postgres
, and sqlite.');
  }
  this.dialect = new Dialect(this);

  this.dialect.QueryGenerator.typeValidation = options.typeValidation;

<span class="apidocCodeCommentSpan">  /**
   * Models are stored here under the name given to 'sequelize.define'
   * @property models
   */
</span>  this.models = {};
  this.mod ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.TimeoutError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>TimeoutError (message)](#apidoc.element.sequelize.Promise.TimeoutError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.TypeError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>TypeError ()](#apidoc.element.sequelize.Promise.TypeError)
- description and source-code
```javascript
function TypeError() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>_SomePromiseArray (values)](#apidoc.element.sequelize.Promise._SomePromiseArray)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._peekContext"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>_peekContext ()](#apidoc.element.sequelize.Promise._peekContext)
- description and source-code
```javascript
_peekContext = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.all"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>all (promises)](#apidoc.element.sequelize.Promise.all)
- description and source-code
```javascript
all = function (promises) {
    return new PromiseArray(promises).promise();
}
```
- example usage
```shell
...
InstanceValidator.prototype.validate = function() {
if (this.inProgress) {
  throw new Error('Validations already in progress.');
}
this.inProgress = true;

var self = this;
return Promise.all([
  self._builtinValidators(),
  self._customValidators()
].map(function(promise) {
  return promise.reflect();
})).then(function() {
  if (self.errors.length) {
    return new sequelizeError.ValidationError(null, self.errors);
...
```

#### <a name="apidoc.element.sequelize.Promise.any"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>any (promises)](#apidoc.element.sequelize.Promise.any)
- description and source-code
```javascript
any = function (promises) {
    return any(promises);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.attempt"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>attempt (fn)](#apidoc.element.sequelize.Promise.attempt)
- description and source-code
```javascript
attempt = function (fn) {
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._pushContext();
    var value;
    if (arguments.length > 1) {
        debug.deprecated("calling Promise.try with more than 1 argument");
        var arg = arguments[1];
        var ctx = arguments[2];
        value = util.isArray(arg) ? tryCatch(fn).apply(ctx, arg)
                                  : tryCatch(fn).call(ctx, arg);
    } else {
        value = tryCatch(fn)();
    }
    var promiseCreated = ret._popContext();
    debug.checkForgottenReturns(
        value, promiseCreated, "Promise.try", ret);
    ret._resolveFromSyncValue(value);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.bind"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>bind (thisArg, value)](#apidoc.element.sequelize.Promise.bind)
- description and source-code
```javascript
bind = function (thisArg, value) {
    return Promise.resolve(value).bind(thisArg);
}
```
- example usage
```shell
...

   var validatorPromise = self._invokeBuiltinValidator(value, test, validatorType, field);
   // errors are handled in settling, stub this
   validatorPromise.catch(function() {});
   validators.push(validatorPromise.reflect());
 });

 return Promise.all(validators).then(this._handleReflectedResult.bind(this, field));
};

/**
* Prepare and invoke a custom validator.
*
* @param {Function} validator The custom validator.
* @param {string} validatorType the custom validator type (name).
...
```

#### <a name="apidoc.element.sequelize.Promise.cast"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>cast (obj)](#apidoc.element.sequelize.Promise.cast)
- description and source-code
```javascript
cast = function (obj) {
    var ret = tryConvertToPromise(obj);
    if (!(ret instanceof Promise)) {
        ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._setFulfilled();
        ret._rejectionHandler0 = obj;
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.config"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>config (opts)](#apidoc.element.sequelize.Promise.config)
- description and source-code
```javascript
config = function (opts) {
    opts = Object(opts);
    if ("longStackTraces" in opts) {
        if (opts.longStackTraces) {
            Promise.longStackTraces();
        } else if (!opts.longStackTraces && Promise.hasLongStackTraces()) {
            disableLongStackTraces();
        }
    }
    if ("warnings" in opts) {
        var warningsOption = opts.warnings;
        config.warnings = !!warningsOption;
        wForgottenReturn = config.warnings;

        if (util.isObject(warningsOption)) {
            if ("wForgottenReturn" in warningsOption) {
                wForgottenReturn = !!warningsOption.wForgottenReturn;
            }
        }
    }
    if ("cancellation" in opts && opts.cancellation && !config.cancellation) {
        if (async.haveItemsQueued()) {
            throw new Error(
                "cannot enable cancellation after promises are in use");
        }
        Promise.prototype._clearCancellationData =
            cancellationClearCancellationData;
        Promise.prototype._propagateFrom = cancellationPropagateFrom;
        Promise.prototype._onCancel = cancellationOnCancel;
        Promise.prototype._setOnCancel = cancellationSetOnCancel;
        Promise.prototype._attachCancellationCallback =
            cancellationAttachCancellationCallback;
        Promise.prototype._execute = cancellationExecute;
        propagateFromFunction = cancellationPropagateFrom;
        config.cancellation = true;
    }
    if ("monitoring" in opts) {
        if (opts.monitoring && !config.monitoring) {
            config.monitoring = true;
            Promise.prototype._fireEvent = activeFireEvent;
        } else if (!opts.monitoring && config.monitoring) {
            config.monitoring = false;
            Promise.prototype._fireEvent = defaultFireEvent;
        }
    }
    return Promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.coroutine"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.sequelize.Promise.coroutine)
- description and source-code
```javascript
coroutine = function (generatorFunction, options) {
    if (typeof generatorFunction !== "function") {
        throw new TypeError("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var yieldHandler = Object(options).yieldHandler;
    var PromiseSpawn$ = PromiseSpawn;
    var stack = new Error().stack;
    return function () {
        var generator = generatorFunction.apply(this, arguments);
        var spawn = new PromiseSpawn$(undefined, undefined, yieldHandler,
                                      stack);
        var ret = spawn.promise();
        spawn._generator = generator;
        spawn._promiseFulfilled(undefined);
        return ret;
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.defer"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>defer ()](#apidoc.element.sequelize.Promise.defer)
- description and source-code
```javascript
defer = function () {
    debug.deprecated("Promise.defer", "new Promise");
    var promise = new Promise(INTERNAL);
    return {
        promise: promise,
        resolve: deferResolve,
        reject: deferReject
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.delay"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>delay (ms, value)](#apidoc.element.sequelize.Promise.delay)
- description and source-code
```javascript
delay = function (ms, value) {
    var ret;
    var handle;
    if (value !== undefined) {
        ret = Promise.resolve(value)
                ._then(afterValue, null, null, ms, undefined);
        if (debug.cancellation() && value instanceof Promise) {
            ret._setOnCancel(value);
        }
    } else {
        ret = new Promise(INTERNAL);
        handle = setTimeout(function() { ret._fulfill(); }, +ms);
        if (debug.cancellation()) {
            ret._setOnCancel(new HandleWrapper(handle));
        }
        ret._captureStackTrace();
    }
    ret._setAsyncGuaranteed();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.each"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>each ()](#apidoc.element.sequelize.Promise.each)
- description and source-code
```javascript
each = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  beforeConnection: 'beforeConnect'
};

var Hooks = {
  replaceHookAliases: function(hooks) {
    var realHookName;

    Utils._.each(hooks, function(hooksArray, name) {
      // Does an alias for this hook name exist?
      if (realHookName = hookAliases[name]) {
// Add the hooks to the actual hook
hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

// Delete the alias
delete hooks[name];
...
```

#### <a name="apidoc.element.sequelize.Promise.filter"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>filter ()](#apidoc.element.sequelize.Promise.filter)
- description and source-code
```javascript
filter = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
  }

  return typeof hook === 'object' && hook.name !== name;
});
...
```

#### <a name="apidoc.element.sequelize.Promise.fromCallback"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>fromCallback (fn)](#apidoc.element.sequelize.Promise.fromCallback)
- description and source-code
```javascript
fromCallback = function (fn) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    var multiArgs = arguments.length > 1 ? !!Object(arguments[1]).multiArgs
                                         : false;
    var result = tryCatch(fn)(nodebackForPromise(ret, multiArgs));
    if (result === errorObj) {
        ret._rejectCallback(result.e, true);
    }
    if (!ret._isFateSealed()) ret._setAsyncGuaranteed();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.fromNode"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>fromNode (fn)](#apidoc.element.sequelize.Promise.fromNode)
- description and source-code
```javascript
fromNode = function (fn) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    var multiArgs = arguments.length > 1 ? !!Object(arguments[1]).multiArgs
                                         : false;
    var result = tryCatch(fn)(nodebackForPromise(ret, multiArgs));
    if (result === errorObj) {
        ret._rejectCallback(result.e, true);
    }
    if (!ret._isFateSealed()) ret._setAsyncGuaranteed();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.fulfilled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>fulfilled (obj)](#apidoc.element.sequelize.Promise.fulfilled)
- description and source-code
```javascript
fulfilled = function (obj) {
    var ret = tryConvertToPromise(obj);
    if (!(ret instanceof Promise)) {
        ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._setFulfilled();
        ret._rejectionHandler0 = obj;
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.getNewLibraryCopy"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>getNewLibraryCopy ()](#apidoc.element.sequelize.Promise.getNewLibraryCopy)
- description and source-code
```javascript
getNewLibraryCopy = function () {
var makeSelfResolutionError = function () {
    return new TypeError("circular promise resolution chain\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
};
var reflectHandler = function() {
    return new Promise.PromiseInspection(this._target());
};
var apiRejection = function(msg) {
    return Promise.reject(new TypeError(msg));
};
function Proxyable() {}
var UNDEFINED_BINDING = {};
var util = require("./util");

var getDomain;
if (util.isNode) {
    getDomain = function() {
        var ret = process.domain;
        if (ret === undefined) ret = null;
        return ret;
    };
} else {
    getDomain = function() {
        return null;
    };
}
util.notEnumerableProp(Promise, "_getDomain", getDomain);

var es5 = require("./es5");
var Async = require("./async");
var async = new Async();
es5.defineProperty(Promise, "_async", {value: async});
var errors = require("./errors");
var TypeError = Promise.TypeError = errors.TypeError;
Promise.RangeError = errors.RangeError;
var CancellationError = Promise.CancellationError = errors.CancellationError;
Promise.TimeoutError = errors.TimeoutError;
Promise.OperationalError = errors.OperationalError;
Promise.RejectionError = errors.OperationalError;
Promise.AggregateError = errors.AggregateError;
var INTERNAL = function(){};
var APPLY = {};
var NEXT_FILTER = {};
var tryConvertToPromise = require("./thenables")(Promise, INTERNAL);
var PromiseArray =
    require("./promise_array")(Promise, INTERNAL,
                               tryConvertToPromise, apiRejection, Proxyable);
var Context = require("./context")(Promise);
<span class="apidocCodeCommentSpan"> /*jshint unused:false*/
</span>var createContext = Context.create;
var debug = require("./debuggability")(Promise, Context);
var CapturedTrace = debug.CapturedTrace;
var PassThroughHandlerContext =
    require("./finally")(Promise, tryConvertToPromise, NEXT_FILTER);
var catchFilter = require("./catch_filter")(NEXT_FILTER);
var nodebackForPromise = require("./nodeback");
var errorObj = util.errorObj;
var tryCatch = util.tryCatch;
function check(self, executor) {
    if (self == null || self.constructor !== Promise) {
        throw new TypeError("the promise constructor cannot be invoked directly\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    if (typeof executor !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(executor));
    }

}

function Promise(executor) {
    if (executor !== INTERNAL) {
        check(this, executor);
    }
    this._bitField = 0;
    this._fulfillmentHandler0 = undefined;
    this._rejectionHandler0 = undefined;
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._resolveFromExecutor(executor);
    this._promiseCreated();
    this._fireEvent("promiseCreated", this);
}

Promise.prototype.toString = function () {
    return "[object Promise]";
};

Promise.prototype.caught = Promise.prototype["catch"] = function (fn) {
    var len = arguments.length;
    if (len > 1) {
        var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return apiRejection("Catch statement predicate: " +
                    "expecting an object but got " + util.classString(item));
            }
        }
        catchInstances.length = j;
        fn = arguments[i];
        return this.then(undefined, catchFilter(catchInstances, fn, this));
    }
    return this.then(undefined, fn);
};

Promise.prototype.reflect = function () {
    return this._then(reflectHandler,
        reflectHandler, undefined, this, undefined);
};

Promise.prototype.then = function (didFulfill, didReject) {
    if (debug.warnings() && arguments.length > 0 &&
        typeof didFulfill !== "function" &&
        typeof didReject !== "function") {
        var msg = ".then() only accepts functions but was passed: " +
                util.classString(didFulfill);
        if (arguments.length > 1) {
            msg += ", ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.hasLongStackTraces"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>hasLongStackTraces ()](#apidoc.element.sequelize.Promise.hasLongStackTraces)
- description and source-code
```javascript
hasLongStackTraces = function () {
    return config.longStackTraces && longStackTracesIsSupported();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.is"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>is (val)](#apidoc.element.sequelize.Promise.is)
- description and source-code
```javascript
is = function (val) {
    return val instanceof Promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.join"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>join ()](#apidoc.element.sequelize.Promise.join)
- description and source-code
```javascript
join = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
 if (message) {
   this.message = message;

 // ... otherwise create a concatenated message out of existing errors.
 } else if (this.errors.length > 0 && this.errors[0].message) {
   this.message = this.errors.map(function(err) {
     return err.type + ': ' + err.message;
   }).join(',\n');
 }
};
util.inherits(error.ValidationError, error.BaseError);

/**
* Gets all validation error items for the path / field specified.
*
...
```

#### <a name="apidoc.element.sequelize.Promise.longStackTraces"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>longStackTraces ()](#apidoc.element.sequelize.Promise.longStackTraces)
- description and source-code
```javascript
longStackTraces = function () {
    if (async.haveItemsQueued() && !config.longStackTraces) {
        throw new Error("cannot enable long stack traces after promises have been created\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    if (!config.longStackTraces && longStackTracesIsSupported()) {
        var Promise_captureStackTrace = Promise.prototype._captureStackTrace;
        var Promise_attachExtraTrace = Promise.prototype._attachExtraTrace;
        config.longStackTraces = true;
        disableLongStackTraces = function() {
            if (async.haveItemsQueued() && !config.longStackTraces) {
                throw new Error("cannot enable long stack traces after promises have been created\u000a\u000a    See http://goo.
gl/MqrFmX\u000a");
            }
            Promise.prototype._captureStackTrace = Promise_captureStackTrace;
            Promise.prototype._attachExtraTrace = Promise_attachExtraTrace;
            Context.deactivateLongStackTraces();
            async.enableTrampoline();
            config.longStackTraces = false;
        };
        Promise.prototype._captureStackTrace = longStackTracesCaptureStackTrace;
        Promise.prototype._attachExtraTrace = longStackTracesAttachExtraTrace;
        Context.activateLongStackTraces();
        async.disableTrampolineIfNecessary();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.map"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>map ()](#apidoc.element.sequelize.Promise.map)
- description and source-code
```javascript
map = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
};
util.inherits(error.ValidationError, error.BaseError);

/**
...
```

#### <a name="apidoc.element.sequelize.Promise.mapSeries"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>mapSeries ()](#apidoc.element.sequelize.Promise.mapSeries)
- description and source-code
```javascript
mapSeries = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.method"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>method (fn)](#apidoc.element.sequelize.Promise.method)
- description and source-code
```javascript
method = function (fn) {
    if (typeof fn !== "function") {
        throw new Promise.TypeError("expecting a function but got " + util.classString(fn));
    }
    return function () {
        var ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._pushContext();
        var value = tryCatch(fn).apply(this, arguments);
        var promiseCreated = ret._popContext();
        debug.checkForgottenReturns(
            value, promiseCreated, "Promise.method", ret);
        ret._resolveFromSyncValue(value);
        return ret;
    };
}
```
- example usage
```shell
...
 * @param {Function} validator The custom validator.
 * @param {string} validatorType the custom validator type (name).
 * @param {boolean=} optAttrDefined Set to true if custom validator was defined
 *   from the Attribute
 * @return {Promise} A promise.
 * @private
 */
InstanceValidator.prototype._invokeCustomValidator = Promise.method(function(validator, validatorType, optAttrDefined, optValue,
optField) {
var validatorFunction = null;  // the validation function to call
var isAsync = false;

var validatorArity = validator.length;
// check if validator is async and requires a callback
var asyncArity = 1;
var errorKey = validatorType;
...
```

#### <a name="apidoc.element.sequelize.Promise.noConflict"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>noConflict ()](#apidoc.element.sequelize.Promise.noConflict)
- description and source-code
```javascript
function noConflict() {
    try { if (Promise === bluebird) Promise = old; }
    catch (e) {}
    return bluebird;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.onPossiblyUnhandledRejection"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>onPossiblyUnhandledRejection (fn)](#apidoc.element.sequelize.Promise.onPossiblyUnhandledRejection)
- description and source-code
```javascript
onPossiblyUnhandledRejection = function (fn) {
    var domain = getDomain();
    possiblyUnhandledRejection =
        typeof fn === "function" ? (domain === null ?
                                            fn : util.domainBind(domain, fn))
                                 : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.onUnhandledRejectionHandled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>onUnhandledRejectionHandled (fn)](#apidoc.element.sequelize.Promise.onUnhandledRejectionHandled)
- description and source-code
```javascript
onUnhandledRejectionHandled = function (fn) {
    var domain = getDomain();
    unhandledRejectionHandled =
        typeof fn === "function" ? (domain === null ?
                                            fn : util.domainBind(domain, fn))
                                 : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.pending"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>pending ()](#apidoc.element.sequelize.Promise.pending)
- description and source-code
```javascript
pending = function () {
    debug.deprecated("Promise.defer", "new Promise");
    var promise = new Promise(INTERNAL);
    return {
        promise: promise,
        resolve: deferResolve,
        reject: deferReject
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.promisify"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>promisify (fn, options)](#apidoc.element.sequelize.Promise.promisify)
- description and source-code
```javascript
promisify = function (fn, options) {
    if (typeof fn !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(fn));
    }
    if (isPromisified(fn)) {
        return fn;
    }
    options = Object(options);
    var receiver = options.context === undefined ? THIS : options.context;
    var multiArgs = !!options.multiArgs;
    var ret = promisify(fn, receiver, multiArgs);
    util.copyDescriptors(fn, ret, propsFilter);
    return ret;
}
```
- example usage
```shell
...
// run hooks async
var promise = Promise.each(hooks, function (hook) {
  if (typeof hook === 'object') {
    hook = hook.fn;
  }

  if (hookType && hook.length > hookTypes[hookType].params) {
    hook = Promise.promisify(hook, self);
  }

  return hook.apply(self, fnArgs);
}).return();

if (fn) {
  return promise.nodeify(fn);
...
```

#### <a name="apidoc.element.sequelize.Promise.promisifyAll"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>promisifyAll (target, options)](#apidoc.element.sequelize.Promise.promisifyAll)
- description and source-code
```javascript
promisifyAll = function (target, options) {
    if (typeof target !== "function" && typeof target !== "object") {
        throw new TypeError("the target of promisifyAll must be an object or a function\u000a\u000a    See http://goo.gl/MqrFmX\
u000a");
    }
    options = Object(options);
    var multiArgs = !!options.multiArgs;
    var suffix = options.suffix;
    if (typeof suffix !== "string") suffix = defaultSuffix;
    var filter = options.filter;
    if (typeof filter !== "function") filter = defaultFilter;
    var promisifier = options.promisifier;
    if (typeof promisifier !== "function") promisifier = makeNodePromisified;

    if (!util.isIdentifier(suffix)) {
        throw new RangeError("suffix must be a valid identifier\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }

    var keys = util.inheritedDataKeys(target);
    for (var i = 0; i < keys.length; ++i) {
        var value = target[keys[i]];
        if (keys[i] !== "constructor" &&
            util.isClass(value)) {
            promisifyAll(value.prototype, suffix, filter, promisifier,
                multiArgs);
            promisifyAll(value, suffix, filter, promisifier, multiArgs);
        }
    }

    return promisifyAll(target, suffix, filter, promisifier, multiArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.props"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>props (promises)](#apidoc.element.sequelize.Promise.props)
- description and source-code
```javascript
props = function (promises) {
    return props(promises);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.race"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>race (promises)](#apidoc.element.sequelize.Promise.race)
- description and source-code
```javascript
race = function (promises) {
    return race(promises, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>reduce ()](#apidoc.element.sequelize.Promise.reduce)
- description and source-code
```javascript
reduce = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
/**
 * Gets all validation error items for the path / field specified.
 *
 * @param {string} path The path to be checked for error items
 * @returns {Array} Validation error items for the specified path
 */
error.ValidationError.prototype.get = function(path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
};
...
```

#### <a name="apidoc.element.sequelize.Promise.reject"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>reject (reason)](#apidoc.element.sequelize.Promise.reject)
- description and source-code
```javascript
reject = function (reason) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._rejectCallback(reason, true);
    return ret;
}
```
- example usage
```shell
...
    sql,
    _.assign({}, options, { type: QueryTypes.DESCRIBE })
  ).then(function(data) {
    // If no data is returned from the query, then the table name may be wrong.
    // Query generators that use information_schema for retrieving table info will just return an empty result set,
    // it will not throw an error like built-ins do (e.g. DESCRIBE on MySql).
    if (Utils._.isEmpty(data)) {
      return Promise.reject('No description found for "' + tableName + '" table. Check the table name and schema; remember, they
 _are_ case sensitive.');
    } else {
      return Promise.resolve(data);
    }
  });
};

QueryInterface.prototype.addColumn = function(table, key, attribute, options) {
...
```

#### <a name="apidoc.element.sequelize.Promise.rejected"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>rejected (reason)](#apidoc.element.sequelize.Promise.rejected)
- description and source-code
```javascript
rejected = function (reason) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._rejectCallback(reason, true);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.resolve"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>resolve (obj)](#apidoc.element.sequelize.Promise.resolve)
- description and source-code
```javascript
resolve = function (obj) {
    var ret = tryConvertToPromise(obj);
    if (!(ret instanceof Promise)) {
        ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._setFulfilled();
        ret._rejectionHandler0 = obj;
    }
    return ret;
}
```
- example usage
```shell
...
 *   auto populates error on this.error local object.
 * @private
 */
InstanceValidator.prototype._builtinAttrValidate = function(value, field) {
var self = this;
// check if value is null (if null not allowed the Schema pass will capture it)
if (value === null || typeof value === 'undefined') {
  return Promise.resolve();
}

// Promisify each validator
var validators = [];
Utils._.forIn(this.modelInstance.validators[field], function(test,
  validatorType) {
...
```

#### <a name="apidoc.element.sequelize.Promise.setScheduler"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>setScheduler (fn)](#apidoc.element.sequelize.Promise.setScheduler)
- description and source-code
```javascript
setScheduler = function (fn) {
    if (typeof fn !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(fn));
    }
    return async.setScheduler(fn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.settle"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>settle (promises)](#apidoc.element.sequelize.Promise.settle)
- description and source-code
```javascript
settle = function (promises) {
    debug.deprecated(".settle()", ".reflect()");
    return new SettledPromiseArray(promises).promise();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.some"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>some (promises, howMany)](#apidoc.element.sequelize.Promise.some)
- description and source-code
```javascript
some = function (promises, howMany) {
    return some(promises, howMany);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.spawn"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>spawn (generatorFunction)](#apidoc.element.sequelize.Promise.spawn)
- description and source-code
```javascript
spawn = function (generatorFunction) {
    debug.deprecated("Promise.spawn()", "Promise.coroutine()");
    if (typeof generatorFunction !== "function") {
        return apiRejection("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var spawn = new PromiseSpawn(generatorFunction, this);
    var ret = spawn.promise();
    spawn._run(Promise.spawn);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.try"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>try (fn)](#apidoc.element.sequelize.Promise.try)
- description and source-code
```javascript
try = function (fn) {
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._pushContext();
    var value;
    if (arguments.length > 1) {
        debug.deprecated("calling Promise.try with more than 1 argument");
        var arg = arguments[1];
        var ctx = arguments[2];
        value = util.isArray(arg) ? tryCatch(fn).apply(ctx, arg)
                                  : tryCatch(fn).call(ctx, arg);
    } else {
        value = tryCatch(fn)();
    }
    var promiseCreated = ret._popContext();
    debug.checkForgottenReturns(
        value, promiseCreated, "Promise.try", ret);
    ret._resolveFromSyncValue(value);
    return ret;
}
```
- example usage
```shell
...
   if (optAttrDefined) {
     validatorFunction = Promise.promisify(validator.bind(this.modelInstance, invokeArgs));
   } else {
     validatorFunction = Promise.promisify(validator.bind(this.modelInstance));
   }
   return validatorFunction().catch(this._pushError.bind(this, false, errorKey));
 } else {
   return Promise.try(validator.bind(this.modelInstance, invokeArgs)).catch(this._pushError.bind(this, false, errorKey));
 }
});

/**
* Prepare and invoke a build-in validator.
*
* @param {*} value Anything.
...
```

#### <a name="apidoc.element.sequelize.Promise.using"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>using ()](#apidoc.element.sequelize.Promise.using)
- description and source-code
```javascript
using = function () {
    var len = arguments.length;
    if (len < 2) return apiRejection(
                    "you must pass at least 2 arguments to Promise.using");
    var fn = arguments[len - 1];
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    var input;
    var spreadArgs = true;
    if (len === 2 && Array.isArray(arguments[0])) {
        input = arguments[0];
        len = input.length;
        spreadArgs = false;
    } else {
        input = arguments;
        len--;
    }
    var resources = new ResourceList(len);
    for (var i = 0; i < len; ++i) {
        var resource = input[i];
        if (Disposer.isDisposer(resource)) {
            var disposer = resource;
            resource = resource.promise();
            resource._setDisposable(disposer);
        } else {
            var maybePromise = tryConvertToPromise(resource);
            if (maybePromise instanceof Promise) {
                resource =
                    maybePromise._then(maybeUnwrapDisposer, null, null, {
                        resources: resources,
                        index: i
                }, undefined);
            }
        }
        resources[i] = resource;
    }

    var reflectedResources = new Array(resources.length);
    for (var i = 0; i < reflectedResources.length; ++i) {
        reflectedResources[i] = Promise.resolve(resources[i]).reflect();
    }

    var resultPromise = Promise.all(reflectedResources)
        .then(function(inspections) {
            for (var i = 0; i < inspections.length; ++i) {
                var inspection = inspections[i];
                if (inspection.isRejected()) {
                    errorObj.e = inspection.error();
                    return errorObj;
                } else if (!inspection.isFulfilled()) {
                    resultPromise.cancel();
                    return;
                }
                inspections[i] = inspection.value();
            }
            promise._pushContext();

            fn = tryCatch(fn);
            var ret = spreadArgs
                ? fn.apply(undefined, inspections) : fn(inspections);
            var promiseCreated = promise._popContext();
            debug.checkForgottenReturns(
                ret, promiseCreated, "Promise.using", promise);
            return ret;
        });

    var promise = resultPromise.lastly(function() {
        var inspection = new Promise.PromiseInspection(resultPromise);
        return dispose(resources, inspection);
    });
    resources.promise = promise;
    promise._setOnCancel(resources);
    return promise;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.AggregateError"></a>[module sequelize.Promise.AggregateError](#apidoc.module.sequelize.Promise.AggregateError)

#### <a name="apidoc.element.sequelize.Promise.AggregateError.AggregateError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>AggregateError (message)](#apidoc.element.sequelize.Promise.AggregateError.AggregateError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.AggregateError.prototype"></a>[module sequelize.Promise.AggregateError.prototype](#apidoc.module.sequelize.Promise.AggregateError.prototype)

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.AggregateError.prototype.constructor)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.every"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>every ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.every)
- description and source-code
```javascript
function every() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.filter"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>filter ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.filter)
- description and source-code
```javascript
function filter() { [native code] }
```
- example usage
```shell
...
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
  }

  return typeof hook === 'object' && hook.name !== name;
});
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.forEach"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>forEach ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.forEach)
- description and source-code
```javascript
function forEach() { [native code] }
```
- example usage
```shell
...

if (!Array.isArray(hooks)) {
  hooks = hooks === undefined ? [] : [hooks];
}

// run hooks as sync functions if flagged as sync
if (hookTypes[hookType] && hookTypes[hookType].sync) {
  hooks.forEach(function(hook) {
    if (typeof hook === 'object') hook = hook.fn;
    return hook.apply(self, fnArgs);
  });
  return;
}

// run hooks async
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.indexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>indexOf ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.indexOf)
- description and source-code
```javascript
function indexOf() { [native code] }
```
- example usage
```shell
...
 */
InstanceValidator.prototype._builtinValidators = function() {
  var self = this;

  // promisify all attribute invocations
  var validators = [];
  Utils._.forIn(this.modelInstance.rawAttributes, function(rawAttribute, field) {
if (self.options.skip.indexOf(field) >= 0) {
  return;
}

var value = self.modelInstance.dataValues[field];

if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
  // perform validations based on schema
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.join"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>join ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.join)
- description and source-code
```javascript
function join() { [native code] }
```
- example usage
```shell
...
 if (message) {
   this.message = message;

 // ... otherwise create a concatenated message out of existing errors.
 } else if (this.errors.length > 0 && this.errors[0].message) {
   this.message = this.errors.map(function(err) {
     return err.type + ': ' + err.message;
   }).join(',\n');
 }
};
util.inherits(error.ValidationError, error.BaseError);

/**
* Gets all validation error items for the path / field specified.
*
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.lastIndexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>lastIndexOf ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.lastIndexOf)
- description and source-code
```javascript
function lastIndexOf() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.map"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>map ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.map)
- description and source-code
```javascript
function map() { [native code] }
```
- example usage
```shell
...

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
};
util.inherits(error.ValidationError, error.BaseError);

/**
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.pop"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>pop ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.pop)
- description and source-code
```javascript
function pop() { [native code] }
```
- example usage
```shell
...
  runHooks: function(hooks) {
var self = this
  , fn
  , fnArgs = Utils.sliceArgs(arguments, 1)
  , hookType;

if (typeof fnArgs[fnArgs.length - 1] === 'function') {
  fn = fnArgs.pop();
}

if (typeof hooks === 'string') {
  hookType = hooks;
  hooks = this.options.hooks[hookType] || [];
  if (!Array.isArray(hooks)) hooks = hooks === undefined ? [] : [hooks];
  if (this.sequelize) hooks = hooks.concat(this.sequelize.options.hooks[hookType] || []);
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.push"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>push ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.push)
- description and source-code
```javascript
function push() { [native code] }
```
- example usage
```shell
...
*
* @param {string} path The path to be checked for error items
* @returns {Array} Validation error items for the specified path
*/
error.ValidationError.prototype.get = function(path) {
 return this.errors.reduce(function(reduced, error) {
   if (error.path === path) {
     reduced.push(error);
   }
   return reduced;
 }, []);
};

/**
* A base class for all database related errors.
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>reduce ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.reduce)
- description and source-code
```javascript
function reduce() { [native code] }
```
- example usage
```shell
...
/**
 * Gets all validation error items for the path / field specified.
 *
 * @param {string} path The path to be checked for error items
 * @returns {Array} Validation error items for the specified path
 */
error.ValidationError.prototype.get = function(path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
};
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.reduceRight"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>reduceRight ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.reduceRight)
- description and source-code
```javascript
function reduceRight() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.reverse"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>reverse ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.reverse)
- description and source-code
```javascript
function reverse() { [native code] }
```
- example usage
```shell
...
    });

    sorter.add(tableName, deps);
  });

  sorted = sorter.sort();
  if (options.reverse) {
    sorted = sorted.reverse();
  }
  sorted.forEach(function(name) {
    iterator(models[name], name);
  });
};

module.exports = ModelManager;
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.shift"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>shift ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.shift)
- description and source-code
```javascript
function shift() { [native code] }
```
- example usage
```shell
...
values = [].concat(values);

return sql.replace(/\?/g, function(match) {
  if (!values.length) {
    return match;
  }

  return SqlString.escape(values.shift(), timeZone, dialect, true);
});
};

SqlString.formatNamedParameters = function(sql, values, timeZone, dialect) {
return sql.replace(/\:+(?!\d)(\w+)/g, function(value, key) {
  if ('postgres' === dialect && '::' === value.slice(0, 2)) {
    return value;
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.slice"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>slice ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.slice)
- description and source-code
```javascript
function slice() { [native code] }
```
- example usage
```shell
...
     validatorArgs = [validatorArgs, field];
   } else if (isLocalizedValidator || validatorType === 'isIP') {
     validatorArgs = [];
   } else {
     validatorArgs = [validatorArgs];
   }
 } else {
   validatorArgs = validatorArgs.slice(0);
 }
 return validatorArgs;
};

/**
* Will validate a single field against its schema definition (isnull).
*
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.some"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>some ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.some)
- description and source-code
```javascript
function some() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.sort"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>sort ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.sort)
- description and source-code
```javascript
function sort() { [native code] }
```
- example usage
```shell
...
    deps = deps.filter(function(dep) {
      return tableName !== dep;
    });

    sorter.add(tableName, deps);
  });

  sorted = sorter.sort();
  if (options.reverse) {
    sorted = sorted.reverse();
  }
  sorted.forEach(function(name) {
    iterator(models[name], name);
  });
};
...
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>toString ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.toString)
- description and source-code
```javascript
toString = function () {
    var indent = Array(level * 4 + 1).join(" ");
    var ret = "\n" + indent + "AggregateError of:" + "\n";
    level++;
    indent = Array(level * 4 + 1).join(" ");
    for (var i = 0; i < this.length; ++i) {
        var str = this[i] === this ? "[Circular AggregateError]" : this[i] + "";
        var lines = str.split("\n");
        for (var j = 0; j < lines.length; ++j) {
            lines[j] = indent + lines[j];
        }
        str = lines.join("\n");
        ret += str + "\n";
    }
    level--;
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.AggregateError.prototype.unshift"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.AggregateError.prototype.</span>unshift ()](#apidoc.element.sequelize.Promise.AggregateError.prototype.unshift)
- description and source-code
```javascript
function unshift() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.CancellationError"></a>[module sequelize.Promise.CancellationError](#apidoc.module.sequelize.Promise.CancellationError)

#### <a name="apidoc.element.sequelize.Promise.CancellationError.CancellationError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>CancellationError (message)](#apidoc.element.sequelize.Promise.CancellationError.CancellationError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.CancellationError.prototype"></a>[module sequelize.Promise.CancellationError.prototype](#apidoc.module.sequelize.Promise.CancellationError.prototype)

#### <a name="apidoc.element.sequelize.Promise.CancellationError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.CancellationError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.CancellationError.prototype.constructor)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.OperationalError"></a>[module sequelize.Promise.OperationalError](#apidoc.module.sequelize.Promise.OperationalError)

#### <a name="apidoc.element.sequelize.Promise.OperationalError.OperationalError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>OperationalError (message)](#apidoc.element.sequelize.Promise.OperationalError.OperationalError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.OperationalError.prototype"></a>[module sequelize.Promise.OperationalError.prototype](#apidoc.module.sequelize.Promise.OperationalError.prototype)

#### <a name="apidoc.element.sequelize.Promise.OperationalError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.OperationalError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.OperationalError.prototype.constructor)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.PromiseInspection"></a>[module sequelize.Promise.PromiseInspection](#apidoc.module.sequelize.Promise.PromiseInspection)

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.PromiseInspection"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>PromiseInspection (promise)](#apidoc.element.sequelize.Promise.PromiseInspection.PromiseInspection)
- description and source-code
```javascript
function PromiseInspection(promise) {
    if (promise !== undefined) {
        promise = promise._target();
        this._bitField = promise._bitField;
        this._settledValueField = promise._isFateSealed()
            ? promise._settledValue() : undefined;
    }
    else {
        this._bitField = 0;
        this._settledValueField = undefined;
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.PromiseInspection.prototype"></a>[module sequelize.Promise.PromiseInspection.prototype](#apidoc.module.sequelize.Promise.PromiseInspection.prototype)

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype._settledValue"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>_settledValue ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype._settledValue)
- description and source-code
```javascript
_settledValue = function () {
    return this._settledValueField;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.error"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>error ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.error)
- description and source-code
```javascript
error = function () {
    if (!this.isRejected()) {
        throw new TypeError("cannot get rejection reason of a non-rejected promise\u000a\u000a    See http://goo.gl/MqrFmX\u000a
");
    }
    return this._settledValue();
}
```
- example usage
```shell
...
* @param {Array.<Promise.PromiseInspection>} Promise inspection objects.
* @private
*/
InstanceValidator.prototype._handleReflectedResult = function(field, promiseInspections) {
 var self = this;
 promiseInspections.forEach(function(promiseInspection) {
   if (promiseInspection.isRejected()) {
     var rejection = promiseInspection.error();
     self._pushError(true, field, rejection);
   }
 });
};

/**
* Signs all errors retaining the original.
...
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.isCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isCancelled ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isCancelled)
- description and source-code
```javascript
isCancelled = function () {
    return (this._bitField & 8454144) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.isFulfilled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isFulfilled ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isFulfilled)
- description and source-code
```javascript
isFulfilled = function () {
    return (this._bitField & 33554432) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.isPending"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isPending ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isPending)
- description and source-code
```javascript
isPending = function () {
    return (this._bitField & 50397184) === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.isRejected"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isRejected ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isRejected)
- description and source-code
```javascript
isRejected = function () {
    return (this._bitField & 16777216) !== 0;
}
```
- example usage
```shell
...
 * @param {string} field The attribute name.
 * @param {Array.<Promise.PromiseInspection>} Promise inspection objects.
 * @private
 */
InstanceValidator.prototype._handleReflectedResult = function(field, promiseInspections) {
  var self = this;
  promiseInspections.forEach(function(promiseInspection) {
    if (promiseInspection.isRejected()) {
      var rejection = promiseInspection.error();
      self._pushError(true, field, rejection);
    }
  });
};

/**
...
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.isResolved"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>isResolved ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.isResolved)
- description and source-code
```javascript
isResolved = function () {
    return (this._bitField & 50331648) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.reason"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>reason ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.reason)
- description and source-code
```javascript
reason = function () {
    if (!this.isRejected()) {
        throw new TypeError("cannot get rejection reason of a non-rejected promise\u000a\u000a    See http://goo.gl/MqrFmX\u000a
");
    }
    return this._settledValue();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.PromiseInspection.prototype.value"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.PromiseInspection.prototype.</span>value ()](#apidoc.element.sequelize.Promise.PromiseInspection.prototype.value)
- description and source-code
```javascript
value = function () {
    if (!this.isFulfilled()) {
        throw new TypeError("cannot get fulfillment value of a non-fulfilled promise\u000a\u000a    See http://goo.gl/MqrFmX\u000a
");
    }
    return this._settledValue();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.TimeoutError"></a>[module sequelize.Promise.TimeoutError](#apidoc.module.sequelize.Promise.TimeoutError)

#### <a name="apidoc.element.sequelize.Promise.TimeoutError.TimeoutError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>TimeoutError (message)](#apidoc.element.sequelize.Promise.TimeoutError.TimeoutError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.TimeoutError.prototype"></a>[module sequelize.Promise.TimeoutError.prototype](#apidoc.module.sequelize.Promise.TimeoutError.prototype)

#### <a name="apidoc.element.sequelize.Promise.TimeoutError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.TimeoutError.prototype.</span>constructor (message)](#apidoc.element.sequelize.Promise.TimeoutError.prototype.constructor)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise._SomePromiseArray"></a>[module sequelize.Promise._SomePromiseArray](#apidoc.module.sequelize.Promise._SomePromiseArray)

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray._SomePromiseArray"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>_SomePromiseArray (values)](#apidoc.element.sequelize.Promise._SomePromiseArray._SomePromiseArray)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise._SomePromiseArray.prototype"></a>[module sequelize.Promise._SomePromiseArray.prototype](#apidoc.module.sequelize.Promise._SomePromiseArray.prototype)

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._addFulfilled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_addFulfilled (value)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._addFulfilled)
- description and source-code
```javascript
_addFulfilled = function (value) {
    this._values[this._totalResolved++] = value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._addRejected"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_addRejected (reason)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._addRejected)
- description and source-code
```javascript
_addRejected = function (reason) {
    this._values.push(reason);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._canPossiblyFulfill"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_canPossiblyFulfill ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._canPossiblyFulfill)
- description and source-code
```javascript
_canPossiblyFulfill = function () {
    return this.length() - this._rejected();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._checkOutcome"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_checkOutcome ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._checkOutcome)
- description and source-code
```javascript
_checkOutcome = function () {
    if (this.howMany() > this._canPossiblyFulfill()) {
        var e = new AggregateError();
        for (var i = this.length(); i < this._values.length; ++i) {
            if (this._values[i] !== CANCELLATION) {
                e.push(this._values[i]);
            }
        }
        if (e.length > 0) {
            this._reject(e);
        } else {
            this._cancel();
        }
        return true;
    }
    return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._fulfilled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_fulfilled ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._fulfilled)
- description and source-code
```javascript
_fulfilled = function () {
    return this._totalResolved;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._getRangeError"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_getRangeError (count)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._getRangeError)
- description and source-code
```javascript
_getRangeError = function (count) {
    var message = "Input array must contain at least " +
            this._howMany + " items but contains only " + count + " items";
    return new RangeError(message);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._init"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_init ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._init)
- description and source-code
```javascript
_init = function () {
    if (!this._initialized) {
        return;
    }
    if (this._howMany === 0) {
        this._resolve([]);
        return;
    }
    this._init$(undefined, -5);
    var isArrayResolved = isArray(this._values);
    if (!this._isResolved() &&
        isArrayResolved &&
        this._howMany > this._canPossiblyFulfill()) {
        this._reject(this._getRangeError(this.length()));
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_promiseCancelled ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseCancelled)
- description and source-code
```javascript
_promiseCancelled = function () {
    if (this._values instanceof Promise || this._values == null) {
        return this._cancel();
    }
    this._addRejected(CANCELLATION);
    return this._checkOutcome();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseFulfilled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_promiseFulfilled (value)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseFulfilled)
- description and source-code
```javascript
_promiseFulfilled = function (value) {
    this._addFulfilled(value);
    if (this._fulfilled() === this.howMany()) {
        this._values.length = this.howMany();
        if (this.howMany() === 1 && this._unwrap) {
            this._resolve(this._values[0]);
        } else {
            this._resolve(this._values);
        }
        return true;
    }
    return false;

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseRejected"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_promiseRejected (reason)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._promiseRejected)
- description and source-code
```javascript
_promiseRejected = function (reason) {
    this._addRejected(reason);
    return this._checkOutcome();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._rejected"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_rejected ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._rejected)
- description and source-code
```javascript
_rejected = function () {
    return this._values.length - this.length();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype._resolveEmptyArray"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>_resolveEmptyArray ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype._resolveEmptyArray)
- description and source-code
```javascript
_resolveEmptyArray = function () {
    this._reject(this._getRangeError(0));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype.constructor"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>constructor (values)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.constructor)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype.howMany"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>howMany ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.howMany)
- description and source-code
```javascript
howMany = function () {
    return this._howMany;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype.init"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>init ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.init)
- description and source-code
```javascript
init = function () {
    this._initialized = true;
    this._init();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype.setHowMany"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>setHowMany (count)](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.setHowMany)
- description and source-code
```javascript
setHowMany = function (count) {
    this._howMany = count;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise._SomePromiseArray.prototype.setUnwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise._SomePromiseArray.prototype.</span>setUnwrap ()](#apidoc.element.sequelize.Promise._SomePromiseArray.prototype.setUnwrap)
- description and source-code
```javascript
setUnwrap = function () {
    this._unwrap = true;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.coroutine"></a>[module sequelize.Promise.coroutine](#apidoc.module.sequelize.Promise.coroutine)

#### <a name="apidoc.element.sequelize.Promise.coroutine.coroutine"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.sequelize.Promise.coroutine.coroutine)
- description and source-code
```javascript
coroutine = function (generatorFunction, options) {
    if (typeof generatorFunction !== "function") {
        throw new TypeError("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var yieldHandler = Object(options).yieldHandler;
    var PromiseSpawn$ = PromiseSpawn;
    var stack = new Error().stack;
    return function () {
        var generator = generatorFunction.apply(this, arguments);
        var spawn = new PromiseSpawn$(undefined, undefined, yieldHandler,
                                      stack);
        var ret = spawn.promise();
        spawn._generator = generator;
        spawn._promiseFulfilled(undefined);
        return ret;
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.coroutine.addYieldHandler"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.coroutine.</span>addYieldHandler (fn)](#apidoc.element.sequelize.Promise.coroutine.addYieldHandler)
- description and source-code
```javascript
addYieldHandler = function (fn) {
    if (typeof fn !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(fn));
    }
    yieldHandlers.push(fn);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.each"></a>[module sequelize.Promise.each](#apidoc.module.sequelize.Promise.each)

#### <a name="apidoc.element.sequelize.Promise.each.each"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>each ()](#apidoc.element.sequelize.Promise.each.each)
- description and source-code
```javascript
each = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  beforeConnection: 'beforeConnect'
};

var Hooks = {
  replaceHookAliases: function(hooks) {
    var realHookName;

    Utils._.each(hooks, function(hooksArray, name) {
      // Does an alias for this hook name exist?
      if (realHookName = hookAliases[name]) {
// Add the hooks to the actual hook
hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

// Delete the alias
delete hooks[name];
...
```

#### <a name="apidoc.element.sequelize.Promise.each.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.each.</span>__original (promises, fn)](#apidoc.element.sequelize.Promise.each.__original)
- description and source-code
```javascript
__original = function (promises, fn) {
    return PromiseReduce(promises, fn, INTERNAL, 0)
              ._then(promiseAllThis, undefined, undefined, promises, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.each.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.each.</span>__unwrap ()](#apidoc.element.sequelize.Promise.each.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.filter"></a>[module sequelize.Promise.filter](#apidoc.module.sequelize.Promise.filter)

#### <a name="apidoc.element.sequelize.Promise.filter.filter"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>filter ()](#apidoc.element.sequelize.Promise.filter.filter)
- description and source-code
```javascript
filter = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
  }

  return typeof hook === 'object' && hook.name !== name;
});
...
```

#### <a name="apidoc.element.sequelize.Promise.filter.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.filter.</span>__original (promises, fn, options)](#apidoc.element.sequelize.Promise.filter.__original)
- description and source-code
```javascript
__original = function (promises, fn, options) {
    return PromiseMap(promises, fn, options, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.filter.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.filter.</span>__unwrap ()](#apidoc.element.sequelize.Promise.filter.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.join"></a>[module sequelize.Promise.join](#apidoc.module.sequelize.Promise.join)

#### <a name="apidoc.element.sequelize.Promise.join.join"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>join ()](#apidoc.element.sequelize.Promise.join.join)
- description and source-code
```javascript
join = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
 if (message) {
   this.message = message;

 // ... otherwise create a concatenated message out of existing errors.
 } else if (this.errors.length > 0 && this.errors[0].message) {
   this.message = this.errors.map(function(err) {
     return err.type + ': ' + err.message;
   }).join(',\n');
 }
};
util.inherits(error.ValidationError, error.BaseError);

/**
* Gets all validation error items for the path / field specified.
*
...
```

#### <a name="apidoc.element.sequelize.Promise.join.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.join.</span>__original ()](#apidoc.element.sequelize.Promise.join.__original)
- description and source-code
```javascript
__original = function () {
    var last = arguments.length - 1;
    var fn;
    if (last > 0 && typeof arguments[last] === "function") {
        fn = arguments[last];
        if (!false) {
            if (last <= 8 && canEvaluate) {
                var ret = new Promise(INTERNAL);
                ret._captureStackTrace();
                var HolderClass = holderClasses[last - 1];
                var holder = new HolderClass(fn);
                var callbacks = thenCallbacks;

                for (var i = 0; i < last; ++i) {
                    var maybePromise = tryConvertToPromise(arguments[i], ret);
                    if (maybePromise instanceof Promise) {
                        maybePromise = maybePromise._target();
                        var bitField = maybePromise._bitField;
                        ;
                        if (((bitField & 50397184) === 0)) {
                            maybePromise._then(callbacks[i], reject,
                                               undefined, ret, holder);
                            promiseSetters[i](maybePromise, holder);
                            holder.asyncNeeded = false;
                        } else if (((bitField & 33554432) !== 0)) {
                            callbacks[i].call(ret,
                                              maybePromise._value(), holder);
                        } else if (((bitField & 16777216) !== 0)) {
                            ret._reject(maybePromise._reason());
                        } else {
                            ret._cancel();
                        }
                    } else {
                        callbacks[i].call(ret, maybePromise, holder);
                    }
                }

                if (!ret._isFateSealed()) {
                    if (holder.asyncNeeded) {
                        var domain = getDomain();
                        if (domain !== null) {
                            holder.fn = util.domainBind(domain, holder.fn);
                        }
                    }
                    ret._setAsyncGuaranteed();
                    ret._setOnCancel(holder);
                }
                return ret;
            }
        }
    }
    var $_len = arguments.length;var args = new Array($_len); for(var $_i = 0; $_i < $_len; ++$_i) {args[$_i] = arguments[$_i];};
    if (fn) args.pop();
    var ret = new PromiseArray(args).promise();
    return fn !== undefined ? ret.spread(fn) : ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.join.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.join.</span>__unwrap ()](#apidoc.element.sequelize.Promise.join.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.map"></a>[module sequelize.Promise.map](#apidoc.module.sequelize.Promise.map)

#### <a name="apidoc.element.sequelize.Promise.map.map"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>map ()](#apidoc.element.sequelize.Promise.map.map)
- description and source-code
```javascript
map = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
};
util.inherits(error.ValidationError, error.BaseError);

/**
...
```

#### <a name="apidoc.element.sequelize.Promise.map.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.map.</span>__original (promises, fn, options, _filter)](#apidoc.element.sequelize.Promise.map.__original)
- description and source-code
```javascript
__original = function (promises, fn, options, _filter) {
    return map(promises, fn, options, _filter);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.map.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.map.</span>__unwrap ()](#apidoc.element.sequelize.Promise.map.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.mapSeries"></a>[module sequelize.Promise.mapSeries](#apidoc.module.sequelize.Promise.mapSeries)

#### <a name="apidoc.element.sequelize.Promise.mapSeries.mapSeries"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>mapSeries ()](#apidoc.element.sequelize.Promise.mapSeries.mapSeries)
- description and source-code
```javascript
mapSeries = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.mapSeries.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.mapSeries.</span>__original (promises, fn)](#apidoc.element.sequelize.Promise.mapSeries.__original)
- description and source-code
```javascript
function PromiseMapSeries(promises, fn) {
    return PromiseReduce(promises, fn, INTERNAL, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.mapSeries.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.mapSeries.</span>__unwrap ()](#apidoc.element.sequelize.Promise.mapSeries.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype"></a>[module sequelize.Promise.prototype](#apidoc.module.sequelize.Promise.prototype)

#### <a name="apidoc.element.sequelize.Promise.prototype.__isCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>__isCancelled ()](#apidoc.element.sequelize.Promise.prototype.__isCancelled)
- description and source-code
```javascript
__isCancelled = function () {
    return (this._bitField & 65536) === 65536;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._addCallbacks"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_addCallbacks ( fulfill, reject, promise, receiver, domain )](#apidoc.element.sequelize.Promise.prototype._addCallbacks)
- description and source-code
```javascript
_addCallbacks = function ( fulfill, reject, promise, receiver, domain ) {
    var index = this._length();

    if (index >= 65535 - 4) {
        index = 0;
        this._setLength(0);
    }

    if (index === 0) {
        this._promise0 = promise;
        this._receiver0 = receiver;
        if (typeof fulfill === "function") {
            this._fulfillmentHandler0 =
                domain === null ? fulfill : util.domainBind(domain, fulfill);
        }
        if (typeof reject === "function") {
            this._rejectionHandler0 =
                domain === null ? reject : util.domainBind(domain, reject);
        }
    } else {
        var base = index * 4 - 4;
        this[base + 2] = promise;
        this[base + 3] = receiver;
        if (typeof fulfill === "function") {
            this[base + 0] =
                domain === null ? fulfill : util.domainBind(domain, fulfill);
        }
        if (typeof reject === "function") {
            this[base + 1] =
                domain === null ? reject : util.domainBind(domain, reject);
        }
    }
    this._setLength(index + 1);
    return index;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._attachCancellationCallback"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_attachCancellationCallback (onCancel)](#apidoc.element.sequelize.Promise.prototype._attachCancellationCallback)
- description and source-code
```javascript
_attachCancellationCallback = function (onCancel) {
    ;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._attachExtraTrace"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_attachExtraTrace ()](#apidoc.element.sequelize.Promise.prototype._attachExtraTrace)
- description and source-code
```javascript
_attachExtraTrace = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._boundValue"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_boundValue ()](#apidoc.element.sequelize.Promise.prototype._boundValue)
- description and source-code
```javascript
_boundValue = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._branchHasCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_branchHasCancelled ()](#apidoc.element.sequelize.Promise.prototype._branchHasCancelled)
- description and source-code
```javascript
_branchHasCancelled = function () {
    this._branchesRemainingToCancel--;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._cancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancel ()](#apidoc.element.sequelize.Promise.prototype._cancel)
- description and source-code
```javascript
_cancel = function () {
    if (!this._isCancellable()) return;
    this._setCancelled();
    async.invoke(this._cancelPromises, this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._cancelBranched"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancelBranched ()](#apidoc.element.sequelize.Promise.prototype._cancelBranched)
- description and source-code
```javascript
_cancelBranched = function () {
    if (this._enoughBranchesHaveCancelled()) {
        this._cancel();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._cancelBy"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancelBy (canceller)](#apidoc.element.sequelize.Promise.prototype._cancelBy)
- description and source-code
```javascript
_cancelBy = function (canceller) {
    if (canceller === this) {
        this._branchesRemainingToCancel = 0;
        this._invokeOnCancel();
        return true;
    } else {
        this._branchHasCancelled();
        if (this._enoughBranchesHaveCancelled()) {
            this._invokeOnCancel();
            return true;
        }
    }
    return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._cancelPromises"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_cancelPromises ()](#apidoc.element.sequelize.Promise.prototype._cancelPromises)
- description and source-code
```javascript
_cancelPromises = function () {
    if (this._length() > 0) this._settlePromises();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._captureStackTrace"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_captureStackTrace ()](#apidoc.element.sequelize.Promise.prototype._captureStackTrace)
- description and source-code
```javascript
_captureStackTrace = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._clearCallbackDataAtIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_clearCallbackDataAtIndex (index)](#apidoc.element.sequelize.Promise.prototype._clearCallbackDataAtIndex)
- description and source-code
```javascript
_clearCallbackDataAtIndex = function (index) {
    var base = index * 4 - 4;
    this[base + 2] =
    this[base + 3] =
    this[base + 0] =
    this[base + 1] = undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._clearCancellationData"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_clearCancellationData ()](#apidoc.element.sequelize.Promise.prototype._clearCancellationData)
- description and source-code
```javascript
_clearCancellationData = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._doInvokeOnCancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_doInvokeOnCancel (onCancelCallback, internalOnly)](#apidoc.element.sequelize.Promise.prototype._doInvokeOnCancel)
- description and source-code
```javascript
_doInvokeOnCancel = function (onCancelCallback, internalOnly) {
    if (util.isArray(onCancelCallback)) {
        for (var i = 0; i < onCancelCallback.length; ++i) {
            this._doInvokeOnCancel(onCancelCallback[i], internalOnly);
        }
    } else if (onCancelCallback !== undefined) {
        if (typeof onCancelCallback === "function") {
            if (!internalOnly) {
                var e = tryCatch(onCancelCallback).call(this._boundValue());
                if (e === errorObj) {
                    this._attachExtraTrace(e.e);
                    async.throwLater(e.e);
                }
            }
        } else {
            onCancelCallback._resultCancelled(this);
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._enoughBranchesHaveCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_enoughBranchesHaveCancelled ()](#apidoc.element.sequelize.Promise.prototype._enoughBranchesHaveCancelled)
- description and source-code
```javascript
_enoughBranchesHaveCancelled = function () {
    return this._branchesRemainingToCancel === undefined ||
           this._branchesRemainingToCancel <= 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._ensurePossibleRejectionHandled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_ensurePossibleRejectionHandled ()](#apidoc.element.sequelize.Promise.prototype._ensurePossibleRejectionHandled)
- description and source-code
```javascript
_ensurePossibleRejectionHandled = function () {
    if ((this._bitField & 524288) !== 0) return;
    this._setRejectionIsUnhandled();
    async.invokeLater(this._notifyUnhandledRejection, this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._execute"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_execute (executor, resolve, reject)](#apidoc.element.sequelize.Promise.prototype._execute)
- description and source-code
```javascript
_execute = function (executor, resolve, reject) {
    try {
        executor(resolve, reject);
    } catch (e) {
        return e;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._fireEvent"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fireEvent ()](#apidoc.element.sequelize.Promise.prototype._fireEvent)
- description and source-code
```javascript
function defaultFireEvent() { return false; }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._followee"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_followee ()](#apidoc.element.sequelize.Promise.prototype._followee)
- description and source-code
```javascript
_followee = function () {
    return this._rejectionHandler0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._fulfill"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fulfill (value)](#apidoc.element.sequelize.Promise.prototype._fulfill)
- description and source-code
```javascript
_fulfill = function (value) {
    var bitField = this._bitField;
    if (((bitField & 117506048) >>> 16)) return;
    if (value === this) {
        var err = makeSelfResolutionError();
        this._attachExtraTrace(err);
        return this._reject(err);
    }
    this._setFulfilled();
    this._rejectionHandler0 = value;

    if ((bitField & 65535) > 0) {
        if (((bitField & 134217728) !== 0)) {
            this._settlePromises();
        } else {
            async.settlePromises(this);
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._fulfillPromises"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fulfillPromises (len, value)](#apidoc.element.sequelize.Promise.prototype._fulfillPromises)
- description and source-code
```javascript
_fulfillPromises = function (len, value) {
    for (var i = 1; i < len; i++) {
        var handler = this._fulfillmentHandlerAt(i);
        var promise = this._promiseAt(i);
        var receiver = this._receiverAt(i);
        this._clearCallbackDataAtIndex(i);
        this._settlePromise(promise, handler, receiver, value);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._fulfillmentHandlerAt"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_fulfillmentHandlerAt (index)](#apidoc.element.sequelize.Promise.prototype._fulfillmentHandlerAt)
- description and source-code
```javascript
_fulfillmentHandlerAt = function (index) {
    return this[
            index * 4 - 4 + 0];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._getDisposer"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_getDisposer ()](#apidoc.element.sequelize.Promise.prototype._getDisposer)
- description and source-code
```javascript
_getDisposer = function () {
    return this._disposer;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._invokeInternalOnCancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_invokeInternalOnCancel ()](#apidoc.element.sequelize.Promise.prototype._invokeInternalOnCancel)
- description and source-code
```javascript
_invokeInternalOnCancel = function () {
    if (this._isCancellable()) {
        this._doInvokeOnCancel(this._onCancel(), true);
        this._unsetOnCancel();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._invokeOnCancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_invokeOnCancel ()](#apidoc.element.sequelize.Promise.prototype._invokeOnCancel)
- description and source-code
```javascript
_invokeOnCancel = function () {
    var onCancelCallback = this._onCancel();
    this._unsetOnCancel();
    async.invoke(this._doInvokeOnCancel, this, onCancelCallback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isBound"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isBound ()](#apidoc.element.sequelize.Promise.prototype._isBound)
- description and source-code
```javascript
_isBound = function () {
    return (this._bitField & 2097152) === 2097152;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isCancellable"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isCancellable ()](#apidoc.element.sequelize.Promise.prototype._isCancellable)
- description and source-code
```javascript
_isCancellable = function () {
    return this.isPending() && !this._isCancelled();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isCancelled ()](#apidoc.element.sequelize.Promise.prototype._isCancelled)
- description and source-code
```javascript
_isCancelled = function () {
    return this._target().__isCancelled();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isDisposable"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isDisposable ()](#apidoc.element.sequelize.Promise.prototype._isDisposable)
- description and source-code
```javascript
_isDisposable = function () {
    return (this._bitField & 131072) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isFateSealed"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isFateSealed ()](#apidoc.element.sequelize.Promise.prototype._isFateSealed)
- description and source-code
```javascript
_isFateSealed = function () {
    return (this._bitField & 117506048) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isFinal"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isFinal ()](#apidoc.element.sequelize.Promise.prototype._isFinal)
- description and source-code
```javascript
_isFinal = function () {
    return (this._bitField & 4194304) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isFollowing"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isFollowing ()](#apidoc.element.sequelize.Promise.prototype._isFollowing)
- description and source-code
```javascript
_isFollowing = function () {
    return (this._bitField & 67108864) === 67108864;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isRejectionUnhandled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isRejectionUnhandled ()](#apidoc.element.sequelize.Promise.prototype._isRejectionUnhandled)
- description and source-code
```javascript
_isRejectionUnhandled = function () {
    return (this._bitField & 1048576) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._isUnhandledRejectionNotified"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_isUnhandledRejectionNotified ()](#apidoc.element.sequelize.Promise.prototype._isUnhandledRejectionNotified)
- description and source-code
```javascript
_isUnhandledRejectionNotified = function () {
    return (this._bitField & 262144) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._length"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_length ()](#apidoc.element.sequelize.Promise.prototype._length)
- description and source-code
```javascript
_length = function () {
    return this._bitField & 65535;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._migrateCallback0"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_migrateCallback0 (follower)](#apidoc.element.sequelize.Promise.prototype._migrateCallback0)
- description and source-code
```javascript
_migrateCallback0 = function (follower) {
    var bitField = follower._bitField;
    var fulfill = follower._fulfillmentHandler0;
    var reject = follower._rejectionHandler0;
    var promise = follower._promise0;
    var receiver = follower._receiverAt(0);
    if (receiver === undefined) receiver = UNDEFINED_BINDING;
    this._addCallbacks(fulfill, reject, promise, receiver, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._migrateCallbackAt"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_migrateCallbackAt (follower, index)](#apidoc.element.sequelize.Promise.prototype._migrateCallbackAt)
- description and source-code
```javascript
_migrateCallbackAt = function (follower, index) {
    var fulfill = follower._fulfillmentHandlerAt(index);
    var reject = follower._rejectionHandlerAt(index);
    var promise = follower._promiseAt(index);
    var receiver = follower._receiverAt(index);
    if (receiver === undefined) receiver = UNDEFINED_BINDING;
    this._addCallbacks(fulfill, reject, promise, receiver, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._notifyUnhandledRejection"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_notifyUnhandledRejection ()](#apidoc.element.sequelize.Promise.prototype._notifyUnhandledRejection)
- description and source-code
```javascript
_notifyUnhandledRejection = function () {
    if (this._isRejectionUnhandled()) {
        var reason = this._settledValue();
        this._setUnhandledRejectionIsNotified();
        fireRejectionEvent("unhandledRejection",
                                      possiblyUnhandledRejection, reason, this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._notifyUnhandledRejectionIsHandled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_notifyUnhandledRejectionIsHandled ()](#apidoc.element.sequelize.Promise.prototype._notifyUnhandledRejectionIsHandled)
- description and source-code
```javascript
_notifyUnhandledRejectionIsHandled = function () {
    fireRejectionEvent("rejectionHandled",
                                  unhandledRejectionHandled, undefined, this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._onCancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_onCancel ()](#apidoc.element.sequelize.Promise.prototype._onCancel)
- description and source-code
```javascript
_onCancel = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._passThrough"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_passThrough (handler, type, success, fail)](#apidoc.element.sequelize.Promise.prototype._passThrough)
- description and source-code
```javascript
_passThrough = function (handler, type, success, fail) {
    if (typeof handler !== "function") return this.then();
    return this._then(success,
                      fail,
                      undefined,
                      new PassThroughHandlerContext(this, type, handler),
                      undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._peekContext"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_peekContext ()](#apidoc.element.sequelize.Promise.prototype._peekContext)
- description and source-code
```javascript
_peekContext = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._popContext"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_popContext ()](#apidoc.element.sequelize.Promise.prototype._popContext)
- description and source-code
```javascript
_popContext = function () {return null;}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._promiseAt"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_promiseAt (index)](#apidoc.element.sequelize.Promise.prototype._promiseAt)
- description and source-code
```javascript
_promiseAt = function (index) {
    return this[
            index * 4 - 4 + 2];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._promiseCreated"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_promiseCreated ()](#apidoc.element.sequelize.Promise.prototype._promiseCreated)
- description and source-code
```javascript
_promiseCreated = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._propagateFrom"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_propagateFrom (parent, flags)](#apidoc.element.sequelize.Promise.prototype._propagateFrom)
- description and source-code
```javascript
_propagateFrom = function (parent, flags) {
    ;
    ;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._proxy"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_proxy (proxyable, arg)](#apidoc.element.sequelize.Promise.prototype._proxy)
- description and source-code
```javascript
_proxy = function (proxyable, arg) {
    this._addCallbacks(undefined, undefined, arg, proxyable, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._pushContext"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_pushContext ()](#apidoc.element.sequelize.Promise.prototype._pushContext)
- description and source-code
```javascript
_pushContext = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._reason"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_reason ()](#apidoc.element.sequelize.Promise.prototype._reason)
- description and source-code
```javascript
_reason = function () {
    this._unsetRejectionIsUnhandled();
    return this._settledValue();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._receiverAt"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_receiverAt (index)](#apidoc.element.sequelize.Promise.prototype._receiverAt)
- description and source-code
```javascript
_receiverAt = function (index) {
    var ret = index === 0 ? this._receiver0 : this[
            index * 4 - 4 + 3];
    if (ret === UNDEFINED_BINDING) {
        return undefined;
    } else if (ret === undefined && this._isBound()) {
        return this._boundValue();
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._reject"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_reject (reason)](#apidoc.element.sequelize.Promise.prototype._reject)
- description and source-code
```javascript
_reject = function (reason) {
    var bitField = this._bitField;
    if (((bitField & 117506048) >>> 16)) return;
    this._setRejected();
    this._fulfillmentHandler0 = reason;

    if (this._isFinal()) {
        return async.fatalError(reason, util.isNode);
    }

    if ((bitField & 65535) > 0) {
        async.settlePromises(this);
    } else {
        this._ensurePossibleRejectionHandled();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._rejectCallback"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_rejectCallback (reason, synchronous, ignoreNonErrorWarnings)](#apidoc.element.sequelize.Promise.prototype._rejectCallback)
- description and source-code
```javascript
_rejectCallback = function (reason, synchronous, ignoreNonErrorWarnings) {
    var trace = util.ensureErrorObject(reason);
    var hasStack = trace === reason;
    if (!hasStack && !ignoreNonErrorWarnings && debug.warnings()) {
        var message = "a promise was rejected with a non-error: " +
            util.classString(reason);
        this._warn(message, true);
    }
    this._attachExtraTrace(trace, synchronous ? hasStack : false);
    this._reject(reason);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._rejectPromises"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_rejectPromises (len, reason)](#apidoc.element.sequelize.Promise.prototype._rejectPromises)
- description and source-code
```javascript
_rejectPromises = function (len, reason) {
    for (var i = 1; i < len; i++) {
        var handler = this._rejectionHandlerAt(i);
        var promise = this._promiseAt(i);
        var receiver = this._receiverAt(i);
        this._clearCallbackDataAtIndex(i);
        this._settlePromise(promise, handler, receiver, reason);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._rejectionHandlerAt"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_rejectionHandlerAt (index)](#apidoc.element.sequelize.Promise.prototype._rejectionHandlerAt)
- description and source-code
```javascript
_rejectionHandlerAt = function (index) {
    return this[
            index * 4 - 4 + 1];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._resolveCallback"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resolveCallback (value, shouldBind)](#apidoc.element.sequelize.Promise.prototype._resolveCallback)
- description and source-code
```javascript
_resolveCallback = function (value, shouldBind) {
    if (((this._bitField & 117506048) !== 0)) return;
    if (value === this)
        return this._rejectCallback(makeSelfResolutionError(), false);
    var maybePromise = tryConvertToPromise(value, this);
    if (!(maybePromise instanceof Promise)) return this._fulfill(value);

    if (shouldBind) this._propagateFrom(maybePromise, 2);

    var promise = maybePromise._target();

    if (promise === this) {
        this._reject(makeSelfResolutionError());
        return;
    }

    var bitField = promise._bitField;
    if (((bitField & 50397184) === 0)) {
        var len = this._length();
        if (len > 0) promise._migrateCallback0(this);
        for (var i = 1; i < len; ++i) {
            promise._migrateCallbackAt(this, i);
        }
        this._setFollowing();
        this._setLength(0);
        this._setFollowee(promise);
    } else if (((bitField & 33554432) !== 0)) {
        this._fulfill(promise._value());
    } else if (((bitField & 16777216) !== 0)) {
        this._reject(promise._reason());
    } else {
        var reason = new CancellationError("late cancellation observer");
        promise._attachExtraTrace(reason);
        this._reject(reason);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._resolveFromExecutor"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resolveFromExecutor (executor)](#apidoc.element.sequelize.Promise.prototype._resolveFromExecutor)
- description and source-code
```javascript
_resolveFromExecutor = function (executor) {
    if (executor === INTERNAL) return;
    var promise = this;
    this._captureStackTrace();
    this._pushContext();
    var synchronous = true;
    var r = this._execute(executor, function(value) {
        promise._resolveCallback(value);
    }, function (reason) {
        promise._rejectCallback(reason, synchronous);
    });
    synchronous = false;
    this._popContext();

    if (r !== undefined) {
        promise._rejectCallback(r, true);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._resolveFromSyncValue"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resolveFromSyncValue (value)](#apidoc.element.sequelize.Promise.prototype._resolveFromSyncValue)
- description and source-code
```javascript
_resolveFromSyncValue = function (value) {
    if (value === util.errorObj) {
        this._rejectCallback(value.e, false);
    } else {
        this._resolveCallback(value, true);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._resultCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_resultCancelled ()](#apidoc.element.sequelize.Promise.prototype._resultCancelled)
- description and source-code
```javascript
_resultCancelled = function () {
    this.cancel();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._returnedNonUndefined"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_returnedNonUndefined ()](#apidoc.element.sequelize.Promise.prototype._returnedNonUndefined)
- description and source-code
```javascript
_returnedNonUndefined = function () {
    return (this._bitField & 268435456) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setAsyncGuaranteed"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setAsyncGuaranteed ()](#apidoc.element.sequelize.Promise.prototype._setAsyncGuaranteed)
- description and source-code
```javascript
_setAsyncGuaranteed = function () {
    if (async.hasCustomScheduler()) return;
    this._bitField = this._bitField | 134217728;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setBoundTo"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setBoundTo (obj)](#apidoc.element.sequelize.Promise.prototype._setBoundTo)
- description and source-code
```javascript
_setBoundTo = function (obj) {
    if (obj !== undefined) {
        this._bitField = this._bitField | 2097152;
        this._boundTo = obj;
    } else {
        this._bitField = this._bitField & (~2097152);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setCancelled ()](#apidoc.element.sequelize.Promise.prototype._setCancelled)
- description and source-code
```javascript
_setCancelled = function () {
    this._bitField = this._bitField | 65536;
    this._fireEvent("promiseCancelled", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setDisposable"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setDisposable (disposer)](#apidoc.element.sequelize.Promise.prototype._setDisposable)
- description and source-code
```javascript
_setDisposable = function (disposer) {
    this._bitField = this._bitField | 131072;
    this._disposer = disposer;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setFollowee"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setFollowee (promise)](#apidoc.element.sequelize.Promise.prototype._setFollowee)
- description and source-code
```javascript
_setFollowee = function (promise) {
    this._rejectionHandler0 = promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setFollowing"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setFollowing ()](#apidoc.element.sequelize.Promise.prototype._setFollowing)
- description and source-code
```javascript
_setFollowing = function () {
    this._bitField = this._bitField | 67108864;
    this._fireEvent("promiseResolved", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setFulfilled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setFulfilled ()](#apidoc.element.sequelize.Promise.prototype._setFulfilled)
- description and source-code
```javascript
_setFulfilled = function () {
    this._bitField = this._bitField | 33554432;
    this._fireEvent("promiseFulfilled", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setIsFinal"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setIsFinal ()](#apidoc.element.sequelize.Promise.prototype._setIsFinal)
- description and source-code
```javascript
_setIsFinal = function () {
    this._bitField = this._bitField | 4194304;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setLength"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setLength (len)](#apidoc.element.sequelize.Promise.prototype._setLength)
- description and source-code
```javascript
_setLength = function (len) {
    this._bitField = (this._bitField & -65536) |
        (len & 65535);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setOnCancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setOnCancel (handler)](#apidoc.element.sequelize.Promise.prototype._setOnCancel)
- description and source-code
```javascript
_setOnCancel = function (handler) { ; }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setRejected"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setRejected ()](#apidoc.element.sequelize.Promise.prototype._setRejected)
- description and source-code
```javascript
_setRejected = function () {
    this._bitField = this._bitField | 16777216;
    this._fireEvent("promiseRejected", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setRejectionIsUnhandled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setRejectionIsUnhandled ()](#apidoc.element.sequelize.Promise.prototype._setRejectionIsUnhandled)
- description and source-code
```javascript
_setRejectionIsUnhandled = function () {
    this._bitField = this._bitField | 1048576;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setReturnedNonUndefined"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setReturnedNonUndefined ()](#apidoc.element.sequelize.Promise.prototype._setReturnedNonUndefined)
- description and source-code
```javascript
_setReturnedNonUndefined = function () {
    this._bitField = this._bitField | 268435456;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setUnhandledRejectionIsNotified"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setUnhandledRejectionIsNotified ()](#apidoc.element.sequelize.Promise.prototype._setUnhandledRejectionIsNotified)
- description and source-code
```javascript
_setUnhandledRejectionIsNotified = function () {
    this._bitField = this._bitField | 262144;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._setWillBeCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_setWillBeCancelled ()](#apidoc.element.sequelize.Promise.prototype._setWillBeCancelled)
- description and source-code
```javascript
_setWillBeCancelled = function () {
    this._bitField = this._bitField | 8388608;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._settlePromise"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromise (promise, handler, receiver, value)](#apidoc.element.sequelize.Promise.prototype._settlePromise)
- description and source-code
```javascript
_settlePromise = function (promise, handler, receiver, value) {
    var isPromise = promise instanceof Promise;
    var bitField = this._bitField;
    var asyncGuaranteed = ((bitField & 134217728) !== 0);
    if (((bitField & 65536) !== 0)) {
        if (isPromise) promise._invokeInternalOnCancel();

        if (receiver instanceof PassThroughHandlerContext &&
            receiver.isFinallyHandler()) {
            receiver.cancelPromise = promise;
            if (tryCatch(handler).call(receiver, value) === errorObj) {
                promise._reject(errorObj.e);
            }
        } else if (handler === reflectHandler) {
            promise._fulfill(reflectHandler.call(receiver));
        } else if (receiver instanceof Proxyable) {
            receiver._promiseCancelled(promise);
        } else if (isPromise || promise instanceof PromiseArray) {
            promise._cancel();
        } else {
            receiver.cancel();
        }
    } else if (typeof handler === "function") {
        if (!isPromise) {
            handler.call(receiver, value, promise);
        } else {
            if (asyncGuaranteed) promise._setAsyncGuaranteed();
            this._settlePromiseFromHandler(handler, receiver, value, promise);
        }
    } else if (receiver instanceof Proxyable) {
        if (!receiver._isResolved()) {
            if (((bitField & 33554432) !== 0)) {
                receiver._promiseFulfilled(value, promise);
            } else {
                receiver._promiseRejected(value, promise);
            }
        }
    } else if (isPromise) {
        if (asyncGuaranteed) promise._setAsyncGuaranteed();
        if (((bitField & 33554432) !== 0)) {
            promise._fulfill(value);
        } else {
            promise._reject(value);
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._settlePromise0"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromise0 (handler, value, bitField)](#apidoc.element.sequelize.Promise.prototype._settlePromise0)
- description and source-code
```javascript
_settlePromise0 = function (handler, value, bitField) {
    var promise = this._promise0;
    var receiver = this._receiverAt(0);
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._settlePromise(promise, handler, receiver, value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._settlePromiseCtx"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromiseCtx (ctx)](#apidoc.element.sequelize.Promise.prototype._settlePromiseCtx)
- description and source-code
```javascript
_settlePromiseCtx = function (ctx) {
    this._settlePromise(ctx.promise, ctx.handler, ctx.receiver, ctx.value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._settlePromiseFromHandler"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromiseFromHandler ( handler, receiver, value, promise )](#apidoc.element.sequelize.Promise.prototype._settlePromiseFromHandler)
- description and source-code
```javascript
_settlePromiseFromHandler = function ( handler, receiver, value, promise ) {
    var bitField = promise._bitField;
    if (((bitField & 65536) !== 0)) return;
    promise._pushContext();
    var x;
    if (receiver === APPLY) {
        if (!value || typeof value.length !== "number") {
            x = errorObj;
            x.e = new TypeError("cannot .spread() a non-array: " +
                                    util.classString(value));
        } else {
            x = tryCatch(handler).apply(this._boundValue(), value);
        }
    } else {
        x = tryCatch(handler).call(receiver, value);
    }
    var promiseCreated = promise._popContext();
    bitField = promise._bitField;
    if (((bitField & 65536) !== 0)) return;

    if (x === NEXT_FILTER) {
        promise._reject(value);
    } else if (x === errorObj) {
        promise._rejectCallback(x.e, false);
    } else {
        debug.checkForgottenReturns(x, promiseCreated, "",  promise, this);
        promise._resolveCallback(x);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._settlePromiseLateCancellationObserver"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromiseLateCancellationObserver (ctx)](#apidoc.element.sequelize.Promise.prototype._settlePromiseLateCancellationObserver)
- description and source-code
```javascript
_settlePromiseLateCancellationObserver = function (ctx) {
    var handler = ctx.handler;
    var promise = ctx.promise;
    var receiver = ctx.receiver;
    var value = ctx.value;
    if (typeof handler === "function") {
        if (!(promise instanceof Promise)) {
            handler.call(receiver, value, promise);
        } else {
            this._settlePromiseFromHandler(handler, receiver, value, promise);
        }
    } else if (promise instanceof Promise) {
        promise._reject(value);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._settlePromises"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settlePromises ()](#apidoc.element.sequelize.Promise.prototype._settlePromises)
- description and source-code
```javascript
_settlePromises = function () {
    var bitField = this._bitField;
    var len = (bitField & 65535);

    if (len > 0) {
        if (((bitField & 16842752) !== 0)) {
            var reason = this._fulfillmentHandler0;
            this._settlePromise0(this._rejectionHandler0, reason, bitField);
            this._rejectPromises(len, reason);
        } else {
            var value = this._rejectionHandler0;
            this._settlePromise0(this._fulfillmentHandler0, value, bitField);
            this._fulfillPromises(len, value);
        }
        this._setLength(0);
    }
    this._clearCancellationData();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._settledValue"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_settledValue ()](#apidoc.element.sequelize.Promise.prototype._settledValue)
- description and source-code
```javascript
_settledValue = function () {
    var bitField = this._bitField;
    if (((bitField & 33554432) !== 0)) {
        return this._rejectionHandler0;
    } else if (((bitField & 16777216) !== 0)) {
        return this._fulfillmentHandler0;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._target"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_target ()](#apidoc.element.sequelize.Promise.prototype._target)
- description and source-code
```javascript
_target = function () {
    var ret = this;
    while (ret._isFollowing()) ret = ret._followee();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._then"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_then ( didFulfill, didReject, _, receiver, internalData )](#apidoc.element.sequelize.Promise.prototype._then)
- description and source-code
```javascript
_then = function ( didFulfill, didReject, _, receiver, internalData ) {
    var haveInternalData = internalData !== undefined;
    var promise = haveInternalData ? internalData : new Promise(INTERNAL);
    var target = this._target();
    var bitField = target._bitField;

    if (!haveInternalData) {
        promise._propagateFrom(this, 3);
        promise._captureStackTrace();
        if (receiver === undefined &&
            ((this._bitField & 2097152) !== 0)) {
            if (!((bitField & 50397184) === 0)) {
                receiver = this._boundValue();
            } else {
                receiver = target === this ? undefined : this._boundTo;
            }
        }
        this._fireEvent("promiseChained", this, promise);
    }

    var domain = getDomain();
    if (!((bitField & 50397184) === 0)) {
        var handler, value, settler = target._settlePromiseCtx;
        if (((bitField & 33554432) !== 0)) {
            value = target._rejectionHandler0;
            handler = didFulfill;
        } else if (((bitField & 16777216) !== 0)) {
            value = target._fulfillmentHandler0;
            handler = didReject;
            target._unsetRejectionIsUnhandled();
        } else {
            settler = target._settlePromiseLateCancellationObserver;
            value = new CancellationError("late cancellation observer");
            target._attachExtraTrace(value);
            handler = didReject;
        }

        async.invoke(settler, target, {
            handler: domain === null ? handler
                : (typeof handler === "function" &&
                    util.domainBind(domain, handler)),
            promise: promise,
            receiver: receiver,
            value: value
        });
    } else {
        target._addCallbacks(didFulfill, didReject, promise, receiver, domain);
    }

    return promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._unsetCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetCancelled ()](#apidoc.element.sequelize.Promise.prototype._unsetCancelled)
- description and source-code
```javascript
_unsetCancelled = function () {
    this._bitField = this._bitField & (~65536);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._unsetDisposable"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetDisposable ()](#apidoc.element.sequelize.Promise.prototype._unsetDisposable)
- description and source-code
```javascript
_unsetDisposable = function () {
    this._bitField = this._bitField & (~131072);
    this._disposer = undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._unsetOnCancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetOnCancel ()](#apidoc.element.sequelize.Promise.prototype._unsetOnCancel)
- description and source-code
```javascript
_unsetOnCancel = function () {
    this._onCancelField = undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._unsetRejectionIsUnhandled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetRejectionIsUnhandled ()](#apidoc.element.sequelize.Promise.prototype._unsetRejectionIsUnhandled)
- description and source-code
```javascript
_unsetRejectionIsUnhandled = function () {
    this._bitField = this._bitField & (~1048576);
    if (this._isUnhandledRejectionNotified()) {
        this._unsetUnhandledRejectionIsNotified();
        this._notifyUnhandledRejectionIsHandled();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._unsetUnhandledRejectionIsNotified"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_unsetUnhandledRejectionIsNotified ()](#apidoc.element.sequelize.Promise.prototype._unsetUnhandledRejectionIsNotified)
- description and source-code
```javascript
_unsetUnhandledRejectionIsNotified = function () {
    this._bitField = this._bitField & (~262144);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._value"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_value ()](#apidoc.element.sequelize.Promise.prototype._value)
- description and source-code
```javascript
_value = function () {
    return this._settledValue();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype._warn"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>_warn (message, shouldUseOwnTrace, promise)](#apidoc.element.sequelize.Promise.prototype._warn)
- description and source-code
```javascript
_warn = function (message, shouldUseOwnTrace, promise) {
    return warn(message, shouldUseOwnTrace, promise || this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.all"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>all ()](#apidoc.element.sequelize.Promise.prototype.all)
- description and source-code
```javascript
all = function () {
    if (arguments.length > 0) {
        this._warn(".all() was passed arguments but it does not take any");
    }
    return new PromiseArray(this).promise();
}
```
- example usage
```shell
...
InstanceValidator.prototype.validate = function() {
if (this.inProgress) {
  throw new Error('Validations already in progress.');
}
this.inProgress = true;

var self = this;
return Promise.all([
  self._builtinValidators(),
  self._customValidators()
].map(function(promise) {
  return promise.reflect();
})).then(function() {
  if (self.errors.length) {
    return new sequelizeError.ValidationError(null, self.errors);
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.any"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>any ()](#apidoc.element.sequelize.Promise.prototype.any)
- description and source-code
```javascript
any = function () {
    return any(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.asCallback"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>asCallback (nodeback, options)](#apidoc.element.sequelize.Promise.prototype.asCallback)
- description and source-code
```javascript
asCallback = function (nodeback, options) {
    if (typeof nodeback == "function") {
        var adapter = successAdapter;
        if (options !== undefined && Object(options).spread) {
            adapter = spreadAdapter;
        }
        this._then(
            adapter,
            errorAdapter,
            undefined,
            this,
            nodeback
        );
    }
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.bind"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>bind (thisArg)](#apidoc.element.sequelize.Promise.prototype.bind)
- description and source-code
```javascript
bind = function (thisArg) {
    if (!calledBind) {
        calledBind = true;
        Promise.prototype._propagateFrom = debug.propagateFromFunction();
        Promise.prototype._boundValue = debug.boundValueFunction();
    }
    var maybePromise = tryConvertToPromise(thisArg);
    var ret = new Promise(INTERNAL);
    ret._propagateFrom(this, 1);
    var target = this._target();
    ret._setBoundTo(maybePromise);
    if (maybePromise instanceof Promise) {
        var context = {
            promiseRejectionQueued: false,
            promise: ret,
            target: target,
            bindingPromise: maybePromise
        };
        target._then(INTERNAL, targetRejected, undefined, ret, context);
        maybePromise._then(
            bindingResolved, bindingRejected, undefined, ret, context);
        ret._setOnCancel(maybePromise);
    } else {
        ret._resolveCallback(target);
    }
    return ret;
}
```
- example usage
```shell
...

   var validatorPromise = self._invokeBuiltinValidator(value, test, validatorType, field);
   // errors are handled in settling, stub this
   validatorPromise.catch(function() {});
   validators.push(validatorPromise.reflect());
 });

 return Promise.all(validators).then(this._handleReflectedResult.bind(this, field));
};

/**
* Prepare and invoke a custom validator.
*
* @param {Function} validator The custom validator.
* @param {string} validatorType the custom validator type (name).
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.break"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>break ()](#apidoc.element.sequelize.Promise.prototype.break)
- description and source-code
```javascript
break = function () {
    if (!debug.cancellation()) return this._warn("cancellation is disabled");

    var promise = this;
    var child = promise;
    while (promise._isCancellable()) {
        if (!promise._cancelBy(child)) {
            if (child._isFollowing()) {
                child._followee().cancel();
            } else {
                child._cancelBranched();
            }
            break;
        }

        var parent = promise._cancellationParent;
        if (parent == null || !parent._isCancellable()) {
            if (promise._isFollowing()) {
                promise._followee().cancel();
            } else {
                promise._cancelBranched();
            }
            break;
        } else {
            if (promise._isFollowing()) promise._followee().cancel();
            promise._setWillBeCancelled();
            child = promise;
            promise = parent;
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.call"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>call (methodName)](#apidoc.element.sequelize.Promise.prototype.call)
- description and source-code
```javascript
call = function (methodName) {
    var $_len = arguments.length;var args = new Array(Math.max($_len - 1, 0)); for(var $_i = 1; $_i < $_len; ++$_i) {args[$_i -
1] = arguments[$_i];};
    if (!false) {
        if (canEvaluate) {
            var maybeCaller = getMethodCaller(methodName);
            if (maybeCaller !== null) {
                return this._then(
                    maybeCaller, undefined, undefined, args, undefined);
            }
        }
    }
    args.push(methodName);
    return this._then(caller, undefined, undefined, args, undefined);
}
```
- example usage
```shell
...

/**
* Thrown when a database query times out because of a deadlock
* @extends DatabaseError
* @constructor
*/
error.TimeoutError = function (parent) {
 error.DatabaseError.call(this, parent);
 this.name = 'SequelizeTimeoutError';
};
util.inherits(error.TimeoutError, error.BaseError);

/**
* Thrown when a unique constraint is violated in the database
* @extends DatabaseError
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.cancel"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>cancel ()](#apidoc.element.sequelize.Promise.prototype.cancel)
- description and source-code
```javascript
cancel = function () {
    if (!debug.cancellation()) return this._warn("cancellation is disabled");

    var promise = this;
    var child = promise;
    while (promise._isCancellable()) {
        if (!promise._cancelBy(child)) {
            if (child._isFollowing()) {
                child._followee().cancel();
            } else {
                child._cancelBranched();
            }
            break;
        }

        var parent = promise._cancellationParent;
        if (parent == null || !parent._isCancellable()) {
            if (promise._isFollowing()) {
                promise._followee().cancel();
            } else {
                promise._cancelBranched();
            }
            break;
        } else {
            if (promise._isFollowing()) promise._followee().cancel();
            promise._setWillBeCancelled();
            child = promise;
            promise = parent;
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.catch"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catch ()](#apidoc.element.sequelize.Promise.prototype.catch)
- description and source-code
```javascript
catch = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  Utils._.each(this.modelInstance.$modelOptions.validate, function(validator, validatorType) {
    if (self.options.skip.indexOf(validatorType) >= 0) {
      return;
    }

    var valprom = self._invokeCustomValidator(validator, validatorType)
      // errors are handled in settling, stub this
      .catch(function() {})
      .reflect();

    validators.push(valprom);
  });

  return Promise.all(validators);
};
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.catchReturn"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catchReturn (value)](#apidoc.element.sequelize.Promise.prototype.catchReturn)
- description and source-code
```javascript
catchReturn = function (value) {
    if (arguments.length <= 1) {
        if (value instanceof Promise) value.suppressUnhandledRejections();
        return this._then(
            undefined, returner, undefined, {value: value}, undefined);
    } else {
        var _value = arguments[1];
        if (_value instanceof Promise) _value.suppressUnhandledRejections();
        var handler = function() {return _value;};
        return this.caught(value, handler);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.catchThrow"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catchThrow (reason)](#apidoc.element.sequelize.Promise.prototype.catchThrow)
- description and source-code
```javascript
catchThrow = function (reason) {
    if (arguments.length <= 1) {
        return this._then(
            undefined, thrower, undefined, {reason: reason}, undefined);
    } else {
        var _reason = arguments[1];
        var handler = function() {throw _reason;};
        return this.caught(reason, handler);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.caught"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>caught (fn)](#apidoc.element.sequelize.Promise.prototype.caught)
- description and source-code
```javascript
caught = function (fn) {
    var len = arguments.length;
    if (len > 1) {
        var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return apiRejection("Catch statement predicate: " +
                    "expecting an object but got " + util.classString(item));
            }
        }
        catchInstances.length = j;
        fn = arguments[i];
        return this.then(undefined, catchFilter(catchInstances, fn, this));
    }
    return this.then(undefined, fn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.delay"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>delay (ms)](#apidoc.element.sequelize.Promise.prototype.delay)
- description and source-code
```javascript
delay = function (ms) {
    return delay(ms, this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.disposer"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>disposer (fn)](#apidoc.element.sequelize.Promise.prototype.disposer)
- description and source-code
```javascript
disposer = function (fn) {
    if (typeof fn === "function") {
        return new FunctionDisposer(fn, this, createContext());
    }
    throw new TypeError();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.done"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>done ()](#apidoc.element.sequelize.Promise.prototype.done)
- description and source-code
```javascript
done = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.each"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>each ()](#apidoc.element.sequelize.Promise.prototype.each)
- description and source-code
```javascript
each = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  beforeConnection: 'beforeConnect'
};

var Hooks = {
  replaceHookAliases: function(hooks) {
    var realHookName;

    Utils._.each(hooks, function(hooksArray, name) {
      // Does an alias for this hook name exist?
      if (realHookName = hookAliases[name]) {
// Add the hooks to the actual hook
hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

// Delete the alias
delete hooks[name];
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.error"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>error ()](#apidoc.element.sequelize.Promise.prototype.error)
- description and source-code
```javascript
error = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
* @param {Array.<Promise.PromiseInspection>} Promise inspection objects.
* @private
*/
InstanceValidator.prototype._handleReflectedResult = function(field, promiseInspections) {
 var self = this;
 promiseInspections.forEach(function(promiseInspection) {
   if (promiseInspection.isRejected()) {
     var rejection = promiseInspection.error();
     self._pushError(true, field, rejection);
   }
 });
};

/**
* Signs all errors retaining the original.
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.filter"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>filter ()](#apidoc.element.sequelize.Promise.prototype.filter)
- description and source-code
```javascript
filter = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
  }

  return typeof hook === 'object' && hook.name !== name;
});
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.finally"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>finally ()](#apidoc.element.sequelize.Promise.prototype.finally)
- description and source-code
```javascript
finally = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
    }
  });
})
.then(console.log)
.finally(() => {
  sequelize.close();
});
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>get (propertyName)](#apidoc.element.sequelize.Promise.prototype.get)
- description and source-code
```javascript
get = function (propertyName) {
    var isIndex = (typeof propertyName === "number");
    var getter;
    if (!isIndex) {
        if (canEvaluate) {
            var maybeGetter = getGetter(propertyName);
            getter = maybeGetter !== null ? maybeGetter : namedGetter;
        } else {
            getter = namedGetter;
        }
    } else {
        getter = indexedGetter;
    }
    return this._then(getter, undefined, undefined, propertyName, undefined);
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.isCancellable"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isCancellable ()](#apidoc.element.sequelize.Promise.prototype.isCancellable)
- description and source-code
```javascript
isCancellable = function () {
    return this.isPending() && !this.isCancelled();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.isCancelled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isCancelled ()](#apidoc.element.sequelize.Promise.prototype.isCancelled)
- description and source-code
```javascript
isCancelled = function () {
    return (this._target()._bitField & 8454144) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.isFulfilled"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isFulfilled ()](#apidoc.element.sequelize.Promise.prototype.isFulfilled)
- description and source-code
```javascript
isFulfilled = function () {
    return isFulfilled.call(this._target());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.isPending"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isPending ()](#apidoc.element.sequelize.Promise.prototype.isPending)
- description and source-code
```javascript
isPending = function () {
    return isPending.call(this._target());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.isRejected"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isRejected ()](#apidoc.element.sequelize.Promise.prototype.isRejected)
- description and source-code
```javascript
isRejected = function () {
    return isRejected.call(this._target());
}
```
- example usage
```shell
...
 * @param {string} field The attribute name.
 * @param {Array.<Promise.PromiseInspection>} Promise inspection objects.
 * @private
 */
InstanceValidator.prototype._handleReflectedResult = function(field, promiseInspections) {
  var self = this;
  promiseInspections.forEach(function(promiseInspection) {
    if (promiseInspection.isRejected()) {
      var rejection = promiseInspection.error();
      self._pushError(true, field, rejection);
    }
  });
};

/**
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.isResolved"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>isResolved ()](#apidoc.element.sequelize.Promise.prototype.isResolved)
- description and source-code
```javascript
isResolved = function () {
    return isResolved.call(this._target());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.lastly"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>lastly (handler)](#apidoc.element.sequelize.Promise.prototype.lastly)
- description and source-code
```javascript
lastly = function (handler) {
    return this._passThrough(handler,
                             0,
                             finallyHandler,
                             finallyHandler);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.map"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>map ()](#apidoc.element.sequelize.Promise.prototype.map)
- description and source-code
```javascript
map = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
};
util.inherits(error.ValidationError, error.BaseError);

/**
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.mapSeries"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>mapSeries ()](#apidoc.element.sequelize.Promise.prototype.mapSeries)
- description and source-code
```javascript
mapSeries = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.nodeify"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>nodeify ()](#apidoc.element.sequelize.Promise.prototype.nodeify)
- description and source-code
```javascript
nodeify = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
      hook = Promise.promisify(hook, self);
    }

    return hook.apply(self, fnArgs);
  }).return();

  if (fn) {
    return promise.nodeify(fn);
  }

  return promise;
},

hook: function() {
  return Hooks.addHook.apply(this, arguments);
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.props"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>props ()](#apidoc.element.sequelize.Promise.prototype.props)
- description and source-code
```javascript
props = function () {
    return props(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.race"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>race ()](#apidoc.element.sequelize.Promise.prototype.race)
- description and source-code
```javascript
race = function () {
    return race(this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.reason"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reason ()](#apidoc.element.sequelize.Promise.prototype.reason)
- description and source-code
```javascript
reason = function () {
    var target = this._target();
    target._unsetRejectionIsUnhandled();
    return reason.call(target);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reduce ()](#apidoc.element.sequelize.Promise.prototype.reduce)
- description and source-code
```javascript
reduce = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
/**
 * Gets all validation error items for the path / field specified.
 *
 * @param {string} path The path to be checked for error items
 * @returns {Array} Validation error items for the specified path
 */
error.ValidationError.prototype.get = function(path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
};
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.reflect"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reflect ()](#apidoc.element.sequelize.Promise.prototype.reflect)
- description and source-code
```javascript
reflect = function () {
    return this._then(reflectHandler,
        reflectHandler, undefined, this, undefined);
}
```
- example usage
```shell
...
this.inProgress = true;

var self = this;
return Promise.all([
  self._builtinValidators(),
  self._customValidators()
].map(function(promise) {
  return promise.reflect();
})).then(function() {
  if (self.errors.length) {
    return new sequelizeError.ValidationError(null, self.errors);
  } else {
    return null;
  }
});
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.return"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>return (value)](#apidoc.element.sequelize.Promise.prototype.return)
- description and source-code
```javascript
return = function (value) {
    if (value instanceof Promise) value.suppressUnhandledRejections();
    return this._then(
        returner, undefined, undefined, {value: value}, undefined);
}
```
- example usage
```shell
...
    }

    if (hookType && hook.length > hookTypes[hookType].params) {
      hook = Promise.promisify(hook, self);
    }

    return hook.apply(self, fnArgs);
  }).return();

  if (fn) {
    return promise.nodeify(fn);
  }

  return promise;
},
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.settle"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>settle ()](#apidoc.element.sequelize.Promise.prototype.settle)
- description and source-code
```javascript
settle = function () {
    return Promise.settle(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.some"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>some (howMany)](#apidoc.element.sequelize.Promise.prototype.some)
- description and source-code
```javascript
some = function (howMany) {
    return some(this, howMany);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.spread"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>spread ()](#apidoc.element.sequelize.Promise.prototype.spread)
- description and source-code
```javascript
spread = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.suppressUnhandledRejections"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>suppressUnhandledRejections ()](#apidoc.element.sequelize.Promise.prototype.suppressUnhandledRejections)
- description and source-code
```javascript
suppressUnhandledRejections = function () {
    var target = this._target();
    target._bitField = ((target._bitField & (~1048576)) |
                      524288);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.tap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>tap ()](#apidoc.element.sequelize.Promise.prototype.tap)
- description and source-code
```javascript
tap = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.tapCatch"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>tapCatch (handlerOrPredicate)](#apidoc.element.sequelize.Promise.prototype.tapCatch)
- description and source-code
```javascript
tapCatch = function (handlerOrPredicate) {
    var len = arguments.length;
    if(len === 1) {
        return this._passThrough(handlerOrPredicate,
                                 1,
                                 undefined,
                                 finallyHandler);
    } else {
         var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return Promise.reject(new TypeError(
                    "tapCatch statement predicate: "
                    + "expecting an object but got " + util.classString(item)
                ));
            }
        }
        catchInstances.length = j;
        var handler = arguments[i];
        return this._passThrough(catchFilter(catchInstances, handler, this),
                                 1,
                                 undefined,
                                 finallyHandler);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.then"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>then ()](#apidoc.element.sequelize.Promise.prototype.then)
- description and source-code
```javascript
then = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...



return sequelize.sync({
force: true,
logging: console.log
})
.then(() => {
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
    }
  });
})
.then(console.log)
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.thenReturn"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>thenReturn (value)](#apidoc.element.sequelize.Promise.prototype.thenReturn)
- description and source-code
```javascript
thenReturn = function (value) {
    if (value instanceof Promise) value.suppressUnhandledRejections();
    return this._then(
        returner, undefined, undefined, {value: value}, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.thenThrow"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>thenThrow (reason)](#apidoc.element.sequelize.Promise.prototype.thenThrow)
- description and source-code
```javascript
thenThrow = function (reason) {
    return this._then(
        thrower, undefined, undefined, {reason: reason}, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.throw"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>throw (reason)](#apidoc.element.sequelize.Promise.prototype.throw)
- description and source-code
```javascript
throw = function (reason) {
    return this._then(
        thrower, undefined, undefined, {reason: reason}, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.timeout"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>timeout (ms, message)](#apidoc.element.sequelize.Promise.prototype.timeout)
- description and source-code
```javascript
timeout = function (ms, message) {
    ms = +ms;
    var ret, parent;

    var handleWrapper = new HandleWrapper(setTimeout(function timeoutTimeout() {
        if (ret.isPending()) {
            afterTimeout(ret, message, parent);
        }
    }, ms));

    if (debug.cancellation()) {
        parent = this.then();
        ret = parent._then(successClear, failureClear,
                            undefined, handleWrapper, undefined);
        ret._setOnCancel(handleWrapper);
    } else {
        ret = this._then(successClear, failureClear,
                            undefined, handleWrapper, undefined);
    }

    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>toJSON ()](#apidoc.element.sequelize.Promise.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
    var ret = {
        isFulfilled: false,
        isRejected: false,
        fulfillmentValue: undefined,
        rejectionReason: undefined
    };
    if (this.isFulfilled()) {
        ret.fulfillmentValue = this.value();
        ret.isFulfilled = true;
    } else if (this.isRejected()) {
        ret.rejectionReason = this.reason();
        ret.isRejected = true;
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>toString ()](#apidoc.element.sequelize.Promise.prototype.toString)
- description and source-code
```javascript
toString = function () {
    return "[object Promise]";
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.value"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>value ()](#apidoc.element.sequelize.Promise.prototype.value)
- description and source-code
```javascript
value = function () {
    return value.call(this._target());
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.catch"></a>[module sequelize.Promise.prototype.catch](#apidoc.module.sequelize.Promise.prototype.catch)

#### <a name="apidoc.element.sequelize.Promise.prototype.catch.catch"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>catch ()](#apidoc.element.sequelize.Promise.prototype.catch.catch)
- description and source-code
```javascript
catch = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  Utils._.each(this.modelInstance.$modelOptions.validate, function(validator, validatorType) {
    if (self.options.skip.indexOf(validatorType) >= 0) {
      return;
    }

    var valprom = self._invokeCustomValidator(validator, validatorType)
      // errors are handled in settling, stub this
      .catch(function() {})
      .reflect();

    validators.push(valprom);
  });

  return Promise.all(validators);
};
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.catch.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.catch.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.catch.__original)
- description and source-code
```javascript
__original = function (fn) {
    var len = arguments.length;
    if (len > 1) {
        var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return apiRejection("Catch statement predicate: " +
                    "expecting an object but got " + util.classString(item));
            }
        }
        catchInstances.length = j;
        fn = arguments[i];
        return this.then(undefined, catchFilter(catchInstances, fn, this));
    }
    return this.then(undefined, fn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.catch.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.catch.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.catch.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.done"></a>[module sequelize.Promise.prototype.done](#apidoc.module.sequelize.Promise.prototype.done)

#### <a name="apidoc.element.sequelize.Promise.prototype.done.done"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>done ()](#apidoc.element.sequelize.Promise.prototype.done.done)
- description and source-code
```javascript
done = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.done.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.done.</span>__original (didFulfill, didReject)](#apidoc.element.sequelize.Promise.prototype.done.__original)
- description and source-code
```javascript
__original = function (didFulfill, didReject) {
    var promise =
        this._then(didFulfill, didReject, undefined, undefined, undefined);
    promise._setIsFinal();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.done.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.done.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.done.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.each"></a>[module sequelize.Promise.prototype.each](#apidoc.module.sequelize.Promise.prototype.each)

#### <a name="apidoc.element.sequelize.Promise.prototype.each.each"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>each ()](#apidoc.element.sequelize.Promise.prototype.each.each)
- description and source-code
```javascript
each = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  beforeConnection: 'beforeConnect'
};

var Hooks = {
  replaceHookAliases: function(hooks) {
    var realHookName;

    Utils._.each(hooks, function(hooksArray, name) {
      // Does an alias for this hook name exist?
      if (realHookName = hookAliases[name]) {
// Add the hooks to the actual hook
hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

// Delete the alias
delete hooks[name];
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.each.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.each.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.each.__original)
- description and source-code
```javascript
__original = function (fn) {
    return PromiseReduce(this, fn, INTERNAL, 0)
              ._then(promiseAllThis, undefined, undefined, this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.each.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.each.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.each.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.error"></a>[module sequelize.Promise.prototype.error](#apidoc.module.sequelize.Promise.prototype.error)

#### <a name="apidoc.element.sequelize.Promise.prototype.error.error"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>error ()](#apidoc.element.sequelize.Promise.prototype.error.error)
- description and source-code
```javascript
error = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
* @param {Array.<Promise.PromiseInspection>} Promise inspection objects.
* @private
*/
InstanceValidator.prototype._handleReflectedResult = function(field, promiseInspections) {
 var self = this;
 promiseInspections.forEach(function(promiseInspection) {
   if (promiseInspection.isRejected()) {
     var rejection = promiseInspection.error();
     self._pushError(true, field, rejection);
   }
 });
};

/**
* Signs all errors retaining the original.
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.error.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.error.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.error.__original)
- description and source-code
```javascript
__original = function (fn) {
    return this.caught(util.originatesFromRejection, fn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.error.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.error.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.error.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.filter"></a>[module sequelize.Promise.prototype.filter](#apidoc.module.sequelize.Promise.prototype.filter)

#### <a name="apidoc.element.sequelize.Promise.prototype.filter.filter"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>filter ()](#apidoc.element.sequelize.Promise.prototype.filter.filter)
- description and source-code
```javascript
filter = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
  }

  return typeof hook === 'object' && hook.name !== name;
});
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.filter.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.filter.</span>__original (fn, options)](#apidoc.element.sequelize.Promise.prototype.filter.__original)
- description and source-code
```javascript
__original = function (fn, options) {
    return PromiseMap(this, fn, options, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.filter.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.filter.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.filter.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.finally"></a>[module sequelize.Promise.prototype.finally](#apidoc.module.sequelize.Promise.prototype.finally)

#### <a name="apidoc.element.sequelize.Promise.prototype.finally.finally"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>finally ()](#apidoc.element.sequelize.Promise.prototype.finally.finally)
- description and source-code
```javascript
finally = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
    }
  });
})
.then(console.log)
.finally(() => {
  sequelize.close();
});
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.finally.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.finally.</span>__original (handler)](#apidoc.element.sequelize.Promise.prototype.finally.__original)
- description and source-code
```javascript
__original = function (handler) {
    return this._passThrough(handler,
                             0,
                             finallyHandler,
                             finallyHandler);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.finally.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.finally.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.finally.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.map"></a>[module sequelize.Promise.prototype.map](#apidoc.module.sequelize.Promise.prototype.map)

#### <a name="apidoc.element.sequelize.Promise.prototype.map.map"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>map ()](#apidoc.element.sequelize.Promise.prototype.map.map)
- description and source-code
```javascript
map = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
};
util.inherits(error.ValidationError, error.BaseError);

/**
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.map.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.map.</span>__original (fn, options)](#apidoc.element.sequelize.Promise.prototype.map.__original)
- description and source-code
```javascript
__original = function (fn, options) {
    return map(this, fn, options, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.map.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.map.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.map.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.mapSeries"></a>[module sequelize.Promise.prototype.mapSeries](#apidoc.module.sequelize.Promise.prototype.mapSeries)

#### <a name="apidoc.element.sequelize.Promise.prototype.mapSeries.mapSeries"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>mapSeries ()](#apidoc.element.sequelize.Promise.prototype.mapSeries.mapSeries)
- description and source-code
```javascript
mapSeries = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.mapSeries.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.mapSeries.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.mapSeries.__original)
- description and source-code
```javascript
__original = function (fn) {
    return PromiseReduce(this, fn, INTERNAL, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.mapSeries.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.mapSeries.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.mapSeries.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.nodeify"></a>[module sequelize.Promise.prototype.nodeify](#apidoc.module.sequelize.Promise.prototype.nodeify)

#### <a name="apidoc.element.sequelize.Promise.prototype.nodeify.nodeify"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>nodeify ()](#apidoc.element.sequelize.Promise.prototype.nodeify.nodeify)
- description and source-code
```javascript
nodeify = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
      hook = Promise.promisify(hook, self);
    }

    return hook.apply(self, fnArgs);
  }).return();

  if (fn) {
    return promise.nodeify(fn);
  }

  return promise;
},

hook: function() {
  return Hooks.addHook.apply(this, arguments);
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.nodeify.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.nodeify.</span>__original (nodeback, options)](#apidoc.element.sequelize.Promise.prototype.nodeify.__original)
- description and source-code
```javascript
__original = function (nodeback, options) {
    if (typeof nodeback == "function") {
        var adapter = successAdapter;
        if (options !== undefined && Object(options).spread) {
            adapter = spreadAdapter;
        }
        this._then(
            adapter,
            errorAdapter,
            undefined,
            this,
            nodeback
        );
    }
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.nodeify.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.nodeify.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.nodeify.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.reduce"></a>[module sequelize.Promise.prototype.reduce](#apidoc.module.sequelize.Promise.prototype.reduce)

#### <a name="apidoc.element.sequelize.Promise.prototype.reduce.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>reduce ()](#apidoc.element.sequelize.Promise.prototype.reduce.reduce)
- description and source-code
```javascript
reduce = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
/**
 * Gets all validation error items for the path / field specified.
 *
 * @param {string} path The path to be checked for error items
 * @returns {Array} Validation error items for the specified path
 */
error.ValidationError.prototype.get = function(path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
};
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.reduce.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.reduce.</span>__original (fn, initialValue)](#apidoc.element.sequelize.Promise.prototype.reduce.__original)
- description and source-code
```javascript
__original = function (fn, initialValue) {
    return reduce(this, fn, initialValue, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.reduce.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.reduce.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.reduce.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.spread"></a>[module sequelize.Promise.prototype.spread](#apidoc.module.sequelize.Promise.prototype.spread)

#### <a name="apidoc.element.sequelize.Promise.prototype.spread.spread"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>spread ()](#apidoc.element.sequelize.Promise.prototype.spread.spread)
- description and source-code
```javascript
spread = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.spread.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.spread.</span>__original (fn)](#apidoc.element.sequelize.Promise.prototype.spread.__original)
- description and source-code
```javascript
__original = function (fn) {
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    return this.all()._then(fn, undefined, undefined, APPLY, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.spread.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.spread.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.spread.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.tap"></a>[module sequelize.Promise.prototype.tap](#apidoc.module.sequelize.Promise.prototype.tap)

#### <a name="apidoc.element.sequelize.Promise.prototype.tap.tap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>tap ()](#apidoc.element.sequelize.Promise.prototype.tap.tap)
- description and source-code
```javascript
tap = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.tap.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.tap.</span>__original (handler)](#apidoc.element.sequelize.Promise.prototype.tap.__original)
- description and source-code
```javascript
__original = function (handler) {
    return this._passThrough(handler, 1, finallyHandler);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.tap.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.tap.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.tap.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.prototype.then"></a>[module sequelize.Promise.prototype.then](#apidoc.module.sequelize.Promise.prototype.then)

#### <a name="apidoc.element.sequelize.Promise.prototype.then.then"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.</span>then ()](#apidoc.element.sequelize.Promise.prototype.then.then)
- description and source-code
```javascript
then = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...



return sequelize.sync({
force: true,
logging: console.log
})
.then(() => {
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
    }
  });
})
.then(console.log)
...
```

#### <a name="apidoc.element.sequelize.Promise.prototype.then.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.then.</span>__original (didFulfill, didReject)](#apidoc.element.sequelize.Promise.prototype.then.__original)
- description and source-code
```javascript
__original = function (didFulfill, didReject) {
    if (debug.warnings() && arguments.length > 0 &&
        typeof didFulfill !== "function" &&
        typeof didReject !== "function") {
        var msg = ".then() only accepts functions but was passed: " +
                util.classString(didFulfill);
        if (arguments.length > 1) {
            msg += ", " + util.classString(didReject);
        }
        this._warn(msg);
    }
    return this._then(didFulfill, didReject, undefined, undefined, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.prototype.then.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.prototype.then.</span>__unwrap ()](#apidoc.element.sequelize.Promise.prototype.then.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Promise.reduce"></a>[module sequelize.Promise.reduce](#apidoc.module.sequelize.Promise.reduce)

#### <a name="apidoc.element.sequelize.Promise.reduce.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.</span>reduce ()](#apidoc.element.sequelize.Promise.reduce.reduce)
- description and source-code
```javascript
reduce = function () {
  if (Promise.Sequelize && Promise.Sequelize.cls) {
    var ns = Promise.Sequelize.cls;
    for(var x=0; x<fnArgs.length; x++) {
      var argIndex = fnArgs[x] < 0 ? arguments.length + fnArgs[x] : fnArgs[x];
      if ( argIndex < arguments.length && typeof arguments[argIndex] === 'function' ) {
        arguments[argIndex] = ns.bind( arguments[argIndex] );
      }
    }
  }

  return fn.apply(this, arguments);
}
```
- example usage
```shell
...
/**
 * Gets all validation error items for the path / field specified.
 *
 * @param {string} path The path to be checked for error items
 * @returns {Array} Validation error items for the specified path
 */
error.ValidationError.prototype.get = function(path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
};
...
```

#### <a name="apidoc.element.sequelize.Promise.reduce.__original"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.reduce.</span>__original (promises, fn, initialValue, _each)](#apidoc.element.sequelize.Promise.reduce.__original)
- description and source-code
```javascript
__original = function (promises, fn, initialValue, _each) {
    return reduce(promises, fn, initialValue, _each);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Promise.reduce.__unwrap"></a>[function <span class="apidocSignatureSpan">sequelize.Promise.reduce.</span>__unwrap ()](#apidoc.element.sequelize.Promise.reduce.__unwrap)
- description and source-code
```javascript
__unwrap = function () {
  if (nodule[name] === wrapped) nodule[name] = original
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.RANGE"></a>[module sequelize.RANGE](#apidoc.module.sequelize.RANGE)

#### <a name="apidoc.element.sequelize.RANGE.RANGE"></a>[function <span class="apidocSignatureSpan">sequelize.</span>RANGE (subtype)](#apidoc.element.sequelize.RANGE.RANGE)
- description and source-code
```javascript
RANGE = function (subtype) {
  var options = _.isPlainObject(subtype) ? subtype : { subtype: subtype };

  if (!options.subtype) options.subtype = new INTEGER();

  if (_.isFunction(options.subtype)) {
    options.subtype = new options.subtype();
  }

  if (!(this instanceof RANGE)) return new RANGE(options);
  ABSTRACT.apply(this, arguments);

  this._subtype = options.subtype.key;
  this.options = options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.RANGE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.RANGE.</span>inherits (Constructor)](#apidoc.element.sequelize.RANGE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.RANGE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.RANGE.</span>super_ (options)](#apidoc.element.sequelize.RANGE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.RANGE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.RANGE.</span>warn (link, text)](#apidoc.element.sequelize.RANGE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.RANGE.prototype"></a>[module sequelize.RANGE.prototype](#apidoc.module.sequelize.RANGE.prototype)

#### <a name="apidoc.element.sequelize.RANGE.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.RANGE.prototype.</span>toSql ()](#apidoc.element.sequelize.RANGE.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return pgRangeSubtypes[this._subtype.toLowerCase()];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.RANGE.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.RANGE.prototype.</span>validate (value)](#apidoc.element.sequelize.RANGE.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (_.isPlainObject(value) && value.inclusive) {
    value = value.inclusive;
  }

  if (!_.isArray(value)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid range', value));
  }

  if (value.length !== 2) {
    throw new sequelizeErrors.ValidationError('A range must be an array with two elements');
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.REAL"></a>[module sequelize.REAL](#apidoc.module.sequelize.REAL)

#### <a name="apidoc.element.sequelize.REAL.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>REAL (length, decimals)](#apidoc.element.sequelize.REAL.REAL)
- description and source-code
```javascript
REAL = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof REAL)) return new REAL(options);
  NUMBER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.REAL.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.REAL.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.REAL.super_"></a>[function <span class="apidocSignatureSpan">sequelize.REAL.</span>super_ (options)](#apidoc.element.sequelize.REAL.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.REAL.warn"></a>[function <span class="apidocSignatureSpan">sequelize.REAL.</span>warn (link, text)](#apidoc.element.sequelize.REAL.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.REAL.prototype"></a>[module sequelize.REAL.prototype](#apidoc.module.sequelize.REAL.prototype)



# <a name="apidoc.module.sequelize.STRING"></a>[module sequelize.STRING](#apidoc.module.sequelize.STRING)

#### <a name="apidoc.element.sequelize.STRING.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.</span>STRING (length, binary)](#apidoc.element.sequelize.STRING.STRING)
- description and source-code
```javascript
STRING = function (length, binary) {
  var options = typeof length === 'object' && length || {
    length: length,
    binary: binary
  };

  if (!(this instanceof STRING)) return new STRING(options);

  this.options = options;
  this._binary = options.binary;
  this._length = options.length || 255;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.STRING.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.STRING.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.STRING.super_"></a>[function <span class="apidocSignatureSpan">sequelize.STRING.</span>super_ (options)](#apidoc.element.sequelize.STRING.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.STRING.warn"></a>[function <span class="apidocSignatureSpan">sequelize.STRING.</span>warn (link, text)](#apidoc.element.sequelize.STRING.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.STRING.prototype"></a>[module sequelize.STRING.prototype](#apidoc.module.sequelize.STRING.prototype)

#### <a name="apidoc.element.sequelize.STRING.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.STRING.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'VARCHAR(' + this._length + ')' + ((this._binary) ? ' BINARY' : '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.STRING.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.STRING.prototype.</span>validate (value)](#apidoc.element.sequelize.STRING.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (Object.prototype.toString.call(value) !== '[object String]') {
    if ((this.options.binary && Buffer.isBuffer(value)) || _.isNumber(value)) {
      return true;
    }
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid string', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.TEXT"></a>[module sequelize.TEXT](#apidoc.module.sequelize.TEXT)

#### <a name="apidoc.element.sequelize.TEXT.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.</span>TEXT (length)](#apidoc.element.sequelize.TEXT.TEXT)
- description and source-code
```javascript
TEXT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof TEXT)) return new TEXT(options);
  this.options = options;
  this._length = options.length || '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TEXT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.TEXT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.TEXT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.TEXT.</span>super_ (options)](#apidoc.element.sequelize.TEXT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TEXT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.TEXT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.TEXT.prototype"></a>[module sequelize.TEXT.prototype](#apidoc.module.sequelize.TEXT.prototype)

#### <a name="apidoc.element.sequelize.TEXT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.TEXT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  switch (this._length.toLowerCase()) {
  case 'tiny':
    return 'TINYTEXT';
  case 'medium':
    return 'MEDIUMTEXT';
  case 'long':
    return 'LONGTEXT';
  default:
    return this.key;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TEXT.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.TEXT.prototype.</span>validate (value)](#apidoc.element.sequelize.TEXT.prototype.validate)
- description and source-code
```javascript
validate = function (value) {
  if (!_.isString(value)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid string', value));
   }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.TIME"></a>[module sequelize.TIME](#apidoc.module.sequelize.TIME)

#### <a name="apidoc.element.sequelize.TIME.TIME"></a>[function <span class="apidocSignatureSpan">sequelize.</span>TIME ()](#apidoc.element.sequelize.TIME.TIME)
- description and source-code
```javascript
TIME = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TIME.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.TIME.</span>inherits (Constructor)](#apidoc.element.sequelize.TIME.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.TIME.super_"></a>[function <span class="apidocSignatureSpan">sequelize.TIME.</span>super_ (options)](#apidoc.element.sequelize.TIME.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TIME.warn"></a>[function <span class="apidocSignatureSpan">sequelize.TIME.</span>warn (link, text)](#apidoc.element.sequelize.TIME.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.TIME.prototype"></a>[module sequelize.TIME.prototype](#apidoc.module.sequelize.TIME.prototype)

#### <a name="apidoc.element.sequelize.TIME.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.TIME.prototype.</span>toSql ()](#apidoc.element.sequelize.TIME.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'TIME';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.TimeoutError"></a>[module sequelize.TimeoutError](#apidoc.module.sequelize.TimeoutError)

#### <a name="apidoc.element.sequelize.TimeoutError.TimeoutError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>TimeoutError (parent)](#apidoc.element.sequelize.TimeoutError.TimeoutError)
- description and source-code
```javascript
TimeoutError = function (parent) {
  error.DatabaseError.call(this, parent);
  this.name = 'SequelizeTimeoutError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.TimeoutError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.TimeoutError.</span>super_ ()](#apidoc.element.sequelize.TimeoutError.super_)
- description and source-code
```javascript
super_ = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Transaction"></a>[module sequelize.Transaction](#apidoc.module.sequelize.Transaction)

#### <a name="apidoc.element.sequelize.Transaction.Transaction"></a>[function <span class="apidocSignatureSpan">sequelize.</span>Transaction (sequelize, options)](#apidoc.element.sequelize.Transaction.Transaction)
- description and source-code
```javascript
function Transaction(sequelize, options) {
  this.sequelize = sequelize;
  this.savepoints = [];
  var generateTransactionId = this.sequelize.dialect.QueryGenerator.generateTransactionId;

  this.options = Utils._.extend({
    autocommit: true,
    type: sequelize.options.transactionType,
    isolationLevel: sequelize.options.isolationLevel,
    readOnly: false
  }, options || {});

  this.parent = this.options.transaction;
  this.id = this.parent ? this.parent.id : generateTransactionId();

  if (this.parent) {
    this.id = this.parent.id;
    this.parent.savepoints.push(this);
    this.name = this.id + '-savepoint-' + this.parent.savepoints.length;
  } else {
    this.id = this.name = generateTransactionId();
  }

  delete this.options.transaction;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.default"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.</span>default (sequelize, options)](#apidoc.element.sequelize.Transaction.default)
- description and source-code
```javascript
function Transaction(sequelize, options) {
  this.sequelize = sequelize;
  this.savepoints = [];
  var generateTransactionId = this.sequelize.dialect.QueryGenerator.generateTransactionId;

  this.options = Utils._.extend({
    autocommit: true,
    type: sequelize.options.transactionType,
    isolationLevel: sequelize.options.isolationLevel,
    readOnly: false
  }, options || {});

  this.parent = this.options.transaction;
  this.id = this.parent ? this.parent.id : generateTransactionId();

  if (this.parent) {
    this.id = this.parent.id;
    this.parent.savepoints.push(this);
    this.name = this.id + '-savepoint-' + this.parent.savepoints.length;
  } else {
    this.id = this.name = generateTransactionId();
  }

  delete this.options.transaction;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Transaction.prototype"></a>[module sequelize.Transaction.prototype](#apidoc.module.sequelize.Transaction.prototype)

#### <a name="apidoc.element.sequelize.Transaction.prototype.begin"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>begin ()](#apidoc.element.sequelize.Transaction.prototype.begin)
- description and source-code
```javascript
begin = function () {
  return this
    .sequelize
    .getQueryInterface()
    .startTransaction(this, this.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.prototype.cleanup"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>cleanup ()](#apidoc.element.sequelize.Transaction.prototype.cleanup)
- description and source-code
```javascript
cleanup = function () {
  var res = this.sequelize.connectionManager.releaseConnection(this.connection);
  this.connection.uuid = undefined;
  return res;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.prototype.commit"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>commit ()](#apidoc.element.sequelize.Transaction.prototype.commit)
- description and source-code
```javascript
commit = function () {
  var self = this;

  if (this.finished) {
    throw new Error('Transaction cannot be committed because it has been finished with state: ' + self.finished);
  }

  this.$clearCls();

  return this
    .sequelize
    .getQueryInterface()
    .commitTransaction(this, this.options)
    .finally(function() {
      self.finished = 'commit';
      if (!self.parent) {
        return self.cleanup();
      }
      return null;
    });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.prototype.prepareEnvironment"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>prepareEnvironment ()](#apidoc.element.sequelize.Transaction.prototype.prepareEnvironment)
- description and source-code
```javascript
prepareEnvironment = function () {
  var self = this;
  var connectionPromise;

  if (this.parent) {
    connectionPromise = Utils.Promise.resolve(this.parent.connection);
  } else {
    var acquireOptions = {uuid: this.id};

    if (this.options.readOnly) {
      acquireOptions.type = 'SELECT';
    }

    connectionPromise = this.sequelize.connectionManager.getConnection(acquireOptions);
  }

  return connectionPromise
  .then(function (connection) {
    self.connection = connection;
    self.connection.uuid = self.id;
  }).then(function () {
    return self.begin();
  }).then(function () {
    return self.setDeferrable();
  }).then(function () {
    return self.setIsolationLevel();
  }).then(function () {
    return self.setAutocommit();
  }).catch(function (setupErr) {
    return self.rollback().finally(function () {
      throw setupErr;
    });
  }).tap(function () {
    if (self.sequelize.constructor.cls) {
      self.sequelize.constructor.cls.set('transaction', self);
    }
    return null;
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.prototype.rollback"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>rollback ()](#apidoc.element.sequelize.Transaction.prototype.rollback)
- description and source-code
```javascript
rollback = function () {
  var self = this;

  if (this.finished) {
    throw new Error('Transaction cannot be rolled back because it has been finished with state: ' + self.finished);
  }

  this.$clearCls();

  return this
    .sequelize
    .getQueryInterface()
    .rollbackTransaction(this, this.options)
    .finally(function() {
      if (!self.parent) {
        return self.cleanup();
      }
      return self;
    });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.prototype.setAutocommit"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>setAutocommit ()](#apidoc.element.sequelize.Transaction.prototype.setAutocommit)
- description and source-code
```javascript
setAutocommit = function () {
  return this
    .sequelize
    .getQueryInterface()
    .setAutocommit(this, this.options.autocommit, this.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.prototype.setDeferrable"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>setDeferrable ()](#apidoc.element.sequelize.Transaction.prototype.setDeferrable)
- description and source-code
```javascript
setDeferrable = function () {
  if (this.options.deferrable) {
    return this
      .sequelize
      .getQueryInterface()
      .deferConstraints(this, this.options);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Transaction.prototype.setIsolationLevel"></a>[function <span class="apidocSignatureSpan">sequelize.Transaction.prototype.</span>setIsolationLevel ()](#apidoc.element.sequelize.Transaction.prototype.setIsolationLevel)
- description and source-code
```javascript
setIsolationLevel = function () {
  return this
    .sequelize
    .getQueryInterface()
    .setIsolationLevel(this, this.options.isolationLevel, this.options);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.UUID"></a>[module sequelize.UUID](#apidoc.module.sequelize.UUID)

#### <a name="apidoc.element.sequelize.UUID.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UUID ()](#apidoc.element.sequelize.UUID.UUID)
- description and source-code
```javascript
UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UUID.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.UUID.</span>inherits (Constructor)](#apidoc.element.sequelize.UUID.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.UUID.super_"></a>[function <span class="apidocSignatureSpan">sequelize.UUID.</span>super_ (options)](#apidoc.element.sequelize.UUID.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UUID.warn"></a>[function <span class="apidocSignatureSpan">sequelize.UUID.</span>warn (link, text)](#apidoc.element.sequelize.UUID.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.UUID.prototype"></a>[module sequelize.UUID.prototype](#apidoc.module.sequelize.UUID.prototype)

#### <a name="apidoc.element.sequelize.UUID.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.UUID.prototype.</span>validate (value, options)](#apidoc.element.sequelize.UUID.prototype.validate)
- description and source-code
```javascript
validate = function (value, options) {
  if (!_.isString(value) || !Validator.isUUID(value) && (!options || !options.acceptStrings)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid uuid', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.UUIDV1"></a>[module sequelize.UUIDV1](#apidoc.module.sequelize.UUIDV1)

#### <a name="apidoc.element.sequelize.UUIDV1.UUIDV1"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UUIDV1 ()](#apidoc.element.sequelize.UUIDV1.UUIDV1)
- description and source-code
```javascript
UUIDV1 = function () {
  if (!(this instanceof UUIDV1)) return new UUIDV1();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UUIDV1.super_"></a>[function <span class="apidocSignatureSpan">sequelize.UUIDV1.</span>super_ (options)](#apidoc.element.sequelize.UUIDV1.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.UUIDV1.prototype"></a>[module sequelize.UUIDV1.prototype](#apidoc.module.sequelize.UUIDV1.prototype)

#### <a name="apidoc.element.sequelize.UUIDV1.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.UUIDV1.prototype.</span>validate (value, options)](#apidoc.element.sequelize.UUIDV1.prototype.validate)
- description and source-code
```javascript
validate = function (value, options) {
  if (!_.isString(value) || !Validator.isUUID(value) && (!options || !options.acceptStrings)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid uuid', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.UUIDV4"></a>[module sequelize.UUIDV4](#apidoc.module.sequelize.UUIDV4)

#### <a name="apidoc.element.sequelize.UUIDV4.UUIDV4"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UUIDV4 ()](#apidoc.element.sequelize.UUIDV4.UUIDV4)
- description and source-code
```javascript
UUIDV4 = function () {
  if (!(this instanceof UUIDV4)) return new UUIDV4();
  ABSTRACT.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UUIDV4.super_"></a>[function <span class="apidocSignatureSpan">sequelize.UUIDV4.</span>super_ (options)](#apidoc.element.sequelize.UUIDV4.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.UUIDV4.prototype"></a>[module sequelize.UUIDV4.prototype](#apidoc.module.sequelize.UUIDV4.prototype)

#### <a name="apidoc.element.sequelize.UUIDV4.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.UUIDV4.prototype.</span>validate (value, options)](#apidoc.element.sequelize.UUIDV4.prototype.validate)
- description and source-code
```javascript
validate = function (value, options) {
  if (!_.isString(value) || !Validator.isUUID(value, 4) && (!options || !options.acceptStrings)) {
    throw new sequelizeErrors.ValidationError(util.format('%j is not a valid uuidv4', value));
  }

  return true;
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.UniqueConstraintError"></a>[module sequelize.UniqueConstraintError](#apidoc.module.sequelize.UniqueConstraintError)

#### <a name="apidoc.element.sequelize.UniqueConstraintError.UniqueConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>UniqueConstraintError (options)](#apidoc.element.sequelize.UniqueConstraintError.UniqueConstraintError)
- description and source-code
```javascript
UniqueConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };
  options.message = options.message || options.parent.message || 'Validation Error';
  options.errors = options.errors || {};

  error.ValidationError.call(this, options.message, options.errors);
  this.name = 'SequelizeUniqueConstraintError';
  this.message = options.message;
  this.errors = options.errors;
  this.fields = options.fields;
  this.parent = options.parent;
  this.original = options.parent;
  this.sql = options.parent.sql;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.UniqueConstraintError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.UniqueConstraintError.</span>super_ (message, errors)](#apidoc.element.sequelize.UniqueConstraintError.super_)
- description and source-code
```javascript
super_ = function (message, errors) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeValidationError';
  this.message = 'Validation Error';
<span class="apidocCodeCommentSpan">/**
 * An array of ValidationErrorItems
 * @member errors
 */
</span>  this.errors = errors || [];

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Utils"></a>[module sequelize.Utils](#apidoc.module.sequelize.Utils)

#### <a name="apidoc.element.sequelize.Utils.Promise"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>Promise (executor)](#apidoc.element.sequelize.Utils.Promise)
- description and source-code
```javascript
function Promise(executor) {
    if (executor !== INTERNAL) {
        check(this, executor);
    }
    this._bitField = 0;
    this._fulfillmentHandler0 = undefined;
    this._rejectionHandler0 = undefined;
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._resolveFromExecutor(executor);
    this._promiseCreated();
    this._fireEvent("promiseCreated", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.addTicks"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>addTicks (s, tickChar)](#apidoc.element.sequelize.Utils.addTicks)
- description and source-code
```javascript
addTicks = function (s, tickChar) {
  tickChar = tickChar || Utils.TICK_CHAR;
  return tickChar + Utils.removeTicks(s, tickChar) + tickChar;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.argsArePrimaryKeys"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>argsArePrimaryKeys (args, primaryKeys)](#apidoc.element.sequelize.Utils.argsArePrimaryKeys)
- description and source-code
```javascript
argsArePrimaryKeys = function (args, primaryKeys) {
  var result = (args.length === Object.keys(primaryKeys).length);
  if (result) {
    Utils._.each(args, function(arg) {
      if (result) {
        if (['number', 'string'].indexOf(typeof arg) !== -1) {
          result = true;
        } else {
          result = (arg instanceof Date) || Buffer.isBuffer(arg);
        }
      }
    });
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.camelize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>camelize (str)](#apidoc.element.sequelize.Utils.camelize)
- description and source-code
```javascript
camelize = function (str){
  return str.trim().replace(/[-_\s]+(.)?/g, function(match, c){ return c.toUpperCase(); });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.camelizeIf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>camelizeIf (string, condition)](#apidoc.element.sequelize.Utils.camelizeIf)
- description and source-code
```javascript
camelizeIf = function (string, condition) {
  var result = string;

  if (condition) {
    result = Utils.camelize(string);
  }

  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.canTreatArrayAsAnd"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>canTreatArrayAsAnd (arr)](#apidoc.element.sequelize.Utils.canTreatArrayAsAnd)
- description and source-code
```javascript
canTreatArrayAsAnd = function (arr) {
  return arr.reduce(function(treatAsAnd, arg) {
    if (treatAsAnd) {
      return treatAsAnd;
    } else {
      return Utils._.isPlainObject(arg);
    }
  }, false);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.cast"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>cast (val, type)](#apidoc.element.sequelize.Utils.cast)
- description and source-code
```javascript
cast = function (val, type) {
  this.val = val;
  this.type = (type || '').trim();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.cloneDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>cloneDeep (obj)](#apidoc.element.sequelize.Utils.cloneDeep)
- description and source-code
```javascript
cloneDeep = function (obj) {
  obj = obj || {};
  return _.cloneDeepWith(obj, function (elem) {
    // Do not try to customize cloning of arrays or POJOs
    if (Array.isArray(elem) || _.isPlainObject(elem)) {
      return undefined;
    }

    // Don't clone stuff that's an object, but not a plain one - fx example sequelize models and instances
    if (typeof elem === 'object') {
      return elem;
    }

    // Preserve special data-types like 'fn' across clones. _.get() is used for checking up the prototype chain
    if (elem && typeof elem.clone === 'function') {
      return elem.clone();
    }
  });
}
```
- example usage
```shell
...
// testhint argsConform.end

if (!rawTablename) {
  // Map for backwards compat
  rawTablename = tableName;
}

options = Utils.cloneDeep(options);
options.fields = attributes;
var sql = this.QueryGenerator.addIndexQuery(tableName, options, rawTablename);
return this.sequelize.query(sql, _.assign({}, options, { supportsSearchPath: false }));
};

QueryInterface.prototype.showIndex = function(tableName, options) {
var sql = this.QueryGenerator.showIndexesQuery(tableName, options);
...
```

#### <a name="apidoc.element.sequelize.Utils.col"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>col (col)](#apidoc.element.sequelize.Utils.col)
- description and source-code
```javascript
col = function (col) {
  if (arguments.length > 1) {
    col = this.sliceArgs(arguments);
  }
  this.col = col;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.combineTableNames"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>combineTableNames (tableName1, tableName2)](#apidoc.element.sequelize.Utils.combineTableNames)
- description and source-code
```javascript
combineTableNames = function (tableName1, tableName2) {
  return (tableName1.toLowerCase() < tableName2.toLowerCase()) ? (tableName1 + tableName2) : (tableName2 + tableName1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.defaultValueSchemable"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>defaultValueSchemable (value)](#apidoc.element.sequelize.Utils.defaultValueSchemable)
- description and source-code
```javascript
defaultValueSchemable = function (value) {
  if (typeof value === 'undefined') { return false; }

  // TODO this will be schemable when all supported db
  // have been normalized for this case
  if (value instanceof DataTypes.NOW) { return false; }

  if (value instanceof DataTypes.UUIDV1 || value instanceof DataTypes.UUIDV4) { return false; }

  if (_.isFunction(value)) {
    return false;
  }

  return true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.fn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>fn (fn, args)](#apidoc.element.sequelize.Utils.fn)
- description and source-code
```javascript
fn = function (fn, args) {
  this.fn = fn;
  this.args = args;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.format"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>format (arr, dialect)](#apidoc.element.sequelize.Utils.format)
- description and source-code
```javascript
format = function (arr, dialect) {
  var timeZone = null;
  // Make a clone of the array beacuse format modifies the passed args
  return SqlString.format(arr[0], arr.slice(1), timeZone, dialect);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.formatNamedParameters"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>formatNamedParameters (sql, parameters, dialect)](#apidoc.element.sequelize.Utils.formatNamedParameters)
- description and source-code
```javascript
formatNamedParameters = function (sql, parameters, dialect) {
  var timeZone = null;
  return SqlString.formatNamedParameters(sql, parameters, timeZone, dialect);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.formatReferences"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>formatReferences (obj)](#apidoc.element.sequelize.Utils.formatReferences)
- description and source-code
```javascript
formatReferences = function (obj) {
  if (!_.isPlainObject(obj.references)) {
    deprecate('Non-object references property found. Support for that will be removed in version 4. Expected { references: { model
: "value", key: "key" } } instead of { references: "value", referencesKey: "key" }.');
    obj.references = { model: obj.references, key: obj.referencesKey, deferrable: obj.referencesDeferrable };
    obj.referencesKey = undefined;
    obj.referencesDeferrable = undefined;
  }

  return obj;
}
```
- example usage
```shell
...
    models[tableName] = model;

    for (var attrName in model.rawAttributes) {
      if (model.rawAttributes.hasOwnProperty(attrName)) {
        var attribute = model.rawAttributes[attrName];

        if (attribute.references) {
attribute = Utils.formatReferences(attribute);
dep       = attribute.references.model;

if (_.isObject(dep)) {
  dep = dep.schema + '.' + dep.tableName;
}

deps.push(dep);
...
```

#### <a name="apidoc.element.sequelize.Utils.inherit"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>inherit (SubClass, SuperClass)](#apidoc.element.sequelize.Utils.inherit)
- description and source-code
```javascript
inherit = function (SubClass, SuperClass) {
  if (SuperClass.constructor === Function) {
    // Normal Inheritance
    SubClass.prototype = new SuperClass();
    SubClass.prototype.constructor = SubClass;
    SubClass.prototype.parent = SuperClass.prototype;
  } else {
    // Pure Virtual Inheritance
    SubClass.prototype = SuperClass;
    SubClass.prototype.constructor = SubClass;
    SubClass.prototype.parent = SuperClass;
  }

  return SubClass;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.isColString"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>isColString (value)](#apidoc.element.sequelize.Utils.isColString)
- description and source-code
```javascript
isColString = function (value) {
  return typeof value === 'string' && value.substr(0, 1) === '$' && value.substr(value.length - 1, 1) === '$';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.isPrimitive"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>isPrimitive (val)](#apidoc.element.sequelize.Utils.isPrimitive)
- description and source-code
```javascript
isPrimitive = function (val) {
  return primitives.indexOf(typeof val) !== -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.json"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>json (conditionsOrPath, value)](#apidoc.element.sequelize.Utils.json)
- description and source-code
```javascript
json = function (conditionsOrPath, value) {
  if (Utils._.isObject(conditionsOrPath)) {
    this.conditions = conditionsOrPath;
  } else {
    this.path = conditionsOrPath;
    if (value) {
      this.value = value;
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.literal"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>literal (val)](#apidoc.element.sequelize.Utils.literal)
- description and source-code
```javascript
literal = function (val) {
  this.val = val;
}
```
- example usage
```shell
...
return sequelize.sync({
force: true,
logging: console.log
})
.then(() => {
  return User.findAll({
    attributes: {
      include: [[sequelize.literal('COUNT(*) OVER()'), 'totalCount']]
    }
  });
})
.then(console.log)
.finally(() => {
  sequelize.close();
});
...
```

#### <a name="apidoc.element.sequelize.Utils.lowercaseFirst"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>lowercaseFirst (s)](#apidoc.element.sequelize.Utils.lowercaseFirst)
- description and source-code
```javascript
lowercaseFirst = function (s) {
  return s[0].toLowerCase() + s.slice(1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.mapFinderOptions"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapFinderOptions (options, Model)](#apidoc.element.sequelize.Utils.mapFinderOptions)
- description and source-code
```javascript
mapFinderOptions = function (options, Model) {
  if (Model._hasVirtualAttributes && Array.isArray(options.attributes)) {
    options.attributes.forEach(function (attribute) {
      if (Model._isVirtualAttribute(attribute) && Model.rawAttributes[attribute].type.fields) {
        options.attributes = options.attributes.concat(Model.rawAttributes[attribute].type.fields);
      }
    }.bind(Model));
    options.attributes = _.without.apply(_, [options.attributes].concat(Model._virtualAttributes));
    options.attributes = _.uniq(options.attributes);
  }

  Utils.mapOptionFieldNames(options, Model);

  return options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.mapOptionFieldNames"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapOptionFieldNames (options, Model)](#apidoc.element.sequelize.Utils.mapOptionFieldNames)
- description and source-code
```javascript
mapOptionFieldNames = function (options, Model) {
  if (Array.isArray(options.attributes)) {
    options.attributes = options.attributes.map(function(attr) {
      // Object lookups will force any variable to strings, we don't want that for special objects etc
      if (typeof attr !== 'string') return attr;
      // Map attributes to aliased syntax attributes
      if (Model.rawAttributes[attr] && attr !== Model.rawAttributes[attr].field) {
        return [Model.rawAttributes[attr].field, attr];
      }
      return attr;
    });
  }

  if (options.where && _.isPlainObject(options.where)) {
    options.where = Utils.mapWhereFieldNames(options.where, Model);
  }

  if (Array.isArray(options.order)) {
    options.order.forEach(function(oGroup) {
      var OrderModel, attr, attrOffset;

      if (Array.isArray(oGroup)) {
        OrderModel = Model;

        // Check if we have ['attr', 'DESC'] or [Model, 'attr', 'DESC']
        if (typeof oGroup[oGroup.length - 2] === 'string') {
          attrOffset = 2;

        // Assume ['attr'], [Model, 'attr'] or [seq.fn('somefn', 1), 'DESC']
        } else {
          attrOffset = 1;
        }

        attr = oGroup[oGroup.length - attrOffset];
        if (oGroup.length > attrOffset) {
          OrderModel = oGroup[oGroup.length - (attrOffset + 1)];
          if (OrderModel.model) {
            OrderModel = OrderModel.model;
          }
        }

        if (OrderModel.rawAttributes && OrderModel.rawAttributes[attr] && attr !== OrderModel.rawAttributes[attr].field) {
          oGroup[oGroup.length - attrOffset] = OrderModel.rawAttributes[attr].field;
        }
      }
    });
  }

  return options;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.mapValueFieldNames"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapValueFieldNames (dataValues, fields, Model)](#apidoc.element.sequelize.Utils.mapValueFieldNames)
- description and source-code
```javascript
mapValueFieldNames = function (dataValues, fields, Model) {
  var values = {};

  fields.forEach(function(attr) {
    if (dataValues[attr] !== undefined && !Model._isVirtualAttribute(attr)) {
      // Field name mapping
      if (Model.rawAttributes[attr] && Model.rawAttributes[attr].field && Model.rawAttributes[attr].field !== attr) {
        values[Model.rawAttributes[attr].field] = dataValues[attr];
      } else {
        values[attr] = dataValues[attr];
      }
    }
  });

  return values;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.mapWhereFieldNames"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>mapWhereFieldNames (attributes, Model)](#apidoc.element.sequelize.Utils.mapWhereFieldNames)
- description and source-code
```javascript
mapWhereFieldNames = function (attributes, Model) {
  var attribute
    , rawAttribute;

  if (attributes) {
    for (attribute in attributes) {
      rawAttribute = Model.rawAttributes[attribute];

      if (rawAttribute && rawAttribute.field !== rawAttribute.fieldName) {
        attributes[rawAttribute.field] = attributes[attribute];
        delete attributes[attribute];
      }

      if (_.isPlainObject(attributes[attribute])) {
        attributes[attribute] = Utils.mapOptionFieldNames({
          where: attributes[attribute]
        }, Model).where;
      }

      if (Array.isArray(attributes[attribute])) {
        attributes[attribute] = attributes[attribute].map(function (where) {
          if (_.isPlainObject(where)) {
            return Utils.mapWhereFieldNames(where, Model);
          }

          return where;
        });
      }
    }
  }

  return attributes;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.merge"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>merge ()](#apidoc.element.sequelize.Utils.merge)
- description and source-code
```javascript
merge = function () {
  var result = {};

  Array.prototype.slice.apply(arguments).forEach(function (obj) {
    _.forOwn(obj, function (value, key) {
      if (typeof value !== 'undefined') {
        if (!result[key]) {
          result[key] = value;
        } else if (_.isPlainObject(value) && _.isPlainObject(result[key])) {
          result[key] = Utils.merge(result[key], value);
        } else if (Array.isArray(value) && Array.isArray(result[key])) {
          result[key] = value.concat(result[key]);
        } else {
          result[key] = value;
        }
      }
    });
  });

  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.mergeDefaults"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>mergeDefaults (a, b)](#apidoc.element.sequelize.Utils.mergeDefaults)
- description and source-code
```javascript
mergeDefaults = function (a, b) {
  return _.mergeWith(a, b, function (objectValue, sourceValue) {
    // If it's an object, let _ handle it this time, we will be called again for each property
    if (!this._.isPlainObject(objectValue) && objectValue !== undefined) {
      return objectValue;
    }
  }.bind(this));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.now"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>now (dialect)](#apidoc.element.sequelize.Utils.now)
- description and source-code
```javascript
now = function (dialect) {
  var now = new Date();
  if (['postgres', 'sqlite'].indexOf(dialect) === -1) {
    now.setMilliseconds(0);
  }
  return now;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.pluralize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>pluralize (s)](#apidoc.element.sequelize.Utils.pluralize)
- description and source-code
```javascript
pluralize = function (s) {
  return inflection.pluralize(s);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.removeCommentsFromFunctionString"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>removeCommentsFromFunctionString (s)](#apidoc.element.sequelize.Utils.removeCommentsFromFunctionString)
- description and source-code
```javascript
removeCommentsFromFunctionString = function (s) {
  s = s.replace(/\s*(\/\/.*)/g, '');
  s = s.replace(/(\/\*[\n\r\s\S]*?\*\/)/mg, '');

  return s;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.removeNullValuesFromHash"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>removeNullValuesFromHash (hash, omitNull, options)](#apidoc.element.sequelize.Utils.removeNullValuesFromHash)
- description and source-code
```javascript
removeNullValuesFromHash = function (hash, omitNull, options) {
  var result = hash;

  options = options || {};
  options.allowNull = options.allowNull || [];

  if (omitNull) {
    var _hash = {};

    Utils._.forIn(hash, function(val, key) {
      if (options.allowNull.indexOf(key) > -1 || key.match(/Id$/) || ((val !== null) && (val !== undefined))) {
        _hash[key] = val;
      }
    });

    result = _hash;
  }

  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.removeTicks"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>removeTicks (s, tickChar)](#apidoc.element.sequelize.Utils.removeTicks)
- description and source-code
```javascript
removeTicks = function (s, tickChar) {
  tickChar = tickChar || Utils.TICK_CHAR;
  return s.replace(new RegExp(tickChar, 'g'), '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.singularize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>singularize (s)](#apidoc.element.sequelize.Utils.singularize)
- description and source-code
```javascript
singularize = function (s) {
  return inflection.singularize(s);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.sliceArgs"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>sliceArgs (args, begin)](#apidoc.element.sequelize.Utils.sliceArgs)
- description and source-code
```javascript
sliceArgs = function (args, begin) {
  begin = begin || 0;
  var tmp = new Array(args.length - begin);
  for (var i = begin; i < args.length; ++i) {
      tmp[i - begin] = args[i];
  }
  return tmp;
}
```
- example usage
```shell
...

return hooks;
  },

  runHooks: function(hooks) {
var self = this
  , fn
  , fnArgs = Utils.sliceArgs(arguments, 1)
  , hookType;

if (typeof fnArgs[fnArgs.length - 1] === 'function') {
  fn = fnArgs.pop();
}

if (typeof hooks === 'string') {
...
```

#### <a name="apidoc.element.sequelize.Utils.spliceStr"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>spliceStr (str, index, count, add)](#apidoc.element.sequelize.Utils.spliceStr)
- description and source-code
```javascript
spliceStr = function (str, index, count, add) {
  return str.slice(0, index) + add + str.slice(index + count);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.stack"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>stack ()](#apidoc.element.sequelize.Utils.stack)
- description and source-code
```javascript
function _stackGrabber() {
  var orig = Error.prepareStackTrace;
  Error.prepareStackTrace = function(_, stack) { return stack; };
  var err = new Error();
  Error.captureStackTrace(err, _stackGrabber);
  var errStack = err.stack;
  Error.prepareStackTrace = orig;
  return errStack;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.tick"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>tick (func)](#apidoc.element.sequelize.Utils.tick)
- description and source-code
```javascript
tick = function (func) {
  var tick = (global.hasOwnProperty('setImmediate') ? global.setImmediate : process.nextTick);
  tick(func);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.toDefaultValue"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>toDefaultValue (value)](#apidoc.element.sequelize.Utils.toDefaultValue)
- description and source-code
```javascript
toDefaultValue = function (value) {
  if (typeof value === 'function') {
    var tmp = value();
    if (tmp instanceof DataTypes.ABSTRACT) {
      return tmp.toSql();
    } else {
      return tmp;
    }
  } else if (value instanceof DataTypes.UUIDV1) {
    return uuid.v1();
  } else if (value instanceof DataTypes.UUIDV4) {
    return uuid.v4();
  } else if (value instanceof DataTypes.NOW) {
    return Utils.now();
  } else if(_.isPlainObject(value) || _.isArray(value)) {
    return _.clone(value);
  } else {
    return value;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.underscoredIf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>underscoredIf (string, condition)](#apidoc.element.sequelize.Utils.underscoredIf)
- description and source-code
```javascript
underscoredIf = function (string, condition) {
  var result = string;

  if (condition) {
    result = inflection.underscore(string);
  }

  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.uppercaseFirst"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>uppercaseFirst (s)](#apidoc.element.sequelize.Utils.uppercaseFirst)
- description and source-code
```javascript
uppercaseFirst = function (s) {
  return s[0].toUpperCase() + s.slice(1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.validateParameter"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>validateParameter (value, expectation, options)](#apidoc.element.sequelize.Utils.validateParameter)
- description and source-code
```javascript
function check(value, expectation, options) {
  options = _.extend({
    deprecated: false,
    index: null,
    method: null,
    optional: false
  }, options || {});

  if (!value && options.optional) {
    return true;
  }

  if (value === undefined) {
    throw new Error('No value has been passed.');
  }

  if (expectation === undefined) {
    throw new Error('No expectation has been passed.');
  }

  return false
    || validateDeprecation(value, expectation, options)
    || validate(value, expectation, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.where"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>where (attribute, comparator, logic)](#apidoc.element.sequelize.Utils.where)
- description and source-code
```javascript
where = function (attribute, comparator, logic) {
  if (logic === undefined) {
    logic = comparator;
    comparator = '=';
  }

  this.attribute = attribute;
  this.comparator = comparator;
  this.logic = logic;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Utils._"></a>[module sequelize.Utils._](#apidoc.module.sequelize.Utils._)

#### <a name="apidoc.element.sequelize.Utils._.add"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>add (value, other)](#apidoc.element.sequelize.Utils._.add)
- description and source-code
```javascript
add = function (value, other) {
  var result;
  if (value === undefined && other === undefined) {
    return 0;
  }
  if (value !== undefined) {
    result = value;
  }
  if (other !== undefined) {
    if (result === undefined) {
      return other;
    }
    if (typeof value == 'string' || typeof other == 'string') {
      value = baseToString(value);
      other = baseToString(other);
    } else {
      value = baseToNumber(value);
      other = baseToNumber(other);
    }
    result = operator(value, other);
  }
  return result;
}
```
- example usage
```shell
...
    }
  }

  deps = deps.filter(function(dep) {
    return tableName !== dep;
  });

  sorter.add(tableName, deps);
});

sorted = sorter.sort();
if (options.reverse) {
  sorted = sorted.reverse();
}
sorted.forEach(function(name) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.after"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>after (n, func)](#apidoc.element.sequelize.Utils._.after)
- description and source-code
```javascript
function after(n, func) {
  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  n = toInteger(n);
  return function() {
    if (--n < 1) {
      return func.apply(this, arguments);
    }
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.ary"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>ary (func, n, guard)](#apidoc.element.sequelize.Utils._.ary)
- description and source-code
```javascript
function ary(func, n, guard) {
  n = guard ? undefined : n;
  n = (func && n == null) ? func.length : n;
  return createWrapper(func, ARY_FLAG, undefined, undefined, undefined, undefined, n);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.assign"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assign ()](#apidoc.element.sequelize.Utils._.assign)
- description and source-code
```javascript
assign = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
...
  });
}
};

QueryInterface.prototype.showAllSchemas = function(options) {
var self = this;

options = _.assign({}, options, {
  raw: true,
  type: this.sequelize.QueryTypes.SELECT
});

var showSchemasSql = self.QueryGenerator.showSchemasQuery();

return this.sequelize.query(showSchemasSql, options).then(function(schemaNames) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.assignIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assignIn ()](#apidoc.element.sequelize.Utils._.assignIn)
- description and source-code
```javascript
assignIn = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.assignInWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assignInWith ()](#apidoc.element.sequelize.Utils._.assignInWith)
- description and source-code
```javascript
assignInWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.assignWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>assignWith ()](#apidoc.element.sequelize.Utils._.assignWith)
- description and source-code
```javascript
assignWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.at"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>at ()](#apidoc.element.sequelize.Utils._.at)
- description and source-code
```javascript
at = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.attempt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>attempt ()](#apidoc.element.sequelize.Utils._.attempt)
- description and source-code
```javascript
attempt = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.before"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>before (n, func)](#apidoc.element.sequelize.Utils._.before)
- description and source-code
```javascript
function before(n, func) {
  var result;
  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  n = toInteger(n);
  return function() {
    if (--n > 0) {
      result = func.apply(this, arguments);
    }
    if (n <= 1) {
      func = undefined;
    }
    return result;
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.bind"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>bind ()](#apidoc.element.sequelize.Utils._.bind)
- description and source-code
```javascript
bind = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
...

   var validatorPromise = self._invokeBuiltinValidator(value, test, validatorType, field);
   // errors are handled in settling, stub this
   validatorPromise.catch(function() {});
   validators.push(validatorPromise.reflect());
 });

 return Promise.all(validators).then(this._handleReflectedResult.bind(this, field));
};

/**
* Prepare and invoke a custom validator.
*
* @param {Function} validator The custom validator.
* @param {string} validatorType the custom validator type (name).
...
```

#### <a name="apidoc.element.sequelize.Utils._.bindAll"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>bindAll ()](#apidoc.element.sequelize.Utils._.bindAll)
- description and source-code
```javascript
bindAll = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.bindKey"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>bindKey ()](#apidoc.element.sequelize.Utils._.bindKey)
- description and source-code
```javascript
bindKey = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.camelCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>camelCase (string)](#apidoc.element.sequelize.Utils._.camelCase)
- description and source-code
```javascript
camelCase = function (string) {
  return arrayReduce(words(deburr(string).replace(reApos, '')), callback, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.capitalize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>capitalize (string)](#apidoc.element.sequelize.Utils._.capitalize)
- description and source-code
```javascript
function capitalize(string) {
  return upperFirst(toString(string).toLowerCase());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.castArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>castArray ()](#apidoc.element.sequelize.Utils._.castArray)
- description and source-code
```javascript
function castArray() {
  if (!arguments.length) {
    return [];
  }
  var value = arguments[0];
  return isArray(value) ? value : [value];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.ceil"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>ceil (number, precision)](#apidoc.element.sequelize.Utils._.ceil)
- description and source-code
```javascript
ceil = function (number, precision) {
  number = toNumber(number);
  precision = toInteger(precision);
  if (precision) {
    // Shift with exponential notation to avoid floating-point issues.
    // See [MDN](https://mdn.io/round#Examples) for more details.
    var pair = (toString(number) + 'e').split('e'),
        value = func(pair[0] + 'e' + (+pair[1] + precision));

    pair = (toString(value) + 'e').split('e');
    return +(pair[0] + 'e' + (+pair[1] - precision));
  }
  return func(number);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.chain"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>chain (value)](#apidoc.element.sequelize.Utils._.chain)
- description and source-code
```javascript
function chain(value) {
  var result = lodash(value);
  result.__chain__ = true;
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.chunk"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>chunk (array, size, guard)](#apidoc.element.sequelize.Utils._.chunk)
- description and source-code
```javascript
function chunk(array, size, guard) {
  if ((guard ? isIterateeCall(array, size, guard) : size === undefined)) {
    size = 1;
  } else {
    size = nativeMax(toInteger(size), 0);
  }
  var length = array ? array.length : 0;
  if (!length || size < 1) {
    return [];
  }
  var index = 0,
      resIndex = 0,
      result = Array(nativeCeil(length / size));

  while (index < length) {
    result[resIndex++] = baseSlice(array, index, (index += size));
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.clamp"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>clamp (number, lower, upper)](#apidoc.element.sequelize.Utils._.clamp)
- description and source-code
```javascript
function clamp(number, lower, upper) {
  if (upper === undefined) {
    upper = lower;
    lower = undefined;
  }
  if (upper !== undefined) {
    upper = toNumber(upper);
    upper = upper === upper ? upper : 0;
  }
  if (lower !== undefined) {
    lower = toNumber(lower);
    lower = lower === lower ? lower : 0;
  }
  return baseClamp(toNumber(number), lower, upper);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.clone"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>clone (value)](#apidoc.element.sequelize.Utils._.clone)
- description and source-code
```javascript
function clone(value) {
  return baseClone(value, false, true);
}
```
- example usage
```shell
...
 * The Main Instance Validator.
 *
 * @param {Instance} modelInstance The model instance.
 * @param {Object} options A dict with options.
 * @constructor
 */
function InstanceValidator(modelInstance, options) {
options = _.clone(options) || {};

if (options.fields && !options.skip) {
  options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
}

// assign defined and default options
this.options = Utils._.defaults(options, {
...
```

#### <a name="apidoc.element.sequelize.Utils._.cloneDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cloneDeep (value)](#apidoc.element.sequelize.Utils._.cloneDeep)
- description and source-code
```javascript
function cloneDeep(value) {
  return baseClone(value, true, true);
}
```
- example usage
```shell
...
// testhint argsConform.end

if (!rawTablename) {
  // Map for backwards compat
  rawTablename = tableName;
}

options = Utils.cloneDeep(options);
options.fields = attributes;
var sql = this.QueryGenerator.addIndexQuery(tableName, options, rawTablename);
return this.sequelize.query(sql, _.assign({}, options, { supportsSearchPath: false }));
};

QueryInterface.prototype.showIndex = function(tableName, options) {
var sql = this.QueryGenerator.showIndexesQuery(tableName, options);
...
```

#### <a name="apidoc.element.sequelize.Utils._.cloneDeepWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cloneDeepWith (value, customizer)](#apidoc.element.sequelize.Utils._.cloneDeepWith)
- description and source-code
```javascript
function cloneDeepWith(value, customizer) {
  return baseClone(value, true, true, customizer);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.cloneWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cloneWith (value, customizer)](#apidoc.element.sequelize.Utils._.cloneWith)
- description and source-code
```javascript
function cloneWith(value, customizer) {
  return baseClone(value, false, true, customizer);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.compact"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>compact (array)](#apidoc.element.sequelize.Utils._.compact)
- description and source-code
```javascript
function compact(array) {
  var index = -1,
      length = array ? array.length : 0,
      resIndex = 0,
      result = [];

  while (++index < length) {
    var value = array[index];
    if (value) {
      result[resIndex++] = value;
    }
  }
  return result;
}
```
- example usage
```shell
...
    var result = {};

    tableNames.forEach(function(tableName, i) {
      if (Utils._.isObject(tableName)) {
        tableName = tableName.schema + '.' + tableName.tableName;
      }

      result[tableName] = Utils._.compact(results[i]).map(function(r) {
        return r.constraint_name;
      });
    });

    return result;
  });
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.concat"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>concat ()](#apidoc.element.sequelize.Utils._.concat)
- description and source-code
```javascript
function concat() {
  var length = arguments.length,
      args = Array(length ? length - 1 : 0),
      array = arguments[0],
      index = length;

  while (index--) {
    args[index - 1] = arguments[index];
  }
  return length
    ? arrayPush(isArray(array) ? copyArray(array) : [array], baseFlatten(args, 1))
    : [];
}
```
- example usage
```shell
...
  replaceHookAliases: function(hooks) {
var realHookName;

Utils._.each(hooks, function(hooksArray, name) {
  // Does an alias for this hook name exist?
  if (realHookName = hookAliases[name]) {
    // Add the hooks to the actual hook
    hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

    // Delete the alias
    delete hooks[name];
  }
});

return hooks;
...
```

#### <a name="apidoc.element.sequelize.Utils._.cond"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>cond (pairs)](#apidoc.element.sequelize.Utils._.cond)
- description and source-code
```javascript
function cond(pairs) {
  var length = pairs ? pairs.length : 0,
      toIteratee = getIteratee();

  pairs = !length ? [] : arrayMap(pairs, function(pair) {
    if (typeof pair[1] != 'function') {
      throw new TypeError(FUNC_ERROR_TEXT);
    }
    return [toIteratee(pair[0]), pair[1]];
  });

  return rest(function(args) {
    var index = -1;
    while (++index < length) {
      var pair = pairs[index];
      if (apply(pair[0], this, args)) {
        return apply(pair[1], this, args);
      }
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.conforms"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>conforms (source)](#apidoc.element.sequelize.Utils._.conforms)
- description and source-code
```javascript
function conforms(source) {
  return baseConforms(baseClone(source, true));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.constant"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>constant (value)](#apidoc.element.sequelize.Utils._.constant)
- description and source-code
```javascript
function constant(value) {
  return function() {
    return value;
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.countBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>countBy (collection, iteratee)](#apidoc.element.sequelize.Utils._.countBy)
- description and source-code
```javascript
countBy = function (collection, iteratee) {
  var func = isArray(collection) ? arrayAggregator : baseAggregator,
      accumulator = initializer ? initializer() : {};

  return func(collection, setter, getIteratee(iteratee), accumulator);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.create"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>create (prototype, properties)](#apidoc.element.sequelize.Utils._.create)
- description and source-code
```javascript
function create(prototype, properties) {
  var result = baseCreate(prototype);
  return properties ? baseAssign(result, properties) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.curry"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>curry (func, arity, guard)](#apidoc.element.sequelize.Utils._.curry)
- description and source-code
```javascript
function curry(func, arity, guard) {
  arity = guard ? undefined : arity;
  var result = createWrapper(func, CURRY_FLAG, undefined, undefined, undefined, undefined, undefined, arity);
  result.placeholder = curry.placeholder;
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.curryRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>curryRight (func, arity, guard)](#apidoc.element.sequelize.Utils._.curryRight)
- description and source-code
```javascript
function curryRight(func, arity, guard) {
  arity = guard ? undefined : arity;
  var result = createWrapper(func, CURRY_RIGHT_FLAG, undefined, undefined, undefined, undefined, undefined, arity);
  result.placeholder = curryRight.placeholder;
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.debounce"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>debounce (func, wait, options)](#apidoc.element.sequelize.Utils._.debounce)
- description and source-code
```javascript
function debounce(func, wait, options) {
  var lastArgs,
      lastThis,
      maxWait,
      result,
      timerId,
      lastCallTime = 0,
      lastInvokeTime = 0,
      leading = false,
      maxing = false,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  wait = toNumber(wait) || 0;
  if (isObject(options)) {
    leading = !!options.leading;
    maxing = 'maxWait' in options;
    maxWait = maxing ? nativeMax(toNumber(options.maxWait) || 0, wait) : maxWait;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }

  function invokeFunc(time) {
    var args = lastArgs,
        thisArg = lastThis;

    lastArgs = lastThis = undefined;
    lastInvokeTime = time;
    result = func.apply(thisArg, args);
    return result;
  }

  function leadingEdge(time) {
    // Reset any 'maxWait' timer.
    lastInvokeTime = time;
    // Start the timer for the trailing edge.
    timerId = setTimeout(timerExpired, wait);
    // Invoke the leading edge.
    return leading ? invokeFunc(time) : result;
  }

  function remainingWait(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime,
        result = wait - timeSinceLastCall;

    return maxing ? nativeMin(result, maxWait - timeSinceLastInvoke) : result;
  }

  function shouldInvoke(time) {
    var timeSinceLastCall = time - lastCallTime,
        timeSinceLastInvoke = time - lastInvokeTime;

    // Either this is the first call, activity has stopped and we're at the
    // trailing edge, the system time has gone backwards and we're treating
    // it as the trailing edge, or we've hit the 'maxWait' limit.
    return (!lastCallTime || (timeSinceLastCall >= wait) ||
      (timeSinceLastCall < 0) || (maxing && timeSinceLastInvoke >= maxWait));
  }

  function timerExpired() {
    var time = now();
    if (shouldInvoke(time)) {
      return trailingEdge(time);
    }
    // Restart the timer.
    timerId = setTimeout(timerExpired, remainingWait(time));
  }

  function trailingEdge(time) {
    clearTimeout(timerId);
    timerId = undefined;

    // Only invoke if we have 'lastArgs' which means 'func' has been
    // debounced at least once.
    if (trailing && lastArgs) {
      return invokeFunc(time);
    }
    lastArgs = lastThis = undefined;
    return result;
  }

  function cancel() {
    if (timerId !== undefined) {
      clearTimeout(timerId);
    }
    lastCallTime = lastInvokeTime = 0;
    lastArgs = lastThis = timerId = undefined;
  }

  function flush() {
    return timerId === undefined ? result : trailingEdge(now());
  }

  function debounced() {
    var time = now(),
        isInvoking = shouldInvoke(time);

    lastArgs = arguments;
    lastThis = this;
    lastCallTime = time;

    if (isInvoking) {
      if (timerId === undefined) {
        return leadingEdge(lastCallTime);
      }
      if (maxing) {
        // Handle invocations in a tight loop.
        clearTimeout(timerId);
        timerId = setTimeout(timerExpired, wait);
        return invokeFunc(lastCallTime);
      }
    }
    if (timerId === undefined) {
      timerId = setTimeout(timerExpired, wait);
    }
    return result;
  }
  debounced.cancel = cancel;
  debounced.flush = flush;
  return debounced;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.deburr"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>deburr (string)](#apidoc.element.sequelize.Utils._.deburr)
- description and source-code
```javascript
function deburr(string) {
  string = toString(string);
  return string && string.replace(reLatin1, deburrLetter).replace(reComboMark, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.defaults"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>defaults ()](#apidoc.element.sequelize.Utils._.defaults)
- description and source-code
```javascript
defaults = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
...
options = _.clone(options) || {};

if (options.fields && !options.skip) {
  options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
}

// assign defined and default options
this.options = Utils._.defaults(options, {
  skip: []
});

this.modelInstance = modelInstance;

/**
 * Exposes a reference to validator.js. This allows you to add custom validations using 'validator.extend'
...
```

#### <a name="apidoc.element.sequelize.Utils._.defaultsDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>defaultsDeep ()](#apidoc.element.sequelize.Utils._.defaultsDeep)
- description and source-code
```javascript
defaultsDeep = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.defer"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>defer ()](#apidoc.element.sequelize.Utils._.defer)
- description and source-code
```javascript
defer = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.delay"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>delay ()](#apidoc.element.sequelize.Utils._.delay)
- description and source-code
```javascript
delay = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.difference"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>difference ()](#apidoc.element.sequelize.Utils._.difference)
- description and source-code
```javascript
difference = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
...
 * @param {Object} options A dict with options.
 * @constructor
 */
function InstanceValidator(modelInstance, options) {
options = _.clone(options) || {};

if (options.fields && !options.skip) {
  options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
}

// assign defined and default options
this.options = Utils._.defaults(options, {
  skip: []
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.differenceBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>differenceBy ()](#apidoc.element.sequelize.Utils._.differenceBy)
- description and source-code
```javascript
differenceBy = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.differenceWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>differenceWith ()](#apidoc.element.sequelize.Utils._.differenceWith)
- description and source-code
```javascript
differenceWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.divide"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>divide (value, other)](#apidoc.element.sequelize.Utils._.divide)
- description and source-code
```javascript
divide = function (value, other) {
  var result;
  if (value === undefined && other === undefined) {
    return 0;
  }
  if (value !== undefined) {
    result = value;
  }
  if (other !== undefined) {
    if (result === undefined) {
      return other;
    }
    if (typeof value == 'string' || typeof other == 'string') {
      value = baseToString(value);
      other = baseToString(other);
    } else {
      value = baseToNumber(value);
      other = baseToNumber(other);
    }
    result = operator(value, other);
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.drop"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>drop (array, n, guard)](#apidoc.element.sequelize.Utils._.drop)
- description and source-code
```javascript
function drop(array, n, guard) {
  var length = array ? array.length : 0;
  if (!length) {
    return [];
  }
  n = (guard || n === undefined) ? 1 : toInteger(n);
  return baseSlice(array, n < 0 ? 0 : n, length);
}
```
- example usage
```shell
...
};

QueryInterface.prototype.dropAllSchemas = function(options) {
  options = options || {};

  var self = this;
  if (!this.QueryGenerator._dialect.supports.schemas) {
    return this.sequelize.drop(options);
  } else {
    return this.showAllSchemas(options).map(function(schemaName) {
      return self.dropSchema(schemaName, options);
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.dropRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>dropRight (array, n, guard)](#apidoc.element.sequelize.Utils._.dropRight)
- description and source-code
```javascript
function dropRight(array, n, guard) {
  var length = array ? array.length : 0;
  if (!length) {
    return [];
  }
  n = (guard || n === undefined) ? 1 : toInteger(n);
  n = length - n;
  return baseSlice(array, 0, n < 0 ? 0 : n);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.dropRightWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>dropRightWhile (array, predicate)](#apidoc.element.sequelize.Utils._.dropRightWhile)
- description and source-code
```javascript
function dropRightWhile(array, predicate) {
  return (array && array.length)
    ? baseWhile(array, getIteratee(predicate, 3), true, true)
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.dropWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>dropWhile (array, predicate)](#apidoc.element.sequelize.Utils._.dropWhile)
- description and source-code
```javascript
function dropWhile(array, predicate) {
  return (array && array.length)
    ? baseWhile(array, getIteratee(predicate, 3), true)
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.each"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>each (collection, iteratee)](#apidoc.element.sequelize.Utils._.each)
- description and source-code
```javascript
function forEach(collection, iteratee) {
  var func = isArray(collection) ? arrayEach : baseEach;
  return func(collection, getIteratee(iteratee, 3));
}
```
- example usage
```shell
...
  beforeConnection: 'beforeConnect'
};

var Hooks = {
  replaceHookAliases: function(hooks) {
    var realHookName;

    Utils._.each(hooks, function(hooksArray, name) {
      // Does an alias for this hook name exist?
      if (realHookName = hookAliases[name]) {
// Add the hooks to the actual hook
hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

// Delete the alias
delete hooks[name];
...
```

#### <a name="apidoc.element.sequelize.Utils._.eachRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>eachRight (collection, iteratee)](#apidoc.element.sequelize.Utils._.eachRight)
- description and source-code
```javascript
function forEachRight(collection, iteratee) {
  var func = isArray(collection) ? arrayEachRight : baseEachRight;
  return func(collection, getIteratee(iteratee, 3));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.endsWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>endsWith (string, target, position)](#apidoc.element.sequelize.Utils._.endsWith)
- description and source-code
```javascript
function endsWith(string, target, position) {
  string = toString(string);
  target = baseToString(target);

  var length = string.length;
  position = position === undefined
    ? length
    : baseClamp(toInteger(position), 0, length);

  position -= target.length;
  return position >= 0 && string.indexOf(target, position) == position;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.entries"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>entries (object)](#apidoc.element.sequelize.Utils._.entries)
- description and source-code
```javascript
entries = function (object) {
  var tag = getTag(object);
  if (tag == mapTag) {
    return mapToArray(object);
  }
  if (tag == setTag) {
    return setToPairs(object);
  }
  return baseToPairs(object, keysFunc(object));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.entriesIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>entriesIn (object)](#apidoc.element.sequelize.Utils._.entriesIn)
- description and source-code
```javascript
entriesIn = function (object) {
  var tag = getTag(object);
  if (tag == mapTag) {
    return mapToArray(object);
  }
  if (tag == setTag) {
    return setToPairs(object);
  }
  return baseToPairs(object, keysFunc(object));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.eq"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>eq (value, other)](#apidoc.element.sequelize.Utils._.eq)
- description and source-code
```javascript
function eq(value, other) {
  return value === other || (value !== value && other !== other);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.escape"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>escape (string)](#apidoc.element.sequelize.Utils._.escape)
- description and source-code
```javascript
function escape(string) {
  string = toString(string);
  return (string && reHasUnescapedHtml.test(string))
    ? string.replace(reUnescapedHtml, escapeHtmlChar)
    : string;
}
```
- example usage
```shell
...
return this.QueryGenerator.quoteIdentifiers(identifiers, force);
};

/**
 * Escape a value (e.g. a string, number or date)
 */
QueryInterface.prototype.escape = function(value) {
return this.QueryGenerator.escape(value);
};

QueryInterface.prototype.setAutocommit = function(transaction, value, options) {
if (!transaction || !(transaction instanceof Transaction)) {
  throw new Error('Unable to set autocommit for a transaction without transaction object!');
}
if (transaction.parent) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.escapeRegExp"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>escapeRegExp (string)](#apidoc.element.sequelize.Utils._.escapeRegExp)
- description and source-code
```javascript
function escapeRegExp(string) {
  string = toString(string);
  return (string && reHasRegExpChar.test(string))
    ? string.replace(reRegExpChar, '\\$&')
    : string;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.every"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>every (collection, predicate, guard)](#apidoc.element.sequelize.Utils._.every)
- description and source-code
```javascript
function every(collection, predicate, guard) {
  var func = isArray(collection) ? arrayEvery : baseEvery;
  if (guard && isIterateeCall(collection, predicate, guard)) {
    predicate = undefined;
  }
  return func(collection, getIteratee(predicate, 3));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.extend"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>extend ()](#apidoc.element.sequelize.Utils._.extend)
- description and source-code
```javascript
extend = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.extendWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>extendWith ()](#apidoc.element.sequelize.Utils._.extendWith)
- description and source-code
```javascript
extendWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.fill"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>fill (array, value, start, end)](#apidoc.element.sequelize.Utils._.fill)
- description and source-code
```javascript
function fill(array, value, start, end) {
  var length = array ? array.length : 0;
  if (!length) {
    return [];
  }
  if (start && typeof start != 'number' && isIterateeCall(array, value, start)) {
    start = 0;
    end = length;
  }
  return baseFill(array, value, start, end);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.filter"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>filter (collection, predicate)](#apidoc.element.sequelize.Utils._.filter)
- description and source-code
```javascript
function filter(collection, predicate) {
  var func = isArray(collection) ? arrayFilter : baseFilter;
  return func(collection, getIteratee(predicate, 3));
}
```
- example usage
```shell
...
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
  }

  return typeof hook === 'object' && hook.name !== name;
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.find"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>find (collection, predicate)](#apidoc.element.sequelize.Utils._.find)
- description and source-code
```javascript
function find(collection, predicate) {
  predicate = getIteratee(predicate, 3);
  if (isArray(collection)) {
    var index = baseFindIndex(collection, predicate);
    return index > -1 ? collection[index] : undefined;
  }
  return baseFind(collection, predicate, baseEach);
}
```
- example usage
```shell
...

QueryInterface.prototype.bulkUpdate = function(tableName, values, identifier, options, attributes) {
options = Utils.cloneDeep(options);
if (typeof identifier === 'object') identifier = Utils.cloneDeep(identifier);

var sql = this.QueryGenerator.updateQuery(tableName, values, identifier, options, attributes)
  , table = Utils._.isObject(tableName) ? tableName : { tableName: tableName }
  , model = Utils._.find(this.sequelize.modelManager.models, { tableName: table.tableName });

options.model = model;
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.delete = function(instance, tableName, identifier, options) {
var self = this
...
```

#### <a name="apidoc.element.sequelize.Utils._.findIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findIndex (array, predicate)](#apidoc.element.sequelize.Utils._.findIndex)
- description and source-code
```javascript
function findIndex(array, predicate) {
  return (array && array.length)
    ? baseFindIndex(array, getIteratee(predicate, 3))
    : -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.findKey"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findKey (object, predicate)](#apidoc.element.sequelize.Utils._.findKey)
- description and source-code
```javascript
function findKey(object, predicate) {
  return baseFind(object, getIteratee(predicate, 3), baseForOwn, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.findLast"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findLast (collection, predicate)](#apidoc.element.sequelize.Utils._.findLast)
- description and source-code
```javascript
function findLast(collection, predicate) {
  predicate = getIteratee(predicate, 3);
  if (isArray(collection)) {
    var index = baseFindIndex(collection, predicate, true);
    return index > -1 ? collection[index] : undefined;
  }
  return baseFind(collection, predicate, baseEachRight);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.findLastIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findLastIndex (array, predicate)](#apidoc.element.sequelize.Utils._.findLastIndex)
- description and source-code
```javascript
function findLastIndex(array, predicate) {
  return (array && array.length)
    ? baseFindIndex(array, getIteratee(predicate, 3), true)
    : -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.findLastKey"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>findLastKey (object, predicate)](#apidoc.element.sequelize.Utils._.findLastKey)
- description and source-code
```javascript
function findLastKey(object, predicate) {
  return baseFind(object, getIteratee(predicate, 3), baseForOwnRight, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.first"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>first (array)](#apidoc.element.sequelize.Utils._.first)
- description and source-code
```javascript
function head(array) {
  return (array && array.length) ? array[0] : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flatMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatMap (collection, iteratee)](#apidoc.element.sequelize.Utils._.flatMap)
- description and source-code
```javascript
function flatMap(collection, iteratee) {
  return baseFlatten(map(collection, iteratee), 1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flatMapDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatMapDeep (collection, iteratee)](#apidoc.element.sequelize.Utils._.flatMapDeep)
- description and source-code
```javascript
function flatMapDeep(collection, iteratee) {
  return baseFlatten(map(collection, iteratee), INFINITY);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flatMapDepth"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatMapDepth (collection, iteratee, depth)](#apidoc.element.sequelize.Utils._.flatMapDepth)
- description and source-code
```javascript
function flatMapDepth(collection, iteratee, depth) {
  depth = depth === undefined ? 1 : toInteger(depth);
  return baseFlatten(map(collection, iteratee), depth);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flatten"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flatten (array)](#apidoc.element.sequelize.Utils._.flatten)
- description and source-code
```javascript
function flatten(array) {
  var length = array ? array.length : 0;
  return length ? baseFlatten(array, 1) : [];
}
```
- example usage
```shell
...
    raw: true,
    type: this.sequelize.QueryTypes.SELECT
  });

  var showSchemasSql = self.QueryGenerator.showSchemasQuery();

  return this.sequelize.query(showSchemasSql, options).then(function(schemaNames) {
    return Utils._.flatten(
      Utils._.map(schemaNames, function(value) {
        return (!!value.schema_name ? value.schema_name : value);
      })
    );
  });
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.flattenDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flattenDeep (array)](#apidoc.element.sequelize.Utils._.flattenDeep)
- description and source-code
```javascript
function flattenDeep(array) {
  var length = array ? array.length : 0;
  return length ? baseFlatten(array, INFINITY) : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flattenDepth"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flattenDepth (array, depth)](#apidoc.element.sequelize.Utils._.flattenDepth)
- description and source-code
```javascript
function flattenDepth(array, depth) {
  var length = array ? array.length : 0;
  if (!length) {
    return [];
  }
  depth = depth === undefined ? 1 : toInteger(depth);
  return baseFlatten(array, depth);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flip"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flip (func)](#apidoc.element.sequelize.Utils._.flip)
- description and source-code
```javascript
function flip(func) {
  return createWrapper(func, FLIP_FLAG);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.floor"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>floor (number, precision)](#apidoc.element.sequelize.Utils._.floor)
- description and source-code
```javascript
floor = function (number, precision) {
  number = toNumber(number);
  precision = toInteger(precision);
  if (precision) {
    // Shift with exponential notation to avoid floating-point issues.
    // See [MDN](https://mdn.io/round#Examples) for more details.
    var pair = (toString(number) + 'e').split('e'),
        value = func(pair[0] + 'e' + (+pair[1] + precision));

    pair = (toString(value) + 'e').split('e');
    return +(pair[0] + 'e' + (+pair[1] - precision));
  }
  return func(number);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flow"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flow ()](#apidoc.element.sequelize.Utils._.flow)
- description and source-code
```javascript
flow = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.flowRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>flowRight ()](#apidoc.element.sequelize.Utils._.flowRight)
- description and source-code
```javascript
flowRight = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.forEach"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forEach (collection, iteratee)](#apidoc.element.sequelize.Utils._.forEach)
- description and source-code
```javascript
function forEach(collection, iteratee) {
  var func = isArray(collection) ? arrayEach : baseEach;
  return func(collection, getIteratee(iteratee, 3));
}
```
- example usage
```shell
...

if (!Array.isArray(hooks)) {
  hooks = hooks === undefined ? [] : [hooks];
}

// run hooks as sync functions if flagged as sync
if (hookTypes[hookType] && hookTypes[hookType].sync) {
  hooks.forEach(function(hook) {
    if (typeof hook === 'object') hook = hook.fn;
    return hook.apply(self, fnArgs);
  });
  return;
}

// run hooks async
...
```

#### <a name="apidoc.element.sequelize.Utils._.forEachRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forEachRight (collection, iteratee)](#apidoc.element.sequelize.Utils._.forEachRight)
- description and source-code
```javascript
function forEachRight(collection, iteratee) {
  var func = isArray(collection) ? arrayEachRight : baseEachRight;
  return func(collection, getIteratee(iteratee, 3));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.forIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forIn (object, iteratee)](#apidoc.element.sequelize.Utils._.forIn)
- description and source-code
```javascript
function forIn(object, iteratee) {
  return object == null
    ? object
    : baseFor(object, getIteratee(iteratee, 3), keysIn);
}
```
- example usage
```shell
...
 * @private
 */
InstanceValidator.prototype._builtinValidators = function() {
  var self = this;

  // promisify all attribute invocations
  var validators = [];
  Utils._.forIn(this.modelInstance.rawAttributes, function(rawAttribute, field) {
if (self.options.skip.indexOf(field) >= 0) {
  return;
}

var value = self.modelInstance.dataValues[field];

if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.forInRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forInRight (object, iteratee)](#apidoc.element.sequelize.Utils._.forInRight)
- description and source-code
```javascript
function forInRight(object, iteratee) {
  return object == null
    ? object
    : baseForRight(object, getIteratee(iteratee, 3), keysIn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.forOwn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forOwn (object, iteratee)](#apidoc.element.sequelize.Utils._.forOwn)
- description and source-code
```javascript
function forOwn(object, iteratee) {
  return object && baseForOwn(object, getIteratee(iteratee, 3));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.forOwnRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>forOwnRight (object, iteratee)](#apidoc.element.sequelize.Utils._.forOwnRight)
- description and source-code
```javascript
function forOwnRight(object, iteratee) {
  return object && baseForOwnRight(object, getIteratee(iteratee, 3));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.fromPairs"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>fromPairs (pairs)](#apidoc.element.sequelize.Utils._.fromPairs)
- description and source-code
```javascript
function fromPairs(pairs) {
  var index = -1,
      length = pairs ? pairs.length : 0,
      result = {};

  while (++index < length) {
    var pair = pairs[index];
    result[pair[0]] = pair[1];
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.functions"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>functions (object)](#apidoc.element.sequelize.Utils._.functions)
- description and source-code
```javascript
function functions(object) {
  return object == null ? [] : baseFunctions(object, keys(object));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.functionsIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>functionsIn (object)](#apidoc.element.sequelize.Utils._.functionsIn)
- description and source-code
```javascript
function functionsIn(object) {
  return object == null ? [] : baseFunctions(object, keysIn(object));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.get"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>get (object, path, defaultValue)](#apidoc.element.sequelize.Utils._.get)
- description and source-code
```javascript
function get(object, path, defaultValue) {
  var result = object == null ? undefined : baseGet(object, path);
  return result === undefined ? defaultValue : result;
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Utils._.groupBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>groupBy (collection, iteratee)](#apidoc.element.sequelize.Utils._.groupBy)
- description and source-code
```javascript
groupBy = function (collection, iteratee) {
  var func = isArray(collection) ? arrayAggregator : baseAggregator,
      accumulator = initializer ? initializer() : {};

  return func(collection, setter, getIteratee(iteratee), accumulator);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.gt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>gt (value, other)](#apidoc.element.sequelize.Utils._.gt)
- description and source-code
```javascript
gt = function (value, other) {
  if (!(typeof value == 'string' && typeof other == 'string')) {
    value = toNumber(value);
    other = toNumber(other);
  }
  return operator(value, other);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.gte"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>gte (value, other)](#apidoc.element.sequelize.Utils._.gte)
- description and source-code
```javascript
gte = function (value, other) {
  if (!(typeof value == 'string' && typeof other == 'string')) {
    value = toNumber(value);
    other = toNumber(other);
  }
  return operator(value, other);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.has"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>has (object, path)](#apidoc.element.sequelize.Utils._.has)
- description and source-code
```javascript
function has(object, path) {
  return object != null && hasPath(object, path, baseHas);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.hasIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>hasIn (object, path)](#apidoc.element.sequelize.Utils._.hasIn)
- description and source-code
```javascript
function hasIn(object, path) {
  return object != null && hasPath(object, path, baseHasIn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.head"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>head (array)](#apidoc.element.sequelize.Utils._.head)
- description and source-code
```javascript
function head(array) {
  return (array && array.length) ? array[0] : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.identity"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>identity (value)](#apidoc.element.sequelize.Utils._.identity)
- description and source-code
```javascript
function identity(value) {
  return value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.inRange"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>inRange (number, start, end)](#apidoc.element.sequelize.Utils._.inRange)
- description and source-code
```javascript
function inRange(number, start, end) {
  start = toNumber(start) || 0;
  if (end === undefined) {
    end = start;
    start = 0;
  } else {
    end = toNumber(end) || 0;
  }
  number = toNumber(number);
  return baseInRange(number, start, end);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.includes"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>includes (collection, value, fromIndex, guard)](#apidoc.element.sequelize.Utils._.includes)
- description and source-code
```javascript
function includes(collection, value, fromIndex, guard) {
  collection = isArrayLike(collection) ? collection : values(collection);
  fromIndex = (fromIndex && !guard) ? toInteger(fromIndex) : 0;

  var length = collection.length;
  if (fromIndex < 0) {
    fromIndex = nativeMax(length + fromIndex, 0);
  }
  return isString(collection)
    ? (fromIndex <= length && collection.indexOf(value, fromIndex) > -1)
    : (!!length && baseIndexOf(collection, value, fromIndex) > -1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.indexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>indexOf (array, value, fromIndex)](#apidoc.element.sequelize.Utils._.indexOf)
- description and source-code
```javascript
function indexOf(array, value, fromIndex) {
  var length = array ? array.length : 0;
  if (!length) {
    return -1;
  }
  fromIndex = toInteger(fromIndex);
  if (fromIndex < 0) {
    fromIndex = nativeMax(length + fromIndex, 0);
  }
  return baseIndexOf(array, value, fromIndex);
}
```
- example usage
```shell
...
 */
InstanceValidator.prototype._builtinValidators = function() {
  var self = this;

  // promisify all attribute invocations
  var validators = [];
  Utils._.forIn(this.modelInstance.rawAttributes, function(rawAttribute, field) {
if (self.options.skip.indexOf(field) >= 0) {
  return;
}

var value = self.modelInstance.dataValues[field];

if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
  // perform validations based on schema
...
```

#### <a name="apidoc.element.sequelize.Utils._.initial"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>initial (array)](#apidoc.element.sequelize.Utils._.initial)
- description and source-code
```javascript
function initial(array) {
  return dropRight(array, 1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.intersection"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>intersection ()](#apidoc.element.sequelize.Utils._.intersection)
- description and source-code
```javascript
intersection = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
...
      return field;
    });
    indexes.push(indexFields);
  }
});

indexes.forEach(function (index) {
  if (Utils._.intersection(attributes, index).length === index.length) {
    where = {};
    index.forEach(function (field) {
      where[field] = valuesByField[field];
    });
    wheres.push(where);
  }
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.intersectionBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>intersectionBy ()](#apidoc.element.sequelize.Utils._.intersectionBy)
- description and source-code
```javascript
intersectionBy = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.intersectionWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>intersectionWith ()](#apidoc.element.sequelize.Utils._.intersectionWith)
- description and source-code
```javascript
intersectionWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.invert"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invert (object, iteratee)](#apidoc.element.sequelize.Utils._.invert)
- description and source-code
```javascript
invert = function (object, iteratee) {
  return baseInverter(object, setter, toIteratee(iteratee), {});
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.invertBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invertBy (object, iteratee)](#apidoc.element.sequelize.Utils._.invertBy)
- description and source-code
```javascript
invertBy = function (object, iteratee) {
  return baseInverter(object, setter, toIteratee(iteratee), {});
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.invoke"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invoke ()](#apidoc.element.sequelize.Utils._.invoke)
- description and source-code
```javascript
invoke = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.invokeMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>invokeMap ()](#apidoc.element.sequelize.Utils._.invokeMap)
- description and source-code
```javascript
invokeMap = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isArguments"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArguments (value)](#apidoc.element.sequelize.Utils._.isArguments)
- description and source-code
```javascript
function isArguments(value) {
  // Safari 8.1 incorrectly makes 'arguments.callee' enumerable in strict mode.
  return isArrayLikeObject(value) && hasOwnProperty.call(value, 'callee') &&
    (!propertyIsEnumerable.call(value, 'callee') || objectToString.call(value) == argsTag);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArray ()](#apidoc.element.sequelize.Utils._.isArray)
- description and source-code
```javascript
function isArray() { [native code] }
```
- example usage
```shell
...
if (typeof fnArgs[fnArgs.length - 1] === 'function') {
  fn = fnArgs.pop();
}

if (typeof hooks === 'string') {
  hookType = hooks;
  hooks = this.options.hooks[hookType] || [];
  if (!Array.isArray(hooks)) hooks = hooks === undefined ? [] : [hooks];
  if (this.sequelize) hooks = hooks.concat(this.sequelize.options.hooks[hookType] || []);
}

if (!Array.isArray(hooks)) {
  hooks = hooks === undefined ? [] : [hooks];
}
...
```

#### <a name="apidoc.element.sequelize.Utils._.isArrayBuffer"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArrayBuffer (value)](#apidoc.element.sequelize.Utils._.isArrayBuffer)
- description and source-code
```javascript
function isArrayBuffer(value) {
  return isObjectLike(value) && objectToString.call(value) == arrayBufferTag;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isArrayLike"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArrayLike (value)](#apidoc.element.sequelize.Utils._.isArrayLike)
- description and source-code
```javascript
function isArrayLike(value) {
  return value != null && isLength(getLength(value)) && !isFunction(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isArrayLikeObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isArrayLikeObject (value)](#apidoc.element.sequelize.Utils._.isArrayLikeObject)
- description and source-code
```javascript
function isArrayLikeObject(value) {
  return isObjectLike(value) && isArrayLike(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isBoolean"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isBoolean (value)](#apidoc.element.sequelize.Utils._.isBoolean)
- description and source-code
```javascript
function isBoolean(value) {
  return value === true || value === false ||
    (isObjectLike(value) && objectToString.call(value) == boolTag);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isBuffer"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isBuffer (value)](#apidoc.element.sequelize.Utils._.isBuffer)
- description and source-code
```javascript
isBuffer = function (value) {
  return value instanceof Buffer;
}
```
- example usage
```shell
...

  if (rawAttribute.allowNull === false && ((value === null) || (value === undefined))) {
    error = new sequelizeError.ValidationErrorItem(field + ' cannot be null', 'notNull Violation', field, value);
    this.errors.push(error);
  }

  if (rawAttribute.type === DataTypes.STRING || rawAttribute.type instanceof DataTypes.STRING || rawAttribute.type === DataTypes
.TEXT || rawAttribute.type instanceof DataTypes.TEXT) {
    if (Array.isArray(value) || (_.isObject(value) && !value._isSequelizeMethod) && !Buffer.isBuffer(value)) {
      error = new sequelizeError.ValidationErrorItem(field + ' cannot be an array or an object', 'string violation', field, value
);
      this.errors.push(error);
    }
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.isDate"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isDate (value)](#apidoc.element.sequelize.Utils._.isDate)
- description and source-code
```javascript
function isDate(value) {
  return isObjectLike(value) && objectToString.call(value) == dateTag;
}
```
- example usage
```shell
...
  var dataType = options.dataType;

  if (dataType instanceof DataTypes.DECIMAL || dataType instanceof DataTypes.FLOAT) {
    result = parseFloat(result);
  } else if (dataType instanceof DataTypes.INTEGER || dataType instanceof DataTypes.BIGINT) {
    result = parseInt(result, 10);
  } else if (dataType instanceof DataTypes.DATE) {
    if (!Utils._.isNull(result) && !Utils._.isDate(result)) {
      result = new Date(result);
    }
  } else if (dataType instanceof DataTypes.STRING) {
    // Nothing to do, result is already a string.
  }
}
...
```

#### <a name="apidoc.element.sequelize.Utils._.isElement"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isElement (value)](#apidoc.element.sequelize.Utils._.isElement)
- description and source-code
```javascript
function isElement(value) {
  return !!value && value.nodeType === 1 && isObjectLike(value) && !isPlainObject(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isEmpty"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isEmpty (value)](#apidoc.element.sequelize.Utils._.isEmpty)
- description and source-code
```javascript
function isEmpty(value) {
  if (isArrayLike(value) &&
      (isArray(value) || isString(value) || isFunction(value.splice) ||
        isArguments(value) || isBuffer(value))) {
    return !value.length;
  }
  if (isObjectLike(value)) {
    var tag = getTag(value);
    if (tag == mapTag || tag == setTag) {
      return !value.size;
    }
  }
  for (var key in value) {
    if (hasOwnProperty.call(value, key)) {
      return false;
    }
  }
  return !(nonEnumShadows && keys(value).length);
}
```
- example usage
```shell
...
  return this.sequelize.query(
    sql,
    _.assign({}, options, { type: QueryTypes.DESCRIBE })
  ).then(function(data) {
    // If no data is returned from the query, then the table name may be wrong.
    // Query generators that use information_schema for retrieving table info will just return an empty result set,
    // it will not throw an error like built-ins do (e.g. DESCRIBE on MySql).
    if (Utils._.isEmpty(data)) {
      return Promise.reject('No description found for "' + tableName + '" table. Check the table name and schema; remember, they
 _are_ case sensitive.');
    } else {
      return Promise.resolve(data);
    }
  });
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.isEqual"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isEqual (value, other)](#apidoc.element.sequelize.Utils._.isEqual)
- description and source-code
```javascript
function isEqual(value, other) {
  return baseIsEqual(value, other);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isEqualWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isEqualWith (value, other, customizer)](#apidoc.element.sequelize.Utils._.isEqualWith)
- description and source-code
```javascript
function isEqualWith(value, other, customizer) {
  customizer = typeof customizer == 'function' ? customizer : undefined;
  var result = customizer ? customizer(value, other) : undefined;
  return result === undefined ? baseIsEqual(value, other, customizer) : !!result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isError"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isError (value)](#apidoc.element.sequelize.Utils._.isError)
- description and source-code
```javascript
function isError(value) {
  if (!isObjectLike(value)) {
    return false;
  }
  return (objectToString.call(value) == errorTag) ||
    (typeof value.message == 'string' && typeof value.name == 'string');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isFinite"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isFinite (value)](#apidoc.element.sequelize.Utils._.isFinite)
- description and source-code
```javascript
function isFinite(value) {
  return typeof value == 'number' && nativeIsFinite(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isFunction"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isFunction (value)](#apidoc.element.sequelize.Utils._.isFunction)
- description and source-code
```javascript
function isFunction(value) {
  // The use of 'Object#toString' avoids issues with the 'typeof' operator
  // in Safari 8 which returns 'object' for typed array and weak map constructors,
  // and PhantomJS 1.9 which returns 'function' for 'NodeList' instances.
  var tag = isObject(value) ? objectToString.call(value) : '';
  return tag == funcTag || tag == genTag;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isInteger (value)](#apidoc.element.sequelize.Utils._.isInteger)
- description and source-code
```javascript
function isInteger(value) {
  return typeof value == 'number' && value == toInteger(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isLength"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isLength (value)](#apidoc.element.sequelize.Utils._.isLength)
- description and source-code
```javascript
function isLength(value) {
  return typeof value == 'number' &&
    value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isMap (value)](#apidoc.element.sequelize.Utils._.isMap)
- description and source-code
```javascript
function isMap(value) {
  return isObjectLike(value) && getTag(value) == mapTag;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isMatch"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isMatch (object, source)](#apidoc.element.sequelize.Utils._.isMatch)
- description and source-code
```javascript
function isMatch(object, source) {
  return object === source || baseIsMatch(object, source, getMatchData(source));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isMatchWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isMatchWith (object, source, customizer)](#apidoc.element.sequelize.Utils._.isMatchWith)
- description and source-code
```javascript
function isMatchWith(object, source, customizer) {
  customizer = typeof customizer == 'function' ? customizer : undefined;
  return baseIsMatch(object, source, getMatchData(source), customizer);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isNaN"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNaN (value)](#apidoc.element.sequelize.Utils._.isNaN)
- description and source-code
```javascript
function isNaN(value) {
  // An 'NaN' primitive is the only value that is not equal to itself.
  // Perform the 'toStringTag' check first to avoid errors with some
  // ActiveX objects in IE.
  return isNumber(value) && value != +value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isNative"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNative (value)](#apidoc.element.sequelize.Utils._.isNative)
- description and source-code
```javascript
function isNative(value) {
  if (!isObject(value)) {
    return false;
  }
  var pattern = (isFunction(value) || isHostObject(value)) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isNil"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNil (value)](#apidoc.element.sequelize.Utils._.isNil)
- description and source-code
```javascript
function isNil(value) {
  return value == null;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isNull"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNull (value)](#apidoc.element.sequelize.Utils._.isNull)
- description and source-code
```javascript
function isNull(value) {
  return value === null;
}
```
- example usage
```shell
...
  var dataType = options.dataType;

  if (dataType instanceof DataTypes.DECIMAL || dataType instanceof DataTypes.FLOAT) {
    result = parseFloat(result);
  } else if (dataType instanceof DataTypes.INTEGER || dataType instanceof DataTypes.BIGINT) {
    result = parseInt(result, 10);
  } else if (dataType instanceof DataTypes.DATE) {
    if (!Utils._.isNull(result) && !Utils._.isDate(result)) {
      result = new Date(result);
    }
  } else if (dataType instanceof DataTypes.STRING) {
    // Nothing to do, result is already a string.
  }
}
...
```

#### <a name="apidoc.element.sequelize.Utils._.isNumber"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isNumber (value)](#apidoc.element.sequelize.Utils._.isNumber)
- description and source-code
```javascript
function isNumber(value) {
  return typeof value == 'number' ||
    (isObjectLike(value) && objectToString.call(value) == numberTag);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isObject (value)](#apidoc.element.sequelize.Utils._.isObject)
- description and source-code
```javascript
function isObject(value) {
  var type = typeof value;
  return !!value && (type == 'object' || type == 'function');
}
```
- example usage
```shell
...

  if (rawAttribute.allowNull === false && ((value === null) || (value === undefined))) {
    error = new sequelizeError.ValidationErrorItem(field + ' cannot be null', 'notNull Violation', field, value);
    this.errors.push(error);
  }

  if (rawAttribute.type === DataTypes.STRING || rawAttribute.type instanceof DataTypes.STRING || rawAttribute.type === DataTypes
.TEXT || rawAttribute.type instanceof DataTypes.TEXT) {
    if (Array.isArray(value) || (_.isObject(value) && !value._isSequelizeMethod) && !Buffer.isBuffer(value)) {
      error = new sequelizeError.ValidationErrorItem(field + ' cannot be an array or an object', 'string violation', field, value
);
      this.errors.push(error);
    }
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.isObjectLike"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isObjectLike (value)](#apidoc.element.sequelize.Utils._.isObjectLike)
- description and source-code
```javascript
function isObjectLike(value) {
  return !!value && typeof value == 'object';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isPlainObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isPlainObject (value)](#apidoc.element.sequelize.Utils._.isPlainObject)
- description and source-code
```javascript
function isPlainObject(value) {
  if (!isObjectLike(value) ||
      objectToString.call(value) != objectTag || isHostObject(value)) {
    return false;
  }
  var proto = getPrototype(value);
  if (proto === null) {
    return true;
  }
  var Ctor = hasOwnProperty.call(proto, 'constructor') && proto.constructor;
  return (typeof Ctor == 'function' &&
    Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString);
}
```
- example usage
```shell
...
  , self = this
  , sql = ''
  , i = 0;

options = _.clone(options) || {};

attributes = Utils._.mapValues(attributes, function(attribute) {
  if (!Utils._.isPlainObject(attribute)) {
    attribute = { type: attribute, allowNull: true };
  }

  attribute = self.sequelize.normalizeAttribute(attribute);

  return attribute;
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.isRegExp"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isRegExp (value)](#apidoc.element.sequelize.Utils._.isRegExp)
- description and source-code
```javascript
function isRegExp(value) {
  return isObject(value) && objectToString.call(value) == regexpTag;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isSafeInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isSafeInteger (value)](#apidoc.element.sequelize.Utils._.isSafeInteger)
- description and source-code
```javascript
function isSafeInteger(value) {
  return isInteger(value) && value >= -MAX_SAFE_INTEGER && value <= MAX_SAFE_INTEGER;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isSet"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isSet (value)](#apidoc.element.sequelize.Utils._.isSet)
- description and source-code
```javascript
function isSet(value) {
  return isObjectLike(value) && getTag(value) == setTag;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isString"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isString (value)](#apidoc.element.sequelize.Utils._.isString)
- description and source-code
```javascript
function isString(value) {
  return typeof value == 'string' ||
    (!isArray(value) && isObjectLike(value) && objectToString.call(value) == stringTag);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isSymbol"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isSymbol (value)](#apidoc.element.sequelize.Utils._.isSymbol)
- description and source-code
```javascript
function isSymbol(value) {
  return typeof value == 'symbol' ||
    (isObjectLike(value) && objectToString.call(value) == symbolTag);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isTypedArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isTypedArray (value)](#apidoc.element.sequelize.Utils._.isTypedArray)
- description and source-code
```javascript
function isTypedArray(value) {
  return isObjectLike(value) &&
    isLength(value.length) && !!typedArrayTags[objectToString.call(value)];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isUndefined"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isUndefined (value)](#apidoc.element.sequelize.Utils._.isUndefined)
- description and source-code
```javascript
function isUndefined(value) {
  return value === undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isWeakMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isWeakMap (value)](#apidoc.element.sequelize.Utils._.isWeakMap)
- description and source-code
```javascript
function isWeakMap(value) {
  return isObjectLike(value) && getTag(value) == weakMapTag;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.isWeakSet"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>isWeakSet (value)](#apidoc.element.sequelize.Utils._.isWeakSet)
- description and source-code
```javascript
function isWeakSet(value) {
  return isObjectLike(value) && objectToString.call(value) == weakSetTag;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.iteratee"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>iteratee (func)](#apidoc.element.sequelize.Utils._.iteratee)
- description and source-code
```javascript
function iteratee(func) {
  return baseIteratee(typeof func == 'function' ? func : baseClone(func, true));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.join"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>join (array, separator)](#apidoc.element.sequelize.Utils._.join)
- description and source-code
```javascript
function join(array, separator) {
  return array ? nativeJoin.call(array, separator) : '';
}
```
- example usage
```shell
...
 if (message) {
   this.message = message;

 // ... otherwise create a concatenated message out of existing errors.
 } else if (this.errors.length > 0 && this.errors[0].message) {
   this.message = this.errors.map(function(err) {
     return err.type + ': ' + err.message;
   }).join(',\n');
 }
};
util.inherits(error.ValidationError, error.BaseError);

/**
* Gets all validation error items for the path / field specified.
*
...
```

#### <a name="apidoc.element.sequelize.Utils._.kebabCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>kebabCase (string)](#apidoc.element.sequelize.Utils._.kebabCase)
- description and source-code
```javascript
kebabCase = function (string) {
  return arrayReduce(words(deburr(string).replace(reApos, '')), callback, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.keyBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>keyBy (collection, iteratee)](#apidoc.element.sequelize.Utils._.keyBy)
- description and source-code
```javascript
keyBy = function (collection, iteratee) {
  var func = isArray(collection) ? arrayAggregator : baseAggregator,
      accumulator = initializer ? initializer() : {};

  return func(collection, setter, getIteratee(iteratee), accumulator);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.keys"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>keys (object)](#apidoc.element.sequelize.Utils._.keys)
- description and source-code
```javascript
function keys(object) {
  var isProto = isPrototype(object);
  if (!(isProto || isArrayLike(object))) {
    return baseKeys(object);
  }
  var indexes = indexKeys(object),
      skipIndexes = !!indexes,
      result = indexes || [],
      length = result.length;

  for (var key in object) {
    if (baseHas(object, key) &&
        !(skipIndexes && (key == 'length' || isIndex(key, length))) &&
        !(isProto && key == 'constructor')) {
      result.push(key);
    }
  }
  return result;
}
```
- example usage
```shell
...
  hooks: hookTypes,
  hookAliases: hookAliases,

  applyTo: function(Model) {
    Utils._.mixin(Model, Hooks);
    Utils._.mixin(Model.prototype, Hooks);

    var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
    allHooks.forEach(function(hook) {
      Model.prototype[hook] = function(name, callback) {
        return this.addHook(hook, name, callback);
      };
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.keysIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>keysIn (object)](#apidoc.element.sequelize.Utils._.keysIn)
- description and source-code
```javascript
function keysIn(object) {
  var index = -1,
      isProto = isPrototype(object),
      props = baseKeysIn(object),
      propsLength = props.length,
      indexes = indexKeys(object),
      skipIndexes = !!indexes,
      result = indexes || [],
      length = result.length;

  while (++index < propsLength) {
    var key = props[index];
    if (!(skipIndexes && (key == 'length' || isIndex(key, length))) &&
        !(key == 'constructor' && (isProto || !hasOwnProperty.call(object, key)))) {
      result.push(key);
    }
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.last"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>last (array)](#apidoc.element.sequelize.Utils._.last)
- description and source-code
```javascript
function last(array) {
  var length = array ? array.length : 0;
  return length ? array[length - 1] : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.lastIndexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lastIndexOf (array, value, fromIndex)](#apidoc.element.sequelize.Utils._.lastIndexOf)
- description and source-code
```javascript
function lastIndexOf(array, value, fromIndex) {
  var length = array ? array.length : 0;
  if (!length) {
    return -1;
  }
  var index = length;
  if (fromIndex !== undefined) {
    index = toInteger(fromIndex);
    index = (
      index < 0
        ? nativeMax(length + index, 0)
        : nativeMin(index, length - 1)
    ) + 1;
  }
  if (value !== value) {
    return indexOfNaN(array, index, true);
  }
  while (index--) {
    if (array[index] === value) {
      return index;
    }
  }
  return -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.lowerCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lowerCase (string)](#apidoc.element.sequelize.Utils._.lowerCase)
- description and source-code
```javascript
lowerCase = function (string) {
  return arrayReduce(words(deburr(string).replace(reApos, '')), callback, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.lowerFirst"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lowerFirst (string)](#apidoc.element.sequelize.Utils._.lowerFirst)
- description and source-code
```javascript
lowerFirst = function (string) {
  string = toString(string);

  var strSymbols = reHasComplexSymbol.test(string)
    ? stringToArray(string)
    : undefined;

  var chr = strSymbols
    ? strSymbols[0]
    : string.charAt(0);

  var trailing = strSymbols
    ? castSlice(strSymbols, 1).join('')
    : string.slice(1);

  return chr[methodName]() + trailing;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.lt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lt (value, other)](#apidoc.element.sequelize.Utils._.lt)
- description and source-code
```javascript
lt = function (value, other) {
  if (!(typeof value == 'string' && typeof other == 'string')) {
    value = toNumber(value);
    other = toNumber(other);
  }
  return operator(value, other);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.lte"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>lte (value, other)](#apidoc.element.sequelize.Utils._.lte)
- description and source-code
```javascript
lte = function (value, other) {
  if (!(typeof value == 'string' && typeof other == 'string')) {
    value = toNumber(value);
    other = toNumber(other);
  }
  return operator(value, other);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.map"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>map (collection, iteratee)](#apidoc.element.sequelize.Utils._.map)
- description and source-code
```javascript
function map(collection, iteratee) {
  var func = isArray(collection) ? arrayMap : baseMap;
  return func(collection, getIteratee(iteratee, 3));
}
```
- example usage
```shell
...

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
};
util.inherits(error.ValidationError, error.BaseError);

/**
...
```

#### <a name="apidoc.element.sequelize.Utils._.mapKeys"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mapKeys (object, iteratee)](#apidoc.element.sequelize.Utils._.mapKeys)
- description and source-code
```javascript
function mapKeys(object, iteratee) {
  var result = {};
  iteratee = getIteratee(iteratee, 3);

  baseForOwn(object, function(value, key, object) {
    result[iteratee(value, key, object)] = value;
  });
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.mapValues"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mapValues (object, iteratee)](#apidoc.element.sequelize.Utils._.mapValues)
- description and source-code
```javascript
function mapValues(object, iteratee) {
  var result = {};
  iteratee = getIteratee(iteratee, 3);

  baseForOwn(object, function(value, key, object) {
    result[key] = iteratee(value, key, object);
  });
  return result;
}
```
- example usage
```shell
...
, keyLen = keys.length
, self = this
, sql = ''
, i = 0;

  options = _.clone(options) || {};

  attributes = Utils._.mapValues(attributes, function(attribute) {
if (!Utils._.isPlainObject(attribute)) {
  attribute = { type: attribute, allowNull: true };
}

attribute = self.sequelize.normalizeAttribute(attribute);

return attribute;
...
```

#### <a name="apidoc.element.sequelize.Utils._.matches"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>matches (source)](#apidoc.element.sequelize.Utils._.matches)
- description and source-code
```javascript
function matches(source) {
  return baseMatches(baseClone(source, true));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.matchesProperty"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>matchesProperty (path, srcValue)](#apidoc.element.sequelize.Utils._.matchesProperty)
- description and source-code
```javascript
function matchesProperty(path, srcValue) {
  return baseMatchesProperty(path, baseClone(srcValue, true));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.max"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>max (array)](#apidoc.element.sequelize.Utils._.max)
- description and source-code
```javascript
function max(array) {
  return (array && array.length)
    ? baseExtremum(array, identity, baseGt)
    : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.maxBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>maxBy (array, iteratee)](#apidoc.element.sequelize.Utils._.maxBy)
- description and source-code
```javascript
function maxBy(array, iteratee) {
  return (array && array.length)
    ? baseExtremum(array, getIteratee(iteratee), baseGt)
    : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.mean"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mean (array)](#apidoc.element.sequelize.Utils._.mean)
- description and source-code
```javascript
function mean(array) {
  return baseMean(array, identity);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.meanBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>meanBy (array, iteratee)](#apidoc.element.sequelize.Utils._.meanBy)
- description and source-code
```javascript
function meanBy(array, iteratee) {
  return baseMean(array, getIteratee(iteratee));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.memoize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>memoize (func, resolver)](#apidoc.element.sequelize.Utils._.memoize)
- description and source-code
```javascript
function memoize(func, resolver) {
  if (typeof func != 'function' || (resolver && typeof resolver != 'function')) {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  var memoized = function() {
    var args = arguments,
        key = resolver ? resolver.apply(this, args) : args[0],
        cache = memoized.cache;

    if (cache.has(key)) {
      return cache.get(key);
    }
    var result = func.apply(this, args);
    memoized.cache = cache.set(key, result);
    return result;
  };
  memoized.cache = new (memoize.Cache || MapCache);
  return memoized;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.merge"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>merge ()](#apidoc.element.sequelize.Utils._.merge)
- description and source-code
```javascript
merge = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.mergeWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mergeWith ()](#apidoc.element.sequelize.Utils._.mergeWith)
- description and source-code
```javascript
mergeWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.method"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>method ()](#apidoc.element.sequelize.Utils._.method)
- description and source-code
```javascript
method = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
...
 * @param {Function} validator The custom validator.
 * @param {string} validatorType the custom validator type (name).
 * @param {boolean=} optAttrDefined Set to true if custom validator was defined
 *   from the Attribute
 * @return {Promise} A promise.
 * @private
 */
InstanceValidator.prototype._invokeCustomValidator = Promise.method(function(validator, validatorType, optAttrDefined, optValue,
optField) {
var validatorFunction = null;  // the validation function to call
var isAsync = false;

var validatorArity = validator.length;
// check if validator is async and requires a callback
var asyncArity = 1;
var errorKey = validatorType;
...
```

#### <a name="apidoc.element.sequelize.Utils._.methodOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>methodOf ()](#apidoc.element.sequelize.Utils._.methodOf)
- description and source-code
```javascript
methodOf = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.min"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>min (array)](#apidoc.element.sequelize.Utils._.min)
- description and source-code
```javascript
function min(array) {
  return (array && array.length)
    ? baseExtremum(array, identity, baseLt)
    : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.minBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>minBy (array, iteratee)](#apidoc.element.sequelize.Utils._.minBy)
- description and source-code
```javascript
function minBy(array, iteratee) {
  return (array && array.length)
    ? baseExtremum(array, getIteratee(iteratee), baseLt)
    : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.mixin"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>mixin (object, source, options)](#apidoc.element.sequelize.Utils._.mixin)
- description and source-code
```javascript
function mixin(object, source, options) {
  var props = keys(source),
      methodNames = baseFunctions(source, props);

  if (options == null &&
      !(isObject(source) && (methodNames.length || !props.length))) {
    options = source;
    source = object;
    object = this;
    methodNames = baseFunctions(source, keys(source));
  }
  var chain = !(isObject(options) && 'chain' in options) || !!options.chain,
      isFunc = isFunction(object);

  arrayEach(methodNames, function(methodName) {
    var func = source[methodName];
    object[methodName] = func;
    if (isFunc) {
      object.prototype[methodName] = function() {
        var chainAll = this.__chain__;
        if (chain || chainAll) {
          var result = object(this.__wrapped__),
              actions = result.__actions__ = copyArray(this.__actions__);

          actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
          result.__chain__ = chainAll;
          return result;
        }
        return func.apply(object, arrayPush([this.value()], arguments));
      };
    }
  });

  return object;
}
```
- example usage
```shell
...
   */

module.exports = {
  hooks: hookTypes,
  hookAliases: hookAliases,

  applyTo: function(Model) {
Utils._.mixin(Model, Hooks);
Utils._.mixin(Model.prototype, Hooks);

var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
allHooks.forEach(function(hook) {
  Model.prototype[hook] = function(name, callback) {
    return this.addHook(hook, name, callback);
  };
...
```

#### <a name="apidoc.element.sequelize.Utils._.multiply"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>multiply (value, other)](#apidoc.element.sequelize.Utils._.multiply)
- description and source-code
```javascript
multiply = function (value, other) {
  var result;
  if (value === undefined && other === undefined) {
    return 0;
  }
  if (value !== undefined) {
    result = value;
  }
  if (other !== undefined) {
    if (result === undefined) {
      return other;
    }
    if (typeof value == 'string' || typeof other == 'string') {
      value = baseToString(value);
      other = baseToString(other);
    } else {
      value = baseToNumber(value);
      other = baseToNumber(other);
    }
    result = operator(value, other);
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.negate"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>negate (predicate)](#apidoc.element.sequelize.Utils._.negate)
- description and source-code
```javascript
function negate(predicate) {
  if (typeof predicate != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  return function() {
    return !predicate.apply(this, arguments);
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.noConflict"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>noConflict ()](#apidoc.element.sequelize.Utils._.noConflict)
- description and source-code
```javascript
function noConflict() {
  if (root._ === this) {
    root._ = oldDash;
  }
  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.noop"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>noop ()](#apidoc.element.sequelize.Utils._.noop)
- description and source-code
```javascript
function noop() {
  // No operation performed.
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.now"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>now ()](#apidoc.element.sequelize.Utils._.now)
- description and source-code
```javascript
function now() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.nth"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>nth (array, n)](#apidoc.element.sequelize.Utils._.nth)
- description and source-code
```javascript
function nth(array, n) {
  return (array && array.length) ? baseNth(array, toInteger(n)) : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.nthArg"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>nthArg (n)](#apidoc.element.sequelize.Utils._.nthArg)
- description and source-code
```javascript
function nthArg(n) {
  n = toInteger(n);
  return rest(function(args) {
    return baseNth(args, n);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.omit"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>omit ()](#apidoc.element.sequelize.Utils._.omit)
- description and source-code
```javascript
omit = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.omitBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>omitBy (object, predicate)](#apidoc.element.sequelize.Utils._.omitBy)
- description and source-code
```javascript
function omitBy(object, predicate) {
  predicate = getIteratee(predicate);
  return basePickBy(object, function(value, key) {
    return !predicate(value, key);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.once"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>once (func)](#apidoc.element.sequelize.Utils._.once)
- description and source-code
```javascript
function once(func) {
  return before(2, func);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.orderBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>orderBy (collection, iteratees, orders, guard)](#apidoc.element.sequelize.Utils._.orderBy)
- description and source-code
```javascript
function orderBy(collection, iteratees, orders, guard) {
  if (collection == null) {
    return [];
  }
  if (!isArray(iteratees)) {
    iteratees = iteratees == null ? [] : [iteratees];
  }
  orders = guard ? undefined : orders;
  if (!isArray(orders)) {
    orders = orders == null ? [] : [orders];
  }
  return baseOrderBy(collection, iteratees, orders);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.over"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>over ()](#apidoc.element.sequelize.Utils._.over)
- description and source-code
```javascript
over = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.overArgs"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>overArgs ()](#apidoc.element.sequelize.Utils._.overArgs)
- description and source-code
```javascript
overArgs = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.overEvery"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>overEvery ()](#apidoc.element.sequelize.Utils._.overEvery)
- description and source-code
```javascript
overEvery = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.overSome"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>overSome ()](#apidoc.element.sequelize.Utils._.overSome)
- description and source-code
```javascript
overSome = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pad"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pad (string, length, chars)](#apidoc.element.sequelize.Utils._.pad)
- description and source-code
```javascript
function pad(string, length, chars) {
  string = toString(string);
  length = toInteger(length);

  var strLength = length ? stringSize(string) : 0;
  if (!length || strLength >= length) {
    return string;
  }
  var mid = (length - strLength) / 2;
  return (
    createPadding(nativeFloor(mid), chars) +
    string +
    createPadding(nativeCeil(mid), chars)
  );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.padEnd"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>padEnd (string, length, chars)](#apidoc.element.sequelize.Utils._.padEnd)
- description and source-code
```javascript
function padEnd(string, length, chars) {
  string = toString(string);
  length = toInteger(length);

  var strLength = length ? stringSize(string) : 0;
  return (length && strLength < length)
    ? (string + createPadding(length - strLength, chars))
    : string;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.padStart"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>padStart (string, length, chars)](#apidoc.element.sequelize.Utils._.padStart)
- description and source-code
```javascript
function padStart(string, length, chars) {
  string = toString(string);
  length = toInteger(length);

  var strLength = length ? stringSize(string) : 0;
  return (length && strLength < length)
    ? (createPadding(length - strLength, chars) + string)
    : string;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.parseInt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>parseInt (string, radix, guard)](#apidoc.element.sequelize.Utils._.parseInt)
- description and source-code
```javascript
function parseInt(string, radix, guard) {
  // Chrome fails to trim leading <BOM> whitespace characters.
  // See https://bugs.chromium.org/p/v8/issues/detail?id=3109 for more details.
  if (guard || radix == null) {
    radix = 0;
  } else if (radix) {
    radix = +radix;
  }
  string = toString(string).replace(reTrim, '');
  return nativeParseInt(string, radix || (reHasHexPrefix.test(string) ? 16 : 10));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.partial"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>partial ()](#apidoc.element.sequelize.Utils._.partial)
- description and source-code
```javascript
partial = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
...
    return dataTypes[dialect].BLOB.prototype.stringify(val);
  }

  return dataTypes.BLOB.prototype.stringify(val);
}

if (Array.isArray(val)) {
  var escape = _.partial(SqlString.escape, _, timeZone, dialect, format);
  if (dialect === 'postgres' && !format) {
    return dataTypes.ARRAY.prototype.stringify(val, {escape: escape});
  }
  return val.map(escape);
}

if (dialect === 'postgres' || dialect === 'sqlite' || dialect === 'mssql') {
...
```

#### <a name="apidoc.element.sequelize.Utils._.partialRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>partialRight ()](#apidoc.element.sequelize.Utils._.partialRight)
- description and source-code
```javascript
partialRight = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.partition"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>partition (collection, iteratee)](#apidoc.element.sequelize.Utils._.partition)
- description and source-code
```javascript
partition = function (collection, iteratee) {
  var func = isArray(collection) ? arrayAggregator : baseAggregator,
      accumulator = initializer ? initializer() : {};

  return func(collection, setter, getIteratee(iteratee), accumulator);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pick"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pick ()](#apidoc.element.sequelize.Utils._.pick)
- description and source-code
```javascript
pick = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pickBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pickBy (object, predicate)](#apidoc.element.sequelize.Utils._.pickBy)
- description and source-code
```javascript
function pickBy(object, predicate) {
  return object == null ? {} : basePickBy(object, getIteratee(predicate));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.property"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>property (path)](#apidoc.element.sequelize.Utils._.property)
- description and source-code
```javascript
function property(path) {
  return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.propertyOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>propertyOf (object)](#apidoc.element.sequelize.Utils._.propertyOf)
- description and source-code
```javascript
function propertyOf(object) {
  return function(path) {
    return object == null ? undefined : baseGet(object, path);
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pull"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pull ()](#apidoc.element.sequelize.Utils._.pull)
- description and source-code
```javascript
pull = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pullAll"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAll (array, values)](#apidoc.element.sequelize.Utils._.pullAll)
- description and source-code
```javascript
function pullAll(array, values) {
  return (array && array.length && values && values.length)
    ? basePullAll(array, values)
    : array;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pullAllBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAllBy (array, values, iteratee)](#apidoc.element.sequelize.Utils._.pullAllBy)
- description and source-code
```javascript
function pullAllBy(array, values, iteratee) {
  return (array && array.length && values && values.length)
    ? basePullAll(array, values, getIteratee(iteratee))
    : array;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pullAllWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAllWith (array, values, comparator)](#apidoc.element.sequelize.Utils._.pullAllWith)
- description and source-code
```javascript
function pullAllWith(array, values, comparator) {
  return (array && array.length && values && values.length)
    ? basePullAll(array, values, undefined, comparator)
    : array;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.pullAt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>pullAt ()](#apidoc.element.sequelize.Utils._.pullAt)
- description and source-code
```javascript
pullAt = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.random"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>random (lower, upper, floating)](#apidoc.element.sequelize.Utils._.random)
- description and source-code
```javascript
function random(lower, upper, floating) {
  if (floating && typeof floating != 'boolean' && isIterateeCall(lower, upper, floating)) {
    upper = floating = undefined;
  }
  if (floating === undefined) {
    if (typeof upper == 'boolean') {
      floating = upper;
      upper = undefined;
    }
    else if (typeof lower == 'boolean') {
      floating = lower;
      lower = undefined;
    }
  }
  if (lower === undefined && upper === undefined) {
    lower = 0;
    upper = 1;
  }
  else {
    lower = toNumber(lower) || 0;
    if (upper === undefined) {
      upper = lower;
      lower = 0;
    } else {
      upper = toNumber(upper) || 0;
    }
  }
  if (lower > upper) {
    var temp = lower;
    lower = upper;
    upper = temp;
  }
  if (floating || lower % 1 || upper % 1) {
    var rand = nativeRandom();
    return nativeMin(lower + (rand * (upper - lower + freeParseFloat('1e-' + ((rand + '').length - 1)))), upper);
  }
  return baseRandom(lower, upper);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.range"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>range (start, end, step)](#apidoc.element.sequelize.Utils._.range)
- description and source-code
```javascript
range = function (start, end, step) {
  if (step && typeof step != 'number' && isIterateeCall(start, end, step)) {
    end = step = undefined;
  }
  // Ensure the sign of '-0' is preserved.
  start = toNumber(start);
  start = start === start ? start : 0;
  if (end === undefined) {
    end = start;
    start = 0;
  } else {
    end = toNumber(end) || 0;
  }
  step = step === undefined ? (start < end ? 1 : -1) : (toNumber(step) || 0);
  return baseRange(start, end, step, fromRight);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.rangeRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>rangeRight (start, end, step)](#apidoc.element.sequelize.Utils._.rangeRight)
- description and source-code
```javascript
rangeRight = function (start, end, step) {
  if (step && typeof step != 'number' && isIterateeCall(start, end, step)) {
    end = step = undefined;
  }
  // Ensure the sign of '-0' is preserved.
  start = toNumber(start);
  start = start === start ? start : 0;
  if (end === undefined) {
    end = start;
    start = 0;
  } else {
    end = toNumber(end) || 0;
  }
  step = step === undefined ? (start < end ? 1 : -1) : (toNumber(step) || 0);
  return baseRange(start, end, step, fromRight);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.rearg"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>rearg ()](#apidoc.element.sequelize.Utils._.rearg)
- description and source-code
```javascript
rearg = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reduce (collection, iteratee, accumulator)](#apidoc.element.sequelize.Utils._.reduce)
- description and source-code
```javascript
function reduce(collection, iteratee, accumulator) {
  var func = isArray(collection) ? arrayReduce : baseReduce,
      initAccum = arguments.length < 3;

  return func(collection, getIteratee(iteratee, 4), accumulator, initAccum, baseEach);
}
```
- example usage
```shell
...
/**
 * Gets all validation error items for the path / field specified.
 *
 * @param {string} path The path to be checked for error items
 * @returns {Array} Validation error items for the specified path
 */
error.ValidationError.prototype.get = function(path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.reduceRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reduceRight (collection, iteratee, accumulator)](#apidoc.element.sequelize.Utils._.reduceRight)
- description and source-code
```javascript
function reduceRight(collection, iteratee, accumulator) {
  var func = isArray(collection) ? arrayReduceRight : baseReduce,
      initAccum = arguments.length < 3;

  return func(collection, getIteratee(iteratee, 4), accumulator, initAccum, baseEachRight);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.reject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reject (collection, predicate)](#apidoc.element.sequelize.Utils._.reject)
- description and source-code
```javascript
function reject(collection, predicate) {
  var func = isArray(collection) ? arrayFilter : baseFilter;
  predicate = getIteratee(predicate, 3);
  return func(collection, function(value, index, collection) {
    return !predicate(value, index, collection);
  });
}
```
- example usage
```shell
...
    sql,
    _.assign({}, options, { type: QueryTypes.DESCRIBE })
  ).then(function(data) {
    // If no data is returned from the query, then the table name may be wrong.
    // Query generators that use information_schema for retrieving table info will just return an empty result set,
    // it will not throw an error like built-ins do (e.g. DESCRIBE on MySql).
    if (Utils._.isEmpty(data)) {
      return Promise.reject('No description found for "' + tableName + '" table. Check the table name and schema; remember, they
 _are_ case sensitive.');
    } else {
      return Promise.resolve(data);
    }
  });
};

QueryInterface.prototype.addColumn = function(table, key, attribute, options) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.remove"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>remove (array, predicate)](#apidoc.element.sequelize.Utils._.remove)
- description and source-code
```javascript
function remove(array, predicate) {
  var result = [];
  if (!(array && array.length)) {
    return result;
  }
  var index = -1,
      indexes = [],
      length = array.length;

  predicate = getIteratee(predicate, 3);
  while (++index < length) {
    var value = array[index];
    if (predicate(value, index, array)) {
      result.push(value);
      indexes.push(index);
    }
  }
  basePullAt(array, indexes);
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.repeat"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>repeat (string, n, guard)](#apidoc.element.sequelize.Utils._.repeat)
- description and source-code
```javascript
function repeat(string, n, guard) {
  if ((guard ? isIterateeCall(string, n, guard) : n === undefined)) {
    n = 1;
  } else {
    n = toInteger(n);
  }
  return baseRepeat(toString(string), n);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.replace"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>replace ()](#apidoc.element.sequelize.Utils._.replace)
- description and source-code
```javascript
function replace() {
  var args = arguments,
      string = toString(args[0]);

  return args.length < 3 ? string : nativeReplace.call(string, args[1], args[2]);
}
```
- example usage
```shell
...
/* jshint -W110 */
var dataTypes = require('./data-types')
, _ = require('lodash')
, SqlString = exports;

SqlString.escapeId = function(val, forbidQualified) {
if (forbidQualified) {
  return ''' + val.replace(/'/g, '''') + ''';
}
return ''' + val.replace(/'/g, '''').replace(/\./g, ''.'') + ''';
};

SqlString.escape = function(val, timeZone, dialect, format) {
var prependN = false;
if (val === undefined || val === null) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.rest"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>rest (func, start)](#apidoc.element.sequelize.Utils._.rest)
- description and source-code
```javascript
function rest(func, start) {
  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  start = nativeMax(start === undefined ? (func.length - 1) : toInteger(start), 0);
  return function() {
    var args = arguments,
        index = -1,
        length = nativeMax(args.length - start, 0),
        array = Array(length);

    while (++index < length) {
      array[index] = args[start + index];
    }
    switch (start) {
      case 0: return func.call(this, array);
      case 1: return func.call(this, args[0], array);
      case 2: return func.call(this, args[0], args[1], array);
    }
    var otherArgs = Array(start + 1);
    index = -1;
    while (++index < start) {
      otherArgs[index] = args[index];
    }
    otherArgs[start] = array;
    return apply(func, this, otherArgs);
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.result"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>result (object, path, defaultValue)](#apidoc.element.sequelize.Utils._.result)
- description and source-code
```javascript
function result(object, path, defaultValue) {
  path = isKey(path, object) ? [path] : castPath(path);

  var index = -1,
      length = path.length;

  // Ensure the loop is entered when path is empty.
  if (!length) {
    object = undefined;
    length = 1;
  }
  while (++index < length) {
    var value = object == null ? undefined : object[toKey(path[index])];
    if (value === undefined) {
      index = length;
      value = defaultValue;
    }
    object = isFunction(value) ? value.call(object) : value;
  }
  return object;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.reverse"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>reverse (array)](#apidoc.element.sequelize.Utils._.reverse)
- description and source-code
```javascript
function reverse(array) {
  return array ? nativeReverse.call(array) : array;
}
```
- example usage
```shell
...
    });

    sorter.add(tableName, deps);
  });

  sorted = sorter.sort();
  if (options.reverse) {
    sorted = sorted.reverse();
  }
  sorted.forEach(function(name) {
    iterator(models[name], name);
  });
};

module.exports = ModelManager;
...
```

#### <a name="apidoc.element.sequelize.Utils._.round"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>round (number, precision)](#apidoc.element.sequelize.Utils._.round)
- description and source-code
```javascript
round = function (number, precision) {
  number = toNumber(number);
  precision = toInteger(precision);
  if (precision) {
    // Shift with exponential notation to avoid floating-point issues.
    // See [MDN](https://mdn.io/round#Examples) for more details.
    var pair = (toString(number) + 'e').split('e'),
        value = func(pair[0] + 'e' + (+pair[1] + precision));

    pair = (toString(value) + 'e').split('e');
    return +(pair[0] + 'e' + (+pair[1] - precision));
  }
  return func(number);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.runInContext"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>runInContext (context)](#apidoc.element.sequelize.Utils._.runInContext)
- description and source-code
```javascript
function runInContext(context) {
  context = context ? _.defaults({}, context, _.pick(root, contextProps)) : root;

<span class="apidocCodeCommentSpan">  /** Built-in constructor references. */
</span>  var Date = context.Date,
      Error = context.Error,
      Math = context.Math,
      RegExp = context.RegExp,
      TypeError = context.TypeError;

  /** Used for built-in method references. */
  var arrayProto = context.Array.prototype,
      objectProto = context.Object.prototype,
      stringProto = context.String.prototype;

  /** Used to resolve the decompiled source of functions. */
  var funcToString = context.Function.prototype.toString;

  /** Used to check objects for own properties. */
  var hasOwnProperty = objectProto.hasOwnProperty;

  /** Used to generate unique IDs. */
  var idCounter = 0;

  /** Used to infer the 'Object' constructor. */
  var objectCtorString = funcToString.call(Object);

  /**
   * Used to resolve the
   * ['toStringTag'](http://ecma-international.org/ecma-262/6.0/#sec-object.prototype.tostring)
   * of values.
   */
  var objectToString = objectProto.toString;

  /** Used to restore the original '_' reference in '_.noConflict'. */
  var oldDash = root._;

  /** Used to detect if a method is native. */
  var reIsNative = RegExp('^' +
    funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\$&')
    .replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$'
  );

  /** Built-in value references. */
  var Buffer = moduleExports ? context.Buffer : undefined,
      Reflect = context.Reflect,
      Symbol = context.Symbol,
      Uint8Array = context.Uint8Array,
      clearTimeout = context.clearTimeout,
      enumerate = Reflect ? Reflect.enumerate : undefined,
      getOwnPropertySymbols = Object.getOwnPropertySymbols,
      iteratorSymbol = typeof (iteratorSymbol = Symbol && Symbol.iterator) == 'symbol' ? iteratorSymbol : undefined,
      objectCreate = Object.create,
      propertyIsEnumerable = objectProto.propertyIsEnumerable,
      setTimeout = context.setTimeout,
      splice = arrayProto.splice;

  /* Built-in method references for those with the same name as other 'lodash' methods. */
  var nativeCeil = Math.ceil,
      nativeFloor = Math.floor,
      nativeGetPrototype = Object.getPrototypeOf,
      nativeIsFinite = context.isFinite,
      nativeJoin = arrayProto.join,
      nativeKeys = Object.keys,
      nativeMax = Math.max,
      nativeMin = Math.min,
      nativeParseInt = context.parseInt,
      nativeRandom = Math.random,
      nativeReplace = stringProto.replace,
      nativeReverse = arrayProto.reverse,
      nativeSplit = stringProto.split;

  /* Built-in method references that are verified to be native. */
  var DataView = getNative(context, 'DataView'),
      Map = getNative(context, 'Map'),
      Promise = getNative(context, 'Promise'),
      Set = getNative(context, 'Set'),
      WeakMap = getNative(context, 'WeakMap'),
      nativeCreate = getNative(Object, 'create');

  /** Used to store function metadata. */
  var metaMap = WeakMap && new WeakMap;

  /** Detect if properties shadowing those on 'Object.prototype' are non-enumerable. */
  var nonEnumShadows = !propertyIsEnumerable.call({ 'valueOf': 1 }, 'valueOf');

  /** Used to lookup unminified function names. */
  var realNames = {};

  /** Used to detect maps, sets, and weakmaps. */
  var dataViewCtorString = toSource(DataView),
      mapCtorString = toSource(Map),
      promiseCtorString = toSource(Promise),
      setCtorString = toSource(Set),
      weakMapCtorString = toSource(WeakMap);

  /** Used to convert symbols to primitives and strings. */
  var symbolProto = Symbol ? Symbol.prototype : undefined,
      symbolValueOf = symbolProto ? symbolProto.valueOf : undefined,
      symbolToString = symbolProto ? symbolProto.toString : undefined;

  /*------------------------------------------------------------------------*/

  /**
   * Creates a 'lodash' object which wraps 'value' to enable implicit method
   * chain sequences. Methods that operate on and return arrays, collections,
   * and functions can be chained together ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sample"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sample (collection)](#apidoc.element.sequelize.Utils._.sample)
- description and source-code
```javascript
function sample(collection) {
  var array = isArrayLike(collection) ? collection : values(collection),
      length = array.length;

  return length > 0 ? array[baseRandom(0, length - 1)] : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sampleSize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sampleSize (collection, n, guard)](#apidoc.element.sequelize.Utils._.sampleSize)
- description and source-code
```javascript
function sampleSize(collection, n, guard) {
  var index = -1,
      result = toArray(collection),
      length = result.length,
      lastIndex = length - 1;

  if ((guard ? isIterateeCall(collection, n, guard) : n === undefined)) {
    n = 1;
  } else {
    n = baseClamp(toInteger(n), 0, length);
  }
  while (++index < n) {
    var rand = baseRandom(index, lastIndex),
        value = result[rand];

    result[rand] = result[index];
    result[index] = value;
  }
  result.length = n;
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.set"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>set (object, path, value)](#apidoc.element.sequelize.Utils._.set)
- description and source-code
```javascript
function set(object, path, value) {
  return object == null ? object : baseSet(object, path, value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.setWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>setWith (object, path, value, customizer)](#apidoc.element.sequelize.Utils._.setWith)
- description and source-code
```javascript
function setWith(object, path, value, customizer) {
  customizer = typeof customizer == 'function' ? customizer : undefined;
  return object == null ? object : baseSet(object, path, value, customizer);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.shuffle"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>shuffle (collection)](#apidoc.element.sequelize.Utils._.shuffle)
- description and source-code
```javascript
function shuffle(collection) {
  return sampleSize(collection, MAX_ARRAY_LENGTH);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.size"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>size (collection)](#apidoc.element.sequelize.Utils._.size)
- description and source-code
```javascript
function size(collection) {
  if (collection == null) {
    return 0;
  }
  if (isArrayLike(collection)) {
    var result = collection.length;
    return (result && isString(collection)) ? stringSize(collection) : result;
  }
  if (isObjectLike(collection)) {
    var tag = getTag(collection);
    if (tag == mapTag || tag == setTag) {
      return collection.size;
    }
  }
  return keys(collection).length;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.slice"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>slice (array, start, end)](#apidoc.element.sequelize.Utils._.slice)
- description and source-code
```javascript
function slice(array, start, end) {
  var length = array ? array.length : 0;
  if (!length) {
    return [];
  }
  if (end && typeof end != 'number' && isIterateeCall(array, start, end)) {
    start = 0;
    end = length;
  }
  else {
    start = start == null ? 0 : toInteger(start);
    end = end === undefined ? length : toInteger(end);
  }
  return baseSlice(array, start, end);
}
```
- example usage
```shell
...
     validatorArgs = [validatorArgs, field];
   } else if (isLocalizedValidator || validatorType === 'isIP') {
     validatorArgs = [];
   } else {
     validatorArgs = [validatorArgs];
   }
 } else {
   validatorArgs = validatorArgs.slice(0);
 }
 return validatorArgs;
};

/**
* Will validate a single field against its schema definition (isnull).
*
...
```

#### <a name="apidoc.element.sequelize.Utils._.snakeCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>snakeCase (string)](#apidoc.element.sequelize.Utils._.snakeCase)
- description and source-code
```javascript
snakeCase = function (string) {
  return arrayReduce(words(deburr(string).replace(reApos, '')), callback, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.some"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>some (collection, predicate, guard)](#apidoc.element.sequelize.Utils._.some)
- description and source-code
```javascript
function some(collection, predicate, guard) {
  var func = isArray(collection) ? arraySome : baseSome;
  if (guard && isIterateeCall(collection, predicate, guard)) {
    predicate = undefined;
  }
  return func(collection, getIteratee(predicate, 3));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortBy ()](#apidoc.element.sequelize.Utils._.sortBy)
- description and source-code
```javascript
sortBy = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedIndex (array, value)](#apidoc.element.sequelize.Utils._.sortedIndex)
- description and source-code
```javascript
function sortedIndex(array, value) {
  return baseSortedIndex(array, value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedIndexBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedIndexBy (array, value, iteratee)](#apidoc.element.sequelize.Utils._.sortedIndexBy)
- description and source-code
```javascript
function sortedIndexBy(array, value, iteratee) {
  return baseSortedIndexBy(array, value, getIteratee(iteratee));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedIndexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedIndexOf (array, value)](#apidoc.element.sequelize.Utils._.sortedIndexOf)
- description and source-code
```javascript
function sortedIndexOf(array, value) {
  var length = array ? array.length : 0;
  if (length) {
    var index = baseSortedIndex(array, value);
    if (index < length && eq(array[index], value)) {
      return index;
    }
  }
  return -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedLastIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedLastIndex (array, value)](#apidoc.element.sequelize.Utils._.sortedLastIndex)
- description and source-code
```javascript
function sortedLastIndex(array, value) {
  return baseSortedIndex(array, value, true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedLastIndexBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedLastIndexBy (array, value, iteratee)](#apidoc.element.sequelize.Utils._.sortedLastIndexBy)
- description and source-code
```javascript
function sortedLastIndexBy(array, value, iteratee) {
  return baseSortedIndexBy(array, value, getIteratee(iteratee), true);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedLastIndexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedLastIndexOf (array, value)](#apidoc.element.sequelize.Utils._.sortedLastIndexOf)
- description and source-code
```javascript
function sortedLastIndexOf(array, value) {
  var length = array ? array.length : 0;
  if (length) {
    var index = baseSortedIndex(array, value, true) - 1;
    if (eq(array[index], value)) {
      return index;
    }
  }
  return -1;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedUniq"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedUniq (array)](#apidoc.element.sequelize.Utils._.sortedUniq)
- description and source-code
```javascript
function sortedUniq(array) {
  return (array && array.length)
    ? baseSortedUniq(array)
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sortedUniqBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sortedUniqBy (array, iteratee)](#apidoc.element.sequelize.Utils._.sortedUniqBy)
- description and source-code
```javascript
function sortedUniqBy(array, iteratee) {
  return (array && array.length)
    ? baseSortedUniq(array, getIteratee(iteratee))
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.split"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>split (string, separator, limit)](#apidoc.element.sequelize.Utils._.split)
- description and source-code
```javascript
function split(string, separator, limit) {
  if (limit && typeof limit != 'number' && isIterateeCall(string, separator, limit)) {
    separator = limit = undefined;
  }
  limit = limit === undefined ? MAX_ARRAY_LENGTH : limit >>> 0;
  if (!limit) {
    return [];
  }
  string = toString(string);
  if (string && (
        typeof separator == 'string' ||
        (separator != null && !isRegExp(separator))
      )) {
    separator = baseToString(separator);
    if (separator == '' && reHasComplexSymbol.test(string)) {
      return castSlice(stringToArray(string), 0, limit);
    }
  }
  return nativeSplit.call(string, separator, limit);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.spread"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>spread (func, start)](#apidoc.element.sequelize.Utils._.spread)
- description and source-code
```javascript
function spread(func, start) {
  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  start = start === undefined ? 0 : nativeMax(toInteger(start), 0);
  return rest(function(args) {
    var array = args[start],
        otherArgs = castSlice(args, 0, start);

    if (array) {
      arrayPush(otherArgs, array);
    }
    return apply(func, this, otherArgs);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.startCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>startCase (string)](#apidoc.element.sequelize.Utils._.startCase)
- description and source-code
```javascript
startCase = function (string) {
  return arrayReduce(words(deburr(string).replace(reApos, '')), callback, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.startsWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>startsWith (string, target, position)](#apidoc.element.sequelize.Utils._.startsWith)
- description and source-code
```javascript
function startsWith(string, target, position) {
  string = toString(string);
  position = baseClamp(toInteger(position), 0, string.length);
  return string.lastIndexOf(baseToString(target), position) == position;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.subtract"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>subtract (value, other)](#apidoc.element.sequelize.Utils._.subtract)
- description and source-code
```javascript
subtract = function (value, other) {
  var result;
  if (value === undefined && other === undefined) {
    return 0;
  }
  if (value !== undefined) {
    result = value;
  }
  if (other !== undefined) {
    if (result === undefined) {
      return other;
    }
    if (typeof value == 'string' || typeof other == 'string') {
      value = baseToString(value);
      other = baseToString(other);
    } else {
      value = baseToNumber(value);
      other = baseToNumber(other);
    }
    result = operator(value, other);
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sum"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sum (array)](#apidoc.element.sequelize.Utils._.sum)
- description and source-code
```javascript
function sum(array) {
  return (array && array.length)
    ? baseSum(array, identity)
    : 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.sumBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>sumBy (array, iteratee)](#apidoc.element.sequelize.Utils._.sumBy)
- description and source-code
```javascript
function sumBy(array, iteratee) {
  return (array && array.length)
    ? baseSum(array, getIteratee(iteratee))
    : 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.tail"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>tail (array)](#apidoc.element.sequelize.Utils._.tail)
- description and source-code
```javascript
function tail(array) {
  return drop(array, 1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.take"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>take (array, n, guard)](#apidoc.element.sequelize.Utils._.take)
- description and source-code
```javascript
function take(array, n, guard) {
  if (!(array && array.length)) {
    return [];
  }
  n = (guard || n === undefined) ? 1 : toInteger(n);
  return baseSlice(array, 0, n < 0 ? 0 : n);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.takeRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>takeRight (array, n, guard)](#apidoc.element.sequelize.Utils._.takeRight)
- description and source-code
```javascript
function takeRight(array, n, guard) {
  var length = array ? array.length : 0;
  if (!length) {
    return [];
  }
  n = (guard || n === undefined) ? 1 : toInteger(n);
  n = length - n;
  return baseSlice(array, n < 0 ? 0 : n, length);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.takeRightWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>takeRightWhile (array, predicate)](#apidoc.element.sequelize.Utils._.takeRightWhile)
- description and source-code
```javascript
function takeRightWhile(array, predicate) {
  return (array && array.length)
    ? baseWhile(array, getIteratee(predicate, 3), false, true)
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.takeWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>takeWhile (array, predicate)](#apidoc.element.sequelize.Utils._.takeWhile)
- description and source-code
```javascript
function takeWhile(array, predicate) {
  return (array && array.length)
    ? baseWhile(array, getIteratee(predicate, 3))
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.tap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>tap (value, interceptor)](#apidoc.element.sequelize.Utils._.tap)
- description and source-code
```javascript
function tap(value, interceptor) {
  interceptor(value);
  return value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.template"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>template (string, options, guard)](#apidoc.element.sequelize.Utils._.template)
- description and source-code
```javascript
function template(string, options, guard) {
  // Based on John Resig's 'tmpl' implementation
  // (http://ejohn.org/blog/javascript-micro-templating/)
  // and Laura Doktorova's doT.js (https://github.com/olado/doT).
  var settings = lodash.templateSettings;

  if (guard && isIterateeCall(string, options, guard)) {
    options = undefined;
  }
  string = toString(string);
  options = assignInWith({}, options, settings, assignInDefaults);

  var imports = assignInWith({}, options.imports, settings.imports, assignInDefaults),
      importsKeys = keys(imports),
      importsValues = baseValues(imports, importsKeys);

  var isEscaping,
      isEvaluating,
      index = 0,
      interpolate = options.interpolate || reNoMatch,
      source = "__p += '";

  // Compile the regexp to match each delimiter.
  var reDelimiters = RegExp(
    (options.escape || reNoMatch).source + '|' +
    interpolate.source + '|' +
    (interpolate === reInterpolate ? reEsTemplate : reNoMatch).source + '|' +
    (options.evaluate || reNoMatch).source + '|$'
  , 'g');

  // Use a sourceURL for easier debugging.
  var sourceURL = '//# sourceURL=' +
    ('sourceURL' in options
      ? options.sourceURL
      : ('lodash.templateSources[' + (++templateCounter) + ']')
    ) + '\n';

  string.replace(reDelimiters, function(match, escapeValue, interpolateValue, esTemplateValue, evaluateValue, offset) {
    interpolateValue || (interpolateValue = esTemplateValue);

    // Escape characters that can't be included in string literals.
    source += string.slice(index, offset).replace(reUnescapedString, escapeStringChar);

    // Replace delimiters with snippets.
    if (escapeValue) {
      isEscaping = true;
      source += "' +\n__e(" + escapeValue + ") +\n'";
    }
    if (evaluateValue) {
      isEvaluating = true;
      source += "';\n" + evaluateValue + ";\n__p += '";
    }
    if (interpolateValue) {
      source += "' +\n((__t = (" + interpolateValue + ")) == null ? '' : __t) +\n'";
    }
    index = offset + match.length;

    // The JS engine embedded in Adobe products needs 'match' returned in
    // order to produce the correct 'offset' value.
    return match;
  });

  source += "';\n";

  // If 'variable' is not specified wrap a with-statement around the generated
  // code to add the data object to the top of the scope chain.
  var variable = options.variable;
  if (!variable) {
    source = 'with (obj) {\n' + source + '\n}\n';
  }
  // Cleanup code by stripping empty strings.
  source = (isEvaluating ? source.replace(reEmptyStringLeading, '') : source)
    .replace(reEmptyStringMiddle, '$1')
    .replace(reEmptyStringTrailing, '$1;');

  // Frame code as the function body.
  source = 'function(' + (variable || 'obj') + ') {\n' +
    (variable
      ? ''
      : 'obj || (obj = {});\n'
    ) +
    "var __t, __p = ''" +
    (isEscaping
       ? ', __e = _.escape'
       : ''
    ) +
    (isEvaluating
      ? ', __j = Array.prototype.join;\n' +
        "function print() { __p += __j.call(arguments, '') }\n"
      : ';\n'
    ) +
    source +
    'return __p\n}';

  var result = attempt(function() {
    return Function(importsKeys, sourceURL + 'return ' + source)
      .apply(undefined, importsValues);
  });

  // Provide the compiled function's source by its 'toString' method or
  // the 'source' property as a convenience for inlining compiled templates.
  result.source = source;
  if (isError(result)) {
    throw result;
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.throttle"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>throttle (func, wait, options)](#apidoc.element.sequelize.Utils._.throttle)
- description and source-code
```javascript
function throttle(func, wait, options) {
  var leading = true,
      trailing = true;

  if (typeof func != 'function') {
    throw new TypeError(FUNC_ERROR_TEXT);
  }
  if (isObject(options)) {
    leading = 'leading' in options ? !!options.leading : leading;
    trailing = 'trailing' in options ? !!options.trailing : trailing;
  }
  return debounce(func, wait, {
    'leading': leading,
    'maxWait': wait,
    'trailing': trailing
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.thru"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>thru (value, interceptor)](#apidoc.element.sequelize.Utils._.thru)
- description and source-code
```javascript
function thru(value, interceptor) {
  return interceptor(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.times"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>times (n, iteratee)](#apidoc.element.sequelize.Utils._.times)
- description and source-code
```javascript
function times(n, iteratee) {
  n = toInteger(n);
  if (n < 1 || n > MAX_SAFE_INTEGER) {
    return [];
  }
  var index = MAX_ARRAY_LENGTH,
      length = nativeMin(n, MAX_ARRAY_LENGTH);

  iteratee = getIteratee(iteratee);
  n -= MAX_ARRAY_LENGTH;

  var result = baseTimes(length, iteratee);
  while (++index < n) {
    iteratee(index);
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toArray (value)](#apidoc.element.sequelize.Utils._.toArray)
- description and source-code
```javascript
function toArray(value) {
  if (!value) {
    return [];
  }
  if (isArrayLike(value)) {
    return isString(value) ? stringToArray(value) : copyArray(value);
  }
  if (iteratorSymbol && value[iteratorSymbol]) {
    return iteratorToArray(value[iteratorSymbol]());
  }
  var tag = getTag(value),
      func = tag == mapTag ? mapToArray : (tag == setTag ? setToArray : values);

  return func(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toFinite"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toFinite (value)](#apidoc.element.sequelize.Utils._.toFinite)
- description and source-code
```javascript
function toFinite(value) {
  if (!value) {
    return value === 0 ? value : 0;
  }
  value = toNumber(value);
  if (value === INFINITY || value === -INFINITY) {
    var sign = (value < 0 ? -1 : 1);
    return sign * MAX_INTEGER;
  }
  return value === value ? value : 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toInteger (value)](#apidoc.element.sequelize.Utils._.toInteger)
- description and source-code
```javascript
function toInteger(value) {
  var result = toFinite(value),
      remainder = result % 1;

  return result === result ? (remainder ? result - remainder : result) : 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toLength"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toLength (value)](#apidoc.element.sequelize.Utils._.toLength)
- description and source-code
```javascript
function toLength(value) {
  return value ? baseClamp(toInteger(value), 0, MAX_ARRAY_LENGTH) : 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toLower"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toLower (value)](#apidoc.element.sequelize.Utils._.toLower)
- description and source-code
```javascript
function toLower(value) {
  return toString(value).toLowerCase();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toNumber"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toNumber (value)](#apidoc.element.sequelize.Utils._.toNumber)
- description and source-code
```javascript
function toNumber(value) {
  if (typeof value == 'number') {
    return value;
  }
  if (isSymbol(value)) {
    return NAN;
  }
  if (isObject(value)) {
    var other = isFunction(value.valueOf) ? value.valueOf() : value;
    value = isObject(other) ? (other + '') : other;
  }
  if (typeof value != 'string') {
    return value === 0 ? value : +value;
  }
  value = value.replace(reTrim, '');
  var isBinary = reIsBinary.test(value);
  return (isBinary || reIsOctal.test(value))
    ? freeParseInt(value.slice(2), isBinary ? 2 : 8)
    : (reIsBadHex.test(value) ? NAN : +value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toPairs"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPairs (object)](#apidoc.element.sequelize.Utils._.toPairs)
- description and source-code
```javascript
toPairs = function (object) {
  var tag = getTag(object);
  if (tag == mapTag) {
    return mapToArray(object);
  }
  if (tag == setTag) {
    return setToPairs(object);
  }
  return baseToPairs(object, keysFunc(object));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toPairsIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPairsIn (object)](#apidoc.element.sequelize.Utils._.toPairsIn)
- description and source-code
```javascript
toPairsIn = function (object) {
  var tag = getTag(object);
  if (tag == mapTag) {
    return mapToArray(object);
  }
  if (tag == setTag) {
    return setToPairs(object);
  }
  return baseToPairs(object, keysFunc(object));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toPath"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPath (value)](#apidoc.element.sequelize.Utils._.toPath)
- description and source-code
```javascript
function toPath(value) {
  if (isArray(value)) {
    return arrayMap(value, toKey);
  }
  return isSymbol(value) ? [value] : copyArray(stringToPath(value));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toPlainObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toPlainObject (value)](#apidoc.element.sequelize.Utils._.toPlainObject)
- description and source-code
```javascript
function toPlainObject(value) {
  return copyObject(value, keysIn(value));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toSafeInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toSafeInteger (value)](#apidoc.element.sequelize.Utils._.toSafeInteger)
- description and source-code
```javascript
function toSafeInteger(value) {
  return baseClamp(toInteger(value), -MAX_SAFE_INTEGER, MAX_SAFE_INTEGER);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toString (value)](#apidoc.element.sequelize.Utils._.toString)
- description and source-code
```javascript
function toString(value) {
  return value == null ? '' : baseToString(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.toUpper"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>toUpper (value)](#apidoc.element.sequelize.Utils._.toUpper)
- description and source-code
```javascript
function toUpper(value) {
  return toString(value).toUpperCase();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.transform"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>transform (object, iteratee, accumulator)](#apidoc.element.sequelize.Utils._.transform)
- description and source-code
```javascript
function transform(object, iteratee, accumulator) {
  var isArr = isArray(object) || isTypedArray(object);
  iteratee = getIteratee(iteratee, 4);

  if (accumulator == null) {
    if (isArr || isObject(object)) {
      var Ctor = object.constructor;
      if (isArr) {
        accumulator = isArray(object) ? new Ctor : [];
      } else {
        accumulator = isFunction(Ctor) ? baseCreate(getPrototype(object)) : {};
      }
    } else {
      accumulator = {};
    }
  }
  (isArr ? arrayEach : baseForOwn)(object, function(value, index, object) {
    return iteratee(accumulator, value, index, object);
  });
  return accumulator;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.trim"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>trim (string, chars, guard)](#apidoc.element.sequelize.Utils._.trim)
- description and source-code
```javascript
function trim(string, chars, guard) {
  string = toString(string);
  if (string && (guard || chars === undefined)) {
    return string.replace(reTrim, '');
  }
  if (!string || !(chars = baseToString(chars))) {
    return string;
  }
  var strSymbols = stringToArray(string),
      chrSymbols = stringToArray(chars),
      start = charsStartIndex(strSymbols, chrSymbols),
      end = charsEndIndex(strSymbols, chrSymbols) + 1;

  return castSlice(strSymbols, start, end).join('');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.trimEnd"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>trimEnd (string, chars, guard)](#apidoc.element.sequelize.Utils._.trimEnd)
- description and source-code
```javascript
function trimEnd(string, chars, guard) {
  string = toString(string);
  if (string && (guard || chars === undefined)) {
    return string.replace(reTrimEnd, '');
  }
  if (!string || !(chars = baseToString(chars))) {
    return string;
  }
  var strSymbols = stringToArray(string),
      end = charsEndIndex(strSymbols, stringToArray(chars)) + 1;

  return castSlice(strSymbols, 0, end).join('');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.trimStart"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>trimStart (string, chars, guard)](#apidoc.element.sequelize.Utils._.trimStart)
- description and source-code
```javascript
function trimStart(string, chars, guard) {
  string = toString(string);
  if (string && (guard || chars === undefined)) {
    return string.replace(reTrimStart, '');
  }
  if (!string || !(chars = baseToString(chars))) {
    return string;
  }
  var strSymbols = stringToArray(string),
      start = charsStartIndex(strSymbols, stringToArray(chars));

  return castSlice(strSymbols, start).join('');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.truncate"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>truncate (string, options)](#apidoc.element.sequelize.Utils._.truncate)
- description and source-code
```javascript
function truncate(string, options) {
  var length = DEFAULT_TRUNC_LENGTH,
      omission = DEFAULT_TRUNC_OMISSION;

  if (isObject(options)) {
    var separator = 'separator' in options ? options.separator : separator;
    length = 'length' in options ? toInteger(options.length) : length;
    omission = 'omission' in options ? baseToString(options.omission) : omission;
  }
  string = toString(string);

  var strLength = string.length;
  if (reHasComplexSymbol.test(string)) {
    var strSymbols = stringToArray(string);
    strLength = strSymbols.length;
  }
  if (length >= strLength) {
    return string;
  }
  var end = length - stringSize(omission);
  if (end < 1) {
    return omission;
  }
  var result = strSymbols
    ? castSlice(strSymbols, 0, end).join('')
    : string.slice(0, end);

  if (separator === undefined) {
    return result + omission;
  }
  if (strSymbols) {
    end += (result.length - end);
  }
  if (isRegExp(separator)) {
    if (string.slice(end).search(separator)) {
      var match,
          substring = result;

      if (!separator.global) {
        separator = RegExp(separator.source, toString(reFlags.exec(separator)) + 'g');
      }
      separator.lastIndex = 0;
      while ((match = separator.exec(substring))) {
        var newEnd = match.index;
      }
      result = result.slice(0, newEnd === undefined ? end : newEnd);
    }
  } else if (string.indexOf(baseToString(separator), end) != end) {
    var index = result.lastIndexOf(separator);
    if (index > -1) {
      result = result.slice(0, index);
    }
  }
  return result + omission;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.unary"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unary (func)](#apidoc.element.sequelize.Utils._.unary)
- description and source-code
```javascript
function unary(func) {
  return ary(func, 1);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.unescape"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unescape (string)](#apidoc.element.sequelize.Utils._.unescape)
- description and source-code
```javascript
function unescape(string) {
  string = toString(string);
  return (string && reHasEscapedHtml.test(string))
    ? string.replace(reEscapedHtml, unescapeHtmlChar)
    : string;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.union"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>union ()](#apidoc.element.sequelize.Utils._.union)
- description and source-code
```javascript
union = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.unionBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unionBy ()](#apidoc.element.sequelize.Utils._.unionBy)
- description and source-code
```javascript
unionBy = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.unionWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unionWith ()](#apidoc.element.sequelize.Utils._.unionWith)
- description and source-code
```javascript
unionWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.uniq"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniq (array)](#apidoc.element.sequelize.Utils._.uniq)
- description and source-code
```javascript
function uniq(array) {
  return (array && array.length)
    ? baseUniq(array)
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.uniqBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniqBy (array, iteratee)](#apidoc.element.sequelize.Utils._.uniqBy)
- description and source-code
```javascript
function uniqBy(array, iteratee) {
  return (array && array.length)
    ? baseUniq(array, getIteratee(iteratee))
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.uniqWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniqWith (array, comparator)](#apidoc.element.sequelize.Utils._.uniqWith)
- description and source-code
```javascript
function uniqWith(array, comparator) {
  return (array && array.length)
    ? baseUniq(array, undefined, comparator)
    : [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.uniqueId"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>uniqueId (prefix)](#apidoc.element.sequelize.Utils._.uniqueId)
- description and source-code
```javascript
function uniqueId(prefix) {
  var id = ++idCounter;
  return toString(prefix) + id;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.unset"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unset (object, path)](#apidoc.element.sequelize.Utils._.unset)
- description and source-code
```javascript
function unset(object, path) {
  return object == null ? true : baseUnset(object, path);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.unzip"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unzip (array)](#apidoc.element.sequelize.Utils._.unzip)
- description and source-code
```javascript
function unzip(array) {
  if (!(array && array.length)) {
    return [];
  }
  var length = 0;
  array = arrayFilter(array, function(group) {
    if (isArrayLikeObject(group)) {
      length = nativeMax(group.length, length);
      return true;
    }
  });
  return baseTimes(length, function(index) {
    return arrayMap(array, baseProperty(index));
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.unzipWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>unzipWith (array, iteratee)](#apidoc.element.sequelize.Utils._.unzipWith)
- description and source-code
```javascript
function unzipWith(array, iteratee) {
  if (!(array && array.length)) {
    return [];
  }
  var result = unzip(array);
  if (iteratee == null) {
    return result;
  }
  return arrayMap(result, function(group) {
    return apply(iteratee, undefined, group);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.update"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>update (object, path, updater)](#apidoc.element.sequelize.Utils._.update)
- description and source-code
```javascript
function update(object, path, updater) {
  return object == null ? object : baseUpdate(object, path, castFunction(updater));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.updateWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>updateWith (object, path, updater, customizer)](#apidoc.element.sequelize.Utils._.updateWith)
- description and source-code
```javascript
function updateWith(object, path, updater, customizer) {
  customizer = typeof customizer == 'function' ? customizer : undefined;
  return object == null ? object : baseUpdate(object, path, castFunction(updater), customizer);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.upperCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>upperCase (string)](#apidoc.element.sequelize.Utils._.upperCase)
- description and source-code
```javascript
upperCase = function (string) {
  return arrayReduce(words(deburr(string).replace(reApos, '')), callback, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.upperFirst"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>upperFirst (string)](#apidoc.element.sequelize.Utils._.upperFirst)
- description and source-code
```javascript
upperFirst = function (string) {
  string = toString(string);

  var strSymbols = reHasComplexSymbol.test(string)
    ? stringToArray(string)
    : undefined;

  var chr = strSymbols
    ? strSymbols[0]
    : string.charAt(0);

  var trailing = strSymbols
    ? castSlice(strSymbols, 1).join('')
    : string.slice(1);

  return chr[methodName]() + trailing;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.values"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>values (object)](#apidoc.element.sequelize.Utils._.values)
- description and source-code
```javascript
function values(object) {
  return object ? baseValues(object, keys(object)) : [];
}
```
- example usage
```shell
...
}
};

QueryInterface.prototype.changeColumn = function(tableName, attributeName, dataTypeOrOptions, options) {
var attributes = {};
options = options || {};

if (Utils._.values(DataTypes).indexOf(dataTypeOrOptions) > -1) {
  attributes[attributeName] = { type: dataTypeOrOptions, allowNull: true };
} else {
  attributes[attributeName] = dataTypeOrOptions;
}

attributes[attributeName].type = this.sequelize.normalizeDataType(attributes[attributeName].type);
...
```

#### <a name="apidoc.element.sequelize.Utils._.valuesIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>valuesIn (object)](#apidoc.element.sequelize.Utils._.valuesIn)
- description and source-code
```javascript
function valuesIn(object) {
  return object == null ? [] : baseValues(object, keysIn(object));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.without"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>without ()](#apidoc.element.sequelize.Utils._.without)
- description and source-code
```javascript
without = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.words"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>words (string, pattern, guard)](#apidoc.element.sequelize.Utils._.words)
- description and source-code
```javascript
function words(string, pattern, guard) {
  string = toString(string);
  pattern = guard ? undefined : pattern;

  if (pattern === undefined) {
    pattern = reHasComplexWord.test(string) ? reComplexWord : reBasicWord;
  }
  return string.match(pattern) || [];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.wrap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>wrap (value, wrapper)](#apidoc.element.sequelize.Utils._.wrap)
- description and source-code
```javascript
function wrap(value, wrapper) {
  wrapper = wrapper == null ? identity : wrapper;
  return partial(wrapper, value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.xor"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>xor ()](#apidoc.element.sequelize.Utils._.xor)
- description and source-code
```javascript
xor = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.xorBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>xorBy ()](#apidoc.element.sequelize.Utils._.xorBy)
- description and source-code
```javascript
xorBy = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.xorWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>xorWith ()](#apidoc.element.sequelize.Utils._.xorWith)
- description and source-code
```javascript
xorWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.zip"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zip ()](#apidoc.element.sequelize.Utils._.zip)
- description and source-code
```javascript
zip = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.zipObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zipObject (props, values)](#apidoc.element.sequelize.Utils._.zipObject)
- description and source-code
```javascript
function zipObject(props, values) {
  return baseZipObject(props || [], values || [], assignValue);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.zipObjectDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zipObjectDeep (props, values)](#apidoc.element.sequelize.Utils._.zipObjectDeep)
- description and source-code
```javascript
function zipObjectDeep(props, values) {
  return baseZipObject(props || [], values || [], baseSet);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.zipWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.</span>zipWith ()](#apidoc.element.sequelize.Utils._.zipWith)
- description and source-code
```javascript
zipWith = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Utils._.prototype"></a>[module sequelize.Utils._.prototype](#apidoc.module.sequelize.Utils._.prototype)

#### <a name="apidoc.element.sequelize.Utils._.prototype.add"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>add ()](#apidoc.element.sequelize.Utils._.prototype.add)
- description and source-code
```javascript
add = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
    }
  }

  deps = deps.filter(function(dep) {
    return tableName !== dep;
  });

  sorter.add(tableName, deps);
});

sorted = sorter.sort();
if (options.reverse) {
  sorted = sorted.reverse();
}
sorted.forEach(function(name) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.after"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>after ()](#apidoc.element.sequelize.Utils._.prototype.after)
- description and source-code
```javascript
after = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.ary"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>ary ()](#apidoc.element.sequelize.Utils._.prototype.ary)
- description and source-code
```javascript
ary = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.assign"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assign ()](#apidoc.element.sequelize.Utils._.prototype.assign)
- description and source-code
```javascript
assign = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  });
}
};

QueryInterface.prototype.showAllSchemas = function(options) {
var self = this;

options = _.assign({}, options, {
  raw: true,
  type: this.sequelize.QueryTypes.SELECT
});

var showSchemasSql = self.QueryGenerator.showSchemasQuery();

return this.sequelize.query(showSchemasSql, options).then(function(schemaNames) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.assignIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assignIn ()](#apidoc.element.sequelize.Utils._.prototype.assignIn)
- description and source-code
```javascript
assignIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.assignInWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assignInWith ()](#apidoc.element.sequelize.Utils._.prototype.assignInWith)
- description and source-code
```javascript
assignInWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.assignWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>assignWith ()](#apidoc.element.sequelize.Utils._.prototype.assignWith)
- description and source-code
```javascript
assignWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.at"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>at ()](#apidoc.element.sequelize.Utils._.prototype.at)
- description and source-code
```javascript
at = function () {
  var args = arguments,
      index = -1,
      length = nativeMax(args.length - start, 0),
      array = Array(length);

  while (++index < length) {
    array[index] = args[start + index];
  }
  switch (start) {
    case 0: return func.call(this, array);
    case 1: return func.call(this, args[0], array);
    case 2: return func.call(this, args[0], args[1], array);
  }
  var otherArgs = Array(start + 1);
  index = -1;
  while (++index < start) {
    otherArgs[index] = args[index];
  }
  otherArgs[start] = array;
  return apply(func, this, otherArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.attempt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>attempt ()](#apidoc.element.sequelize.Utils._.prototype.attempt)
- description and source-code
```javascript
attempt = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.before"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>before ()](#apidoc.element.sequelize.Utils._.prototype.before)
- description and source-code
```javascript
before = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.bind"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>bind ()](#apidoc.element.sequelize.Utils._.prototype.bind)
- description and source-code
```javascript
bind = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...

   var validatorPromise = self._invokeBuiltinValidator(value, test, validatorType, field);
   // errors are handled in settling, stub this
   validatorPromise.catch(function() {});
   validators.push(validatorPromise.reflect());
 });

 return Promise.all(validators).then(this._handleReflectedResult.bind(this, field));
};

/**
* Prepare and invoke a custom validator.
*
* @param {Function} validator The custom validator.
* @param {string} validatorType the custom validator type (name).
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.bindAll"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>bindAll ()](#apidoc.element.sequelize.Utils._.prototype.bindAll)
- description and source-code
```javascript
bindAll = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.bindKey"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>bindKey ()](#apidoc.element.sequelize.Utils._.prototype.bindKey)
- description and source-code
```javascript
bindKey = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.camelCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>camelCase ()](#apidoc.element.sequelize.Utils._.prototype.camelCase)
- description and source-code
```javascript
camelCase = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.capitalize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>capitalize ()](#apidoc.element.sequelize.Utils._.prototype.capitalize)
- description and source-code
```javascript
capitalize = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.castArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>castArray ()](#apidoc.element.sequelize.Utils._.prototype.castArray)
- description and source-code
```javascript
castArray = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.ceil"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>ceil ()](#apidoc.element.sequelize.Utils._.prototype.ceil)
- description and source-code
```javascript
ceil = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.chain"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>chain ()](#apidoc.element.sequelize.Utils._.prototype.chain)
- description and source-code
```javascript
function wrapperChain() {
  return chain(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.chunk"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>chunk ()](#apidoc.element.sequelize.Utils._.prototype.chunk)
- description and source-code
```javascript
chunk = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.clamp"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>clamp ()](#apidoc.element.sequelize.Utils._.prototype.clamp)
- description and source-code
```javascript
clamp = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.clone"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>clone ()](#apidoc.element.sequelize.Utils._.prototype.clone)
- description and source-code
```javascript
clone = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
 * The Main Instance Validator.
 *
 * @param {Instance} modelInstance The model instance.
 * @param {Object} options A dict with options.
 * @constructor
 */
function InstanceValidator(modelInstance, options) {
options = _.clone(options) || {};

if (options.fields && !options.skip) {
  options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
}

// assign defined and default options
this.options = Utils._.defaults(options, {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.cloneDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cloneDeep ()](#apidoc.element.sequelize.Utils._.prototype.cloneDeep)
- description and source-code
```javascript
cloneDeep = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
// testhint argsConform.end

if (!rawTablename) {
  // Map for backwards compat
  rawTablename = tableName;
}

options = Utils.cloneDeep(options);
options.fields = attributes;
var sql = this.QueryGenerator.addIndexQuery(tableName, options, rawTablename);
return this.sequelize.query(sql, _.assign({}, options, { supportsSearchPath: false }));
};

QueryInterface.prototype.showIndex = function(tableName, options) {
var sql = this.QueryGenerator.showIndexesQuery(tableName, options);
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.cloneDeepWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cloneDeepWith ()](#apidoc.element.sequelize.Utils._.prototype.cloneDeepWith)
- description and source-code
```javascript
cloneDeepWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.cloneWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cloneWith ()](#apidoc.element.sequelize.Utils._.prototype.cloneWith)
- description and source-code
```javascript
cloneWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.commit"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>commit ()](#apidoc.element.sequelize.Utils._.prototype.commit)
- description and source-code
```javascript
function wrapperCommit() {
  return new LodashWrapper(this.value(), this.__chain__);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.compact"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>compact ()](#apidoc.element.sequelize.Utils._.prototype.compact)
- description and source-code
```javascript
compact = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
...
    var result = {};

    tableNames.forEach(function(tableName, i) {
      if (Utils._.isObject(tableName)) {
        tableName = tableName.schema + '.' + tableName.tableName;
      }

      result[tableName] = Utils._.compact(results[i]).map(function(r) {
        return r.constraint_name;
      });
    });

    return result;
  });
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.concat"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>concat ()](#apidoc.element.sequelize.Utils._.prototype.concat)
- description and source-code
```javascript
concat = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  replaceHookAliases: function(hooks) {
var realHookName;

Utils._.each(hooks, function(hooksArray, name) {
  // Does an alias for this hook name exist?
  if (realHookName = hookAliases[name]) {
    // Add the hooks to the actual hook
    hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

    // Delete the alias
    delete hooks[name];
  }
});

return hooks;
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.cond"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>cond ()](#apidoc.element.sequelize.Utils._.prototype.cond)
- description and source-code
```javascript
cond = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.conforms"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>conforms ()](#apidoc.element.sequelize.Utils._.prototype.conforms)
- description and source-code
```javascript
conforms = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.constant"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>constant ()](#apidoc.element.sequelize.Utils._.prototype.constant)
- description and source-code
```javascript
constant = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.countBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>countBy ()](#apidoc.element.sequelize.Utils._.prototype.countBy)
- description and source-code
```javascript
countBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.create"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>create ()](#apidoc.element.sequelize.Utils._.prototype.create)
- description and source-code
```javascript
create = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.curry"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>curry ()](#apidoc.element.sequelize.Utils._.prototype.curry)
- description and source-code
```javascript
curry = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.curryRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>curryRight ()](#apidoc.element.sequelize.Utils._.prototype.curryRight)
- description and source-code
```javascript
curryRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.debounce"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>debounce ()](#apidoc.element.sequelize.Utils._.prototype.debounce)
- description and source-code
```javascript
debounce = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.deburr"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>deburr ()](#apidoc.element.sequelize.Utils._.prototype.deburr)
- description and source-code
```javascript
deburr = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.defaults"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>defaults ()](#apidoc.element.sequelize.Utils._.prototype.defaults)
- description and source-code
```javascript
defaults = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
options = _.clone(options) || {};

if (options.fields && !options.skip) {
  options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
}

// assign defined and default options
this.options = Utils._.defaults(options, {
  skip: []
});

this.modelInstance = modelInstance;

/**
 * Exposes a reference to validator.js. This allows you to add custom validations using 'validator.extend'
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.defaultsDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>defaultsDeep ()](#apidoc.element.sequelize.Utils._.prototype.defaultsDeep)
- description and source-code
```javascript
defaultsDeep = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.defer"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>defer ()](#apidoc.element.sequelize.Utils._.prototype.defer)
- description and source-code
```javascript
defer = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.delay"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>delay ()](#apidoc.element.sequelize.Utils._.prototype.delay)
- description and source-code
```javascript
delay = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.difference"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>difference ()](#apidoc.element.sequelize.Utils._.prototype.difference)
- description and source-code
```javascript
difference = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
 * @param {Object} options A dict with options.
 * @constructor
 */
function InstanceValidator(modelInstance, options) {
options = _.clone(options) || {};

if (options.fields && !options.skip) {
  options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
}

// assign defined and default options
this.options = Utils._.defaults(options, {
  skip: []
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.differenceBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>differenceBy ()](#apidoc.element.sequelize.Utils._.prototype.differenceBy)
- description and source-code
```javascript
differenceBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.differenceWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>differenceWith ()](#apidoc.element.sequelize.Utils._.prototype.differenceWith)
- description and source-code
```javascript
differenceWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.divide"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>divide ()](#apidoc.element.sequelize.Utils._.prototype.divide)
- description and source-code
```javascript
divide = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.drop"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>drop ()](#apidoc.element.sequelize.Utils._.prototype.drop)
- description and source-code
```javascript
drop = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
...
};

QueryInterface.prototype.dropAllSchemas = function(options) {
  options = options || {};

  var self = this;
  if (!this.QueryGenerator._dialect.supports.schemas) {
    return this.sequelize.drop(options);
  } else {
    return this.showAllSchemas(options).map(function(schemaName) {
      return self.dropSchema(schemaName, options);
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.dropRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>dropRight ()](#apidoc.element.sequelize.Utils._.prototype.dropRight)
- description and source-code
```javascript
dropRight = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.dropRightWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>dropRightWhile ()](#apidoc.element.sequelize.Utils._.prototype.dropRightWhile)
- description and source-code
```javascript
dropRightWhile = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.dropWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>dropWhile ()](#apidoc.element.sequelize.Utils._.prototype.dropWhile)
- description and source-code
```javascript
dropWhile = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.each"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>each ()](#apidoc.element.sequelize.Utils._.prototype.each)
- description and source-code
```javascript
each = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  beforeConnection: 'beforeConnect'
};

var Hooks = {
  replaceHookAliases: function(hooks) {
    var realHookName;

    Utils._.each(hooks, function(hooksArray, name) {
      // Does an alias for this hook name exist?
      if (realHookName = hookAliases[name]) {
// Add the hooks to the actual hook
hooks[realHookName] = (hooks[realHookName] || []).concat(hooksArray);

// Delete the alias
delete hooks[name];
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.eachRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>eachRight ()](#apidoc.element.sequelize.Utils._.prototype.eachRight)
- description and source-code
```javascript
eachRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.endsWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>endsWith ()](#apidoc.element.sequelize.Utils._.prototype.endsWith)
- description and source-code
```javascript
endsWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.entries"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>entries ()](#apidoc.element.sequelize.Utils._.prototype.entries)
- description and source-code
```javascript
entries = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.entriesIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>entriesIn ()](#apidoc.element.sequelize.Utils._.prototype.entriesIn)
- description and source-code
```javascript
entriesIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.eq"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>eq ()](#apidoc.element.sequelize.Utils._.prototype.eq)
- description and source-code
```javascript
eq = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.escape"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>escape ()](#apidoc.element.sequelize.Utils._.prototype.escape)
- description and source-code
```javascript
escape = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
return this.QueryGenerator.quoteIdentifiers(identifiers, force);
};

/**
 * Escape a value (e.g. a string, number or date)
 */
QueryInterface.prototype.escape = function(value) {
return this.QueryGenerator.escape(value);
};

QueryInterface.prototype.setAutocommit = function(transaction, value, options) {
if (!transaction || !(transaction instanceof Transaction)) {
  throw new Error('Unable to set autocommit for a transaction without transaction object!');
}
if (transaction.parent) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.escapeRegExp"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>escapeRegExp ()](#apidoc.element.sequelize.Utils._.prototype.escapeRegExp)
- description and source-code
```javascript
escapeRegExp = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.every"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>every ()](#apidoc.element.sequelize.Utils._.prototype.every)
- description and source-code
```javascript
every = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.extend"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>extend ()](#apidoc.element.sequelize.Utils._.prototype.extend)
- description and source-code
```javascript
extend = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.extendWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>extendWith ()](#apidoc.element.sequelize.Utils._.prototype.extendWith)
- description and source-code
```javascript
extendWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.fill"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>fill ()](#apidoc.element.sequelize.Utils._.prototype.fill)
- description and source-code
```javascript
fill = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.filter"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>filter ()](#apidoc.element.sequelize.Utils._.prototype.filter)
- description and source-code
```javascript
filter = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
...
  removeHook: function(hookType, name) {
hookType = hookAliases[hookType] || hookType;

if (!this.hasHook(hookType)) {
  return this;
}

this.options.hooks[hookType] = this.options.hooks[hookType].filter(function (hook) {
  // don't remove unnamed hooks
  if (typeof hook === 'function') {
    return true;
  }

  return typeof hook === 'object' && hook.name !== name;
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.find"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>find ()](#apidoc.element.sequelize.Utils._.prototype.find)
- description and source-code
```javascript
find = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
...

QueryInterface.prototype.bulkUpdate = function(tableName, values, identifier, options, attributes) {
options = Utils.cloneDeep(options);
if (typeof identifier === 'object') identifier = Utils.cloneDeep(identifier);

var sql = this.QueryGenerator.updateQuery(tableName, values, identifier, options, attributes)
  , table = Utils._.isObject(tableName) ? tableName : { tableName: tableName }
  , model = Utils._.find(this.sequelize.modelManager.models, { tableName: table.tableName });

options.model = model;
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.delete = function(instance, tableName, identifier, options) {
var self = this
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.findIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findIndex ()](#apidoc.element.sequelize.Utils._.prototype.findIndex)
- description and source-code
```javascript
findIndex = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.findKey"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findKey ()](#apidoc.element.sequelize.Utils._.prototype.findKey)
- description and source-code
```javascript
findKey = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.findLast"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findLast ()](#apidoc.element.sequelize.Utils._.prototype.findLast)
- description and source-code
```javascript
findLast = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.findLastIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findLastIndex ()](#apidoc.element.sequelize.Utils._.prototype.findLastIndex)
- description and source-code
```javascript
findLastIndex = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.findLastKey"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>findLastKey ()](#apidoc.element.sequelize.Utils._.prototype.findLastKey)
- description and source-code
```javascript
findLastKey = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.first"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>first ()](#apidoc.element.sequelize.Utils._.prototype.first)
- description and source-code
```javascript
first = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flatMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatMap ()](#apidoc.element.sequelize.Utils._.prototype.flatMap)
- description and source-code
```javascript
flatMap = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flatMapDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatMapDeep ()](#apidoc.element.sequelize.Utils._.prototype.flatMapDeep)
- description and source-code
```javascript
flatMapDeep = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flatMapDepth"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatMapDepth ()](#apidoc.element.sequelize.Utils._.prototype.flatMapDepth)
- description and source-code
```javascript
flatMapDepth = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flatten"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flatten ()](#apidoc.element.sequelize.Utils._.prototype.flatten)
- description and source-code
```javascript
flatten = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
    raw: true,
    type: this.sequelize.QueryTypes.SELECT
  });

  var showSchemasSql = self.QueryGenerator.showSchemasQuery();

  return this.sequelize.query(showSchemasSql, options).then(function(schemaNames) {
    return Utils._.flatten(
      Utils._.map(schemaNames, function(value) {
        return (!!value.schema_name ? value.schema_name : value);
      })
    );
  });
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flattenDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flattenDeep ()](#apidoc.element.sequelize.Utils._.prototype.flattenDeep)
- description and source-code
```javascript
flattenDeep = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flattenDepth"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flattenDepth ()](#apidoc.element.sequelize.Utils._.prototype.flattenDepth)
- description and source-code
```javascript
flattenDepth = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flip"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flip ()](#apidoc.element.sequelize.Utils._.prototype.flip)
- description and source-code
```javascript
flip = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.floor"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>floor ()](#apidoc.element.sequelize.Utils._.prototype.floor)
- description and source-code
```javascript
floor = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flow"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flow ()](#apidoc.element.sequelize.Utils._.prototype.flow)
- description and source-code
```javascript
flow = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.flowRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>flowRight ()](#apidoc.element.sequelize.Utils._.prototype.flowRight)
- description and source-code
```javascript
flowRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.forEach"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forEach ()](#apidoc.element.sequelize.Utils._.prototype.forEach)
- description and source-code
```javascript
forEach = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...

if (!Array.isArray(hooks)) {
  hooks = hooks === undefined ? [] : [hooks];
}

// run hooks as sync functions if flagged as sync
if (hookTypes[hookType] && hookTypes[hookType].sync) {
  hooks.forEach(function(hook) {
    if (typeof hook === 'object') hook = hook.fn;
    return hook.apply(self, fnArgs);
  });
  return;
}

// run hooks async
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.forEachRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forEachRight ()](#apidoc.element.sequelize.Utils._.prototype.forEachRight)
- description and source-code
```javascript
forEachRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.forIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forIn ()](#apidoc.element.sequelize.Utils._.prototype.forIn)
- description and source-code
```javascript
forIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
 * @private
 */
InstanceValidator.prototype._builtinValidators = function() {
  var self = this;

  // promisify all attribute invocations
  var validators = [];
  Utils._.forIn(this.modelInstance.rawAttributes, function(rawAttribute, field) {
if (self.options.skip.indexOf(field) >= 0) {
  return;
}

var value = self.modelInstance.dataValues[field];

if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.forInRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forInRight ()](#apidoc.element.sequelize.Utils._.prototype.forInRight)
- description and source-code
```javascript
forInRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.forOwn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forOwn ()](#apidoc.element.sequelize.Utils._.prototype.forOwn)
- description and source-code
```javascript
forOwn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.forOwnRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>forOwnRight ()](#apidoc.element.sequelize.Utils._.prototype.forOwnRight)
- description and source-code
```javascript
forOwnRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.fromPairs"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>fromPairs ()](#apidoc.element.sequelize.Utils._.prototype.fromPairs)
- description and source-code
```javascript
fromPairs = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.functions"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>functions ()](#apidoc.element.sequelize.Utils._.prototype.functions)
- description and source-code
```javascript
functions = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.functionsIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>functionsIn ()](#apidoc.element.sequelize.Utils._.prototype.functionsIn)
- description and source-code
```javascript
functionsIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>get ()](#apidoc.element.sequelize.Utils._.prototype.get)
- description and source-code
```javascript
get = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.groupBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>groupBy ()](#apidoc.element.sequelize.Utils._.prototype.groupBy)
- description and source-code
```javascript
groupBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.gt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>gt ()](#apidoc.element.sequelize.Utils._.prototype.gt)
- description and source-code
```javascript
gt = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.gte"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>gte ()](#apidoc.element.sequelize.Utils._.prototype.gte)
- description and source-code
```javascript
gte = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.has"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>has ()](#apidoc.element.sequelize.Utils._.prototype.has)
- description and source-code
```javascript
has = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.hasIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>hasIn ()](#apidoc.element.sequelize.Utils._.prototype.hasIn)
- description and source-code
```javascript
hasIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.head"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>head ()](#apidoc.element.sequelize.Utils._.prototype.head)
- description and source-code
```javascript
head = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.identity"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>identity ()](#apidoc.element.sequelize.Utils._.prototype.identity)
- description and source-code
```javascript
identity = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.inRange"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>inRange ()](#apidoc.element.sequelize.Utils._.prototype.inRange)
- description and source-code
```javascript
inRange = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.includes"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>includes ()](#apidoc.element.sequelize.Utils._.prototype.includes)
- description and source-code
```javascript
includes = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.indexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>indexOf ()](#apidoc.element.sequelize.Utils._.prototype.indexOf)
- description and source-code
```javascript
indexOf = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
 */
InstanceValidator.prototype._builtinValidators = function() {
  var self = this;

  // promisify all attribute invocations
  var validators = [];
  Utils._.forIn(this.modelInstance.rawAttributes, function(rawAttribute, field) {
if (self.options.skip.indexOf(field) >= 0) {
  return;
}

var value = self.modelInstance.dataValues[field];

if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
  // perform validations based on schema
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.initial"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>initial ()](#apidoc.element.sequelize.Utils._.prototype.initial)
- description and source-code
```javascript
initial = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.intersection"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>intersection ()](#apidoc.element.sequelize.Utils._.prototype.intersection)
- description and source-code
```javascript
intersection = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
      return field;
    });
    indexes.push(indexFields);
  }
});

indexes.forEach(function (index) {
  if (Utils._.intersection(attributes, index).length === index.length) {
    where = {};
    index.forEach(function (field) {
      where[field] = valuesByField[field];
    });
    wheres.push(where);
  }
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.intersectionBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>intersectionBy ()](#apidoc.element.sequelize.Utils._.prototype.intersectionBy)
- description and source-code
```javascript
intersectionBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.intersectionWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>intersectionWith ()](#apidoc.element.sequelize.Utils._.prototype.intersectionWith)
- description and source-code
```javascript
intersectionWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.invert"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invert ()](#apidoc.element.sequelize.Utils._.prototype.invert)
- description and source-code
```javascript
invert = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.invertBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invertBy ()](#apidoc.element.sequelize.Utils._.prototype.invertBy)
- description and source-code
```javascript
invertBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.invoke"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invoke ()](#apidoc.element.sequelize.Utils._.prototype.invoke)
- description and source-code
```javascript
invoke = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.invokeMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>invokeMap ()](#apidoc.element.sequelize.Utils._.prototype.invokeMap)
- description and source-code
```javascript
invokeMap = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isArguments"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArguments ()](#apidoc.element.sequelize.Utils._.prototype.isArguments)
- description and source-code
```javascript
isArguments = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArray ()](#apidoc.element.sequelize.Utils._.prototype.isArray)
- description and source-code
```javascript
isArray = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
if (typeof fnArgs[fnArgs.length - 1] === 'function') {
  fn = fnArgs.pop();
}

if (typeof hooks === 'string') {
  hookType = hooks;
  hooks = this.options.hooks[hookType] || [];
  if (!Array.isArray(hooks)) hooks = hooks === undefined ? [] : [hooks];
  if (this.sequelize) hooks = hooks.concat(this.sequelize.options.hooks[hookType] || []);
}

if (!Array.isArray(hooks)) {
  hooks = hooks === undefined ? [] : [hooks];
}
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isArrayBuffer"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArrayBuffer ()](#apidoc.element.sequelize.Utils._.prototype.isArrayBuffer)
- description and source-code
```javascript
isArrayBuffer = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isArrayLike"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArrayLike ()](#apidoc.element.sequelize.Utils._.prototype.isArrayLike)
- description and source-code
```javascript
isArrayLike = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isArrayLikeObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isArrayLikeObject ()](#apidoc.element.sequelize.Utils._.prototype.isArrayLikeObject)
- description and source-code
```javascript
isArrayLikeObject = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isBoolean"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isBoolean ()](#apidoc.element.sequelize.Utils._.prototype.isBoolean)
- description and source-code
```javascript
isBoolean = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isBuffer"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isBuffer ()](#apidoc.element.sequelize.Utils._.prototype.isBuffer)
- description and source-code
```javascript
isBuffer = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...

  if (rawAttribute.allowNull === false && ((value === null) || (value === undefined))) {
    error = new sequelizeError.ValidationErrorItem(field + ' cannot be null', 'notNull Violation', field, value);
    this.errors.push(error);
  }

  if (rawAttribute.type === DataTypes.STRING || rawAttribute.type instanceof DataTypes.STRING || rawAttribute.type === DataTypes
.TEXT || rawAttribute.type instanceof DataTypes.TEXT) {
    if (Array.isArray(value) || (_.isObject(value) && !value._isSequelizeMethod) && !Buffer.isBuffer(value)) {
      error = new sequelizeError.ValidationErrorItem(field + ' cannot be an array or an object', 'string violation', field, value
);
      this.errors.push(error);
    }
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isDate"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isDate ()](#apidoc.element.sequelize.Utils._.prototype.isDate)
- description and source-code
```javascript
isDate = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  var dataType = options.dataType;

  if (dataType instanceof DataTypes.DECIMAL || dataType instanceof DataTypes.FLOAT) {
    result = parseFloat(result);
  } else if (dataType instanceof DataTypes.INTEGER || dataType instanceof DataTypes.BIGINT) {
    result = parseInt(result, 10);
  } else if (dataType instanceof DataTypes.DATE) {
    if (!Utils._.isNull(result) && !Utils._.isDate(result)) {
      result = new Date(result);
    }
  } else if (dataType instanceof DataTypes.STRING) {
    // Nothing to do, result is already a string.
  }
}
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isElement"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isElement ()](#apidoc.element.sequelize.Utils._.prototype.isElement)
- description and source-code
```javascript
isElement = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isEmpty"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isEmpty ()](#apidoc.element.sequelize.Utils._.prototype.isEmpty)
- description and source-code
```javascript
isEmpty = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  return this.sequelize.query(
    sql,
    _.assign({}, options, { type: QueryTypes.DESCRIBE })
  ).then(function(data) {
    // If no data is returned from the query, then the table name may be wrong.
    // Query generators that use information_schema for retrieving table info will just return an empty result set,
    // it will not throw an error like built-ins do (e.g. DESCRIBE on MySql).
    if (Utils._.isEmpty(data)) {
      return Promise.reject('No description found for "' + tableName + '" table. Check the table name and schema; remember, they
 _are_ case sensitive.');
    } else {
      return Promise.resolve(data);
    }
  });
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isEqual"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isEqual ()](#apidoc.element.sequelize.Utils._.prototype.isEqual)
- description and source-code
```javascript
isEqual = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isEqualWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isEqualWith ()](#apidoc.element.sequelize.Utils._.prototype.isEqualWith)
- description and source-code
```javascript
isEqualWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isError"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isError ()](#apidoc.element.sequelize.Utils._.prototype.isError)
- description and source-code
```javascript
isError = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isFinite"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isFinite ()](#apidoc.element.sequelize.Utils._.prototype.isFinite)
- description and source-code
```javascript
isFinite = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isFunction"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isFunction ()](#apidoc.element.sequelize.Utils._.prototype.isFunction)
- description and source-code
```javascript
isFunction = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isInteger ()](#apidoc.element.sequelize.Utils._.prototype.isInteger)
- description and source-code
```javascript
isInteger = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isLength"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isLength ()](#apidoc.element.sequelize.Utils._.prototype.isLength)
- description and source-code
```javascript
isLength = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isMap ()](#apidoc.element.sequelize.Utils._.prototype.isMap)
- description and source-code
```javascript
isMap = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isMatch"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isMatch ()](#apidoc.element.sequelize.Utils._.prototype.isMatch)
- description and source-code
```javascript
isMatch = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isMatchWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isMatchWith ()](#apidoc.element.sequelize.Utils._.prototype.isMatchWith)
- description and source-code
```javascript
isMatchWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isNaN"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNaN ()](#apidoc.element.sequelize.Utils._.prototype.isNaN)
- description and source-code
```javascript
isNaN = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isNative"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNative ()](#apidoc.element.sequelize.Utils._.prototype.isNative)
- description and source-code
```javascript
isNative = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isNil"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNil ()](#apidoc.element.sequelize.Utils._.prototype.isNil)
- description and source-code
```javascript
isNil = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isNull"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNull ()](#apidoc.element.sequelize.Utils._.prototype.isNull)
- description and source-code
```javascript
isNull = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  var dataType = options.dataType;

  if (dataType instanceof DataTypes.DECIMAL || dataType instanceof DataTypes.FLOAT) {
    result = parseFloat(result);
  } else if (dataType instanceof DataTypes.INTEGER || dataType instanceof DataTypes.BIGINT) {
    result = parseInt(result, 10);
  } else if (dataType instanceof DataTypes.DATE) {
    if (!Utils._.isNull(result) && !Utils._.isDate(result)) {
      result = new Date(result);
    }
  } else if (dataType instanceof DataTypes.STRING) {
    // Nothing to do, result is already a string.
  }
}
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isNumber"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isNumber ()](#apidoc.element.sequelize.Utils._.prototype.isNumber)
- description and source-code
```javascript
isNumber = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isObject ()](#apidoc.element.sequelize.Utils._.prototype.isObject)
- description and source-code
```javascript
isObject = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...

  if (rawAttribute.allowNull === false && ((value === null) || (value === undefined))) {
    error = new sequelizeError.ValidationErrorItem(field + ' cannot be null', 'notNull Violation', field, value);
    this.errors.push(error);
  }

  if (rawAttribute.type === DataTypes.STRING || rawAttribute.type instanceof DataTypes.STRING || rawAttribute.type === DataTypes
.TEXT || rawAttribute.type instanceof DataTypes.TEXT) {
    if (Array.isArray(value) || (_.isObject(value) && !value._isSequelizeMethod) && !Buffer.isBuffer(value)) {
      error = new sequelizeError.ValidationErrorItem(field + ' cannot be an array or an object', 'string violation', field, value
);
      this.errors.push(error);
    }
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isObjectLike"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isObjectLike ()](#apidoc.element.sequelize.Utils._.prototype.isObjectLike)
- description and source-code
```javascript
isObjectLike = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isPlainObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isPlainObject ()](#apidoc.element.sequelize.Utils._.prototype.isPlainObject)
- description and source-code
```javascript
isPlainObject = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  , self = this
  , sql = ''
  , i = 0;

options = _.clone(options) || {};

attributes = Utils._.mapValues(attributes, function(attribute) {
  if (!Utils._.isPlainObject(attribute)) {
    attribute = { type: attribute, allowNull: true };
  }

  attribute = self.sequelize.normalizeAttribute(attribute);

  return attribute;
});
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isRegExp"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isRegExp ()](#apidoc.element.sequelize.Utils._.prototype.isRegExp)
- description and source-code
```javascript
isRegExp = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isSafeInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isSafeInteger ()](#apidoc.element.sequelize.Utils._.prototype.isSafeInteger)
- description and source-code
```javascript
isSafeInteger = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isSet"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isSet ()](#apidoc.element.sequelize.Utils._.prototype.isSet)
- description and source-code
```javascript
isSet = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isString"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isString ()](#apidoc.element.sequelize.Utils._.prototype.isString)
- description and source-code
```javascript
isString = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isSymbol"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isSymbol ()](#apidoc.element.sequelize.Utils._.prototype.isSymbol)
- description and source-code
```javascript
isSymbol = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isTypedArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isTypedArray ()](#apidoc.element.sequelize.Utils._.prototype.isTypedArray)
- description and source-code
```javascript
isTypedArray = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isUndefined"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isUndefined ()](#apidoc.element.sequelize.Utils._.prototype.isUndefined)
- description and source-code
```javascript
isUndefined = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isWeakMap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isWeakMap ()](#apidoc.element.sequelize.Utils._.prototype.isWeakMap)
- description and source-code
```javascript
isWeakMap = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.isWeakSet"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>isWeakSet ()](#apidoc.element.sequelize.Utils._.prototype.isWeakSet)
- description and source-code
```javascript
isWeakSet = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.iteratee"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>iteratee ()](#apidoc.element.sequelize.Utils._.prototype.iteratee)
- description and source-code
```javascript
iteratee = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.join"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>join ()](#apidoc.element.sequelize.Utils._.prototype.join)
- description and source-code
```javascript
join = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
 if (message) {
   this.message = message;

 // ... otherwise create a concatenated message out of existing errors.
 } else if (this.errors.length > 0 && this.errors[0].message) {
   this.message = this.errors.map(function(err) {
     return err.type + ': ' + err.message;
   }).join(',\n');
 }
};
util.inherits(error.ValidationError, error.BaseError);

/**
* Gets all validation error items for the path / field specified.
*
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.kebabCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>kebabCase ()](#apidoc.element.sequelize.Utils._.prototype.kebabCase)
- description and source-code
```javascript
kebabCase = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.keyBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>keyBy ()](#apidoc.element.sequelize.Utils._.prototype.keyBy)
- description and source-code
```javascript
keyBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.keys"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>keys ()](#apidoc.element.sequelize.Utils._.prototype.keys)
- description and source-code
```javascript
keys = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
  hooks: hookTypes,
  hookAliases: hookAliases,

  applyTo: function(Model) {
    Utils._.mixin(Model, Hooks);
    Utils._.mixin(Model.prototype, Hooks);

    var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
    allHooks.forEach(function(hook) {
      Model.prototype[hook] = function(name, callback) {
        return this.addHook(hook, name, callback);
      };
    });
  }
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.keysIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>keysIn ()](#apidoc.element.sequelize.Utils._.prototype.keysIn)
- description and source-code
```javascript
keysIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.last"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>last ()](#apidoc.element.sequelize.Utils._.prototype.last)
- description and source-code
```javascript
last = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.lastIndexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lastIndexOf ()](#apidoc.element.sequelize.Utils._.prototype.lastIndexOf)
- description and source-code
```javascript
lastIndexOf = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.lowerCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lowerCase ()](#apidoc.element.sequelize.Utils._.prototype.lowerCase)
- description and source-code
```javascript
lowerCase = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.lowerFirst"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lowerFirst ()](#apidoc.element.sequelize.Utils._.prototype.lowerFirst)
- description and source-code
```javascript
lowerFirst = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.lt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lt ()](#apidoc.element.sequelize.Utils._.prototype.lt)
- description and source-code
```javascript
lt = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.lte"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>lte ()](#apidoc.element.sequelize.Utils._.prototype.lte)
- description and source-code
```javascript
lte = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.map"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>map ()](#apidoc.element.sequelize.Utils._.prototype.map)
- description and source-code
```javascript
map = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
...

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
};
util.inherits(error.ValidationError, error.BaseError);

/**
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.mapKeys"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mapKeys ()](#apidoc.element.sequelize.Utils._.prototype.mapKeys)
- description and source-code
```javascript
mapKeys = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.mapValues"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mapValues ()](#apidoc.element.sequelize.Utils._.prototype.mapValues)
- description and source-code
```javascript
mapValues = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
, keyLen = keys.length
, self = this
, sql = ''
, i = 0;

  options = _.clone(options) || {};

  attributes = Utils._.mapValues(attributes, function(attribute) {
if (!Utils._.isPlainObject(attribute)) {
  attribute = { type: attribute, allowNull: true };
}

attribute = self.sequelize.normalizeAttribute(attribute);

return attribute;
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.matches"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>matches ()](#apidoc.element.sequelize.Utils._.prototype.matches)
- description and source-code
```javascript
matches = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.matchesProperty"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>matchesProperty ()](#apidoc.element.sequelize.Utils._.prototype.matchesProperty)
- description and source-code
```javascript
matchesProperty = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.max"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>max ()](#apidoc.element.sequelize.Utils._.prototype.max)
- description and source-code
```javascript
max = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.maxBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>maxBy ()](#apidoc.element.sequelize.Utils._.prototype.maxBy)
- description and source-code
```javascript
maxBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.mean"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mean ()](#apidoc.element.sequelize.Utils._.prototype.mean)
- description and source-code
```javascript
mean = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.meanBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>meanBy ()](#apidoc.element.sequelize.Utils._.prototype.meanBy)
- description and source-code
```javascript
meanBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.memoize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>memoize ()](#apidoc.element.sequelize.Utils._.prototype.memoize)
- description and source-code
```javascript
memoize = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.merge"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>merge ()](#apidoc.element.sequelize.Utils._.prototype.merge)
- description and source-code
```javascript
merge = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.mergeWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mergeWith ()](#apidoc.element.sequelize.Utils._.prototype.mergeWith)
- description and source-code
```javascript
mergeWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.method"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>method ()](#apidoc.element.sequelize.Utils._.prototype.method)
- description and source-code
```javascript
method = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
 * @param {Function} validator The custom validator.
 * @param {string} validatorType the custom validator type (name).
 * @param {boolean=} optAttrDefined Set to true if custom validator was defined
 *   from the Attribute
 * @return {Promise} A promise.
 * @private
 */
InstanceValidator.prototype._invokeCustomValidator = Promise.method(function(validator, validatorType, optAttrDefined, optValue,
optField) {
var validatorFunction = null;  // the validation function to call
var isAsync = false;

var validatorArity = validator.length;
// check if validator is async and requires a callback
var asyncArity = 1;
var errorKey = validatorType;
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.methodOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>methodOf ()](#apidoc.element.sequelize.Utils._.prototype.methodOf)
- description and source-code
```javascript
methodOf = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.min"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>min ()](#apidoc.element.sequelize.Utils._.prototype.min)
- description and source-code
```javascript
min = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.minBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>minBy ()](#apidoc.element.sequelize.Utils._.prototype.minBy)
- description and source-code
```javascript
minBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.mixin"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>mixin ()](#apidoc.element.sequelize.Utils._.prototype.mixin)
- description and source-code
```javascript
mixin = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
   */

module.exports = {
  hooks: hookTypes,
  hookAliases: hookAliases,

  applyTo: function(Model) {
Utils._.mixin(Model, Hooks);
Utils._.mixin(Model.prototype, Hooks);

var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
allHooks.forEach(function(hook) {
  Model.prototype[hook] = function(name, callback) {
    return this.addHook(hook, name, callback);
  };
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.multiply"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>multiply ()](#apidoc.element.sequelize.Utils._.prototype.multiply)
- description and source-code
```javascript
multiply = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.negate"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>negate ()](#apidoc.element.sequelize.Utils._.prototype.negate)
- description and source-code
```javascript
negate = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.next"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>next ()](#apidoc.element.sequelize.Utils._.prototype.next)
- description and source-code
```javascript
function wrapperNext() {
  if (this.__values__ === undefined) {
    this.__values__ = toArray(this.value());
  }
  var done = this.__index__ >= this.__values__.length,
      value = done ? undefined : this.__values__[this.__index__++];

  return { 'done': done, 'value': value };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.noConflict"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>noConflict ()](#apidoc.element.sequelize.Utils._.prototype.noConflict)
- description and source-code
```javascript
noConflict = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.noop"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>noop ()](#apidoc.element.sequelize.Utils._.prototype.noop)
- description and source-code
```javascript
noop = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.now"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>now ()](#apidoc.element.sequelize.Utils._.prototype.now)
- description and source-code
```javascript
now = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.nth"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>nth ()](#apidoc.element.sequelize.Utils._.prototype.nth)
- description and source-code
```javascript
nth = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.nthArg"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>nthArg ()](#apidoc.element.sequelize.Utils._.prototype.nthArg)
- description and source-code
```javascript
nthArg = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.omit"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>omit ()](#apidoc.element.sequelize.Utils._.prototype.omit)
- description and source-code
```javascript
omit = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.omitBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>omitBy ()](#apidoc.element.sequelize.Utils._.prototype.omitBy)
- description and source-code
```javascript
omitBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.once"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>once ()](#apidoc.element.sequelize.Utils._.prototype.once)
- description and source-code
```javascript
once = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.orderBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>orderBy ()](#apidoc.element.sequelize.Utils._.prototype.orderBy)
- description and source-code
```javascript
orderBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.over"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>over ()](#apidoc.element.sequelize.Utils._.prototype.over)
- description and source-code
```javascript
over = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.overArgs"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>overArgs ()](#apidoc.element.sequelize.Utils._.prototype.overArgs)
- description and source-code
```javascript
overArgs = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.overEvery"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>overEvery ()](#apidoc.element.sequelize.Utils._.prototype.overEvery)
- description and source-code
```javascript
overEvery = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.overSome"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>overSome ()](#apidoc.element.sequelize.Utils._.prototype.overSome)
- description and source-code
```javascript
overSome = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pad"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pad ()](#apidoc.element.sequelize.Utils._.prototype.pad)
- description and source-code
```javascript
pad = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.padEnd"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>padEnd ()](#apidoc.element.sequelize.Utils._.prototype.padEnd)
- description and source-code
```javascript
padEnd = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.padStart"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>padStart ()](#apidoc.element.sequelize.Utils._.prototype.padStart)
- description and source-code
```javascript
padStart = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.parseInt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>parseInt ()](#apidoc.element.sequelize.Utils._.prototype.parseInt)
- description and source-code
```javascript
parseInt = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.partial"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>partial ()](#apidoc.element.sequelize.Utils._.prototype.partial)
- description and source-code
```javascript
partial = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
    return dataTypes[dialect].BLOB.prototype.stringify(val);
  }

  return dataTypes.BLOB.prototype.stringify(val);
}

if (Array.isArray(val)) {
  var escape = _.partial(SqlString.escape, _, timeZone, dialect, format);
  if (dialect === 'postgres' && !format) {
    return dataTypes.ARRAY.prototype.stringify(val, {escape: escape});
  }
  return val.map(escape);
}

if (dialect === 'postgres' || dialect === 'sqlite' || dialect === 'mssql') {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.partialRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>partialRight ()](#apidoc.element.sequelize.Utils._.prototype.partialRight)
- description and source-code
```javascript
partialRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.partition"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>partition ()](#apidoc.element.sequelize.Utils._.prototype.partition)
- description and source-code
```javascript
partition = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pick"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pick ()](#apidoc.element.sequelize.Utils._.prototype.pick)
- description and source-code
```javascript
pick = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pickBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pickBy ()](#apidoc.element.sequelize.Utils._.prototype.pickBy)
- description and source-code
```javascript
pickBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.plant"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>plant (value)](#apidoc.element.sequelize.Utils._.prototype.plant)
- description and source-code
```javascript
function wrapperPlant(value) {
  var result,
      parent = this;

  while (parent instanceof baseLodash) {
    var clone = wrapperClone(parent);
    clone.__index__ = 0;
    clone.__values__ = undefined;
    if (result) {
      previous.__wrapped__ = clone;
    } else {
      result = clone;
    }
    var previous = clone;
    parent = parent.__wrapped__;
  }
  previous.__wrapped__ = value;
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pop"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pop ()](#apidoc.element.sequelize.Utils._.prototype.pop)
- description and source-code
```javascript
pop = function () {
  var args = arguments;
  if (retUnwrapped && !this.__chain__) {
    var value = this.value();
    return func.apply(isArray(value) ? value : [], args);
  }
  return this[chainName](function(value) {
    return func.apply(isArray(value) ? value : [], args);
  });
}
```
- example usage
```shell
...
  runHooks: function(hooks) {
var self = this
  , fn
  , fnArgs = Utils.sliceArgs(arguments, 1)
  , hookType;

if (typeof fnArgs[fnArgs.length - 1] === 'function') {
  fn = fnArgs.pop();
}

if (typeof hooks === 'string') {
  hookType = hooks;
  hooks = this.options.hooks[hookType] || [];
  if (!Array.isArray(hooks)) hooks = hooks === undefined ? [] : [hooks];
  if (this.sequelize) hooks = hooks.concat(this.sequelize.options.hooks[hookType] || []);
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.property"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>property ()](#apidoc.element.sequelize.Utils._.prototype.property)
- description and source-code
```javascript
property = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.propertyOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>propertyOf ()](#apidoc.element.sequelize.Utils._.prototype.propertyOf)
- description and source-code
```javascript
propertyOf = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pull"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pull ()](#apidoc.element.sequelize.Utils._.prototype.pull)
- description and source-code
```javascript
pull = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pullAll"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAll ()](#apidoc.element.sequelize.Utils._.prototype.pullAll)
- description and source-code
```javascript
pullAll = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pullAllBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAllBy ()](#apidoc.element.sequelize.Utils._.prototype.pullAllBy)
- description and source-code
```javascript
pullAllBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pullAllWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAllWith ()](#apidoc.element.sequelize.Utils._.prototype.pullAllWith)
- description and source-code
```javascript
pullAllWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.pullAt"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>pullAt ()](#apidoc.element.sequelize.Utils._.prototype.pullAt)
- description and source-code
```javascript
pullAt = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.push"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>push ()](#apidoc.element.sequelize.Utils._.prototype.push)
- description and source-code
```javascript
push = function () {
  var args = arguments;
  if (retUnwrapped && !this.__chain__) {
    var value = this.value();
    return func.apply(isArray(value) ? value : [], args);
  }
  return this[chainName](function(value) {
    return func.apply(isArray(value) ? value : [], args);
  });
}
```
- example usage
```shell
...
*
* @param {string} path The path to be checked for error items
* @returns {Array} Validation error items for the specified path
*/
error.ValidationError.prototype.get = function(path) {
 return this.errors.reduce(function(reduced, error) {
   if (error.path === path) {
     reduced.push(error);
   }
   return reduced;
 }, []);
};

/**
* A base class for all database related errors.
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.random"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>random ()](#apidoc.element.sequelize.Utils._.prototype.random)
- description and source-code
```javascript
random = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.range"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>range ()](#apidoc.element.sequelize.Utils._.prototype.range)
- description and source-code
```javascript
range = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.rangeRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>rangeRight ()](#apidoc.element.sequelize.Utils._.prototype.rangeRight)
- description and source-code
```javascript
rangeRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.rearg"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>rearg ()](#apidoc.element.sequelize.Utils._.prototype.rearg)
- description and source-code
```javascript
rearg = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.reduce"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reduce ()](#apidoc.element.sequelize.Utils._.prototype.reduce)
- description and source-code
```javascript
reduce = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
/**
 * Gets all validation error items for the path / field specified.
 *
 * @param {string} path The path to be checked for error items
 * @returns {Array} Validation error items for the specified path
 */
error.ValidationError.prototype.get = function(path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.reduceRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reduceRight ()](#apidoc.element.sequelize.Utils._.prototype.reduceRight)
- description and source-code
```javascript
reduceRight = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.reject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reject ()](#apidoc.element.sequelize.Utils._.prototype.reject)
- description and source-code
```javascript
reject = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
...
    sql,
    _.assign({}, options, { type: QueryTypes.DESCRIBE })
  ).then(function(data) {
    // If no data is returned from the query, then the table name may be wrong.
    // Query generators that use information_schema for retrieving table info will just return an empty result set,
    // it will not throw an error like built-ins do (e.g. DESCRIBE on MySql).
    if (Utils._.isEmpty(data)) {
      return Promise.reject('No description found for "' + tableName + '" table. Check the table name and schema; remember, they
 _are_ case sensitive.');
    } else {
      return Promise.resolve(data);
    }
  });
};

QueryInterface.prototype.addColumn = function(table, key, attribute, options) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.remove"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>remove ()](#apidoc.element.sequelize.Utils._.prototype.remove)
- description and source-code
```javascript
remove = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.repeat"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>repeat ()](#apidoc.element.sequelize.Utils._.prototype.repeat)
- description and source-code
```javascript
repeat = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.replace"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>replace ()](#apidoc.element.sequelize.Utils._.prototype.replace)
- description and source-code
```javascript
replace = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
/* jshint -W110 */
var dataTypes = require('./data-types')
, _ = require('lodash')
, SqlString = exports;

SqlString.escapeId = function(val, forbidQualified) {
if (forbidQualified) {
  return ''' + val.replace(/'/g, '''') + ''';
}
return ''' + val.replace(/'/g, '''').replace(/\./g, ''.'') + ''';
};

SqlString.escape = function(val, timeZone, dialect, format) {
var prependN = false;
if (val === undefined || val === null) {
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.rest"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>rest ()](#apidoc.element.sequelize.Utils._.prototype.rest)
- description and source-code
```javascript
rest = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.result"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>result ()](#apidoc.element.sequelize.Utils._.prototype.result)
- description and source-code
```javascript
result = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.reverse"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>reverse ()](#apidoc.element.sequelize.Utils._.prototype.reverse)
- description and source-code
```javascript
function wrapperReverse() {
  var value = this.__wrapped__;
  if (value instanceof LazyWrapper) {
    var wrapped = value;
    if (this.__actions__.length) {
      wrapped = new LazyWrapper(this);
    }
    wrapped = wrapped.reverse();
    wrapped.__actions__.push({
      'func': thru,
      'args': [reverse],
      'thisArg': undefined
    });
    return new LodashWrapper(wrapped, this.__chain__);
  }
  return this.thru(reverse);
}
```
- example usage
```shell
...
    });

    sorter.add(tableName, deps);
  });

  sorted = sorter.sort();
  if (options.reverse) {
    sorted = sorted.reverse();
  }
  sorted.forEach(function(name) {
    iterator(models[name], name);
  });
};

module.exports = ModelManager;
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.round"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>round ()](#apidoc.element.sequelize.Utils._.prototype.round)
- description and source-code
```javascript
round = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.runInContext"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>runInContext ()](#apidoc.element.sequelize.Utils._.prototype.runInContext)
- description and source-code
```javascript
runInContext = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sample"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sample ()](#apidoc.element.sequelize.Utils._.prototype.sample)
- description and source-code
```javascript
sample = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sampleSize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sampleSize ()](#apidoc.element.sequelize.Utils._.prototype.sampleSize)
- description and source-code
```javascript
sampleSize = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.set"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>set ()](#apidoc.element.sequelize.Utils._.prototype.set)
- description and source-code
```javascript
set = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.setWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>setWith ()](#apidoc.element.sequelize.Utils._.prototype.setWith)
- description and source-code
```javascript
setWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.shift"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>shift ()](#apidoc.element.sequelize.Utils._.prototype.shift)
- description and source-code
```javascript
shift = function () {
  var args = arguments;
  if (retUnwrapped && !this.__chain__) {
    var value = this.value();
    return func.apply(isArray(value) ? value : [], args);
  }
  return this[chainName](function(value) {
    return func.apply(isArray(value) ? value : [], args);
  });
}
```
- example usage
```shell
...
values = [].concat(values);

return sql.replace(/\?/g, function(match) {
  if (!values.length) {
    return match;
  }

  return SqlString.escape(values.shift(), timeZone, dialect, true);
});
};

SqlString.formatNamedParameters = function(sql, values, timeZone, dialect) {
return sql.replace(/\:+(?!\d)(\w+)/g, function(value, key) {
  if ('postgres' === dialect && '::' === value.slice(0, 2)) {
    return value;
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.shuffle"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>shuffle ()](#apidoc.element.sequelize.Utils._.prototype.shuffle)
- description and source-code
```javascript
shuffle = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.size"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>size ()](#apidoc.element.sequelize.Utils._.prototype.size)
- description and source-code
```javascript
size = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.slice"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>slice ()](#apidoc.element.sequelize.Utils._.prototype.slice)
- description and source-code
```javascript
slice = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
...
     validatorArgs = [validatorArgs, field];
   } else if (isLocalizedValidator || validatorType === 'isIP') {
     validatorArgs = [];
   } else {
     validatorArgs = [validatorArgs];
   }
 } else {
   validatorArgs = validatorArgs.slice(0);
 }
 return validatorArgs;
};

/**
* Will validate a single field against its schema definition (isnull).
*
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.snakeCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>snakeCase ()](#apidoc.element.sequelize.Utils._.prototype.snakeCase)
- description and source-code
```javascript
snakeCase = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.some"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>some ()](#apidoc.element.sequelize.Utils._.prototype.some)
- description and source-code
```javascript
some = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sort"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sort ()](#apidoc.element.sequelize.Utils._.prototype.sort)
- description and source-code
```javascript
sort = function () {
  var args = arguments;
  if (retUnwrapped && !this.__chain__) {
    var value = this.value();
    return func.apply(isArray(value) ? value : [], args);
  }
  return this[chainName](function(value) {
    return func.apply(isArray(value) ? value : [], args);
  });
}
```
- example usage
```shell
...
    deps = deps.filter(function(dep) {
      return tableName !== dep;
    });

    sorter.add(tableName, deps);
  });

  sorted = sorter.sort();
  if (options.reverse) {
    sorted = sorted.reverse();
  }
  sorted.forEach(function(name) {
    iterator(models[name], name);
  });
};
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortBy ()](#apidoc.element.sequelize.Utils._.prototype.sortBy)
- description and source-code
```javascript
sortBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedIndex ()](#apidoc.element.sequelize.Utils._.prototype.sortedIndex)
- description and source-code
```javascript
sortedIndex = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedIndexBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedIndexBy ()](#apidoc.element.sequelize.Utils._.prototype.sortedIndexBy)
- description and source-code
```javascript
sortedIndexBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedIndexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedIndexOf ()](#apidoc.element.sequelize.Utils._.prototype.sortedIndexOf)
- description and source-code
```javascript
sortedIndexOf = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedLastIndex"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedLastIndex ()](#apidoc.element.sequelize.Utils._.prototype.sortedLastIndex)
- description and source-code
```javascript
sortedLastIndex = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedLastIndexBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedLastIndexBy ()](#apidoc.element.sequelize.Utils._.prototype.sortedLastIndexBy)
- description and source-code
```javascript
sortedLastIndexBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedLastIndexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedLastIndexOf ()](#apidoc.element.sequelize.Utils._.prototype.sortedLastIndexOf)
- description and source-code
```javascript
sortedLastIndexOf = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedUniq"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedUniq ()](#apidoc.element.sequelize.Utils._.prototype.sortedUniq)
- description and source-code
```javascript
sortedUniq = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sortedUniqBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sortedUniqBy ()](#apidoc.element.sequelize.Utils._.prototype.sortedUniqBy)
- description and source-code
```javascript
sortedUniqBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.splice"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>splice ()](#apidoc.element.sequelize.Utils._.prototype.splice)
- description and source-code
```javascript
splice = function () {
  var args = arguments;
  if (retUnwrapped && !this.__chain__) {
    var value = this.value();
    return func.apply(isArray(value) ? value : [], args);
  }
  return this[chainName](function(value) {
    return func.apply(isArray(value) ? value : [], args);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.split"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>split ()](#apidoc.element.sequelize.Utils._.prototype.split)
- description and source-code
```javascript
split = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.spread"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>spread ()](#apidoc.element.sequelize.Utils._.prototype.spread)
- description and source-code
```javascript
spread = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.startCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>startCase ()](#apidoc.element.sequelize.Utils._.prototype.startCase)
- description and source-code
```javascript
startCase = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.startsWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>startsWith ()](#apidoc.element.sequelize.Utils._.prototype.startsWith)
- description and source-code
```javascript
startsWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.subtract"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>subtract ()](#apidoc.element.sequelize.Utils._.prototype.subtract)
- description and source-code
```javascript
subtract = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sum"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sum ()](#apidoc.element.sequelize.Utils._.prototype.sum)
- description and source-code
```javascript
sum = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.sumBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>sumBy ()](#apidoc.element.sequelize.Utils._.prototype.sumBy)
- description and source-code
```javascript
sumBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.tail"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>tail ()](#apidoc.element.sequelize.Utils._.prototype.tail)
- description and source-code
```javascript
tail = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.take"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>take ()](#apidoc.element.sequelize.Utils._.prototype.take)
- description and source-code
```javascript
take = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.takeRight"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>takeRight ()](#apidoc.element.sequelize.Utils._.prototype.takeRight)
- description and source-code
```javascript
takeRight = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.takeRightWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>takeRightWhile ()](#apidoc.element.sequelize.Utils._.prototype.takeRightWhile)
- description and source-code
```javascript
takeRightWhile = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.takeWhile"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>takeWhile ()](#apidoc.element.sequelize.Utils._.prototype.takeWhile)
- description and source-code
```javascript
takeWhile = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.tap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>tap ()](#apidoc.element.sequelize.Utils._.prototype.tap)
- description and source-code
```javascript
tap = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.template"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>template ()](#apidoc.element.sequelize.Utils._.prototype.template)
- description and source-code
```javascript
template = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.throttle"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>throttle ()](#apidoc.element.sequelize.Utils._.prototype.throttle)
- description and source-code
```javascript
throttle = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.thru"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>thru ()](#apidoc.element.sequelize.Utils._.prototype.thru)
- description and source-code
```javascript
thru = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.times"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>times ()](#apidoc.element.sequelize.Utils._.prototype.times)
- description and source-code
```javascript
times = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toArray"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toArray ()](#apidoc.element.sequelize.Utils._.prototype.toArray)
- description and source-code
```javascript
toArray = function () {
  var value = this.__wrapped__,
      args = isTaker ? [1] : arguments,
      isLazy = value instanceof LazyWrapper,
      iteratee = args[0],
      useLazy = isLazy || isArray(value);

  var interceptor = function(value) {
    var result = lodashFunc.apply(lodash, arrayPush([value], args));
    return (isTaker && chainAll) ? result[0] : result;
  };

  if (useLazy && checkIteratee && typeof iteratee == 'function' && iteratee.length != 1) {
    // Avoid lazy use if the iteratee has a "length" value other than '1'.
    isLazy = useLazy = false;
  }
  var chainAll = this.__chain__,
      isHybrid = !!this.__actions__.length,
      isUnwrapped = retUnwrapped && !chainAll,
      onlyLazy = isLazy && !isHybrid;

  if (!retUnwrapped && useLazy) {
    value = onlyLazy ? value : new LazyWrapper(this);
    var result = func.apply(value, args);
    result.__actions__.push({ 'func': thru, 'args': [interceptor], 'thisArg': undefined });
    return new LodashWrapper(result, chainAll);
  }
  if (isUnwrapped && onlyLazy) {
    return func.apply(this, args);
  }
  result = this.thru(interceptor);
  return isUnwrapped ? (isTaker ? result.value()[0] : result.value()) : result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toFinite"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toFinite ()](#apidoc.element.sequelize.Utils._.prototype.toFinite)
- description and source-code
```javascript
toFinite = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toInteger ()](#apidoc.element.sequelize.Utils._.prototype.toInteger)
- description and source-code
```javascript
toInteger = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toJSON ()](#apidoc.element.sequelize.Utils._.prototype.toJSON)
- description and source-code
```javascript
function wrapperValue() {
  return baseWrapperValue(this.__wrapped__, this.__actions__);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toLength"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toLength ()](#apidoc.element.sequelize.Utils._.prototype.toLength)
- description and source-code
```javascript
toLength = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toLower"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toLower ()](#apidoc.element.sequelize.Utils._.prototype.toLower)
- description and source-code
```javascript
toLower = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toNumber"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toNumber ()](#apidoc.element.sequelize.Utils._.prototype.toNumber)
- description and source-code
```javascript
toNumber = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toPairs"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPairs ()](#apidoc.element.sequelize.Utils._.prototype.toPairs)
- description and source-code
```javascript
toPairs = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toPairsIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPairsIn ()](#apidoc.element.sequelize.Utils._.prototype.toPairsIn)
- description and source-code
```javascript
toPairsIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toPath"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPath ()](#apidoc.element.sequelize.Utils._.prototype.toPath)
- description and source-code
```javascript
toPath = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toPlainObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toPlainObject ()](#apidoc.element.sequelize.Utils._.prototype.toPlainObject)
- description and source-code
```javascript
toPlainObject = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toSafeInteger"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toSafeInteger ()](#apidoc.element.sequelize.Utils._.prototype.toSafeInteger)
- description and source-code
```javascript
toSafeInteger = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toString ()](#apidoc.element.sequelize.Utils._.prototype.toString)
- description and source-code
```javascript
toString = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.toUpper"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>toUpper ()](#apidoc.element.sequelize.Utils._.prototype.toUpper)
- description and source-code
```javascript
toUpper = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.transform"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>transform ()](#apidoc.element.sequelize.Utils._.prototype.transform)
- description and source-code
```javascript
transform = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.trim"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>trim ()](#apidoc.element.sequelize.Utils._.prototype.trim)
- description and source-code
```javascript
trim = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.trimEnd"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>trimEnd ()](#apidoc.element.sequelize.Utils._.prototype.trimEnd)
- description and source-code
```javascript
trimEnd = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.trimStart"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>trimStart ()](#apidoc.element.sequelize.Utils._.prototype.trimStart)
- description and source-code
```javascript
trimStart = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.truncate"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>truncate ()](#apidoc.element.sequelize.Utils._.prototype.truncate)
- description and source-code
```javascript
truncate = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unary"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unary ()](#apidoc.element.sequelize.Utils._.prototype.unary)
- description and source-code
```javascript
unary = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unescape"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unescape ()](#apidoc.element.sequelize.Utils._.prototype.unescape)
- description and source-code
```javascript
unescape = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.union"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>union ()](#apidoc.element.sequelize.Utils._.prototype.union)
- description and source-code
```javascript
union = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unionBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unionBy ()](#apidoc.element.sequelize.Utils._.prototype.unionBy)
- description and source-code
```javascript
unionBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unionWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unionWith ()](#apidoc.element.sequelize.Utils._.prototype.unionWith)
- description and source-code
```javascript
unionWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.uniq"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniq ()](#apidoc.element.sequelize.Utils._.prototype.uniq)
- description and source-code
```javascript
uniq = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.uniqBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniqBy ()](#apidoc.element.sequelize.Utils._.prototype.uniqBy)
- description and source-code
```javascript
uniqBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.uniqWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniqWith ()](#apidoc.element.sequelize.Utils._.prototype.uniqWith)
- description and source-code
```javascript
uniqWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.uniqueId"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>uniqueId ()](#apidoc.element.sequelize.Utils._.prototype.uniqueId)
- description and source-code
```javascript
uniqueId = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unset"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unset ()](#apidoc.element.sequelize.Utils._.prototype.unset)
- description and source-code
```javascript
unset = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unshift"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unshift ()](#apidoc.element.sequelize.Utils._.prototype.unshift)
- description and source-code
```javascript
unshift = function () {
  var args = arguments;
  if (retUnwrapped && !this.__chain__) {
    var value = this.value();
    return func.apply(isArray(value) ? value : [], args);
  }
  return this[chainName](function(value) {
    return func.apply(isArray(value) ? value : [], args);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unzip"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unzip ()](#apidoc.element.sequelize.Utils._.prototype.unzip)
- description and source-code
```javascript
unzip = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.unzipWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>unzipWith ()](#apidoc.element.sequelize.Utils._.prototype.unzipWith)
- description and source-code
```javascript
unzipWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.update"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>update ()](#apidoc.element.sequelize.Utils._.prototype.update)
- description and source-code
```javascript
update = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.updateWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>updateWith ()](#apidoc.element.sequelize.Utils._.prototype.updateWith)
- description and source-code
```javascript
updateWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.upperCase"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>upperCase ()](#apidoc.element.sequelize.Utils._.prototype.upperCase)
- description and source-code
```javascript
upperCase = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.upperFirst"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>upperFirst ()](#apidoc.element.sequelize.Utils._.prototype.upperFirst)
- description and source-code
```javascript
upperFirst = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.value"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>value ()](#apidoc.element.sequelize.Utils._.prototype.value)
- description and source-code
```javascript
function wrapperValue() {
  return baseWrapperValue(this.__wrapped__, this.__actions__);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.valueOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>valueOf ()](#apidoc.element.sequelize.Utils._.prototype.valueOf)
- description and source-code
```javascript
function wrapperValue() {
  return baseWrapperValue(this.__wrapped__, this.__actions__);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.values"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>values ()](#apidoc.element.sequelize.Utils._.prototype.values)
- description and source-code
```javascript
values = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
...
}
};

QueryInterface.prototype.changeColumn = function(tableName, attributeName, dataTypeOrOptions, options) {
var attributes = {};
options = options || {};

if (Utils._.values(DataTypes).indexOf(dataTypeOrOptions) > -1) {
  attributes[attributeName] = { type: dataTypeOrOptions, allowNull: true };
} else {
  attributes[attributeName] = dataTypeOrOptions;
}

attributes[attributeName].type = this.sequelize.normalizeDataType(attributes[attributeName].type);
...
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.valuesIn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>valuesIn ()](#apidoc.element.sequelize.Utils._.prototype.valuesIn)
- description and source-code
```javascript
valuesIn = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.without"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>without ()](#apidoc.element.sequelize.Utils._.prototype.without)
- description and source-code
```javascript
without = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.words"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>words ()](#apidoc.element.sequelize.Utils._.prototype.words)
- description and source-code
```javascript
words = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.wrap"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>wrap ()](#apidoc.element.sequelize.Utils._.prototype.wrap)
- description and source-code
```javascript
wrap = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.xor"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>xor ()](#apidoc.element.sequelize.Utils._.prototype.xor)
- description and source-code
```javascript
xor = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.xorBy"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>xorBy ()](#apidoc.element.sequelize.Utils._.prototype.xorBy)
- description and source-code
```javascript
xorBy = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.xorWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>xorWith ()](#apidoc.element.sequelize.Utils._.prototype.xorWith)
- description and source-code
```javascript
xorWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.zip"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zip ()](#apidoc.element.sequelize.Utils._.prototype.zip)
- description and source-code
```javascript
zip = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.zipObject"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zipObject ()](#apidoc.element.sequelize.Utils._.prototype.zipObject)
- description and source-code
```javascript
zipObject = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.zipObjectDeep"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zipObjectDeep ()](#apidoc.element.sequelize.Utils._.prototype.zipObjectDeep)
- description and source-code
```javascript
zipObjectDeep = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils._.prototype.zipWith"></a>[function <span class="apidocSignatureSpan">sequelize.Utils._.prototype.</span>zipWith ()](#apidoc.element.sequelize.Utils._.prototype.zipWith)
- description and source-code
```javascript
zipWith = function () {
  var chainAll = this.__chain__;
  if (chain || chainAll) {
    var result = object(this.__wrapped__),
        actions = result.__actions__ = copyArray(this.__actions__);

    actions.push({ 'func': func, 'args': arguments, 'thisArg': object });
    result.__chain__ = chainAll;
    return result;
  }
  return func.apply(object, arrayPush([this.value()], arguments));
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Utils.fn"></a>[module sequelize.Utils.fn](#apidoc.module.sequelize.Utils.fn)

#### <a name="apidoc.element.sequelize.Utils.fn.fn"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.</span>fn (fn, args)](#apidoc.element.sequelize.Utils.fn.fn)
- description and source-code
```javascript
fn = function (fn, args) {
  this.fn = fn;
  this.args = args;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.Utils.fn.prototype"></a>[module sequelize.Utils.fn.prototype](#apidoc.module.sequelize.Utils.fn.prototype)

#### <a name="apidoc.element.sequelize.Utils.fn.prototype.clone"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.fn.prototype.</span>clone ()](#apidoc.element.sequelize.Utils.fn.prototype.clone)
- description and source-code
```javascript
clone = function () {
  return new Utils.fn(this.fn, this.args);
}
```
- example usage
```shell
...
 * The Main Instance Validator.
 *
 * @param {Instance} modelInstance The model instance.
 * @param {Object} options A dict with options.
 * @constructor
 */
function InstanceValidator(modelInstance, options) {
options = _.clone(options) || {};

if (options.fields && !options.skip) {
  options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
}

// assign defined and default options
this.options = Utils._.defaults(options, {
...
```



# <a name="apidoc.module.sequelize.Utils.inflection"></a>[module sequelize.Utils.inflection](#apidoc.module.sequelize.Utils.inflection)

#### <a name="apidoc.element.sequelize.Utils.inflection._apply_rules"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>_apply_rules ( str, rules, skip, override )](#apidoc.element.sequelize.Utils.inflection._apply_rules)
- description and source-code
```javascript
_apply_rules = function ( str, rules, skip, override ){
  if( override ){
    str = override;
  }else{
    var ignore = ( inflector.indexOf( skip, str.toLowerCase()) > -1 );

    if( !ignore ){
      var i = 0;
      var j = rules.length;

      for( ; i < j; i++ ){
        if( str.match( rules[ i ][ 0 ])){
          if( rules[ i ][ 1 ] !== undefined ){
            str = str.replace( rules[ i ][ 0 ], rules[ i ][ 1 ]);
          }
          break;
        }
      }
    }
  }

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.camelize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>camelize ( str, low_first_letter )](#apidoc.element.sequelize.Utils.inflection.camelize)
- description and source-code
```javascript
camelize = function ( str, low_first_letter ){
  var str_path = str.split( '/' );
  var i        = 0;
  var j        = str_path.length;
  var str_arr, init_x, k, l, first;

  for( ; i < j; i++ ){
    str_arr = str_path[ i ].split( '_' );
    k       = 0;
    l       = str_arr.length;

    for( ; k < l; k++ ){
      if( k !== 0 ){
        str_arr[ k ] = str_arr[ k ].toLowerCase();
      }

      first = str_arr[ k ].charAt( 0 );
      first = low_first_letter && i === 0 && k === 0
        ? first.toLowerCase() : first.toUpperCase();
      str_arr[ k ] = first + str_arr[ k ].substring( 1 );
    }

    str_path[ i ] = str_arr.join( '' );
  }

  return str_path.join( '::' );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.capitalize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>capitalize ( str )](#apidoc.element.sequelize.Utils.inflection.capitalize)
- description and source-code
```javascript
capitalize = function ( str ){
  str = str.toLowerCase();

  return str.substring( 0, 1 ).toUpperCase() + str.substring( 1 );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.classify"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>classify ( str )](#apidoc.element.sequelize.Utils.inflection.classify)
- description and source-code
```javascript
classify = function ( str ){
  str = inflector.camelize( str );
  str = inflector.singularize( str );

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.dasherize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>dasherize ( str )](#apidoc.element.sequelize.Utils.inflection.dasherize)
- description and source-code
```javascript
dasherize = function ( str ){
  return str.replace( space_or_underbar, '-' );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.demodulize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>demodulize ( str )](#apidoc.element.sequelize.Utils.inflection.demodulize)
- description and source-code
```javascript
demodulize = function ( str ){
  var str_arr = str.split( '::' );

  return str_arr[ str_arr.length - 1 ];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.foreign_key"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>foreign_key ( str, drop_id_ubar )](#apidoc.element.sequelize.Utils.inflection.foreign_key)
- description and source-code
```javascript
foreign_key = function ( str, drop_id_ubar ){
  str = inflector.demodulize( str );
  str = inflector.underscore( str ) + (( drop_id_ubar ) ? ( '' ) : ( '_' )) + 'id';

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.humanize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>humanize ( str, low_first_letter )](#apidoc.element.sequelize.Utils.inflection.humanize)
- description and source-code
```javascript
humanize = function ( str, low_first_letter ){
  str = str.toLowerCase();
  str = str.replace( id_suffix, '' );
  str = str.replace( underbar, ' ' );

  if( !low_first_letter ){
    str = inflector.capitalize( str );
  }

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.indexOf"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>indexOf ( arr, item, from_index, compare_func )](#apidoc.element.sequelize.Utils.inflection.indexOf)
- description and source-code
```javascript
indexOf = function ( arr, item, from_index, compare_func ){
  if( !from_index ){
    from_index = -1;
  }

  var index = -1;
  var i     = from_index;
  var j     = arr.length;

  for( ; i < j; i++ ){
    if( arr[ i ]  === item || compare_func && compare_func( arr[ i ], item )){
      index = i;
      break;
    }
  }

  return index;
}
```
- example usage
```shell
...
 */
InstanceValidator.prototype._builtinValidators = function() {
  var self = this;

  // promisify all attribute invocations
  var validators = [];
  Utils._.forIn(this.modelInstance.rawAttributes, function(rawAttribute, field) {
if (self.options.skip.indexOf(field) >= 0) {
  return;
}

var value = self.modelInstance.dataValues[field];

if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
  // perform validations based on schema
...
```

#### <a name="apidoc.element.sequelize.Utils.inflection.inflect"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>inflect ( str, count, singular, plural )](#apidoc.element.sequelize.Utils.inflection.inflect)
- description and source-code
```javascript
inflect = function ( str, count, singular, plural ){
  count = parseInt( count, 10 );

  if( isNaN( count )) return str;

  if( count === 0 || count > 1 ){
    return inflector._apply_rules( str, plural_rules, uncountable_words, plural );
  }else{
    return inflector._apply_rules( str, singular_rules, uncountable_words, singular );
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.ordinalize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>ordinalize ( str )](#apidoc.element.sequelize.Utils.inflection.ordinalize)
- description and source-code
```javascript
ordinalize = function ( str ){
  var str_arr = str.split( ' ' );
  var i       = 0;
  var j       = str_arr.length;

  for( ; i < j; i++ ){
    var k = parseInt( str_arr[ i ], 10 );

    if( !isNaN( k )){
      var ltd = str_arr[ i ].substring( str_arr[ i ].length - 2 );
      var ld  = str_arr[ i ].substring( str_arr[ i ].length - 1 );
      var suf = 'th';

      if( ltd != '11' && ltd != '12' && ltd != '13' ){
        if( ld === '1' ){
          suf = 'st';
        }else if( ld === '2' ){
          suf = 'nd';
        }else if( ld === '3' ){
          suf = 'rd';
        }
      }

      str_arr[ i ] += suf;
    }
  }

  return str_arr.join( ' ' );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.pluralize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>pluralize ( str, plural )](#apidoc.element.sequelize.Utils.inflection.pluralize)
- description and source-code
```javascript
pluralize = function ( str, plural ){
  return inflector._apply_rules( str, plural_rules, uncountable_words, plural );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.singularize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>singularize ( str, singular )](#apidoc.element.sequelize.Utils.inflection.singularize)
- description and source-code
```javascript
singularize = function ( str, singular ){
  return inflector._apply_rules( str, singular_rules, uncountable_words, singular );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.tableize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>tableize ( str )](#apidoc.element.sequelize.Utils.inflection.tableize)
- description and source-code
```javascript
tableize = function ( str ){
  str = inflector.underscore( str );
  str = inflector.pluralize( str );

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.titleize"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>titleize ( str )](#apidoc.element.sequelize.Utils.inflection.titleize)
- description and source-code
```javascript
titleize = function ( str ){
  str         = str.toLowerCase().replace( underbar, ' ' );
  var str_arr = str.split( ' ' );
  var i       = 0;
  var j       = str_arr.length;
  var d, k, l;

  for( ; i < j; i++ ){
    d = str_arr[ i ].split( '-' );
    k = 0;
    l = d.length;

    for( ; k < l; k++){
      if( inflector.indexOf( non_titlecased_words, d[ k ].toLowerCase()) < 0 ){
        d[ k ] = inflector.capitalize( d[ k ]);
      }
    }

    str_arr[ i ] = d.join( '-' );
  }

  str = str_arr.join( ' ' );
  str = str.substring( 0, 1 ).toUpperCase() + str.substring( 1 );

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.transform"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>transform ( str, arr )](#apidoc.element.sequelize.Utils.inflection.transform)
- description and source-code
```javascript
transform = function ( str, arr ){
  var i = 0;
  var j = arr.length;

  for( ;i < j; i++ ){
    var method = arr[ i ];

    if( inflector.hasOwnProperty( method )){
      str = inflector[ method ]( str );
    }
  }

  return str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Utils.inflection.underscore"></a>[function <span class="apidocSignatureSpan">sequelize.Utils.inflection.</span>underscore ( str, all_upper_case )](#apidoc.element.sequelize.Utils.inflection.underscore)
- description and source-code
```javascript
underscore = function ( str, all_upper_case ){
  if( all_upper_case && str === str.toUpperCase()) return str;

  var str_path = str.split( '::' );
  var i        = 0;
  var j        = str_path.length;

  for( ; i < j; i++ ){
    str_path[ i ] = str_path[ i ].replace( uppercase, '_$1' );
    str_path[ i ] = str_path[ i ].replace( underbar_prefix, '' );
  }

  return str_path.join( '/' ).toLowerCase();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.VIRTUAL"></a>[module sequelize.VIRTUAL](#apidoc.module.sequelize.VIRTUAL)

#### <a name="apidoc.element.sequelize.VIRTUAL.VIRTUAL"></a>[function <span class="apidocSignatureSpan">sequelize.</span>VIRTUAL (ReturnType, fields)](#apidoc.element.sequelize.VIRTUAL.VIRTUAL)
- description and source-code
```javascript
VIRTUAL = function (ReturnType, fields) {
  if (!(this instanceof VIRTUAL)) return new VIRTUAL(ReturnType, fields);
  if (typeof ReturnType === 'function') ReturnType = new ReturnType();

  this.returnType = ReturnType;
  this.fields = fields;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.VIRTUAL.super_"></a>[function <span class="apidocSignatureSpan">sequelize.VIRTUAL.</span>super_ (options)](#apidoc.element.sequelize.VIRTUAL.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ValidationError"></a>[module sequelize.ValidationError](#apidoc.module.sequelize.ValidationError)

#### <a name="apidoc.element.sequelize.ValidationError.ValidationError"></a>[function <span class="apidocSignatureSpan">sequelize.</span>ValidationError (message, errors)](#apidoc.element.sequelize.ValidationError.ValidationError)
- description and source-code
```javascript
ValidationError = function (message, errors) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeValidationError';
  this.message = 'Validation Error';
<span class="apidocCodeCommentSpan">/**
 * An array of ValidationErrorItems
 * @member errors
 */
</span>  this.errors = errors || [];

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
}
```
- example usage
```shell
...
  return Promise.all([
    self._builtinValidators(),
    self._customValidators()
  ].map(function(promise) {
    return promise.reflect();
  })).then(function() {
    if (self.errors.length) {
      return new sequelizeError.ValidationError(null, self.errors);
    } else {
      return null;
    }
  });
};

/**
...
```

#### <a name="apidoc.element.sequelize.ValidationError.super_"></a>[function <span class="apidocSignatureSpan">sequelize.ValidationError.</span>super_ ()](#apidoc.element.sequelize.ValidationError.super_)
- description and source-code
```javascript
super_ = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.ValidationError.prototype"></a>[module sequelize.ValidationError.prototype](#apidoc.module.sequelize.ValidationError.prototype)

#### <a name="apidoc.element.sequelize.ValidationError.prototype.get"></a>[function <span class="apidocSignatureSpan">sequelize.ValidationError.prototype.</span>get (path)](#apidoc.element.sequelize.ValidationError.prototype.get)
- description and source-code
```javascript
get = function (path) {
  return this.errors.reduce(function(reduced, error) {
    if (error.path === path) {
      reduced.push(error);
    }
    return reduced;
  }, []);
}
```
- example usage
```shell
...
          options.supportsSearchPath = false;
          promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
        }
      }
    }
  }

  return Promise.all(promises).get(0);
});
};

QueryInterface.prototype.dropAllTables = function(options) {
var self = this
  , skip;
...
```



# <a name="apidoc.module.sequelize.Validator"></a>[module sequelize.Validator](#apidoc.module.sequelize.Validator)

#### <a name="apidoc.element.sequelize.Validator.blacklist"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>blacklist (str, chars)](#apidoc.element.sequelize.Validator.blacklist)
- description and source-code
```javascript
function blacklist(str, chars) {
  (0, _assertString2.default)(str);
  return str.replace(new RegExp('[' + chars + ']+', 'g'), '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.contains"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>contains (str, elem)](#apidoc.element.sequelize.Validator.contains)
- description and source-code
```javascript
contains = function (str, elem) {
  return str.indexOf(elem) >= 0 && !!elem;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.equals"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>equals (str, comparison)](#apidoc.element.sequelize.Validator.equals)
- description and source-code
```javascript
function equals(str, comparison) {
  (0, _assertString2.default)(str);
  return str === comparison;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.escape"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>escape (str)](#apidoc.element.sequelize.Validator.escape)
- description and source-code
```javascript
function escape(str) {
      (0, _assertString2.default)(str);
      return str.replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/'/g, '&#x27;').replace(/</g, '&lt;').replace(/>/g, '&gt
;').replace(/\//g, '&#x2F;').replace(/'/g, '&#96;');
}
```
- example usage
```shell
...
return this.QueryGenerator.quoteIdentifiers(identifiers, force);
};

/**
 * Escape a value (e.g. a string, number or date)
 */
QueryInterface.prototype.escape = function(value) {
return this.QueryGenerator.escape(value);
};

QueryInterface.prototype.setAutocommit = function(transaction, value, options) {
if (!transaction || !(transaction instanceof Transaction)) {
  throw new Error('Unable to set autocommit for a transaction without transaction object!');
}
if (transaction.parent) {
...
```

#### <a name="apidoc.element.sequelize.Validator.extend"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>extend (name, fn)](#apidoc.element.sequelize.Validator.extend)
- description and source-code
```javascript
extend = function (name, fn) {
  this[name] = fn;

  return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.is"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>is (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.is)
- description and source-code
```javascript
is = function (str, pattern, modifiers) {
  return this.regex(str, pattern, modifiers);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isAfter"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAfter (str)](#apidoc.element.sequelize.Validator.isAfter)
- description and source-code
```javascript
function isAfter(str) {
  var date = arguments.length <= 1 || arguments[1] === undefined ? String(new Date()) : arguments[1];

  (0, _assertString2.default)(str);
  var comparison = (0, _toDate2.default)(date);
  var original = (0, _toDate2.default)(str);
  return !!(original && comparison && original > comparison);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isAlpha"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAlpha (str)](#apidoc.element.sequelize.Validator.isAlpha)
- description and source-code
```javascript
function isAlpha(str) {
  var locale = arguments.length <= 1 || arguments[1] === undefined ? 'en-US' : arguments[1];

  (0, _assertString2.default)(str);
  if (locale in _alpha.alpha) {
    return _alpha.alpha[locale].test(str);
  }
  throw new Error('Invalid locale \'' + locale + '\'');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isAlphanumeric"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAlphanumeric (str)](#apidoc.element.sequelize.Validator.isAlphanumeric)
- description and source-code
```javascript
function isAlphanumeric(str) {
  var locale = arguments.length <= 1 || arguments[1] === undefined ? 'en-US' : arguments[1];

  (0, _assertString2.default)(str);
  if (locale in _alpha.alphanumeric) {
    return _alpha.alphanumeric[locale].test(str);
  }
  throw new Error('Invalid locale \'' + locale + '\'');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isAscii"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isAscii (str)](#apidoc.element.sequelize.Validator.isAscii)
- description and source-code
```javascript
function isAscii(str) {
  (0, _assertString2.default)(str);
  return ascii.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isBase64"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isBase64 (str)](#apidoc.element.sequelize.Validator.isBase64)
- description and source-code
```javascript
function isBase64(str) {
  (0, _assertString2.default)(str);
  var len = str.length;
  if (!len || len % 4 !== 0 || notBase64.test(str)) {
    return false;
  }
  var firstPaddingChar = str.indexOf('=');
  return firstPaddingChar === -1 || firstPaddingChar === len - 1 || firstPaddingChar === len - 2 && str[len - 1] === '=';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isBefore"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isBefore (str)](#apidoc.element.sequelize.Validator.isBefore)
- description and source-code
```javascript
function isBefore(str) {
  var date = arguments.length <= 1 || arguments[1] === undefined ? String(new Date()) : arguments[1];

  (0, _assertString2.default)(str);
  var comparison = (0, _toDate2.default)(date);
  var original = (0, _toDate2.default)(str);
  return !!(original && comparison && original < comparison);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isBoolean"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isBoolean (str)](#apidoc.element.sequelize.Validator.isBoolean)
- description and source-code
```javascript
function isBoolean(str) {
  (0, _assertString2.default)(str);
  return ['true', 'false', '1', '0'].indexOf(str) >= 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isByteLength"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isByteLength (str, options)](#apidoc.element.sequelize.Validator.isByteLength)
- description and source-code
```javascript
function isByteLength(str, options) {
  (0, _assertString2.default)(str);
  var min = void 0;
  var max = void 0;
  if ((typeof options === 'undefined' ? 'undefined' : _typeof(options)) === 'object') {
    min = options.min || 0;
    max = options.max;
  } else {
    // backwards compatibility: isByteLength(str, min [, max])
    min = arguments[1];
    max = arguments[2];
  }
  var len = encodeURI(str).split(/%..|./).length - 1;
  return len >= min && (typeof max === 'undefined' || len <= max);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isCreditCard"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isCreditCard (str)](#apidoc.element.sequelize.Validator.isCreditCard)
- description and source-code
```javascript
function isCreditCard(str) {
  (0, _assertString2.default)(str);
  var sanitized = str.replace(/[^0-9]+/g, '');
  if (!creditCard.test(sanitized)) {
    return false;
  }
  var sum = 0;
  var digit = void 0;
  var tmpNum = void 0;
  var shouldDouble = void 0;
  for (var i = sanitized.length - 1; i >= 0; i--) {
    digit = sanitized.substring(i, i + 1);
    tmpNum = parseInt(digit, 10);
    if (shouldDouble) {
      tmpNum *= 2;
      if (tmpNum >= 10) {
        sum += tmpNum % 10 + 1;
      } else {
        sum += tmpNum;
      }
    } else {
      sum += tmpNum;
    }
    shouldDouble = !shouldDouble;
  }
  return !!(sum % 10 === 0 ? sanitized : false);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isCurrency"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isCurrency (str, options)](#apidoc.element.sequelize.Validator.isCurrency)
- description and source-code
```javascript
function isCurrency(str, options) {
  (0, _assertString2.default)(str);
  options = (0, _merge2.default)(options, default_currency_options);
  return currencyRegex(options).test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isDataURI"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDataURI (str)](#apidoc.element.sequelize.Validator.isDataURI)
- description and source-code
```javascript
function isDataURI(str) {
  (0, _assertString2.default)(str);
  return dataURI.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isDate"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDate (str)](#apidoc.element.sequelize.Validator.isDate)
- description and source-code
```javascript
function isDate(str) {
  (0, _assertString2.default)(str);
  var normalizedDate = new Date(Date.parse(str));
  if (isNaN(normalizedDate)) {
    return false;
  }

  // normalizedDate is in the user's timezone. Apply the input
  // timezone offset to the date so that the year and day match
  // the input
  var timezoneOffset = getTimezoneOffset(str);
  if (timezoneOffset !== null) {
    var timezoneDifference = normalizedDate.getTimezoneOffset() - timezoneOffset;
    normalizedDate = new Date(normalizedDate.getTime() + 60000 * timezoneDifference);
  }

  var day = String(normalizedDate.getDate());
  var dayOrYear = void 0,
      dayOrYearMatches = void 0,
      year = void 0;
  // check for valid double digits that could be late days
  // check for all matches since a string like '12/23' is a valid date
  // ignore everything with nearby colons
  dayOrYearMatches = str.match(/(^|[^:\d])[23]\d([^T:\d]|$)/g);
  if (!dayOrYearMatches) {
    return true;
  }
  dayOrYear = dayOrYearMatches.map(function (digitString) {
    return digitString.match(/\d+/g)[0];
  }).join('/');

  year = String(normalizedDate.getFullYear()).slice(-2);
  if (dayOrYear === day || dayOrYear === year) {
    return true;
  } else if (dayOrYear === '' + day / year || dayOrYear === '' + year / day) {
    return true;
  }
  return false;
}
```
- example usage
```shell
...
  var dataType = options.dataType;

  if (dataType instanceof DataTypes.DECIMAL || dataType instanceof DataTypes.FLOAT) {
    result = parseFloat(result);
  } else if (dataType instanceof DataTypes.INTEGER || dataType instanceof DataTypes.BIGINT) {
    result = parseInt(result, 10);
  } else if (dataType instanceof DataTypes.DATE) {
    if (!Utils._.isNull(result) && !Utils._.isDate(result)) {
      result = new Date(result);
    }
  } else if (dataType instanceof DataTypes.STRING) {
    // Nothing to do, result is already a string.
  }
}
...
```

#### <a name="apidoc.element.sequelize.Validator.isDecimal"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDecimal (str)](#apidoc.element.sequelize.Validator.isDecimal)
- description and source-code
```javascript
isDecimal = function (str) {
  return str !== '' && str.match(/^(?:-?(?:[0-9]+))?(?:\.[0-9]*)?(?:[eE][\+\-]?(?:[0-9]+))?$/);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isDivisibleBy"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isDivisibleBy (str, num)](#apidoc.element.sequelize.Validator.isDivisibleBy)
- description and source-code
```javascript
function isDivisibleBy(str, num) {
  (0, _assertString2.default)(str);
  return (0, _toFloat2.default)(str) % parseInt(num, 10) === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isEmail"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isEmail (str, options)](#apidoc.element.sequelize.Validator.isEmail)
- description and source-code
```javascript
function isEmail(str, options) {
  (0, _assertString2.default)(str);
  options = (0, _merge2.default)(options, default_email_options);

  if (options.allow_display_name) {
    var display_email = str.match(displayName);
    if (display_email) {
      str = display_email[1];
    }
  }

  var parts = str.split('@');
  var domain = parts.pop();
  var user = parts.join('@');

  var lower_domain = domain.toLowerCase();
  if (lower_domain === 'gmail.com' || lower_domain === 'googlemail.com') {
    user = user.replace(/\./g, '').toLowerCase();
  }

  if (!(0, _isByteLength2.default)(user, { max: 64 }) || !(0, _isByteLength2.default)(domain, { max: 256 })) {
    return false;
  }

  if (!(0, _isFQDN2.default)(domain, { require_tld: options.require_tld })) {
    return false;
  }

  if (user[0] === '"') {
    user = user.slice(1, user.length - 1);
    return options.allow_utf8_local_part ? quotedEmailUserUtf8.test(user) : quotedEmailUser.test(user);
  }

  var pattern = options.allow_utf8_local_part ? emailUserUtf8Part : emailUserPart;

  var user_parts = user.split('.');
  for (var i = 0; i < user_parts.length; i++) {
    if (!pattern.test(user_parts[i])) {
      return false;
    }
  }

  return true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isFQDN"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isFQDN (str, options)](#apidoc.element.sequelize.Validator.isFQDN)
- description and source-code
```javascript
function isFDQN(str, options) {
  (0, _assertString2.default)(str);
  options = (0, _merge2.default)(options, default_fqdn_options);

<span class="apidocCodeCommentSpan">  /* Remove the optional trailing dot before checking validity */
</span>  if (options.allow_trailing_dot && str[str.length - 1] === '.') {
    str = str.substring(0, str.length - 1);
  }
  var parts = str.split('.');
  if (options.require_tld) {
    var tld = parts.pop();
    if (!parts.length || !/^([a-z\u00a1-\uffff]{2,}|xn[a-z0-9-]{2,})$/i.test(tld)) {
      return false;
    }
  }
  for (var part, i = 0; i < parts.length; i++) {
    part = parts[i];
    if (options.allow_underscores) {
      part = part.replace(/_/g, '');
    }
    if (!/^[a-z\u00a1-\uffff0-9-]+$/i.test(part)) {
      return false;
    }
    if (/[\uff01-\uff5e]/.test(part)) {
      // disallow full-width chars
      return false;
    }
    if (part[0] === '-' || part[part.length - 1] === '-') {
      return false;
    }
  }
  return true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isFloat"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isFloat (str, options)](#apidoc.element.sequelize.Validator.isFloat)
- description and source-code
```javascript
function isFloat(str, options) {
  (0, _assertString2.default)(str);
  options = options || {};
  if (str === '' || str === '.') {
    return false;
  }
  return float.test(str) && (!options.hasOwnProperty('min') || str >= options.min) && (!options.hasOwnProperty('max') || str <=
options.max);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isFullWidth"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isFullWidth (str)](#apidoc.element.sequelize.Validator.isFullWidth)
- description and source-code
```javascript
function isFullWidth(str) {
  (0, _assertString2.default)(str);
  return fullWidth.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isHalfWidth"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isHalfWidth (str)](#apidoc.element.sequelize.Validator.isHalfWidth)
- description and source-code
```javascript
function isHalfWidth(str) {
  (0, _assertString2.default)(str);
  return halfWidth.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isHexColor"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isHexColor (str)](#apidoc.element.sequelize.Validator.isHexColor)
- description and source-code
```javascript
function isHexColor(str) {
  (0, _assertString2.default)(str);
  return hexcolor.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isHexadecimal"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isHexadecimal (str)](#apidoc.element.sequelize.Validator.isHexadecimal)
- description and source-code
```javascript
function isHexadecimal(str) {
  (0, _assertString2.default)(str);
  return hexadecimal.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isIP"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIP (str)](#apidoc.element.sequelize.Validator.isIP)
- description and source-code
```javascript
function isIP(str) {
  var version = arguments.length <= 1 || arguments[1] === undefined ? '' : arguments[1];

  (0, _assertString2.default)(str);
  version = String(version);
  if (!version) {
    return isIP(str, 4) || isIP(str, 6);
  } else if (version === '4') {
    if (!ipv4Maybe.test(str)) {
      return false;
    }
    var parts = str.split('.').sort(function (a, b) {
      return a - b;
    });
    return parts[3] <= 255;
  } else if (version === '6') {
    var blocks = str.split(':');
    var foundOmissionBlock = false; // marker to indicate ::

    // At least some OS accept the last 32 bits of an IPv6 address
    // (i.e. 2 of the blocks) in IPv4 notation, and RFC 3493 says
    // that '::ffff:a.b.c.d' is valid for IPv4-mapped IPv6 addresses,
    // and '::a.b.c.d' is deprecated, but also valid.
    var foundIPv4TransitionBlock = isIP(blocks[blocks.length - 1], 4);
    var expectedNumberOfBlocks = foundIPv4TransitionBlock ? 7 : 8;

    if (blocks.length > expectedNumberOfBlocks) {
      return false;
    }
    // initial or final ::
    if (str === '::') {
      return true;
    } else if (str.substr(0, 2) === '::') {
      blocks.shift();
      blocks.shift();
      foundOmissionBlock = true;
    } else if (str.substr(str.length - 2) === '::') {
      blocks.pop();
      blocks.pop();
      foundOmissionBlock = true;
    }

    for (var i = 0; i < blocks.length; ++i) {
      // test for a :: which can not be at the string start/end
      // since those cases have been handled above
      if (blocks[i] === '' && i > 0 && i < blocks.length - 1) {
        if (foundOmissionBlock) {
          return false; // multiple :: in address
        }
        foundOmissionBlock = true;
      } else if (foundIPv4TransitionBlock && i === blocks.length - 1) {
        // it has been checked before that the last
        // block is a valid IPv4 address
      } else if (!ipv6Block.test(blocks[i])) {
        return false;
      }
    }
    if (foundOmissionBlock) {
      return blocks.length >= 1;
    }
    return blocks.length === expectedNumberOfBlocks;
  }
  return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isIPv4"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIPv4 (str)](#apidoc.element.sequelize.Validator.isIPv4)
- description and source-code
```javascript
isIPv4 = function (str) {
  return this.isIP(str, 4);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isIPv6"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIPv6 (str)](#apidoc.element.sequelize.Validator.isIPv6)
- description and source-code
```javascript
isIPv6 = function (str) {
  return this.isIP(str, 6);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isISBN"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isISBN (str)](#apidoc.element.sequelize.Validator.isISBN)
- description and source-code
```javascript
function isISBN(str) {
  var version = arguments.length <= 1 || arguments[1] === undefined ? '' : arguments[1];

  (0, _assertString2.default)(str);
  version = String(version);
  if (!version) {
    return isISBN(str, 10) || isISBN(str, 13);
  }
  var sanitized = str.replace(/[\s-]+/g, '');
  var checksum = 0;
  var i = void 0;
  if (version === '10') {
    if (!isbn10Maybe.test(sanitized)) {
      return false;
    }
    for (i = 0; i < 9; i++) {
      checksum += (i + 1) * sanitized.charAt(i);
    }
    if (sanitized.charAt(9) === 'X') {
      checksum += 10 * 10;
    } else {
      checksum += 10 * sanitized.charAt(9);
    }
    if (checksum % 11 === 0) {
      return !!sanitized;
    }
  } else if (version === '13') {
    if (!isbn13Maybe.test(sanitized)) {
      return false;
    }
    for (i = 0; i < 12; i++) {
      checksum += factor[i % 2] * sanitized.charAt(i);
    }
    if (sanitized.charAt(12) - (10 - checksum % 10) % 10 === 0) {
      return !!sanitized;
    }
  }
  return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isISIN"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isISIN (str)](#apidoc.element.sequelize.Validator.isISIN)
- description and source-code
```javascript
function isISIN(str) {
  (0, _assertString2.default)(str);
  if (!isin.test(str)) {
    return false;
  }

  var checksumStr = str.replace(/[A-Z]/g, function (character) {
    return parseInt(character, 36);
  });

  var sum = 0;
  var digit = void 0;
  var tmpNum = void 0;
  var shouldDouble = true;
  for (var i = checksumStr.length - 2; i >= 0; i--) {
    digit = checksumStr.substring(i, i + 1);
    tmpNum = parseInt(digit, 10);
    if (shouldDouble) {
      tmpNum *= 2;
      if (tmpNum >= 10) {
        sum += tmpNum + 1;
      } else {
        sum += tmpNum;
      }
    } else {
      sum += tmpNum;
    }
    shouldDouble = !shouldDouble;
  }

  return parseInt(str.substr(str.length - 1), 10) === (10000 - sum) % 10;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isISO8601"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isISO8601 (str)](#apidoc.element.sequelize.Validator.isISO8601)
- description and source-code
```javascript
isISO8601 = function (str) {
  (0, _assertString2.default)(str);
  return iso8601.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isIn"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isIn (str, options)](#apidoc.element.sequelize.Validator.isIn)
- description and source-code
```javascript
function isIn(str, options) {
  (0, _assertString2.default)(str);
  var i = void 0;
  if (Object.prototype.toString.call(options) === '[object Array]') {
    var array = [];
    for (i in options) {
      if ({}.hasOwnProperty.call(options, i)) {
        array[i] = (0, _toString2.default)(options[i]);
      }
    }
    return array.indexOf(str) >= 0;
  } else if ((typeof options === 'undefined' ? 'undefined' : _typeof(options)) === 'object') {
    return options.hasOwnProperty(str);
  } else if (options && typeof options.indexOf === 'function') {
    return options.indexOf(str) >= 0;
  }
  return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isInt"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isInt (str, options)](#apidoc.element.sequelize.Validator.isInt)
- description and source-code
```javascript
function isInt(str, options) {
  (0, _assertString2.default)(str);
  options = options || {};

  // Get the regex to use for testing, based on whether
  // leading zeroes are allowed or not.
  var regex = options.hasOwnProperty('allow_leading_zeroes') && options.allow_leading_zeroes ? intLeadingZeroes : int;

  // Check min/max
  var minCheckPassed = !options.hasOwnProperty('min') || str >= options.min;
  var maxCheckPassed = !options.hasOwnProperty('max') || str <= options.max;

  return regex.test(str) && minCheckPassed && maxCheckPassed;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isJSON"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isJSON (str)](#apidoc.element.sequelize.Validator.isJSON)
- description and source-code
```javascript
function isJSON(str) {
  (0, _assertString2.default)(str);
  try {
    var obj = JSON.parse(str);
    return !!obj && (typeof obj === 'undefined' ? 'undefined' : _typeof(obj)) === 'object';
  } catch (e) {/* ignore */}
  return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isLength"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isLength (str, options)](#apidoc.element.sequelize.Validator.isLength)
- description and source-code
```javascript
function isLength(str, options) {
  (0, _assertString2.default)(str);
  var min = void 0;
  var max = void 0;
  if ((typeof options === 'undefined' ? 'undefined' : _typeof(options)) === 'object') {
    min = options.min || 0;
    max = options.max;
  } else {
    // backwards compatibility: isLength(str, min [, max])
    min = arguments[1];
    max = arguments[2];
  }
  var surrogatePairs = str.match(/[\uD800-\uDBFF][\uDC00-\uDFFF]/g) || [];
  var len = str.length - surrogatePairs.length;
  return len >= min && (typeof max === 'undefined' || len <= max);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isLowercase"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isLowercase (str)](#apidoc.element.sequelize.Validator.isLowercase)
- description and source-code
```javascript
function isLowercase(str) {
  (0, _assertString2.default)(str);
  return str === str.toLowerCase();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isMACAddress"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMACAddress (str)](#apidoc.element.sequelize.Validator.isMACAddress)
- description and source-code
```javascript
function isMACAddress(str) {
  (0, _assertString2.default)(str);
  return macAddress.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isMD5"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMD5 (str)](#apidoc.element.sequelize.Validator.isMD5)
- description and source-code
```javascript
function isMD5(str) {
  (0, _assertString2.default)(str);
  return md5.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isMobilePhone"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMobilePhone (str, locale)](#apidoc.element.sequelize.Validator.isMobilePhone)
- description and source-code
```javascript
function isMobilePhone(str, locale) {
  (0, _assertString2.default)(str);
  if (locale in phones) {
    return phones[locale].test(str);
  }
  return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isMongoId"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMongoId (str)](#apidoc.element.sequelize.Validator.isMongoId)
- description and source-code
```javascript
function isMongoId(str) {
  (0, _assertString2.default)(str);
  return (0, _isHexadecimal2.default)(str) && str.length === 24;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isMultibyte"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isMultibyte (str)](#apidoc.element.sequelize.Validator.isMultibyte)
- description and source-code
```javascript
function isMultibyte(str) {
  (0, _assertString2.default)(str);
  return multibyte.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isNull"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isNull (str)](#apidoc.element.sequelize.Validator.isNull)
- description and source-code
```javascript
function isNull(str) {
  (0, _assertString2.default)(str);
  return str.length === 0;
}
```
- example usage
```shell
...
  var dataType = options.dataType;

  if (dataType instanceof DataTypes.DECIMAL || dataType instanceof DataTypes.FLOAT) {
    result = parseFloat(result);
  } else if (dataType instanceof DataTypes.INTEGER || dataType instanceof DataTypes.BIGINT) {
    result = parseInt(result, 10);
  } else if (dataType instanceof DataTypes.DATE) {
    if (!Utils._.isNull(result) && !Utils._.isDate(result)) {
      result = new Date(result);
    }
  } else if (dataType instanceof DataTypes.STRING) {
    // Nothing to do, result is already a string.
  }
}
...
```

#### <a name="apidoc.element.sequelize.Validator.isNumeric"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isNumeric (str)](#apidoc.element.sequelize.Validator.isNumeric)
- description and source-code
```javascript
function isNumeric(str) {
  (0, _assertString2.default)(str);
  return numeric.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isSurrogatePair"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isSurrogatePair (str)](#apidoc.element.sequelize.Validator.isSurrogatePair)
- description and source-code
```javascript
function isSurrogatePair(str) {
  (0, _assertString2.default)(str);
  return surrogatePair.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isURL"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isURL (url, options)](#apidoc.element.sequelize.Validator.isURL)
- description and source-code
```javascript
function isURL(url, options) {
  (0, _assertString2.default)(url);
  if (!url || url.length >= 2083 || /\s/.test(url)) {
    return false;
  }
  if (url.indexOf('mailto:') === 0) {
    return false;
  }
  options = (0, _merge2.default)(options, default_url_options);
  var protocol = void 0,
      auth = void 0,
      host = void 0,
      hostname = void 0,
      port = void 0,
      port_str = void 0,
      split = void 0,
      ipv6 = void 0;

  split = url.split('#');
  url = split.shift();

  split = url.split('?');
  url = split.shift();

  split = url.split('://');
  if (split.length > 1) {
    protocol = split.shift();
    if (options.require_valid_protocol && options.protocols.indexOf(protocol) === -1) {
      return false;
    }
  } else if (options.require_protocol) {
    return false;
  } else if (options.allow_protocol_relative_urls && url.substr(0, 2) === '//') {
    split[0] = url.substr(2);
  }
  url = split.join('://');

  split = url.split('/');
  url = split.shift();

  if (url === '' && !options.require_host) {
    return true;
  }

  split = url.split('@');
  if (split.length > 1) {
    auth = split.shift();
    if (auth.indexOf(':') >= 0 && auth.split(':').length > 2) {
      return false;
    }
  }
  hostname = split.join('@');

  port_str = ipv6 = null;
  var ipv6_match = hostname.match(wrapped_ipv6);
  if (ipv6_match) {
    host = '';
    ipv6 = ipv6_match[1];
    port_str = ipv6_match[2] || null;
  } else {
    split = hostname.split(':');
    host = split.shift();
    if (split.length) {
      port_str = split.join(':');
    }
  }

  if (port_str !== null) {
    port = parseInt(port_str, 10);
    if (!/^[0-9]+$/.test(port_str) || port <= 0 || port > 65535) {
      return false;
    }
  }

  if (!(0, _isIP2.default)(host) && !(0, _isFQDN2.default)(host, options) && (!ipv6 || !(0, _isIP2.default)(ipv6, 6)) && host !== '
localhost') {
    return false;
  }

  host = host || ipv6;

  if (options.host_whitelist && !checkHost(host, options.host_whitelist)) {
    return false;
  }
  if (options.host_blacklist && checkHost(host, options.host_blacklist)) {
    return false;
  }

  return true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isUUID"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isUUID (str)](#apidoc.element.sequelize.Validator.isUUID)
- description and source-code
```javascript
function isUUID(str) {
  var version = arguments.length <= 1 || arguments[1] === undefined ? 'all' : arguments[1];

  (0, _assertString2.default)(str);
  var pattern = uuid[version];
  return pattern && pattern.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isUppercase"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isUppercase (str)](#apidoc.element.sequelize.Validator.isUppercase)
- description and source-code
```javascript
function isUppercase(str) {
  (0, _assertString2.default)(str);
  return str === str.toUpperCase();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isUrl"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isUrl (str)](#apidoc.element.sequelize.Validator.isUrl)
- description and source-code
```javascript
isUrl = function (str) {
  return this.isURL(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isVariableWidth"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isVariableWidth (str)](#apidoc.element.sequelize.Validator.isVariableWidth)
- description and source-code
```javascript
function isVariableWidth(str) {
  (0, _assertString2.default)(str);
  return _isFullWidth.fullWidth.test(str) && _isHalfWidth.halfWidth.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.isWhitelisted"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>isWhitelisted (str, chars)](#apidoc.element.sequelize.Validator.isWhitelisted)
- description and source-code
```javascript
function isWhitelisted(str, chars) {
  (0, _assertString2.default)(str);
  for (var i = str.length - 1; i >= 0; i--) {
    if (chars.indexOf(str[i]) === -1) {
      return false;
    }
  }
  return true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.len"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>len (str, min, max)](#apidoc.element.sequelize.Validator.len)
- description and source-code
```javascript
len = function (str, min, max) {
  return this.isLength(str, min, max);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.ltrim"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>ltrim (str, chars)](#apidoc.element.sequelize.Validator.ltrim)
- description and source-code
```javascript
function ltrim(str, chars) {
  (0, _assertString2.default)(str);
  var pattern = chars ? new RegExp('^[' + chars + ']+', 'g') : /^\s+/g;
  return str.replace(pattern, '');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.matches"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>matches (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.matches)
- description and source-code
```javascript
function matches(str, pattern, modifiers) {
  (0, _assertString2.default)(str);
  if (Object.prototype.toString.call(pattern) !== '[object RegExp]') {
    pattern = new RegExp(pattern, modifiers);
  }
  return pattern.test(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.max"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>max (str, val)](#apidoc.element.sequelize.Validator.max)
- description and source-code
```javascript
max = function (str, val) {
  var number = parseFloat(str);
  return isNaN(number) || number <= val;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.min"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>min (str, val)](#apidoc.element.sequelize.Validator.min)
- description and source-code
```javascript
min = function (str, val) {
  var number = parseFloat(str);
  return isNaN(number) || number >= val;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.normalizeEmail"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>normalizeEmail (email, options)](#apidoc.element.sequelize.Validator.normalizeEmail)
- description and source-code
```javascript
function normalizeEmail(email, options) {
  options = (0, _merge2.default)(options, default_normalize_email_options);
  if (!(0, _isEmail2.default)(email)) {
    return false;
  }
  var parts = email.split('@', 2);
  parts[1] = parts[1].toLowerCase();
  if (parts[1] === 'gmail.com' || parts[1] === 'googlemail.com') {
    if (options.remove_extension) {
      parts[0] = parts[0].split('+')[0];
    }
    if (options.remove_dots) {
      parts[0] = parts[0].replace(/\./g, '');
    }
    if (!parts[0].length) {
      return false;
    }
    parts[0] = parts[0].toLowerCase();
    parts[1] = 'gmail.com';
  } else if (options.lowercase) {
    parts[0] = parts[0].toLowerCase();
  }
  return parts.join('@');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.not"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>not (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.not)
- description and source-code
```javascript
not = function (str, pattern, modifiers) {
  return this.notRegex(str, pattern, modifiers);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.notContains"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>notContains (str, elem)](#apidoc.element.sequelize.Validator.notContains)
- description and source-code
```javascript
notContains = function (str, elem) {
  return !this.contains(str, elem);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.notEmpty"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>notEmpty (str)](#apidoc.element.sequelize.Validator.notEmpty)
- description and source-code
```javascript
notEmpty = function (str) {
  return !str.match(/^[\s\t\r\n]*$/);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.notIn"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>notIn (str, values)](#apidoc.element.sequelize.Validator.notIn)
- description and source-code
```javascript
notIn = function (str, values) {
  return !this.isIn(str, values);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.notNull"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>notNull ()](#apidoc.element.sequelize.Validator.notNull)
- description and source-code
```javascript
notNull = function () {
  throw new Error('Warning "notNull" validation has been deprecated in favor of Schema based "allowNull"');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.notRegex"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>notRegex (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.notRegex)
- description and source-code
```javascript
notRegex = function (str, pattern, modifiers) {
  return !this.regex(str, pattern, modifiers);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.regex"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>regex (str, pattern, modifiers)](#apidoc.element.sequelize.Validator.regex)
- description and source-code
```javascript
regex = function (str, pattern, modifiers) {
  str += '';
  if (Object.prototype.toString.call(pattern).slice(8, -1) !== 'RegExp') {
    pattern = new RegExp(pattern, modifiers);
  }
  return str.match(pattern);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.rtrim"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>rtrim (str, chars)](#apidoc.element.sequelize.Validator.rtrim)
- description and source-code
```javascript
function rtrim(str, chars) {
  (0, _assertString2.default)(str);
  var pattern = chars ? new RegExp('[' + chars + ']') : /\s/;

  var idx = str.length - 1;
  while (idx >= 0 && pattern.test(str[idx])) {
    idx--;
  }

  return idx < str.length ? str.substr(0, idx + 1) : str;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.stripLow"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>stripLow (str, keep_new_lines)](#apidoc.element.sequelize.Validator.stripLow)
- description and source-code
```javascript
function stripLow(str, keep_new_lines) {
  (0, _assertString2.default)(str);
  var chars = keep_new_lines ? '\\x00-\\x09\\x0B\\x0C\\x0E-\\x1F\\x7F' : '\\x00-\\x1F\\x7F';
  return (0, _blacklist2.default)(str, chars);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.toBoolean"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>toBoolean (str, strict)](#apidoc.element.sequelize.Validator.toBoolean)
- description and source-code
```javascript
function toBoolean(str, strict) {
  (0, _assertString2.default)(str);
  if (strict) {
    return str === '1' || str === 'true';
  }
  return str !== '0' && str !== 'false' && str !== '';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.toDate"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>toDate (date)](#apidoc.element.sequelize.Validator.toDate)
- description and source-code
```javascript
function toDate(date) {
  (0, _assertString2.default)(date);
  date = Date.parse(date);
  return !isNaN(date) ? new Date(date) : null;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.toFloat"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>toFloat (str)](#apidoc.element.sequelize.Validator.toFloat)
- description and source-code
```javascript
function toFloat(str) {
  (0, _assertString2.default)(str);
  return parseFloat(str);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.toInt"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>toInt (str, radix)](#apidoc.element.sequelize.Validator.toInt)
- description and source-code
```javascript
function toInt(str, radix) {
  (0, _assertString2.default)(str);
  return parseInt(str, radix || 10);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.toString"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>toString (input)](#apidoc.element.sequelize.Validator.toString)
- description and source-code
```javascript
function toString(input) {
  if ((typeof input === 'undefined' ? 'undefined' : _typeof(input)) === 'object' && input !== null) {
    if (typeof input.toString === 'function') {
      input = input.toString();
    } else {
      input = '[object Object]';
    }
  } else if (input === null || typeof input === 'undefined' || isNaN(input) && !input.length) {
    input = '';
  }
  return String(input);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.trim"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>trim (str, chars)](#apidoc.element.sequelize.Validator.trim)
- description and source-code
```javascript
function trim(str, chars) {
  return (0, _rtrim2.default)((0, _ltrim2.default)(str, chars), chars);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.unescape"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>unescape (str)](#apidoc.element.sequelize.Validator.unescape)
- description and source-code
```javascript
function unescape(str) {
      (0, _assertString2.default)(str);
      return str.replace(/&amp;/g, '&').replace(/&quot;/g, '"').replace(/&#x27;/g, "'").replace(/&lt;/g, '<').replace(/&gt;/g, '>').
replace(/&#x2F;/g, '/').replace(/&#96;/g, ''');
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.Validator.whitelist"></a>[function <span class="apidocSignatureSpan">sequelize.Validator.</span>whitelist (str, chars)](#apidoc.element.sequelize.Validator.whitelist)
- description and source-code
```javascript
function whitelist(str, chars) {
  (0, _assertString2.default)(str);
  return str.replace(new RegExp('[^' + chars + ']+', 'g'), '');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.errors"></a>[module sequelize.errors](#apidoc.module.sequelize.errors)

#### <a name="apidoc.element.sequelize.errors.AccessDeniedError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>AccessDeniedError (parent)](#apidoc.element.sequelize.errors.AccessDeniedError)
- description and source-code
```javascript
AccessDeniedError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeAccessDeniedError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.BaseError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>BaseError ()](#apidoc.element.sequelize.errors.BaseError)
- description and source-code
```javascript
BaseError = function () {
  var tmp = Error.apply(this, arguments);
  tmp.name = this.name = 'SequelizeBaseError';

  this.message = tmp.message;
  if (Error.captureStackTrace)
    Error.captureStackTrace(this, this.constructor);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.ConnectionError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>ConnectionError (parent)](#apidoc.element.sequelize.errors.ConnectionError)
- description and source-code
```javascript
ConnectionError = function (parent) {
  error.BaseError.apply(this, [parent ? parent.message : '']);
  this.name = 'SequelizeConnectionError';
<span class="apidocCodeCommentSpan">/**
 * The connection specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.ConnectionRefusedError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>ConnectionRefusedError (parent)](#apidoc.element.sequelize.errors.ConnectionRefusedError)
- description and source-code
```javascript
ConnectionRefusedError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeConnectionRefusedError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.ConnectionTimedOutError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>ConnectionTimedOutError (parent)](#apidoc.element.sequelize.errors.ConnectionTimedOutError)
- description and source-code
```javascript
ConnectionTimedOutError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeConnectionTimedOutError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.DatabaseError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>DatabaseError (parent)](#apidoc.element.sequelize.errors.DatabaseError)
- description and source-code
```javascript
DatabaseError = function (parent) {
  error.BaseError.apply(this, [parent.message]);
  this.name = 'SequelizeDatabaseError';

<span class="apidocCodeCommentSpan">/**
 * The database specific error which triggered this one
 * @member parent
 */
</span>  this.parent = parent;
  this.original = parent;
/**
 * The SQL that triggered the error
 * @member sql
 */
  this.sql = parent.sql;
/**
 * The message from the DB.
 * @member message
 * @name message
 */
/**
 * The fields of the unique constraint
 * @member fields
 * @name fields
 */
/**
 * The value(s) which triggered the error
 * @member value
 * @name value
 */
/**
 * The name of the index that triggered the error
 * @member index
 * @name index
 */
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.EmptyResultError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>EmptyResultError (message)](#apidoc.element.sequelize.errors.EmptyResultError)
- description and source-code
```javascript
EmptyResultError = function (message) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeEmptyResultError';
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.ExclusionConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>ExclusionConstraintError (options)](#apidoc.element.sequelize.errors.ExclusionConstraintError)
- description and source-code
```javascript
ExclusionConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };

  error.DatabaseError.call(this, options.parent);
  this.name = 'SequelizeExclusionConstraintError';

  this.message = options.message || options.parent.message;
  this.constraint = options.constraint;
  this.fields = options.fields;
  this.table = options.table;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.ForeignKeyConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>ForeignKeyConstraintError (options)](#apidoc.element.sequelize.errors.ForeignKeyConstraintError)
- description and source-code
```javascript
ForeignKeyConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };

  error.DatabaseError.call(this, options.parent);
  this.name = 'SequelizeForeignKeyConstraintError';

  this.message = options.message || options.parent.message || 'Database Error';
  this.fields = options.fields;
  this.table = options.table;
  this.value = options.value;
  this.index = options.index;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.HostNotFoundError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>HostNotFoundError (parent)](#apidoc.element.sequelize.errors.HostNotFoundError)
- description and source-code
```javascript
HostNotFoundError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeHostNotFoundError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.HostNotReachableError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>HostNotReachableError (parent)](#apidoc.element.sequelize.errors.HostNotReachableError)
- description and source-code
```javascript
HostNotReachableError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeHostNotReachableError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.InstanceError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>InstanceError (message)](#apidoc.element.sequelize.errors.InstanceError)
- description and source-code
```javascript
InstanceError = function (message) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeInstanceError';
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.InvalidConnectionError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>InvalidConnectionError (parent)](#apidoc.element.sequelize.errors.InvalidConnectionError)
- description and source-code
```javascript
InvalidConnectionError = function (parent) {
  error.ConnectionError.call(this, parent);
  this.name = 'SequelizeInvalidConnectionError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.TimeoutError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>TimeoutError (parent)](#apidoc.element.sequelize.errors.TimeoutError)
- description and source-code
```javascript
TimeoutError = function (parent) {
  error.DatabaseError.call(this, parent);
  this.name = 'SequelizeTimeoutError';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.UniqueConstraintError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>UniqueConstraintError (options)](#apidoc.element.sequelize.errors.UniqueConstraintError)
- description and source-code
```javascript
UniqueConstraintError = function (options) {
  options = options || {};
  options.parent = options.parent || { sql: '' };
  options.message = options.message || options.parent.message || 'Validation Error';
  options.errors = options.errors || {};

  error.ValidationError.call(this, options.message, options.errors);
  this.name = 'SequelizeUniqueConstraintError';
  this.message = options.message;
  this.errors = options.errors;
  this.fields = options.fields;
  this.parent = options.parent;
  this.original = options.parent;
  this.sql = options.parent.sql;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.errors.ValidationError"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>ValidationError (message, errors)](#apidoc.element.sequelize.errors.ValidationError)
- description and source-code
```javascript
ValidationError = function (message, errors) {
  error.BaseError.apply(this, arguments);
  this.name = 'SequelizeValidationError';
  this.message = 'Validation Error';
<span class="apidocCodeCommentSpan">/**
 * An array of ValidationErrorItems
 * @member errors
 */
</span>  this.errors = errors || [];

  // Use provided error message if available...
  if (message) {
    this.message = message;

  // ... otherwise create a concatenated message out of existing errors.
  } else if (this.errors.length > 0 && this.errors[0].message) {
    this.message = this.errors.map(function(err) {
      return err.type + ': ' + err.message;
    }).join(',\n');
  }
}
```
- example usage
```shell
...
  return Promise.all([
    self._builtinValidators(),
    self._customValidators()
  ].map(function(promise) {
    return promise.reflect();
  })).then(function() {
    if (self.errors.length) {
      return new sequelizeError.ValidationError(null, self.errors);
    } else {
      return null;
    }
  });
};

/**
...
```

#### <a name="apidoc.element.sequelize.errors.ValidationErrorItem"></a>[function <span class="apidocSignatureSpan">sequelize.errors.</span>ValidationErrorItem (message, type, path, value)](#apidoc.element.sequelize.errors.ValidationErrorItem)
- description and source-code
```javascript
ValidationErrorItem = function (message, type, path, value) {
  this.message = message || '';
  this.type = type || null;
  this.path = path || null;
  this.value = value || null;
}
```
- example usage
```shell
...
 * @param {*} value anything.
 * @private
 */
InstanceValidator.prototype._validateSchema = function(rawAttribute, field, value) {
var error;

if (rawAttribute.allowNull === false && ((value === null) || (value === undefined))) {
  error = new sequelizeError.ValidationErrorItem(field + ' cannot be null', 'notNull Violation', field, value);
  this.errors.push(error);
}

if (rawAttribute.type === DataTypes.STRING || rawAttribute.type instanceof DataTypes.STRING || rawAttribute.type === DataTypes.TEXT
 || rawAttribute.type instanceof DataTypes.TEXT) {
  if (Array.isArray(value) || (_.isObject(value) && !value._isSequelizeMethod) && !Buffer.isBuffer(value)) {
    error = new sequelizeError.ValidationErrorItem(field + ' cannot be an array or an object', 'string violation', field, value);
    this.errors.push(error);
...
```



# <a name="apidoc.module.sequelize.hooks"></a>[module sequelize.hooks](#apidoc.module.sequelize.hooks)

#### <a name="apidoc.element.sequelize.hooks.applyTo"></a>[function <span class="apidocSignatureSpan">sequelize.hooks.</span>applyTo (Model)](#apidoc.element.sequelize.hooks.applyTo)
- description and source-code
```javascript
applyTo = function (Model) {
  Utils._.mixin(Model, Hooks);
  Utils._.mixin(Model.prototype, Hooks);

  var allHooks = Object.keys(hookTypes).concat(Object.keys(hookAliases));
  allHooks.forEach(function(hook) {
    Model.prototype[hook] = function(name, callback) {
      return this.addHook(hook, name, callback);
    };
  });
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.instance_validator"></a>[module sequelize.instance_validator](#apidoc.module.sequelize.instance_validator)

#### <a name="apidoc.element.sequelize.instance_validator.instance_validator"></a>[function <span class="apidocSignatureSpan">sequelize.</span>instance_validator (modelInstance, options)](#apidoc.element.sequelize.instance_validator.instance_validator)
- description and source-code
```javascript
function InstanceValidator(modelInstance, options) {
  options = _.clone(options) || {};

  if (options.fields && !options.skip) {
    options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
  }

  // assign defined and default options
  this.options = Utils._.defaults(options, {
    skip: []
  });

  this.modelInstance = modelInstance;

<span class="apidocCodeCommentSpan">  /**
   * Exposes a reference to validator.js. This allows you to add custom validations using 'validator.extend'
   * @name validator
   */
</span>  this.validator = validator;

  /**
   *  All errors will be stored here from the validations.
   *
   * @type {Array} Will contain keys that correspond to attributes which will
   *   be Arrays of Errors.
   */
  this.errors = [];

  /** @type {boolean} Indicates if validations are in progress */
  this.inProgress = false;

  extendModelValidations(modelInstance);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.instance_validator.InstanceValidator"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.</span>InstanceValidator (modelInstance, options)](#apidoc.element.sequelize.instance_validator.InstanceValidator)
- description and source-code
```javascript
function InstanceValidator(modelInstance, options) {
  options = _.clone(options) || {};

  if (options.fields && !options.skip) {
    options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
  }

  // assign defined and default options
  this.options = Utils._.defaults(options, {
    skip: []
  });

  this.modelInstance = modelInstance;

<span class="apidocCodeCommentSpan">  /**
   * Exposes a reference to validator.js. This allows you to add custom validations using 'validator.extend'
   * @name validator
   */
</span>  this.validator = validator;

  /**
   *  All errors will be stored here from the validations.
   *
   * @type {Array} Will contain keys that correspond to attributes which will
   *   be Arrays of Errors.
   */
  this.errors = [];

  /** @type {boolean} Indicates if validations are in progress */
  this.inProgress = false;

  extendModelValidations(modelInstance);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.instance_validator.default"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.</span>default (modelInstance, options)](#apidoc.element.sequelize.instance_validator.default)
- description and source-code
```javascript
function InstanceValidator(modelInstance, options) {
  options = _.clone(options) || {};

  if (options.fields && !options.skip) {
    options.skip = Utils._.difference(Object.keys(modelInstance.Model.attributes), options.fields);
  }

  // assign defined and default options
  this.options = Utils._.defaults(options, {
    skip: []
  });

  this.modelInstance = modelInstance;

<span class="apidocCodeCommentSpan">  /**
   * Exposes a reference to validator.js. This allows you to add custom validations using 'validator.extend'
   * @name validator
   */
</span>  this.validator = validator;

  /**
   *  All errors will be stored here from the validations.
   *
   * @type {Array} Will contain keys that correspond to attributes which will
   *   be Arrays of Errors.
   */
  this.errors = [];

  /** @type {boolean} Indicates if validations are in progress */
  this.inProgress = false;

  extendModelValidations(modelInstance);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.instance_validator.prototype"></a>[module sequelize.instance_validator.prototype](#apidoc.module.sequelize.instance_validator.prototype)

#### <a name="apidoc.element.sequelize.instance_validator.prototype._builtinAttrValidate"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_builtinAttrValidate (value, field)](#apidoc.element.sequelize.instance_validator.prototype._builtinAttrValidate)
- description and source-code
```javascript
_builtinAttrValidate = function (value, field) {
  var self = this;
  // check if value is null (if null not allowed the Schema pass will capture it)
  if (value === null || typeof value === 'undefined') {
    return Promise.resolve();
  }

  // Promisify each validator
  var validators = [];
  Utils._.forIn(this.modelInstance.validators[field], function(test,
    validatorType) {

    if (['isUrl', 'isURL', 'isEmail'].indexOf(validatorType) !== -1) {
      // Preserve backwards compat. Validator.js now expects the second param to isURL and isEmail to be an object
      if (typeof test === 'object' && test !== null && test.msg) {
        test = {
          msg: test.msg
        };
      } else if (test === true) {
        test = {};
      }
    }

    // Check for custom validator.
    if (typeof test === 'function') {
      return validators.push(self._invokeCustomValidator(test, validatorType, true, value, field).reflect());
    }

    var validatorPromise = self._invokeBuiltinValidator(value, test, validatorType, field);
    // errors are handled in settling, stub this
    validatorPromise.catch(function() {});
    validators.push(validatorPromise.reflect());
  });

  return Promise.all(validators).then(this._handleReflectedResult.bind(this, field));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._builtinValidators"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_builtinValidators ()](#apidoc.element.sequelize.instance_validator.prototype._builtinValidators)
- description and source-code
```javascript
_builtinValidators = function () {
  var self = this;

  // promisify all attribute invocations
  var validators = [];
  Utils._.forIn(this.modelInstance.rawAttributes, function(rawAttribute, field) {
    if (self.options.skip.indexOf(field) >= 0) {
      return;
    }

    var value = self.modelInstance.dataValues[field];

    if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
      // perform validations based on schema
      self._validateSchema(rawAttribute, field, value);
    }

    if (self.modelInstance.validators.hasOwnProperty(field)) {
      validators.push(self._builtinAttrValidate.call(self, value, field).reflect());
    }
  });

  return Promise.all(validators);
}
```
- example usage
```shell
...
if (this.inProgress) {
  throw new Error('Validations already in progress.');
}
this.inProgress = true;

var self = this;
return Promise.all([
  self._builtinValidators(),
  self._customValidators()
].map(function(promise) {
  return promise.reflect();
})).then(function() {
  if (self.errors.length) {
    return new sequelizeError.ValidationError(null, self.errors);
  } else {
...
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._customValidators"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_customValidators ()](#apidoc.element.sequelize.instance_validator.prototype._customValidators)
- description and source-code
```javascript
_customValidators = function () {
  var validators = [];
  var self = this;
  Utils._.each(this.modelInstance.$modelOptions.validate, function(validator, validatorType) {
    if (self.options.skip.indexOf(validatorType) >= 0) {
      return;
    }

    var valprom = self._invokeCustomValidator(validator, validatorType)
      // errors are handled in settling, stub this
      .catch(function() {})
      .reflect();

    validators.push(valprom);
  });

  return Promise.all(validators);
}
```
- example usage
```shell
...
  throw new Error('Validations already in progress.');
}
this.inProgress = true;

var self = this;
return Promise.all([
  self._builtinValidators(),
  self._customValidators()
].map(function(promise) {
  return promise.reflect();
})).then(function() {
  if (self.errors.length) {
    return new sequelizeError.ValidationError(null, self.errors);
  } else {
    return null;
...
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._extractValidatorArgs"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_extractValidatorArgs (test, validatorType, field)](#apidoc.element.sequelize.instance_validator.prototype._extractValidatorArgs)
- description and source-code
```javascript
_extractValidatorArgs = function (test, validatorType, field) {
  var validatorArgs = test.args || test;
  var isLocalizedValidator = typeof(validatorArgs) !== 'string' && (validatorType === 'isAlpha' || validatorType === 'isAlphanumeric
' || validatorType === 'isMobilePhone');

  if (!Array.isArray(validatorArgs)) {
    if (validatorType === 'isImmutable') {
      validatorArgs = [validatorArgs, field];
    } else if (isLocalizedValidator || validatorType === 'isIP') {
      validatorArgs = [];
    } else {
      validatorArgs = [validatorArgs];
    }
  } else {
    validatorArgs = validatorArgs.slice(0);
  }
  return validatorArgs;
}
```
- example usage
```shell
...
  var self = this;
  // Cast value as string to pass new Validator.js string requirement
  var valueString = String(value);
  // check if Validator knows that kind of validation test
  if (typeof validator[validatorType] !== 'function') {
    throw new Error('Invalid validator function: ' + validatorType);
  }
  var validatorArgs = self._extractValidatorArgs(test, validatorType, field);
  if (!validator[validatorType].apply(validator, [valueString].concat(validatorArgs))) {
  // extract the error msg
    throw new Error(test.msg || 'Validation ' + validatorType + ' failed');
  }
});

/**
...
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._handleReflectedResult"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_handleReflectedResult (field, promiseInspections)](#apidoc.element.sequelize.instance_validator.prototype._handleReflectedResult)
- description and source-code
```javascript
_handleReflectedResult = function (field, promiseInspections) {
  var self = this;
  promiseInspections.forEach(function(promiseInspection) {
    if (promiseInspection.isRejected()) {
      var rejection = promiseInspection.error();
      self._pushError(true, field, rejection);
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._invokeBuiltinValidator"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_invokeBuiltinValidator ()](#apidoc.element.sequelize.instance_validator.prototype._invokeBuiltinValidator)
- description and source-code
```javascript
_invokeBuiltinValidator = function () {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._pushContext();
    var value = tryCatch(fn).apply(this, arguments);
    var promiseCreated = ret._popContext();
    debug.checkForgottenReturns(
        value, promiseCreated, "Promise.method", ret);
    ret._resolveFromSyncValue(value);
    return ret;
}
```
- example usage
```shell
...
    }

    // Check for custom validator.
    if (typeof test === 'function') {
      return validators.push(self._invokeCustomValidator(test, validatorType, true, value, field).reflect());
    }

    var validatorPromise = self._invokeBuiltinValidator(value, test, validatorType, field);
    // errors are handled in settling, stub this
    validatorPromise.catch(function() {});
    validators.push(validatorPromise.reflect());
  });

  return Promise.all(validators).then(this._handleReflectedResult.bind(this, field));
};
...
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._invokeCustomValidator"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_invokeCustomValidator ()](#apidoc.element.sequelize.instance_validator.prototype._invokeCustomValidator)
- description and source-code
```javascript
_invokeCustomValidator = function () {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._pushContext();
    var value = tryCatch(fn).apply(this, arguments);
    var promiseCreated = ret._popContext();
    debug.checkForgottenReturns(
        value, promiseCreated, "Promise.method", ret);
    ret._resolveFromSyncValue(value);
    return ret;
}
```
- example usage
```shell
...
var validators = [];
var self = this;
Utils._.each(this.modelInstance.$modelOptions.validate, function(validator, validatorType) {
  if (self.options.skip.indexOf(validatorType) >= 0) {
    return;
  }

  var valprom = self._invokeCustomValidator(validator, validatorType)
    // errors are handled in settling, stub this
    .catch(function() {})
    .reflect();

  validators.push(valprom);
});
...
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._pushError"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_pushError (isBuiltin, errorKey, rawError)](#apidoc.element.sequelize.instance_validator.prototype._pushError)
- description and source-code
```javascript
_pushError = function (isBuiltin, errorKey, rawError) {
  var message = rawError.message || rawError || 'Validation error';
  var error = new sequelizeError.ValidationErrorItem(message, 'Validation error', errorKey, rawError);
  error[InstanceValidator.RAW_KEY_NAME] = rawError;

  this.errors.push(error);
}
```
- example usage
```shell
...
* @private
*/
InstanceValidator.prototype._handleReflectedResult = function(field, promiseInspections) {
 var self = this;
 promiseInspections.forEach(function(promiseInspection) {
   if (promiseInspection.isRejected()) {
     var rejection = promiseInspection.error();
     self._pushError(true, field, rejection);
   }
 });
};

/**
* Signs all errors retaining the original.
*
...
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype._validateSchema"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>_validateSchema (rawAttribute, field, value)](#apidoc.element.sequelize.instance_validator.prototype._validateSchema)
- description and source-code
```javascript
_validateSchema = function (rawAttribute, field, value) {
  var error;

  if (rawAttribute.allowNull === false && ((value === null) || (value === undefined))) {
    error = new sequelizeError.ValidationErrorItem(field + ' cannot be null', 'notNull Violation', field, value);
    this.errors.push(error);
  }

  if (rawAttribute.type === DataTypes.STRING || rawAttribute.type instanceof DataTypes.STRING || rawAttribute.type === DataTypes
.TEXT || rawAttribute.type instanceof DataTypes.TEXT) {
    if (Array.isArray(value) || (_.isObject(value) && !value._isSequelizeMethod) && !Buffer.isBuffer(value)) {
      error = new sequelizeError.ValidationErrorItem(field + ' cannot be an array or an object', 'string violation', field, value
);
      this.errors.push(error);
    }
  }
}
```
- example usage
```shell
...
    return;
  }

  var value = self.modelInstance.dataValues[field];

  if (!rawAttribute._autoGenerated && !rawAttribute.autoIncrement) {
    // perform validations based on schema
    self._validateSchema(rawAttribute, field, value);
  }

  if (self.modelInstance.validators.hasOwnProperty(field)) {
    validators.push(self._builtinAttrValidate.call(self, value, field).reflect());
  }
});
...
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype.hookValidate"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>hookValidate ()](#apidoc.element.sequelize.instance_validator.prototype.hookValidate)
- description and source-code
```javascript
hookValidate = function () {
  var self = this;
  return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
    return self.validate().then(function(error) {
      if (error) {
        return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
          throw newError || error;
        });
      }
    });
  }).then(function() {
    return self.modelInstance.Model.runHooks('afterValidate', self.modelInstance, self.options);
  }).return(self.modelInstance);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.instance_validator.prototype.validate"></a>[function <span class="apidocSignatureSpan">sequelize.instance_validator.prototype.</span>validate ()](#apidoc.element.sequelize.instance_validator.prototype.validate)
- description and source-code
```javascript
validate = function () {
  if (this.inProgress) {
    throw new Error('Validations already in progress.');
  }
  this.inProgress = true;

  var self = this;
  return Promise.all([
    self._builtinValidators(),
    self._customValidators()
  ].map(function(promise) {
    return promise.reflect();
  })).then(function() {
    if (self.errors.length) {
      return new sequelizeError.ValidationError(null, self.errors);
    } else {
      return null;
    }
  });
}
```
- example usage
```shell
...
 *   - On validation failure: Validation Failed Model Hooks
 *
 * @return {Promise}
 */
InstanceValidator.prototype.hookValidate = function() {
var self = this;
return self.modelInstance.Model.runHooks('beforeValidate', self.modelInstance, self.options).then(function() {
  return self.validate().then(function(error) {
    if (error) {
      return self.modelInstance.Model.runHooks('validationFailed', self.modelInstance, self.options, error).then(function(newError
) {
        throw newError || error;
      });
    }
  });
}).then(function() {
...
```



# <a name="apidoc.module.sequelize.model_manager"></a>[module sequelize.model_manager](#apidoc.module.sequelize.model_manager)

#### <a name="apidoc.element.sequelize.model_manager.model_manager"></a>[function <span class="apidocSignatureSpan">sequelize.</span>model_manager (sequelize)](#apidoc.element.sequelize.model_manager.model_manager)
- description and source-code
```javascript
model_manager = function (sequelize) {
  this.models = [];
  this.sequelize = sequelize;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.model_manager.ModelManager"></a>[function <span class="apidocSignatureSpan">sequelize.model_manager.</span>ModelManager (sequelize)](#apidoc.element.sequelize.model_manager.ModelManager)
- description and source-code
```javascript
ModelManager = function (sequelize) {
  this.models = [];
  this.sequelize = sequelize;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.model_manager.default"></a>[function <span class="apidocSignatureSpan">sequelize.model_manager.</span>default (sequelize)](#apidoc.element.sequelize.model_manager.default)
- description and source-code
```javascript
default = function (sequelize) {
  this.models = [];
  this.sequelize = sequelize;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.model_manager.prototype"></a>[module sequelize.model_manager.prototype](#apidoc.module.sequelize.model_manager.prototype)

#### <a name="apidoc.element.sequelize.model_manager.prototype.addModel"></a>[function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>addModel (model)](#apidoc.element.sequelize.model_manager.prototype.addModel)
- description and source-code
```javascript
addModel = function (model) {
  this.models.push(model);
  this.sequelize.models[model.name] = model;

  return model;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.model_manager.prototype.forEachModel"></a>[function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>forEachModel (iterator, options)](#apidoc.element.sequelize.model_manager.prototype.forEachModel)
- description and source-code
```javascript
forEachModel = function (iterator, options) {
  var models = {}
    , sorter = new Toposort()
    , sorted
    , dep;

  options = _.defaults(options || {}, {
    reverse: true
  });

  this.models.forEach(function(model) {
    var deps = []
      , tableName = model.getTableName();

    if (_.isObject(tableName)) {
      tableName = tableName.schema + '.' + tableName.tableName;
    }

    models[tableName] = model;

    for (var attrName in model.rawAttributes) {
      if (model.rawAttributes.hasOwnProperty(attrName)) {
        var attribute = model.rawAttributes[attrName];

        if (attribute.references) {
          attribute = Utils.formatReferences(attribute);
          dep       = attribute.references.model;

          if (_.isObject(dep)) {
            dep = dep.schema + '.' + dep.tableName;
          }

          deps.push(dep);
        }
      }
    }

    deps = deps.filter(function(dep) {
      return tableName !== dep;
    });

    sorter.add(tableName, deps);
  });

  sorted = sorter.sort();
  if (options.reverse) {
    sorted = sorted.reverse();
  }
  sorted.forEach(function(name) {
    iterator(models[name], name);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.model_manager.prototype.getModel"></a>[function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>getModel (against, options)](#apidoc.element.sequelize.model_manager.prototype.getModel)
- description and source-code
```javascript
getModel = function (against, options) {
  options = _.defaults(options || {}, {
    attribute: 'name'
  });

  var model = this.models.filter(function(model) {
    return model[options.attribute] === against;
  });

  return !!model ? model[0] : null;
}
```
- example usage
```shell
...

  return this.sequelize.query(sql, options).then(function() {
    var promises = [];

    // Since postgres has a special case for enums, we should drop the related
    // enum type within the table and attribute
    if (self.sequelize.options.dialect === 'postgres') {
      var instanceTable = self.sequelize.modelManager.getModel(tableName, { attribute: 'tableName' });

      if (!!instanceTable) {
var getTableName = (!options || !options.schema || options.schema === 'public' ? '' : options.schema + '_') + tableName;

var keys = Object.keys(instanceTable.rawAttributes)
  , keyLen = keys.length
  , i = 0;
...
```

#### <a name="apidoc.element.sequelize.model_manager.prototype.removeModel"></a>[function <span class="apidocSignatureSpan">sequelize.model_manager.prototype.</span>removeModel (model)](#apidoc.element.sequelize.model_manager.prototype.removeModel)
- description and source-code
```javascript
removeModel = function (model) {
  this.models = this.models.filter(function($model) {
    return $model.name !== model.name;
  });

  delete this.sequelize.models[model.name];
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql"></a>[module sequelize.mssql](#apidoc.module.sequelize.mssql)

#### <a name="apidoc.element.sequelize.mssql.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>BIGINT ()](#apidoc.element.sequelize.mssql.BIGINT)
- description and source-code
```javascript
BIGINT = function () {
  if (!(this instanceof BIGINT)) return new BIGINT();
  BaseTypes.BIGINT.apply(this, arguments);

  // MSSQL does not support any options for bigint
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support BIGINT with options. Plain 'BIGINT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>BLOB ()](#apidoc.element.sequelize.mssql.BLOB)
- description and source-code
```javascript
BLOB = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>BOOLEAN ()](#apidoc.element.sequelize.mssql.BOOLEAN)
- description and source-code
```javascript
BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>DATE ()](#apidoc.element.sequelize.mssql.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>ENUM ()](#apidoc.element.sequelize.mssql.ENUM)
- description and source-code
```javascript
ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>FLOAT ()](#apidoc.element.sequelize.mssql.FLOAT)
- description and source-code
```javascript
FLOAT = function () {
  if (!(this instanceof FLOAT)) return new FLOAT();
  BaseTypes.FLOAT.apply(this, arguments);

  // MSSQL does only support lengths as option.
  // Values between 1-24 result in 7 digits precision (4 bytes storage size)
  // Values between 25-53 result in 15 digits precision (8 bytes storage size)
  // If decimals are provided remove these and print a warning
  if (this._decimals) {
    warn('MSSQL does not support Float with decimals. Plain 'FLOAT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
  }
  if (this._unsigned) {
    warn('MSSQL does not support Float unsigned. 'UNSIGNED' was removed.');
    this._unsigned = undefined;
  }
  if (this._zerofill) {
    warn('MSSQL does not support Float zerofill. 'ZEROFILL' was removed.');
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>INTEGER ()](#apidoc.element.sequelize.mssql.INTEGER)
- description and source-code
```javascript
INTEGER = function () {
  if (!(this instanceof INTEGER)) return new INTEGER();
  BaseTypes.INTEGER.apply(this, arguments);

  // MSSQL does not support any options for integer
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support INTEGER with options. Plain 'INTEGER' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.NOW"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>NOW ()](#apidoc.element.sequelize.mssql.NOW)
- description and source-code
```javascript
NOW = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>REAL ()](#apidoc.element.sequelize.mssql.REAL)
- description and source-code
```javascript
REAL = function () {
  if (!(this instanceof REAL)) return new REAL();
  BaseTypes.REAL.apply(this, arguments);

  // MSSQL does not support any options for real
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support REAL with options. Plain 'REAL' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>STRING ()](#apidoc.element.sequelize.mssql.STRING)
- description and source-code
```javascript
STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>TEXT ()](#apidoc.element.sequelize.mssql.TEXT)
- description and source-code
```javascript
TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>UUID ()](#apidoc.element.sequelize.mssql.UUID)
- description and source-code
```javascript
UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.BIGINT"></a>[module sequelize.mssql.BIGINT](#apidoc.module.sequelize.mssql.BIGINT)

#### <a name="apidoc.element.sequelize.mssql.BIGINT.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>BIGINT ()](#apidoc.element.sequelize.mssql.BIGINT.BIGINT)
- description and source-code
```javascript
BIGINT = function () {
  if (!(this instanceof BIGINT)) return new BIGINT();
  BaseTypes.BIGINT.apply(this, arguments);

  // MSSQL does not support any options for bigint
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support BIGINT with options. Plain 'BIGINT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BIGINT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>extend (oldType)](#apidoc.element.sequelize.mssql.BIGINT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BIGINT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.BIGINT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.BIGINT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.mssql.BIGINT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BIGINT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.mssql.BIGINT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.BLOB"></a>[module sequelize.mssql.BLOB](#apidoc.module.sequelize.mssql.BLOB)

#### <a name="apidoc.element.sequelize.mssql.BLOB.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>BLOB ()](#apidoc.element.sequelize.mssql.BLOB.BLOB)
- description and source-code
```javascript
BLOB = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BLOB.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>extend (oldType)](#apidoc.element.sequelize.mssql.BLOB.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BLOB.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.BLOB.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.BLOB.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>super_ (options)](#apidoc.element.sequelize.mssql.BLOB.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BLOB.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.</span>warn (link, text)](#apidoc.element.sequelize.mssql.BLOB.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.BLOB.prototype"></a>[module sequelize.mssql.BLOB.prototype](#apidoc.module.sequelize.mssql.BLOB.prototype)

#### <a name="apidoc.element.sequelize.mssql.BLOB.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BLOB.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.BLOB.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._length) {
    if (this._length.toLowerCase() === 'tiny') { // tiny = 2^8
      warn('MSSQL does not support BLOB with the 'length' = 'tiny' option. 'VARBINARY(256)' will be used instead.');
      return 'VARBINARY(256)';
    }
    warn('MSSQL does not support BLOB with the 'length' option. 'VARBINARY(MAX)' will be used instead.');
  }
  return 'VARBINARY(MAX)';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.BOOLEAN"></a>[module sequelize.mssql.BOOLEAN](#apidoc.module.sequelize.mssql.BOOLEAN)

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>BOOLEAN ()](#apidoc.element.sequelize.mssql.BOOLEAN.BOOLEAN)
- description and source-code
```javascript
BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>extend (oldType)](#apidoc.element.sequelize.mssql.BOOLEAN.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.BOOLEAN.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>super_ (options)](#apidoc.element.sequelize.mssql.BOOLEAN.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.</span>warn (link, text)](#apidoc.element.sequelize.mssql.BOOLEAN.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.BOOLEAN.prototype"></a>[module sequelize.mssql.BOOLEAN.prototype](#apidoc.module.sequelize.mssql.BOOLEAN.prototype)

#### <a name="apidoc.element.sequelize.mssql.BOOLEAN.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.BOOLEAN.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.BOOLEAN.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'BIT';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.DATE"></a>[module sequelize.mssql.DATE](#apidoc.module.sequelize.mssql.DATE)

#### <a name="apidoc.element.sequelize.mssql.DATE.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>DATE ()](#apidoc.element.sequelize.mssql.DATE.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.DATE.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>extend (oldType)](#apidoc.element.sequelize.mssql.DATE.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.DATE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.DATE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.DATE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>super_ (options)](#apidoc.element.sequelize.mssql.DATE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.DATE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.DATE.</span>warn (link, text)](#apidoc.element.sequelize.mssql.DATE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.DATE.prototype"></a>[module sequelize.mssql.DATE.prototype](#apidoc.module.sequelize.mssql.DATE.prototype)

#### <a name="apidoc.element.sequelize.mssql.DATE.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.DATE.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'DATETIME2';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.ENUM"></a>[module sequelize.mssql.ENUM](#apidoc.module.sequelize.mssql.ENUM)

#### <a name="apidoc.element.sequelize.mssql.ENUM.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>ENUM ()](#apidoc.element.sequelize.mssql.ENUM.ENUM)
- description and source-code
```javascript
ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.ENUM.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>extend (oldType)](#apidoc.element.sequelize.mssql.ENUM.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.ENUM.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.ENUM.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.ENUM.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>super_ (options)](#apidoc.element.sequelize.mssql.ENUM.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.ENUM.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.mssql.ENUM.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.ENUM.prototype"></a>[module sequelize.mssql.ENUM.prototype](#apidoc.module.sequelize.mssql.ENUM.prototype)

#### <a name="apidoc.element.sequelize.mssql.ENUM.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.ENUM.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.ENUM.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'VARCHAR(255)';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.FLOAT"></a>[module sequelize.mssql.FLOAT](#apidoc.module.sequelize.mssql.FLOAT)

#### <a name="apidoc.element.sequelize.mssql.FLOAT.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>FLOAT ()](#apidoc.element.sequelize.mssql.FLOAT.FLOAT)
- description and source-code
```javascript
FLOAT = function () {
  if (!(this instanceof FLOAT)) return new FLOAT();
  BaseTypes.FLOAT.apply(this, arguments);

  // MSSQL does only support lengths as option.
  // Values between 1-24 result in 7 digits precision (4 bytes storage size)
  // Values between 25-53 result in 15 digits precision (8 bytes storage size)
  // If decimals are provided remove these and print a warning
  if (this._decimals) {
    warn('MSSQL does not support Float with decimals. Plain 'FLOAT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
  }
  if (this._unsigned) {
    warn('MSSQL does not support Float unsigned. 'UNSIGNED' was removed.');
    this._unsigned = undefined;
  }
  if (this._zerofill) {
    warn('MSSQL does not support Float zerofill. 'ZEROFILL' was removed.');
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.FLOAT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>extend (oldType)](#apidoc.element.sequelize.mssql.FLOAT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.FLOAT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.FLOAT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.FLOAT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.mssql.FLOAT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.FLOAT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.mssql.FLOAT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.INTEGER"></a>[module sequelize.mssql.INTEGER](#apidoc.module.sequelize.mssql.INTEGER)

#### <a name="apidoc.element.sequelize.mssql.INTEGER.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>INTEGER ()](#apidoc.element.sequelize.mssql.INTEGER.INTEGER)
- description and source-code
```javascript
INTEGER = function () {
  if (!(this instanceof INTEGER)) return new INTEGER();
  BaseTypes.INTEGER.apply(this, arguments);

  // MSSQL does not support any options for integer
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support INTEGER with options. Plain 'INTEGER' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.INTEGER.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>extend (oldType)](#apidoc.element.sequelize.mssql.INTEGER.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.INTEGER.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.INTEGER.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.INTEGER.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.mssql.INTEGER.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.INTEGER.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.mssql.INTEGER.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.NOW"></a>[module sequelize.mssql.NOW](#apidoc.module.sequelize.mssql.NOW)

#### <a name="apidoc.element.sequelize.mssql.NOW.NOW"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>NOW ()](#apidoc.element.sequelize.mssql.NOW.NOW)
- description and source-code
```javascript
NOW = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.NOW.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>extend (oldType)](#apidoc.element.sequelize.mssql.NOW.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.NOW.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.NOW.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.NOW.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>super_ (options)](#apidoc.element.sequelize.mssql.NOW.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.NOW.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.NOW.</span>warn (link, text)](#apidoc.element.sequelize.mssql.NOW.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.NOW.prototype"></a>[module sequelize.mssql.NOW.prototype](#apidoc.module.sequelize.mssql.NOW.prototype)

#### <a name="apidoc.element.sequelize.mssql.NOW.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.NOW.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.NOW.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'GETDATE()';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.REAL"></a>[module sequelize.mssql.REAL](#apidoc.module.sequelize.mssql.REAL)

#### <a name="apidoc.element.sequelize.mssql.REAL.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>REAL ()](#apidoc.element.sequelize.mssql.REAL.REAL)
- description and source-code
```javascript
REAL = function () {
  if (!(this instanceof REAL)) return new REAL();
  BaseTypes.REAL.apply(this, arguments);

  // MSSQL does not support any options for real
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('MSSQL does not support REAL with options. Plain 'REAL' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.REAL.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>extend (oldType)](#apidoc.element.sequelize.mssql.REAL.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.REAL.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.REAL.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.REAL.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>super_ (options)](#apidoc.element.sequelize.mssql.REAL.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.REAL.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.REAL.</span>warn (link, text)](#apidoc.element.sequelize.mssql.REAL.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.STRING"></a>[module sequelize.mssql.STRING](#apidoc.module.sequelize.mssql.STRING)

#### <a name="apidoc.element.sequelize.mssql.STRING.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>STRING ()](#apidoc.element.sequelize.mssql.STRING.STRING)
- description and source-code
```javascript
STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.STRING.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>extend (oldType)](#apidoc.element.sequelize.mssql.STRING.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.STRING.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.STRING.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.STRING.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>super_ (options)](#apidoc.element.sequelize.mssql.STRING.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.STRING.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.STRING.</span>warn (link, text)](#apidoc.element.sequelize.mssql.STRING.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.STRING.prototype"></a>[module sequelize.mssql.STRING.prototype](#apidoc.module.sequelize.mssql.STRING.prototype)

#### <a name="apidoc.element.sequelize.mssql.STRING.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.STRING.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (!this._binary) {
    return 'NVARCHAR(' + this._length + ')';
  } else{
    return 'BINARY(' + this._length + ')';
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.TEXT"></a>[module sequelize.mssql.TEXT](#apidoc.module.sequelize.mssql.TEXT)

#### <a name="apidoc.element.sequelize.mssql.TEXT.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>TEXT ()](#apidoc.element.sequelize.mssql.TEXT.TEXT)
- description and source-code
```javascript
TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.TEXT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>extend (oldType)](#apidoc.element.sequelize.mssql.TEXT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.TEXT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.TEXT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.TEXT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>super_ (options)](#apidoc.element.sequelize.mssql.TEXT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.TEXT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.mssql.TEXT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.TEXT.prototype"></a>[module sequelize.mssql.TEXT.prototype](#apidoc.module.sequelize.mssql.TEXT.prototype)

#### <a name="apidoc.element.sequelize.mssql.TEXT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.TEXT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  // TEXT is deprecated in mssql and it would normally be saved as a non-unicode string.
  // Using unicode is just future proof
  if (this._length) {
    if (this._length.toLowerCase() === 'tiny') { // tiny = 2^8
      warn('MSSQL does not support TEXT with the 'length' = 'tiny' option. 'NVARCHAR(256)' will be used instead.');
      return 'NVARCHAR(256)';
    }
    warn('MSSQL does not support TEXT with the 'length' option. 'NVARCHAR(MAX)' will be used instead.');
  }
  return 'NVARCHAR(MAX)';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.UUID"></a>[module sequelize.mssql.UUID](#apidoc.module.sequelize.mssql.UUID)

#### <a name="apidoc.element.sequelize.mssql.UUID.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.</span>UUID ()](#apidoc.element.sequelize.mssql.UUID.UUID)
- description and source-code
```javascript
UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.UUID.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>extend (oldType)](#apidoc.element.sequelize.mssql.UUID.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.UUID.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>inherits (Constructor)](#apidoc.element.sequelize.mssql.UUID.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mssql.UUID.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>super_ (options)](#apidoc.element.sequelize.mssql.UUID.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mssql.UUID.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.UUID.</span>warn (link, text)](#apidoc.element.sequelize.mssql.UUID.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mssql.UUID.prototype"></a>[module sequelize.mssql.UUID.prototype](#apidoc.module.sequelize.mssql.UUID.prototype)

#### <a name="apidoc.element.sequelize.mssql.UUID.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mssql.UUID.prototype.</span>toSql ()](#apidoc.element.sequelize.mssql.UUID.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'CHAR(36)';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql"></a>[module sequelize.mysql](#apidoc.module.sequelize.mysql)

#### <a name="apidoc.element.sequelize.mysql.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>DATE ()](#apidoc.element.sequelize.mysql.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>ENUM ()](#apidoc.element.sequelize.mysql.ENUM)
- description and source-code
```javascript
ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>GEOMETRY ()](#apidoc.element.sequelize.mysql.GEOMETRY)
- description and source-code
```javascript
GEOMETRY = function () {
  if (!(this instanceof GEOMETRY)) return new GEOMETRY();
  BaseTypes.GEOMETRY.apply(this, arguments);

  if (_.isEmpty(this.type)) {
    this.sqlType = this.key;
  } else if (_.includes(SUPPORTED_GEOMETRY_TYPES, this.type)) {
    this.sqlType = this.type;
  } else {
    throw new Error('Supported geometry types are: ' + SUPPORTED_GEOMETRY_TYPES.join(', '));
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>UUID ()](#apidoc.element.sequelize.mysql.UUID)
- description and source-code
```javascript
UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.DATE"></a>[module sequelize.mysql.DATE](#apidoc.module.sequelize.mysql.DATE)

#### <a name="apidoc.element.sequelize.mysql.DATE.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>DATE ()](#apidoc.element.sequelize.mysql.DATE.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.DATE.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>extend (oldType)](#apidoc.element.sequelize.mysql.DATE.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.DATE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.DATE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mysql.DATE.parse"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>parse (value, options)](#apidoc.element.sequelize.mysql.DATE.parse)
- description and source-code
```javascript
parse = function (value, options) {
  value = value.string();

  if (value === null) {
    return value;
  }

  if (moment.tz.zone(options.timezone)) {
    value = moment.tz(value, options.timezone).toDate();
  } else {
    value = new Date(value + ' ' + options.timezone);
  }

  return value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.DATE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>super_ (options)](#apidoc.element.sequelize.mysql.DATE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.DATE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.DATE.</span>warn (link, text)](#apidoc.element.sequelize.mysql.DATE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.DATE.prototype"></a>[module sequelize.mysql.DATE.prototype](#apidoc.module.sequelize.mysql.DATE.prototype)

#### <a name="apidoc.element.sequelize.mysql.DATE.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.mysql.DATE.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'DATETIME' + (this._length ? '(' + this._length + ')' : '');
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.ENUM"></a>[module sequelize.mysql.ENUM](#apidoc.module.sequelize.mysql.ENUM)

#### <a name="apidoc.element.sequelize.mysql.ENUM.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>ENUM ()](#apidoc.element.sequelize.mysql.ENUM.ENUM)
- description and source-code
```javascript
ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.ENUM.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>extend (oldType)](#apidoc.element.sequelize.mysql.ENUM.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.ENUM.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.ENUM.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mysql.ENUM.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>super_ (options)](#apidoc.element.sequelize.mysql.ENUM.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.ENUM.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.mysql.ENUM.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.ENUM.prototype"></a>[module sequelize.mysql.ENUM.prototype](#apidoc.module.sequelize.mysql.ENUM.prototype)

#### <a name="apidoc.element.sequelize.mysql.ENUM.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.ENUM.prototype.</span>toSql (options)](#apidoc.element.sequelize.mysql.ENUM.prototype.toSql)
- description and source-code
```javascript
toSql = function (options) {
  return 'ENUM(' + _.map(this.values, function(value) {
    return options.escape(value);
  }).join(', ') + ')';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.GEOMETRY"></a>[module sequelize.mysql.GEOMETRY](#apidoc.module.sequelize.mysql.GEOMETRY)

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>GEOMETRY ()](#apidoc.element.sequelize.mysql.GEOMETRY.GEOMETRY)
- description and source-code
```javascript
GEOMETRY = function () {
  if (!(this instanceof GEOMETRY)) return new GEOMETRY();
  BaseTypes.GEOMETRY.apply(this, arguments);

  if (_.isEmpty(this.type)) {
    this.sqlType = this.key;
  } else if (_.includes(SUPPORTED_GEOMETRY_TYPES, this.type)) {
    this.sqlType = this.type;
  } else {
    throw new Error('Supported geometry types are: ' + SUPPORTED_GEOMETRY_TYPES.join(', '));
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>extend (oldType)](#apidoc.element.sequelize.mysql.GEOMETRY.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.GEOMETRY.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.parse"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>parse (value)](#apidoc.element.sequelize.mysql.GEOMETRY.parse)
- description and source-code
```javascript
parse = function (value) {
  value = value.buffer();

  //MySQL doesn't support POINT EMPTY, https://dev.mysql.com/worklog/task/?id=2381
  if (value === null) {
    return null;
  }

  // For some reason, discard the first 4 bytes
  value = value.slice(4);

  return wkx.Geometry.parse(value).toGeoJSON();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>super_ (options)](#apidoc.element.sequelize.mysql.GEOMETRY.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.</span>warn (link, text)](#apidoc.element.sequelize.mysql.GEOMETRY.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.GEOMETRY.prototype"></a>[module sequelize.mysql.GEOMETRY.prototype](#apidoc.module.sequelize.mysql.GEOMETRY.prototype)

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.prototype.parse"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.prototype.</span>parse (value)](#apidoc.element.sequelize.mysql.GEOMETRY.prototype.parse)
- description and source-code
```javascript
parse = function (value) {
  value = value.buffer();

  //MySQL doesn't support POINT EMPTY, https://dev.mysql.com/worklog/task/?id=2381
  if (value === null) {
    return null;
  }

  // For some reason, discard the first 4 bytes
  value = value.slice(4);

  return wkx.Geometry.parse(value).toGeoJSON();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.GEOMETRY.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.GEOMETRY.prototype.</span>toSql ()](#apidoc.element.sequelize.mysql.GEOMETRY.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return this.sqlType;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.UUID"></a>[module sequelize.mysql.UUID](#apidoc.module.sequelize.mysql.UUID)

#### <a name="apidoc.element.sequelize.mysql.UUID.UUID"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.</span>UUID ()](#apidoc.element.sequelize.mysql.UUID.UUID)
- description and source-code
```javascript
UUID = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.UUID.extend"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>extend (oldType)](#apidoc.element.sequelize.mysql.UUID.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.UUID.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>inherits (Constructor)](#apidoc.element.sequelize.mysql.UUID.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.mysql.UUID.super_"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>super_ (options)](#apidoc.element.sequelize.mysql.UUID.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.mysql.UUID.warn"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.UUID.</span>warn (link, text)](#apidoc.element.sequelize.mysql.UUID.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.mysql.UUID.prototype"></a>[module sequelize.mysql.UUID.prototype](#apidoc.module.sequelize.mysql.UUID.prototype)

#### <a name="apidoc.element.sequelize.mysql.UUID.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.mysql.UUID.prototype.</span>toSql ()](#apidoc.element.sequelize.mysql.UUID.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'CHAR(36) BINARY';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres"></a>[module sequelize.postgres](#apidoc.module.sequelize.postgres)

#### <a name="apidoc.element.sequelize.postgres.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>BIGINT ()](#apidoc.element.sequelize.postgres.BIGINT)
- description and source-code
```javascript
BIGINT = function () {
  if (!(this instanceof BIGINT)) return new BIGINT();
  BaseTypes.BIGINT.apply(this, arguments);

  // POSTGRES does not support any parameters for bigint
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support BIGINT with options. Plain 'BIGINT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>BLOB ()](#apidoc.element.sequelize.postgres.BLOB)
- description and source-code
```javascript
BLOB = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>BOOLEAN ()](#apidoc.element.sequelize.postgres.BOOLEAN)
- description and source-code
```javascript
BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>CHAR ()](#apidoc.element.sequelize.postgres.CHAR)
- description and source-code
```javascript
CHAR = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>DATE ()](#apidoc.element.sequelize.postgres.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DECIMAL"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>DECIMAL ()](#apidoc.element.sequelize.postgres.DECIMAL)
- description and source-code
```javascript
DECIMAL = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>FLOAT ()](#apidoc.element.sequelize.postgres.FLOAT)
- description and source-code
```javascript
FLOAT = function () {
  if (!(this instanceof FLOAT)) return new FLOAT();
  BaseTypes.FLOAT.apply(this, arguments);

  // POSTGRES does only support lengths as parameter.
  // Values between 1-24 result in REAL
  // Values between 25-53 result in DOUBLE PRECISION
  // If decimals are provided remove these and print a warning
  if (this._decimals) {
    warn('PostgreSQL does not support FLOAT with decimals. Plain 'FLOAT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._decimals = undefined;
  }
  if (this._unsigned) {
    warn('PostgreSQL does not support FLOAT unsigned. 'UNSIGNED' was removed.');
    this._unsigned = undefined;
  }
  if (this._zerofill) {
    warn('PostgreSQL does not support FLOAT zerofill. 'ZEROFILL' was removed.');
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOGRAPHY ()](#apidoc.element.sequelize.postgres.GEOGRAPHY)
- description and source-code
```javascript
GEOGRAPHY = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOMETRY ()](#apidoc.element.sequelize.postgres.GEOMETRY)
- description and source-code
```javascript
GEOMETRY = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.HSTORE"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>HSTORE ()](#apidoc.element.sequelize.postgres.HSTORE)
- description and source-code
```javascript
HSTORE = function () {
  if (!(this instanceof HSTORE)) return new HSTORE();
  BaseTypes.HSTORE.apply(this, arguments);

  if (!hstore) {
    // All datatype files are loaded at import - make sure we don't load the hstore parser before a hstore is instantiated
    hstore = require('./hstore');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>INTEGER ()](#apidoc.element.sequelize.postgres.INTEGER)
- description and source-code
```javascript
INTEGER = function () {
  if (!(this instanceof INTEGER)) return new INTEGER();
  BaseTypes.INTEGER.apply(this, arguments);

  // POSTGRES does not support any parameters for integer
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support INTEGER with options. Plain 'INTEGER' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.RANGE"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>RANGE ()](#apidoc.element.sequelize.postgres.RANGE)
- description and source-code
```javascript
RANGE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>REAL ()](#apidoc.element.sequelize.postgres.REAL)
- description and source-code
```javascript
REAL = function () {
  if (!(this instanceof REAL)) return new REAL();
  BaseTypes.REAL.apply(this, arguments);

  // POSTGRES does not support any parameters for real
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support REAL with options. Plain 'REAL' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>STRING ()](#apidoc.element.sequelize.postgres.STRING)
- description and source-code
```javascript
STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>TEXT ()](#apidoc.element.sequelize.postgres.TEXT)
- description and source-code
```javascript
TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.BIGINT"></a>[module sequelize.postgres.BIGINT](#apidoc.module.sequelize.postgres.BIGINT)

#### <a name="apidoc.element.sequelize.postgres.BIGINT.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>BIGINT ()](#apidoc.element.sequelize.postgres.BIGINT.BIGINT)
- description and source-code
```javascript
BIGINT = function () {
  if (!(this instanceof BIGINT)) return new BIGINT();
  BaseTypes.BIGINT.apply(this, arguments);

  // POSTGRES does not support any parameters for bigint
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support BIGINT with options. Plain 'BIGINT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BIGINT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>extend (oldType)](#apidoc.element.sequelize.postgres.BIGINT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BIGINT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.BIGINT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.BIGINT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.postgres.BIGINT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BIGINT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.postgres.BIGINT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.BLOB"></a>[module sequelize.postgres.BLOB](#apidoc.module.sequelize.postgres.BLOB)

#### <a name="apidoc.element.sequelize.postgres.BLOB.BLOB"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>BLOB ()](#apidoc.element.sequelize.postgres.BLOB.BLOB)
- description and source-code
```javascript
BLOB = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BLOB.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>extend (oldType)](#apidoc.element.sequelize.postgres.BLOB.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BLOB.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.BLOB.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.BLOB.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>super_ (options)](#apidoc.element.sequelize.postgres.BLOB.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BLOB.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.</span>warn (link, text)](#apidoc.element.sequelize.postgres.BLOB.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.BLOB.prototype"></a>[module sequelize.postgres.BLOB.prototype](#apidoc.module.sequelize.postgres.BLOB.prototype)

#### <a name="apidoc.element.sequelize.postgres.BLOB.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BLOB.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.BLOB.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._length) {
    warn('PostgreSQL does not support BLOB (BYTEA) with options. Plain 'BYTEA' will be used instead.');
    this._length = undefined;
  }
  return 'BYTEA';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.BOOLEAN"></a>[module sequelize.postgres.BOOLEAN](#apidoc.module.sequelize.postgres.BOOLEAN)

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN.BOOLEAN"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>BOOLEAN ()](#apidoc.element.sequelize.postgres.BOOLEAN.BOOLEAN)
- description and source-code
```javascript
BOOLEAN = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>extend (oldType)](#apidoc.element.sequelize.postgres.BOOLEAN.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.BOOLEAN.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>super_ (options)](#apidoc.element.sequelize.postgres.BOOLEAN.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.</span>warn (link, text)](#apidoc.element.sequelize.postgres.BOOLEAN.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.BOOLEAN.prototype"></a>[module sequelize.postgres.BOOLEAN.prototype](#apidoc.module.sequelize.postgres.BOOLEAN.prototype)

#### <a name="apidoc.element.sequelize.postgres.BOOLEAN.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.BOOLEAN.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.BOOLEAN.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'BOOLEAN';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.CHAR"></a>[module sequelize.postgres.CHAR](#apidoc.module.sequelize.postgres.CHAR)

#### <a name="apidoc.element.sequelize.postgres.CHAR.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>CHAR ()](#apidoc.element.sequelize.postgres.CHAR.CHAR)
- description and source-code
```javascript
CHAR = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.CHAR.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>extend (oldType)](#apidoc.element.sequelize.postgres.CHAR.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.CHAR.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.CHAR.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.CHAR.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>super_ (options)](#apidoc.element.sequelize.postgres.CHAR.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.CHAR.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.</span>warn (link, text)](#apidoc.element.sequelize.postgres.CHAR.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.CHAR.prototype"></a>[module sequelize.postgres.CHAR.prototype](#apidoc.module.sequelize.postgres.CHAR.prototype)

#### <a name="apidoc.element.sequelize.postgres.CHAR.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.CHAR.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.CHAR.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._binary) {
    return 'BYTEA';
  }
  return BaseTypes.CHAR.prototype.toSql.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.DATE"></a>[module sequelize.postgres.DATE](#apidoc.module.sequelize.postgres.DATE)

#### <a name="apidoc.element.sequelize.postgres.DATE.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>DATE ()](#apidoc.element.sequelize.postgres.DATE.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DATE.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>extend (oldType)](#apidoc.element.sequelize.postgres.DATE.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DATE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.DATE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.DATE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>super_ (options)](#apidoc.element.sequelize.postgres.DATE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DATE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DATE.</span>warn (link, text)](#apidoc.element.sequelize.postgres.DATE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.DATE.prototype"></a>[module sequelize.postgres.DATE.prototype](#apidoc.module.sequelize.postgres.DATE.prototype)

#### <a name="apidoc.element.sequelize.postgres.DATE.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DATE.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.DATE.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'TIMESTAMP WITH TIME ZONE';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.DECIMAL"></a>[module sequelize.postgres.DECIMAL](#apidoc.module.sequelize.postgres.DECIMAL)

#### <a name="apidoc.element.sequelize.postgres.DECIMAL.DECIMAL"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>DECIMAL ()](#apidoc.element.sequelize.postgres.DECIMAL.DECIMAL)
- description and source-code
```javascript
DECIMAL = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DECIMAL.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>extend (oldType)](#apidoc.element.sequelize.postgres.DECIMAL.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DECIMAL.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.DECIMAL.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.DECIMAL.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>parse (value)](#apidoc.element.sequelize.postgres.DECIMAL.parse)
- description and source-code
```javascript
parse = function (value) {
  return value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DECIMAL.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>super_ (options)](#apidoc.element.sequelize.postgres.DECIMAL.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.DECIMAL.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.DECIMAL.</span>warn (link, text)](#apidoc.element.sequelize.postgres.DECIMAL.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.FLOAT"></a>[module sequelize.postgres.FLOAT](#apidoc.module.sequelize.postgres.FLOAT)

#### <a name="apidoc.element.sequelize.postgres.FLOAT.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>FLOAT ()](#apidoc.element.sequelize.postgres.FLOAT.FLOAT)
- description and source-code
```javascript
FLOAT = function () {
  if (!(this instanceof FLOAT)) return new FLOAT();
  BaseTypes.FLOAT.apply(this, arguments);

  // POSTGRES does only support lengths as parameter.
  // Values between 1-24 result in REAL
  // Values between 25-53 result in DOUBLE PRECISION
  // If decimals are provided remove these and print a warning
  if (this._decimals) {
    warn('PostgreSQL does not support FLOAT with decimals. Plain 'FLOAT' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._decimals = undefined;
  }
  if (this._unsigned) {
    warn('PostgreSQL does not support FLOAT unsigned. 'UNSIGNED' was removed.');
    this._unsigned = undefined;
  }
  if (this._zerofill) {
    warn('PostgreSQL does not support FLOAT zerofill. 'ZEROFILL' was removed.');
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.FLOAT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>extend (oldType)](#apidoc.element.sequelize.postgres.FLOAT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.FLOAT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.FLOAT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.FLOAT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.postgres.FLOAT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.FLOAT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.postgres.FLOAT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.GEOGRAPHY"></a>[module sequelize.postgres.GEOGRAPHY](#apidoc.module.sequelize.postgres.GEOGRAPHY)

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.GEOGRAPHY"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOGRAPHY ()](#apidoc.element.sequelize.postgres.GEOGRAPHY.GEOGRAPHY)
- description and source-code
```javascript
GEOGRAPHY = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>extend (oldType)](#apidoc.element.sequelize.postgres.GEOGRAPHY.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.GEOGRAPHY.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOGRAPHY.parse)
- description and source-code
```javascript
parse = function (value) {
  var b = new Buffer(value, 'hex');
  return wkx.Geometry.parse(b).toGeoJSON();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>super_ (options)](#apidoc.element.sequelize.postgres.GEOGRAPHY.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.</span>warn (link, text)](#apidoc.element.sequelize.postgres.GEOGRAPHY.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.GEOGRAPHY.prototype"></a>[module sequelize.postgres.GEOGRAPHY.prototype](#apidoc.module.sequelize.postgres.GEOGRAPHY.prototype)

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.prototype.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.prototype.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOGRAPHY.prototype.parse)
- description and source-code
```javascript
parse = function (value) {
  var b = new Buffer(value, 'hex');
  return wkx.Geometry.parse(b).toGeoJSON();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOGRAPHY.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOGRAPHY.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.GEOGRAPHY.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  var result = 'GEOGRAPHY';

  if (this.type){
    result += '(' + this.type;

    if (this.srid){
      result += ',' + this.srid;
    }

    result += ')';
  }

  return result;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.GEOMETRY"></a>[module sequelize.postgres.GEOMETRY](#apidoc.module.sequelize.postgres.GEOMETRY)

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.GEOMETRY"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>GEOMETRY ()](#apidoc.element.sequelize.postgres.GEOMETRY.GEOMETRY)
- description and source-code
```javascript
GEOMETRY = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>extend (oldType)](#apidoc.element.sequelize.postgres.GEOMETRY.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.GEOMETRY.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOMETRY.parse)
- description and source-code
```javascript
parse = function (value) {
  var b = new Buffer(value, 'hex');
  return wkx.Geometry.parse(b).toGeoJSON();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>super_ (options)](#apidoc.element.sequelize.postgres.GEOMETRY.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.</span>warn (link, text)](#apidoc.element.sequelize.postgres.GEOMETRY.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.GEOMETRY.prototype"></a>[module sequelize.postgres.GEOMETRY.prototype](#apidoc.module.sequelize.postgres.GEOMETRY.prototype)

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.prototype.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.prototype.</span>parse (value)](#apidoc.element.sequelize.postgres.GEOMETRY.prototype.parse)
- description and source-code
```javascript
parse = function (value) {
  var b = new Buffer(value, 'hex');
  return wkx.Geometry.parse(b).toGeoJSON();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.GEOMETRY.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.GEOMETRY.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.GEOMETRY.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  var result = this.key;

  if (this.type){
    result += '(' + this.type;

    if (this.srid){
      result += ',' + this.srid;
    }

    result += ')';
  }

  return result;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.HSTORE"></a>[module sequelize.postgres.HSTORE](#apidoc.module.sequelize.postgres.HSTORE)

#### <a name="apidoc.element.sequelize.postgres.HSTORE.HSTORE"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>HSTORE ()](#apidoc.element.sequelize.postgres.HSTORE.HSTORE)
- description and source-code
```javascript
HSTORE = function () {
  if (!(this instanceof HSTORE)) return new HSTORE();
  BaseTypes.HSTORE.apply(this, arguments);

  if (!hstore) {
    // All datatype files are loaded at import - make sure we don't load the hstore parser before a hstore is instantiated
    hstore = require('./hstore');
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.HSTORE.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>extend (oldType)](#apidoc.element.sequelize.postgres.HSTORE.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.HSTORE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.HSTORE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.HSTORE.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>parse (value)](#apidoc.element.sequelize.postgres.HSTORE.parse)
- description and source-code
```javascript
parse = function (value) {
  if (!hstore) {
    // All datatype files are loaded at import - make sure we don't load the hstore parser before a hstore is instantiated
    hstore = require('./hstore');
  }
  return hstore.parse(value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.HSTORE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>super_ (options)](#apidoc.element.sequelize.postgres.HSTORE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.HSTORE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.HSTORE.</span>warn (link, text)](#apidoc.element.sequelize.postgres.HSTORE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.HSTORE.prototype"></a>[module sequelize.postgres.HSTORE.prototype](#apidoc.module.sequelize.postgres.HSTORE.prototype)



# <a name="apidoc.module.sequelize.postgres.INTEGER"></a>[module sequelize.postgres.INTEGER](#apidoc.module.sequelize.postgres.INTEGER)

#### <a name="apidoc.element.sequelize.postgres.INTEGER.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>INTEGER ()](#apidoc.element.sequelize.postgres.INTEGER.INTEGER)
- description and source-code
```javascript
INTEGER = function () {
  if (!(this instanceof INTEGER)) return new INTEGER();
  BaseTypes.INTEGER.apply(this, arguments);

  // POSTGRES does not support any parameters for integer
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support INTEGER with options. Plain 'INTEGER' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.INTEGER.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>extend (oldType)](#apidoc.element.sequelize.postgres.INTEGER.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.INTEGER.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.INTEGER.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.INTEGER.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>parse (value)](#apidoc.element.sequelize.postgres.INTEGER.parse)
- description and source-code
```javascript
parse = function (value) {
  return parseInt(value, 10);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.INTEGER.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.postgres.INTEGER.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.INTEGER.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.postgres.INTEGER.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.RANGE"></a>[module sequelize.postgres.RANGE](#apidoc.module.sequelize.postgres.RANGE)

#### <a name="apidoc.element.sequelize.postgres.RANGE.RANGE"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>RANGE ()](#apidoc.element.sequelize.postgres.RANGE.RANGE)
- description and source-code
```javascript
RANGE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.RANGE.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>extend (oldType)](#apidoc.element.sequelize.postgres.RANGE.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.RANGE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.RANGE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.RANGE.parse"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>parse (value, oid, getTypeParser)](#apidoc.element.sequelize.postgres.RANGE.parse)
- description and source-code
```javascript
parse = function (value, oid, getTypeParser) {
  var parser = getTypeParser(RANGE.oid_map[oid]);

  return range.parse(value, parser);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.RANGE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>super_ (options)](#apidoc.element.sequelize.postgres.RANGE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.RANGE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.RANGE.</span>warn (link, text)](#apidoc.element.sequelize.postgres.RANGE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.RANGE.prototype"></a>[module sequelize.postgres.RANGE.prototype](#apidoc.module.sequelize.postgres.RANGE.prototype)



# <a name="apidoc.module.sequelize.postgres.REAL"></a>[module sequelize.postgres.REAL](#apidoc.module.sequelize.postgres.REAL)

#### <a name="apidoc.element.sequelize.postgres.REAL.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>REAL ()](#apidoc.element.sequelize.postgres.REAL.REAL)
- description and source-code
```javascript
REAL = function () {
  if (!(this instanceof REAL)) return new REAL();
  BaseTypes.REAL.apply(this, arguments);

  // POSTGRES does not support any parameters for real
  if (this._length || this.options.length || this._unsigned || this._zerofill) {
    warn('PostgreSQL does not support REAL with options. Plain 'REAL' will be used instead.');
    this._length = undefined;
    this.options.length = undefined;
    this._unsigned = undefined;
    this._zerofill = undefined;
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.REAL.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>extend (oldType)](#apidoc.element.sequelize.postgres.REAL.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.REAL.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.REAL.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.REAL.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>super_ (options)](#apidoc.element.sequelize.postgres.REAL.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.REAL.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.REAL.</span>warn (link, text)](#apidoc.element.sequelize.postgres.REAL.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.STRING"></a>[module sequelize.postgres.STRING](#apidoc.module.sequelize.postgres.STRING)

#### <a name="apidoc.element.sequelize.postgres.STRING.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>STRING ()](#apidoc.element.sequelize.postgres.STRING.STRING)
- description and source-code
```javascript
STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.STRING.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>extend (oldType)](#apidoc.element.sequelize.postgres.STRING.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.STRING.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.STRING.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.STRING.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>super_ (options)](#apidoc.element.sequelize.postgres.STRING.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.STRING.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.STRING.</span>warn (link, text)](#apidoc.element.sequelize.postgres.STRING.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.STRING.prototype"></a>[module sequelize.postgres.STRING.prototype](#apidoc.module.sequelize.postgres.STRING.prototype)

#### <a name="apidoc.element.sequelize.postgres.STRING.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.STRING.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._binary) {
    return 'BYTEA';
  }
  return BaseTypes.STRING.prototype.toSql.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.TEXT"></a>[module sequelize.postgres.TEXT](#apidoc.module.sequelize.postgres.TEXT)

#### <a name="apidoc.element.sequelize.postgres.TEXT.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.</span>TEXT ()](#apidoc.element.sequelize.postgres.TEXT.TEXT)
- description and source-code
```javascript
TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.TEXT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>extend (oldType)](#apidoc.element.sequelize.postgres.TEXT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.TEXT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.postgres.TEXT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.postgres.TEXT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>super_ (options)](#apidoc.element.sequelize.postgres.TEXT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.postgres.TEXT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.postgres.TEXT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.postgres.TEXT.prototype"></a>[module sequelize.postgres.TEXT.prototype](#apidoc.module.sequelize.postgres.TEXT.prototype)

#### <a name="apidoc.element.sequelize.postgres.TEXT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.postgres.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.postgres.TEXT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._length) {
    warn('PostgreSQL does not support TEXT with options. Plain 'TEXT' will be used instead.');
    this._length = undefined;
  }
  return 'TEXT';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.query_interface"></a>[module sequelize.query_interface](#apidoc.module.sequelize.query_interface)

#### <a name="apidoc.element.sequelize.query_interface.query_interface"></a>[function <span class="apidocSignatureSpan">sequelize.</span>query_interface (sequelize)](#apidoc.element.sequelize.query_interface.query_interface)
- description and source-code
```javascript
query_interface = function (sequelize) {
  this.sequelize = sequelize;
  this.QueryGenerator = this.sequelize.dialect.QueryGenerator;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.query_interface.prototype"></a>[module sequelize.query_interface.prototype](#apidoc.module.sequelize.query_interface.prototype)

#### <a name="apidoc.element.sequelize.query_interface.prototype.addColumn"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>addColumn (table, key, attribute, options)](#apidoc.element.sequelize.query_interface.prototype.addColumn)
- description and source-code
```javascript
addColumn = function (table, key, attribute, options) {
  if (!table || !key || !attribute) {
    throw new Error('addColumn takes atleast 3 arguments (table, attribute name, attribute definition)');
  }

  options = options || {};
  attribute = this.sequelize.normalizeAttribute(attribute);
  return this.sequelize.query(this.QueryGenerator.addColumnQuery(table, key, attribute), options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.addIndex"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>addIndex (tableName, attributes, options, rawTablename)](#apidoc.element.sequelize.query_interface.prototype.addIndex)
- description and source-code
```javascript
addIndex = function (tableName, attributes, options, rawTablename) {
  // Support for passing tableName, attributes, options or tableName, options (with a fields param which is the attributes)
  if (!Array.isArray(attributes)) {
    rawTablename = options;
    options = attributes;
    attributes = options.fields;
  }
  // testhint argsConform.end

  if (!rawTablename) {
    // Map for backwards compat
    rawTablename = tableName;
  }

  options = Utils.cloneDeep(options);
  options.fields = attributes;
  var sql = this.QueryGenerator.addIndexQuery(tableName, options, rawTablename);
  return this.sequelize.query(sql, _.assign({}, options, { supportsSearchPath: false }));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.bulkDelete"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>bulkDelete (tableName, identifier, options, model)](#apidoc.element.sequelize.query_interface.prototype.bulkDelete)
- description and source-code
```javascript
bulkDelete = function (tableName, identifier, options, model) {
  options = Utils.cloneDeep(options);
  options = _.defaults(options, {limit: null});
  if (typeof identifier === 'object') identifier = Utils.cloneDeep(identifier);

  var sql = this.QueryGenerator.deleteQuery(tableName, identifier, options, model);
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.bulkInsert"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>bulkInsert (tableName, records, options, attributes)](#apidoc.element.sequelize.query_interface.prototype.bulkInsert)
- description and source-code
```javascript
bulkInsert = function (tableName, records, options, attributes) {
  options = _.clone(options) || {};
  options.type = QueryTypes.INSERT;
  var sql = this.QueryGenerator.bulkInsertQuery(tableName, records, options, attributes);
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.bulkUpdate"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>bulkUpdate (tableName, values, identifier, options, attributes)](#apidoc.element.sequelize.query_interface.prototype.bulkUpdate)
- description and source-code
```javascript
bulkUpdate = function (tableName, values, identifier, options, attributes) {
  options = Utils.cloneDeep(options);
  if (typeof identifier === 'object') identifier = Utils.cloneDeep(identifier);

  var sql = this.QueryGenerator.updateQuery(tableName, values, identifier, options, attributes)
    , table = Utils._.isObject(tableName) ? tableName : { tableName: tableName }
    , model = Utils._.find(this.sequelize.modelManager.models, { tableName: table.tableName });

  options.model = model;
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.changeColumn"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>changeColumn (tableName, attributeName, dataTypeOrOptions, options)](#apidoc.element.sequelize.query_interface.prototype.changeColumn)
- description and source-code
```javascript
changeColumn = function (tableName, attributeName, dataTypeOrOptions, options) {
  var attributes = {};
  options = options || {};

  if (Utils._.values(DataTypes).indexOf(dataTypeOrOptions) > -1) {
    attributes[attributeName] = { type: dataTypeOrOptions, allowNull: true };
  } else {
    attributes[attributeName] = dataTypeOrOptions;
  }

  attributes[attributeName].type = this.sequelize.normalizeDataType(attributes[attributeName].type);

  if (this.sequelize.options.dialect === 'sqlite') {
    // sqlite needs some special treatment as it cannot change a column
    return SQLiteQueryInterface.changeColumn.call(this, tableName, attributes, options);
  } else {
    var query = this.QueryGenerator.attributesToSQL(attributes)
      , sql = this.QueryGenerator.changeColumnQuery(tableName, query);

    return this.sequelize.query(sql, options);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.commitTransaction"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>commitTransaction (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.commitTransaction)
- description and source-code
```javascript
commitTransaction = function (transaction, options) {
  if (!transaction || !(transaction instanceof Transaction)) {
    throw new Error('Unable to commit a transaction without transaction object!');
  }
  if (transaction.parent) {
    // Savepoints cannot be committed
    return Promise.resolve();
  }

  options = _.assign({}, options, {
    transaction: transaction.parent || transaction,
    supportsSearchPath: false
  });

  var sql = this.QueryGenerator.commitTransactionQuery(transaction);
  var promise = this.sequelize.query(sql, options);

  transaction.finished = 'commit';

  return promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.createFunction"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createFunction (functionName, params, returnType, language, body, options)](#apidoc.element.sequelize.query_interface.prototype.createFunction)
- description and source-code
```javascript
createFunction = function (functionName, params, returnType, language, body, options) {
  var sql = this.QueryGenerator.createFunction(functionName, params, returnType, language, body, options);
  options = options || {};

  if (sql) {
    return this.sequelize.query(sql, options);
  } else {
    return Promise.resolve();
  }
}
```
- example usage
```shell
...
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
};

QueryInterface.prototype.createFunction = function(functionName, params, returnType, language, body, options) {
var sql = this.QueryGenerator.createFunction(functionName, params, returnType, language, body, options);
options = options || {};

if (sql) {
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.createSchema"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createSchema (schema, options)](#apidoc.element.sequelize.query_interface.prototype.createSchema)
- description and source-code
```javascript
createSchema = function (schema, options) {
  options = options || {};
  var sql = this.QueryGenerator.createSchema(schema);
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
...
var QueryInterface = function(sequelize) {
this.sequelize = sequelize;
this.QueryGenerator = this.sequelize.dialect.QueryGenerator;
};

QueryInterface.prototype.createSchema = function(schema, options) {
options = options || {};
var sql = this.QueryGenerator.createSchema(schema);
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.dropSchema = function(schema, options) {
options = options || {};
var sql = this.QueryGenerator.dropSchema(schema);
return this.sequelize.query(sql, options);
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.createTable"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createTable (tableName, attributes, options, model)](#apidoc.element.sequelize.query_interface.prototype.createTable)
- description and source-code
```javascript
createTable = function (tableName, attributes, options, model) {
  var keys = Object.keys(attributes)
    , keyLen = keys.length
    , self = this
    , sql = ''
    , i = 0;

  options = _.clone(options) || {};

  attributes = Utils._.mapValues(attributes, function(attribute) {
    if (!Utils._.isPlainObject(attribute)) {
      attribute = { type: attribute, allowNull: true };
    }

    attribute = self.sequelize.normalizeAttribute(attribute);

    return attribute;
  });

  // Postgres requires a special SQL command for enums
  if (self.sequelize.options.dialect === 'postgres') {
    var promises = [];

    for (i = 0; i < keyLen; i++) {
      if (attributes[keys[i]].type instanceof DataTypes.ENUM) {
        sql = self.QueryGenerator.pgListEnums(tableName, attributes[keys[i]].field || keys[i], options);
        promises.push(self.sequelize.query(
          sql,
          _.assign({}, options, { plain: true, raw: true, type: QueryTypes.SELECT })
        ));
      }
    }

    return Promise.all(promises).then(function(results) {
      var promises = []
        , enumIdx = 0;

      for (i = 0; i < keyLen; i++) {
        if (attributes[keys[i]].type instanceof DataTypes.ENUM) {
          // If the enum type doesn't exist then create it
          if (!results[enumIdx]) {
            sql = self.QueryGenerator.pgEnum(tableName, attributes[keys[i]].field || keys[i], attributes[keys[i]], options);
            promises.push(self.sequelize.query(
              sql,
              _.assign({}, options, { raw: true })
            ));
          } else if (!!results[enumIdx] && !!model) {
            var enumVals = self.QueryGenerator.fromArray(results[enumIdx].enum_value)
              , vals = model.rawAttributes[keys[i]].values;

            vals.forEach(function(value, idx) {
              // reset out after/before options since it's for every enum value
              var valueOptions = _.clone(options);
              valueOptions.before = null;
              valueOptions.after = null;

              if (enumVals.indexOf(value) === -1) {
                if (!!vals[idx + 1]) {
                  valueOptions.before = vals[idx + 1];
                }
                else if (!!vals[idx - 1]) {
                  valueOptions.after = vals[idx - 1];
                }
                valueOptions.supportsSearchPath = false;
                promises.push(self.sequelize.query(self.QueryGenerator.pgEnumAdd(tableName, keys[i], value, valueOptions), valueOptions
));
              }
            });
            enumIdx++;
          }
        }
      }

      if (!tableName.schema &&
        (options.schema || (!!model && model.$schema))) {
        tableName = self.QueryGenerator.addSchema({
          tableName: tableName,
          $schema: (!!model && model.$schema) || options.schema
        });
      }

      attributes = self.QueryGenerator.attributesToSQL(attributes, {
        context: 'createTable'
      });
      sql = self.QueryGenerator.createTableQuery(tableName, attributes, options);

      return Promise.all(promises).then(function() {
        return self.sequelize.query(sql, options);
      });
    });
  } else {
    if (!tableName.schema &&
      (options.schema || (!!model && model.$schema))) {
      tableName = self.QueryGenerator.addSchema({
        tableName: tableName,
        $schema: (!!model && model.$schema) || options.schema
      });
    }

    attributes = self.QueryGenerator.attributesToSQL(attributes, {
      context: 'createTable'
    });
    sql = self.QueryGenerator.createTableQuery(tableName, attributes, options);

    return self.sequelize.query(sql, options);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.createTrigger"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>createTrigger (tableName, triggerName, timingType, fireOnArray, functionName, functionParams, optionsArray, options)](#apidoc.element.sequelize.query_interface.prototype.createTrigger)
- description and source-code
```javascript
createTrigger = function (tableName, triggerName, timingType, fireOnArray, functionName, functionParams, optionsArray, options) {
  var sql = this.QueryGenerator.createTrigger(tableName, triggerName, timingType, fireOnArray, functionName, functionParams, optionsArray
);
  options = options || {};
  if (sql) {
    return this.sequelize.query(sql, options);
  } else {
    return Promise.resolve();
  }
}
```
- example usage
```shell
...
    }

    return result;
  });
};

QueryInterface.prototype.createTrigger = function(tableName, triggerName, timingType, fireOnArray, functionName, functionParams,
optionsArray, options) {
  var sql = this.QueryGenerator.createTrigger(tableName, triggerName, timingType, fireOnArray, functionName, functionParams, optionsArray
);
  options = options || {};
  if (sql) {
    return this.sequelize.query(sql, options);
  } else {
    return Promise.resolve();
  }
};
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.databaseVersion"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>databaseVersion (options)](#apidoc.element.sequelize.query_interface.prototype.databaseVersion)
- description and source-code
```javascript
databaseVersion = function (options) {
  return this.sequelize.query(
    this.QueryGenerator.versionQuery(),
    _.assign({}, options, { type: QueryTypes.VERSION })
  );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.deferConstraints"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>deferConstraints (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.deferConstraints)
- description and source-code
```javascript
deferConstraints = function (transaction, options) {
  options = _.assign({}, options, {
    transaction: transaction.parent || transaction
  });

  var sql = this.QueryGenerator.deferConstraintsQuery(options);

  if (sql) {
    return this.sequelize.query(sql, options);
  }

  return Promise.resolve();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.delete"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>delete (instance, tableName, identifier, options)](#apidoc.element.sequelize.query_interface.prototype.delete)
- description and source-code
```javascript
delete = function (instance, tableName, identifier, options) {
  var self = this
    , cascades = []
    , sql = self.QueryGenerator.deleteQuery(tableName, identifier, null, instance.Model);

  options = _.clone(options) || {};

  // Check for a restrict field
  if (!!instance.Model && !!instance.Model.associations) {
    var keys = Object.keys(instance.Model.associations)
      , length = keys.length
      , association;

    for (var i = 0; i < length; i++) {
      association = instance.Model.associations[keys[i]];
      if (association.options && association.options.onDelete &&
        association.options.onDelete.toLowerCase() === 'cascade' &&
        association.options.useHooks === true) {
        cascades.push(association.accessors.get);
      }
    }
  }

  return Promise.each(cascades, function (cascade) {
    return instance[cascade](options).then(function (instances) {
      // Check for hasOne relationship with non-existing associate ("has zero")
      if (!instances) {
        return Promise.resolve();
      }

      if (!Array.isArray(instances)) instances = [instances];

      return Promise.each(instances, function (instance) {
        return instance.destroy(options);
      });
    });
  }).then(function () {
    options.instance = instance;
    return self.sequelize.query(sql, options);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.describeTable"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>describeTable (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.describeTable)
- description and source-code
```javascript
describeTable = function (tableName, options) {
  var schema = null
    , schemaDelimiter = null;

  if (typeof options === 'string') {
    schema = options;
  } else if (typeof options === 'object' && options !== null) {
    schema = options.schema || null;
    schemaDelimiter = options.schemaDelimiter || null;
  }

  if (typeof tableName === 'object' && tableName !== null) {
    schema = tableName.schema;
    tableName = tableName.tableName;
  }

  var sql = this.QueryGenerator.describeTableQuery(tableName, schema, schemaDelimiter);

  return this.sequelize.query(
    sql,
    _.assign({}, options, { type: QueryTypes.DESCRIBE })
  ).then(function(data) {
    // If no data is returned from the query, then the table name may be wrong.
    // Query generators that use information_schema for retrieving table info will just return an empty result set,
    // it will not throw an error like built-ins do (e.g. DESCRIBE on MySql).
    if (Utils._.isEmpty(data)) {
      return Promise.reject('No description found for "' + tableName + '" table. Check the table name and schema; remember, they
 _are_ case sensitive.');
    } else {
      return Promise.resolve(data);
    }
  });
}
```
- example usage
```shell
...

return this.sequelize.query(sql, options);
  }
};

QueryInterface.prototype.renameColumn = function(tableName, attrNameBefore, attrNameAfter, options) {
  options = options || {};
  return this.describeTable(tableName, options).then(function(data) {
data = data[attrNameBefore] || {};

var _options = {};

_options[attrNameAfter] = {
  attribute: attrNameAfter,
  type: data.type,
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.dropAllEnums"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropAllEnums (options)](#apidoc.element.sequelize.query_interface.prototype.dropAllEnums)
- description and source-code
```javascript
dropAllEnums = function (options) {
  if (this.sequelize.getDialect() !== 'postgres') {
    return Promise.resolve();
  }

  options = options || {};

  var self = this;

  return this.pgListEnums(null, options).map(function(result) {
    return self.sequelize.query(
      self.QueryGenerator.pgEnumDrop(null, null, self.QueryGenerator.pgEscapeAndQuote(result.enum_name)),
      _.assign({}, options, { raw: true })
    );
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.dropAllSchemas"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropAllSchemas (options)](#apidoc.element.sequelize.query_interface.prototype.dropAllSchemas)
- description and source-code
```javascript
dropAllSchemas = function (options) {
  options = options || {};

  var self = this;
  if (!this.QueryGenerator._dialect.supports.schemas) {
    return this.sequelize.drop(options);
  } else {
    return this.showAllSchemas(options).map(function(schemaName) {
      return self.dropSchema(schemaName, options);
    });
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.dropAllTables"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropAllTables (options)](#apidoc.element.sequelize.query_interface.prototype.dropAllTables)
- description and source-code
```javascript
dropAllTables = function (options) {
  var self = this
    , skip;

  options = options || {};
  skip = options.skip || [];

  var dropAllTables = function(tableNames) {
    return Promise.each(tableNames, function(tableName) {
      // if tableName is not in the Array of tables names then dont drop it
      if (skip.indexOf(tableName.tableName || tableName) === -1) {
        return self.dropTable(tableName, _.assign({}, options, { cascade: true }) );
      }
    });
  };

  return self.showAllTables(options).then(function(tableNames) {
    if (self.sequelize.options.dialect === 'sqlite') {
      return self.sequelize.query('PRAGMA foreign_keys;', options).then(function(result) {
        var foreignKeysAreEnabled = result.foreign_keys === 1;

        if (foreignKeysAreEnabled) {
          return self.sequelize.query('PRAGMA foreign_keys = OFF', options).then(function() {
            return dropAllTables(tableNames).then(function() {
              return self.sequelize.query('PRAGMA foreign_keys = ON', options);
            });
          });
        } else {
          return dropAllTables(tableNames);
        }
      });
    } else {
      return self.getForeignKeysForTables(tableNames, options).then(function(foreignKeys) {
        var promises = [];

        tableNames.forEach(function(tableName) {
          var normalizedTableName = tableName;
          if (Utils._.isObject(tableName)) {
             normalizedTableName = tableName.schema + '.' + tableName.tableName;
          }

          foreignKeys[normalizedTableName].forEach(function(foreignKey) {
            var sql = self.QueryGenerator.dropForeignKeyQuery(tableName, foreignKey);
            promises.push(self.sequelize.query(sql, options));
          });
        });

        return Promise.all(promises).then(function() {
          return dropAllTables(tableNames);
        });
      });
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.dropFunction"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropFunction (functionName, params, options)](#apidoc.element.sequelize.query_interface.prototype.dropFunction)
- description and source-code
```javascript
dropFunction = function (functionName, params, options) {
  var sql = this.QueryGenerator.dropFunction(functionName, params);
  options = options || {};

  if (sql) {
    return this.sequelize.query(sql, options);
  } else {
    return Promise.resolve();
  }
}
```
- example usage
```shell
...
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
};

QueryInterface.prototype.dropFunction = function(functionName, params, options) {
var sql = this.QueryGenerator.dropFunction(functionName, params);
options = options || {};

if (sql) {
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.dropSchema"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropSchema (schema, options)](#apidoc.element.sequelize.query_interface.prototype.dropSchema)
- description and source-code
```javascript
dropSchema = function (schema, options) {
  options = options || {};
  var sql = this.QueryGenerator.dropSchema(schema);
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
...
options = options || {};
var sql = this.QueryGenerator.createSchema(schema);
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.dropSchema = function(schema, options) {
options = options || {};
var sql = this.QueryGenerator.dropSchema(schema);
return this.sequelize.query(sql, options);
};

QueryInterface.prototype.dropAllSchemas = function(options) {
options = options || {};

var self = this;
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.dropTable"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropTable (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.dropTable)
- description and source-code
```javascript
dropTable = function (tableName, options) {
  // if we're forcing we should be cascading unless explicitly stated otherwise
  options = _.clone(options) || {};
  options.cascade = options.cascade || options.force || false;

  var sql = this.QueryGenerator.dropTableQuery(tableName, options)
    , self = this;

  return this.sequelize.query(sql, options).then(function() {
    var promises = [];

    // Since postgres has a special case for enums, we should drop the related
    // enum type within the table and attribute
    if (self.sequelize.options.dialect === 'postgres') {
      var instanceTable = self.sequelize.modelManager.getModel(tableName, { attribute: 'tableName' });

      if (!!instanceTable) {
        var getTableName = (!options || !options.schema || options.schema === 'public' ? '' : options.schema + '_') + tableName;

        var keys = Object.keys(instanceTable.rawAttributes)
          , keyLen = keys.length
          , i = 0;

        for (i = 0; i < keyLen; i++) {
          if (instanceTable.rawAttributes[keys[i]].type instanceof DataTypes.ENUM) {
            sql = self.QueryGenerator.pgEnumDrop(getTableName, keys[i]);
            options.supportsSearchPath = false;
            promises.push(self.sequelize.query(sql, _.assign({}, options, { raw: true })));
          }
        }
      }
    }

    return Promise.all(promises).get(0);
  });
}
```
- example usage
```shell
...
options = options || {};
skip = options.skip || [];

var dropAllTables = function(tableNames) {
  return Promise.each(tableNames, function(tableName) {
    // if tableName is not in the Array of tables names then dont drop it
    if (skip.indexOf(tableName.tableName || tableName) === -1) {
      return self.dropTable(tableName, _.assign({}, options, { cascade: true }) );
    }
  });
};

return self.showAllTables(options).then(function(tableNames) {
  if (self.sequelize.options.dialect === 'sqlite') {
    return self.sequelize.query('PRAGMA foreign_keys;', options).then(function(result) {
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.dropTrigger"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>dropTrigger (tableName, triggerName, options)](#apidoc.element.sequelize.query_interface.prototype.dropTrigger)
- description and source-code
```javascript
dropTrigger = function (tableName, triggerName, options) {
  var sql = this.QueryGenerator.dropTrigger(tableName, triggerName);
  options = options || {};

  if (sql) {
    return this.sequelize.query(sql, options);
  } else {
    return Promise.resolve();
  }
}
```
- example usage
```shell
...
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
};

QueryInterface.prototype.dropTrigger = function(tableName, triggerName, options) {
var sql = this.QueryGenerator.dropTrigger(tableName, triggerName);
options = options || {};

if (sql) {
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.escape"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>escape (value)](#apidoc.element.sequelize.query_interface.prototype.escape)
- description and source-code
```javascript
escape = function (value) {
  return this.QueryGenerator.escape(value);
}
```
- example usage
```shell
...
return this.QueryGenerator.quoteIdentifiers(identifiers, force);
};

/**
 * Escape a value (e.g. a string, number or date)
 */
QueryInterface.prototype.escape = function(value) {
return this.QueryGenerator.escape(value);
};

QueryInterface.prototype.setAutocommit = function(transaction, value, options) {
if (!transaction || !(transaction instanceof Transaction)) {
  throw new Error('Unable to set autocommit for a transaction without transaction object!');
}
if (transaction.parent) {
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.getForeignKeysForTables"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>getForeignKeysForTables (tableNames, options)](#apidoc.element.sequelize.query_interface.prototype.getForeignKeysForTables)
- description and source-code
```javascript
getForeignKeysForTables = function (tableNames, options) {
  var self = this;
  options = options || {};

  if (tableNames.length === 0) {
    return Promise.resolve({});
  }

  return Promise.map(tableNames, function(tableName) {
    return self.sequelize.query(self.QueryGenerator.getForeignKeysQuery(tableName, self.sequelize.config.database), options).get
(0);
  }).then(function(results) {
    var result = {};

    tableNames.forEach(function(tableName, i) {
      if (Utils._.isObject(tableName)) {
        tableName = tableName.schema + '.' + tableName.tableName;
      }

      result[tableName] = Utils._.compact(results[i]).map(function(r) {
        return r.constraint_name;
      });
    });

    return result;
  });
}
```
- example usage
```shell
...
    });
  });
} else {
  return dropAllTables(tableNames);
}
      });
    } else {
      return self.getForeignKeysForTables(tableNames, options).then(function(foreignKeys) {
var promises = [];

tableNames.forEach(function(tableName) {
  var normalizedTableName = tableName;
  if (Utils._.isObject(tableName)) {
     normalizedTableName = tableName.schema + '.' + tableName.tableName;
  }
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.increment"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>increment (instance, tableName, values, identifier, options)](#apidoc.element.sequelize.query_interface.prototype.increment)
- description and source-code
```javascript
increment = function (instance, tableName, values, identifier, options) {
  var sql = this.QueryGenerator.incrementQuery(tableName, values, identifier, options.attributes);

  options = _.clone(options) || {};

  options.type = QueryTypes.UPDATE;
  options.instance = instance;
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.insert"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>insert (instance, tableName, values, options)](#apidoc.element.sequelize.query_interface.prototype.insert)
- description and source-code
```javascript
insert = function (instance, tableName, values, options) {
  options = Utils.cloneDeep(options);
  options.hasTrigger = instance && instance.Model.options.hasTrigger;
  var sql = this.QueryGenerator.insertQuery(tableName, values, instance && instance.Model.rawAttributes, options);

  options.type = QueryTypes.INSERT;
  options.instance = instance;

  return this.sequelize.query(sql, options).then(function(result) {
    if (instance) result.isNewRecord = false;
    return result;
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.nameIndexes"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>nameIndexes (indexes, rawTablename)](#apidoc.element.sequelize.query_interface.prototype.nameIndexes)
- description and source-code
```javascript
nameIndexes = function (indexes, rawTablename) {
  return this.QueryGenerator.nameIndexes(indexes, rawTablename);
}
```
- example usage
```shell
...

QueryInterface.prototype.showIndex = function(tableName, options) {
var sql = this.QueryGenerator.showIndexesQuery(tableName, options);
return this.sequelize.query(sql, _.assign({}, options, { type: QueryTypes.SHOWINDEXES }));
};

QueryInterface.prototype.nameIndexes = function(indexes, rawTablename) {
return this.QueryGenerator.nameIndexes(indexes, rawTablename);
};

QueryInterface.prototype.getForeignKeysForTables = function(tableNames, options) {
var self = this;
options = options || {};

if (tableNames.length === 0) {
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.pgListEnums"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>pgListEnums (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.pgListEnums)
- description and source-code
```javascript
pgListEnums = function (tableName, options) {
  options = options || {};
  var sql = this.QueryGenerator.pgListEnums(tableName);
  return this.sequelize.query(sql, _.assign({}, options, { plain: false, raw: true, type: QueryTypes.SELECT }));
}
```
- example usage
```shell
...

  // Postgres requires a special SQL command for enums
  if (self.sequelize.options.dialect === 'postgres') {
var promises = [];

for (i = 0; i < keyLen; i++) {
  if (attributes[keys[i]].type instanceof DataTypes.ENUM) {
    sql = self.QueryGenerator.pgListEnums(tableName, attributes[keys[i]].field || keys[i], options);
    promises.push(self.sequelize.query(
      sql,
      _.assign({}, options, { plain: true, raw: true, type: QueryTypes.SELECT })
    ));
  }
}
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.quoteIdentifier"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>quoteIdentifier (identifier, force)](#apidoc.element.sequelize.query_interface.prototype.quoteIdentifier)
- description and source-code
```javascript
quoteIdentifier = function (identifier, force) {
  return this.QueryGenerator.quoteIdentifier(identifier, force);
}
```
- example usage
```shell
...

/**
 * Escape an identifier (e.g. a table or attribute name). If force is true,
 * the identifier will be quoted even if the 'quoteIdentifiers' option is
 * false.
 */
QueryInterface.prototype.quoteIdentifier = function(identifier, force) {
  return this.QueryGenerator.quoteIdentifier(identifier, force);
};

QueryInterface.prototype.quoteTable = function(identifier) {
  return this.QueryGenerator.quoteTable(identifier);
};

/**
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.quoteIdentifiers"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>quoteIdentifiers (identifiers, force)](#apidoc.element.sequelize.query_interface.prototype.quoteIdentifiers)
- description and source-code
```javascript
quoteIdentifiers = function (identifiers, force) {
  return this.QueryGenerator.quoteIdentifiers(identifiers, force);
}
```
- example usage
```shell
...

/**
 * Split an identifier into .-separated tokens and quote each part.
 * If force is true, the identifier will be quoted even if the
 * 'quoteIdentifiers' option is false.
 */
QueryInterface.prototype.quoteIdentifiers = function(identifiers, force) {
return this.QueryGenerator.quoteIdentifiers(identifiers, force);
};

/**
 * Escape a value (e.g. a string, number or date)
 */
QueryInterface.prototype.escape = function(value) {
return this.QueryGenerator.escape(value);
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.quoteTable"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>quoteTable (identifier)](#apidoc.element.sequelize.query_interface.prototype.quoteTable)
- description and source-code
```javascript
quoteTable = function (identifier) {
  return this.QueryGenerator.quoteTable(identifier);
}
```
- example usage
```shell
...
* false.
*/
QueryInterface.prototype.quoteIdentifier = function(identifier, force) {
 return this.QueryGenerator.quoteIdentifier(identifier, force);
};

QueryInterface.prototype.quoteTable = function(identifier) {
 return this.QueryGenerator.quoteTable(identifier);
};

/**
* Split an identifier into .-separated tokens and quote each part.
* If force is true, the identifier will be quoted even if the
* 'quoteIdentifiers' option is false.
*/
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.rawSelect"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>rawSelect (tableName, options, attributeSelector, Model)](#apidoc.element.sequelize.query_interface.prototype.rawSelect)
- description and source-code
```javascript
rawSelect = function (tableName, options, attributeSelector, Model) {
  if (options.schema) {
    tableName = this.QueryGenerator.addSchema({
      tableName: tableName,
      $schema: options.schema
    });
  }

  options = Utils.cloneDeep(options);
  options = _.defaults(options, {
    raw: true,
    plain: true,
    type: QueryTypes.SELECT
  });

  var sql = this.QueryGenerator.selectQuery(tableName, options, Model);

  if (attributeSelector === undefined) {
    throw new Error('Please pass an attribute selector!');
  }

  return this.sequelize.query(sql, options).then(function(data) {
    if (!options.plain) {
      return data;
    }

    var result = data ? data[attributeSelector] : null;

    if (options && options.dataType) {
      var dataType = options.dataType;

      if (dataType instanceof DataTypes.DECIMAL || dataType instanceof DataTypes.FLOAT) {
        result = parseFloat(result);
      } else if (dataType instanceof DataTypes.INTEGER || dataType instanceof DataTypes.BIGINT) {
        result = parseInt(result, 10);
      } else if (dataType instanceof DataTypes.DATE) {
        if (!Utils._.isNull(result) && !Utils._.isDate(result)) {
          result = new Date(result);
        }
      } else if (dataType instanceof DataTypes.STRING) {
        // Nothing to do, result is already a string.
      }
    }

    return result;
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.removeColumn"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>removeColumn (tableName, attributeName, options)](#apidoc.element.sequelize.query_interface.prototype.removeColumn)
- description and source-code
```javascript
removeColumn = function (tableName, attributeName, options) {
  options = options || {};
  switch (this.sequelize.options.dialect) {
    case 'sqlite':
      // sqlite needs some special treatment as it cannot drop a column
      return SQLiteQueryInterface.removeColumn.call(this, tableName, attributeName, options);
    case 'mssql':
      // mssql needs special treatment as it cannot drop a column with a default or foreign key constraint
      return MSSSQLQueryInterface.removeColumn.call(this, tableName, attributeName, options);
    case 'mysql':
    case 'mariadb':
      // mysql/maria needs special treatment as it cannot drop a column with a foreign key constraint
      return MySQLQueryInterface.removeColumn.call(this, tableName, attributeName, options);
    default:
        var sql = this.QueryGenerator.removeColumnQuery(tableName, attributeName);
        return this.sequelize.query(sql, options);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.removeIndex"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>removeIndex (tableName, indexNameOrAttributes, options)](#apidoc.element.sequelize.query_interface.prototype.removeIndex)
- description and source-code
```javascript
removeIndex = function (tableName, indexNameOrAttributes, options) {
  options = options || {};
  var sql = this.QueryGenerator.removeIndexQuery(tableName, indexNameOrAttributes);
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.renameColumn"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameColumn (tableName, attrNameBefore, attrNameAfter, options)](#apidoc.element.sequelize.query_interface.prototype.renameColumn)
- description and source-code
```javascript
renameColumn = function (tableName, attrNameBefore, attrNameAfter, options) {
  options = options || {};
  return this.describeTable(tableName, options).then(function(data) {
    data = data[attrNameBefore] || {};

    var _options = {};

    _options[attrNameAfter] = {
      attribute: attrNameAfter,
      type: data.type,
      allowNull: data.allowNull,
      defaultValue: data.defaultValue
    };

    // fix: a not-null column cannot have null as default value
    if (data.defaultValue === null && !data.allowNull) {
      delete _options[attrNameAfter].defaultValue;
    }

    if (this.sequelize.options.dialect === 'sqlite') {
      // sqlite needs some special treatment as it cannot rename a column
      return SQLiteQueryInterface.renameColumn.call(this, tableName, attrNameBefore, attrNameAfter, options);
    } else {
      var sql = this.QueryGenerator.renameColumnQuery(
        tableName,
        attrNameBefore,
        this.QueryGenerator.attributesToSQL(_options)
      );
      return this.sequelize.query(sql, options);
    }
  }.bind(this));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.renameFunction"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameFunction (oldFunctionName, params, newFunctionName, options)](#apidoc.element.sequelize.query_interface.prototype.renameFunction)
- description and source-code
```javascript
renameFunction = function (oldFunctionName, params, newFunctionName, options) {
  var sql = this.QueryGenerator.renameFunction(oldFunctionName, params, newFunctionName);
  options = options || {};

  if (sql) {
    return this.sequelize.query(sql, options);
  } else {
    return Promise.resolve();
  }
}
```
- example usage
```shell
...
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
};

QueryInterface.prototype.renameFunction = function(oldFunctionName, params, newFunctionName, options) {
var sql = this.QueryGenerator.renameFunction(oldFunctionName, params, newFunctionName);
options = options || {};

if (sql) {
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.renameTable"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameTable (before, after, options)](#apidoc.element.sequelize.query_interface.prototype.renameTable)
- description and source-code
```javascript
renameTable = function (before, after, options) {
  options = options || {};
  var sql = this.QueryGenerator.renameTableQuery(before, after);
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.renameTrigger"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>renameTrigger (tableName, oldTriggerName, newTriggerName, options)](#apidoc.element.sequelize.query_interface.prototype.renameTrigger)
- description and source-code
```javascript
renameTrigger = function (tableName, oldTriggerName, newTriggerName, options) {
  var sql = this.QueryGenerator.renameTrigger(tableName, oldTriggerName, newTriggerName);
  options = options || {};

  if (sql) {
    return this.sequelize.query(sql, options);
  } else {
    return Promise.resolve();
  }
}
```
- example usage
```shell
...
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
};

QueryInterface.prototype.renameTrigger = function(tableName, oldTriggerName, newTriggerName, options) {
var sql = this.QueryGenerator.renameTrigger(tableName, oldTriggerName, newTriggerName);
options = options || {};

if (sql) {
  return this.sequelize.query(sql, options);
} else {
  return Promise.resolve();
}
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.rollbackTransaction"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>rollbackTransaction (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.rollbackTransaction)
- description and source-code
```javascript
rollbackTransaction = function (transaction, options) {
  if (!transaction || !(transaction instanceof Transaction)) {
    throw new Error('Unable to rollback a transaction without transaction object!');
  }

  options = _.assign({}, options, {
    transaction: transaction.parent || transaction,
    supportsSearchPath: false
  });
  options.transaction.name = transaction.parent ? transaction.name : undefined;
  var sql = this.QueryGenerator.rollbackTransactionQuery(transaction);
  var promise = this.sequelize.query(sql, options);

  transaction.finished = 'rollback';

  return promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.select"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>select (model, tableName, options)](#apidoc.element.sequelize.query_interface.prototype.select)
- description and source-code
```javascript
select = function (model, tableName, options) {
  options = Utils.cloneDeep(options);
  options.type = QueryTypes.SELECT;
  options.model = model;

  return this.sequelize.query(
    this.QueryGenerator.selectQuery(tableName, options, model),
    options
  );
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.setAutocommit"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>setAutocommit (transaction, value, options)](#apidoc.element.sequelize.query_interface.prototype.setAutocommit)
- description and source-code
```javascript
setAutocommit = function (transaction, value, options) {
  if (!transaction || !(transaction instanceof Transaction)) {
    throw new Error('Unable to set autocommit for a transaction without transaction object!');
  }
  if (transaction.parent) {
    // Not possible to set a seperate isolation level for savepoints
    return Promise.resolve();
  }

  options = _.assign({}, options, {
    transaction: transaction.parent || transaction
  });

  var sql = this.QueryGenerator.setAutocommitQuery(value, {
    parent: transaction.parent
  });

  if (!sql) return Promise.resolve();

  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.setIsolationLevel"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>setIsolationLevel (transaction, value, options)](#apidoc.element.sequelize.query_interface.prototype.setIsolationLevel)
- description and source-code
```javascript
setIsolationLevel = function (transaction, value, options) {
  if (!transaction || !(transaction instanceof Transaction)) {
    throw new Error('Unable to set isolation level for a transaction without transaction object!');
  }
  if (transaction.parent) {
    // Not possible to set a seperate isolation level for savepoints
    return Promise.resolve();
  }

  options = _.assign({}, options, {
    transaction: transaction.parent || transaction
  });

  var sql = this.QueryGenerator.setIsolationLevelQuery(value, {
    parent: transaction.parent
  });

  if (!sql) return Promise.resolve();

  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.showAllSchemas"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>showAllSchemas (options)](#apidoc.element.sequelize.query_interface.prototype.showAllSchemas)
- description and source-code
```javascript
showAllSchemas = function (options) {
  var self = this;

  options = _.assign({}, options, {
    raw: true,
    type: this.sequelize.QueryTypes.SELECT
  });

  var showSchemasSql = self.QueryGenerator.showSchemasQuery();

  return this.sequelize.query(showSchemasSql, options).then(function(schemaNames) {
    return Utils._.flatten(
      Utils._.map(schemaNames, function(value) {
        return (!!value.schema_name ? value.schema_name : value);
      })
    );
  });
}
```
- example usage
```shell
...
QueryInterface.prototype.dropAllSchemas = function(options) {
options = options || {};

var self = this;
if (!this.QueryGenerator._dialect.supports.schemas) {
  return this.sequelize.drop(options);
} else {
  return this.showAllSchemas(options).map(function(schemaName) {
    return self.dropSchema(schemaName, options);
  });
}
};

QueryInterface.prototype.showAllSchemas = function(options) {
var self = this;
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.showAllTables"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>showAllTables (options)](#apidoc.element.sequelize.query_interface.prototype.showAllTables)
- description and source-code
```javascript
showAllTables = function (options) {
  var self = this;
  options = _.assign({}, options, {
    raw: true,
    type: QueryTypes.SHOWTABLES
  });

  var showTablesSql = self.QueryGenerator.showTablesQuery();
  return self.sequelize.query(showTablesSql, options).then(function(tableNames) {
    return Utils._.flatten(tableNames);
  });
}
```
- example usage
```shell
...
      // if tableName is not in the Array of tables names then dont drop it
      if (skip.indexOf(tableName.tableName || tableName) === -1) {
return self.dropTable(tableName, _.assign({}, options, { cascade: true }) );
      }
    });
  };

  return self.showAllTables(options).then(function(tableNames) {
    if (self.sequelize.options.dialect === 'sqlite') {
      return self.sequelize.query('PRAGMA foreign_keys;', options).then(function(result) {
var foreignKeysAreEnabled = result.foreign_keys === 1;

if (foreignKeysAreEnabled) {
  return self.sequelize.query('PRAGMA foreign_keys = OFF', options).then(function() {
    return dropAllTables(tableNames).then(function() {
...
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.showIndex"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>showIndex (tableName, options)](#apidoc.element.sequelize.query_interface.prototype.showIndex)
- description and source-code
```javascript
showIndex = function (tableName, options) {
  var sql = this.QueryGenerator.showIndexesQuery(tableName, options);
  return this.sequelize.query(sql, _.assign({}, options, { type: QueryTypes.SHOWINDEXES }));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.startTransaction"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>startTransaction (transaction, options)](#apidoc.element.sequelize.query_interface.prototype.startTransaction)
- description and source-code
```javascript
startTransaction = function (transaction, options) {
  if (!transaction || !(transaction instanceof Transaction)) {
    throw new Error('Unable to start a transaction without transaction object!');
  }

  options = _.assign({}, options, {
    transaction: transaction.parent || transaction
  });
  options.transaction.name = transaction.parent ? transaction.name : undefined;
  var sql = this.QueryGenerator.startTransactionQuery(transaction);

  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.update"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>update (instance, tableName, values, identifier, options)](#apidoc.element.sequelize.query_interface.prototype.update)
- description and source-code
```javascript
update = function (instance, tableName, values, identifier, options) {
  options = _.clone(options || {});
  options.hasTrigger = !!(instance && instance.$modelOptions && instance.$modelOptions.hasTrigger);


  var self = this
    , restrict = false
    , sql = self.QueryGenerator.updateQuery(tableName, values, identifier, options, instance.Model.rawAttributes);


  options.type = QueryTypes.UPDATE;

  // Check for a restrict field
  if (!!instance.Model && !!instance.Model.associations) {
    var keys = Object.keys(instance.Model.associations)
      , length = keys.length;

    for (var i = 0; i < length; i++) {
      if (instance.Model.associations[keys[i]].options && instance.Model.associations[keys[i]].options.onUpdate && instance.Model
.associations[keys[i]].options.onUpdate === 'restrict') {
        restrict = true;
      }
    }
  }

  options.instance = instance;
  return this.sequelize.query(sql, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.query_interface.prototype.upsert"></a>[function <span class="apidocSignatureSpan">sequelize.query_interface.prototype.</span>upsert (tableName, valuesByField, updateValues, where, model, options)](#apidoc.element.sequelize.query_interface.prototype.upsert)
- description and source-code
```javascript
upsert = function (tableName, valuesByField, updateValues, where, model, options) {
  var wheres = []
    , indexFields
    , indexes = []
    , attributes = Object.keys(valuesByField);

  options = _.clone(options);

  if (!Utils._.isEmpty(where)) {
    wheres.push(where);
  }

  // Lets combine uniquekeys and indexes into one
  indexes = Utils._.map(model.options.uniqueKeys, function (value) {
    return value.fields;
  });

  Utils._.each(model.options.indexes, function (value) {
    if (value.unique === true) {
      // fields in the index may both the strings or objects with an attribute property - lets sanitize that
      indexFields = Utils._.map(value.fields, function (field) {
        if (Utils._.isPlainObject(field)) {
          return field.attribute;
        }
        return field;
      });
      indexes.push(indexFields);
    }
  });

  indexes.forEach(function (index) {
    if (Utils._.intersection(attributes, index).length === index.length) {
      where = {};
      index.forEach(function (field) {
        where[field] = valuesByField[field];
      });
      wheres.push(where);
    }
  });

  where = { $or: wheres };

  options.type = QueryTypes.UPSERT;
  options.raw = true;

  var sql = this.QueryGenerator.upsertQuery(tableName, valuesByField, updateValues, where, model.rawAttributes, options);
  return this.sequelize.query(sql, options).then(function (rowCount) {
    if (rowCount === undefined) {
      return rowCount;
    }

    // MySQL returns 1 for inserted, 2 for updated http://dev.mysql.com/doc/refman/5.0/en/insert-on-duplicate.html. Postgres has
 been modded to do the same

    return rowCount === 1;
  });
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sql_string"></a>[module sequelize.sql_string](#apidoc.module.sequelize.sql_string)

#### <a name="apidoc.element.sequelize.sql_string.escape"></a>[function <span class="apidocSignatureSpan">sequelize.sql_string.</span>escape (val, timeZone, dialect, format)](#apidoc.element.sequelize.sql_string.escape)
- description and source-code
```javascript
escape = function (val, timeZone, dialect, format) {
  var prependN = false;
  if (val === undefined || val === null) {
    return 'NULL';
  }
  switch (typeof val) {
  case 'boolean':
    // SQLite doesn't have true/false support. MySQL aliases true/false to 1/0
    // for us. Postgres actually has a boolean type with true/false literals,
    // but sequelize doesn't use it yet.
    if (dialect === 'sqlite' || dialect === 'mssql') {
      return +!!val;
    }
    return '' + !!val;
  case 'number':
    return val + '';
  case 'string':
    // In mssql, prepend N to all quoted vals which are originally a string (for
    // unicode compatibility)
    prependN = dialect === 'mssql';
    break;
  }

  if (val instanceof Date) {
    val = dataTypes[dialect].DATE.prototype.stringify(val, { timezone: timeZone });
  }

  if (Buffer.isBuffer(val)) {
    if (dataTypes[dialect].BLOB) {
      return dataTypes[dialect].BLOB.prototype.stringify(val);
    }

    return dataTypes.BLOB.prototype.stringify(val);
  }

  if (Array.isArray(val)) {
    var escape = _.partial(SqlString.escape, _, timeZone, dialect, format);
    if (dialect === 'postgres' && !format) {
      return dataTypes.ARRAY.prototype.stringify(val, {escape: escape});
    }
    return val.map(escape);
  }

  if (dialect === 'postgres' || dialect === 'sqlite' || dialect === 'mssql') {
    // http://www.postgresql.org/docs/8.2/static/sql-syntax-lexical.html#SQL-SYNTAX-STRINGS
    // http://stackoverflow.com/q/603572/130598
    val = val.replace(/'/g, "''");
  } else {
    val = val.replace(/[\0\n\r\b\t\\\'\"\x1a]/g, function(s) {
      switch (s) {
        case '\0': return '\\0';
        case '\n': return '\\n';
        case '\r': return '\\r';
        case '\b': return '\\b';
        case '\t': return '\\t';
        case '\x1a': return '\\Z';
        default: return '\\' + s;
      }
    });
  }
  return (prependN ? "N'" : "'") + val + "'";
}
```
- example usage
```shell
...
return this.QueryGenerator.quoteIdentifiers(identifiers, force);
};

/**
 * Escape a value (e.g. a string, number or date)
 */
QueryInterface.prototype.escape = function(value) {
return this.QueryGenerator.escape(value);
};

QueryInterface.prototype.setAutocommit = function(transaction, value, options) {
if (!transaction || !(transaction instanceof Transaction)) {
  throw new Error('Unable to set autocommit for a transaction without transaction object!');
}
if (transaction.parent) {
...
```

#### <a name="apidoc.element.sequelize.sql_string.escapeId"></a>[function <span class="apidocSignatureSpan">sequelize.sql_string.</span>escapeId (val, forbidQualified)](#apidoc.element.sequelize.sql_string.escapeId)
- description and source-code
```javascript
escapeId = function (val, forbidQualified) {
  if (forbidQualified) {
    return ''' + val.replace(/'/g, '''') + ''';
  }
  return ''' + val.replace(/'/g, '''').replace(/\./g, ''.'') + ''';
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sql_string.format"></a>[function <span class="apidocSignatureSpan">sequelize.sql_string.</span>format (sql, values, timeZone, dialect)](#apidoc.element.sequelize.sql_string.format)
- description and source-code
```javascript
format = function (sql, values, timeZone, dialect) {
  values = [].concat(values);

  return sql.replace(/\?/g, function(match) {
    if (!values.length) {
      return match;
    }

    return SqlString.escape(values.shift(), timeZone, dialect, true);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sql_string.formatNamedParameters"></a>[function <span class="apidocSignatureSpan">sequelize.sql_string.</span>formatNamedParameters (sql, values, timeZone, dialect)](#apidoc.element.sequelize.sql_string.formatNamedParameters)
- description and source-code
```javascript
formatNamedParameters = function (sql, values, timeZone, dialect) {
  return sql.replace(/\:+(?!\d)(\w+)/g, function(value, key) {
    if ('postgres' === dialect && '::' === value.slice(0, 2)) {
      return value;
    }

    if (values[key] !== undefined) {
      return SqlString.escape(values[key], timeZone, dialect, true);
    } else {
      throw new Error('Named parameter "' + value + '" has no value in the given object.');
    }
  });
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite"></a>[module sequelize.sqlite](#apidoc.module.sequelize.sqlite)

#### <a name="apidoc.element.sequelize.sqlite.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>BIGINT (length)](#apidoc.element.sequelize.sqlite.BIGINT)
- description and source-code
```javascript
BIGINT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BIGINT)) return new BIGINT(options);
  BaseTypes.BIGINT.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>CHAR ()](#apidoc.element.sequelize.sqlite.CHAR)
- description and source-code
```javascript
CHAR = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>DATE ()](#apidoc.element.sequelize.sqlite.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>ENUM ()](#apidoc.element.sequelize.sqlite.ENUM)
- description and source-code
```javascript
ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.sqlite.FLOAT)
- description and source-code
```javascript
FLOAT = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof FLOAT)) return new FLOAT(options);
  BaseTypes.FLOAT.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>INTEGER (length)](#apidoc.element.sequelize.sqlite.INTEGER)
- description and source-code
```javascript
INTEGER = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof INTEGER)) return new INTEGER(options);
  BaseTypes.INTEGER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.NUMBER"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>NUMBER ()](#apidoc.element.sequelize.sqlite.NUMBER)
- description and source-code
```javascript
NUMBER = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>REAL (length, decimals)](#apidoc.element.sequelize.sqlite.REAL)
- description and source-code
```javascript
REAL = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof REAL)) return new REAL(options);
  BaseTypes.REAL.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>STRING ()](#apidoc.element.sequelize.sqlite.STRING)
- description and source-code
```javascript
STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>TEXT ()](#apidoc.element.sequelize.sqlite.TEXT)
- description and source-code
```javascript
TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.BIGINT"></a>[module sequelize.sqlite.BIGINT](#apidoc.module.sequelize.sqlite.BIGINT)

#### <a name="apidoc.element.sequelize.sqlite.BIGINT.BIGINT"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>BIGINT (length)](#apidoc.element.sequelize.sqlite.BIGINT.BIGINT)
- description and source-code
```javascript
BIGINT = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof BIGINT)) return new BIGINT(options);
  BaseTypes.BIGINT.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.BIGINT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.BIGINT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.BIGINT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.BIGINT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.BIGINT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>super_ (options)](#apidoc.element.sequelize.sqlite.BIGINT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.BIGINT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.BIGINT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.BIGINT.prototype"></a>[module sequelize.sqlite.BIGINT.prototype](#apidoc.module.sequelize.sqlite.BIGINT.prototype)

#### <a name="apidoc.element.sequelize.sqlite.BIGINT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.BIGINT.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.BIGINT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return NUMBER.prototype.toSql.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.CHAR"></a>[module sequelize.sqlite.CHAR](#apidoc.module.sequelize.sqlite.CHAR)

#### <a name="apidoc.element.sequelize.sqlite.CHAR.CHAR"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>CHAR ()](#apidoc.element.sequelize.sqlite.CHAR.CHAR)
- description and source-code
```javascript
CHAR = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.CHAR.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.CHAR.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.CHAR.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.CHAR.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.CHAR.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>super_ (options)](#apidoc.element.sequelize.sqlite.CHAR.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.CHAR.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.CHAR.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.CHAR.prototype"></a>[module sequelize.sqlite.CHAR.prototype](#apidoc.module.sequelize.sqlite.CHAR.prototype)

#### <a name="apidoc.element.sequelize.sqlite.CHAR.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.CHAR.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.CHAR.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._binary) {
    return 'CHAR BINARY(' + this._length + ')';
  } else {
    return BaseTypes.CHAR.prototype.toSql.call(this);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.DATE"></a>[module sequelize.sqlite.DATE](#apidoc.module.sequelize.sqlite.DATE)

#### <a name="apidoc.element.sequelize.sqlite.DATE.DATE"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>DATE ()](#apidoc.element.sequelize.sqlite.DATE.DATE)
- description and source-code
```javascript
DATE = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.DATE.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.DATE.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.DATE.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.DATE.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.DATE.parse"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>parse (date, options)](#apidoc.element.sequelize.sqlite.DATE.parse)
- description and source-code
```javascript
parse = function (date, options) {
  if (date.indexOf('+') === -1) {
    // For backwards compat. Dates inserted by sequelize < 2.0dev12 will not have a timestamp set
    return new Date(date + options.timezone);
  } else {
    return new Date(date); // We already have a timezone stored in the string
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.DATE.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>super_ (options)](#apidoc.element.sequelize.sqlite.DATE.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.DATE.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.DATE.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.DATE.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.ENUM"></a>[module sequelize.sqlite.ENUM](#apidoc.module.sequelize.sqlite.ENUM)

#### <a name="apidoc.element.sequelize.sqlite.ENUM.ENUM"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>ENUM ()](#apidoc.element.sequelize.sqlite.ENUM.ENUM)
- description and source-code
```javascript
ENUM = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.ENUM.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.ENUM.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.ENUM.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.ENUM.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.ENUM.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>super_ (options)](#apidoc.element.sequelize.sqlite.ENUM.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.ENUM.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.ENUM.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.ENUM.prototype"></a>[module sequelize.sqlite.ENUM.prototype](#apidoc.module.sequelize.sqlite.ENUM.prototype)

#### <a name="apidoc.element.sequelize.sqlite.ENUM.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.ENUM.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.ENUM.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return 'TEXT';
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.FLOAT"></a>[module sequelize.sqlite.FLOAT](#apidoc.module.sequelize.sqlite.FLOAT)

#### <a name="apidoc.element.sequelize.sqlite.FLOAT.FLOAT"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>FLOAT (length, decimals)](#apidoc.element.sequelize.sqlite.FLOAT.FLOAT)
- description and source-code
```javascript
FLOAT = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof FLOAT)) return new FLOAT(options);
  BaseTypes.FLOAT.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.FLOAT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.FLOAT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.FLOAT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.FLOAT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.FLOAT.parse"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>parse (value)](#apidoc.element.sequelize.sqlite.FLOAT.parse)
- description and source-code
```javascript
parse = function (value) {
  if (_.isString(value)) {
    if (value === 'NaN') {
      return NaN;
    } else if (value === 'Infinity') {
      return Infinity;
    } else if (value === '-Infinity') {
      return -Infinity;
    }
  }
  return value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.FLOAT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>super_ (options)](#apidoc.element.sequelize.sqlite.FLOAT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.FLOAT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.FLOAT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.FLOAT.prototype"></a>[module sequelize.sqlite.FLOAT.prototype](#apidoc.module.sequelize.sqlite.FLOAT.prototype)

#### <a name="apidoc.element.sequelize.sqlite.FLOAT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.FLOAT.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.FLOAT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return NUMBER.prototype.toSql.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.INTEGER"></a>[module sequelize.sqlite.INTEGER](#apidoc.module.sequelize.sqlite.INTEGER)

#### <a name="apidoc.element.sequelize.sqlite.INTEGER.INTEGER"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>INTEGER (length)](#apidoc.element.sequelize.sqlite.INTEGER.INTEGER)
- description and source-code
```javascript
INTEGER = function (length) {
  var options = typeof length === 'object' && length || {
    length: length
  };
  if (!(this instanceof INTEGER)) return new INTEGER(options);
  BaseTypes.INTEGER.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.INTEGER.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.INTEGER.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.INTEGER.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.INTEGER.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.INTEGER.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>super_ (options)](#apidoc.element.sequelize.sqlite.INTEGER.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.INTEGER.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.INTEGER.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.INTEGER.prototype"></a>[module sequelize.sqlite.INTEGER.prototype](#apidoc.module.sequelize.sqlite.INTEGER.prototype)

#### <a name="apidoc.element.sequelize.sqlite.INTEGER.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.INTEGER.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.INTEGER.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return NUMBER.prototype.toSql.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.NUMBER"></a>[module sequelize.sqlite.NUMBER](#apidoc.module.sequelize.sqlite.NUMBER)

#### <a name="apidoc.element.sequelize.sqlite.NUMBER.NUMBER"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>NUMBER ()](#apidoc.element.sequelize.sqlite.NUMBER.NUMBER)
- description and source-code
```javascript
NUMBER = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.NUMBER.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.NUMBER.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.NUMBER.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.NUMBER.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.NUMBER.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>super_ (options)](#apidoc.element.sequelize.sqlite.NUMBER.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.NUMBER.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.NUMBER.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.NUMBER.prototype"></a>[module sequelize.sqlite.NUMBER.prototype](#apidoc.module.sequelize.sqlite.NUMBER.prototype)

#### <a name="apidoc.element.sequelize.sqlite.NUMBER.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.NUMBER.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.NUMBER.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  var result = this.key;

  if (this._unsigned) {
    result += ' UNSIGNED';
  }
  if (this._zerofill) {
    result += ' ZEROFILL';
  }

  if (this._length) {
    result += '(' + this._length;
    if (typeof this._decimals === 'number') {
      result += ',' + this._decimals;
    }
    result += ')';
  }
  return result;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.REAL"></a>[module sequelize.sqlite.REAL](#apidoc.module.sequelize.sqlite.REAL)

#### <a name="apidoc.element.sequelize.sqlite.REAL.REAL"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>REAL (length, decimals)](#apidoc.element.sequelize.sqlite.REAL.REAL)
- description and source-code
```javascript
REAL = function (length, decimals) {
  var options = typeof length === 'object' && length || {
    length: length,
    decimals: decimals
  };
  if (!(this instanceof REAL)) return new REAL(options);
  BaseTypes.REAL.call(this, options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.REAL.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.REAL.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.REAL.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.REAL.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.REAL.parse"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>parse (value)](#apidoc.element.sequelize.sqlite.REAL.parse)
- description and source-code
```javascript
parse = function (value) {
  if (_.isString(value)) {
    if (value === 'NaN') {
      return NaN;
    } else if (value === 'Infinity') {
      return Infinity;
    } else if (value === '-Infinity') {
      return -Infinity;
    }
  }
  return value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.REAL.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>super_ (options)](#apidoc.element.sequelize.sqlite.REAL.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.REAL.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.REAL.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.REAL.prototype"></a>[module sequelize.sqlite.REAL.prototype](#apidoc.module.sequelize.sqlite.REAL.prototype)

#### <a name="apidoc.element.sequelize.sqlite.REAL.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.REAL.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.REAL.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  return NUMBER.prototype.toSql.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.STRING"></a>[module sequelize.sqlite.STRING](#apidoc.module.sequelize.sqlite.STRING)

#### <a name="apidoc.element.sequelize.sqlite.STRING.STRING"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>STRING ()](#apidoc.element.sequelize.sqlite.STRING.STRING)
- description and source-code
```javascript
STRING = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.STRING.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.STRING.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.STRING.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.STRING.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.STRING.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>super_ (options)](#apidoc.element.sequelize.sqlite.STRING.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.STRING.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.STRING.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.STRING.prototype"></a>[module sequelize.sqlite.STRING.prototype](#apidoc.module.sequelize.sqlite.STRING.prototype)

#### <a name="apidoc.element.sequelize.sqlite.STRING.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.STRING.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.STRING.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._binary) {
    return 'VARCHAR BINARY(' + this._length + ')';
  } else {
    return BaseTypes.STRING.prototype.toSql.call(this);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.TEXT"></a>[module sequelize.sqlite.TEXT](#apidoc.module.sequelize.sqlite.TEXT)

#### <a name="apidoc.element.sequelize.sqlite.TEXT.TEXT"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.</span>TEXT ()](#apidoc.element.sequelize.sqlite.TEXT.TEXT)
- description and source-code
```javascript
TEXT = function () {
  if (!(this instanceof Constructor)) {
    var args = [null].concat(arguments);
    var FactoryFunction = Constructor.bind.apply(Constructor, args);
    return new FactoryFunction();
  }
  baseType.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.TEXT.extend"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>extend (oldType)](#apidoc.element.sequelize.sqlite.TEXT.extend)
- description and source-code
```javascript
extend = function (oldType) {
  return new DataType(oldType.options);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.TEXT.inherits"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>inherits (Constructor)](#apidoc.element.sequelize.sqlite.TEXT.inherits)
- description and source-code
```javascript
inherits = function (Constructor) {
  var baseType = this;

  if (!Constructor) {
    Constructor = function () {
      if (!(this instanceof Constructor)) {
        var args = [null].concat(arguments);
        var FactoryFunction = Constructor.bind.apply(Constructor, args);
        return new FactoryFunction();
      }
      baseType.apply(this, arguments);
    };
  }

  util.inherits(Constructor, baseType); // Instance (prototype) methods
  _.extend(Constructor, this); // Static methods

  return Constructor;
}
```
- example usage
```shell
...
 var tmp = Error.apply(this, arguments);
 tmp.name = this.name = 'SequelizeBaseError';

 this.message = tmp.message;
 if (Error.captureStackTrace)
   Error.captureStackTrace(this, this.constructor);
};
util.inherits(error.BaseError, Error);


/**
* Validation Error. Thrown when the sequelize validation has failed. The error contains an 'errors' property,
* which is an array with 1 or more ValidationErrorItems, one for each validation that failed.
*
* @param {string} message Error message
...
```

#### <a name="apidoc.element.sequelize.sqlite.TEXT.super_"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>super_ (options)](#apidoc.element.sequelize.sqlite.TEXT.super_)
- description and source-code
```javascript
super_ = function (options) {
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.sequelize.sqlite.TEXT.warn"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.</span>warn (link, text)](#apidoc.element.sequelize.sqlite.TEXT.warn)
- description and source-code
```javascript
warn = function (link, text) {
  if (!warnings[text]) {
    warnings[text] = true;
    console.warn('>> WARNING:', text, '\n>> Check:', link);
  }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.sequelize.sqlite.TEXT.prototype"></a>[module sequelize.sqlite.TEXT.prototype](#apidoc.module.sequelize.sqlite.TEXT.prototype)

#### <a name="apidoc.element.sequelize.sqlite.TEXT.prototype.toSql"></a>[function <span class="apidocSignatureSpan">sequelize.sqlite.TEXT.prototype.</span>toSql ()](#apidoc.element.sequelize.sqlite.TEXT.prototype.toSql)
- description and source-code
```javascript
toSql = function () {
  if (this._length) {
    warn('SQLite does not support TEXT with options. Plain 'TEXT' will be used instead.');
    this._length = undefined;
  }
  return 'TEXT';
}
```
- example usage
```shell
n/a
```



# misc
- this document was created with [utility2](https://github.com/kaizhu256/node-utility2)
